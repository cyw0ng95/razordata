## TBD

| ID | Subsystem | Requirement | Priority | Effort | Deps | Touches |
| --- | --- | --- | --- | --- | --- | --- |
| REQ000825 | EX/plan | **VIEW returns duplicate rows — GROUP BY / materialization produces extra rows.** SLT `evidence/slt_lang_createview.test` (5 failures) and `evidence/slt_lang_dropview.test` (2 failures). Example: `SELECT x FROM t1 WHERE x>0` returns 2 rows when SQLite expects 1 row. Same for views: `SELECT x FROM view1` returns 2 rows instead of 1. Root cause: view materialization or the underlying SELECT execution is producing duplicate rows. Could be: (1) GROUP BY not deduplicating correctly, (2) view materialization storing duplicate entries, (3) scan operator returning duplicate page entries. **Fix:** (1) Trace view creation to see how rows are stored. (2) Verify SELECT under view definition produces correct row count. (3) Check if materialized view uses proper deduplication. (4) Add test: `TestView_NoDuplicateRows` verifying view SELECT returns exact row count. **Test evidence:** `createview.test` L29, L34; `dropview.test` similar patterns. | high | medium | — | EX/view.go, EX/aggregate.go, MV/store.go |
| REQ000826 | EX/eval | **REPLACE command does not implement proper upsert semantics.** SLT `evidence/slt_lang_replace.test` reports 3 failures. REPLACE should delete conflicting row and insert new row (or update if ON CONFLICT clause). Current behavior: (1) Row count mismatch — `SELECT x, y FROM t1 WHERE x=2` returns 1 row when 0 expected after REPLACE. (2) Value mismatch — row shows `[2 | replace]` when `[2 | insert]` expected. (3) Row count after REPLACE is wrong (got 1, want 2; got 1, want 3; got 1, want 2). Root cause: REPLACE appears to be doing INSERT without proper conflict detection/deletion, or the delete phase is not executing. **Fix:** (1) Implement REPLACE as: (a) detect UNIQUE/PRIMARY KEY conflict, (b) DELETE conflicting row, (c) INSERT new row. (2) Or implement as INSERT ... ON CONFLICT REPLACE per SQLite spec. (3) Verify row counts after each REPLACE operation. (4) Add test: `TestReplace_UpsertSemantics` with UNIQUE constraint conflict scenarios. **Test evidence:** `slt_lang_replace.test` L24, L31, L42, L62, L71. | critical | large | — | EX/writers.go, EX/replace.go, MV/store.go |
| REQ000827 | TXN/trigger | **TRIGGER lifecycle operations have failures.** SLT `evidence/slt_lang_createtrigger.test` (1 fail), `evidence/slt_lang_droptrigger.test` (3 fails). Trigger creation, dropping, and firing semantics appear incorrect. **Fix:** (1) Audit trigger creation path — verify trigger is registered correctly. (2) Audit trigger firing — verify BEFORE/AFTER triggers fire at correct points. (3) Audit trigger dropping — verify trigger is removed and no longer fires. (4) Add comprehensive trigger tests covering CREATE, DROP, BEFORE INSERT, AFTER UPDATE, etc. **Test evidence:** `createtrigger.test` 1 fail; `droptrigger.test` 3 fails. | high | medium | — | TXN/trigger.go, EX/eval.go |
| REQ000828 | MV/meta | **DROP TABLE has failures.** SLT `evidence/slt_lang_droptable.test` reports 2 failures. Table drop operations are not completing correctly — either the table isn't being removed, metadata isn't updated, or dependent objects aren't handled. **Fix:** (1) Trace DROP TABLE execution path. (2) Verify table data directory is removed. (3) Verify catalog entry is deleted. (4) Verify indexes/triggers on table are also dropped. (5) Add test: `TestDropTable_Complete` verifying full cleanup. **Test evidence:** `slt_lang_droptable.test` 2 fails. | high | small | — | MV/meta.go, TXN/catalog.go |
| REQ000829 | EX/index | **REINDEX operation has failures.** SLT `evidence/slt_lang_reindex.test` reports 1 failure. REINDEX should rebuild index structures but appears to have issues. **Fix:** (1) Trace REINDEX execution. (2) Verify index is rebuilt correctly. (3) Verify query results after REINDEX match expected. (4) Add test: `TestReindex_Correctness` with various index types. **Test evidence:** `slt_lang_reindex.test` 1 fail. | medium | small | — | EX/index.go, MV/index.go |
| REQ000832 | EX/eval | **COALESCE with nested CASE returns NULL instead of computed value.** SLT `random/expr/slt_good_0.test`: `SELECT 74 * - COALESCE(+CASE-CASE WHEN NOT(NOT-79>=NULL) THEN 48 END WHEN+COUNT(*) THEN 6 END, MIN(ALL+-30)*45*77) * -14 - CASE` returns `[NULL]` but should return `[-107692200]`. The CASE expression inside COALESCE evaluates to NULL incorrectly. Root cause: (1) CASE WHEN with NULL comparison `NOT(NOT-79>=NULL)` — comparing with NULL produces NULL, and the nested NOT logic may be wrong. (2) COALESCE may not be evaluating its arguments correctly. **Fix:** (1) Audit CASE expression evaluation — ensure WHEN conditions are evaluated correctly with NULL semantics. (2) Verify COALESCE returns first non-NULL argument. (3) Fix NULL comparison in CASE conditions. **Test evidence:** `slt_good_0.test` L86, L92. | high | medium | — | EX/eval.go, EX/case.go |
| REQ000834 | PS/parser | **Correlated subquery with CROSS JOIN syntax error.** SLT `random/aggregates/slt_good_0.test`: `SELECT MIN(ALL - + 32) AS col0, + COUNT(*) * + COUNT(*) FROM (tab0 AS cor0 CROSS JOIN tab0 cor1)` fails with `ps: syntax error at line 1 col 74: expected SELECT, got identifier ("tab0")`. The parser fails to handle correlated table references in subquery FROM clauses where the same table appears twice with different aliases. Root cause: parser's `QualifiedName` or `TableExpr` handling doesn't correctly parse `tab0 cor1` after `tab0 AS cor0` — the second table name is being misinterpreted. **Fix:** (1) Audit parser's FROM clause parsing for multiple table references. (2) Verify correlated subquery alias resolution. (3) Add test: `TestParser_CorrelatedCrossJoin` with various correlated subquery patterns. **Test evidence:** `slt_good_0.test` L74, L81. | high | small | — | PS/ps.go |
| REQ000835 | EX/agg | **MIN/MAX over NULL-only input returns 0 instead of NULL.** SLT `random/aggregates/slt_good_0.test`: `MIN(-+32)` over a cross join producing only NULL values returns `[0 | 3]` but should return `[NULL | 3]`. MIN/MAX should return NULL when all input values are NULL (or input set is empty). Root cause: MIN/MAX accumulator initializes to 0 instead of NULL, or the NULL check in accumulation is wrong. **Fix:** (1) In `Aggregate.Next()`, initialize MIN/MAX accumulators to NULL (not 0). (2) On first non-NULL value, set accumulator to that value. (3) If all values are NULL, return NULL. (4) Add test: `TestMinMax_NullOnly` verifying MIN/MAX returns NULL for null-only inputs. **Test evidence:** `slt_good_0.test` L446, L453. | high | small | — | EX/aggregate.go |
| REQ000836 | EX/agg | **Aggregates return NULL instead of computed values.** SLT `random/aggregates/slt_good_1.test`: various aggregate queries return `[NULL]` when they should return computed values (e.g., `got [NULL], want [-91]`, `got [NULL], want [51]`). COUNT(*) fix (REQ000830, committed 8d19592) resolved the no-FROM case but some FROM-based aggregate queries may still fail due to other aggregate initialization issues. **Fix:** (1) Audit all aggregate implementations (SUM, AVG, MIN, MAX, COUNT, GROUP_CONCAT). (2) Verify each aggregate's initialization and finalization logic. (3) Add test: `TestAggregates_Basic` with simple aggregate queries. **Test evidence:** `slt_good_1.test` L64, L70, L107. | high | medium | — | EX/aggregate.go |
| REQ000837 | EX/plan | **Planner panic in n3JoinOrdering — heap[0] index out of range.** SLT `random/groupby/slt_good_0.test` and `random/select/slt_good_0.test` both panic with `panic: runtime error: index out of range [0]` at `planner.go:2575: best := heap[0]`. The `n3JoinOrdering` function's join ordering algorithm produces an empty `heap` slice when no valid join order can be found (e.g., complex multi-table joins with certain predicate patterns). The function returns `heap[0]` without checking if `heap` is empty. **Root cause:** The N3 join ordering heuristic (a k-best join ordering algorithm) can exhaust all candidates when: (1) no cross-table predicates exist to guide join order, (2) all tables are independent (cross product), (3) the `n3HeapMaxSize` limit causes all candidates to be discarded. When `heap` becomes empty after the conversion loop (lines 2559-2572), accessing `heap[0]` panics. **Fix:** (1) Add empty check before `heap[0]`: if `len(heap) == 0`, return a default join order (e.g., original table order from FROM clause). (2) Or ensure the algorithm always produces at least one candidate by falling back to a simple left-deep join order. (3) Add test: `TestPlanner_EmptyJoinOrder` with cross-product queries. **Test evidence:** `random/groupby/slt_good_0.test` panic; `random/select/slt_good_0.test` panic at `planner.go:2575`. | **critical** | small | — | EX/planner.go |
| REQ000749 | EX/eval | **JSON_INSERT, JSON_REPLACE, JSON_REMOVE functions not supported.** `SELECT JSON_INSERT('{"a":1}', '$.b', 2)` returns `ex: eval error`. SQLite 3.38+ supports `JSON_INSERT`, `JSON_REPLACE`, and `JSON_REMOVE` for modifying JSON values. The `json.go` file implements `JSON_EXTRACT`, `JSON_TYPE`, `JSON_VALID`, `JSON_ARRAY`, `JSON_OBJECT`, `JSON_SET` but not the INSERT/REPLACE/REMOVE variants. Fix: add `JSON_INSERT`, `JSON_REPLACE`, and `JSON_REMOVE` to the JSON function dispatch in `json.go`. | low | medium | — | EX/json.go |
| REQ000750 | PS/parser | **Three-part qualified names (`database.table.column`) not supported.** `SELECT main.t.id FROM main.t` fails with a syntax error. SQLite supports three-part qualified names for cross-database references. Razordata only supports two-part names (`table.column`). The parser's `QualifiedName` handling does not chain a third component. Fix: extend the parser to recognize `ident.ident.ident` as a three-part qualified name. Since multi-database is not supported at runtime (ATTACH is rejected), this is low priority. | low | small | — | PS/ps.go |
| REQ000776 | EX/eval | **Replace `any` (interface{}) with tagged-union `Value` type to eliminate boxing overhead.** CPU profile: `convT64` (Go's runtime function for boxing int64 into interface{}) accounts for 10.32% of CPU. `mallocgc` from all boxing operations totals 27.78%. Every `int64` value returned from `Eval`, stored in `Row.Data`, passed through `numericArith`, `compare`, `equalValue`, or returned from `SeqScan.nextFromStore` gets boxed into a 16-byte interface header (type pointer + value pointer). For a 100K-row scan with 5 columns, this is 500K boxing operations. The `any` type pervades the entire executor: `Row.Data []any`, `Eval() (any, error)`, `numericArith(a, b any, ...)`, `compare(a, b any)`, `equalValue(a, b any)`, all aggregate accumulators, all function dispatch. **Root cause:** Go's `any` (empty interface) requires a heap allocation for non-pointer types (int64, float64, bool). The runtime's `convT64` allocates an 8-byte box on the heap, then creates a 16-byte interface header pointing to it. This happens on every value transition: decode→Row.Data, Row.Data→Eval, Eval→arithmetic, arithmetic→comparison, comparison→result. **Fix:** Define a `Value` tagged-union struct that stores all SQL types inline without heap allocation. **Implementation:** (1) Define the type in `ex.go`: `type Value struct { Kind uint8; I64 int64; F64 float64; S string; B []byte }` with kind constants `KindNull=0, KindInt=1, KindFloat=2, KindText=3, KindBlob=4, KindBool=5`. (2) Change `Row.Data` from `[]any` to `[]Value`. (3) Change `Eval` signature from `(any, error)` to `(Value, error)`. (4) Update all 22 expression eval functions (`evalUnary`, `evalBinary`, `evalBetween`, `evalIn`, `evalCase`, `evalAggregate`, `evalFunction`, etc.) to use `Value` instead of `any`. (5) Update `numericArith(a, b Value, op rune)` to switch on `a.Kind` directly — no interface dispatch. (6) Update `compare(a, b Value) int` similarly. (7) Update `equalValue(a, b Value) bool`. (8) Update `decod... (line truncated to 2000 chars) | high | large | — | EX/ex.go, EX/eval.go, EX/operators.go |
| REQ000810 | EX/eval | **GROUP BY with column reordering produces wrong results.** `SELECT * FROM tab0 GROUP BY col1, col2, col0` returns different hash than `GROUP BY col0, col1, col2` when the column order differs. The group key serialization should be order-dependent (different column order = different grouping), but the test expects both to produce the same hash (8ad40ad40eb1e471572580fddc8d2aaf). This suggests either: (1) the group key serialization is order-independent when it should be order-dependent, or (2) the test expects order-independent behavior (like SQLite's GROUP BY with SELECT * non-aggregated columns). Root cause: when SELECT * is used with GROUP BY, non-aggregated columns not in GROUP BY produce indeterminate values — SQLite picks the value from the row that GROUP BY placed first. Razordata may be picking different rows. Affects random/groupby SLT failures. **Fix:** Ensure deterministic row selection for non-aggregated columns in GROUP BY, matching SQLite behavior (value from the row that defined the group). **Implementation:** (1) In `Aggregate.materialize`, when building group buckets, store the first row encountered for each group. (2) For non-aggregated columns in SELECT *, return values from the stored first row. (3) Verify the first row is deterministic (order of input rows). (4) Add test: `TestGroupBy_DeterministicSelectStar` verifying GROUP BY with SELECT * returns consistent results. | high | medium | — | EX/aggregate.go |
| REQ000812 | EX/scalar | **Razor returns 0 for `CHANGES()` and `TOTAL_CHANGES()`; oracle returns the correct change count.** Dual test `total_changes_function` returns 0 from razor (expected 3 = 1 INSERT + 1 UPDATE + 1 DELETE). The change counter is either not wired in the in-memory path or wired with wrong scope (per-statement vs per-session). **Fix:** Wire `CHANGES()`/`TOTAL_CHANGES()` to track DML operations per session. **Implementation:** (1) Add `lastChanges`, `totalChanges` fields to executor, scoped per-session. (2) Increment in INSERT (1 per row), UPDATE (rows matched), DELETE (rows deleted). (3) Implement `evalChanges`/`evalTotalChanges` in `eval.go` to read those counters. (4) Add tests for CHANGES() after each DML type. (5) Verify against oracle. | medium | medium | — | EX/ex.go, EX/eval.go, EX/writers.go |
| REQ000813 | EX/cte | **CTE queries return empty when razor engine returns fewer rows than oracle.** Dual tests `cte_basic`, `cte_with_aggregate`, `multiple_ctes` all return `razor=[]` while oracle returns `[[10] [20] [30]]`. The CTE execution path is either short-circuiting on first iteration or the Materialized CTE planner bug drops the SELECT result. **Fix:** Trace CTE evaluation, ensure Materialize operator emits all rows from the CTE definition. **Implementation:** (1) Add `slog` debug log in `CTE.Next()` and `Materialize.Next()` to verify row flow. (2) Verify `cte_definitions` and `cteRows` are populated before returning. (3) Add test: `TestCTE_Basic` verifying `WITH cte AS (SELECT 10 UNION ALL SELECT 20 UNION ALL SELECT 30) SELECT * FROM cte` returns 3 rows. (4) Add test: `TestCTE_Aggregate` with `WITH cte AS (...) SELECT COUNT(*) FROM cte`. | high | medium | — | EX/cte.go, EX/plan.go, EX/intermediate.go |
| REQ000816 | EX/eval | **`Row.Lookup` triggers `buildColIndex` on every miss — dominant j3_mixed hotspot.** pprof of `BenchmarkJ3_Mixed` (100×100×100 cross join, 932ms/800MB/3M allocs) shows `Row.Lookup` cum=23.45% with 82% of its time in `buildColIndex` (1.06s). `buildColIndex` allocates a fresh `map[string]int` and runs `strings.ToLower` over every column name on first Lookup miss — repeated for every projected row in 3-table join (3M rows × 2-3 cols/expr × 2-3 expr/SELECT = 18M Lookup calls). Also allocates 29.92% of total memory (0.90GB of 3.02GB). REQ000770 partially addressed this by pre-lowercasing at parse time but the `Lookup` fallback (`strings.ToLower` on every call) and `buildColIndex` re-allocation remain. **Fix:** (1) Make `Lookup` skip `strings.ToLower` when caller-provided name is already lowercase (fast path). (2) Make `buildColIndex` use a per-Row lazy map that is built **once** per Row instance (already lazy via `colIndex == nil` guard — verify it's actually being checked on all paths). (3) Cache `buildColIndex` output on the source Row when child rows shadow it via Outer pointer — share the parent map. (4) Benchmark: `BenchmarkLookup_HitMiss_Ratio` showing lookup scaling. (5) Verify: post-fix j3_mixed < 600ms (was 932ms) and < 600MB (was 800MB). | high | small | REQ000770 | EX/ex.go, EX/operators.go |
| REQ000817 | EX/eval | **Scalar IN-list linear scan for each row — O(N*M) per evaluation.** select4 profile shows 8-table join queries with `a1 IN (477,821,380,294,879,109,330)` (7-100 values). The scalar `evalIn` path iterates over each list item for every row: `for _, item := range e.List { Eval(item, row) }`. For a 6-table cross product (100⁶ = 1e12 logical rows) with a 7-element IN list, this is 7e12 Eval calls just for the IN predicate. The vectorized path (`eval_vec.go`) already uses hash-set for strings and binary-search for int64, but the scalar fallback does not. **Fix:** (1) In `evalIn` (`eval.go:460`), when `len(e.List) >= 4`, pre-build a `map[Value]struct{}` hash set on first call and probe O(1) per row instead of O(N). Cache by pointer identity of the list. (2) For int64-only lists, use a `map[int64]struct{}` to avoid Value boxing. (3) For single-column IN lists on SeqScan, push the IN filter into SeqScan as a bloom/probe filter to skip non-matching rows before join. Expected: 5-10x speedup on 6-8 table select4 queries with IN predicates. | high | medium | — | EX/eval.go, EX/operators.go |
| REQ000818 | EX/planner | **HashCrossJoin silently returns 0 rows when either side exceeds 1024 rows at runtime.** `HashCrossJoin.build()` (`hashcrossjoin.go:233`) and `materializeLeft()` (`hashcrossjoin.go:131`) return `ErrNoRows` when a side exceeds 1024 rows, which propagates as "query returned 0 rows" to the caller. Unlike `NLJ.tryHashCrossJoin()` which gracefully falls back to NLJ, HashCrossJoin's hard limit produces wrong results silently. select4 queries on larger-than-100-row tables would be affected. **Fix:** (1) Add a `fallback Operator` field to `HashCrossJoin`. (2) When the 1024 limit is exceeded, drain remaining rows from both sides through the fallback operator and switch to it. (3) Alternatively, have the planner not select HashCrossJoin when estimated row count exceeds 1024 on either side. (4) Use `BlockNestedLoopJoin` (block NLJ) as the fallback since it handles arbitrary sizes. Expected: correctness guarantee for all table sizes. | critical | medium | — | EX/hashcrossjoin.go, EX/planner.go |
| REQ000822 | EX/eval | **Filter still evaluates compiled predicate row-by-row under ADQC.** Even with `compiledFilterFn` (ADQC), the Filter calls the function once per input row. For cross products with selective predicates (e.g., 8-table join where only 0.001% of rows pass), the Filter is called 100⁸ times. The `Filter.Next()` loop structure (`for { r, err := f.child.Next(ctx); if !f.compiledFilterFn(&r) { continue }; return r }`) means all join overhead is paid before filtering. **Fix:** (1) Implement batch-mode Filter: read up to 1024 rows from child, apply compiled predicate as a vectorized SIMD-style loop, return matching rows one at a time. (2) The batch buffer reduces function call overhead and improves cache locality. (3) For cross-table predicates that reference both sides, push the filter INTO the join operator (filter-join fusion): in `NestedLoopJoin.nextBlock`, apply the ON predicate during row emission instead of filtering afterward. Expected: 1.5-3x speedup on selective multi-table joins. | low | large | REQ000802 | EX/intermediate.go, EX/join.go |

