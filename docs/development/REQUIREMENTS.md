## TBD

| ID | Subsystem | Requirement | Priority | Effort | Deps | Touches |
| --- | --- | --- | --- | --- | --- | --- |
| REQ000547 | TXN/MV | Per-NUMA arena pools — extend the generational arena (REQ000064/305) with per-NUMA-node arena pools; thread first-touch policy (REQ000309) places new arenas on the calling node; reclaim drains per-node; eliminates contention on global `reclaimMu` and `arenaPool` under high transaction throughput on multi-socket hosts | medium | M | REQ000064 (generational arena), REQ000305 (epoch reclaim), REQ000309 (NUMA) | `TXN/MV/arena.go` `numaArenaPool`; `TXN/LC/epoch.go` per-node drain lists; `ENG/NM/numa.go` allocator policy |
| REQ000571 | ENG/LS | SST page cache — replace `os.ReadFile` with block-level cache + sync.Pool. Currently `readFromSST` (`engine.go:193`) and `mergeIterator.init` (`api.go:246`) call `os.ReadFile` for every SST file on every read/iterate. For a 100 MB SST this allocates 100 MB per read. New `internal/ENG/LS/page_cache.go`: fixed-size cache (default 256 MB), 4 KB blocks keyed by `(fileID, blockOffset)`, clock-sweep eviction, `sync.Pool` for page buffers. `mergeIterator` consults cache before file read. Must handle concurrent access (read path is hot). Benchmarks: measure allocs/op and ns/op for sequential and random reads before/after | critical | L | iter-04 (L0 SST), iter-02 (buffer pool) | `ENG/LS/page_cache.go` (new); `ENG/LS/engine.go` `readFromSST` — replace `os.ReadFile` with cache lookup; `ENG/LS/api.go` `mergeIterator.init` — same; `ENG/LS/compaction.go` — compaction reads can bypass cache (bulk reads) |
| REQ000583 | SQL/PS | Add visitor pattern to AST — currently AST traversal relies on manual type-switch in `PL/memo.go:107-223` with no exhaustiveness check. Missing node types (e.g. `IntervalLiteral`, `WindowFunc`, `RaiseFunc`) silently produce different memo hashes. Fix: add `Accept(Visitor)` to `Expr` and `Stmt` interfaces; create `BaseVisitor` with default no-op implementations; implement `HashVisitor` that writes to `hash.Hash` for memo keys. Refactor `memo.go` to use the visitor | medium | L | iter-07 (parser) | `SQL/PS/ast.go` — add `Accept` methods; new `SQL/PS/visitor.go`; `SQL/PL/memo.go` — refactor to use visitor |
| REQ000586 | SQL/EX | Thread ExecContext through operators (eliminate globals) — `eval.go:383` `currentSubqueryPlanner`, `ex.go:32` `currentSessionID`, `store.go:98-112` `tables`/`storeSchemas`/`viewRegistry` are all package-level globals. This makes concurrent Executor instances unsafe. Fix: add `ExecContext` struct; thread it through operators via `Operator.SetContext(ctx)` or pass in `Next(ctx)`. Start with `currentSubqueryPlanner` | medium | L | iter-08 (SQL execute) | `SQL/EX/ex.go` — new `ExecContext` struct; `SQL/EX/eval.go` — remove `currentSubqueryPlanner` global; all operators — accept context |
| REQ000588 | TXN/MV | **TOCTOU race in `arena.promote()`** — `promote()` at `arena.go:84-110` takes a snapshot of `a.youngOff` at line 85, then another goroutine's `tryAllocYoung()` may CAS-succeed after line 103 but before `youngOff.Store(0)` at line 105, losing concurrent allocations. Fix: replace `youngOff.Store(0)` with CAS loop that drains all concurrent allocations before resetting | critical | M | iter-27 (MPMC epoch reclaim), iter-23 (generational arena) | `TXN/MV/arena.go` `promote()` method |
| REQ000608 | ENG/LS | **`skiplist.Find` and `Iterator.Value` acquire `mu.Lock()` on `atomic.Value` reads** — remove unnecessary mutex lock/unlock around `atomic.Value.Load()` calls in skiplist | medium | S | iter-04 (skiplist) | `ENG/LS/skiplist.go` `Find()` lines 176-179, `Iterator.Value()` lines 213-219 |
| REQ000609 | ENG/LS | **`randomLevel` uses global `math/rand` with implicit mutex** — use `rand.New(rand.NewPCG(...))` for per-skiplist random source | medium | S | iter-04 (skiplist) | `ENG/LS/skiplist.go` `randomLevel()` lines 58-63 |
| REQ000610 | ENG/LS | **`compaction.fileName` produces non-digit chars for Level > 9** — use `strconv.Itoa` instead of `string(rune('0' + Level))` | medium | S | iter-04 (compaction) | `ENG/LS/compaction.go` `fileName()` lines 224-227 |
| REQ000611 | ENG/LS | **`table.DropTable` ignores index deletion errors** — return or log the error from `index.Delete()` | medium | S | iter-12 (catalog) | `ENG/LS/table.go` `DropTable()` lines 77-90 |
| REQ000612 | ENG/LS | **`flush.go` uses `fmt.Fprintf(os.Stderr)` instead of `slog`** — replace with `slog.Warn` | low | S | iter-04 (flush) | `ENG/LS/flush.go` line 160-162 |
| REQ000613 | ENG/LS | **`memtable.Insert` returns `error` but always returns nil** — remove the `error` return value | low | S | iter-04 (memtable) | `ENG/LS/memtable.go` `Insert()` method + all callers |
| REQ000614 | ENG/LS | **`columnar.go` unused variables `keysLen` and `valsLen`** — use for validation or remove | low | S | iter-15 (columnar) | `ENG/LS/columnar.go` lines 74, 118 |
| REQ000615 | TXN/MV | **Custom `maxUint64` duplicates `math.MaxUint64`** — use `math.MaxUint64` | low | S | iter-23 (MVCC) | `TXN/MV/version.go` line 8 |
| REQ000616 | ENG/LS | **`sst_writer.Finish` allocates byte arrays in loop body** — hoist `offsetBuf`/`sizeBuf` before the loop | low | S | iter-03 (SST) | `ENG/LS/sst_writer.go` `Finish()` lines 200-205 |
| REQ000620 | ENG/LS | **`u64toa(0)` returns empty string** — add `if n == 0` guard | high | S | iter-04 (compaction) | `ENG/LS/compaction.go` `u64toa` lines 240-249 |
| REQ000617 | ENG/SC | Write `sc_test.go`: round-trip encode/decode for all 8 type pairs, ValidateRow, ValidateConstraints, CompareColumnDef, property-based fuzz | critical | L | iter-06 (schema) | new `ENG/SC/sc_test.go` |
| REQ000618 | ENG/DP | Write `dp_test.go`: encode/decode round-trip, empty rows, corrupt data, concurrent safety | critical | M | iter-03 (SST) | new `ENG/DP/dp_test.go` |
| REQ000619 | MEM/BF | Write `pmem_test.go`: create/read/write/sync/close round-trip, large writes, property-based | high | M | iter-02 (buffer pool) | `MEM/BF/pmem_test.go` |
| REQ000621 | ENG/LS | **memtable `encodeVarint` buffer overflow** — add bounds check; write `varint_test.go` | high | S | iter-04 (memtable) | `ENG/LS/memtable.go` + new `varint_test.go` |
| REQ000622 | ENG/LS | Write `fnv_test.go`: known FNV-1a hash, deterministic output, property-based | high | S | iter-03 (SST) | new `ENG/LS/fnv_test.go` |
| REQ000623 | ENG/LS | Write `table_test.go`: CreateTable/DropTable/GetTable round-trip, encodeTableID | medium | M | iter-12 (catalog) | new `ENG/LS/table_test.go` |
| REQ000624 | ENG/LS | Extend `compaction_test.go`: keyRangeOverlap, fileName, newCompactionManager, Stop, MaybeCompact | medium | M | iter-04 (compaction) | extend `ENG/LS/compaction_test.go` |
| REQ000625 | ENG/LS | Extend `flush_compaction_test.go`: flushToSST error, updateManifest I/O failure, requestFlush retry | medium | M | iter-04 (flush) | extend `ENG/LS/flush_compaction_test.go` |
| REQ000626 | ENG/LS | Extend `api_test.go`: mergeIterator edge cases, isTombstone, iterHeap | medium | M | iter-04 (api) | extend `ENG/LS/api_test.go` |
| REQ000627 | ENG/LS | Extend `catalog_test.go`: flushLocked failure, GetIndex/PutIndex/DeleteIndex error paths, nil receiver | medium | M | iter-12 (catalog) | extend `ENG/LS/catalog_test.go` |
| REQ000628 | ENG/LS | Extend `sst_reader_test.go`/`sst_writer_test.go`: readBlock fallback, compress/decompress error paths | medium | M | iter-03 (SST) | extend existing SST test files |
| REQ000629 | ENG/LS | Extend `engine_test.go`: Sync lastErr, Close aggregation, readFromSST corrupt files | medium | M | iter-04 (L0 engine) | extend `ENG/LS/engine_test.go` |
| REQ000630 | ENG/LS | Extend `index_test.go`: CAS retry, empty iterator, nil Seek | medium | S | iter-04 (index) | extend `ENG/LS/index_test.go` |
| REQ000631 | ENG/LS | Extend `subcompact_test.go`: empty inputs, cancelled context, pivotKeys edges | medium | S | iter-04 (compaction) | extend `ENG/LS/subcompact_test.go` |
| REQ000632 | TXN/LC | Test `madviseHugePage` on Linux path | low | S | iter-02 (buffer pool) | extend `MEM/BF/bf_test.go` |
| REQ000633 | TXN/VL | Test `ForceAbortAll` and `StopGCWithCtx` | medium | S | iter-17 (WAL protocol) | extend `TXN/VL/coverage_test.go` |
| REQ000634 | SQL/LX | Test `Peek2` and `ParseIntLiteral` | medium | S | iter-07 (parser) | extend `SQL/LX/lx_test.go` |
| REQ000635 | SQL/PS | Test parser helper functions: isAggregateName, parseFloat, parseCoalesce, etc. | medium | M | iter-07 (parser) | extend `SQL/PS/ps_test.go` |
| REQ000636 | SQL/PL | Test Memo and LearnedModel functions | medium | M | iter-07 (planner) | extend `SQL/PL/pl_test.go` or `memo_test.go` |
| REQ000637 | SQL/RE | Test rewrite functions: rewriteInsert, constantFoldBinary, cloneExprSlice, SplitOr | medium | M | iter-07 (rewrite) | extend `SQL/RE/rewrite_test.go` |
| REQ000638 | SQL/EX | Test ParamType extraction and operator edge cases | medium | M | iter-08 (SQL execute) | extend `SQL/EX/exec_test.go` or `operators_test.go` |
| REQ000639 | SYS/SE | Test savepoint, changes count, snapshot functions | medium | M | iter-20 (session) | extend `SYS/SE/session_test.go` |
| REQ000640 | SYS/SY | Test `InstallSignalHandler`, `SetSnapshot`, `CurrentTS` | low | S | iter-20 (SYS) | extend `SYS/SY/init_test.go` |
