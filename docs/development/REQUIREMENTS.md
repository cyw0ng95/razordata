## TBD
| ID | Subsystem | Requirement | Priority | Effort | Deps | Touches |
| --- | --- | --- | --- | --- | --- | --- |
| REQ001271 | DBG/DC | **SQL PRAGMAs for debug tracing orphaned — HandleDebugPragma defined in pragma_debug.go but never wired into the PRAGMA dispatch path. Join tracing only works via socket commands.** The code for debug_join_tracing, debug_join_flush, debug_join_filter, debug_join_summary PRAGMAs exists in UT/pragma_debug.go (HandleDebugPragma) but is never called from the PRAGMA dispatch path in WT.Pragma.Next(). This means all the debug infrastructure (JoinTracer, BufferedTracer, 5 event types, 3 verbosity levels) is inaccessible via SQL — developers can only use socket commands (nc -U <dbdir>/.debug/debug.sock). Missing //go:build !debug stub for HandleDebugPragma also prevents compilation without debug tag. **Fix:** (1) Wire HandleDebugPragma into the PRAGMA dispatch path in WT.Pragma.Next() — route recognized debug PRAGMAs to DC.Control.EnableTrace and the BufferedTracer. (2) Add //go:build !debug stub file pragma_nodebug.go returning ErrRequiresDebugBuild. (3) Add TestDebugPragma_JoinTracing_OnOff and TestDebugPragma_JoinTracing_Flush verifying PRAGMAs work end-to-end. (4) Update docs/development/DEBUG.md SQL PRAGMA examples. | high | small | none | SQB/UT/pragma_debug.go, SQB/UT/pragma_nodebug.go (new), SQB/WT/pragma.go |
| REQ001249 | SQB/EX | **Covering-index scan — skip heap row fetch when all projected columns are in index.** When a query's projection is a subset of the index columns (plus primary key), the index alone provides all data — no heap row fetch is needed. SQLite documents this: "can make many queries run twice as fast." Razordata has `IndexOnlyScan` but it is not auto-emitted by the planner. **Fix:** (1) In planner, detect `IndexScan` with `Project` child where projected column set ⊆ index column set ∪ {PK}. (2) Replace with `IndexOnlyScan` that reads index entries and extracts columns directly, skipping `Store.Get(rowKey)`. (3) Support multi-column indexes with prefix coverage (leftmost N index columns cover projection). (4) Add `TestCoveringIndex_Basic` and `TestCoveringIndex_MultiCol` verifying index-only path. (5) Add `BenchmarkSelect1_CoveringIndex` on queries where index covers projection. Estimate: ~100 lines, 3 files, 0.75 day. | high | small | none | SQB/EX/planner.go, SQB/OP/index_only_scan.go, SQB/OP/operators.go |
| REQ001287 | SQB/OP | **DEFERRED — pruneRowCols allocates 10.3% of total heap — buffer reuse causes aliasing.** Naive buffer reuse (Cols/Types/Data/ColIndex) corrupts Rows held by downstream operators that don't clone data. Requires deeper analysis of operator data-flow lifetime before safe reuse is possible. | high | small | none | SQB/OP/operators.go |
| REQ001292 | SQB/EV | **Correlated scalar subquery re-evaluated per row — cache or pre-bind correlated bounds.** EXPLAIN shows `PROJECT ? [interpreted]` for `(SELECT count(*) FROM t1 AS x WHERE x.c>t1.c AND x.d<t1.d)` — the subquery plan is opaque and executed via `evalScalarSubquery` per row. For 30 rows, this runs 30 full subquery executions against the same table. The `correlatedLRU` cache exists but uses string key serialization (`strings.Join`), adding allocation overhead per cache lookup. **Fix:** (1) Pre-compute correlated column indices at plan time (avoid per-row string key construction). (2) Use `bytes.Buffer` pool for cache key serialization instead of `strings.Join`. (3) Add `BenchmarkEvalScalarSubquery_CorrelatedCache` measuring cache hit vs miss for 1K/10K row workloads. (4) Verify select1/select2/select4 pass. Estimate: ~40 lines, 1 file, 0.25 day. | high | small | none | SQB/EV/eval.go |
| REQ001391 | SQB/EX + tests/sqlcmp | **SLT corpus gap: `wal_mode.test` does not exist.** REQ001297 item (6) ("Verify SLT `wal.test` passes after impl") and REQ001299's SLT concurrency checks reference files that **do not exist** in this repo's SLT corpus. `tests/sqlcmp/corpus` is the `jzombie/sqlite-sqllogictest-corpus` submodule (HEAD `515601a Change output_dir`); `git ls-tree -r HEAD --name-only` on its `test/` tree returns only `select1..select5.test` plus `evidence/`, `index/`, `random/` subdirs. No `wal.test`, `wal3.test`, `wal4.test`, or any `wal_*` file exists. The corpus is **read-only per AGENTS.md** ("Agents may run tests but must never modify corpus files"). **Fix:** (1) Create a locally-managed SLT file that does NOT live in the read-only submodule: add `tests/sqlcmp/wal_mode.test` (or similar non-submodule path) covering basic journal_mode=WAL semantics: `CREATE TABLE`, `INSERT`, `SELECT`, `PRAGMA journal_mode = WAL`, transactional rollback, frame boundary checks. (2) Update `tests/sqlcmp/slt/corpus_test.go` (or equivalent driver entry) to load files from the new local path in addition to the submodule. (3) Add `TestSLT_WalMode_Basic` proving end-to-end WAL-mode semantics on the new file. Estimate: ~150 lines (50 SLT + 100 driver glue), 1 file, 0.5 day. **Source:** audit surfacing in iteration `iter-WAL-SKELETON-REQ1297-1299.md`. | high | small | REQ001297 | tests/sqlcmp/wal_mode.test (new), tests/sqlcmp/slt/corpus_test.go |
| REQ001392 | SQB/EX | **`PRAGMA journal_mode` dispatch is a no-op.** `internal/SQB/EX/planner.go:692` lumps `journal_mode` into a catch-all `case "cache_size", "journal_mode", "synchronous", "user_version"` that returns the current value via `OP.NewPragmaResult` but never actually flips the engine into WAL-as-journal mode. Any SLT or driver that issues `PRAGMA journal_mode = WAL` observes no behavior change. REQ001297 acceptance requires this PRAGMA to enable the SQLite-style WAL pipeline (which the skeleton delivers in `internal/WAL/WR/frame.go` etc.) once the architectural swap lands. **Fix:** (1) In `planPragma`, route `journal_mode` (and `journal_mode = WAL/DELETE/MEMORY/TRUNCATE/PERSIST/OFF`) to a real switch: read returns the current mode; write triggers a state transition. (2) For `WAL`, invoke the new SQLite-WAL open path (REQ001297 item 4) and reject the transition if the engine is in single-process mode without -shm plumbing. (3) For `DELETE`, revert to LSM-recovery WAL (current behavior). (4) Surface mode via `OP.NewPragmaResult`. (5) Add `TestPragma_JournalMode_ReadWriteRoundTrip`, `TestPragma_JournalModeWAL_RequiresShm`. Estimate: ~80 lines, 1 file, 0.4 day. **Source:** audit surfacing in iteration `iter-WAL-SKELETON-REQ1297-1299.md`. | high | small | REQ001297, REQ001391 | SQB/EX/planner.go |
| REQ001393 | tests/sqlcmp/slt | **SLT driver has no PRAGMA-wal-mode wiring.** `tests/sqlcmp/slt/driver.go` and `tests/sqlcmp/slt/razor_driver.go` carry **zero** matches for `journal_mode` / `wal_mode` / `wal` PRAGMA parsing, propagation, or assertion. Even after the corpus gap (REQ001391) is filled and `journal_mode` dispatch (REQ001392) is wired, an SLT like `PRAGMA journal_mode = WAL; SELECT * FROM t1` would either be parsed by the underlying SLT runner but discarded by the driver, or fall through silently. **Fix:** (1) Audit the driver's PRAGMA-handling path (likely a dispatch function in `driver.go` and a statement-execution path in `razor_driver.go`); add explicit cases for `journal_mode` (read returns current, write calls Engine.SetJournalMode and asserts the round-trip value). (2) Add `journal_mode = ?` parameter binding so SLT files can drive multiple modes from a single test. (3) Verify that the new `tests/sqlcmp/wal_mode.test` (REQ001391) executes end-to-end through the driver. (4) Add `TestSLTDriver_JournalMode_Dispatch`. Estimate: ~60 lines, 2 files, 0.3 day. **Source:** audit surfacing in iteration `iter-WAL-SKELETON-REQ1297-1299.md`. | high | small | REQ001391, REQ001392 | tests/sqlcmp/slt/driver.go, tests/sqlcmp/slt/razor_driver.go |
| REQ001303 | WAL/RP | **WAL checkpoint modes: passive, full, truncate, restart.** SQLite supports four checkpoint modes. Passive: copy as many frames as possible without blocking writers. Full: wait for all readers to finish, then copy remaining. Truncate: same as Full, then truncate -wal to zero. Restart: same as Full, then rewind wal-index to start. **Fix:** (1) Add `walCheckpoint(mode int)` API. (2) Implement Passive: copy frames up to reader-locked position, do not wait. (3) Implement Full: take ckpt lock, wait for read locks to drain, copy remaining. (4) Implement Truncate: Full + ftruncate(-wal, 0). (5) Implement Restart: Full + reset mxFrame in shm. (6) Surface via `PRAGMA wal_checkpoint(MODE)` query PRAGMA. (7) Add `TestWalCheckpoint_AllModes`. Estimate: ~200 lines, 2 files, 1 day. | high | medium | REQ001299 | WAL/RP/checkpoint.go (new), SQB/EX/planner.go |
| REQ001304 | SYS/SY + ENG/LS | **PRAGMA auto_compact → LSM auto-trigger.** SQLite's `auto_vacuum = INCREMENTAL` reclaims pages on commit; FULL rebuilds the file. Razordata is LSM — the analog is "auto-trigger a leveled compaction when garbage ratio exceeds a threshold." **Fix:** (1) Add `auto_compact` PRAGMA accepting `none | incremental | full` (default: none). (2) `none` = current behavior. (3) `incremental` = on commit, if `sst_garbage_bytes / sst_total_bytes > 0.3` (configurable), enqueue a partial compaction for one level. (4) `full` = on commit, if ratio > 0.3, enqueue a full leveled compaction. (5) Compaction runs in the background worker pool (LS cluster has it). (6) Add `TestAutoCompact_Triggered`, `TestAutoCompact_Disabled_NoOp`. Estimate: ~120 lines, 3 files, 0.75 day. | high | medium | none | SYS/SY/init.go, ENG/LS/compaction.go, SQB/EX/planner.go |
| REQ001305 | SQB/EX + ENG/LS | **VACUUM statement → forced full compaction.** `VACUUM` is parsed by `SQF/PS/transaction.go` but unimplemented per `slt_gap_test.go:148`. On an LSM, VACUUM means "rebuild all SSTs with garbage discarded" — a full leveled compaction from L0 upward. **Fix:** (1) Wire `VACUUM` statement from SQB/EX dispatch to a new `vacuumFull()` engine method. (2) In LS, force a full compaction pass: all L0 memtable/SST → sorted run; merge all levels bottom-up, dropping tombstones and rewriting without GC gaps. (3) Block new writes during VACUUM (acquire write lock, drain in-flight). (4) `PRAGMA vacuum` is no-op when auto-compact is enabled and recent. (5) Add `TestVacuum_RebuildsSSTs`, `TestVacuum_DropsTombstones`. Estimate: ~150 lines, 3 files, 1 day. | high | medium | REQ001304 | SQB/EX/ex.go, ENG/LS/compaction.go, SQB/WT/writers_admin.go |
| REQ001306 | SQB/EX + ENG/LS | **PRAGMA incremental_vacuum(N) — partial compaction.** Equivalent to SQLite's `PRAGMA incremental_vacuum(N)`. Allows incremental reclamation of N pages per call. **Fix:** (1) Add `incremental_vacuum` PRAGMA with optional integer argument = pages to reclaim (default: all available). (2) On call, drain the freelist (or LSM GC) for up to N pages or until empty. (3) Returns number of pages reclaimed. (4) Requires `auto_vacuum = INCREMENTAL` to be effective. (5) Add `TestIncrementalVacuum_Partial`, `TestIncrementalVacuum_All`. Estimate: ~80 lines, 2 files, 0.5 day. | high | small | REQ001304 | SQB/EX/planner.go, ENG/LS/compaction.go |
| REQ001307 | SQB/WT + TXN/VL | **PRAGMA foreign_keys = ON/OFF session flag.** SQLite defaults foreign_keys = OFF for backward compat; once ON, FKs are enforced for the connection. Per-connection setting; affects only that Engine instance. **Fix:** (1) Add `foreign_keys` PRAGMA to WT.Pragma dispatch (boolean). (2) Store on the per-session state (`Engine.Session().FKEnabled`). (3) In writers_dml.go INSERT/UPDATE/DELETE path, if FKEnabled and constraint target is defined, run the enforcement check (defers to REQ001308–311). (4) PRAGMA foreign_keys is a no-op inside a transaction (SQLite behavior — must be set outside txn). (5) Add `TestForeignKeys_DefaultOff`, `TestForeignKeys_ToggleOn`, `TestForeignKeys_InsideTxn_NoOp`. Estimate: ~60 lines, 2 files, 0.25 day. | high | small | none | SQB/WT/writers_dml.go, SQB/EX/planner.go |
| REQ001308 | SQB/WT | **FK ON DELETE + ON UPDATE actions: CASCADE, SET NULL, SET DEFAULT, RESTRICT, NO ACTION (covers REQ001309).** All five actions are parsed (SQF/PS/ddl.go:181 area) but not enforced (WT/fk.go cluster doesn't exist per ARCH.md). ON DELETE and ON UPDATE share the same action enum and dispatch path. **Fix:** (1) For each FK constraint, store the action enum (apply to both DELETE and UPDATE). (2) On DELETE/UPDATE of a parent row, evaluate action: NO ACTION = check children exist (deferred or immediate per REQ001311), RESTRICT = same but error, CASCADE = delete children recursively (DELETE) or update child FK cols to new parent PK (UPDATE), SET NULL = set child FK cols to NULL, SET DEFAULT = set to column default. (3) Apply during DML execution; collect into a deferred queue if txn is in immediate mode. (4) Requires PK-change detection in UPDATE writer for ON UPDATE CASCADE. (5) Add `TestFK_OnDelete_Cascade`, `TestFK_OnDelete_SetNull`, `TestFK_OnDelete_Restrict`, `TestFK_OnDelete_SetDefault`, `TestFK_OnDelete_NoAction`, `TestFK_OnUpdate_Cascade`, `TestFK_OnUpdate_SetNull`, `TestFK_OnUpdate_Restrict`. Estimate: ~400 lines, 2 files, 2.25 days. | high | medium | REQ001307 | SQB/WT/fk.go (new), SQB/DT/schema.go |
| REQ001310 | SQB/WT | **FK MATCH FULL / PARTIAL / SIMPLE semantics.** MATCH SIMPLE (default): allow NULL in any FK col. MATCH FULL: all FK cols NULL or all NOT NULL. MATCH PARTIAL: at least one FK col NOT NULL. **Fix:** (1) Store MATCH mode in FK constraint. (2) In FK validation, before applying action, check MATCH: SIMPLE skips check if any FK col is NULL; FULL errors if mixed NULL/NOT-NULL; PARTIAL allows partial-NULL only if at least one NOT-NULL. (3) Test all three modes with multi-column FKs. (4) Add `TestFK_MatchSimple_NullAllowed`, `TestFK_MatchFull_MixedNull_Error`, `TestFK_MatchPartial_PartialNull_Allowed`. Estimate: ~120 lines, 1 file, 0.5 day. | high | small | REQ001308 | SQB/WT/fk.go |
| REQ001311 | SQB/WT + TXN/VL | **FK DEFERRABLE INITIALLY DEFERRED queue.** Parser accepts the syntax (ps_test.go:1490-1573). Default is INITIALLY IMMEDIATE: check at each statement. DEFERRED: collect violations, check at COMMIT. **Fix:** (1) Each FK constraint stores Deferrable and Initially flags. (2) On DML, if Deferrable+Deferred+inside-txn, add violation to a per-txn pending list (TXN/VL). (3) At COMMIT, drain pending list: for each violation, run the action; if action is CASCADE/SET NULL/SET DEFAULT, apply; if NO ACTION/RESTRICT, error → ROLLBACK. (4) IMMEDIATE: check synchronously, error inline. (5) Add `TestFK_Deferred_CollectsUntilCommit`, `TestFK_Deferred_ActionOnCommit`. Estimate: ~200 lines, 2 files, 1 day. | high | medium | REQ001307, REQ001308 | SQB/WT/fk.go, TXN/VL/protocol.go |
| REQ001312 | SQB/WT + ENG/LS | **WITHOUT ROWID: clustered-key SST layout.** Currently hard-rejected at `SQB/WT/writers_ddl.go:198`. On an LSM, the analog is: primary key columns ARE the SST row key, eliminating the secondary rowid pointer. **Fix:** (1) In SQB/WT DDL, accept `WITHOUT ROWID` and store flag in table metadata. (2) In ENG/LS, for WITHOUT ROWID tables, key SST entries by composite (pk_col1, pk_col2, …) instead of (rowid, …). (3) Secondary indexes reference PK directly (no rowid indirection — see REQ001313). (4) UPDATE that changes PK = delete + insert. (5) Add `TestWithoutRowid_BasicInsertSelect`, `TestWithoutRowid_CompositePK`, `TestWithoutRowid_UpdateChangesPK`. Estimate: ~300 lines, 4 files, 2 days. | high | large | none | SQB/WT/writers_ddl.go, ENG/LS/sst_writer.go, ENG/ID/id.go, SQB/DT/schema.go |
| REQ001313 | SQB/WT + ENG/LS | **WITHOUT ROWID: secondary index on clustered key (no rowid pointer).** When the table is WITHOUT ROWID, secondary index entries store the full PK rather than a rowid, so lookups go directly to the data without a second hop. **Fix:** (1) In ENG/ID, when creating a secondary index on a WITHOUT ROWID table, the value part of the index entry is the full PK (concatenated). (2) In IndexScan for WITHOUT ROWID, the seek returns (index_key, pk) — use pk directly to fetch the row, no rowid→row translation. (3) Verify no regression for rowid tables. (4) Add `TestWithoutRowid_SecondaryIndex_NoRowidHop`, `TestWithoutRowid_IndexOnlyScan`. Estimate: ~120 lines, 2 files, 0.75 day. | high | medium | REQ001312 | ENG/ID/id.go, SQB/OP/indexscan_strategy.go |
| REQ001314 | SQB/EX + ENG | **ANALYZE: per-column NDV + rowcount + null count + min/max, persisted to catalog (covers REQ001315, REQ001316, REQ001317, REQ001318).** All four metrics are computed in a single scan, stored in `razor_stat1` catalog table, and loaded into `pl.StatsCatalog` on Engine.Open. NDV is the cornerstone of selectivity estimation (already used by REQ001218 OR-to-IN and REQ001252 stats propagation). rowcount/null_count are required by selectivity estimators. min/max enable the stats-propagation rule that derives `t2.c ∈ [min(t1.c), max(t1.c)]` from join equality. **Fix:** (1) Add ANALYZE statement dispatch in SQB/EX. (2) On ANALYZE [table], run a single full or sampled scan (full for tables < 1M rows, sampled 1% reservoir otherwise) that computes for each column: NDV, rowcount, null_count, min, max. (3) Define `razor_stat1` catalog table: `(table TEXT, col TEXT, ndv INT, rowcount INT, null_count INT, min BLOB, max BLOB)`. (4) ANALYZE writes via the same writer path as user tables; create razor_stat1 on first ANALYZE if absent. (5) In SYS.Open, after loading catalog, SELECT * FROM razor_stat1 and populate the in-memory `pl.StatsCatalog`. (6) For nullable columns, NULL is excluded from min/max. (7) Add `TestAnalyze_ComputesNDV`, `TestAnalyze_LargeTable_Sampling`, `TestAnalyze_ComputesRowCount`, `TestAnalyze_ComputesNullCount_NotNullCol`, `TestAnalyze_ComputesMinMax`, `TestAnalyze_NullExcludedFromMinMax`, `TestAnalyze_PersistsAcrossRestart`, `TestStats_WiredOnOpen`, `TestStats_Queries_UseLoadedNDV`. Estimate: ~350 lines, 4 files, 1.6 days. | high | medium | none | SQB/EX/analyze.go (new), ENG/LS/iter.go, SQB/WT/writers_admin.go, ENG/CT/catalog.go, SQB/EX/ex.go, SYS/SY/sy.go |
| REQ001319 | SQB/WT | **DDL SAVEPOINT: checkpoint catalog version.** The catalog (ENG/CT) has a schema version number that increments on every DDL. SAVEPOINT must snapshot the version. **Fix:** (1) In WT, on SAVEPOINT, push current `catalog.Version` onto a stack (in WT, not TXN/VL, since this is DDL state). (2) Add `catalogCheckpoint()` API. (3) On DDL, increment version; on SAVEPOINT, save current version. (4) Add `TestDDLSavepoint_CheckpointCapturesVersion`. Estimate: ~60 lines, 1 file, 0.25 day. | high | small | none | SQB/WT/alter_table.go, ENG/CT/catalog.go |
| REQ001320 | SQB/WT | **DDL SAVEPOINT: rollback catalog to checkpointed version.** Roll back DDL on `ROLLBACK TO sp`. **Fix:** (1) On `ROLLBACK TO sp`, pop the catalog version stack and restore. (2) Replay catalog from razor_stat1 to rebuild the in-memory schema. (3) Drop any new tables/indexes/triggers added after the savepoint; drop any columns added (compensating ALTER). (4) Add `TestDDLSavepoint_RollbackToDropsAddedColumn`, `TestDDLSavepoint_RollbackToDropsAddedTable`. Estimate: ~150 lines, 2 files, 1 day. | high | medium | REQ001319 | SQB/WT/alter_table.go, ENG/CT/catalog.go |
| REQ001326 | SQB/EX | **CREATE TEMP/TEMPORARY TABLE.** Currently parser rejects per `ddl_test.go:426-429`. Temp tables are connection-scoped, dropped automatically at session end. **Fix:** (1) In WT DDL, accept `TEMP/TEMPORARY` flag; store in a separate `tempCatalog` map (session-scoped, not persisted to disk). (2) TableHandle resolution checks both main catalog and tempCatalog. (3) `sqlite_temp_master` exposes the temp tables (see REQ001328). (4) On session close, drop temp tables. (5) Add `TestTempTable_CreatedInSession`, `TestTempTable_DroppedOnSessionEnd`, `TestTempTable_SameNameAsMain_Shadows`. Estimate: ~150 lines, 3 files, 0.75 day. | high | medium | none | SQB/EX/ex.go, SQB/WT/writers_ddl.go, SQB/DT/schema.go |
| REQ001327 | SQB/EX | **PRAGMA temp_store (DEFAULT/FILE/MEMORY).** Controls where temp tables, temp B-trees, and intermediate sort/aggregate spills are stored. **Fix:** (1) Add `temp_store` PRAGMA accepting `DEFAULT | FILE | MEMORY`. (2) MEMORY = keep all temp data in DRAM (no spill to disk). (3) FILE = spill to `<db>.razor-temp/` directory. (4) DEFAULT = MEMORY up to a threshold, FILE beyond. (5) Add `TestTempStore_Memory_NoTempDir`, `TestTempStore_File_CreatesTempDir`. Estimate: ~100 lines, 2 files, 0.5 day. | high | small | REQ001326 | SQB/EX/planner.go, FIL/FS/fs.go |
| REQ001328 | SQB/OP | **Temp namespace: sqlite_temp_master introspection.** Expose the temp catalog (REQ001326) as a queryable pseudo-table. **Fix:** (1) Add `sqlite_temp_master` to the SqliteMaster operator (SQB/OP/operators.go area). (2) Columns: type, name, tbl_name, rootpage, sql. (3) SELECT against sqlite_temp_master returns temp tables/views/triggers. (4) Add `TestTempMaster_QueryAfterTempCreate`. Estimate: ~60 lines, 1 file, 0.25 day. | high | small | REQ001326 | SQB/OP/operators.go |
| REQ001329 | SYS/DS + FIL/IO | **Multi-process: shared_cache mode.** SQLite's `SQLITE_OPEN_SHAREDCACHE` lets multiple connections in the same process share a single in-memory page cache. Across processes, equivalent to opening the same db file. **Fix:** (1) Add `OpenOptions.SharedCache bool`. (2) When true, acquire a per-DSN RWMutex in SYS/DS; share the engine. (3) Multiple readers, single writer at the engine level. (4) `Engine.Close()` is a no-op for shared instances (last-closer cleans up). (5) Add `TestSharedCache_MultipleSessionsSameEngine`, `TestSharedCache_ConcurrentReaders`. Estimate: ~150 lines, 2 files, 0.75 day. | high | medium | none | SYS/DS/ds.go, SYS/AP/ap.go |
| REQ001330 | SYS/DS + FIL/IO | **Multi-process: POSIX file-locking protocol.** SQLite uses fcntl(F_SETLK) byte-range locks on the db file. Razordata currently has no inter-process locking — only one process can hold the db. **Fix:** (1) At Engine.Open, take a fcntl write lock (or read lock for read-only) on the db file. (2) If lock fails, return ErrLocked. (3) `PRAGMA lock_proxy_file` not needed. (4) `POSIX_FADV_RANDOM` on the fd. (5) Add `TestMultiProcess_SecondOpenErrors`, `TestMultiProcess_FirstCloses_SecondSucceeds`. Estimate: ~150 lines, 2 files, 0.75 day. | high | medium | none | FIL/IO/lock_linux.go (new), SYS/DS/ds.go |
| REQ001331 | SYS/DS + FIL/IO | **Multi-process: SHM mapping for change_counter.** SQLite uses an `*-shm` file for cross-process cache invalidation. A 4-byte change counter at offset 0 of -shm is bumped on every transaction commit; readers reload cache if their snapshot is stale. **Fix:** (1) Open `<db>.razor-shm` with `MAP_SHARED`. (2) On commit, atomic.AddUint32(counter, 1). (3) On read snapshot, check counter; if changed, invalidate caches. (4) Initial size: 32KB reserved (extensible). (5) Add `TestMultiProcess_ChangeCounter_BumpsOnCommit`, `TestMultiProcess_ReaderDetectsStaleCounter`. Estimate: ~200 lines, 2 files, 1 day. | high | medium | REQ001330 | FIL/IO/shm_linux.go (new), TXN/VL/protocol.go |
| REQ001332 | SYS/AP | **Public RegisterCollation API in SYS/AP.** SQLite exposes `sqlite3_create_collation_v2()`. Razordata's parser recognizes COLLATE (SQF/PS/select.go:88) but the engine has no public hook. **Fix:** (1) Add `Engine.RegisterCollation(name string, fn func(a, b []byte) int) error`. (2) Store in a per-engine registry. (3) In ORDER BY/COMPARE evaluation, look up collation by name. (4) On close, clear the registry. (5) Add `TestRegisterCollation_CustomOrder`, `TestRegisterCollation_CollateIndexOrderBy`. Estimate: ~80 lines, 2 files, 0.4 day. | high | small | none | SYS/AP/ap.go, SQB/EV/function_registry.go |
| REQ001333 | SQB/EV | **ICU/locale collation support.** Beyond the built-in `binary`/`nocase`/`rtrim`, support ICU locale-based collations (`en_US`, `de_DE`, `zh_CN`). **Fix:** (1) Add `golang.org/x/text/collate` dependency. (2) Register built-in collations `unicode_en_US`, `unicode_de_DE`, etc. (3) In compareValue, if collation is "unicode_*", use the collate package's StringComparer.Compare. (4) Test: `SELECT 'ä' COLLATE unicode_de_DE` returns different order than binary. (5) Add `TestCollation_ICU_AccentedChars`. Estimate: ~100 lines, 1 file, 0.5 day. | high | small | REQ001332 | SQB/EV/scalar_funcs.go, go.mod |
| REQ001338 | SQB/DT + SQB/WT | **VIRTUAL generated column: on-read evaluation.** Parser accepts VIRTUAL (SQF/PS/ddl.go:100-122), schema stores as nil (SQB/DT/schema.go:39-41), but no read-time evaluation exists. **Fix:** (1) In SQB/DT, mark VIRTUAL columns and store the AST expr. (2) In read path (SQB/OP/operators.go Project/Filter/Scan), before materializing the row, replace VIRTUAL col slots by evaluating the stored expr against the row's other columns. (3) Result is identical to materializing at write; no on-disk cost. (4) Add `TestGenerated_Virtual_SelectMaterializes`, `TestGenerated_Virtual_NotStoredOnDisk`. Estimate: ~150 lines, 3 files, 0.75 day. | high | medium | none | SQB/DT/schema.go, SQB/OP/operators.go, SQB/EV/eval.go |
| REQ001339 | SQB/DT + SQB/EX | **VIRTUAL generated column: usable in indexes.** A VIRTUAL generated column can be referenced in CREATE INDEX and used in WHERE clauses. **Fix:** (1) Allow `CREATE INDEX i ON t (virtual_col)`; persist the index on the VIRTUAL column. (2) On index lookup, recompute the VIRTUAL col from the row, then compare to seek key. (3) Add `TestGenerated_Virtual_IndexLookup`, `TestGenerated_Virtual_IndexUsedInWhere`. Estimate: ~100 lines, 2 files, 0.5 day. | high | small | REQ001338 | SQB/DT/schema.go, ENG/ID/id.go |
| REQ001340 | SQB/DT + SQB/WT | **VIRTUAL generated column: usable in FK references.** A VIRTUAL generated col can be the target of a FK from another table. **Fix:** (1) Allow `FOREIGN KEY (other.x) REFERENCES t(virtual_col)`. (2) On FK enforcement, compute the virtual col on-the-fly. (3) Add `TestGenerated_Virtual_FKTarget`. Estimate: ~60 lines, 1 file, 0.3 day. | high | small | REQ001338, REQ001308 | SQB/WT/fk.go |
| REQ001355 | SQB/OP | **RIGHT [OUTER] JOIN (NLJ + Hash, covers REQ001356).** Return all right rows; left unmatched cols get NULL. Both NestedLoopJoin and HashJoin must support right-outer — same logical feature, two implementations. **Fix:** (1) Extend NestedLoopJoin to support `rightOuter bool`: build right-row index, iterate right rows, probe left. (2) Extend HashJoin to support right-outer: build hash on right rows, probe left, emit unmatched right rows with left NULLs. (3) Add `TestRightJoin_NLJ_Basic`, `TestRightJoin_NLJ_LeftEmpty`, `TestRightJoin_Hash_Basic`, `TestRightJoin_Hash_LeftEmpty`. Estimate: ~160 lines, 2 files, 0.8 day. | high | small | none | SQB/OP/join.go, SQB/OP/hashjoin.go |
| REQ001357 | SQB/OP | **FULL [OUTER] JOIN (NLJ + Hash, covers REQ001358).** Both sides' unmatched rows emitted. Both NestedLoopJoin and HashJoin must support full-outer — same logical feature, two implementations. **Fix:** (1) Extend NestedLoopJoin to support `fullOuter bool`: track matched rows on both sides, emit unmatched at end. (2) Extend HashJoin to support full-outer: two-pass — hash-build on right, probe left, then emit unmatched right. (3) Add `TestFullJoin_NLJ_Basic`, `TestFullJoin_NLJ_BothSidesUnmatched`, `TestFullJoin_Hash_Basic`. Estimate: ~200 lines, 2 files, 1 day. | high | small | REQ001355 | SQB/OP/join.go, SQB/OP/hashjoin.go |
| REQ001359 | SQF/PS + SQB/OP | **NATURAL [INNER] JOIN.** Auto-equality on common column names. **Fix:** (1) In SQF/PS, parse NATURAL; collect common column names from both tables; synthesize ON clause. (2) Lower to INNER JOIN with synthesized ON. (3) Add `TestNaturalJoin_Inner_Basic`, `TestNaturalJoin_NoCommonCols_Cross`. Estimate: ~80 lines, 2 files, 0.4 day. | high | small | none | SQF/PS/select.go, SQB/EX/planner.go |
| REQ001360 | SQF/PS + SQB/OP | **NATURAL LEFT/RIGHT/FULL JOIN.** Same as REQ001359 but with outer semantics. **Fix:** (1) Extend parse to accept NATURAL + outer modifier. (2) Lower to the appropriate outer join (REQs 355–358). (3) Add `TestNaturalJoin_Left`, `TestNaturalJoin_Right`, `TestNaturalJoin_Full`. Estimate: ~60 lines, 1 file, 0.3 day. | high | small | REQ001359, REQ001355, REQ001357 | SQF/PS/select.go |
| REQ001366 | SQB/WT | **INSTEAD OF INSERT/UPDATE/DELETE triggers on views (covers REQ001367, REQ001368).** Allows updatable views via trigger logic. The three operations share the same trigger dispatch path — only the DML verb and the OLD/NEW row references differ. **Fix:** (1) In trigger dispatch, accept `INSTEAD OF INSERT ON view_name`, `INSTEAD OF UPDATE ON view_name`, `INSTEAD OF DELETE ON view_name`. (2) When DMLing into a view, run the INSTEAD OF trigger body instead of the default error. (3) For UPDATE, expose OLD/NEW row references in the trigger body. (4) Add `TestInsteadOf_Insert_Basic`, `TestInsteadOf_Insert_MultipleRows`, `TestInsteadOf_Update_Basic`, `TestInsteadOf_Update_OldNewRefs`, `TestInsteadOf_Delete_Basic`. Estimate: ~370 lines, 2 files, 1.85 days. | high | medium | none | SQB/WT/writers_dml.go, SQB/WT/trigger.go (new) |
| REQ001369 | SQB/WT | **STRICT table-type mode enforcement.** Parses (ps_test.go:896), runtime ignores. **Fix:** (1) In writers_ddl.go, accept STRICT; store in catalog. (2) In row encoding, use SQLite's 1-byte type tag per column (NULL=0, INT=1, REAL=2, TEXT=3, BLOB=4, ANY=5). (3) On INSERT, validate each value's type matches the declared affinity; error if mismatched. (4) Add `TestStrict_TypeEnforced_InsertMismatch_Error`, `TestStrict_TypeEnforced_NullAllowed`. Estimate: ~150 lines, 2 files, 0.75 day. | high | small | none | SQB/WT/writers_ddl.go, SQB/DT/encode.go |
| REQ001370 | SQB/WT | **TEMP trigger support.** Triggers in the temp namespace. **Fix:** (1) Accept `CREATE TEMP TRIGGER`; store in temp catalog (REQ001326). (2) Trigger firing scans both main and temp catalogs. (3) On session end, temp triggers are dropped. (4) Add `TestTempTrigger_CreatedInSession`, `TestTempTrigger_DroppedOnSessionEnd`. Estimate: ~80 lines, 1 file, 0.4 day. | high | small | REQ001326 | SQB/WT/trigger.go |
| REQ001371 | SQB/EX | **Planner: honor INDEXED BY / NOT INDEXED hints (covers REQ001372).** `FROM t INDEXED BY idx_name` forces the planner to use the named index; `FROM t NOT INDEXED` forces a sequential scan, ignoring all indexes. Both hints are parser-recognized (SQF/PS/ddl.go:659) but the planner ignores them. **Fix:** (1) Parse both hints, store on the From node. (2) When planning, if INDEXED BY is set, only consider the named index; error if hint name is invalid. (3) If NOT INDEXED is set, skip index selection entirely; emit SeqScan. (4) Add `TestIndexHint_ForcesIndex`, `TestIndexHint_InvalidName_Error`, `TestIndexHint_NotIndexed_ForcesSeqScan`. Estimate: ~120 lines, 2 files, 0.6 day. | high | small | none | SQB/EX/planner.go, SQF/PS/select.go |
| REQ001373 | SQB/WT | **Trigger: RAISE(IGNORE / ROLLBACK / FAIL) — three control-flow sentinels (covers REQ001374, REQ001375).** All three RAISE variants share the same dispatch path — they return distinct sentinels from `evalRaise` and `writers_dml.go` handles each one. (1) `RAISE(IGNORE)`: return `ErrRaiseIgnore`; trigger dispatcher catches and treats as "row skipped" — advance to next row without aborting. (2) `RAISE(ROLLBACK)`: return `ErrRaiseRollback`; txn dispatcher aborts and rolls back the entire transaction. (3) `RAISE(FAIL)`: return `ErrRaiseFail`; abort the current statement only, transaction continues. (4) Add `TestRaiseIgnore_SkipsRow`, `TestRaiseIgnore_DoesNotIncrementRowsAffected`, `TestRaiseRollback_AbortsTxn`, `TestRaiseRollback_UndoesPriorWrites`, `TestRaiseFail_AbortsStmt`, `TestRaiseFail_KeepsTxn`. Estimate: ~200 lines, 2 files, 1 day. | high | small | none | SQB/WT/trigger.go, SQB/EV/eval.go |
| REQ001379 | SQB/UT | **PRAGMA quick_check — row count consistency only.** Cheap pre-check before integrity_check. **Fix:** (1) Add `quick_check` PRAGMA. (2) For each user table, compare count(*) to razor_stat1.rowcount. (3) Report "ok" or list discrepancies. (4) Add `TestQuickCheck_Ok`, `TestQuickCheck_RowCountMismatch`. Estimate: ~80 lines, 1 file, 0.4 day. | high | small | REQ001317 | SQB/UT/integrity.go |
| REQ001380 | SQB/UT | **PRAGMA integrity_check — SST checksum + FK consistency + index reference (covers REQ001381, REQ001382).** Full integrity check has three modes that all live in SQB/UT/integrity.go. **Fix:** (1) **SST checksum walk**: iterate all SST files in ENG/LS, recompute CRC32 per block, compare to stored; report first 10 failing blocks. (2) **FK consistency**: for each FK constraint, build a set of parent PKs, verify every child FK col matches an existing parent PK. (3) **Index reference**: iterate indexes in ENG/ID, verify each entry's rowKey resolves to a real row. (4) Add `TestIntegrityCheck_SSTChecksum`, `TestIntegrityCheck_FK_DanglingChild`, `TestIntegrityCheck_Index_DanglingPointer`. Estimate: ~350 lines, 2 files, 1.75 days. | high | medium | REQ001308 | SQB/UT/integrity.go, ENG/LS/sst_reader.go, ENG/ID/id.go |
| REQ001383 | SQB/EX + SQB/WT | **RETURNING clause on UPSERT.** `INSERT … ON CONFLICT … DO UPDATE … RETURNING *` returns the post-update row. **Fix:** (1) Extend writers_dml.go UPSERT path to capture returning rows. (2) For DO UPDATE, return the post-update row. (3) For DO NOTHING, return the pre-existing row. (4) Add `TestUpsert_Returning_DoUpdate`, `TestUpsert_Returning_DoNothing`. Estimate: ~80 lines, 1 file, 0.4 day. | high | small | REQ001363 | SQB/WT/writers_dml.go |
| REQ001384 | SQB/WT | **DROP COLUMN: cascade rules doc + tests.** Centralize the cascade rules for DROP COLUMN. **Fix:** (1) Document rules: FK → drop FK; generated col → drop generated col; indexed col → drop index; CHECK → check for reference. (2) Add test matrix `TestDropColumn_CascadeMatrix` covering all 4 cases. (3) Add `TestDropColumn_MultipleCascades`. Estimate: ~120 lines, 2 files, 0.5 day. | high | small | REQ001324, REQ001325 | SQB/WT/alter_table.go |
| REQ001386 | SQB/EX | **Partial index: planner use with multi-clause WHERE.** Use a partial index when the query's WHERE implies the partial predicate. **Fix:** (1) In planner index selection, check if query's WHERE conjuncts imply the partial index's predicate. (2) Use the index if so. (3) Add `TestPartialIndex_PlannerUses_MultiPredicate`, `TestPartialIndex_PlannerSkips_NotImplied`. Estimate: ~100 lines, 1 file, 0.5 day. | high | small | REQ001385 | SQB/EX/planner.go |
| REQ001387 | SQB/UT | **PRAGMA cell_size_check — verify BLOB/text fits in cell.** SQLite 3.46+ checks that no cell overflows the page. Razordata's analog: SST block size check. **Fix:** (1) Add `cell_size_check` PRAGMA. (2) Walk all SST blocks; verify block_size matches declared. (3) Report over/under-sized blocks. (4) Add `TestCellSizeCheck_Ok`, `TestCellSizeCheck_CorruptBlock`. Estimate: ~80 lines, 1 file, 0.4 day. | high | small | none | SQB/UT/integrity.go, ENG/LS/sst_reader.go |
| REQ001390 | SQB/OP | **sqlite_sequence — autoincrement tracking.** Tracks the last used ROWID for AUTOINCREMENT tables. **Fix:** (1) Add sqlite_sequence pseudo-table: `(name TEXT, seq INTEGER)`. (2) On INSERT to an AUTOINCREMENT table, update seq to max(seq, new_rowid). (3) On INSERT when no rowid available, allocate seq+1. (4) Add `TestSqliteSequence_AutoIncrement`. Estimate: ~150 lines, 2 files, 0.75 day. | high | medium | none | SQB/OP/operators.go, SQB/WT/writers_dml.go |
| REQ001412 | SQB/OP | **SeqScan column pruning (REQ001080) — verify effectiveness for select1 query patterns.** REQ001080 implements column-level pruning in SeqScan (only read projected columns from store). Needs verification that pruning actually reaches the storage layer for select1's query patterns (simple SELECT, expression-heavy, subquery). If effective, I/O savings are ~80% for single-column queries vs reading all 5 columns. **Fix:** (1) Add `TestSeqScan_ColumnPruning_Select1Patterns` verifying `SeqScan.Columns()` propagates to storage read path. (2) Add `BenchmarkSeqScan_ColumnPruning_vs_FullScan` measuring I/O reduction. (3) If pruning is not reaching storage, fix the propagation chain. Estimate: ~60 lines, 1 file, 0.25 day. | low | small | REQ001080 | SQB/OP/operators.go |
| REQ001418 | ENG/LS | **`TestMergeIterator_NoHeapBoxing` flakily fails: `mergeIterator allocs/op = 7, want <= 6` (allocation regression).** `internal/ENG/LS/merge_pool_test.go:595` asserts the merge iterator pool path allocates at most 6 bytes per op under `testing.AllocsPerRun`. The test intermittently reports 7 allocs/op. When run alone (`go test -run TestMergeIterator_NoHeapBoxing`) it passes; under `-count=5` it fails 3/5 times. The flake indicates a code path escapes to heap on certain GC pressure / scheduler timing — likely an interface upcast or a deferred closure captures a stack-escaped pointer. **Fix:** (1) Profile with `-benchmem` to find which allocation escapes (heaptrack or `go test -gcflags=-m` for escape analysis). (2) Common culprits: `sync.Pool.Put` taking an interface (causes 1 alloc when value is non-pointer), closure over loop variable, `any` boxing of struct values. (3) Pin the alloc count by eliminating the escape; or relax the assertion to `<= 7` with a comment justifying the relaxed bound. (4) Verify determinism with `go test -race -count=20`. Estimate: ~30 lines (investigation + fix), 1 file, 0.25 day. **Source:** pre-existing failure observed during `go test ./internal/...` audit while implementing REQ001300-1302; verified via `git stash` baseline. | medium | small | none | ENG/LS/merge_pool_test.go, ENG/LS/iterator.go |
| REQ001421 | WAL/WR + SQB/WT | **Write batch buffer for autocommit INSERT/UPDATE.** INSERT is 4.5x, UPDATE is 8.6x slower than modernc.org/sqlite at 100 rows. Current path: each INSERT/UPDATE triggers an independent WAL write with no batch merging. SQLite coalesces autocommit INSERTs into a single WAL frame. **Fix:** (1) Add a per-session `WriteBatch` buffer (in-memory WAL frame accumulator) in WAL/WR. (2) `WAL.Writer` defers fsync until commit or buffer full (e.g. 16 KB). (3) INSERT/UPDATE writers (SQB/WT) append to the buffer instead of flushing per-row. (4) On autocommit, flush once with a single WAL frame. (5) On `BEGIN`, switch to per-row flush (transaction semantics). (6) Verify: 100-row INSERT drops from 4203 µs/op to ≤ 2000 µs/op. Estimate: ~250 lines, 4 files, 1.5 days. | high | medium | REQ001419 | WAL/WR/writer.go (new batch struct), WAL/WR/frame.go, SQB/WT/writers_dml.go |
| REQ001422 | SYS/SE + SQB/EX | **Prepared statement persistence — compile-once, execute-many.** Every query rebuilds the operator tree from scratch. Plan cache exists but does not prevent operator tree construction. **Fix:** (1) Add `Session.Prepare(sql)` returning a reusable `PreparedPlan` that holds the compiled operator tree (closed and re-openable). (2) `PreparedPlan.Exec(args)` and `PreparedPlan.Query(args)` inject parameters and reopen the operator tree without re-planning or re-building. (3) `PreparedPlan.Close()` cleans up the operator tree. (4) Wire `database/sql.Stmt` to cache PreparedPlan across calls. (5) Verify: repeated `SELECT * FROM t` via prepared stmt is ≤ 1.5x the per-query `QueryRow` cost (eliminating plan + tree build). Estimate: ~180 lines, 3 files, 1.5 days. | high | medium | REQ001419 | SYS/SE/stmt.go (new PreparedPlan), SQB/EX/ex.go, driver/stmt.go |
| REQ001423 | ENG/LS + SQB/EX | **B-tree / LSM block-level stats: min, max, NDV for range query planning.** Range scans (e.g. `WHERE age > 30 AND age < 50`) currently traverse the full SeqScan. LSM blocks have per-column min/max that can be used for early block pruning. **Fix:** (1) In ENG/LS SST block stats, expose per-column `min`/`max` during block construction. (2) In SeqScan, when a range predicate is present, skip blocks where the predicate's range is disjoint from the block's column min/max. (3) Expose via `TableStats` column-level `min`/`max` in catalog. (4) Add `TestSeqScan_BlockPruning_RangePredicate` verifying skipped blocks. Estimate: ~120 lines, 2 files, 0.75 day. | high | small | REQ001420 | ENG/LS/sst_block.go, SQB/OP/operators.go |
| REQ001424 | SQB/WT + SQB/DT | **Pre-size UPDATE/DELETE `DT.RowArena` to avoid per-statement slab grow.** `BenchmarkRazordata_Update` shows `(*RowArena).grow` allocates ~626 MB per benchmark run (~26% of total). UPDATE/DELETE writers use the per-statement `ExecContext.RowArena` but never call `Init(estimatedRows, colsPerRow)`. Each statement grows the arena geometrically from 64 KB and discards it on `Reset()` — six grow cycles per statement for a 50-row update. **Fix:** (1) In `WT.(*Update).Next` / `(*Delete).Next`, before the row scan, call `ec.RowArena.(*DT.RowArena).Init(estimatedRows, len(schema.Cols))`. Estimation = `Min(rowCount, filterSelectivity * rowCount)` from stats, fallback 256 rows. (2) For Update with a primary-key WHERE, init with 1 row. (3) Add `TestRowArena_UpdatePresize_EliminatesGrows` verifying slab grew ≤ 1 time over 100 UPDATE iterations. (4) Verify `BenchmarkRazordata_Update` allocations drop ≤ 50%. Estimate: ~40 lines, 2 files, 0.25 day. **Source:** surfacing from REQ001421 perf profile (post single-node NUMA fix + drop driver Begin/Rollback); both NUMA and Begin fixes landed as `85fce6d` + `2a6a171`. | high | small | none | SQB/WT/writers_dml.go, SQB/DT/arena.go |
| REQ001425 | SQB/EX + SYS/SE + driver | **Cache parsed SQL → operator tree mapping for repeated INSERT/UPDATE/DELETE patterns.** `BenchmarkRazordata_Update` profile shows `NewInsert`/`NewUpdate`/`NewDelete` + `OP.NewFilter` + `OP.NewSeqScan` allocate ~0.5 GB across the bench (operator tree construction per exec call). Repeated identical SQL strings (typical OLTP workloads, driver prepared-stmt pattern) re-walk the planner tree for each `Exec`. The parsed-stmt cache (REQ001223) caches the AST but not the operator tree. **Fix:** (1) Add a `sessionExecPlanCache` keyed by `sqlString` (after parameter rebinding) on Session/Executor. (2) On cache hit, validate the cached operator is closed (or reusable); if so, return a reset copy. (3) On cache miss, plan + execute; on success with no active transaction AND no parameters, store the operator tree in the cache. (4) Skip caching when SQL has volatile constructs (`PRAGMA`, `RETURNING`, transactions, EXPLAIN). (5) Add `TestExecPlanCache_RepeatedInsert_ReusesOperator`, `TestExecPlanCache_DriverBench_VerifyHitRate`. Estimate: ~150 lines, 3 files, 1 day. **Source:** surfacing from REQ001421 perf profile. | high | medium | REQ001422 | SQB/EX/ex.go, SYS/SE/se.go, driver/stmt.go |
| REQ001426 | SQB/WT + SQB/DT | **Eliminate per-row `BuildInsertRow` slice copies for bulk INSERT.** `BenchmarkRazordata_Insert` profile after REQ001424+425 lands will show `WT.BuildInsertRow` (0.22 GB / 8%) and `DT.ValueFromAnySlice` (0.24 GB / 9%) as top allocators. BuildInsertRow allocates a new `DT.Row` per row + a `[]Value` per row + an `[]any` boxed-Value conversion per row. **Fix:** (1) Add `WT.NewInsert.InsertRowsBatch(rows []PS.ExprList) (DT.Row, error)` that builds all rows into pre-allocated slices; reuse the `ExecContext.RowArena` for `[]Value` storage (already implemented per REQ001221 but not used in the auto-values path). (2) Add `WT.ValidateRowBatch(rows []DT.Row, schema *StoreSchema) error` that batches Default-fill + Check + Unique-check without per-row interface conversion. (3) The INSERT loop then dispatches the batched path when `len(rows) > 1`. (4) Add `TestInsertBatch_ArenaReuse_NoPerRowAlloc`. Estimate: ~120 lines, 2 files, 0.75 day. **Source:** surfacing from REQ001421 perf profile. | high | medium | REQ001424 | SQB/WT/writers_dml.go, SQB/DT/arena.go |
| REQ001427 | driver | **Route driver-prepared `database/sql.Stmt` through the new `execPlanCache` (REQ001425) — eliminate per-call `session.Exec` lock acquisition.** Per call, `Conn.ExecContext` → `session.Exec(ctx, …)` acquires and releases the session lock even when no transaction is active. Lock cost dominates repeated small INSERTs. With `Stmt.Exec` (driver prepared-stmt pattern), the same SQL runs N times in a row. A per-`Stmt` cached operator tree + parameter binding would skip both parsing and lock acquisition on every call after the first. **Fix:** (1) In `driver/stmt.go` (or equivalent), on first `Stmt.Exec(args)`, lookup the cached prepared operator via REQ001425. (2) On subsequent calls, rebind params and run the cached tree without going through `session.Exec`. (3) Honour per-stmt connection pin. (4) Verify with `BenchmarkRazordata_Insert` running through `db.Prepare("INSERT …").Exec(…)`. (5) Add `TestStmt_Prepared_ReusesOperator`. Estimate: ~80 lines, 2 files, 0.5 day. **Source:** follow-on from REQ001425 + REQ001422 driver-side wiring. | medium | medium | REQ001425, REQ001422 | driver/stmt.go, SYS/SE/se.go |
| REQ001431 | SQB/DT + SQB/OP | **Columnar small-table layout for select1 workloads.** select1 table has 5 int columns. `SELECT a FROM t1` decodes all 5 columns per row and prunes back to 1. Store columns as contiguous int64 arrays for tables < 1000 rows, enabling zero-decay projection. **Fix:** (1) In DT, when a table's estimated size is < 1000 rows, store data column-major: `cols["a"] = []int64{...}` alongside row-major encoding. (2) SeqScan, when RequestedCols is set, reads from columnar storage directly. (3) INSERT appends to both formats. (4) Add `BenchmarkSelect1_ColumnarProjection`. Estimate: ~300 lines, 3 files, 2 days. | medium | large | none | SQB/DT/schema.go, SQB/OP/operators.go, SQB/WT/writers_dml.go |
| REQ001451 | SQB/EX | *(partially done — 24 wrappers deleted, 12 callback-currying wrappers remain EX-bound)* | high | small | REQ001450 | SQB/EX/predicate.go, SQB/EX/cost.go, SQB/EX/join_order.go |
| REQ001454 | tests/sqlcmp/slt + SYS/SY | **SLT engine reuse: avoid per-file engine Close+Connect — reset engine state instead.** Each `.test` file in the SLT corpus creates a fresh engine (MkdirTemp → Engine.Open → sql.Open → run → Close → RemoveAll). With ~30 select1 files, this is 30× ~42 MB allocation + ~300 ms setup = 1.2 GB of wasted allocation. The `.test` files test SQL behavior, not engine initialization — the engine itself is never the subject of the test. **Fix:** (1) Add `Engine.Reset()` that drops all user tables/indexes/triggers/views and resets the catalog to clean state (system tables like `razor_stat1`, `sqlite_sequence` stay). (2) In SLT `razor_driver.go`, add `Reset(ctx) error` that calls `engine.Reset()` instead of Close+Connect. (3) The SLT runner calls `Reset()` between files instead of `Close()`+`Connect()`. (4) Verify: select1-4 pass; per-file setup cost drops from ~300 ms to ~0 ms; total select1 runtime drops ~50%. Estimate: ~80 lines, 2 files, 0.5 day. | medium | small | none | SYS/SY/sy.go, tests/sqlcmp/slt/razor_driver.go, tests/sqlcmp/slt/driver.go |
| REQ001459 | SQB/EV + SQB/OP | **Unify expression compilation — consolidate 5 fragmented `compileFilterExpr`/`compileRowExpr`/`compileProjectExprs`/`compileProjectCaseExpr`/`compileProjectFuncCall` into `EV.CompileExpr` with uniform `func(*Row) (Value, error)` signature, add CASE/FunctionCall/arithmetic coverage, remove duplicate column-name resolution.** Problem: `OP/intermediate_basic.go` has 5 separate compilation entry points with different signatures and overlapping coverage — `compileFilterExpr` (col-op-literal + AND/OR + InExpr only), `compileRowExpr` (Ident/QualifiedName/arith only, swallows errors), `compileProjectExprs`/`compileProjectCaseExpr`/`compileProjectFuncCall` (Project-only, duplicate EV logic). CASE/arithmetic/FunctionCall in WHERE filters silently fall back to the recursive `evalFallbackEvalValue` walker. **Fix:** (1) Add `EV.CompileExpr(expr PS.Expr) func(*Row) (Value, error)` covering all expression types: Literals return constant; Ident/QualifiedName via SlotIdx/ColIndex; BinaryExpr AND/OR short-circuit, arithmetic direct int64/float64, comparison via pl.CompareValue; UnaryExpr NOT/NEG; CaseExpr flatten WHEN/THEN to closure chain; FunctionCall dispatch to registry; InExpr build O(1) lookup map; CastExpr inline coercion; BetweenExpr lower to AND(GE,LE); Subquery/Aggregate/Window return nil (fallback). (2) Add `EV.CompilePredicate` wrapping CompileExpr with truthy check. (3) Remove all 5+ compilation functions from OP/intermediate_basic.go, replace with single calls to EV.CompileExpr/CompilePredicate. (4) Standardize `func(*Row) (Value, error)` — no more error-swallowing `func(*Row) Value`. (5) Add benchmarks: `BenchmarkCompiledExpr_Case/Arith/FuncCall/Nested` vs recursive eval. Estimate: ~300 lines in EV/eval.go, ~60 lines deleted from OP/intermediate_basic.go, 3 files, 1.5 days. | high | medium | none | SQB/EV/eval.go, SQB/OP/intermediate_basic.go, SQB/OP/operators.go |
| REQ001460 | SQB/EX | **Vectorized plan transform: replace all-or-nothing shape check with per-operator fallback via RowOperatorAdapter.** `tryVectorizePlan` in `vec_transform.go` has a two-stage failure mode: `isEligible()` uses a rigid whitelist of operator tree shapes (e.g. `Filter(SeqScan)`, `Project(Filter(SeqScan))`), and trees containing any unrecognized operator (Sort, Limit, IndexScan) are entirely rejected — the whole query runs row-at-a-time. `RowOperatorAdapter` already exists in `UT/row_adapter.go` but `transformOp` never uses it. **Fix:** (1) Remove the `isEligible()` pre-check gate; call `transformOp` directly on all trees. (2) In `transformOp`'s default case, return `UT.NewRowOperatorAdapter(op)` instead of nil — this wraps a row-based operator as a `BatchProducer` so upstream vectorized operators (VectorizedProject, VectorizedFilter) can consume batches from it. (3) Verify: `Project(Sort(SeqScan))` produces a mixed tree: `VectorizedProject(RowOperatorAdapter(Sort(VectorizedSeqScan(SeqScan))))` with Project batch-vectorized and Sort row-based. (4) All select1-5 corpus tests pass. (5) Benchmark: non-ORDER-BY queries in select1 run through vectorized path. Estimate: ~15 lines, 1 file, 0.1 day. | high | small | none | SQB/EX/vec_transform.go |
| REQ001461 | SQB/OP | **VectorizedSeqScan: use SeqScan.NextBatch native LSM batch read instead of row-by-row fill.** `VectorizedSeqScan.NextBatch()` (operators_vec.go:64-83) reads rows one at a time via `v.source.Next(ctx)` and fills a `*UT.Batch` column by column. Meanwhile, `SeqScan.NextBatch()` (operators.go:353) reads directly from the LSM iterator in batches of 512 rows (or `RAZOR_BATCH_SIZE`). The vectorized path ignores the native batch read entirely. **Fix:** (1) In `transformOp` for SeqScan, create a thin adapter that calls `SeqScan.NextBatch()` and returns the `*UT.Batch` directly, instead of wrapping the SeqScan in `VectorizedSeqScan`. (2) The adapter needs to ensure batch column names/types match what downstream operators expect — `SeqScan.NextBatch` already sets column names on the batch. (3) Alternatively, make `VectorizedSeqScan` detect when its source is a `*SeqScan` and delegate to `SeqScan.NextBatch` instead of row-by-row fill. (4) Verify: batch sizes from LSM match UT.BatchSize expectations (512 vs 1024). (5) Benchmark: `BenchmarkSelect1_VecPath` shows reduced per-batch fill overhead. Estimate: ~40 lines, 2 files, 0.25 day. | high | small | none | SQB/OP/operators_vec.go, SQB/EX/vec_transform.go |
| REQ001462 | SQB/OP + SQB/EX | **Add VectorizedSort + VectorizedLimit operators and wire into tryVectorizePlan.** Sort (ORDER BY) and Limit are the most common missing vectorized operators preventing select1 queries from entering the vectorized path. `ParallelSort` already has a `NextBatch()` method but it is not connected through `tryVectorizePlan`. Limit has no vectorized equivalent at all. **Fix:** (1) Add `VectorizedSort` operator wrapping `ParallelSort` as a `BatchProducer`: reads all batches from child, sorts rows via existing sort logic, outputs sorted batches. Leverage `ParallelSort.NextBatch()` if already compiled. (2) Add `VectorizedLimit` operator: wraps a `BatchProducer` child, passes through batches but stops after N rows (or passes through LIMIT + OFFSET to child if child supports push-down). (3) Wire both into `transformOp`: detect `*OP.Sort` → `VectorizedSort(RowOperatorAdapter(sortChild))`, detect `*OP.Limit` → `VectorizedLimit(childBP, limit, offset)`. (4) Verify: `SELECT a FROM t1 ORDER BY a LIMIT 5` runs through vectorized path (VectorizedSeqScan → RowOperatorAdapter(Sort) → VectorizedLimit → BatchToRowAdapter). (5) Add `BenchmarkSelect1_VecSortLimit` comparing row-only vs mixed vs full-vectorized. Estimate: ~120 lines, 2 files, 0.75 day. | high | small | none | SQB/OP/operators_vec.go, SQB/EX/vec_transform.go |
| REQ001463 | SQB/OP | **Fused small-table operator — inline SeqScan+Filter+Project into a tight loop for <1000-row tables, eliminating 3 virtual dispatch layers + Batch adapter overhead.** For select1's 20-row table, each query goes through `SeqScan.Next()` → `Filter.Next()` → `Project.Next()` → `BatchToRowAdapter.Next()`, each a virtual call through the `Operator` interface. After REQ001459 (compiled closures for filter/project expressions), all the per-row logic becomes a closure call — the operator tree dispatch is pure overhead. **Fix:** (1) Add `FusedScan` operator that holds: a local row slice (from `DT.Tables` or `SeqScan` drain), a compiled `filterFn func(*Row) bool` (from REQ001459), and a compiled `projectFn func(*Row) Row` (from REQ001459). (2) `FusedScan.Next()` is a tight loop: iterate rows, apply filterFn, projectFn, return. Zero virtual dispatch per row. (3) In the planner (or `tryVectorizePlan`), detect `Project(Filter(SeqScan))` or `Filter(SeqScan)` or `Project(SeqScan)` patterns where `estimatedRowCount < 1000` and replace with `FusedScan`. (4) `FusedScan` does NOT produce `*UT.Batch` — it produces `Row` directly, so no BatchToRowAdapter conversion needed. (5) Verify: `SELECT a+b*2 FROM t1 WHERE a>150` runs through `FusedScan.Next()` with 1 closure call per row instead of 3-4 virtual calls. (6) Add `BenchmarkSelect1_FusedScan` vs `BenchmarkSelect1_VecPath` vs row-only SeqScan+Filter+Project. Estimate: ~150 lines, 2 files, 0.75 day. | high | medium | REQ001459 | SQB/OP/fused.go (new), SQB/EX/vec_transform.go |

| REQ001465 | SQB/OP | **Small-table direct memtable scan — bypass LSM iterator when table fits in `DT.Tables`.** Current `SeqScan.Next()` always goes through `store.NewIterator()` → LSM iterator → memtable adapter → key-value decode path, even for in-memory tables with <1000 rows already held in `DT.Tables[name]`. The LSM path adds 2-3 interface indirections per row (iterator.Next → adapter → Get → decode) that are pure overhead when the data is already a `[]DT.Row` slice. **Fix:** (1) In `SeqScan.Open()` or first `Next()` call, check if the table exists in `DT.Tables` (in-memory) and `len(DT.Tables[name]) < 1000`. (2) If yes, snapshot the row slice and serve `Next()` directly from it, skipping LSM iterator creation. (3) On `Reset()` or `Close()`, clear the slice reference. (4) Verify: `SELECT * FROM t1` on a 20-row table reads directly from `DT.Tables["t1"]` without LSM iterator setup. (5) Add `BenchmarkSelect1_MemtableDirect` vs LSM iterator path. Estimate: ~60 lines, 1 file, 0.25 day. | medium | small | none | SQB/OP/operators.go |
| REQ001466 | tests/sqlcmp/slt | **SLT result comparison: two-phase hash-check — pre-compute expected result hash at parse time, compare actual hash first, skip full `DiffResultSets`/`rowString`/`sort.Slice` on match.** `Runner.runQuery` fully diffs every result set via `DiffResultSets` (which calls `rowString` + `sort.Slice` + string comparison) even on passing queries. For select1's 20-row × 5-col result sets, each full diff is ~2μs; 12K queries × 2μs = 24ms of pure overhead since 99% pass. **Fix:** (1) During `Parse` of each `.test` file, pre-compute the expected hash (`hashValues(sortedRows)`) and store it on the `Record` struct. (2) In `runQuery`, after getting the actual result set, compute hash via same method; if it matches the pre-computed hash, skip `DiffResultSets` entirely. (3) Only fall through to full `DiffResultSets` on hash mismatch (to produce diagnostic diff). (4) Verify: select1-5 pass identically; hash path matches full diff for all query types (rowsort, valuesort, nosort). (5) Add `BenchmarkSLT_Diff_HashVsFull` on select1 showing hash path is ≤1μs vs ~2μs full. Estimate: ~30 lines, 2 files, 0.15 day. | high | small | none | tests/sqlcmp/slt/runner.go, tests/sqlcmp/slt/parser.go |
| REQ001467 | tests/sqlcmp/slt | **SLT runner short-circuit — skip state machine for straight-through scripts (no skipif/onlyif/halt/label).** `Runner.Run()` iterates every record through a `switch rec.Kind` dispatch, `pendingSkip` state tracking, conditional prefix evaluation, and per-record stats aggregation (`r.stats.Passed++/Failed++/Skipped++`). For linear SLT files like select1-5 with zero control-flow directives, this state machine is pure overhead — 12K iterations of `switch Kind + if pendingSkip + if splitRange + 2x profile checks + stats increment`. **Fix:** (1) After `Parse`, scan records once for control-flow record kinds (`RecordSkipIf`, `RecordOnlyIf`, `RecordHalt`, `RecordHashThreshold`, labels). (2) If none found and no range splitting active, bypass `Run()` with a tight loop: `execStmt(rec.SQL)` with a single error counter. (3) The tight loop calls `driver.Exec/Query` directly, skips `pendingSkip`/`haltOnTimeout`/`haltOnFailure`/`splitStartEnd` checks, and counts pass/fail as a batch (one `r.stats.Passed += N` at the end instead of 12K individual increments). (4) Verify: all select1-5 files without control flow produce identical pass/fail stats. (5) Add `BenchmarkSLT_Runner_DirectVsStateMachine` on select1. Estimate: ~80 lines, 1 file, 0.4 day. | high | small | none | tests/sqlcmp/slt/runner.go |
| REQ001468 | SQB/EX | **Plan cache key pre-computation — cache `SerializeKey` result on parsed Record at batch compile time, skip `NormalizeForMemo` + `SerializeKey` on each `planWithCache` call.** REQ001458 batch-precompiles all SQLs before the execution loop. But `planWithCache` still calls `NormalizeForMemo(stmt)` + `SerializeKey(paramStmt)` for each query on first execution — two full AST traversals. For 12K unique SQLs, that's 12K AST serializations even though the batch compile already parsed every SQL. **Fix:** (1) During REQ001458 batch compile, after parsing the SQL into a `PS.Stmt`, compute `pl.SerializeKey(pl.NormalizeForMemo(stmt))` once and store the key string on the `Record` struct (or a per-record execution cache entry). (2) Add `Executor.planWithCachedKey(stmt, key)` that skips the `NormalizeForMemo` + `SerializeKey` steps and goes directly to plan cache lookup + `BuildTree` on miss. (3) Verify: 12K unique SQLs show 1 AST serialization each instead of 2 (parse + key). (4) Add `BenchmarkPlanWithCache_KeyCached` vs `BenchmarkPlanWithCache_KeyLive`. Estimate: ~20 lines, 2 files, 0.1 day. | high | small | REQ001458 | SQB/EX/ex.go, tests/sqlcmp/slt/runner.go |
| REQ001469 | SQB/EX | **Predictive planner shortcut — pattern-match common simple queries (`SELECT *`, `SELECT count(*)`, `SELECT col WHERE col OP lit`, `SELECT col ORDER BY col LIMIT N`) to emit operator tree directly, skipping memo lookup + cost model + `BuildTree` full planning pipeline.** `planWithCache` for a query like `SELECT * FROM t1` or `SELECT count(*) FROM t1` goes through the entire planning pipeline: memo lookup (serialize key, hash lookup), `BuildTree` (cost model, operator construction), `ResolvePlanSlots`. This is deterministic work that produces the same tree every time. **Fix:** (1) Add `planShortcut(stmt PS.Stmt) (DT.Operator, bool)` at the top of `planWithCache`. (2) Pattern-match: `*PS.Select` with star projection + no WHERE/GROUP/HAVING/ORDER/LIMIT/DISTINCT → emit `SeqScan` directly. `COUNT(*)` with star + no GROUP → emit `ConstRow(1)` directly. `Ident/Star WHERE BinaryExpr(col OP literal)` → emit `NewFilter(NewSeqScan, compiledPred)` directly. ORDER BY + LIMIT → emit `NewLimit(NewSort(NewSeqScan))`. (3) For matched patterns, skip `NormalizeForMemo`, `SerializeKey`, plan cache lookup (populate cache for benefit of non-predictive path), cost model walk, and operator tree build. (4) For non-matching queries, fall through to the normal `planWithCache` path unchanged. (5) Verify: select1 queries matching the patterns produce identical results. (6) Add `BenchmarkPlanShortcut_Star`, `BenchmarkPlanShortcut_CountStar`, `BenchmarkPlanShortcut_WhereCol` vs full planning path. Estimate: ~100 lines, 1 file, 0.5 day. | high | small | none | SQB/EX/ex.go |
| REQ001470 | ENG/LS | **Replace `container/heap` with manual min-heap in `shardedIter` — eliminate any-boxing per `Push`/`Pop` on the sharded memtable merge path.** `shardedIter` (`sharded_memtable.go:202-273`) uses `container/heap` on `entryHeap`, boxing every `entryItem` through `interface{}` on every `heap.Push`/`heap.Pop`. The `mergeIterator` was fixed for the same issue (`mergeHeap`, `api.go:343-439`, REQ001261) but `shardedIter` was not. `shardedIter` is hit on every full-table scan over active memtable shards. **Fix:** (1) Replace `entryHeap` + `container/heap` with a manual min-heap on `[]entryItem` (same pattern as `mergeHeap`). (2) Remove `heap.Push`/`heap.Pop` interface boxing. (3) Add per-source ring buffer slots (`sourceKeys`/`sourceVals`) to `shardedIter`, eliminating the `append([]byte(nil), ...)` allocation per `Push` — same pattern as `mergeIterator.copySourceSlot` (`api.go:569-593`). (4) Verify: `BenchmarkSkiplistIterator` heap allocation drops to 0 on the merge path. Estimate: ~80 lines, 1 file, 0.4 day. | high | medium | none | ENG/LS/sharded_memtable.go |
| REQ001471 | ENG/LS | **`loadSSTMeta` page buffer allocation — use `engine.sp` SyncPool instead of per-page `make([]byte, PageSize)`.** `loadSSTMeta` (`engine.go:433`) allocates `make([]byte, PageSize)` for every page read when loading SST index+bloom metadata. For a 4MB SST, this is ~1024 heap allocations. The engine already has `e.sp` (SyncPool from `SP` package) available but unused. Fix: (1) In `loadSSTMeta`, allocate page buffers via `e.sp.Get(PageSize)` / `e.sp.Put()` instead of `make([]byte, PageSize)`. (2) Apply same pattern to any other page-aligned reads in `engine.go`. (3) Verify: SST metadata loading shows zero per-page heap allocations. (4) Add `BenchmarkSSTLoadMeta` measuring allocs/op. Estimate: ~30 lines, 1 file, 0.15 day. | high | small | none | ENG/LS/engine.go |
| REQ001472 | ENG/LS | **`decodeBlock` allocation — pool `restartPoints` and `pairs` slices per block read.** `decodeBlock` (`sst_reader.go:727,738`) allocates `restartPoints := make([]int, restartCount)` and grows `pairs` via `append` on every SST data block read — the hot query path. **Fix:** (1) Add a `sync.Pool` for `restartPoints` slices keyed by typical restart count (64-256 entries). (2) Add a `sync.Pool` for `[]kvPair` slices. (3) In `decodeBlock`, acquire from pool, grow if needed, return to pool after use. (4) In `sstIterator.loadBlock`, return the old pairs slice to the pool before assigning new ones. (5) Measure: `BenchmarkSSTReadBlock` allocs/op drops from 2+N to ≤2. Estimate: ~50 lines, 1 file, 0.25 day. | high | medium | none | ENG/LS/sst_reader.go |
| REQ001473 | ENG/LS | **`engine.Read` lock granularity — snapshot memtable pointers under `RLock`, release before per-memtable `Get` loop.** `engine.Read` (`engine.go:221-236`) holds `e.mu.RLock()` across the active memtable `Get` + all frozen memtable `Get` calls. While each `Get` is fast (skiplist CAS), holding `RLock` delays `flushActiveMemtable` (needs `Lock()`). Fix: (1) Under `RLock`, snapshot the `activeMem` pointer and the length of the `memtables` slice (plus a reference increment if ref-counting is enabled). (2) Release `RLock`. (3) Run the memtable `Get` loop without any lock. (4) Drop references after loop. (5) Verify: concurrent `Write` (triggering flush) does not block behind `Read`. (6) Add `BenchmarkEngine_ReadConcurrentWrite` measuring read latency under write pressure. Estimate: ~60 lines, 1 file, 0.3 day. | high | small | none | ENG/LS/engine.go |
| REQ001474 | ENG/LS | **`engine.MayContain` bloom-only fast-path — skip SST reader construction, open only footer to extract bloom filter.** `MayContain` (`engine.go:460-491`) calls `e.getSSTReader` which reads footer, parses index block, opens bloom data — the full SST metadata path. `MayContain` only needs the bloom filter probe (a few hash computations). **Fix:** (1) Add `mayContainSST(key, fileID, path) bool` that opens the file, reads only the footer (last 44 bytes), extracts bloom offset/size, reads only the bloom data, probes it, and closes. (2) Skip index block parsing, decompressed block cache lookup, and ribbon configuration entirely — none are needed for a bloom-only existence check. (3) Verify: `MayContain` shows 1/10th the allocation of the full SST reader path. Estimate: ~40 lines, 1 file, 0.2 day. | medium | small | none | ENG/LS/engine.go |
| REQ001475 | MEM/BF | **Buffer pool `Get()` allocates `Page` struct on every cache hit — pool or return by value.** `bf.go:Get()` returns `*Page` on every call, escaping to heap. The `Page` struct (ID uint64 + Data []byte + Dirty bool = ~40 bytes) is heap-allocated on every buffer pool hit — the dominant storage path. For dense scans touching thousands of pages, this creates measurable GC pressure. **Fix:** (1) Add a `sync.Pool` for `Page` structs in the shard. On cache hit, acquire from pool, populate, return. Caller returns via `Unpin` or a dedicated `PagePool.Put`. (2) Alternatively, embed `Page` fields directly in `bufferSlot` and return a pointer into the slot (avoids both allocation and the separate Page struct). (3) Measure: `BenchmarkBufferPool_GetHit` allocs/op drops from 1 to 0. Estimate: ~40 lines, 1 file, 0.2 day. | high | small | none | MEM/BF/bf.go |
| REQ001476 | MEM/BF | **`Get()` + `Pin()` + `Unpin()` do 3× hash table lookups per page access — embed pin count in slot, skip Pin/Unpin lookup.** `Get()` (`bf.go:187`) calls `b.sbp.Slot(blockID)` to find the slot. Then `Pin(page)` (`bf.go:334-341`) calls `b.sbp.Slot(page.ID)` again — second hash table lookup + RLock. `Unpin()` (`bf.go:344-350`) does a third. Total: 3× hash lookups + 3× lock acquisitions per page access. **Fix:** (1) Move `pinCount` and `dirty` from `bufferSlot` into the returned `Page` struct (or return the `*bufferSlot` directly). (2) `Pin()` becomes `page.pinCount.Add(1)` — no lookup. (3) `Unpin()` becomes `page.pinCount.Add(-1)` — no lookup. (4) This also makes Pin/Unpin safe without holding the shard lock. Estimate: ~50 lines, 1 file, 0.25 day. | high | small | none | MEM/BF/bf.go |
| REQ001477 | TXN/VL | **`Unlock()` locks all 64 shards and scans all locks per commit — track per-txn lock set instead.** `lock.go:Unlock(txnID)` acquires locks on all 64 shards (`for i := range 64 { shard.mu.Lock() }`) and iterates every entry in every shard looking for locks held by `txnID`. This is O(total locks) per commit — for a transaction that holds 1 lock, the cost is scanning 64 shards × N entries. **Fix:** (1) Track per-transaction lock references as `[]lockRef{shardIdx, hash}` on the `Tx` struct, populated during `LockWithTimeout`. (2) In `Unlock`, iterate only the tracked refs, lock only the referenced shards, unlock only those entries. (3) Verify: single-row UPDATE commit shows O(1) lock scanning instead of O(all locks). (4) Add `BenchmarkTxnUnlock_SingleRow` vs `BenchmarkTxnUnlock_ManyTxns` measuring shard scan cost. Estimate: ~80 lines, 2 files, 0.4 day. | high | medium | none | TXN/VL/lock.go, TXN/VL/protocol.go |
| REQ001478 | TXN/VL | **`LockWithTimeout` uses `time.After()` — timer leaks until expiry on contended lock grant.** `lock.go:227` uses `select { case <-node.done: ... case <-time.After(timeout): ... }`. When the lock is granted before timeout (the common case), the `time.After` timer is not stopped — it remains live until the timeout fires (default 5s). Under high lock contention, this accumulates N×5s of live timers. **Fix:** (1) Replace `time.After(timeout)` with `time.NewTimer(timeout)` + `defer timer.Stop()`. (2) On the `<-node.done` path, call `timer.Stop()` + drain `timer.C` if needed. (3) Verify: `TestLockContention_GrantBeforeTimeout` shows zero timer leakage. Estimate: ~20 lines, 1 file, 0.1 day. | high | small | none | TXN/VL/lock.go |
| REQ001479 | TXN/VL | **Read path scans writeSet linearly — O(len(writeSet)) per version chain node.** `protocol.go:108-114` iterates the entire `writeSet` slice linearly to check if a key belongs to the current transaction. For transactions with large write sets (bulk INSERT/UPDATE), each read that encounters a version chain node does O(len(writeSet)) comparison. **Fix:** (1) Add a `map[uint64]struct{}` fast-lookup alongside the `writeSet` slice. (2) On `Write()`, add to both slice and map. (3) On read, check the map first (O(1)) before falling back to linear scan for non-trivial write set sizes. (4) Verify: `BenchmarkTxnRead_LargeWriteSet` shows constant-time writeSet membership check. Estimate: ~30 lines, 1 file, 0.15 day. | high | small | none | TXN/VL/protocol.go |
| REQ001480 | WAL/WR | **`sendAppend` allocates a `chan cmdResult` per `Append()` call — pool or use lock-free notification.** `wr.go:232-236` creates `make(chan cmdResult, 1)` on every WAL `Append()`. The channel sends a `cmd` to the writer goroutine and blocks for the reply. This is one allocation per WAL write — dominant cost for single-row INSERT workloads. **Fix:** (1) Use `sync.Pool` to recycle `chan cmdResult`: on `sendAppend`, acquire from pool; in the writer goroutine, after replying, return the channel to pool. (2) Alternatively, replace the channel with `sync.Mutex` + `sync.Cond`: the caller blocks on `Cond.Wait()` and the writer broadcasts on `Cond.Broadcast()` after processing — zero allocations per op. Estimate: ~60 lines, 1 file, 0.3 day. | high | small | none | WAL/WR/wr.go |
| REQ001481 | WAL/FL | **`syncDir()` opens and closes directory fd on every `Sync()` — cache fd in flusher.** `fl.go:169-176` calls `syscall.Open(f.dir, syscall.O_RDONLY, 0)` and `syscall.Close(fd)` on every flush sync. Each commit or checkpoint triggers `syncDir()`, adding 2 extra syscalls per transaction. **Fix:** (1) Open the directory fd once in `New()` and store it in the `flusher` struct. (2) In `syncDir()`, reuse the cached fd. (3) Close the fd in `Close()`. (4) Verify: `BenchmarkFlushSync_SyscallCount` shows 0 syscalls for directory sync after startup. Estimate: ~15 lines, 1 file, 0.1 day. | high | small | none | WAL/FL/fl.go |
| REQ001482 | SQB/OP + ENG/LS | **Dense RowID + array-based hash table for hash join build side — eliminate per-entry hashing + allocation on join probe.** Current `VectorizedHashJoin` uses a generic `HashTable` (`UT/hashtable.go`) with open-addressing and string-keyed entries. Every probe computes `hash(key_bytes)` and compares key bytes. If every row has a dense `int64` RowID (monotonically increasing since DB creation), the hash join build side becomes a `[]int` array indexed by `RowID % N` — zero hashing, zero key comparison, zero per-entry allocation (just store the RowID). **Fix:** (1) Add a `RowID` field to `DT.Value` or to the storage layer in ENG/LS — auto-incrementing, pinned at row creation, never reused. (2) In `VectorizedHashJoin`'s build phase, use a `[]int` lookup table: `lookup[rowID & mask] = buildRowIndex` — no hashing, no key comparison. (3) For non-RowID joins (multi-column key, string key), fall back to the existing `HashTable`. (4) Planner detects equi-join on primary key → emits RowID-optimized join. (5) Verify: `BenchmarkSelect5_Join4` shows 2× throughput for PK equi-joins vs string-keyed hash table. (6) Add `BenchmarkHashJoin_RowIDArray` vs `BenchmarkHashJoin_StringKey`. Estimate: ~200 lines, 3 files, 1 day. | high | medium | REQ001213 | SQB/OP/op_vec_join.go, UT/hashtable.go, ENG/LS/engine.go |
| REQ001483 | SQB/EX | **Plan cache with `uint64` hash key — replace `SerializeKey` AST serialization with a fast hash of the memo-normalized AST.** `planWithCache` (`ex.go:571-606`) calls `NormalizeForMemo(stmt)` (AST walk) + `SerializeKey(paramStmt)` (another AST walk to produce a string key). The cache map is `map[string]*planCacheEntry` — string comparison is O(key length). For 12K unique SQLs in select1, every query pays the full serialization cost with zero cache hits. **Fix:** (1) Add `hashMemoKey(stmt PS.Stmt) uint64` that walks the memo-normalized AST once and produces a 64-bit hash (e.g., `hash/fnv` or `xxhash`). (2) Change `planCache` entries map from `map[string]` to `map[uint64]`. (3) On cache hit, verify with pointer equality or a cheap string comparison to handle collisions. (4) On cache miss, the hash is already computed — use it instead of `SerializeKey`. (5) Verify: `BenchmarkPlanWithCache_Miss` shows 1 AST walk instead of 2; `BenchmarkPlanWithCache_Hit` shows `map[uint64]` lookup in 5ns vs `map[string]` in 50ns+ for complex queries. (6) All select1-5 tests pass. Estimate: ~60 lines, 2 files, 0.3 day. | high | small | none | SQB/EX/ex.go, SQF/PL/memo.go |
| REQ001484 | ENG/LS | **Page-allocated B+-tree memtable — replace skipList for dense-range scan workloads.** Current skipList (`skiplist.go`) allocates one `node` per entry (12 `atomic.Pointer[maxLevel]` = 96 bytes + `atomic.Value` + `sync.Mutex` = ~160 bytes per entry). Range scan follows `next[0]` pointers — one cache miss per entry. A B+-tree with 64 entries per leaf node stores 64 keys per 4KB page, reducing per-entry allocation by 64× and per-entry cache misses by a similar factor. SkipList insert is O(log N) with CAS, B-tree insert is O(log N) with node splits — comparable write cost. **Fix:** (1) Implement a B+-tree with configurable page size (default 4KB, fits one OS page). Internal nodes store key ranges, leaf nodes store sorted `[]struct{key, value []byte}`. (2) Implement `Insert(key, value)`: find leaf via binary search on internal nodes, insert into sorted leaf array, split if full. (3) Implement `Find(key)`: binary search on leaf. (4) Implement `Iterator()`: depth-first traversal, yields keys in sorted order. (5) Replace `skipList` usage in `memtable.go` with B+-tree. (6) Wire into `shardedMemtable` — each shard gets its own B+-tree. (7) Verify: `BenchmarkSkiplistIterator` on 100K entries shows 64× fewer cache misses. `BenchmarkSkiplistInsert` shows comparable or better throughput. (8) All existing memtable tests pass (insert, find, scan, freeze, flush). Estimate: ~400 lines, 2 files, 2 days. | high | large | none | ENG/LS/btree.go (new), ENG/LS/memtable.go |
| REQ001485 | SYS/SE | **Read-only query shortcut — bypass MVCC snapshot, readset tracking, and lock checking for read-only sessions.** Every `SELECT` statement goes through the full MVCC machinery: snapshot creation (`vl.Manager.Begin`), per-key version chain traversal, writeSet membership check, readset tracking. For a connection that never writes (`PRAGMA query_only = 1`), all of this is waste — there are no concurrent writers under the single-writer engine, so the snapshot is always the latest committed state. **Fix:** (1) On session creation or `PRAGMA query_only = 1`, set a `readOnly` flag on the session. (2) For read-only sessions, `Exec`/`Query` skip `BeginTxn` and use a direct executor path: `e.exe.QueryAll(ctx, sql)` without snapshot, without `vl.Manager`, without readset tracking. (3) WAL and lock interactions are skipped entirely for read-only sessions. (4) Verify: read-only benchmark (`BenchmarkSelect1_ReadOnly`) shows throughput equal to the raw executor path, not the transaction manager path. (5) Add `BenchmarkSession_ReadOnly_Query` vs `BenchmarkSession_ReadWrite_Query`. Estimate: ~80 lines, 2 files, 0.4 day. | high | medium | none | SYS/SE/se.go, SYS/SY/sy.go |
| REQ001486 | WAL/WR + SQB/WT | **Batch INSERT API — `ExecBatch(sql []string)` amortizes driver + WAL overhead over N statements.** Each individual INSERT goes through: parse → plan → WAL begin → write → WAL commit → fsync. For bulk INSERT (select1 setup: 20 INSERTs), this is 20 full round trips through the engine. SQLite's `BEGIN; INSERT...; INSERT...; COMMIT;` batching is the common mitigation, but requires explicit transaction management from the caller. **Fix:** (1) Add `Session.ExecBatch(ctx, sqls []string) ([]Result, error)` that parses + plans all statements once, begins one implicit transaction, executes all statements, commits once. (2) In the driver layer, add `Conn.ExecBatch(ctx, sqls []string) ([]Result, error)` that feeds into `Session.ExecBatch`. (3) If any statement fails, rollback the entire batch. (4) Verify: `BenchmarkInsertBatch_20Rows` shows ≥5× throughput vs 20 individual `Exec` calls. (5) Add `BenchmarkInsertBatch` vs `BenchmarkInsertSerial`. Estimate: ~120 lines, 3 files, 0.6 day. | high | medium | none | SYS/SE/se.go, driver/conn.go, SQB/WT/writers_dml.go |
| REQ001487 | FIL/DF + ENG/LS | **SST sequential scan readahead — issue `fadvise(FADV_WILLNEED)` or io_uring batch reads for predictable access patterns.** Sequential scan over an SST file reads blocks one at a time: `sstIterator.Next()` → `loadBlock()` → `readBlock()` → `readRaw()` → `Pread()`. Each `Pread` is a separate system call (or io_uring SQE). For a full-table scan reading 1000 SST blocks, this is 1000 I/O operations. **Fix:** (1) In `sstReader`, add `prefetchRange(blockStart, blockEnd)` that issues `fadvise(FADV_WILLNEED, offset, length)` on the fd (or `POSIX_FADV_WILLNEED`) to trigger kernel readahead. (2) In `sstIterator`, when a sequential access pattern is detected (consecutive block reads), call `prefetchRange(currentBlock+1, currentBlock+16)` to pipeline the next 16 blocks. (3) For io_uring path, submit 16 read SQEs in batch instead of 1 at a time. (4) Verify: `BenchmarkSST_FullScan` shows 2-3× throughput improvement for spinning disk and 1.5× for NVMe. (5) Add `BenchmarkSST_FullScan_Readahead` vs baseline. Estimate: ~80 lines, 2 files, 0.4 day. | high | medium | none | FIL/DF/df.go, ENG/LS/sst_reader.go |
| REQ001488 | MEM/SP + SYS/SY | **Extend huge page (2MB) support to WAL, page cache, and SST block cache — beyond the current buffer pool only.** `AP.Options.EnableHugePages` is currently only consumed by the buffer pool (`bf.go`). The WAL writer, SST block cache, and page cache all allocate large contiguous memory regions through regular `make([]byte, size)` — using 4KB pages. For a 256MB WAL buffer, this creates ~65K TLB entries instead of ~128 with 2MB pages. **Fix:** (1) Extend `sp.SyncPool` with a `GetHuge(size int) []byte` that attempts `hugetlbfs` or `mmap(MAP_HUGETLB)` allocation, falling back to regular allocation on failure. (2) In WAL writer (`wr.go`), use `sp.GetHuge` for the WAL buffer instead of `make([]byte, size)`. (3) In page cache (`cache.go`), use `sp.GetHuge` for SST data storage. (4) In block cache (`cache.go`, ENG/LS), use `sp.GetHuge` for decompressed block cache entries. (5) Verify: `BenchmarkTLBMiss_WithHugePages` shows 512× fewer TLB misses on large allocation benchmarks. (6) All existing tests pass with `EnableHugePages = true`. Estimate: ~100 lines, 4 files, 0.5 day. | medium | medium | none | MEM/SP/sp.go, WAL/WR/wr.go, ENG/LS/cache.go, FIL/DF/cache.go |
| REQ001489 | all packages | **Guard all `slog.Warn`/`slog.Error`/`slog.Debug` calls with `logger.Enabled(ctx, level)` to eliminate allocation when the handler drops the log.** Go's `slog` allocates on every `slog.Warn`, `slog.Error`, `slog.Debug` call — creating `slog.Record` structs, allocating attribute slices — even when the underlying handler is configured to drop that level. For example, `engine.go:272` calls `e.log.Warn(...)` inside an error path that fires on every SST miss — the record is created and formatted before the handler can reject it. **Fix:** (1) Add a `logWarn(l *slog.Logger, msg string, args ...any)` helper that checks `l.Enabled(ctx, slog.LevelWarn)` before formatting. (2) Same for `logError`, `logDebug`, `logInfo`. (3) Replace all `l.Warn(...)` / `l.Error(...)` / `l.Debug(...)` calls in hot paths (ENG/LS, FIL/DF, WAL/WR, TXN/VL, SYS/SY) with the guarded helpers. (4) Verify: with `LogLevel = slog.LevelInfo`, all `Debug` and `Warn` calls show 0 heap allocations in `go test -benchmem`. Estimate: ~80 lines, 6+ files, 0.4 day. | medium | small | none | ENG/LS/engine.go, FIL/DF/df.go, WAL/WR/wr.go, TXN/VL/lock.go, SYS/SY/sy.go |
| REQ001490 | SQO/PF | **Redundant predicate elimination — remove conjuncts that are logically implied by other conjuncts in the same filter.** `WHERE a > 10 AND a > 5` → `WHERE a > 10`. `WHERE a = 5 AND a > 3` → `WHERE a = 5`. `WHERE a BETWEEN 1 AND 10 AND a > 3` → `WHERE a BETWEEN 1 AND 10`. Common in ORM-generated queries and nested view expansion. **Fix:** (1) Add `RedundantPredicateEliminationPass` that splits filter predicates into conjuncts, checks pairwise implication: if `p ⇒ q` (p logically implies q), remove q. (2) Implication rules: for same-column comparisons, `a = c ⇒ a ≥ c ∧ a ≤ c ∧ a > c-1 ∧ a < c+1`; `a BETWEEN x AND y ⇒ a ≥ x ∧ a ≤ y`. (3) Handle `a = c` as the strongest form — implies any comparison on the same column. (4) Handle `a IN (c1, c2, c3)` → `a = c1 OR a = c2 OR a = c3` implication rules. (5) Verify: `WHERE a > 10 AND a > 5` reduces to `WHERE a > 10`. (6) Add `TestRedundantPredicate_SameColumn`, `TestRedundantPredicate_Between`, `TestRedundantPredicate_NoOp`. Estimate: ~120 lines, 1 file, 0.5 day. | high | medium | none | SQO/PF/predicate_elimination.go (new) |
| REQ001491 | SQO/PF | **OR-to-IN expansion — convert `a = 1 OR a = 2 OR a = 3` into `a IN (1,2,3)` to enable index range scan with IN-list.** `IndexSelectionPass` (`index_selection.go`) checks if the leading index column is referenced in the predicate, but only handles simple equality predicates — `a = 1` enables a point lookup. `a IN (1,2,3)` also enables index range scan (multiple point lookups) but is not produced by the parser from OR chains. **Fix:** (1) Add `OrToInExpansionPass` that walks filter predicates and detects `BinaryExpr(OR)` chains where both sides are `a = lit` on the same column. (2) Collect all literals from the chain and rewrite as `a IN (lit1, lit2, lit3)`. (3) Handle nested OR: `(a = 1 OR a = 2) OR a = 3` → `a IN (1,2,3)`. (4) Handle mixed column/constant: `a = 1 OR b = 2` stays unchanged (different columns). (5) Verify: `WHERE a = 1 OR a = 2 OR a = 3` rewrites to `WHERE a IN (1,2,3)`. (6) Add `TestOrToIn_Simple`, `TestOrToIn_Nested`, `TestOrToIn_DifferentColumns_NoOp`. Estimate: ~80 lines, 1 file, 0.4 day. | high | small | none | SQO/PF/or_to_in.go (new) |
| REQ001492 | SQO/PF | **Sort elimination for pre-sorted inputs — skip ORDER BY when the input is already sorted by the requested key.** `SELECT * FROM t ORDER BY PK` — the SeqScan on a primary-key ordered table already produces rows in PK order. No sort needed. `SELECT * FROM t WHERE a > 5 ORDER BY a` — if an index on `a` is used, the IndexScan output is already sorted by `a`. **Fix:** (1) Add `SortEliminationPass` that checks each `Sort` operator's child: if the child is a `SeqScan` on a table where the sort key matches the primary key column order, remove the `Sort`. (2) If the child is an `IndexScan` and the sort key matches the index column prefix, remove the `Sort`. (3) If the child is already a `Sort` with compatible ordering, merge or remove the outer sort. (4) Verify: `SELECT * FROM t1 ORDER BY rowid` on a heap table shows no Sort operator in EXPLAIN. (5) Add `TestSortElimination_PKOrder`, `TestSortElimination_IndexOrder`, `TestSortElimination_NonMatching_NoOp`. Estimate: ~100 lines, 1 file, 0.5 day. | high | medium | none | SQO/PF/sort_elimination.go (new) |
| REQ001493 | SQO/PF | **Implicit cast elimination — fold `WHERE int_col = '42'` into `WHERE int_col = 42` at plan time, avoiding per-row string-to-int conversion.** The parser preserves literal types: `42` is `*PS.NumberLiteral`, `'42'` is `*PS.StringLiteral`. When a comparison between `int_col` and `'42'` reaches the executor, every row evaluation calls `castToInt('42')` — a string parse. With the column type available from the catalog, the optimizer can fold the string literal to the correct type once. **Fix:** (1) Add `ImplicitCastEliminationPass` that uses `ctx.Catalog` to resolve column types for each `BinaryExpr` comparison. (2) If one side is a column reference with known type and the other is a literal with a different type, convert the literal to the column's type. (3) Handle: `int_col = '42'` → `int_col = 42`, `text_col = 42` → `text_col = '42'`, `float_col = '3.14'` → `float_col = 3.14`. (4) Handle IN-list: `int_col IN ('1', '2', '3')` → `int_col IN (1, 2, 3)`. (5) Verify: `SELECT * FROM t1 WHERE a > '150'` shows no string-to-int conversion in the eval path. (6) Add `TestImplicitCast_IntText`, `TestImplicitCast_INList`, `TestImplicitCast_NoColumnType_NoOp`. Estimate: ~100 lines, 1 file, 0.5 day. | high | small | REQ001490 | SQO/PF/cast_elimination.go (new) |
| REQ001494 | SQO/PF + SQB/OP | **Dynamic bloom filter pushdown in hash join — after building the hash table, generate a bloom filter and push it to the probe-side scan to skip non-matching rows early.** In a hash join, the probe side is fully scanned. For selective joins (e.g., orders JOIN customers WHERE customer.id = 123), most probe-side rows don't match. A bloom filter built from the build-side keys lets the probe-side scan skip rows that cannot match. **Fix:** (1) Add a `BloomFilter` type in `UT` or `ENG/LS` — a simple array of bits with two hash functions (same as SST bloom filter in `sst_reader.go:358-378`). (2) In `VectorizedHashJoin`'s build phase, after building the hash table, also build a bloom filter from all build-side keys. (3) The bloom filter is attached to the probe-side `BatchProducer` as a `ProbeFilter`. (4) In `VectorizedSeqScan`/`VectorizedFilter`, before fetching a batch, check the bloom filter against the scan's range — if the bloom says "no match", skip the entire batch. (5) For point-lookup joins (single-row build side), the bloom filter is always a single probe — skip the entire probe scan trivially. (6) Verify: `TPC-H Q6`-style join (small build side, large probe side) shows 2-10× probe-side scan reduction. (7) Add `BenchmarkHashJoin_BloomFilter` vs `BenchmarkHashJoin_NoBloom`. Estimate: ~200 lines, 3 files, 1 day. | high | medium | REQ001213 | SQO/PF/bloom_pushdown.go (new), SQB/OP/op_vec_join.go, UT/bloom.go (new) |
