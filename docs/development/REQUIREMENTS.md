## TBD
| ID | Subsystem | Requirement | Priority | Effort | Deps | Touches |
| --- | --- | --- | --- | --- | --- | --- |
| REQ001314 | SQB/EX + ENG | **ANALYZE: per-column NDV + rowcount + null count + min/max, persisted to catalog (covers REQ001315, REQ001316, REQ001317, REQ001318).** All four metrics are computed in a single scan, stored in `razor_stat1` catalog table, and loaded into `pl.StatsCatalog` on Engine.Open. NDV is the cornerstone of selectivity estimation (already used by REQ001218 OR-to-IN and REQ001252 stats propagation). rowcount/null_count are required by selectivity estimators. min/max enable the stats-propagation rule that derives `t2.c ∈ [min(t1.c), max(t1.c)]` from join equality. **Fix:** (1) Add ANALYZE statement dispatch in SQB/EX. (2) On ANALYZE [table], run a single full or sampled scan (full for tables < 1M rows, sampled 1% reservoir otherwise) that computes for each column: NDV, rowcount, null_count, min, max. (3) Define `razor_stat1` catalog table: `(table TEXT, col TEXT, ndv INT, rowcount INT, null_count INT, min BLOB, max BLOB)`. (4) ANALYZE writes via the same writer path as user tables; create razor_stat1 on first ANALYZE if absent. (5) In SYS.Open, after loading catalog, SELECT * FROM razor_stat1 and populate the in-memory `pl.StatsCatalog`. (6) For nullable columns, NULL is excluded from min/max. (7) Add `TestAnalyze_ComputesNDV`, `TestAnalyze_LargeTable_Sampling`, `TestAnalyze_ComputesRowCount`, `TestAnalyze_ComputesNullCount_NotNullCol`, `TestAnalyze_ComputesMinMax`, `TestAnalyze_NullExcludedFromMinMax`, `TestAnalyze_PersistsAcrossRestart`, `TestStats_WiredOnOpen`, `TestStats_Queries_UseLoadedNDV`. Estimate: ~350 lines, 4 files, 1.6 days. | high | medium | none | SQB/EX/analyze.go (new), ENG/LS/iter.go, SQB/WT/writers_admin.go, ENG/CT/catalog.go, SQB/EX/ex.go, SYS/SY/sy.go |
| REQ001423 | ENG/LS + SQB/EX | **B-tree / LSM block-level stats: min, max, NDV for range query planning.** Range scans (e.g. `WHERE age > 30 AND age < 50`) currently traverse the full SeqScan. LSM blocks have per-column min/max that can be used for early block pruning. **Fix:** (1) In ENG/LS SST block stats, expose per-column `min`/`max` during block construction. (2) In SeqScan, when a range predicate is present, skip blocks where the predicate's range is disjoint from the block's column min/max. (3) Expose via `TableStats` column-level `min`/`max` in catalog. (4) Add `TestSeqScan_BlockPruning_RangePredicate` verifying skipped blocks. Estimate: ~120 lines, 2 files, 0.75 day. | high | small | REQ001420 | ENG/LS/sst_block.go, SQB/OP/operators.go |
| REQ001567 | SQB/EV | **Pre-allocate function argument buffer in Project/Filter.** `eval.go:1552,1567` — every datetime/json function call allocates `args := make([]any, len(e.Args))`. **Fix:** (1) Add `fnArgBuf []any` field to Project and Filter operators. (2) Initialize in constructor with capacity = max expected function args (typically 5-10). (3) Pass `fnArgBuf` into eval path for function calls. (4) Add `BenchmarkScalarFunc_NoAlloc`. Estimate: ~80 lines, 2 files, 0.5 day. | high | small | none | SQB/EV/eval.go, SQB/OP/operators.go |
| REQ001568 | SQB/EV | **Cache inner table column set in correlated subquery resolution.** `eval.go:129` — `set := make(map[string]struct{}, len(cols))` allocated per correlated subquery evaluation in `extractCorrelatedColumns`. **Fix:** (1) Extend `subqueryColRefCache` to store inner table column set alongside correlated columns. (2) Cache key includes subquery pointer + inner table name. (3) Add `BenchmarkCorrelatedSubquery_NoAlloc`. Estimate: ~60 lines, 1 file, 0.25 day. | high | small | REQ001470 | SQB/EV/eval.go |
| REQ001579 | SQB/EX | **Pre-allocate params slice in ExecContext.** `propagateParams()` in ex.go allocates `asAnySlice(args)` creating `[]any` copy every query execution. **Fix:** (1) Add `paramBuf []any` field to `ExecContext`. (2) Initialize with capacity in `QueryAll`/`Exec` caller. (3) Reuse via `paramBuf = paramBuf[:0]` per execution. (4) Add `BenchmarkExecContext_NoParamAlloc`. Estimate: ~40 lines, 1 file, 0.25 day. | medium | small | none | SQB/EX/ex.go |
| REQ001580 | SQB/EX | **Pre-allocate Value slice from any params in ExecContext.** `valueFromAnySlice` in ex.go allocates `[]Value` for every parameterized query execution. **Fix:** (1) Add `valueParamBuf []Value` field to `ExecContext`. (2) Initialize with capacity = max expected params. (3) Reuse via `valueParamBuf = valueParamBuf[:0]` per execution. (4) Add `BenchmarkExecContext_NoValueParamAlloc`. Estimate: ~40 lines, 1 file, 0.25 day. | medium | small | none | SQB/EX/ex.go |
| REQ001582 | SQB/UT | **Batch.GetBatch must clear stale column names from pooled batches.** Surfaced during REQ001581 activation (select4 vec transform fix). Aggregate queries like `SELECT COUNT(*) FROM users` returned `cols=[count, name]` instead of `cols=[count]` because `GetBatch` from the pool only reset `Type`/`Data`/`Nulls` on the first `cols` columns, but the `Name` field was never cleared. A previous owner (e.g. a 2-column SeqScan) left `Name` on column 1, and `ColNames()` (`batch.go:613`) included it because `Type != 0` was not the only gate — `Name != ""` combined with `Type == 0` also triggered inclusion. **Fix:** (1) Clear `b.Cols[i].Name = ""` for i in `[0, cols)` in `GetBatch`. (2) Clear `b.Cols[i].Name = ""` for i in `[cols, MaxColumns)` to prevent stale names from appearing in the trailing pool region. (3) Add `TestBatch_GetBatch_ClearsStaleColumnNames`. Estimate: ~10 lines, 1 file, 0.1 day. | high | small | REQ001581 | SQB/UT/batch.go |
| REQ001587 | SQB/EX | **Remove `hasJoinOp` gate in `transformRoot` — enable vectorized execution for ALL plans.** Currently `transformRoot` only unwraps `AdaptiveOp` for plans containing HashJoin/NestedLoopJoin/MergeJoin. All other operators (Filter, Project, Sort, Aggregate, SeqScan, Limit, Distinct, TopNSort) have fully functional vectorized implementations but are blocked by the gate. The latent bugs that motivated the gate (TestQueryStreaming, TestPreparedCache_HitRate, TestDS_InMemoryBasic, TestCRUD_AggregateCount) have been fixed by REQ001581-REQ001586. **Fix:** (1) Remove the `hasJoinOp` check from `transformRoot` — always call `transformOp(aop.Inner)`. (2) Run full `./before-commit-cases.sh` to verify no regression. (3) Benchmark SLT select1/2/3/4 wall time delta. Estimate: ~10 lines, 1 file, 0.1 day. | high | small | REQ001581, REQ001582, REQ001583, REQ001584, REQ001586 | SQB/EX/vec_transform.go |
| REQ001588 | SQB/OP | **Vec Sort: streaming top-N sort instead of full materialization.** `VectorizedSort.NextBatch` currently drains all child rows into `s.cols` before sorting (`Phase 1: materialize all rows`). For `ORDER BY col LIMIT N` queries, this wastes memory and time sorting rows that will be discarded. **Fix:** (1) Add `VectorizedTopNSort` that uses a min-heap of size N (already exists as `OP.TopNSort` but row-based; convert to batch). (2) Detect `Sort + Limit` pattern in the planner and route to `VectorizedTopNSort`. (3) When `s.numRows` exceeds available memory budget, switch to external merge sort. Estimate: ~150 lines, 2 files, 1 day. | medium | medium | REQ001587 | SQB/OP/sort_vec.go, SQB/EX/vec_transform.go |
| REQ001589 | SQB/OP | **Vec Filter: range predicate pre-filter via block-level min/max.** Current `VectorizedFilter` only has IN-list bloom filter (REQ001631). Range predicates like `WHERE x > 10` scan all rows without pre-filtering. LSM blocks have per-column min/max that can be used for early block skipping during SeqScan. **Fix:** (1) In `VectorizedSeqScan.NextBatch`, when a range predicate is present, check the block's column min/max before decoding. (2) Skip blocks where the predicate range is disjoint. (3) Wire predicate analysis from `VectorizedFilter` to the scan. Estimate: ~80 lines, 2 files, 0.5 day. | medium | medium | REQ001423 | SQB/OP/operators_vec.go, SQB/OP/operators.go |
| REQ001590 | SQB/OP | **Vec HashJoin: Swiss table / Robin Hood hash table for probe path.** Current `VectorizedHashJoin` uses open-addressing with linear probing (`HashTable.Lookup` in `hashtable.go`). This has poor cache locality and high collision cost for high-load-factor tables. Swiss table (used by Google's Abseil) or Robin Hood hashing would improve probe throughput by 10-20% for large hash tables. **Fix:** (1) Implement a new `SwissHashTable` in `SQB/UT` with 16-way SIMD-friendly probing. (2) Replace `HashTable` in `VectorizedHashJoin.buildHashTable` with the new implementation. (3) Keep the existing `HashTable` for small tables (< 64 entries) where open-addressing is faster. Estimate: ~250 lines, 2 files, 1.5 days. | low | high | REQ001587 | SQB/UT/hashtable.go, SQB/OP/op_vec_join.go |
| REQ001591 | SQB/OP | **Vec Project: compile expression chains to batch evaluators.** Current `VectorizedProject.NextBatch` evaluates each expression via `EV.EvalBatchExpr` which interprets the expression tree per batch. For projections with many columns or complex expressions, this interpretation overhead dominates. **Fix:** (1) Add a `compileProjectExpr` function that compiles simple expressions (column refs, arithmetic, string ops) into a fast-path batch evaluator. (2) Fall back to `EvalBatchExpr` for complex expressions (subqueries, CASE, function calls). (3) Use the compiled evaluator when all expressions in the projection are simple. Estimate: ~120 lines, 2 files, 0.75 day. | low | medium | REQ001587 | SQB/OP/operators_vec.go, SQB/EX/vec_transform.go |
| REQ001632 | SQB/DT | **AllocRow returns `*Row` — heap-allocates every Row struct.** `arena.go:103` returns `&Row{...}`, forcing a heap allocation per decoded row. The caller (`decodeRowBuffered`) immediately dereferences `*row`. The Data field is already arena-backed (zero-alloc), but the Row wrapper itself escapes. pprof shows 3.27M alloc objects (11.73%) and 499MB (12.66%) in the select1 ThroughputDirect benchmark. **Fix:** (1) Change `AllocRow` signature from `*Row` to `Row` (return by value). (2) Update all callers to pass `&row` to `DecodeRowInto`/`DecodeRowSubsetInto` instead of the pointer. (3) The Row struct has only slice/map headers (reference types) — copying them is a memcpy, not deep-copy. (4) Add `BenchmarkSelect1_AllocRow_NoHeap` verifying zero allocs per row decode. Estimate: ~40 lines, 2 files, 0.25 day. | high | small | none | SQB/DT/arena.go, SQB/OP/operators.go |
| REQ001633 | SQB/EX | **Remove goroutine streaming path for small aggregate/sort queries.** `stream.go:43-52` — `isEligibleForSyncStream` rejects queries with `hasComplexOperator` (Aggregate, Sort, Join), forcing the goroutine+channel streaming path even for 20-row result sets. pprof shows `QueryStreamCompiled.func2` (goroutine closure) accounts for 383MB (22.25% of alloc space) in select1 count/order/multi_col queries. The sync stream path (`streamFromOperator`) already handles small results without channel overhead. **Fix:** (1) Remove or relax the `hasComplexOperator` gate when `estimatedRows < syncStreamRowThreshold`. (2) Alternatively, always use sync stream for queries with estimated rows ≤ 100, regardless of operator complexity. (3) Add `BenchmarkSelect1_Count_NoGoroutine` and `BenchmarkSelect1_Order_NoGoroutine` verifying the sync path is taken. Estimate: ~30 lines, 1 file, 0.25 day. | high | small | none | SQB/EX/stream.go |
| REQ001634 | SQB/OP | **Sort materialization: in-place sort for small N, eliminate redundant `reordered` copy.** `intermediate_sort.go:95,185,253` — For `ORDER BY a` with 20 rows, Sort allocates `buf` growslice + `keyAccess` + `flatIntKeys` + `intKeyCache` + `indices` + `reordered` (copies entire buffer). pprof shows Sort.Next at 580MB (14.7% of alloc space). The `reordered` slice is especially wasteful — it duplicates `buf` just to apply index permutation. **Fix:** (1) For N ≤ 64, use a stack-allocated `[64]int` index array instead of `make([]int, n)`. (2) Sort indices in-place, then apply permutation by swapping `buf` elements directly (no `reordered` copy). (3) For the int-key fast path, extract keys into a flat `[]int64` without the intermediate `intKeyCache` slice-of-slices. (4) Add `BenchmarkSort_SmallN_NoReordered` verifying eliminated allocs. Estimate: ~100 lines, 1 file, 0.5 day. | high | medium | none | SQB/OP/intermediate_sort.go |
| REQ001635 | SQB/OP | **Pool SeqScan key buffer — eliminate per-row `make([]byte, len(key))`.** `operators.go:671` — `make([]byte, len(s.currentKey))` deep-copies the iterator key on every row. Then `mergeIterator.Next` already copied it into `mi.curKey` (line 753), so the key is copied twice. pprof shows 4.7M alloc objects (14.2% of alloc objects) in the select1 benchmark. **Fix:** (1) Add `keyBuf []byte` field to SeqScan. (2) In `nextFromStore`, grow `keyBuf` only when needed (`if cap(s.keyBuf) < len(s.currentKey) { s.keyBuf = make(...) }`), then copy into it. (3) Set `s.currentKey = s.keyBuf[:len(s.currentKey)]`. (4) Add `BenchmarkSeqScan_KeyBuf_Reuse` verifying zero allocs for key copy. Estimate: ~20 lines, 1 file, 0.1 day. | high | small | none | SQB/OP/operators.go |
| REQ001636 | SQB/AG | **Scalar aggregate fast path — skip group key computation for no GROUP BY.** `aggregate.go:86-111` — For `SELECT count(*) FROM t1` (no GROUP BY), `materialize` still allocates `groupIndex map[string]int`, calls `evalGroupKey` per row (allocates `[]Value`), calls `groupKeyString` per row (allocates via `strings.Builder`), and builds `groupBucket{key, rows}` per row. All unnecessary for a scalar aggregate (exactly 1 group). pprof shows Aggregate.materialize at 710MB (9.60% of alloc space). **Fix:** (1) Detect scalar aggregate (`len(a.groupCols) == 0`) at plan time and store a `scalar bool` flag. (2) In `materialize`, when `scalar`, skip group key computation entirely — accumulate aggregate state directly from input rows. (3) Build output row directly from accumulated state (no `groups` slice, no `groupIndex` map, no `sort`). (4) Add `BenchmarkSelect1_ScalarAggregate_FastPath` and `BenchmarkCount_ScalarAggregate_NoAlloc`. Estimate: ~80 lines, 1 file, 0.5 day. | high | small | none | SQB/AG/aggregate.go |
| REQ001637 | SQB/EX | **Drain output slice: pre-allocate based on engineBatchSize.** `drain_batch.go:95-107` — `drainPlanRows` starts with `var out []DT.Row` (nil) and grows via `append`. For 20 rows this causes 5 growslice operations (0→1→2→4→8→16→32), each copying the entire slice. pprof shows 28.6M alloc objects (86.35%) from this path. **Fix:** (1) In `drainPlanRows`, pre-allocate `out` with capacity = `engineBatchSize` (already available as a package constant). (2) This eliminates all growslice copies for result sets ≤ engineBatchSize. (3) Add `BenchmarkDrain_Prealloc_NoGrowslice` verifying zero growslice operations. Estimate: ~10 lines, 1 file, 0.1 day. | medium | small | none | SQB/EX/drain_batch.go |
| REQ001638 | ENG/LS | **MergeIterator: eliminate redundant curKey/curVal double-copy.** `api.go:753-754` — `mi.curKey = append(mi.curKey[:0], top.key...)` copies key into curKey. Then `SeqScan.nextFromStore:671` copies it AGAIN into `keyCopy`. The merge iterator already owns the data (copied from source slots). pprof shows 385MB (5.20% of alloc space). **Fix:** (1) After REQ001635 (SeqScan key buffer pooling), the merge iterator can return a direct reference to its internal curKey buffer since SeqScan will copy it into its own pooled buffer. (2) Change `Key()` to return `mi.curKey` directly (already does — no change needed). (3) The savings come from SEQ001635 eliminating the second copy. (4) If REQ001635 is not implemented, pool the merge iterator's curKey/curVal buffers with geometric growth. Estimate: ~20 lines, 1 file, 0.1 day. | medium | small | REQ001635 | ENG/LS/api.go |
| REQ001639 | SQB/DT | **RowArena: remove redundant `slabs` tracking — GC overhead.** `arena.go:146` — `a.slabs = append(a.slabs, a.slab)` keeps ALL old slabs alive for GC tracing. Each slab is 64KB+. The `slabs` slice is redundant because `Row.Data` sub-slices already reference the slab backing arrays, keeping them alive through normal GC reachability. pprof shows 15.56% of CPU in GC (scanObject 10.81%, gcBgMark 4.74%). **Fix:** (1) Remove `a.slabs = append(a.slabs, a.slab)` from `growLocked`. (2) Verify that output rows (which hold `Row.Data` sub-slices into old slabs) keep the slabs alive through normal GC tracing. (3) Add `BenchmarkSelect1_GCPressure` comparing GC pause time before/after. (4) Add `TestRowArena_OldSlabsNotCollectedWhileReferenced`. Estimate: ~10 lines, 1 file, 0.1 day. | high | small | none | SQB/DT/arena.go |
| REQ001640 | SQB/DT | **Cache wantedSet in DecodeRowSubsetInto — eliminate per-row map allocation.** `arena.go:349` — `wantedSet := make(map[int]int, len(wantedIdx))` allocates a map on every row decode. For 20 rows × 5 queries, that's 100 small maps. The `wantedIdx` is invariant across rows (set once per SeqScan). **Fix:** (1) Add `wantedSet map[int]int` field to SeqScan (cached alongside `subsetSchema`). (2) Build `wantedSet` once when `subsetSchema` is first constructed (line 804-823). (3) Pass the cached `wantedSet` into `DecodeRowSubsetInto` instead of rebuilding per call. (4) Add `BenchmarkDecodeRowSubset_NoMapAlloc`. Estimate: ~30 lines, 2 files, 0.25 day. | high | small | none | SQB/DT/arena.go, SQB/OP/operators.go |
| REQ001641 | SQB/EX + SQO | **Apply SQO optimizer passes to the full plan tree, not just single-table scans.** Currently `runSQOPasses` is called once in `planSelect` (line 356) before `planSelectJoins`. The 9 registered optimizer passes (ConstantFolding, SubqueryDecorrelation, IndexSelection, RedundantPredicateElimination, OrToInExpansion, ImplicitCastElimination, SortElimination, ColumnPruning, PredicatePushdown, LimitPushdown) only run on the single-table scan subtree. Join trees, aggregations, and compound queries bypass the optimizer entirely. **Fix:** (1) Move `runSQOPasses` call to after `planSelectJoins` completes, so the entire plan tree (including joins) is optimized. (2) Handle the case where the optimizer rewrites the operator tree (e.g., merges Filter+Project, eliminates redundant Sort). (3) Add `TestOptimizer_FullPlanTree` that verifies passes run on a multi-table join plan. Estimate: ~30 lines, 2 files, 0.25 day. | high | small | REQ001450 | SQB/EX/planner_select.go, SQB/EX/planner.go |
| REQ001642 | SQB/EX + SQO | **Wire cost-based join ordering through SQO cost model.** Current N3 join ordering (`join_order.go`) uses `getTableRowCount` and `filteredRowCount` for cost estimation, which are independent of the SQO/CO cost model. The `SQO/CO/cost_estimation.go` functions (`EstimateCost`, `EstimateCardinality`) exist but are not used by the join orderer. **Fix:** (1) In `n3JoinOrdering`, replace `getTableRowCount`/`filteredRowCount` with `CO.EstimateCardinality` when catalog stats are available. (2) Fall back to the current heuristic when stats are absent. (3) Add `TestJoinOrder_CostModel` that verifies the cost model selects the correct join order. Estimate: ~60 lines, 2 files, 0.5 day. | high | small | REQ001641, REQ001314 | SQB/EX/join_order.go, SQO/CO/cost_estimation.go |
| REQ001643 | SQB/EX + SQO | **Subquery decorrelation: run SubqueryDecorrelationPass on the full plan tree.** `SubqueryDecorrelationPass` is registered in the optimizer but only runs on single-table scans (due to REQ001641). Correlated subqueries in WHERE clauses of multi-table joins are not decorrelated, causing row-by-row execution (via `injectOuter` + `runSubqueryPlan` in `subq.go`). **Fix:** (1) After REQ001641 enables full-tree optimization, verify `SubqueryDecorrelationPass` correctly handles subqueries in join predicates. (2) Add `TestSubqueryDecorrelation_JoinPredicate` that verifies a correlated subquery in a JOIN ON clause is decorrelated. (3) Benchmark a TPC-H Q17-style query (correlated subquery with aggregation) to measure the performance gain. Estimate: ~40 lines, 2 files, 0.5 day. | medium | small | REQ001641 | SQO/PF/subquery_decorrelation.go, SQB/EX/planner_select.go |
| REQ001644 | SQB/EX + SQO | **Cost-based index selection for multi-table joins.** `IndexSelectionPass` is registered but only runs on single-table scans. For multi-table joins, the planner uses simple heuristics (e.g., `pickCheaperScan` in `planner_select.go:114`) to choose between SeqScan and IndexScan. A cost-based decision would consider join selectivity, index cardinality, and output column count. **Fix:** (1) After REQ001641 enables full-tree optimization, verify `IndexSelectionPass` correctly handles index selection in the context of join predicates. (2) Add index depth/cardinality information to `OC.TableSchema` so the cost model can estimate index scan cost vs sequential scan cost. (3) Add `TestIndexSelection_JoinPredicate` that verifies an index is selected for a join predicate. Estimate: ~80 lines, 3 files, 0.75 day. | medium | medium | REQ001641, REQ001314 | SQO/PF/index_selection.go, SQO/OC/catalog.go, SQB/EX/planner_select.go |
| REQ001645 | SQB/OP | **Generalize parallel hash join to support arbitrary probe-side batch producers.** Current `ParallelHashJoin` (`parallel_hashjoin.go`) requires the probe side to be a `BatchProducer` and the build side to be materializable. It spawns N workers via `WorkerPool` to probe the hash table in parallel. This works for simple cases but doesn't support: (1) Nested loop join parallelization, (2) Merge join parallelization, (3) Adaptive worker count based on data size. **Fix:** (1) Extract a `ParallelProbe` utility in `SQB/UT/parallel.go` that takes a hash table, a probe batch producer, and a worker count. (2) Re-implement `ParallelHashJoin` using `ParallelProbe`. (3) Add a `Parallelism` parameter to `VectorizedHashJoin` that can be set by the planner when the build side is large (> 10K rows). (4) Add `BenchmarkParallelHashJoin_LargeBuild` that measures speedup on a 100K-row build side. Estimate: ~150 lines, 3 files, 1 day. | low | medium | REQ001587, REQ001622 | SQB/OP/parallel_hashjoin.go, SQB/UT/parallel.go, SQB/OP/op_vec_join.go |
