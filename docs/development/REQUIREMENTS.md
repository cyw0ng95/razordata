## TBD
| ID | Subsystem | Requirement | Priority | Effort | Deps | Touches |
| --- | --- | --- | --- | --- | --- | --- |
| REQ000908 | EX/eval | **ATTACH DATABASE / DETACH DATABASE are parsed but rejected at runtime with `ErrMultiDatabaseNotSupported` (`writers.go:2189`) — the parser successfully builds `AttachStmt`/`DetachStmt` AST nodes but the executor path immediately errors out, making any cross-database query (or even intra-database `ATTACH` for future schema-attach) impossible.** While the multi-database engine is a large architectural change, the bare AST + catalog machinery for attaching an already-open RazorData database directory should be implementable as a thin alias layer. **Fix:** (1) Add a `catalog` field on `*Executor` mapping `name → *store.Store`. (2) On `ATTACH DATABASE '<path>' AS name`, open a second store and register it in the catalog; on `DETACH`, close and remove. (3) For `CREATE TABLE attached.t(...)` and `SELECT * FROM attached.t`, resolve `name` via the catalog when looking up tables. (4) Reject cross-database transactions / joins between attached and main for v1. (5) Add `TestAttachDetach_SamePath` and `TestAttachDetach_QueryAcross`. | medium | medium | iter-31 | EX/writers.go, EX/ex.go, EX/catalog.go (new), PS/ps.go |
| REQ000909 | EX/ddl | **`CREATE VIRTUAL TABLE ... USING module(args)` parses to `CreateVirtualTableStmt` (`ast.go:666-673`) but has no execution path — the writer dispatcher in `writers.go` has no `*PS.CreateVirtualTable` case, so it falls through to a generic syntax error.** Virtual tables are SQLite's module-extensibility point (FTS5, json1, csv, etc.). Implementing a real module is large, but a minimum viable FTS5-style module or a passthrough CSV module would unblock `PRAGMA` introspection code that depends on virtual tables and would let SQLLogicTest files that touch FTS be partially exercised. **Fix:** (1) Add a `*PS.CreateVirtualTable` case in `buildWriterOp` that registers a stub module — for v1, return a clear `feature not supported: virtual table module <name>` error at `CREATE VIRTUAL TABLE` time (no module dispatch). (2) Ensure that `sqlite_master` introspection of virtual tables returns the expected row format (schema column populated with the module name). (3) Add `TestCreateVirtualTable_StubRegistration` verifying that `sqlite_master` shows the table with the right schema and that data ops on it return the unsupported-module error. | medium | small | iter-31 | EX/writers.go, EX/buildWriterOp, PS/ast.go |
| REQ000910 | EX/ddl | **WITHOUT ROWID tables are accepted by the parser (`PS/ddl.go`) and AST (`ast.go`), but the store layer at `EX/store.go:64` and `EX/constraints.go:43` treats them as rowid tables — `STORAGE WITHOUT ROWID` does not change the actual storage layout, so the resulting table uses rowid-based B-tree even though the AST claims rowid is absent.** This means `INSERT INTO without_rowid_t VALUES (...)` with a non-PK column in place of a rowid does not surface any error to the user, but the rowid is still allocated and queries using `WHERE _rowid_ = ?` succeed when they should fail. **Fix:** (1) Add `isRowid bool` to the `schema` struct, set to false when `WITHOUT ROWID` is present. (2) In `Store.Create`, branch to a clustered-index storage path that uses the PRIMARY KEY as the B-tree key (no implicit rowid column). (3) Block `_rowid_` queries on WITHOUT ROWID tables with `no such column: _rowid_`. (4) Add `TestWithoutRowId_TableCreation`, `TestWithoutRowId_PrimaryKeyClustered`, `TestWithoutRowId_NoRowIdCol`. | medium | large | iter-31 | EX/store.go, EX/constraints.go, EX/writers.go, PS/ddl.go |
| REQ000944 | EX/eval | **`-COUNT(DISTINCT + col0) - ++SUM(DISTINCT -(- 11)) + 32` — chain of DISTINCT aggregates with unary minus produces wrong result.** Evidence: `tests/sqlcmp/corpus/test/random/aggregates/slt_good_111.test:121-123`. COUNT(DISTINCT) and SUM(DISTINCT) chained with unary minus and the addition of a constant. Razor returns wrong value (likely just the constant 32). **Fix:** (1) Fix `SUM(DISTINCT -(-11))` = SUM(DISTINCT 11) = 11 (REQ928). (2) Fix `COUNT(DISTINCT +col0)` (REQ936). (3) Add `TestChain_DistinctAggregates_UnaryChain`. **Test evidence:** aggregates/111 L121. | low | small | iter-31 | EX/aggregate.go, EX/eval.go |
| REQ000949 | EX/join | **Block NLJ batch size (32 rows) limits right-side reuse for cross-joins.** (Partially shipped in iter-32: tinyRightThreshold raised from 64 to 256 for right-side caching. Batch size increase 32→64 deferred — causes select4 regression). Remaining: increase block size from 32 to 64 in `join.go:747`. For cross-joins with no ON/predicate, cache right side across all batches (currently cached per-batch when ≤256 rows). **Benchmark:** `BenchmarkSelect4_Join255` allocs=958/op. | medium | small | iter-32 | EX/join.go, EX/join_test.go |
| REQ000971 | EX/const | **CRC32 used for unique constraint key encoding — collision risk causes silent data corruption.** `constraints.go:304-338` uses `crc32.NewIEEE()` (32-bit) to hash column values for UNIQUE constraint checking. CRC32 has ~1-in-2^32 collision probability per distinct value pair. A collision causes either (a) a false "duplicate key" error on INSERT, or (b) a missed duplicate (silent data corruption). The pending set is in-memory, so full binary key encoding is feasible. **Fix:** (1) Replace CRC32 with deterministic binary key encoding (reusing `encodeRow` logic or a similar approach). (2) Alternatively, use `crypto/sha256` truncated to 64 bits — still collision-resistant enough. (3) Add `TestUnique_NoCollision` inserting 100K rows with distinct composite keys and verifying no false positives. | high | small | none | EX/constraints.go |
| REQ000972 | WAL | **`SyncAsync` updates `synced` LSN non-atomically — can regress committed LSN.** `WR/wr.go:288-293` does `if syncedLSN > w.synced.Load() { w.synced.Store(syncedLSN) }` — a non-atomic read-then-write. If two `SyncAsync` goroutines run concurrently, the second may overwrite a higher LSN from the first, causing `SyncedLSN` to regress. Readers checking `synced` may then miss committed data. The synchronous `syncLocked` at line 233-241 correctly uses a CAS loop. **Fix:** (1) Replace with a CAS loop: `for { old := w.synced.Load(); if syncedLSN <= old || w.synced.CompareAndSwap(old, syncedLSN) { break } }`. (2) Add `TestWAL_SyncAsyncConcurrent` verifying that concurrent SyncAsync calls never regress the synced LSN. | high | small | none | WAL/WR/wr.go |
| REQ000973 | CT | **`GetByIDRef` returns mutable pointer under RLock — data race with concurrent Put/Delete.** `CT/catalog.go:251-262` acquires `c.mu.RLock()`, returns a direct `*Entry` pointer into `c.cache`, then releases the lock. Any concurrent `PutRaw`/`Delete` calling `flushLocked` can mutate the entry's fields (e.g., `Indexes` slice) while the caller reads/writes them. This is a data race by design. Similarly, `Cache()` (line 367) and `ByNameMap()` (line 374) return the raw internal maps without any lock held after return. `LS/catalog.go:271-283` (`ColumnStatsByName`) and `LS/catalog.go:288-308` (`TableStats`) iterate these maps without locks. **Fix:** (1) `GetByIDRef` should either return a deep copy or require callers to hold `c.mu.Lock()` explicitly with documented lock ordering. (2) `Cache()`/`ByNameMap()` should return copies or use a snapshot mechanism. (3) `ColumnStatsByName`/`TableStats` should acquire `c.mu.RLock()` before iterating. (4) Add `TestCatalog_ConcurrentAccess` running parallel Put/GetByIDRef/Delete in a race detector test. | high | medium | none | ENG/CT/catalog.go, ENG/LS/catalog.go |
| REQ000974 | EX/store | **`store.go` has inconsistent lock ordering between `storeMu` and `tablesMu` — deadlock risk.** `store.go:270-283` (`nextTableID`) and `store.go:443-453` (`nextTableIDLocked`) have inconsistent lock ordering relative to the catalog lock. `store.go:465` acquires `storeMu` then `store.go:515` acquires `tablesMu` (from `source.go:15`). Other code paths acquire `tablesMu` then `storeMu`. This inconsistent ordering creates a deadlock risk under concurrent DDL + DML. **Fix:** (1) Establish and document a single lock ordering: `tablesMu` → `storeMu` → `catalog.mu` (or vice versa). (2) Audit all call sites that acquire both locks and enforce the ordering. (3) Add `TestStore_LockOrdering` with concurrent CREATE TABLE + INSERT to verify no deadlock. | high | medium | none | EX/store.go, EX/source.go |
| REQ000975 | ENG/ID | **BTree Cursor has no `Close` method and acquires RLock per-operation — concurrent mutation corrupts cursor state.** `ENG/ID/cursor.go` has `Seek`/`Next`/`Key`/`Value`/`Valid` but no `Close`. More critically, `Seek` (line 19-21) acquires `bt.mu.RLock()` for the seek, then releases it. `Next` (line 64-69) re-acquires `bt.mu.RLock()` per step. Between `Seek` and `Next`, a concurrent `Insert`/`Delete` can mutate the tree, invalidating the cursor's `path` and `indices` stacks (which reference page IDs that may be reclaimed/reused). **Fix:** (1) Add `Close() error` to satisfy the `RangeIter` interface. (2) Either hold the lock for the cursor's entire lifetime (cursor = snapshot), or validate path consistency on `Next` (check page IDs haven't been reused). (3) Add `TestBTree_CursorConcurrentMutation` verifying that a cursor scan concurrent with inserts does not return corrupted results. | high | medium | none | ENG/ID/cursor.go |
| REQ000976 | LS | **`shardedIter.Next()` decrements `pos` incorrectly after advancing to next shard — reads from wrong shard.** `LS/sharded_memtable.go:229-246`: when shard `pos` is exhausted, the code does `si.pos++` (advance to next shard), calls `si.its[si.pos].Next()`, then does `si.pos--` (backs up). After the decrement, `pos` points to the *previous* shard (which was exhausted), not the new shard. `Key()`/`Value()` then read from the wrong shard's iterator. **Fix:** (1) After finding a non-empty shard, leave `si.pos` at the new shard index (remove the `si.pos--`). (2) Or restructure as a loop: `for si.pos < len(si.its) { if si.its[si.pos].Next() { return true }; si.pos++ }`. (3) Add `TestShardedIter_AdvanceAcrossShards` verifying correct cross-shard iteration. | high | small | none | LS/sharded_memtable.go |
| REQ000977 | EX/fk | **`encodeFKLookup` is a stub returning nil — FOREIGN KEY store-path validation silently passes.** `fk.go:189-196`: `encodeFKLookup` has `_ = prefix; _ = col; _ = val; return nil`. This means FK validation via the store path (for multi-column FKs or non-in-memory tables) silently succeeds — it never checks anything. Combined with `fk.go:82-88,285-287` where CASCADE/SET NULL/SET DEFAULT are accepted at DDL time but fail at runtime with "not yet implemented". **Fix:** (1) Either implement `encodeFKLookup` with proper key encoding, or return a clear `ErrFeatureNotSupported` at DDL time for FK actions that cannot be enforced. (2) Reject non-RESTRICT/NO-ACTION FK actions at `CREATE TABLE` time with a clear error. (3) Add `TestFK_StorePathValidation` and `TestFK_CascadeRejectedAtDDL`. | high | small | none | EX/fk.go |
| REQ000978 | EX/eval | **`evalFunction` and `evalAggregateOver` use flat switch dispatch — should use function registry pattern.** `eval.go` has 40+ scalar functions dispatched via a `switch name` in `evalFunction`, and `aggregate.go` has 6 aggregate types via `switch name` in `evalAggregateOver`. Both grow linearly with each new function — adding a function requires editing the switch, not just registering. The switch blocks are ~100 lines each. **Fix:** (1) Create `map[string]func(args []Value, row Row) (Value, error)` for scalar functions, populated at init. (2) Create `map[string]func() aggregateState` for aggregates. (3) Replace switch with map lookup. (4) This reduces eval.go by ~100 lines and makes adding functions a one-line registration. (5) Add `TestFunctionRegistry_AllFunctionsRegistered` verifying every documented function name has a registry entry. | high | medium | none | EX/eval.go, EX/aggregate.go |
| REQ000979 | EX/op | **`IndexScan` carries 5 mutually exclusive operational modes as optional fields — Strategy anti-pattern.** `indexscan.go` has grown from an MVP scan to a ~60-line god struct across iterations 22-27. Five modes (in-memory, store-prefix, index-seek, B-tree, range-seek) are encoded as optional fields with mode-switching in `Next()` and 5 constructors. This is a textbook Strategy anti-pattern. **Fix:** (1) Extract a `ScanStrategy` interface with `Next() (Row, error)`, `Close() error`. (2) Create concrete types: `InMemoryScan`, `StorePrefixScan`, `IndexSeekScan`, `BTreeScan`, `RangeSeekScan`. (3) `IndexScan` becomes a thin wrapper that delegates to the selected strategy. (4) Add `TestIndexScan_StrategySelection` verifying each strategy is selected correctly based on table type and predicate. | high | medium | none | EX/indexscan.go |
| REQ000980 | EX/join | **`NestedLoopJoin` struct has 50+ fields — join strategy decided at runtime, not by planner.** `join.go:23-142` is a god object containing fields for 7 execution modes (classic NLJ, hash cross, block NLJ, right-outer materialization, left-outer tracking, column projection, and 4 shared-state pairs). `Next()` (line 295-326) checks conditions to switch between strategies at runtime. The planner has no visibility into which strategy was chosen, making EXPLAIN output incomplete. **Fix:** (1) Extract mode-specific state into sub-structs or, better, make hash cross join and block NLJ truly separate operator types. (2) Move strategy selection to the planner based on statistics. (3) `tryHashCrossJoin` should be `NewHashCrossJoin` called from the planner. (4) Add `TestJoin_StrategyPlannerDriven` verifying the planner selects the right strategy. | high | large | none | EX/join.go, EX/planner.go |
| REQ000981 | EX/plan | **`planSelect()` is 760+ lines — handles 15+ concerns in one function.** `planner.go:1372-2133` handles view resolution, no-FROM expressions, subquery sources, sqlite_master, alias rewriting, index selection, cost-based scan swapping, predicate pushdown, join elimination, N3 ordering, bushy group construction, aggregate selection, HAVING, ORDER BY position resolution, window functions, projection, DISTINCT, LIMIT/OFFSET — all in one function. **Fix:** (1) Split into: `planSelectBase`, `resolveView`, `planSelectJoins`, `pushPredicates`, `planAggregation`, `planOrdering`, `planLimitOffset`. (2) Each sub-function should be under 100 lines. (3) Add `TestPlanner_SelectDecomposition` verifying each sub-function is independently testable. | high | medium | none | EX/planner.go |
| REQ000982 | EX/ddl | **`CreateTable.Next` is 200 lines — violates single-responsibility.** `writers.go:1089-1267` registers the table, resolves PK, builds unique constraints, extracts FK constraints, captures generated columns, builds CHECK constraints, persists to catalog, and writes the SQL reconstruction — all in one function. **Fix:** (1) Split into: `registerTableSchema`, `buildUniqueConstraints`, `buildFKConstraints`, `buildCheckConstraints`, `persistToCatalog`. (2) Each helper should be independently testable. (3) Add `TestCreateTable_SchemaRegistration` verifying each sub-function. | high | medium | none | EX/writers.go |
| REQ000983 | EX/plan | **Three duplicate expression walkers in `planner.go` — must be updated in lockstep.** `walkExprForTables` (line 741-793), `collectTablesFromExpr` (line 1162-1215), and `collectColsFromExpr` (line 1280-1337) each manually type-switch over ~15 expression node types. All three must be updated when a new expression type is added. Additionally, `walkExprForTables` does not handle `SubqueryExpr`/`ExistsExpr`, which can cause incorrect join elimination for correlated subqueries. **Fix:** (1) Create a generic `walkExpr(Expr, func(Expr))` or reuse the existing `AcceptExpr` visitor infrastructure. (2) Replace all three with calls to the shared walker + appropriate callback. (3) This eliminates ~200 lines and prevents future divergence. (4) Add `TestPlanner_ExprWalkerCoverage` verifying all expression types are handled. | high | medium | none | EX/planner.go |
| REQ000984 | EX/writer | **Duplicated RETURNING evaluation block appears 6 times across Insert/Update/Delete.** `writers.go` has the exact same 12-line RETURNING evaluation block in `Insert.Next` (line 209-225), `Insert.nextFromStore` (line 356-372), `Insert.nextFromSelect` (line 662-677), `Update.Next` (line 759-774), `Update.nextFromStore` (line 889-905), and `Delete.Next`/`Delete.nextFromStore` (line 977-994). **Fix:** (1) Extract `evalReturning(exprs []PS.Expr, row Row, params []any) ([]Row, error)` helper. (2) Replace all 6 call sites. (3) Add `TestEvalReturning_Helper` verifying the extracted function. | medium | small | none | EX/writers.go |
| REQ000985 | EX/store | **`encodeRow` allocates per call with no `sync.Pool` — violates hot-path allocation rule.** `store.go:559-601` allocates a new `[]byte` via `append` on every `encodeRow` call. For bulk INSERT (thousands of rows), this creates significant GC pressure. The project rules state "No allocations in hot paths — pre-allocate buffers" and "sync.Pool for reusable page buffers." **Fix:** (1) Use `sync.Pool` for encode/decode scratch buffers. (2) Or accept a pre-allocated buffer parameter. (3) Add `TestStore_EncodeRowNoAlloc` using `testing.AllocsPerRun` to verify zero allocations on the hot path. | medium | small | none | EX/store.go |
| REQ000986 | EX/const | **CHECK constraints re-evaluated from AST per row — not compiled.** `constraints.go:193-211` calls `EvalValue(check, &row, nil)` where `check` is a `PS.Expr` (parser AST node). For every INSERT/UPDATE, the CHECK expression is re-walked from the AST. Complex CHECK expressions (e.g., `CHECK(a > 0 AND b < 100)`) pay the full parsing overhead per row. **Fix:** (1) Pre-compile CHECK expressions into a `CompiledExpr` at schema registration time, similar to how WHERE clauses are compiled for operators. (2) Cache the compiled form in `storeSchema.checks`. (3) Add `TestCheckConstraint_Precompiled` verifying compiled CHECK expressions are faster than AST evaluation. | medium | small | none | EX/constraints.go |
| REQ000987 | EX/store | **No `TableHandle` abstraction — raw Store used everywhere with repeated schema+key+encode pattern.** Every caller (`writers.go`, `operators.go`, `integrity.go`) independently: (1) looks up the schema via `schemaFor`, (2) builds the key prefix, (3) calls `encodeRow`, (4) calls `store.Put/Get/Delete`. This three-step pattern is duplicated across 10+ call sites. **Fix:** (1) Introduce a `TableHandle` struct that bundles `{store Store, schema *storeSchema, prefix []byte}` with methods `InsertRow(Row)`, `GetRow(key)`, `Scan(prefix) Iterator`. (2) Replace the repeated pattern at all call sites. (3) Add `TestTableHandle_InsertGetScan` verifying the abstraction. | medium | medium | none | EX/store.go, EX/writers.go, EX/operators.go |
| REQ000988 | EX/eval | **`BinaryExpr.Op` is `int` instead of `LX.TokenType` — ~40 redundant casts across codebase.** `PS/ast.go:93` declares `Op int` but every construction site casts `int(LX.T_EQ)` and every consumption site casts back `LX.TokenType(op)`. Dozens of redundant casts scattered through `expr.go`, `planner.go`, `eval.go`, `compound.go`, `values.go`. **Fix:** (1) Change `BinaryExpr.Op` to `LX.TokenType`. (2) Remove all cast expressions at construction and consumption sites. (3) Add `go vet` check or lint rule to prevent re-introduction. | low | small | none | PS/ast.go, EX/eval.go, EX/planner.go |
| REQ000989 | EX/planner | **Plan cache eviction is O(n) random deletion — should use LRU or ring buffer.** `planner.go:420-437`: when the memo exceeds `maxPlanCacheSize`, a loop scans the map to find one arbitrary key to delete. Non-LRU, O(n) per eviction. **Fix:** (1) Use a ring buffer of keys for deterministic O(1) eviction. (2) Or implement a proper LRU with `container/list`. (3) Add `TestPlanCache_EvictionPolicy` verifying eviction targets the oldest entry. | low | small | none | EX/planner.go |
| REQ000990 | ENG/ID | **BTree page buffers not pooled — no sync.Pool, no eviction, unbounded memory.** `ENG/ID/id.go:64` (`newPage`) allocates `*page` on the heap. `loadPage` (line 207) allocates `make([]byte, pageSize)` on every cache miss. Pages accumulate in `bt.pages` forever until close — no LRU or clock-sweep eviction. For large indexes this is unbounded memory growth. **Fix:** (1) Add `sync.Pool` for `*page` and `[]byte` buffers. (2) Add LRU eviction to `bt.pages` (e.g., clock sweep or `container/list`). (3) Add `TestBTree_PageEviction` verifying memory is bounded after scanning 10K pages. | low | medium | none | ENG/ID/id.go |
| REQ000991 | EX/vec | **`evalInListBatch` exists but is never wired into `EvalBatch` — vectorized IN-list dead code.** `eval_vec.go:22` declares `evalInListBatch` and `eval_vec.go:882` implements it, but `EvalBatch` (line 22) has no `case *PS.InExpr` dispatch. IN-list filters are one of the most common predicate patterns (used in every `WHERE col IN (...)` query). The vectorized path exists and is tested but never called — all IN-list evaluations fall back to row-at-a-time. **Fix:** (1) Add `case *PS.InExpr` in `EvalBatch` that calls `evalInListBatch`. (2) Verify with `TestEvalBatch_InList` that the batch path produces identical results to the row path. | high | small | none | EX/eval_vec.go |
| REQ000992 | EX/eval | **Missing context cancellation in materialization loops — hashagg, window, subq cannot be cancelled.** `hashagg.go:68`, `window.go:74`, `subq.go:95` each have tight inner loops that materialize all rows before producing output. None check `ctx.Err()` during materialization. A long-running aggregate over millions of rows, a window function with a large partition, or a correlated subquery cannot be cancelled by the caller. This violates the project rule "All public API methods must be goroutine-safe" and the contract that `ctx` controls query lifetime. **Fix:** (1) Insert `if err := ctx.Err(); err != nil { return nil, err }` at the top of each inner materialization loop. (2) Add `TestContext_CancelAggregate`, `TestContext_CancelWindow`, `TestContext_CancelSubquery` verifying cancellation mid-materialization. | high | small | none | EX/hashagg.go, EX/window.go, EX/subq.go |
| REQ000993 | TXN/VL | **MVGC `ReclaimVersionNodes` is a no-op — MVCC version chains grow without bound.** `TXN/VL/gc.go:126-146`: the function receives a batch of version node pointers to reclaim, checks epochs, but the inner loop body at line 143 does nothing (`if ptr != nil { }`). The pointers are never actually freed. This means every UPDATE creates a new version node that is never reclaimed, causing unbounded memory growth for write-heavy workloads. **Fix:** (1) Wire the batch into the epoch-based reclaim mechanism (call `MV.ReclaimOldGenerations()` or queue for deferred free). (2) Add `TestMVGC_VersionNodeReclaim` verifying that after GC, old version nodes are freed. | high | small | none | TXN/VL/gc.go |
| REQ000994 | TXN/VL | **No lock manager — OCC-only concurrency causes abort cascades on write-heavy workloads.** `TXN/VL/` has no lock table, no lock acquisition, no deadlock detection, and no wait-for graph. Concurrency control is entirely OCC-based (validate at commit). Write-heavy workloads cause abort cascades: many transactions read the same rows, all try to commit, only one succeeds, the rest abort and retry, causing livelock. Also, `SELECT ... FOR UPDATE` cannot be implemented without row-level locks. **Fix:** (1) Add a lock table (`map[key]LockState` with wait queues). (2) Implement deadlock detection (wait-for graph or timeout-based abort). (3) Add `TestLockManager_ConcurrentWrites` verifying no abort cascade under contention. | high | large | none | TXN/VL/manager.go, TXN/VL/validate.go |
| REQ000995 | TXN/VL | **No savepoint or partial rollback — compound statements cannot recover from partial failure.** `TXN/VL/protocol.go`: the `Tx` interface exposes `Commit` and `Abort` but no `Savepoint(name)` or `RollbackTo(name)`. The write set is a flat `[]KeyRange` with no ability to truncate to a prior point. Trigger bodies that call multiple DMLs, or multi-statement transactions, cannot recover from partial failure — the entire transaction must abort. **Fix:** (1) Implement savepoints by snapshotting the write-set length and reverting the MVCC chain on rollback-to-savepoint. (2) Add `TestSavepoint_PartialRollback` verifying that rolling back to a savepoint undoes only the writes after the savepoint. | high | medium | none | TXN/VL/protocol.go |
| REQ000996 | LS | **`shardedIter` does not maintain sorted order — violates `RangeIter` contract.** `LS/sharded_memtable.go:222-247`: the iterator iterates shards sequentially, yielding all keys from shard 0, then shard 1, etc. This does not produce globally sorted output. Any consumer expecting sorted iteration (e.g., range scans, merge compaction) gets unsorted data. The `RangeIter` interface contract implies sorted order. **Fix:** (1) Implement merge-sort across shards using a heap (similar to `mergeIterator` in `api.go`). (2) Add `TestShardedIter_SortedOrder` inserting keys across shards and verifying the iterator returns them in sorted order. | high | medium | none | LS/sharded_memtable.go |
| REQ000997 | LS | **`readFromSST` loads entire SST file into memory per cache miss — defeats page cache.** `LS/engine.go:217-233`: `readSSTFile` calls `os.ReadFile(path)` to load the full SST, then caches individual pages. The full file load defeats the purpose of the page cache since the entire file is already in memory. For multi-GB SST files this is catastrophic. **Fix:** (1) Read only the footer, index block, and the relevant data block via the page cache. (2) Add `TestSST_PartialRead` verifying that reading one key from a 10MB SST does not load the entire file. | high | medium | none | LS/engine.go |
| REQ000998 | LS | **Page cache `Get` holds `RLock` for full O(N) scan — cache hit latency is O(capacity).** `LS/page_cache.go:67-82`: `Get` acquires a read lock then scans every slot linearly to find the matching page key. For a 256 MB / 4 KB = 65536-slot cache, this is a 65K-iteration scan per cache hit. **Fix:** (1) Use a concurrent hash map (`sync.Map` or sharded map) for O(1) page lookup. (2) Add `TestPageCache_LookupLatency` verifying O(1) amortized lookup. | high | medium | none | LS/page_cache.go |
| REQ000999 | LS | **Manifest `Apply` does not fsync — crash between rename and next sync loses manifest update.** `LS/manifest.go:82-111`: after writing the manifest file and renaming it, there is no `f.Sync()` on the file or `fsync` on the parent directory. A crash between the rename and the next explicit sync could lose the manifest update, causing the engine to revert to an older SST layout. **Fix:** (1) Call `f.Sync()` before close. (2) Fsync the parent directory after `os.Rename`. (3) Add `TestManifest_FsyncOnApply` verifying durability. | high | small | none | LS/manifest.go |
| REQ001000 | PS/parser | **CREATE/DROP dispatch duplicated in `Parse()` and `parseExplain()` — DRY violation.** `PS/ps.go:322-347` and `PS/explain.go:94-111` contain identical CREATE sub-dispatch logic (peek for INDEX, UNIQUE INDEX, VIEW, TRIGGER, MATERIALIZED, TEMP/TEMPORARY, VIRTUAL). Similarly `ps.go:348-371` and `explain.go:112-133` duplicate DROP dispatch. Any new CREATE variant (e.g., CREATE SEQUENCE) must be added in two places. **Fix:** (1) Extract `parseCreateDispatch() (Stmt, error)` and `parseDropDispatch() (Stmt, error)`. (2) Call from both `Parse()` and `parseExplain()`. (3) Add `TestParseCreateDispatch_Coverage` verifying all CREATE variants are handled. | high | small | none | PS/ps.go, PS/explain.go |
| REQ001001 | PS/ddl | **`parseCreateTable` is 420 lines — violates 200-line threshold, 10+ concerns in one function.** `PS/ddl.go:9-426` handles column definitions, inline constraints, table-level constraints (PK, UNIQUE, FK), generated columns, REFERENCES, MATCH, DEFERRABLE, WITHOUT ROWID, STRICT, and CREATE TABLE AS SELECT. **Fix:** (1) Split into: `parseColumnDef`, `parseTableConstraint`, `parseReferences`, `parseDeferrableClause`. (2) Each under 100 lines. (3) Add `TestParseCreateTable_SubFunctions` verifying each sub-function is independently testable. | high | medium | none | PS/ddl.go |
| REQ001002 | PS/select | **`parseOneSelect` is 282 lines with 3+ levels of nesting — needs decomposition.** `PS/select.go:184-464` handles SELECT columns, FROM (subquery, parenthesized table, comma-joins, explicit joins), WHERE, GROUP BY, HAVING, and alias parsing. The FROM clause alone (lines 228-405) is 177 lines with 3 nesting levels. **Fix:** (1) Split into: `parseSelectList`, `parseFromClause`, `parseWhereGroupHaving`. (2) Each under 100 lines. | high | medium | none | PS/select.go |
| REQ001004 | PS/ast | **No source location tracking on AST nodes — error messages cannot reference precise locations.** | high | medium | none | PS/ast.go, PS/ps.go |
| REQ001006 | TXN/VL | **`ReadView.Get` does linear scan — O(N) per key lookup in snapshot.** `TXN/SN/snapshot.go:42-84`: `Get` copies the snapshot under a lock, then iterates linearly to find the key. For large key spaces this is O(N) per lookup. **Fix:** (1) Use a `map[string]*VersionChainSnapshot` for O(1) key lookup. (2) Add `TestReadView_LookupLatency` verifying O(1) amortized. | medium | small | none | TXN/SN/snapshot.go |
| REQ001007 | TXN/VL | **`transactionSlot.readSet` grows unboundedly per transaction — OOM risk for read-heavy txns.** `TXN/VL/slot.go:72`: `readSet []ReadEntry` is a plain slice that grows with every `Get` call. A transaction that reads millions of rows will cause an OOM. **Fix:** (1) Cap the read set size and fall back to key-range tracking (as the write set does). (2) Or implement a Bloom filter for read-set deduplication. (3) Add `TestReadSet_BoundedMemory` verifying memory stays bounded for large reads. | medium | small | none | TXN/VL/slot.go |
| REQ001008 | LS | **No range tombstone support — `DELETE WHERE col > 100` must delete each row individually.** `LS/api.go:12-16`: deletion uses a magic tombstone value (`0xDEADBEEF`). Range deletions must delete each row individually, which is O(N) for the matching rows. A range-tombstone marker can suppress a key range during SST reads in O(1). **Fix:** (1) Add a range-tombstone record type to SST blocks. (2) Update `sstReader.Find` to check range tombstones before scanning. (3) Add `TestRangeTombstone_SuppressesRange` verifying the optimization. | medium | medium | none | LS/api.go, LS/sst_reader.go |
| REQ001009 | EX/vec | **Missing `AND`/`OR` batch evaluation in `EvalBatch` — most common fallback reason.** `eval_vec.go:27`: compound predicates (`a AND b`, `a OR b`) are ubiquitous but have no batch path. Every compound predicate forces fallback to row-at-a-time evaluation. **Fix:** (1) Implement `evalAndBatch` (intersection of selection vectors) and `evalOrBatch` (union). (2) Wire into `EvalBatch` for `*PS.BinaryExpr` with `AND`/`OR` ops. (3) Add `TestEvalBatch_AndOr` verifying batch AND/OR produce identical results to row path. | medium | medium | none | EX/eval_vec.go |
| REQ001010 | EX/eval | **`Lexer.scanIdent`/`scanString` use `strings.Builder` — avoidable allocation on every token.** `LX/lx.go:340-358` (scanString) and `LX/lx.go:364-380` (scanIdent) build strings character-by-character into `strings.Builder`, then call `.String()` to copy. Since `l.input` is already a string, scanning by start/end index and slicing `l.input[start:end]` eliminates the builder entirely. The builder is only needed when escape processing is required (e.g., `''` escapes in strings). **Fix:** (1) For `scanIdent`: scan start position, advance while valid ident chars, slice. (2) For `scanString`: slice for non-escaped strings, use builder only when escapes are present. (3) Add `BenchmarkLexer_TokenAlloc` verifying zero allocations for simple identifiers. | medium | small | none | LX/lx.go |
| REQ001011 | EX/ex | **No prepared statement cache — every execution re-parses and re-plans.** `EX/memo.go:10-12`: the only memoization is `serializeKey(stmt)` which hashes the AST. There is no cache of compiled plan trees. Every `Exec`/`Query` call re-parses the SQL, re-plans, and re-compiles. For OLTP workloads with repeated queries this is a major performance gap. **Fix:** (1) Add a plan cache (LRU, key=normalized SQL + param types). (2) Cache compiled `Operator` trees. (3) Add `TestPreparedCache_HitRate` verifying repeated queries use cached plans. | medium | medium | none | EX/memo.go, EX/ex.go |
| REQ001012 | PS/expr | **Aggregate parsing duplicated across `parsePrimary` — COUNT/SUM/AVG vs MIN/MAX share 80% logic.** `PS/expr.go:135-176` (COUNT/SUM/AVG) and `PS/expr.go:177-231` (MIN/MAX) are structurally identical: expect `(`, handle DISTINCT/ALL, parse arg, parse `)`, check FILTER, check OVER. MIN/MAX has a multi-arg variant that differs slightly. **Fix:** (1) Extract `parseAggregateFunc(name string, allowMultiArgs bool)`. (2) Replace both case arms. | medium | small | none | PS/expr.go |
| REQ001013 | EX/ex | **`Column.Data` uses `any` boxing — violates "no map[string]interface{} in data paths" rule.** | medium | medium | none | EX/batch.go |
| REQ001014 | TXN/LC | **`ReclaimPool` silently drops half its contents under pressure — memory leak.** `TXN/LC/reclaim_pool.go:33-41`: when the pool exceeds `maxPoolSize`, the first half of pointers are nilled and dropped without being freed. These pointers leak — they reference heap objects that are never freed. **Fix:** (1) Drain the dropped pointers into a deferred-free mechanism or call the epoch reclaimer before dropping. (2) Add `TestReclaimPool_NoLeakUnderPressure` verifying all pointers are eventually freed. | medium | small | none | TXN/LC/reclaim_pool.go |
| REQ001016 | EX/perf | **`groupKeyString` uses `fmt.Fprintf` per-row — allocates hidden `[]any` for format args.** `aggregate.go:214-239`: `fmt.Fprintf(&b, "I:%d", x)` and `fmt.Fprintf(&b, "F:%g", x)` inside a loop over group key columns. `fmt.Fprintf` allocates a hidden `[]any` for the variadic args and dispatches through reflection. For 100K rows with 2 group columns, that is 200K allocations. **Fix:** (1) Replace with `b.WriteString("I:"); strconv.AppendInt(b.Grow(n), x, 10)` pattern (write prefix then append formatted int directly). (2) Similarly for floats with `strconv.AppendFloat`. (3) Add `BenchmarkGroupKey_AllocPerRow` verifying reduced allocations. | high | small | none | EX/aggregate.go |
| REQ001018 | EX/perf | **Sort key pre-computation missing — `Row.Lookup` called per comparison.** `sort_parallel.go:289-307`: `lessRow` calls `a.Lookup(key.ColName)` and `b.Lookup(key.ColName)` for EVERY comparison. `Lookup` does a map lookup per call. With K sort keys and O(N log N) comparisons, this is O(K * N log N) map lookups. For 100K rows with 2 sort keys, that is ~3.6M map lookups. **Fix:** (1) Pre-compute column indices once before sorting: build `[]int` of column indices from sort key names. (2) Index directly into `row.Data[idx]` without map lookup. (3) Add `BenchmarkSort_KeyLookup` verifying O(1) per comparison. | high | small | none | EX/sort_parallel.go |
| REQ001019 | EX/perf | **CompoundOp ORDER BY calls `EvalValue` per comparison — O(N log N * depth) expression evaluations.** `compound.go:129-143`: the sort comparator calls `EvalValue(k.Expr, &a, c.params)` and `EvalValue(k.Expr, &b, c.params)` for EVERY comparison. EvalValue traverses the expression tree. For O(N log N) comparisons with 2 sort keys, this is extremely expensive. **Fix:** (1) Pre-extract sort key values from all rows into a `[]Value` slice before sorting (Schwartzian transform: decorate-sort-undecorate). (2) Sort by pre-extracted values. (3) Add `BenchmarkCompoundOrderBy_EvalCost` verifying reduced eval calls. | high | small | none | EX/compound.go |
| REQ001020 | EX/perf | **No hash-based LEFT/RIGHT/FULL OUTER JOIN — only NLJ with O(N*M/32) right-side scans.** `join.go:227-391`: ALL LEFT, RIGHT, and FULL OUTER JOINs fall through to NestedLoopJoin, which re-scans the right side for every left row (batch of 32). HashJoin only supports INNER JOIN. For a 10K-left × 1K-right LEFT JOIN, NLJ does 312 right-side scans. **Fix:** (1) Implement hash-based LEFT OUTER: build hash table on right side, probe with left, track unmatched right rows. (2) Reduces right-side work from O(N*M/32) to O(N+M). (3) Add `TestHashLeftOuterJoin_Basic` and `BenchmarkHashLeftOuterJoin` verifying correctness and performance. | high | large | none | EX/join.go, EX/hashjoin.go |
| REQ001021 | EX/perf | **Sort `firstBatch` truncates at BatchSize — sorted results > 1024 rows silently lost.** `sort_parallel.go:172-227`: `firstBatch` returns at most `BatchSize` (1024) rows from the sorted result. Subsequent calls to `NextBatch` return `(nil, nil)` because `s.done = true`. The remaining sorted rows are discarded. This is both a correctness bug and a performance issue (only first 1024 rows are ever returned). **Fix:** (1) Track a cursor position (`s.pos`). (2) Return subsequent batches from `s.rows[s.pos:]` on repeated `NextBatch` calls. (3) Add `TestSort_LargeDataset` verifying all 10K sorted rows are returned. | high | small | none | EX/sort_parallel.go |
| REQ001024 | EX/perf | **`evalGroupKey` allocates `[]any` per row — immediately re-serialized by `distinctKey`.** `aggregate.go:158`: `out := make([]any, len(cols))` allocates a new slice per row, boxing each Value to `any`. The result is immediately passed to `distinctKey` which re-serializes it to a string key. The `[]any` intermediate is wasted work. **Fix:** (1) Compute the map key directly from evaluated Values, bypassing the `[]any` intermediate. (2) For single-column GROUP BY, use the raw Value as map key. (3) Add `BenchmarkGroupKey_DirectKey` verifying elimination of intermediate allocation. | high | small | none | EX/aggregate.go |
| REQ001025 | EX/perf | **Sort key extraction allocates `make([]Value)` per row — flat buffer eliminates N allocs.** `intermediate.go:480-491`: `sk := make([]Value, len(s.keys))` allocates a new slice per row during sort materialization. For 100K rows with 2 sort keys, that is 100K allocations. **Fix:** (1) Pre-allocate a flat `[]Value` buffer of size `n * len(s.keys)` and carve sub-slices. (2) This replaces N individual allocations with 1. (3) Add `BenchmarkSort_KeyBuffer` verifying single allocation. | high | small | none | EX/intermediate.go |
| REQ001027 | EX/perf | **`serializeOuterRow` uses `fmt.Sprint` per column — correlated subquery hot path.** `eval.go:112-125`: `fmt.Sprint(v)` for every column value, then `strings.Join(parts, ",")`. For a 10-column table with 10K rows, that is 100K `fmt.Sprint` calls + 10K joins. **Fix:** (1) Switch on concrete type: `strconv.AppendInt` for int64, direct append for string. (2) Pre-size `parts` with `make([]string, 0, len(row.Cols))`. (3) Add `BenchmarkSerializeOuterRow_NoFmtSprint` verifying reduced allocations. | medium | small | none | EX/eval.go |
| REQ001028 | EX/perf | **`encodeUniqueKey` allocates `crc32.NewIEEE()` hasher per row per UNIQUE constraint.** `constraints.go:305`: `h := crc32.NewIEEE()` allocates a new hasher on every call. For a table with 2 UNIQUE constraints, that is 2 allocations per row. **Fix:** (1) Use `crc32.ChecksumIEEE(data)` directly (package-level function, no hasher allocation). (2) Or replace CRC32 with FNV-1a inline hash (no allocation, faster for small inputs). (3) Add `BenchmarkUniqueKey_Encode` verifying zero allocations. | medium | small | none | EX/constraints.go |
| REQ001029 | EX/perf | **`buildInsertRow` copies Cols slice + allocates Data per row — wastes 50% of multi-row INSERT allocs.** `source.go:186,211`: `Cols: append([]string(nil), schema...)` copies the schema slice per row. `Data: make([]Value, len(schema))` allocates a new data slice per row. All rows from the same INSERT share the same schema. **Fix:** (1) Share Cols across all rows (like Filter/refillBatch does with `sharedCols`). (2) Pre-allocate a shared backing array for all rows' Data. (3) Add `BenchmarkInsertRow_SharedCols` verifying reduced allocations. | medium | small | none | EX/source.go |
| REQ001030 | EX/perf | **`buildInsertRow` recomputes `colIdx` per call — should be hoisted to caller.** `source.go:200-210`: `colIdx := make([]int, len(cols))` plus linear scan `strings.EqualFold` per column. For INSERT INTO t(c1,c2) VALUES (...), (…), ..., this fires per row with the same column list. **Fix:** (1) Hoist `colIdx` computation to the Insert operator (compute once, pass to `buildInsertRow`). (2) Add `BenchmarkInsertRow_ColIdxHoist` verifying single computation. | medium | small | none | EX/source.go |
| REQ001031 | EX/perf | **`evalHex` double-allocation in Blob/Text paths.** `eval.go:1277-1281`: `hex.EncodeToString` allocates a string, then `strings.ToUpper` allocates another. For Text, `[]byte(v.S)` is a third allocation. **Fix:** (1) For Blob: `hex.Encode` into pre-allocated buffer, uppercase in-place, return `string(buf)`. (2) For Text: `hex.Encode([]byte(v.S))` directly. (3) Add `BenchmarkHex_NoDoubleAlloc` verifying single allocation. | medium | small | none | EX/eval.go |
| REQ001032 | EX/perf | **`encodeTablePrefix` allocates per call — should be cached per tableID.** `store.go:539-543`: `make([]byte, 8)` + `append(out, ':')` allocates a new 9-byte slice every time. The prefix is deterministic from `tableID` and never changes. **Fix:** (1) Cache the prefix per tableID in a `sync.Map` or on the schema struct. (2) Or return `[9]byte` on the stack. (3) Add `BenchmarkTablePrefix_Cached` verifying zero allocations. | medium | small | none | EX/store.go |
| REQ001033 | EX/perf | **`int64ToBytesBigEndian` allocates per call — should use stack array.** `store.go:876-888`: `make([]byte, 8)` allocates per call. Called by `pkToBytes` per index per DML. **Fix:** (1) Return `[8]byte` on the stack: `var b [8]byte; binary.BigEndian.PutUint64(b[:], uint64(n)); return b`. (2) Callers use `b[:]`. (3) Add `BenchmarkInt64ToBytes_StackAlloc` verifying zero heap allocations. | medium | small | none | EX/store.go |
| REQ001034 | WAL/perf | **WAL record encoding allocates two buffers per record.** `WAL/WR/encode.go:48-72`: `body := make([]byte, 0, capHint)` and `out := make([]byte, MaxVarintLen64, ...)` per record. For high-throughput workloads, this creates significant GC pressure. **Fix:** (1) Pool the `out` buffer via `sync.Pool`. (2) Accept a pre-allocated `dst` parameter. (3) Add `BenchmarkWAL_EncodeBufferPool` verifying reduced allocations. | medium | small | none | WAL/WR/encode.go |
| REQ001035 | EX/perf | **`indexValueFromKey` re-builds prefix per entry — should be cached.** `operators.go:720-728`: calls `buildIndexKey(tableID, idxName, nil)` on EVERY iteration to compute the expected prefix. This allocates a `[]byte` and copies the tableID + indexName each time. For 100K index entries, 100K identical allocations. **Fix:** (1) Cache the prefix in the IndexScan struct during initialization (`prefixIdxKey` is already declared at line 468 but unused in this path). (2) Add `BenchmarkIndexScan_CachedPrefix` verifying single allocation. | medium | small | none | EX/operators.go |
| REQ001036 | EX/perf | **`lookupColumn` does linear scan per probe ignoring existing `colIndex` map.** `hashcrossjoin.go:392-411`: iterates `row.Cols` linearly for every hash probe. `colIndex` is already built on the row (line 193/197) but `lookupColumn` ignores it until the fallback path. **Fix:** (1) Check `row.colIndex` first (O(1)), fall back to linear scan only if colIndex is nil. (2) Add `BenchmarkHashJoin_LookupColumn` verifying O(1) for indexed rows. | medium | small | none | EX/hashcrossjoin.go |
| REQ001037 | LS/perf | **SST block decompression allocates flate reader + bytes.Buffer per block.** `ENG/LS/sst_dict.go:164-184`: `bytes.NewReader(data)`, `flate.NewReaderDict`, and `bytes.Buffer` with `Grow` per block. Total: 3+ allocations per block decompression. For a query reading 100 blocks, that is 300+ allocations. **Fix:** (1) Pool the flate reader and output buffer on the `sstReader`. (2) Reuse `bytes.Buffer` across blocks. (3) Add `BenchmarkSST_Decompress_Pooled` verifying reduced allocations. | medium | small | none | ENG/LS/sst_dict.go |
| REQ001038 | EX/perf | **LRU cache `Get` does O(n) scan to move-to-front.** `eval.go:52-67`: `lruCache.Get` iterates `c.order` linearly to find and move the key to the front. For a cache of 256 entries, this is up to 256 iterations per correlated subquery cache hit. **Fix:** (1) Use `container/list` for O(1) move-to-front (doubly-linked list + map). (2) Add `BenchmarkLRU_GetHitRate` verifying O(1) amortized. | medium | small | none | EX/eval.go |
| REQ001039 | EX/perf | **`hashKey` allocates `maphash.Hash` per call — should use fast inline hash for int64 keys.** `hashjoin.go:305-330`: creates a new `maphash.Hash` on every invocation. For int64 keys (the common case), a direct FNV-1a mixing function (`h = h*prime ^ uint64(v)`) would be 3-5x faster with zero allocation. **Fix:** (1) Add fast path for `KindInt`: `uint64(v.I64) * 0x9e3779b97f4a7c15 ^ (uint64(v.I64) >> 31)`. (2) Keep `maphash` only for string keys. (3) Add `BenchmarkHashJoin_Int64Key` verifying faster hashing. | low | small | none | EX/hashjoin.go |
| REQ001040 | EX/perf | **`evalSubstr` calls `fmt.Sprint(s)` when value is already text.** `eval.go:1363-1364`: `rawStr.ToAny()` boxes the Value, then `fmt.Sprint(s)` does reflection-based formatting. The string is already available as `rawStr.S` when `Kind == KindText`. **Fix:** (1) Fast path: if `rawStr.Kind == KindText`, use `rawStr.S` directly. (2) Only fall back to `fmt.Sprint` for non-text types. (3) Add `BenchmarkSubstr_TextFastPath` verifying zero allocation. | low | small | none | EX/eval.go |
| REQ001042 | EX/perf | **`SeqScan.nextFromStore` calls `ctx.Err()` per row — should be batched.** `operators.go:329`: `ctx.Err()` is called on EVERY row from the store iterator. For 1M rows, that is 1M context checks. Individual cost is minor but adds up. **Fix:** (1) Check every 1024 rows (batch-aligned) using a counter. (2) Add `BenchmarkSeqScan_ContextCheck` verifying reduced overhead. | low | small | none | EX/operators.go |
| REQ001045 | EX/parallel | **Store-backed parallel SeqScan — split LSM key range across workers for real data paths.** Current `ParallelSeqScan` works on `[]Row` (in-memory only). For LSM-backed tables, the key prefix range can be split into N sub-ranges, each assigned to a worker with its own `Store.Iterator`. Workers scan their sub-range concurrently, producing rows in parallel. This covers the real data path (persistent tables via `ENG/LS/`). **Fix:** (1) Add `ParallelStoreSeqScan` operator that accepts `Store`, `schema`, key prefix, and worker count. (2) Split the key prefix range into N equal sub-ranges using `store.SplitRange(prefix, N)`. (3) Each worker opens its own iterator over its sub-range. (4) Coordinator merges results (or streams directly if order doesn't matter). (5) Add `TestParallelStoreSeqScan_RangeSplit` and `BenchmarkParallelStoreSeqScan_Persistent` verifying correctness and speedup on 1M-row LSM table. | high | large | none | EX/operators_parallel.go, EX/store.go |
| REQ001046 | EX/parallel | **Parallel hash aggregation — partition input by group key hash, build N partial hash tables concurrently.** `hashagg.go` builds a single `map[string][]Row` sequentially. For large inputs (1M+ rows), the build phase is CPU-bound and single-threaded. Parallel aggregation: (1) partition input rows by `hash(group_key) % N`, (2) build N partial hash tables in N goroutines, (3) merge partial results by combining groups with the same key. This is the standard pattern used by PostgreSQL and DuckDB. **Fix:** (1) Add `ParallelHashAggregate` operator. (2) Partition input using `WorkerPool.FanOut`. (3) Each worker builds a local hash table and computes partial aggregates. (4) Coordinator merges partial results. (5) Add `TestParallelAgg_Correctness` and `BenchmarkParallelAgg_4Core_1MRows` verifying correctness and speedup. | high | large | none | EX/hashagg.go (new parallel variant) |
| REQ001047 | EX/parallel | **Parallel hash join build/probe — build right-side hash table in parallel, probe with left partitions.** `hashjoin.go` builds the hash table on the right side sequentially, then probes with left rows one at a time. For equi-joins with large right sides (100K+ rows), the build phase is a bottleneck. Parallel approach: (1) partition right side by `hash(join_key) % N`, (2) build N partial hash tables concurrently, (3) partition left side by same hash, (4) probe each left partition against its matching right partition concurrently. **Fix:** (1) Add `ParallelHashJoin` operator. (2) Use `WorkerPool` for build and probe phases. (3) Add `TestParallelHashJoin_Correctness` and `BenchmarkParallelHashJoin_LargeRight` verifying correctness and speedup. | high | large | none | EX/hashjoin.go (new parallel variant) |
| REQ001048 | EX/parallel | **Wire `SubCompactor` into the compaction loop — parallel sub-compaction exists but is orphaned.** `ENG/LS/subcompact.go` implements `SubCompactor` that splits input SSTs into key-range partitions and runs sub-merges through `errgroup` with NUMA-aware thread pinning. However, `compaction.go:compactionJob.Run()` is the only compaction path and it runs serially. The `SubCompactor` is never called. **Fix:** (1) In `compactionManager.requestCompaction()`, dispatch through `SubCompactor` when input SST count > 4. (2) Keep serial path as fallback for small compactions. (3) Add `TestSubCompactor_Integration` and `BenchmarkCompaction_Parallel` verifying correctness and speedup. | high | medium | none | ENG/LS/compaction.go, ENG/LS/subcompact.go |
| REQ001051 | EX/parallel | **Parallel index range scan — multiple index ranges from IN-list or OR predicates can be scanned concurrently.** `IndexScan` currently scans one range at a time. For `WHERE col IN (1, 2, 3, ..., 100)`, the planner creates 100 index lookups that run sequentially. These are independent and embarrassingly parallel. **Fix:** (1) Add `ParallelIndexRangeScan` that fans out N index lookups across workers. (2) Merge results (preserving order if needed). (3) In the Planner, when IN-list has > 10 values, emit `ParallelIndexRangeScan` instead of sequential `IndexScan`. (4) Add `TestParallelIndexRangeScan_INList` and `BenchmarkParallelIndexRangeScan_100Values` verifying correctness and speedup. | medium | medium | none | EX/operators_parallel.go, EX/planner.go |
| REQ001054 | EX/parallel | **Configurable parallelism — `MaxParallelism` setting to control worker count per query.** Currently there is no way to control how many cores a single query uses. A 4-core machine running 4 concurrent queries would have each query using all 4 cores, causing contention. **Fix:** (1) Add `MaxParallelism int` to `Executor` config (default: `runtime.GOMAXPROCS(0)`). (2) Each query's `WorkerPool` is sized to `min(MaxParallelism, availableWorkers)`. (3) For concurrent queries, the total worker count is bounded by the global pool. (4) Add `TestExecutor_MaxParallelism` verifying that 4 concurrent queries with `MaxParallelism=2` don't exceed 8 total workers. | medium | small | none | EX/ex.go, EX/parallel.go |
| REQ001055 | EX/parallel | **NUMA-aware operator placement — detect NUMA topology and pin workers to local memory.** `ENG/NM/numa.go` detects NUMA nodes but is only used by `SubCompactor`. For query execution, workers that access the same data should be pinned to the same NUMA node to avoid cross-node memory access (2-3x latency penalty). **Fix:** (1) In `WorkerPool`, accept a `NUMATopology` parameter. (2) When partitioning work (parallel scan, parallel agg, parallel join), assign partitions to workers on the same NUMA node as the data. (3) Use `nm.BindThread(node)` to pin worker goroutines. (4) Add `TestNUMA_WorkerPlacement` verifying workers are pinned to local NUMA nodes. | low | medium | none | EX/parallel.go, ENG/NM/numa.go |
| REQ001056 | EX/join | **Hash join is fully in-memory with no spill-to-disk and no memory cap — a deep equi-join with a bad planner order can OOM the process.** `hashjoin.go:164-208` materializes the entire right side into `buckets[i].rightRows` (per-partition `[]Row` slices) and the entire left side into `j.leftRows`, then pre-computes every match into `j.dataBuf` and `j.matches` (line 268-298). There is no `join_buffer_size` cap, no disk-spill of over-budget partitions, and no rejection at planning time for estimated-cardinality-too-large plans. The `TestSelect5_MultiTableJoin_OOM` test (`select5_bench_test.go:127`) explicitly documents "OOM due to unnecessary intermediate result sets" as a known failure mode for 7+ table joins where the planner picks a bad join order; the test passes today only because every tested query has a constant-anchored table (`aN=const`) that prunes to 1 row, hiding the latent blow-up. If the planner ever picked a cartesian-like order on a 10-row × 64-table equi-join (10^64 worst case, 10^6+ realistic with weak selectivity), `rightRows` alone would OOM long before the join completes. **Defenses missing that MySQL provides** (per MySQL 8.0 hash join + filesort docs): (1) `join_buffer_size`-bounded in-memory hash table; (2) partition spill to on-disk TempFile when budget exceeded; (3) planner-time cardinality estimate that rejects plans exceeding `maxMemoryPerQuery`; (4) `sort_buffer_size`-bounded filesort with on-disk merge runs for any trailing ORDER BY. None of these exist in razordata — `EX/` has zero references to spill, TempFile, or sort_buffer, and `EX/select5_bench_test.go:85` creates an executor with no memory budget hook. **Fix:** (1) Add `JoinBufferSize int64` and `MaxMemoryPerQuery int64` config on `Executor` (defaults: 64 MiB / 1 GiB). (2) In `HashJoin.buildAndProbe`, when a bucket's right side exceeds its share of `JoinBufferSize`, spill the bucket's rows to a TempFile under `tmpdir` and re-read in the probe phase. (3) In the planner, when the estimated intermediate row count × avg row width exceeds `MaxMemoryPerQuery`, either pick a smaller fan-in (fewer tables per join step) or error with `ErrQueryTooLarge`. (4) Add `TestHashJoin_SpillToDisk` using a synthetic 1M-row build side and `JoinBufferSize=1MiB`, verifying the join completes and returns correct results. (5) Add `TestPlanner_RejectsOOMQuery` with a 64-way unanchored join asserting the planner returns `ErrQueryTooLarge` instead of OOMing. (6) Promote the existing `TestSelect5_MultiTableJoin_OOM` from a soft-log to a hard assertion that alloc delta stays under a configured budget across all queries. | high | large | none | EX/hashjoin.go, EX/planner.go, EX/ex.go, EX/tempfile.go (new), internal/SQB/EX/select5_bench_test.go |
