## TBD

| ID | Subsystem | Requirement | Priority | Effort | Deps | Touches |
| --- | --- | --- | --- | --- | --- | --- |
| REQ000543 | SQL/EX | Shape-specialized fast paths — extend REQ000311/313 with shape-driven specialization that captures hot path inline as small specialized Go funcs (not full JIT); specialize per plan shape: `Filter{int64_col_lit_eq}`, `Project{cols=N}`, `HashAggregate{group_by_int64}`, etc.; registered in `codegen_ops.go`; benchmarks must show >1.5x over the interpreted path. Deferred: XL effort, iter-29. | medium | XL | REQ000311 (codegen framework), REQ000313 (ADQC) | `SQL/EX/codegen/shapes/` (new); `SQL/EX/adqc_specialize.go` (new); `SQL/EX/shape_bench_test.go` (new) |
| REQ000546 | SQL/EX | Vectorized string columns via unsafe pointer + length prefix — replace `[]string` columns with `[]byte` raw + `[]uint32` lengths (or `[]struct{Ptr uintptr; Len uint32}`); eliminates string-header copy and enables real SIMD string compare (memcmp); backward-compat path via `Batch.StringValue(col,row)` accessor. Deferred: L effort, iter-29. | medium | L | REQ000149 (batch memory), REQ000310 (SIMD) | `SQL/EX/batch.go` `Column` for text/varchar/blob; `SQL/EX/eval_vec.go` `compareStringColsVec`; `SQL/EX/simd_dispatch.go` runtime detection |
| REQ000549 | MEM/BF | Block-level MVCC version tagging — buffer pool slots carry an optional `versionRange [startTS, endTS]` field; `BufferPool.GetWithTS(blockID, readTS)` returns the page whose version range covers `readTS` (or the page whose range is the latest, then we filter via the version chain at read time); enables cross-transaction cache sharing with snapshot consistency. Deferred: XL effort, iter-29. | low | XL | iter-02 (buffer pool), REQ000539 (sharded), REQ000307 (MV-OCC) | `MEM/BF/mvcc_slot.go` (new) + `bf.go` `GetWithTS`; `TXN/SN/snapshot.go` registers `readTS` on Get |
| REQ000644 | SQL/PS | **Add doc comments to AST types** — All exported types in `ast.go` need doc comments beginning with the type name. Includes `Expr` and `Stmt` interfaces, all 24 expression types, all statement types, helper types, `Parser`, `NewParser()`, `ErrSyntax`, `SyntaxError`. | medium | S | none | `SQL/PS/ast.go`, `SQL/PS/ps.go`, `SQL/PS/error.go` |
| REQ000645 | TXN/MV | **Add doc comments to MV types** — `MV` struct, `NewMV()`, and all exported methods; `VersionChain` struct and methods; `VersionNode` methods; `Arena` struct and methods; `GetArena`, `PutArena`, `ReclaimOldGenerations` | medium | S | none | `TXN/MV/mv.go`, `TXN/MV/version.go`, `TXN/MV/arena.go` |
| REQ000646 | ENG/LS | **Add doc comments to LS types** — `ReadStats`, `Version`, `SSTFileMeta`, `ManifestCheckpoint`, `RangeIter`, `Options`, `DefaultOptions()`, `Engine` methods, `PageCache` and methods, exported constants | medium | S | none | `ENG/LS/engine.go`, `ENG/LS/api.go`, `ENG/LS/manifest.go`, `ENG/LS/page_cache.go`, `ENG/LS/stats.go` |
| REQ000649 | ENG/LS | **Table-driven tests for skiplist** — Consolidate `TestInsertSingle`, `TestInsertMultiple`, `TestFindNonExistent`, `TestInsertDuplicateKey` into a single `TestSkipList` with `t.Run` subtests | low | S | none | `ENG/LS/skiplist_test.go` |
| REQ000660 | ENG/LS+TB | **Extract shared catalog to `ENG/catalog`** — `ENG/TB/catalog.go` and `ENG/LS/catalog.go` share identical catalog logic. Extract shared catalog to `ENG/catalog/catalog.go`. Eliminates ~400 lines of duplication. | high | L | REQ000653 | new `ENG/catalog/catalog.go`; `ENG/TB/catalog.go` — delete, import from `ENG/catalog`; `ENG/LS/catalog.go` — delete shared portions |
| REQ000664 | SYS/AP | **Embed subsystem stats types instead of re-declaring** — `AP.LSMTreeStats`, `BufferPoolStats`, `WALStats`, `TxnStats` re-declare fields identical to subsystem types. Replace with embedded types. | medium | M | none | `SYS/AP/ap.go` — embed subsystem stats types; `SYS/SY/sy.go` — update `Stats()` aggregation |
| REQ000665 | SYS/AP+ENG/LS | **Centralize default constants** — Subsystems hardcode defaults instead of referencing `AP.Default*`. Audit and replace. | low | S | none | `ENG/LS/engine.go`, `MEM/BF/bf.go`, `WAL/WR/wr.go` |
| REQ000671 | ENG/DP | **Pre-allocate buffers in `EncodeBlock`/`DecodeBlock`/`EncodeRow`** — `var buf []byte` without capacity in hot paths. Pre-calculate size and pre-allocate. | medium | S | none | `ENG/DP/dp.go` |
| REQ000672 | WAL/WR | **Pool LZ4 hash table** — `lz4/lz4.go` allocates 16KB on every compress call. Use `sync.Pool`. | medium | S | none | `WAL/WR/lz4/lz4.go` |
| REQ000673 | FIL/DF | **Remove unnecessary buffer zeroing in `returnTempBuf`** — Zeros 4096 bytes on every return. Buffer is always overwritten before next use. | medium | S | none | `FIL/DF/df.go` |
| REQ000674 | FIL/MF | **Eliminate double-encoding in `writeMeta`** — `MarshalBinary` encodes, then `writeMeta` re-encodes. Compute CRC from `MarshalBinary` output directly. | medium | S | none | `FIL/MF/mf.go` |
| REQ000675 | FIL/LF | **Remove TOCTOU `os.Stat` before `unix.Open`** — `os.Stat` then `unix.Open` is a race. Just `unix.Open` and handle error. | medium | S | none | `FIL/LF/lf.go` |
| REQ000676 | WAL/RP | **Eliminate double file open in replay** — Opens segment via `GetSegment` then again via `unix.Open`. Use `fh.FD` directly. | medium | S | none | `WAL/RP/rp.go` |
| REQ000677 | ENG/SC | **Remove stateless `Validator` type** — Zero-field struct with heap allocation. Convert to package-level functions. Remove dead `ValidateConstraints` and pass-through wrappers. | medium | S | none | `ENG/SC/sc.go` |
| REQ000678 | ENG/DP | **Unify `Pair` and `KV` types** — Structurally identical types. Unify into single type. | low | S | none | `ENG/DP/dp.go` |
| REQ000679 | ENG/SC | **Combine redundant `CTInt`/`CTBigInt` branches** — Identical logic in separate cases. Combine. | low | S | none | `ENG/SC/sc.go` |
| REQ000680 | ENG/LS | **Pre-allocate `memtables` slice in engine** — `make([]memtableIface, 0)` grows via append. Use initial capacity. | low | S | none | `ENG/LS/engine.go`, `ENG/LS/sst_writer.go`, `TXN/SN/snapshot.go` |
| REQ000681 | ENG/SC | **Zero-copy `EncodeVarchar`/`EncodeText`** — `[]byte(v)` allocates. Add `EncodeVarcharBytes` identity variant. | low | S | none | `ENG/SC/sc.go` |
| REQ000683 | FIL/DF+MF+LF | **Disk full graceful handling** — Detect `ENOSPC`, propagate as `AP.ErrIO`. No retry. | medium | M | REQ000651 | `FIL/DF/df.go`, `FIL/MF/mf.go`, `FIL/LF/lf.go` |
| REQ000684 | SQL/EX | **Multi-column hash join keys** — Single key column only. Extend with composite key via length-prefixed concatenation. | medium | M | none | `SQL/EX/hashjoin.go` |
| REQ000685 | SQL/EX+PL | **LEFT OUTER JOIN** — Extend `NestedLoopJoin` with `leftOuter` flag. Emit NULL-padded left row when no match. | medium | L | none | `SQL/EX/join.go`, `SQL/EX/planner.go` |
| REQ000686 | SQL/EX | **RANGE window frame spec** — ROWS implemented. RANGE requires peer-group detection. Defer. | low | L | none | `SQL/EX/window.go` |
| REQ000687 | SYS/SY+AP | **Configurable shutdown timeout** — Add `Options.ShutdownTimeout` field. Default 30s. | low | S | none | `SYS/AP/ap.go`, `SYS/SY/sy.go` |
| REQ000688 | SYS/SY | **Emergency shutdown mode** — Add `Options.EmergencyShutdown` flag. Skip Phase 2+3. | low | S | none | `SYS/AP/ap.go`, `SYS/SY/shutdown.go` |
| REQ000689 | ALL | **Adopt `slices`/`maps`/`cmp` packages** — Replace all `sort.*` with `slices.*`. Replace `reflect.DeepEqual` with `slices.Equal`. | high | M | none | 15+ files |
| REQ000690 | ENG/LS | **Replace `binary.Write` with direct encoding in manifest** — 6 reflection-based calls. Replace with `PutUint64`/`PutUint32`. | high | S | none | `ENG/LS/manifest.go` |
| REQ000691 | ALL | **Migrate `math/rand` to `math/rand/v2`** — 9 files use deprecated API. Standardize. | high | M | none | 9 files |
| REQ000692 | ENG/LS+SQL/EX | **Use `errgroup` for parallel operations** — Replace WaitGroup+semaphore boilerplate. | high | S | none | `ENG/LS/subcompact.go`, `SQL/EX/sort_parallel.go` |
| REQ000693 | ALL | **`for range N` syntax migration** — ~100 C-style loops. Replace with `for i := range N`. | medium | M | none | ~20 files |
| REQ000694 | ENG/LS+SQL/EX+FIL/LF | **Replace `fmt.Sprintf` with `strings.Builder`/`strconv` in hot paths** — ~30 sites. | medium | M | none | 6 files |
| REQ000695 | ALL | **Typed atomics migration** — 34 call sites use old-style `atomic.*` functions. Replace with typed wrappers. | medium | M | none | 8 files |
| REQ000696 | ENG/LS | **Pool `sstWriter` instances** — Allocates new writer per flush/compaction. Add `sync.Pool`. | low | S | none | `ENG/LS/sst_writer.go` |
| REQ000697 | ENG/LS | **Reorder `CatalogEntry` struct fields** — `Version uint8` before `TableID uint64` causes 7B padding. | low | S | none | `ENG/LS/catalog.go` |
| REQ000698 | ENG/LS | **Pre-allocate `bytes.Buffer` in compression paths** — Add `Grow()` before writes. | low | S | none | `ENG/LS/sst_dict.go` |
| REQ000700 | tests/sqlcmp/slt | **Fix over-broad `primary key` classifier pattern** — Matches genuine failures. Narrow to "unsupported" semantic. | medium | S | none | `tests/sqlcmp/slt/razor_driver.go` |
| REQ000701 | tests/sqlcmp/slt | **Add unit tests for SLT framework gaps** — 8 areas with missing test coverage. | medium | M | none | `tests/sqlcmp/slt/parser_test.go`, `runner_test.go` |
| REQ000702 | tests/sqlcmp/slt | **Expand corpus subset to cover missing SQL categories** — 8/622 files (1.3%). Add index/between, index/commute, etc. | high | M | none | `tests/sqlcmp/slt/subset.go` |
| REQ000703 | tests/sqlcmp/slt | **Add blob (B) type code support** — SLT spec defines B for blob. Not needed for current corpus but required for full compliance. | low | S | none | `tests/sqlcmp/slt/types.go` |
| REQ000705 | SQL/EX | **Unroll and specialize vectorized comparison kernels** — Scalar loops with per-element switch. Hoist switch, add 4-wide unrolling. 3-5x improvement. | high | M | none | `SQL/EX/eval_vec.go` |
| REQ000706 | ENG/LS | **Fix primary index key comparison allocations** — `string(curr.key) == string(key)` allocates. Use `bytes.Equal`. | high | S | none | `ENG/LS/index.go` |
| REQ000707 | SQL/EX | **Replace invertSelection map with bitmap** — Allocates map per NOT predicate. Use `[BatchSize]bool`. | high | S | none | `SQL/EX/eval_vec.go` |
| REQ000708 | ENG/LS | **Process FNV-1a in 8-byte chunks** — 1 byte/iteration. Process uint64 chunks for 4-6x improvement. | medium | M | none | `ENG/LS/fnv.go` |
| REQ000709 | ENG/LS | **Make bloom size power-of-2 for bitmask access** — Non-pow2 modulus is 30x slower than bitmask. | medium | M | REQ000571 | `ENG/LS/sst_writer.go`, `ENG/LS/sst_reader.go` |
| REQ000710 | ENG/LS | **Merge bloom construction into single pass** — Two separate passes over all keys. Merge into one. | medium | S | none | `ENG/LS/sst_writer.go` |
| REQ000711 | WAL/WR | **Zero-copy columnar decode** — Allocates per record value. Return borrowed slices. | medium | M | none | `WAL/WR/columnar_decode.go` |
| REQ000712 | ENG/LS | **Skip list node field reordering for cache locality** — `next[0]` in different cache line from `key`. Reorder fields. | low | S | none | `ENG/LS/skiplist.go` |
| REQ000727 | SQL/EX | **Implement `sqlite_master` virtual table** — `SELECT name FROM sqlite_master WHERE type='table'`. SLT corpus compatibility. | medium | M | REQ000726 | `SQL/EX/virtual.go` (new) |
| REQ000729 | SQL/EX | **Implement `PRAGMA table_info`** — Returns column metadata. Most critical missing PRAGMA. | critical | M | REQ000728 | `SQL/EX/pragma.go` |
| REQ000730 | SQL/EX | **Implement `PRAGMA foreign_keys`** — Enable/disable FK enforcement. | critical | M | REQ000728 | `SQL/EX/pragma_config.go`, `constraints.go` |
| REQ000731 | SQL/EX | **Implement `PRAGMA index_list` and `PRAGMA index_info`** — Index introspection. | high | M | REQ000728 | `SQL/EX/pragma.go` |
| REQ000732 | SQL/EX | **Implement `PRAGMA table_list`** — SQLite 3.39.0+ table list with metadata. | high | S | REQ000728 | `SQL/EX/pragma.go` |
| REQ000733 | SQL/EX | **Implement `PRAGMA foreign_key_check` and `PRAGMA foreign_key_list`** — FK introspection. | medium | M | REQ000730 | `SQL/EX/pragma.go` |
| REQ000734 | SQL/EX | **Wire PRAGMA values to subsystems via `PragmaListener`** — WAL/ENG/MEM listen for config changes. | medium | M | REQ000728 | `ENG/LS/engine.go`, `WAL/WR/wr.go`, `SYS/SY/sy.go` |
| REQ000735 | SQL/EX | **Implement `PRAGMA wal_checkpoint` and `PRAGMA wal_autocheckpoint`** — WAL management. | medium | M | REQ000734 | `SQL/EX/pragma.go`, `WAL/FL/` |
