## TBD

| ID | Subsystem | Requirement | Priority | Effort | Deps | Touches |
| --- | --- | --- | --- | --- | --- | --- |
| REQ000719 | EX/eval | **Unary `+`/`-` before column reference causes `ex: eval error` via database/sql driver path.** `SELECT + - col0 FROM tab1` returns `ex: eval error` when executed through the `database/sql` driver path. The same query works correctly through the in-memory executor (`QueryAll`). The issue is specific to the driver/store path where the expression evaluation context differs. The `evalUnary` function for `T_PLUS` returns the operand as-is, but the operand may not be `int64`/`float64` in the driver path (possibly `string` or other type from deserialization). Fix: ensure `evalUnary` handles all numeric types consistently, including values that come through the store deserialization path. | medium | medium | — | EX/eval.go |
| REQ000720 | EX/eval | **`alias.col` qualified name does not resolve via database/sql driver path.** `SELECT cor0.col1 FROM tab1 AS cor0 GROUP BY cor0.col1` returns the string `"cor0.col1"` instead of the column value when executed through the `database/sql` driver. The same query works correctly through the in-memory executor. The `QualifiedName` resolution in `eval.go` tries `e.Table + "." + e.Name` (e.g. `"cor0.col1"`) against row columns, but the driver path may produce rows with different column naming (bare `"col1"` vs prefixed `"cor0.col1"`). Fix: ensure the driver/store path produces rows with consistent column naming that matches the `QualifiedName` resolution expectations. | medium | medium | — | EX/eval.go |
| REQ000721 | EX/eval | **`NULL IN (subquery)` returns NULL instead of 0.** `SELECT null IN (SELECT * FROM t1)` returns `[[NULL]]` instead of `[[0]]`. Per SQL semantics, when the left operand of `IN` is NULL and the subquery contains no NULLs, the result should be 0 (false), not NULL. The `evalInSubquery` function returns `nil` when `target == nil` (line 329-331 of `eval.go`), but this should return `false` when no NULLs exist in the subquery results. Fix: in `evalInSubquery`, when `target` is nil, iterate the subquery results — if any result is NULL, return nil (UNKNOWN); if no NULLs exist, return false. | low | small | — | EX/eval.go |
| REQ000722 | EX/planner | **WHERE with comparison operators on indexed tables returns 0 rows via driver path.** `SELECT pk FROM tab1 WHERE col0 <= 605 ORDER BY 1 DESC` returns 0 rows instead of 68 via the `database/sql` driver path. The same query works correctly through the in-memory executor. This is the single largest SLT failure class (~40,000+ failures across all `index_*` test suites). The issue appears to be specific to how the IndexScan or SeqScan evaluates WHERE predicates when reading from the engine store through the driver. Possible causes: (1) the IndexScan doesn't apply WHERE filters after reading from the store, (2) the store deserialization produces values in a different type than expected (e.g. `string` vs `int64`), causing comparison operators to fail, or (3) the planner picks an IndexScan path that doesn't properly filter results. Fix: investigate the driver path's scan+filter chain to identify where WHERE predicates are dropped or mis-evaluated. | high | large | — | EX/planner.go, EX/operators.go, driver/ |
| REQ000723 | EX/planner | **`NOT(...)` filter returns wrong results on indexed tables via driver path.** `SELECT pk FROM tab0 WHERE NOT ((col0 > 68))` returns fewer rows than expected via the driver path. The `NOT` expression wrapping a comparison predicate is not properly evaluated when the scan reads from the engine store. Related to REQ000722 — likely the same root cause (predicate evaluation failure on store-backed rows). Fix: ensure `NOT` expressions are correctly propagated through the scan+filter chain in the driver path. | medium | medium | REQ000722 | EX/planner.go, EX/eval.go |
| REQ000724 | EX/planner | **Complex OR/AND/IN conditions on indexed tables return 0 rows via driver path.** `SELECT pk FROM tab1 WHERE col4 > 12.48 OR (col1 IN (63.52,29.7,...) OR col4 >= 40.21) AND col3 > 64` returns 0 rows instead of the expected count via the driver path. Multi-clause WHERE conditions with OR, IN, and AND combinations fail when the scan reads from the engine store. Related to REQ000722 — likely the same root cause. Fix: ensure complex boolean expressions are correctly evaluated against store-backed rows. | medium | medium | REQ000722 | EX/planner.go, EX/eval.go |
| REQ000725 | EX/planner | **Multi-table implicit cross join (4+ tables) returns 0 rows.** `SELECT x29,x31,x51,x55 FROM t51,t29,t31,t55 WHERE a51=b31 AND a29=6 AND a29=b51 AND b55=a31` returns 0 rows instead of 4. The query uses implicit cross join syntax (multiple tables in FROM clause) with WHERE conditions acting as join predicates. The planner's `planSelect` only handles explicit JOINs via `s.Joins` — when multiple tables are listed in FROM with comma separation, only the first table is scanned and the rest are ignored. Fix: the parser should populate `s.Joins` for comma-separated tables (treating them as CROSS JOINs), or the planner should handle multiple FROM tables by chaining NestedLoopJoins. | medium | large | — | PS/ps.go, EX/planner.go |
| REQ000736 | PS/parser | **NULLS FIRST / NULLS LAST not supported in ORDER BY.** `SELECT * FROM t ORDER BY col NULLS FIRST` fails with a syntax error. SQLite 3.30+ supports `NULLS FIRST` and `NULLS LAST` modifiers in ORDER BY terms to control whether NULL values sort before or after non-NULL values. The parser's ORDER BY handler does not recognize these keywords after the column name and ASC/DESC. Fix: extend the ORDER BY parser to accept an optional `NULLS FIRST` or `NULLS LAST` modifier, and apply it during the sort comparison. | low | small | — | PS/ps.go, EX/intermediate.go |
| REQ000737 | PS/parser | **CREATE VIRTUAL TABLE not supported.** `CREATE VIRTUAL TABLE t USING fts5(content)` fails with a syntax error. SQLite supports `CREATE VIRTUAL TABLE ... USING module_name(args)` for extension modules like FTS5, R-Tree, and JSON. The parser does not handle the `VIRTUAL` keyword or the `USING module(args)` syntax. Fix: parse `CREATE VIRTUAL TABLE` and store the module name and arguments, even if the runtime doesn't execute virtual table modules. | low | large | — | PS/ps.go |
| REQ000738 | PS/parser | **WITHOUT ROWID table option not supported.** `CREATE TABLE t (id TEXT PRIMARY KEY, v TEXT) WITHOUT ROWID` fails with a syntax error. SQLite supports `WITHOUT ROWID` as a table option after the column definitions to create a table without the implicit rowid, using the PRIMARY KEY as the clustered key. The parser does not recognize `WITHOUT` after the closing `)` of CREATE TABLE. Fix: extend the CREATE TABLE parser to accept an optional `WITHOUT ROWID` suffix. | low | small | — | PS/ps.go |
| REQ000739 | PS/parser | **STRICT table mode not supported.** `CREATE TABLE t (id INTEGER PRIMARY KEY, v TEXT NOT NULL) STRICT` fails with a syntax error. SQLite 3.37+ supports `STRICT` mode which enforces type checking on INSERT/UPDATE. The parser does not recognize the `STRICT` keyword after CREATE TABLE. Fix: extend the CREATE TABLE parser to accept an optional `STRICT` suffix and enforce type validation on DML operations. | low | medium | — | PS/ps.go |
| REQ000740 | PS/parser | **UPDATE ... FROM not supported.** `UPDATE t1 SET v = t2.v FROM t2 WHERE t1.id = t2.id` fails with a syntax error. SQLite 3.33+ supports `UPDATE ... FROM` which allows the SET clause to reference columns from additional tables in a FROM clause (similar to PostgreSQL). The parser exits after the WHERE clause in `parseUpdate` and does not handle a FROM clause. Fix: extend the UPDATE parser to accept an optional `FROM table_reference` after the SET clause. | low | medium | — | PS/ps.go |
| REQ000741 | EX/eval | **Trigger WHEN clause not parsed as expression.** `CREATE TRIGGER t AFTER INSERT ON t1 WHEN NEW.v > 10 BEGIN ... END` — the WHEN clause is consumed by eating tokens until `BEGIN` or `;`, but the expression is discarded and never evaluated. When the trigger fires, the WHEN condition is not checked, so the trigger body always executes. Fix: parse the WHEN clause as a real expression using `parseExpr()`, and evaluate it against the `NEW`/`OLD` rows when the trigger fires. | medium | medium | — | PS/ps.go, EX/source.go |
| REQ000742 | PS/parser | **EXPLAIN does not cover all statement types.** `EXPLAIN CREATE TRIGGER ...`, `EXPLAIN ALTER TABLE ...`, `EXPLAIN PRAGMA ...`, `EXPLAIN SAVEPOINT ...`, `EXPLAIN BEGIN ...` all fail with syntax errors. EXPLAIN only wraps SELECT, INSERT, UPDATE, DELETE, CREATE TABLE/INDEX/VIEW, DROP TABLE/INDEX/VIEW/TRIGGER, ANALYZE, VACUUM. Fix: extend the EXPLAIN parser to wrap any statement type, not just the currently supported subset. | low | small | — | PS/ps.go |
| REQ000743 | EX/planner | **RIGHT OUTER JOIN may not emit unmatched right rows.** `SELECT * FROM t1 RIGHT JOIN t2 ON t1.id = t2.id` — the `NestedLoopJoin` sets `leftOuter` for LEFT and FULL joins (line 44: `leftOuter: kind == JoinKindLeft \|\| kind == JoinKindFull`) but does NOT set a corresponding `rightOuter` flag for RIGHT and FULL joins. This means unmatched right-side rows may not be emitted with NULL-padded left columns. Fix: add a `rightOuter` flag to `NestedLoopJoin` and emit NULL-padded rows when the right side has no matching left rows. | medium | medium | — | EX/join.go |
| REQ000744 | EX/planner | **FULL OUTER JOIN may miss unmatched rows from one side.** Same root cause as REQ000743 — `NestedLoopJoin` only tracks `leftOuter` for unmatched left rows. For FULL OUTER JOIN, unmatched rows from the right side (no matching left rows) may not be emitted. The `rightOuter` flag is missing entirely. Fix: add `rightOuter` tracking and emit NULL-padded rows from both sides when no match is found. | medium | medium | REQ000743 | EX/join.go |
| REQ000745 | PS/parser | **Window frame shorthand (without BETWEEN) not supported.** `SELECT ROW_NUMBER() OVER (ORDER BY id ROWS 3 PRECEDING)` fails with a syntax error. SQLite supports shorthand window frame specifications like `ROWS n PRECEDING` (equivalent to `ROWS BETWEEN n PRECEDING AND CURRENT ROW`). The parser only handles the full `ROWS BETWEEN ... AND ...` form. Fix: extend the window frame parser to accept the shorthand form without `BETWEEN`. | low | small | — | PS/ps.go |
| REQ000746 | PS/parser | **GROUPS window frame type not supported.** `SELECT SUM(v) OVER (ORDER BY id GROUPS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW)` fails with a syntax error. SQLite 3.28+ supports `GROUPS` as a window frame type (alongside `ROWS` and `RANGE`). The parser only recognizes `ROWS` and `RANGE`. Fix: add `GROUPS` as a recognized window frame type. | low | small | — | PS/ps.go |
| REQ000747 | PS/parser | **FILTER clause on aggregate functions not supported.** `SELECT COUNT(*) FILTER (WHERE v > 10) FROM t` fails with a syntax error. SQLite 3.30+ supports the `FILTER (WHERE ...)` clause on aggregate and window functions to apply a filter before aggregation. The parser does not recognize the `FILTER` keyword after a function call. Fix: extend the aggregate/window function parser to accept an optional `FILTER (WHERE expr)` clause, and apply it during aggregation. | low | medium | — | PS/ps.go |
| REQ000748 | PS/parser | **EXCLUDE clause in window frames not supported.** `SELECT SUM(v) OVER (ORDER BY id ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW EXCLUDE NO OTHERS)` fails with a syntax error. SQLite 3.28+ supports `EXCLUDE` clauses in window frame specifications (`EXCLUDE CURRENT ROW`, `EXCLUDE GROUP`, `EXCLUDE TIES`, `EXCLUDE NO OTHERS`). Fix: extend the window frame parser to accept an optional `EXCLUDE` clause. | low | small | — | PS/ps.go |
| REQ000749 | EX/eval | **JSON_INSERT, JSON_REPLACE, JSON_REMOVE functions not supported.** `SELECT JSON_INSERT('{"a":1}', '$.b', 2)` returns `ex: eval error`. SQLite 3.38+ supports `JSON_INSERT`, `JSON_REPLACE`, and `JSON_REMOVE` for modifying JSON values. The `json.go` file implements `JSON_EXTRACT`, `JSON_TYPE`, `JSON_VALID`, `JSON_ARRAY`, `JSON_OBJECT`, `JSON_SET` but not the INSERT/REPLACE/REMOVE variants. Fix: add `JSON_INSERT`, `JSON_REPLACE`, and `JSON_REMOVE` to the JSON function dispatch in `json.go`. | low | medium | — | EX/json.go |
| REQ000750 | PS/parser | **Three-part qualified names (`database.table.column`) not supported.** `SELECT main.t.id FROM main.t` fails with a syntax error. SQLite supports three-part qualified names for cross-database references. Razordata only supports two-part names (`table.column`). The parser's `QualifiedName` handling does not chain a third component. Fix: extend the parser to recognize `ident.ident.ident` as a three-part qualified name. Since multi-database is not supported at runtime (ATTACH is rejected), this is low priority. | low | small | — | PS/ps.go |
| REQ000751 | PS/parser | **TRUNCATE TABLE parsed but not in EXPLAIN.** `EXPLAIN TRUNCATE TABLE t` fails with a syntax error. The TRUNCATE statement is supported as a standalone DML but is not listed in the EXPLAIN dispatch table. Fix: add `TRUNCATE` to the EXPLAIN parser's statement dispatch. | low | small | — | PS/ps.go |
| REQ000753 | EX/eval | **`compare()` converts int64 to float64 before comparing.** CPU profile: `compare` is called from `evalBinary` (T_LT/T_LE/T_GT/T_GE), Sort comparators, and Aggregate MIN/MAX. At `eval.go:1575-1578`, it calls `numericFloat(a)` and `numericFloat(b)` which type-switch and convert int64→float64, then compares as float64. This loses precision for int64 values > 2^53 and wastes cycles on unnecessary conversion. **Fix:** Add int64-int64 and float64-float64 fast paths before the `numericFloat` fallback. When both operands are the same type, compare directly without conversion. **Implementation:** (1) At `eval.go:1575`, insert int64-int64 fast path: `if ai, ok := a.(int64); ok { if bi, ok := b.(int64); ok { if ai < bi { return -1 }; if ai > bi { return 1 }; return 0 } }`. (2) Insert float64-float64 fast path similarly. (3) Keep the existing `numericFloat` fallback for mixed types. (4) Add benchmark: `BenchmarkCompare_IntInt`, `BenchmarkCompare_FloatFloat`. Expected: eliminates float64 conversion for pure-integer comparisons (most common in SQL). | high | small | — | EX/eval.go |
| REQ000754 | EX/eval | **`equalValue` re-boxes values via `normalizeInt` before type-switching.** At `eval.go:1913-1915`, `equalValue` calls `normalizeInt(a)` and `normalizeInt(b)` which return `any` (re-boxing the value), then immediately type-switches on the re-boxed result. For int64 values (the most common case), this is two wasted re-boxings via Go's `convT64` runtime. `equalValue` is called from `evalBinary` (T_EQ, T_NE), `evalIn`, `evalCase`, `rowEqual`, and `keysEqual`. **Fix:** Check int64 directly at the top of `equalValue` without calling `normalizeInt`. Only call `normalizeInt` as a fallback for `int` type (Go's non-64-bit int). **Implementation:** (1) At `eval.go:1913`, replace the `normalizeInt` calls with direct type assertions: `if ai, ok := a.(int64); ok { if bi, ok := b.(int64); ok { return ai == bi }; if bf, ok := b.(float64); ok { return float64(ai) == bf }; if bi, ok := b.(int); ok { return ai == int64(bi) }; return false }`. (2) Add float64 fast path similarly. (3) Keep `normalizeInt` only as last resort for `int` type. (4) Add benchmark: `BenchmarkEqualValue_IntInt`, `BenchmarkEqualValue_StringString`. | high | small | — | EX/eval.go |
| REQ000757 | EX/intermediate | **`Filter.Next` takes `&row` address forcing heap escape.** At `intermediate.go:54`, `Eval(f.predicate, &row, f.params)` takes the address of the local `row` variable. Since `Eval` is a large function that stores the pointer in closures, passes it to sub-evaluations, and accesses it across function boundaries, the compiler must heap-allocate the `Row`. Every filtered row pays this allocation. **Fix:** Use the `Row` returned directly from the child (which is already heap-allocated for store-backed paths via `decodeRow`). For in-memory paths, the `Row` from `cloneRow` is also heap-allocated. The `&row` is unnecessary — the child's returned `Row` is already on the heap. **Implementation:** (1) Verify that all child operators return heap-allocated `Row` values (they do: `SeqScan.nextFromStore` returns from `decodeRow`, `SeqScan.Next` returns from `cloneRow`). (2) Change line 54 to use a pointer that's already heap-resident: store the child's returned row in a `Filter` field `curRow *Row` and pass that to `Eval`. (3) Alternatively, since Go's escape analysis is path-sensitive, a simpler fix is to ensure the `Row` struct is not too large (it's currently 7 fields — should be fine). (4) Add benchmark: `BenchmarkFilter_HeapEscape`. | medium | small | — | EX/intermediate.go |
| REQ000759 | EX/operators | **`SeqScan.snapshot()` eagerly clones all rows even with LIMIT.** At `operators.go:97-106`, `snapshot()` iterates ALL rows in the in-memory table and calls `cloneRow` on each. For a 100K-row table with `LIMIT 10`, this wastes 99,990 clones. The in-memory table is protected by `tablesMu.RLock` during the snapshot. **Fix:** Make the snapshot lazy — clone rows on demand in `Next()` instead of eagerly all at once. Hold the read lock for the duration of the scan (or use a different synchronization strategy). **Implementation:** (1) Remove the eager `snapshot()` call. (2) In `Next()`, take `tablesMu.RLock`, read `tables[s.table][s.pos]`, clone the single row, release lock, increment `pos`. (3) For correctness: the in-memory table may be mutated between `Next()` calls (INSERT/UPDATE/DELETE). Holding a read lock for the entire scan serializes writes. Alternative: take a consistent snapshot by copying the slice header (not the rows) under the lock, then clone rows lazily from the snapshot. (4) Add benchmark: `BenchmarkSeqScan_LazyClone_Limit10`. Expected: for `LIMIT N` queries, reduces allocation from O(table_size) to O(N). | medium | medium | — | EX/operators.go |
| REQ000760 | EX/operators | **`prefixRowCols` allocates new `Cols` slice and concatenates strings per aliased row.** At `operators.go:187-204`, when a FROM alias is used (e.g., `FROM t AS cor0`), every row from `SeqScan.Next` goes through `prefixRowCols` which does `make([]string, len(r.Cols))` and `prefix + c` string concatenation per column. For 10 columns × 100K rows = 100K slice allocations + 1M string concatenations. The prefixed column names are identical for every row — only the alias prefix changes (and it's constant for the scan). **Fix:** Pre-compute the prefixed column names once when the alias is set, and share them across all rows. **Implementation:** (1) In `SeqScan.WithAlias()`, pre-compute `prefixedCols []string` by prefixing each column name. (2) In `Next()`, instead of calling `prefixRowCols`, set `r.Cols = s.prefixedCols` (shared, read-only). (3) The `Row.Cols` slice is now shared — verify no code path mutates it after construction. (4) Add benchmark: `BenchmarkSeqScan_AliasScan`. Expected: eliminates all per-row allocation in aliased scans. | medium | small | — | EX/operators.go |
| REQ000761 | EX/eval | **`evalFunction` calls `strings.ToUpper(e.Name)` on every invocation.** At `eval.go:661`, `evalFunction` does `switch strings.ToUpper(e.Name)` on every function call. For `SELECT LENGTH(x) FROM t` with 100K rows, this allocates 100K uppercase strings. The function name is constant for the entire query — it never changes between rows. **Fix:** Normalize the function name to uppercase once when the `FunctionCall` AST node is created in the parser. Store the normalized name in the AST so `evalFunction` can compare against pre-lowered constants without allocation. **Implementation:** (1) In `PS/ps.go` where `FunctionCall` is constructed, add `Name: strings.ToUpper(rawName)`. (2) In `evalFunction`, change the switch to compare against uppercase constants directly (already the case). (3) Remove the `strings.ToUpper` call. (4) Add benchmark: `BenchmarkEvalFunction_Length`. Expected: eliminates one string allocation per function call per row. | medium | small | — | PS/ps.go, EX/eval.go |
| REQ000762 | EX/eval | **`concat` uses `fmt.Sprintf` even for string-string concatenation.** At `eval.go:1708-1712`, `concat` always uses `fmt.Sprintf("%v%v", a, b)` which allocates via reflection-based formatting. For the common case where both operands are strings (text columns), this is unnecessary overhead. **Fix:** Add a string-string fast path before the `fmt.Sprintf` fallback. **Implementation:** (1) At `eval.go:1709`, insert: `if as, ok := a.(string); ok { if bs, ok := b.(string); ok { return as + bs, nil } }`. (2) Keep `fmt.Sprintf` as fallback for mixed types. (3) Add benchmark: `BenchmarkConcat_StringString`. Expected: eliminates reflection overhead for text concatenation. | low | small | — | EX/eval.go |
| REQ000763 | EX/eval | **`evalHex` allocates intermediate `[]byte` and calls `strings.ToUpper`.** At `eval.go:930-946`, `evalHex` for int64 does `hex.EncodeToString([]byte(strconv.FormatInt(x, 10)))` which allocates a temporary `[]byte` from the decimal string, then allocates the hex output. Then `strings.ToUpper(s)` allocates a third time. **Fix:** Format the int64 directly to uppercase hex without intermediate allocations. **Implementation:** (1) Use `strconv.AppendUint(buf[:0], uint64(x), 16)` then `strings.ToUpper` on the result (or use a custom hex formatter that outputs uppercase directly). (2) Pre-allocate a reusable buffer in the function (or use a sync.Pool). (3) Add benchmark: `BenchmarkHex_Int64`. Expected: reduces from 3 allocations to 1. | low | small | — | EX/eval.go |
| REQ000764 | EX/eval | **`evalUnicode` allocates `[]rune` slice just to get the first rune.** At `eval.go:1361-1366`, `evalUnicode` does `int64([]rune(s)[0])` which converts the entire string to a rune slice (allocating proportional to string length), then takes only the first element. For a 1000-character string, this allocates 4KB just to read 4 bytes. **Fix:** Use `utf8.DecodeRuneInString(s)` which returns the first rune without allocation. **Implementation:** (1) Replace `int64([]rune(s)[0])` with `r, _ := utf8.DecodeRuneInString(s); return int64(r), nil`. (2) Add benchmark: `BenchmarkUnicode_LongString`. Expected: O(1) allocation instead of O(N). | low | small | — | EX/eval.go |
| REQ000765 | EX/aggregate | **`Aggregate.materialize` does O(N×G) group lookup via linear scan.** At `aggregate.go:84-95`, for each incoming row, the code does a linear scan over the `groups` slice to find the matching group bucket. With G groups and N rows, this is O(N×G) comparisons. For a GROUP BY with 1000 distinct values and 100K rows, this is 100M comparisons. **Fix:** Use a `map[string]int` keyed by the serialized group key, mapping to the group bucket index. This makes group lookup O(1) amortized. **Implementation:** (1) Before the row iteration loop, create `groupIndex := make(map[string]int)`. (2) When looking up a group, serialize the key to a string and check `groupIndex`. (3) When creating a new group, add it to `groupIndex`. (4) For the serialization, use `fmt.Sprint` or a custom encoder that avoids allocation (write to a pre-allocated buffer). (5) Add benchmark: `BenchmarkAggregate_1000Groups`. Expected: O(N) instead of O(N×G). | medium | medium | — | EX/aggregate.go |
| REQ000766 | EX/aggregate | **`GROUP_CONCAT` uses string `+=` in a loop — O(K²) total bytes.** At `aggregate.go:446-449`, the GROUP_CONCAT implementation does `out += sep + p` in a loop over K values. Each `+=` allocates a new string and copies all previous content. For K=1000 values of average length 10, this copies ~5MB total instead of ~10KB. **Fix:** Use `strings.Builder` with pre-estimated capacity. **Implementation:** (1) Create `var b strings.Builder` before the loop. (2) Estimate capacity: `len(sep) * (len(parts)-1) + sum(len(p) for p in parts)`. (3) Write each part and separator to the builder. (4) Return `b.String()`. (5) Add benchmark: `BenchmarkGroupConcat_1000Values`. Expected: O(K) total bytes copied instead of O(K²). | low | small | — | EX/aggregate.go |
| REQ000767 | EX/operators | **`indexValueFromKey` allocates on every index iterator key comparison.** At `operators.go:509-518`, `indexValueFromKey` calls `buildIndexKey(tableID, idxName, nil)` to construct the expected prefix for comparison. `buildIndexKey` at line 651 allocates a new `[]byte` via `make([]byte, 0, 32+...)`. Since `nextFromIndex` calls `indexValueFromKey` in a tight loop for range-seek scans, this creates one allocation per index entry visited. **Fix:** Pre-compute the expected prefix once in `openIndexIter` and store it in the `IndexScan` struct. Reuse it in `indexValueFromKey`. **Implementation:** (1) Add `prefixBuf []byte` field to `IndexScan`. (2) In `openIndexIter`, compute `prefixBuf = buildIndexKey(tableID, idxName, nil)` once. (3) In `indexValueFromKey`, use `i.prefixBuf` instead of calling `buildIndexKey` each time. (4) Add benchmark: `BenchmarkIndexScan_RangeSeek`. Expected: eliminates one allocation per index entry. | medium | small | — | EX/operators.go |
| REQ000768 | EX/intermediate | **`Sort.Next` allocates `sortRow` structs and key slices per row.** At `intermediate.go:201-236`, `Sort.Next` creates `[]sortRow` and a `[]any` key slice per row for sort keys. For N rows, this is N+1 allocations (N key slices + 1 sortRow slice). Then it copies back to `s.buf` at line 232, doubling the memory. **Fix:** Sort `s.buf` in-place using a comparator that evaluates sort keys on-the-fly, caching keys in a side array. Or use a single `[]sortRow` as the output buffer and skip the copy-back step. **Implementation:** (1) Pre-allocate `keyBuf [][]any` of size N once. (2) For each row, evaluate sort keys into `keyBuf[i]` (reusing the inner slice if pre-allocated). (3) Use `slices.SortStableFunc` on `s.buf` with a comparator that reads from `keyBuf`. (4) Eliminate the `sortRow` intermediate type entirely. (5) Add benchmark: `BenchmarkSort_1000Rows`. Expected: reduces allocs from O(N) to O(1). | medium | medium | — | EX/intermediate.go |
| REQ000769 | EX/ex | **`Row.colIndex` map is allocated per row — should be shared.** At `ex.go:156-161`, every `Row` that calls `Lookup` gets a `map[string]int` allocated in `buildColIndex()`. For rows flowing through Filter→Project, each row gets its own map. Maps are heap-allocated (8-byte header + bucket arrays) and cause GC pressure. All rows from the same table share the same schema — the colIndex is identical. **Fix:** Build the colIndex once per table schema and share it across all rows from that table. Mark it as read-only after creation. **Implementation:** (1) Add a `sharedColIndex map[string]int` field to `storeSchema` (or a package-level table metadata struct). (2) Build it once when the table is registered. (3) In `SeqScan.Next` / `decodeRow`, assign `row.colIndex = schema.sharedColIndex` instead of building per-row. (4) Verify no code path mutates `colIndex` after construction (it's read-only by design). (5) Add benchmark: `BenchmarkLookup_SharedIndex`. Expected: eliminates one map allocation per row. | high | small | — | EX/ex.go, EX/operators.go |
| REQ000770 | EX/ex | **`Row.Lookup` calls `strings.ToLower` on every invocation.** At `ex.go:150`, `Lookup` does `cur.colIndex[strings.ToLower(name)]` on every call. `strings.ToLower` allocates a new string if any character needs lowercasing. For a typical identifier like "myColumn", this allocates a new 8-byte string every time. **Fix:** Pre-lowercase the lookup key once at the call site. The parser/AST already has the column name — store it in lowercase when the `Ident`/`QualifiedName` AST node is created. Then `Lookup` can do a direct map lookup without `ToLower`. **Implementation:** (1) In `PS/ps.go`, normalize `Ident.Name` and `QualifiedName.Name` to lowercase when constructing AST nodes. (2) In `Lookup`, remove `strings.ToLower` — assume the key is already lowercase. (3) For backward compatibility, add a `LookupFold` variant that does `ToLower` for external callers. (4) Add benchmark: `BenchmarkLookup_AlreadyLower`. Expected: eliminates one string allocation per lookup. | medium | small | — | EX/ex.go, PS/ps.go |
| REQ000771 | EX/planner | **StmtCache exists but is never wired — SQL re-parsed every query.** CPU profile: 12.70% of CPU in `Parser.Parse`. `internal/ENG/LS/engine_cache.go:19` has an LRU `StmtCache` for prepared `*Stmt` objects. But `ST.Stmt.Query()` at `st.go:39` calls `exe.QueryStream(ctx, s.sql, args...)` which re-parses the SQL text every time. For repeated queries (common in OLTP), this is pure waste. **Fix:** Wire the StmtCache so that `Prepare()` caches the parsed AST, and `Query()`/`Exec()` pass the cached AST directly to `Planner.Plan()`. **Implementation:** (1) In `ST.Stmt`, store the parsed `PS.Stmt` AST after the first `Prepare()`. (2) In `Query()`/`Exec()`, pass the cached AST to `Planner.Plan(stmt)` instead of re-parsing. (3) For the `Executor.Query(ctx, sql, args...)` convenience path, check the StmtCache before parsing. (4) Add cache invalidation for DDL statements (CREATE/DROP/ALTER). (5) Add benchmark: `BenchmarkPrepared_vs_ParseEach`. Expected: ~13% CPU reduction for repeated queries. | high | medium | — | EX/planner.go, SYS/ST/st.go, ENG/LS/engine_cache.go |
| REQ000772 | EX/eval | **`numericFloat` called redundantly from both `numericArith` and `compare`.** `numericFloat` at `eval.go:1779-1791` does a type-switch on `any` to extract a float64. It's called from `numericArith` (lines 1719-1720), `compare` (line 1775), `evalRound` (line 959), and `evalSign` (line 1443). In `numericArith`, both `numericFloat(a)` and `numericFloat(b)` are called, then `a.(int64)` is checked again. In `compare`, both `numericFloat(a)` and `numericFloat(b)` are called, losing precision for large int64. **Fix:** (1) For `numericArith`: already addressed by REQ000752 (int64 fast path). (2) For `compare`: already addressed by REQ000753 (int64 fast path). (3) For `evalRound` and `evalSign`: add int64 fast paths that avoid the float64 conversion. (4) Consider inlining `numericFloat` at call sites where the type is already known. **Implementation:** In `evalRound` (line 954): `if v, ok := operand.(int64); ok { return float64(v), true }` before `numericFloat`. In `evalSign` (line 1438): same pattern. | low | small | — | EX/eval.go |
| REQ000773 | EX/intermediate | **`Sort.Next` double-buffers rows (sortRow then copy back).** At `intermediate.go:201-236`, `Sort.Next` materializes all child rows into `s.buf`, then creates a parallel `[]sortRow` with sort keys, sorts it, then copies the sorted rows back to `s.buf`. This doubles memory usage and adds N copy operations. **Fix:** Sort `s.buf` directly using `slices.SortStableFunc` with a comparator that evaluates sort keys on-the-fly. Cache sort keys in a parallel `[][]any` slice (one entry per row) to avoid re-evaluation during comparison. **Implementation:** (1) After materializing `s.buf`, allocate `keyCache := make([][]any, len(s.buf))`. (2) For each row, evaluate sort keys into `keyCache[i]`. (3) Use `slices.SortStableFunc(s.buf, func(a, b Row) int { return compareKeys(keyCache[a.idx], keyCache[b.idx]) })`. (4) Eliminate the `sortRow` type entirely. (5) Add benchmark: `BenchmarkSort_InPlace`. Expected: ~50% memory reduction for sort operations. | medium | medium | — | EX/intermediate.go |
| REQ000774 | EX/ex | **`buildInsertRow` allocates `map[string]any` per INSERT row with column list.** At `source.go:184-200`, when `cols` is non-empty, `buildInsertRow` creates `byName := make(map[string]any, len(cols))` to map column names to values. For bulk INSERT of 1000 rows with a column list, this allocates 1000 maps. The column name→index mapping is constant across all rows. **Fix:** Pre-compute the column-name-to-schema-index mapping once (outside the row loop) and use direct index assignment instead of a map per row. **Implementation:** (1) Before the row loop, build `colIdx := make(map[string]int, len(cols))` mapping each column name to its position in the schema. (2) For each row, use `colIdx` to assign values directly to the output `Row.Data` slice. (3) This eliminates N map allocations for N-row INSERT. (4) Add benchmark: `BenchmarkInsert_100Rows_WithCols`. Expected: eliminates one map allocation per INSERT row. | medium | small | — | EX/source.go |
| REQ000775 | EX/operators | **`SeqScan.nextFromStore` copies `schema.cols` per row via `decodeRow`.** At `store.go:594`, `decodeRow` does `Cols: append([]string(nil), schema.cols...)` for every row. All rows from the same table share the same column names — only the data differs. For 100K rows with 10 columns, this is 100K `[]string` allocations (each 10 elements). **Fix:** Share the `Cols` slice across all decoded rows from the same schema. The slice is read-only after construction. **Implementation:** (1) In `decodeRow`, use `Cols: schema.cols` directly (shared reference) instead of `append([]string(nil), schema.cols...)`. (2) Verify no code path mutates `Row.Cols` after construction — it must be read-only. (3) If mutation is found, add a `ColsShared bool` flag and copy-on-write. (4) Add benchmark: `BenchmarkDecodeRow_SharedCols`. Expected: eliminates one slice allocation per row for store-backed scans. | high | small | — | EX/store.go, EX/operators.go |
| REQ000776 | EX/eval | **Replace `any` (interface{}) with tagged-union `Value` type to eliminate boxing overhead.** CPU profile: `convT64` (Go's runtime function for boxing int64 into interface{}) accounts for 10.32% of CPU. `mallocgc` from all boxing operations totals 27.78%. Every `int64` value returned from `Eval`, stored in `Row.Data`, passed through `numericArith`, `compare`, `equalValue`, or returned from `SeqScan.nextFromStore` gets boxed into a 16-byte interface header (type pointer + value pointer). For a 100K-row scan with 5 columns, this is 500K boxing operations. The `any` type pervades the entire executor: `Row.Data []any`, `Eval() (any, error)`, `numericArith(a, b any, ...)`, `compare(a, b any)`, `equalValue(a, b any)`, all aggregate accumulators, all function dispatch. **Root cause:** Go's `any` (empty interface) requires a heap allocation for non-pointer types (int64, float64, bool). The runtime's `convT64` allocates an 8-byte box on the heap, then creates a 16-byte interface header pointing to it. This happens on every value transition: decode→Row.Data, Row.Data→Eval, Eval→arithmetic, arithmetic→comparison, comparison→result. **Fix:** Define a `Value` tagged-union struct that stores all SQL types inline without heap allocation. **Implementation:** (1) Define the type in `ex.go`: `type Value struct { Kind uint8; I64 int64; F64 float64; S string; B []byte }` with kind constants `KindNull=0, KindInt=1, KindFloat=2, KindText=3, KindBlob=4, KindBool=5`. (2) Change `Row.Data` from `[]any` to `[]Value`. (3) Change `Eval` signature from `(any, error)` to `(Value, error)`. (4) Update all 22 expression eval functions (`evalUnary`, `evalBinary`, `evalBetween`, `evalIn`, `evalCase`, `evalAggregate`, `evalFunction`, etc.) to use `Value` instead of `any`. (5) Update `numericArith(a, b Value, op rune)` to switch on `a.Kind` directly — no interface dispatch. (6) Update `compare(a, b Value) int` similarly. (7) Update `equalValue(a, b Value) bool`. (8) Update `decodeRow` to produce `Value` instead of boxing to `any`. (9) Update `Eval` return paths: `return Value{Kind: KindInt, I64: v}, nil` instead of `return v, nil`. (10) Update `Row.Lookup` to return `(Value, bool)`. (11) Update all callers in `intermediate.go`, `aggregate.go`, `window.go`, `source.go`, `writers.go`, `join.go`. (12) Update the `database/sql` driver (`driver/rows.go`) to scan `Value` into `database/sql` types. (13) Add conversion helpers: `func (v Value) Int64() (int64, bool)`, `func (v Value) Float64() (float64, bool)`, `func (v Value) String() string`, `func (v Value) IsNull() bool`. (14) Add benchmarks: `BenchmarkEval_TaggedUnion_IntArith`, `BenchmarkEval_TaggedUnion_Compare`, `BenchmarkScan_TaggedUnion_100KRows`. **Expected:** ~20%+ total CPU reduction. Eliminates all `convT64` boxing (10.32% CPU) and reduces `mallocgc` pressure (27.78% CPU) by ~50-70%. The `Value` struct is 32 bytes (8+8+16+24 with string header and slice header) — fits in a single cache line on most architectures. **Risk:** Pervasive refactor touching ~30 files and ~2000+ LOC. Must be done atomically — partial conversion creates type mismatch errors everywhere. Recommend doing this as a dedicated iteration with comprehensive test coverage. **Dependencies:** Phase 1-4 optimizations should be committed first to avoid merge conflicts. | high | large | REQ000752, REQ000753, REQ000754, REQ000755, REQ000756 | EX/eval.go, EX/ex.go, EX/operators.go, EX/intermediate.go, EX/aggregate.go, EX/source.go, EX/writers.go, EX/join.go, EX/window.go, EX/store.go, EX/aggregate.go, EX/subq.go, EX/constraints.go, driver/rows.go, driver/conn.go |
| REQ000777 | EX/explain | **EXPLAIN missing 20+ operator types — displays "Unknown" for HashJoin, HashAggregate, Window, Compound, Values, and all DDL operators.** `buildPlanNodeTree` in `plan_node.go:29-143` and `describeOp` in `explain.go:167-227` only handle 13 operator types (SeqScan, IndexScan, Filter, Project, Sort, Limit, Offset, Distinct, Aggregate, NestedLoopJoin, Insert, Update, Delete, CreateTable, DropTable). All other operators render as "Unknown" in EXPLAIN output. Missing operators: `HashJoin` (`hashjoin.go`), `HashAggregate` (`hashagg.go`), `WindowOperator` (`window.go`), `CompoundOp` for UNION/INTERSECT/EXCEPT (`compound.go`), `Values`/`ValuesRows` (`values.go`), `CreateIndex`/`DropIndex` (`writers.go`), `CreateView`/`DropView` (`writers.go`), `CreateTrigger`/`DropTrigger` (`writers.go`), `AlterTable` (`alter_table.go`), `Pragma` (`writers.go`), `Analyze`/`Vacuum` (`analyze.go`), `IntegrityCheck` (`integrity.go`), `Truncate`/`Reindex` (`writers.go`), `CreateMatViewOperator`/`RefreshMatViewOperator`/`DropMatViewOperator` (`matview.go`), `ExplainStmtOp` (`explain.go`), `ParallelSort` (`sort_parallel.go`). **Fix:** Add `case` branches for all missing operators in `buildPlanNodeTree`, `describeOp`, and `operatorType`. For each operator, extract meaningful metadata (table name, index name, join type, aggregate functions, window spec, compound keyword). **Implementation:** (1) In `buildPlanNodeTree` (`plan_node.go`), add cases for each missing operator. (2) In `describeOp` (`explain.go`), add cases with descriptive strings. (3) In `operatorType` (`plan_node.go`), add type name mappings. (4) In `explainOperator` (`explain.go`), add child traversal for multi-child operators. (5) For `HashJoin`: show join type + keys. (6) For `HashAggregate`: show group-by columns + aggregate functions. (7) For `WindowOperator`: show function name + OVER clause. (8) For `CompoundOp`: show UNION/INTERSECT/EXCEPT keyword. (9) For `Values`: show row count. (10) Add test: `TestExplain_AllOperatorTypes`. | medium | medium | — | EX/explain.go, EX/plan_node.go |
| REQ000778 | EX/explain | **EXPLAIN shows Go struct dumps instead of readable SQL for predicates, columns, and expressions.** When `Detail` is populated from `fmt.Sprintf("%s", v.predicate)` at `plan_node.go:59`, it produces Go struct representations like `BinaryExpr{Op:61, Left:QualifiedName{Table:"t", Name:"v"}, Right:NumberLiteral{Val:10}}` instead of readable SQL like `t.v > 10`. Similarly, Project shows "SELECT" without the column list, Sort shows "ORDER BY" without sort keys, Aggregate shows "GROUP BY" without group columns or aggregate functions. **Root cause:** The AST nodes (`PS.BinaryExpr`, `PS.QualifiedName`, etc.) do not implement `String()` or `fmt.Stringer`. When passed to `fmt.Sprintf("%s", ...)`, Go uses the default `%v` representation which dumps struct fields. **Fix:** Use the existing `PS.Format(expr)` function (in `RE/rewrite.go`) to render AST expressions as readable SQL strings. **Implementation:** (1) In `buildPlanNodeTree` for `Filter`: change `node.Detail = fmt.Sprintf("WHERE %s", v.predicate)` to `node.Detail = "WHERE " + PS.Format(v.predicate)`. (2) For `Project`: `node.Detail = "SELECT " + PS.FormatExprList(v.cols)`. (3) For `Sort`: `node.Detail = "ORDER BY " + PS.FormatOrderBy(v.keys)`. (4) For `Aggregate`: `node.Detail = "GROUP BY " + PS.FormatExprList(v.groupCols) + " AGG " + PS.FormatExprList(v.aggs)`. (5) For `Limit`: `node.Detail = fmt.Sprintf("LIMIT %d", v.n)` if `v.n` is available. (6) For `NestedLoopJoin`: `node.Detail = "JOIN " + v.rightTbl + " ON " + PS.Format(v.on)` if `v.on` is available. (7) Verify `PS.Format` handles all expression types correctly. (8) Add test: `TestExplain_ReadableDetail`. | medium | small | — | EX/plan_node.go, EX/explain.go |
| REQ000779 | EX/explain | **EXPLAIN does not show index seek condition for IndexScan.** `IndexScan` in `buildPlanNodeTree` (`plan_node.go:50-54`) only shows `idx=name` but does not show what the seek condition is (equality value, range bounds). The `IndexScan` struct has `indexSeek` (for equality), `indexLower`/`indexUpper` (for range), and `indexRangeEnd` fields that contain the actual seek values. Without this information, the user cannot tell if the index is being used for a point lookup vs a full scan vs a range scan. **Fix:** Extract the seek condition from `IndexScan` fields and display it in `Detail`. **Implementation:** (1) In `buildPlanNodeTree` for `IndexScan`: check `v.indexSeek` for equality seek → show `"idx=name SEEK v=<value>"`. (2) Check `v.indexLower`/`v.indexUpper` for range seek → show `"idx=name RANGE v>lo AND v<up"`. (3) If neither is set (prefix scan), show `"idx=name SCAN"`. (4) For B-tree index path (`v.btree != nil`), show `"idx=name BTREE"`. (5) Add test: `TestExplain_IndexSeekCondition`. | medium | small | — | EX/plan_node.go |
| REQ000780 | EX/explain | **EXPLAIN does not show estimated row count — `PlanNode.Rows` is always 0.** `estimateRowCount` in `planner.go:1686-1691` returns 0 unconditionally. The `PlanNode` struct has a `Rows int64` field but it is never populated with a meaningful value. `formatPlanTree` does not include `Rows` in the output either. Without row count estimates, users cannot assess query selectivity or identify bottlenecks. **Fix:** Wire `estimateRowCount` to return actual estimates based on table statistics, and display the estimated rows in EXPLAIN output. **Implementation:** (1) In `estimateRowCount` (`planner.go:1686`): for in-memory tables, return `len(tables[table])`. For store-backed tables, return an estimate from the catalog stats (row count stored during ANALYZE or INSERT tracking). (2) In `buildPlanNodeTree` for `SeqScan`: `node.Rows = int64(planner.estimateRowCount(v.table, nil))`. (3) For `Filter`: `node.Rows = int64(float64(childRows) * selectivity)`. (4) For `Join`: `node.Rows = leftRows * rightRows * joinSelectivity`. (5) In `formatPlanTree`: add `Rows` to the detail string, e.g., `"Scan t (rows=~100)"`. (6) Add `EXPLAIN QUERY PLAN` test verifying row estimates appear. | medium | medium | — | EX/planner.go, EX/plan_node.go, EX/explain.go |
| REQ000781 | EX/explain | **EXPLAIN does not show which storage engine path is used (in-memory vs store-backed vs B-tree).** `SeqScan` can read from the in-memory `tables` map (`s.store == nil`) or from the engine store (`s.store != nil`). `IndexScan` has three paths: B-tree cursor (`s.btree != nil`), store iterator (`s.store != nil` with `s.indexMode`), or in-memory scan. The EXPLAIN output does not distinguish these, making it impossible to tell if a scan is hitting the fast in-memory path or the slower disk-backed path. **Fix:** Add a storage path indicator to the EXPLAIN detail for scan operators. **Implementation:** (1) In `buildPlanNodeTree` for `SeqScan`: if `v.store != nil`, append `" [store]"` to detail; else append `" [memory]"`. (2) For `IndexScan`: if `v.btree != nil`, append `" [btree]"`; else if `v.indexMode`, append `" [index-seek]"`; else if `v.store != nil`, append `" [store-scan]"`; else append `" [memory]"`. (3) Add test: `TestExplain_StoragePath`. | low | small | — | EX/plan_node.go |
| REQ000782 | EX/explain | **EXPLAIN does not show ADQC (adaptive query compilation) state.** `AdaptiveOp` wraps operators and tracks whether they are `Interpreted`, `Compiling`, or `Compiled` via `AdqcState`. The EXPLAIN output unwraps `AdaptiveOp` (showing the inner operator) but does not display the compilation state. Users cannot tell if a query is running in interpreted mode or has been JIT-compiled. **Fix:** Show the ADQC state in EXPLAIN output. **Implementation:** (1) In `buildPlanNodeTree`: when unwrapping `AdaptiveOp`, add the state to the inner node's detail: append `" [interpreted]"`, `" [compiling]"`, or `" [compiled]"`. (2) In `describeOp`: same treatment. (3) For `EXPLAIN QUERY PLAN`: show the state in the detail string. (4) Add test: `TestExplain_ADQCState`. | low | small | — | EX/plan_node.go, EX/explain.go |
| REQ000783 | EX/explain | **EXPLAIN ANALYZE mode not implemented — no way to see actual runtime statistics.** SQLite 3.31+ supports `EXPLAIN ANALYZE` which executes the query and shows actual runtime stats per operator (actual row count, actual time). Razordata only has `EXPLAIN` (show estimated plan) and `EXPLAIN QUERY PLAN` (show high-level plan). There is no way to see how many rows each operator actually processed, how long each step took, or where the bottleneck is. **Fix:** Add `EXPLAIN ANALYZE` mode that executes the plan, collects runtime stats per operator, and renders them alongside the plan tree. **Implementation:** (1) Add `ExplainAnalyze` to `PS.ExplainMode` enum in `ast.go`. (2) In `PS/explain.go`, parse `EXPLAIN ANALYZE` keyword combination. (3) Add `AnalyzeStats` struct: `{RowsReturned int64, TimeNS int64, Allocs int64}`. (4) Add `analyzeOp` wrapper operator that wraps each operator in the tree, collects stats on each `Next()` call (increment row counter, measure time via `time.Now()`, track allocs via `runtime.MemStats`). (5) In `planExplain`: when mode is `ExplainAnalyze`, wrap the plan tree with `analyzeOp` wrappers, execute the plan to completion, collect stats. (6) In `formatPlanTree`: add actual stats to detail: `"Scan t (rows=~100, actual=42, time=1.2ms)"`. (7) Add parser support for `EXPLAIN ANALYZE`. (8) Add test: `TestExplainAnalyze_BasicQuery`. (9) Add benchmark: `BenchmarkExplainAnalyze_Overhead` to verify stats collection adds <5% overhead. | high | large | — | EX/explain.go, EX/plan_node.go, PS/ps.go, PS/explain.go, PS/ast.go |
| REQ000784 | EX/explain | **`explainOperator` function is dead code — never called from EXPLAIN execution path.** `explainOperator` in `explain.go:97-165` recursively walks the operator tree and produces indented text output. However, `ExplainStmtOp.Next` at `explain.go:31-37` calls `formatPlanTree` or `formatExplainNormal` (which operate on `PlanNode` trees, not raw `Operator` trees). The `explainOperator` function is never invoked. Similarly, `formatExplainNormal` at `explain.go:59-95` is redundant with `formatPlanTree` at `plan_node.go:189-230` — both produce the same `(id, parent, notused, detail)` schema. **Fix:** Remove dead code and unify rendering into a single path. **Implementation:** (1) Delete `explainOperator` function (`explain.go:97-165`). (2) Delete `formatExplainNormal` function (`explain.go:59-95`). (3) In `ExplainStmtOp.Next`, always use `formatPlanTree` regardless of mode. For `ExplainNormal` mode, include more detail (expressions, costs); for `ExplainQueryPlan` mode, use simplified output (SQLite-style SCAN/SEARCH/JOIN). (4) Add a `formatMode` parameter to `formatPlanTree` to control verbosity. (5) Add test: `TestExplain_UnifiedRendering`. | low | small | — | EX/explain.go |
| REQ000785 | EX/explain | **EXPLAIN QUERY PLAN ID numbering may not match SQLite's flat tree numbering.** `formatPlanTree` at `plan_node.go:196-226` assigns IDs using a recursive walk with `currentID` tracking. The `countNodes` helper at line 233-241 is used to skip IDs for subtrees. However, this approach may produce ID gaps or incorrect parent references for complex plan trees (e.g., 3+ table joins). SQLite's EXPLAIN QUERY PLAN uses a flat sequential numbering where each node gets the next available ID regardless of tree depth. **Fix:** Use a simple counter that increments for each node visited in depth-first order. **Implementation:** (1) Replace the recursive `currentID` tracking with a package-level or closure-level counter: `nextID := 1; walk(node, &nextID, 0)`. (2) In `walk`, assign `id = *nextID; *nextID++`. (3) Remove `countNodes` helper (no longer needed). (4) Add test: `TestExplain_QueryPlanIDNumbering` with a 3-table join to verify sequential IDs and correct parent references. | low | small | — | EX/plan_node.go |
