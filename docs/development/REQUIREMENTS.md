## TBD
| ID | Subsystem | Requirement | Priority | Effort | Deps | Touches |
| --- | --- | --- | --- | --- | --- | --- |
| REQ001109 | EX/plan | **No GEQO for large joins — single-start N3 produces suboptimal plans for >8 tables.** PostgreSQL uses GEQO (genetic algorithm) for joins with >12 tables. Razor switches to single-start N3 for >8 tables. For very large joins, N3 may produce suboptimal plans. **Fix:** (1) Implement GEQO with population-based search. (2) Integrate with cost model. (3) Add planning time management. (4) Add `TestGEQO_LargeJoin` verifying GEQO produces better plans than N3 for 15+ table joins. | low | large | REQ001103, REQ001104 | EX/planner.go |
| REQ001113 | EX/join | **Multi-table cross-join (implicit comma-join) with equality predicates produces incorrect cardinality — 25/3857 select4 records fail after e00d132 + REQ001155.** select4.test: 351/3857 fail at start of e00d132. After e00d132 fixed groupBushyJoins (REQ001156) and bare-column resolution, drops to 25/3857. After REQ001155 fixed ON-only join elimination, 25/3857 (no further drop; remaining failures are not ON-only). Remaining 25 are all 3-table+ comma-joins with constant-equality predicates on all sides. Symptom: got [NULL | NULL] in row 0 — join output is zero rows or zero-column output. Example: L39860 SELECT x8, e9+131 FROM t8, t9 WHERE e9=383 AND 561=e8 expects 1 row, observed 0. **Progress:** 2-table sub-items verified fixed by req001113_2table_test.go (3 tests pass). **Remaining:** 3-table+ multi-table cross-joins produce zero surviving rows when they should produce ≥1. Suspected in HashJoin.buildAndProbe, NestedLoopJoin.Next, or Project output column offsets after multi-table joins. **Fix:** (1) TestMultiTableJoin_3Table_AllPermutations — 5 representative 3-table select4 cases × 6 table-order permutations, all permutations must match. (2) TestHashJoin_CrossPredicateEnforcement and TestNestedLoopJoin_CrossPredicateEnforcement. (3) TestProject_ColumnOffset_MultiTableJoin verifying projection column offset survives 2-way and 3-way joins. (4) Run all 25 currently-failing select4 queries through fixed executor, expect 0 failures. | high | large | REQ001092, REQ001093, REQ001111, REQ001156 | SQB/OP/join.go, SQB/OP/hashjoin.go, SQB/OP/intermediate.go |
| REQ001122 | SQB/UT | **Partial: pipeline.go deleted (dead code, 0 callers). sort_parallel.go moved to SQB/OP instead of UT (OP dependency).** UT cluster exists with 23 files. Pure UT files (simd_dispatch, coerce, decimal, json, parallel, batch, txn_debug, analyze, pragma, fk, integrity, datetime) moved in PoC. **Progress 2026-07-01:** (1) UT/pipeline.go deleted — dead code (0 callers, 178 lines). (2) sort_parallel.go moved from EX to SQB/OP (blocked from UT due to OP.VectorizedSeqScan dependency). (3) BatchValueAt from EV/eval_vec.go converted to thin wrapper over UT.BatchValueAt (delegation, no cycle). EX/explain.go references PlanNode from plan_node.go (stays in EX). | high | small | REQ001116, REQ001118 | SQB/EX/explain.go |
| REQ001123 | SQB/EX | **Partial: writers.go (2548 lines) moved to WT. EX now has 5 production files (ex.go, planner.go, plan_node.go, explain.go, source.go).** DT.DBAttachManager interface created; Executor implements it. WT writers use accessor methods (SetExecCtx, SetSelectPlan, SetConflictAction, SetDefaultValues, Table, Iter) for cross-package access. EX re-exports WT.ErrMultiDatabaseNotSupported for SYS backward compat. **Remaining:** planner.go → AD, plan_node.go → AD, explain.go → AD/UT. **Progress 2026-07-01:** Writers extracted to WT (2548 LOC). EX shrank from 6→5 production files. | high | large | REQ001117, REQ001118, REQ001119, REQ001121 | SQB/EX/ex.go, SQB/EX/planner.go, SQB/EX/plan_node.go, SQB/EX/explain.go, SQB/WT/writers.go |
| REQ001159 | build | **Add dependency check (`depcheck`) — codify the current package import graph as the baseline; fail CI on new violations.** No automated enforcement of the subsystem/cluster layering rule today. The dependency report (`docs/compose/reports/dependency-structure-principles.md`, 2026-06) recommends a `tests/depcheck/` Go program that reads package import paths and checks against an allowlist. The allowlist must be derived from the **current** package layout (no `internal/types/` — see REQ001160). **Fix:** (1) Create `tests/depcheck/main.go` (or a `_test.go` so it runs under `go test`) that walks `go list -deps ./...` and asserts each package only imports from layers at-or-below its own. (2) Define the allowlist per ARCH.md §Dependency Order: LOG→FIL→MEM→WAL→ENG→TXN→SQF→SQB→SYS, plus intra-SQB cluster rules from `SQB.md`. (3) Land as a **warning** (not failure) during SQB/WT and SQB/UT cluster fills. (4) Promote to failure after REQ001123 (SQB finalization) ships. (5) Document the rules in `docs/development/DEPENDENCIES.md`. (6) Add depcheck to the CI script. | high | medium | none | tests/depcheck/, CI, docs/development/DEPENDENCIES.md |
| REQ001162 | EX/join | **3-table+ implicit comma-join returns zero rows for SLT select4 cases — HashJoin/NLJ output column shuffling.** Discovered while shipping REQ001113: even after e00d132 + REQ001155 closed, select4.test still has 25/3857 failing records, all 3-table+ comma-joins. Symptom: SLT rowsort output is got [NULL | NULL] in row 0 (e.g., L39860 SELECT x8, e9+131 FROM t8, t9 WHERE e9=383 AND 561=e8 → 0 rows, want 1). Both tables have matching per-table-filtered rows but the join produces no output. Suspected: (a) HashJoin.buildAndProbe drops the cross-table equality predicate when both sides are already per-table-filtered; (b) NestedLoopJoin.Next iterates row pairs but never evaluates the join predicate; (c) Project output column offsets are misaligned after multi-table joins, so x8 and e9+131 map to wrong scan columns. **Fix:** (1) Reproduce minimal 3-table case with hand-seeded data, isolate which operator produces zero rows. (2) Add TestHashJoin_CrossPredicateEnforcement and TestNestedLoopJoin_CrossPredicateEnforcement with both sides single-table-filtered + cross-table predicate. (3) Add TestProject_ColumnOffset_MultiTableJoin verifying the projection column offset survives 2-way and 3-way joins. (4) Run all 25 currently-failing select4 queries through the fixed executor and verify zero failures. **Touches:** SQB/OP/hashjoin.go, SQB/OP/join.go, SQB/OP/intermediate.go. See REQ001113 Gap Analysis Bug 1. | high | large | REQ001113, REQ001155 | SQB/OP/hashjoin.go, SQB/OP/join.go, SQB/OP/intermediate.go |
| REQ001169 | ENG/LS | **Ribbon filter replacing FNV-1a 2-hash Bloom** — Current Bloom uses ~10 bits/key with FNV-1a double-hash (`fnv.go:13`, `sst_writer.go:79-92`). A new SST version writes Ribbon filter instead. Ribbon saves ~30% memory at same FPR or halves FPR at same memory. The current footer format (`sst_writer.go:268-271`) stores bloom + prefixBloom offsets; after migration, store a Ribbon filter and drop the double-bloom layout. **Fix:** (1) Add a Ribbon filter builder in `sst_writer.go` alongside the existing Bloom, gated by a SST format version flag. (2) Add `sst_reader.go` Ribbon query path (`mayContainRibbon`) using the same `fnv1aHash` seeding. (3) Bump SST version in `meta.razor`; old Bloom-only SSTs still read via fallback. (4) Add `TestRibbonFilter_FPR` verifying FPR <= 1% at 10 bits/key and `TestRibbonFilter_BackwardCompat` reading old Bloom SSTs. | medium | medium | none | sst_writer.go, sst_reader.go, fnv.go, meta.razor |
| REQ001170 | ENG/LS | **Interpolation search on SST index** — Current index search is binary search (`sst_reader.go:386-412`): `mid := (lo + hi) / 2`. For uniformly distributed keys, interpolation search probes `mid = lo + (hi-lo)*(probe-first)/(last-first)`. RocksDB reports ~9% throughput gain with auto-detection. **Fix:** (1) Compute coefficient of variation of index key lengths once when opening the SST; if below a threshold (uniform), set a `useInterpolation` flag. (2) Add `searchIndexInterpolation` path probing via interpolation formula. (3) Fall back to binary search if the probe is outside current bounds (non-uniform segment). (4) Add `TestInterpolationSearch_UniformKeys` and `TestInterpolationSearch_FallbackOnNonUniform` verifying correctness against binary search result with different key distributions. | low | small | none | sst_reader.go |
| REQ001171 | ENG/LS | **Zone-aware compaction debt tracking** — Current compaction scheduling uses static per-level size budgets (`compaction.go:24-28`: `levelBudget{l0: 4MB, l1: 32MB, l2: 320MB}`). `MaybeCompact` (`compaction.go:552-571`) iterates levels in order and triggers on the first level exceeding budget — no prioritization of falling-behind levels. **Fix:** (1) Add a `debt` field per level: `max(0, actualSize - budget)` updated after each compaction job. (2) Modify `MaybeCompact` to weigh debt × level multiplier (deeper levels more expensive to ignore). (3) Add `compactionUrgency` score: `debt / budget` ratio, schedule the highest-urgency level first. (4) Add `TestCompactionDebt_PrioritizesLaggingLevel` verifying a level at 200% budget is compacted before a level at 110% irrespective of ordinal order. | medium | medium | REQ001048 | compaction.go, compaction_style.go, rate_limiter.go |
| REQ001172 | all | **VFS abstraction layer** — Engine currently calls `os.Open`, `os.File`, `os.Create` directly at 10+ call sites (`sst_reader.go:105,444`, `engine.go:250`, `compaction.go:83,347,353`, etc.). No abstraction for testing or alternate backends. Pebble's `vfs.VFS` interface is the proven pattern. **Fix:** (1) Define `type FS interface { Open(name string) (File, error); Create(name string) (File, error); ... }` and `type File interface { ReadAt(p []byte, off int64) (int, error); WriteAt(p []byte, off int64) (int, error); Close() error; Sync() error; Stat() (FileInfo, error) }`. (2) Implement `osFS` wrapping standard library calls. (3) Thread `FS` through `Engine` constructor and all storage components. (4) Replace `os.Open`/`os.File` calls with `fs.Open`/`fs.Create` across all packages. (5) Add an `inMemFS` implementation for tests. (6) Add `TestVFS_InMemPutGet`, `TestVFS_CrashRecovery` using `inMemFS` with injected failures. (7) Verify `go build ./...` and `go test ./... -race -count=1` pass. | high | large | none | sst_reader.go, engine.go, compaction.go, pervasive |
| REQ001173 | ENG/LS | **L0 cache sharding** — `l0Cache` (`l0_cache.go:8-14`) uses a single `sync.Mutex` protecting a `container/list` LRU with `capacity` limit. While `PageCache` is already sharded by `GOMAXPROCS` (`page_cache.go:37-40`), the L0 cache becomes a contention point under concurrent read-heavy workloads. **Fix:** (1) Refactor `l0Cache` to use `N` shards (N = `runtime.GOMAXPROCS(0)`), each with its own mutex and LRU list. (2) Shard by `fileID % N`. (3) Keep the same total capacity, split evenly across shards. (4) Add `TestL0Cache_ShardedConcurrent` verifying `-race` clean under 16 goroutines issuing concurrent `Get`+`Put` operations. (5) Run `go test -bench=BenchmarkL0Cache -benchtime=2x ./internal/ENG/LS/` comparing p50/p99 latency before and after. | low | small | none | l0_cache.go, page_cache.go |
| REQ001174 | ENG/LS | **Range tombstone conversion during scan** — REQ001008 ✅ handles writing range tombstones (`sst_writer.go` `AddRangeTombstone`) and checking them during point reads (`sst_reader.go` `isInRangeTombstone`). However, point tombstones (single-key `Delete`) that accumulate between range tombstones still cause O(n) per-key skipping during scans. RocksDB 11.3 (2026) converts contiguous point tombstones into a single range tombstone during scan, achieving 99× (forward) to 368× (reverse) speedup on tombstone-heavy workloads. **Fix:** (1) During scan in `sst_reader.go`, track consecutive point-tombstone keys. (2) When a threshold (e.g., 4 consecutive) is reached, synthesize a range tombstone `[firstTombstoneKey, lastTombstoneKey]` and skip past it in one operation instead of per-key skipping. (3) Handle the reverse-scan case symmetrically. (4) Add `TestRangeTombstoneConversion_ForwardScan` and `TestRangeTombstoneConversion_ReverseScan` verifying contiguous point tombstone blocks are skipped in O(1) instead of O(n). | medium | medium | REQ001008 | sst_reader.go |
| REQ001175 | ENG/LS | **Compaction rate limiter enhancement** — Current `RateLimiter` (`rate_limiter.go`) is an optional global token bucket with fixed rate. No per-level awareness. When multiple levels simultaneously exceed budget, compaction may stall on deeper levels while L0 keeps growing. **Fix:** (1) Add per-level token buckets alongside the global one. (2) Each level gets a write budget proportional to its debt (from REQ001171). (3) When debt exceeds 150% of budget for a level, reduce its compaction concurrency and throttle incoming writes. (4) Expose per-level stats via `Engine.Stats()`. (5) Add `TestRateLimiter_PerLevelThrottle` verifying that a high-debt level gets write-throttled before a low-debt level. (6) Add `TestRateLimiter_NoStallUnderNormalLoad` verifying throughput is unaffected when no level is in debt. | low | medium | REQ001171 | rate_limiter.go, compaction.go |
| REQ001176 | SYS/AP | **ERROR Phase 1 — Foundation: add Code, SQLSTATE, Module, Layer, Fields, Cause to AP.Error; create error_kind.go, error_code.go, error_classify.go.** Current error system is fragmented: 27 package-level sentinels across 12 clusters, only 1.5 structural types (AP.Error, PS.SyntaxError), naming collisions (ErrNoRows in SQF/PL and SYS/AP; ErrDivByZero in SQB/EV and SQB/UT), no stable error codes, retry/fatal classification is per-Kind only. **Frozen design** (17 decisions in `docs/compose/reports/error-system-refactor-design.md`): dual Code+SQLSTATE on every Error, 5 new Kind constants (KindInternal, KindNotImplemented, KindConflict, KindResourceExhausted, KindParse), 8 entity values for KindNotFound, Classification struct, optional Emit hook, backward-compat sentinel shims with `// Deprecated:` comments. **Fix:** (1) New `error_kind.go` — all Kind constants + Kind.String() + 8 Entity constants. (2) New `error_code.go` — Code/SQLSTATE types, 19-row mapping table (CodeOf, SQLStateOf). (3) New `error_classify.go` — Classification struct, Classify(err), retry/fatal lookup tables. (4) Modify `error.go` — add Code, SQLSTATE, Module, Layer, Fields, Cause fields; replace Error() with `{Module}/{Layer} {Code} [{SQLSTATE}]: {Message}`; add Format(s, verb) with %+v chain expansion; add SQLState() string for driver.Error compatibility; add optional Emit func(*Error) + SetEmit; retain all existing methods (IsKind, Unwrap, etc.). (5) Modify `ap.go` — 14 sentinels auto-fill Code/SQLSTATE via New(...); add `// Deprecated:` comment above each. (6) Extend error_test.go: snapshot test for Error() string per Kind, %+v chain expansion, Classify per Kind, CodeOf/SQLStateOf consistency, driver.Error compatibility, legacy sentinel shim test, Emit hook test. (7) All existing 45 errors.Is test sites unchanged, all existing tests pass with zero modification. | high | medium | none | SYS/AP/error_kind.go, SYS/AP/error_code.go, SYS/AP/error_classify.go, SYS/AP/error.go, SYS/AP/ap.go, SYS/AP/error_test.go |
| REQ001177 | SYS/AP | **ERROR Phase 2 — Wrap boundary: rename conflicting sentinels, add WrapAt/FromSentinel, install wrap at cluster exit points.** Without structural wrapping, errors crossing subsystem boundaries lose context — a "key not found" from ENG/LS reaches the caller wrapped only in the raw sentinel, not as an AP.Error with Module/Layer/Code. Two naming collisions must be resolved first. **Fix:** (1) Rename SQB/EV.ErrDivByZero → EV.ErrEvalDivByZero with deprecated alias; rename SQB/UT.ErrDivByZero → UT.ErrDecimalDivByZero with deprecated alias. (2) New `error_wrap.go` — WrapAt(kind, module, layer, err) *Error auto-fills module/layer from caller; FromSentinel(err) *Error recognizes known sentinel pointers and wraps with matching Kind. (3) Install wrap layer at cluster exit points: SQB/EX/Exec, SQB/EX/Query, SYS/SE/Exec, SYS/SE/Query, SYS/ST/Query, SYS/DS/ queries. (4) All errors crossing subsystem boundary must come out as `*AP.Error`. (5) Add `TestWrapAt_ModuleLayerAutoFill`, `TestFromSentinel_RecognizesAllSentinels`, `TestWrapBoundary_AllExitPoints`. | high | medium | REQ001176 | SYS/AP/error_wrap.go, SQB/EV/eval.go, SQB/UT/decimal.go, SQB/EX/ex.go, SYS/SE/se.go, SYS/ST/st.go, SYS/DS/ |
| REQ001178 | SYS/AP | **ERROR Phase 3 — Structural error types: add Op field, promote ConstraintError/ReplayError/DeadlockError, align SyntaxError.** Current error types are flat AP.Error structs with no operation context. Callers cannot distinguish "SELECT failed because of a constraint" from "INSERT failed because of a constraint" without parsing the message string. **Fix:** (1) Add Op field to AP.Error (string, populated from Op constants: OpSelect, OpInsert, OpUpdate, OpDelete, OpCreate, OpDrop, OpAlter, OpBegin, OpCommit, OpRollback, OpSavepoint, OpReplay, OpFlush, OpCompact, OpCheckpoint, OpBackup, OpRestore). (2) New `SQB/EX/constraint_error.go` — ConstraintError{Table, Constraint, Columns, Values, Op} with ToAPError() method. (3) New `WAL/RP/replay_error.go` — ReplayError{LSN, Cause, Op} with ToAPError(). (4) New `TXN/VL/deadlock_error.go` — DeadlockError{TxID, Holders, Op} with ToAPError(). (5) Modify SQF/PS/error.go — align SyntaxError{Line, Col, Msg} field names to match design (add ToAPError()). (6) Add `TestOp_AllConstants`, `TestConstraintError_ToAPError`, `TestReplayError_ToAPError`, `TestDeadlockError_ToAPError`. | high | medium | REQ001177 | SYS/AP/error.go, SQB/EX/constraint_error.go, WAL/RP/replay_error.go, TXN/VL/deadlock_error.go, SQF/PS/error.go |
| REQ001179 | SYS/AP | **ERROR Phase 4 — Code + SQLSTATE snapshot lock: freeze constants, add wire format + JSON marshaling, snapshot tests.** Before v1.0, Code and SQLSTATE values must be stable to prevent breaking external consumers that parse error output. **Fix:** (1) Lock all Code and SQLSTATE constants with `// stable since v0.9.0` comment (bump version to 0.9.0 at start). (2) Add deterministic wire format: AP.Error.Error() string format becomes byte-exact stable contract. (3) Add JSON marshaling (MarshalJSON/UnmarshalJSON) for AP.Error — fields serialized as snake_case JSON. (4) New `error_wire_test.go` — snapshot tests for Error() string per Kind, byte-exact. (5) New `error_json_test.go` — snapshot tests for marshaled JSON per Kind, round-trip test. (6) Add `TestErrorFormat_CrossVersion` verifying old format strings still parse correctly (if applicable). | medium | small | REQ001178 | SYS/AP/error_code.go, SYS/AP/error.go, SYS/AP/error_wire_test.go, SYS/AP/error_json_test.go |
| REQ001180 | SYS/AP | **ERROR Phase 5 — Sentinel removal: delete 14 deprecated sentinels, migrate all errors.Is and == comparisons to IsKind/IsCode.** After Phase 1-4, all code paths produce *AP.Error with Code/SQLSTATE/Module/Layer. The 14 deprecated sentinels in SYS/AP/ap.go and the two renamed ErrDivByZero aliases are dead weight. ~45 errors.Is sites and ~33 sentinel references in tests must migrate. **Fix:** (1) Remove 14 deprecated sentinel vars from SYS/AP/ap.go. (2) Remove EV.ErrDivByZero deprecated alias from SQB/EV/eval.go. (3) Remove UT.ErrDivByZero deprecated alias from SQB/UT/decimal.go. (4) Migrate all errors.Is(err, ErrXxx) to AP.IsKind(err, AP.KindXxx) or AP.IsCode(err, CodeXxx) across all production code. (5) Migrate all err == ErrNoRows pointer comparisons to AP.IsKind(err, AP.KindNotFound). (6) Migrate all test files using removed sentinels. (7) `go vet ./...` reports zero deprecated usages. (8) `go test ./... -race -count=1` passes. (9) Add `TestNoDeprecatedSentinels` scanning for any remaining sentinel references. | medium | large | REQ001179 | SYS/AP/ap.go, SQB/EV/eval.go, SQB/UT/decimal.go, all test files using removed sentinels |

