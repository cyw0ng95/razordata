## TBD
| ID | Subsystem | Requirement | Priority | Effort | Deps | Touches |
| --- | --- | --- | --- | --- | --- | --- |
| REQ001968 | tests/sqlcmp/slt | **Pass random/groupby/slt_good_9 in pre-commit gate.** 6 failures / 10,012 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001969 | tests/sqlcmp/slt | **Pass random/groupby/slt_good_10 in pre-commit gate.** 7 failures / 10,012 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001970 | tests/sqlcmp/slt | **Pass random/groupby/slt_good_11 in pre-commit gate.** 26 failures / 10,012 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001971 | tests/sqlcmp/slt | **Pass random/groupby/slt_good_12 in pre-commit gate.** 812 failures / 10,012 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | medium | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001998 | ENG/LS | **Columnar memtable — store recent writes in columnar format for fast vectorized reads.** Add `columnarMemtable` in front of skiplist; flush to SST at threshold. ~2x throughput for read-after-write. | high | large | REQ001995 | ENG/LS/skiplist.go, ENG/LS/memtable.go, ENG/LS/engine.go |
| REQ001999 | SQB/OP | **Grace hash join — spill-to-disk for large build sides.** Estimate build-side size; partition and spill to temp files when exceeding memory threshold. Verify with 1M×1K join. | high | large | REQ001981 | SQB/OP/op_vec_join.go, SQB/OP/hashjoin.go |
| REQ002005 | tests/sqlcmp/slt | **Pass remaining random/groupby/slt_good_* in pre-commit gate.** 5 files still have failures (3-812 per file) after REQ001979 fix. 9 files (slt_good_0-7, 13) now pass and are in `includedCases`. When resolved, add to `includedCases`. | high | medium | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ002008 | SQF/PS | **Arena-allocate parser Expr-nodes + mutate-in-place constant folding.** Split from REQ001976 (which shipped Parser pooling). 16.5M alloc objects / ~20% CPU per profile. Arena per-parse, reset on `Parser.Close`. Verify with `slt_good_109..119` `-alloc_objects`. | medium | large | REQ001976 | SQF/PS/expr.go, SQF/PS/ps.go, SQF/RE/rewrite.go |
| REQ002012 | SQB/EX | **Eliminate per-row `make+copy` in `drainBatchProducer`.** `drain_batch.go:79` does `make([]Value, len(row.Data))` + `copy()` for every result row because the shared batch buffer is reused. For 10K-row results, that's 10K allocations (5% of alloc bytes, 220 MB). Fix: use `RowArena.AllocRow(len(row.Data))` (bump allocation, zero per-row heap cost) instead of `make+copy`. The arena is already available via `execCtx.RowArena`. Verify with `-alloc_objects` showing `drainBatchProducer` allocations near zero. | medium | medium | REQ002009 | SQB/EX/drain_batch.go |
| REQ002013 | tests/sqlcmp/slt | **Pool `[]Value` conversion in `RazorDriver.QueryRaw`.** `razor_driver.go:287` allocates `make([]Value, len(row.Data))` per result row (6% of alloc objects, 181K objects per file). Fix: pre-allocate a flat `[]Value` buffer sized to `len(rows) × maxCols` and carve sub-slices, or use a `sync.Pool` for result row buffers. Verify with `-alloc_objects` profile. | low | small |  | tests/sqlcmp/slt/razor_driver.go |
| REQ002037 | SQB/EX | **Merge redundant coverage tests — eliminate 160 engine creations in `coverage_test.go`.** `coverage_test.go` has 88 test functions creating 160 engines (avg 1.8/test). Most are simple nil-checks or constructor tests that don't need a full engine (e.g., `TestCoverage_Analyze_WithParams` just calls `NewAnalyze(nil).WithParams()`). Fix: (a) move engine-less tests to a `_noeng_test.go` file with no engine setup, (b) merge remaining engine tests into table-driven subtests sharing one engine, (c) batch `ResetForTest` calls. Target: <30 engine creations for the same coverage. Verify: EX test time drops; `coverage_test.go` engine creations < 30. | medium | small | REQ002035 | SQB/EX/coverage_test.go |
| REQ002066 | SQB/EX | **[P2] planCache eviction O(N²).** ex.go:610-614 evicts oldest LRU entry by scanning entire `entries` map (`for key, val := range e.planCache.entries`). With 128 max entries, each eviction costs ~128 map iterations. High plan-cache pressure → micro-stalls visible in pprof as `runtime.mapIterNext`. Fix: use `container/list` LRU for O(1) MoveToFront + O(1) removal. | p2 | small |  | SQB/EX/ex.go |
| REQ002068 | tests/sqlcmp/slt | **Parallel SLT file execution — worker pool with N engine instances.** `TestSLT_InList` runs 296 files sequentially on a single `RazorDriver`. Each file has independent database state (Reset between files), making them trivially parallelizable. Implement a worker pool where each worker owns its own `RazorDriver`/engine instance, files are dispatched via channel, and results are collected concurrently. Size the pool based on `runtime.NumCPU()` and memory budget (`GOMEMLIMIT / perEngineMemory`). Preserve quick-fail semantics via `atomic.Bool` abort flag. Add result collector that reports per-file results as subtests. Verify with `go test -tags slt_corpus -run TestSLT_InList -count=1` showing ~4-6x wall-clock speedup on 8-core machine. | high | medium | REQ001454 | tests/sqlcmp/slt/slt_inlist_test.go, tests/sqlcmp/slt/razor_driver.go |
| REQ002070 | tests/sqlcmp/slt | **Pre-parse SLT files concurrently before execution.** `TestSLT_InList` parses each .test file sequentially during the execution loop (open + bufio.Scan + parse records). Parsing is CPU-bound and trivially parallelizable. Pre-parse all 296 files concurrently at start of test using goroutines, then feed parsed records to the execution loop. Eliminates serial file I/O + parse wait time between files. Combined with parallel execution (REQ002068), parsing happens inside each worker. Verify: parse time overlaps with execution, visible as reduced gap between file runs in profile. | low | small |  | tests/sqlcmp/slt/slt_inlist_test.go, tests/sqlcmp/slt/parser.go |
| REQ002072 | tests/sqlcmp/slt | **Use Go test built-in t.Parallel() for SLT subtests.** Alternative to REQ002068: each file subtest calls `t.Parallel()` and creates its own `RazorDriver`, letting Go's test harness manage parallelism via the `-p` flag. Simpler code than custom worker pool but less control over memory budgeting and quick-fail propagation. Each subtest creates a fresh engine (higher per-file overhead but simpler lifecycle). Useful as a fallback / comparison baseline for REQ002068. Verify: same correctness as sequential; parallelism controlled by `go test -p N`. | medium | small | REQ001454 | tests/sqlcmp/slt/slt_inlist_test.go |
| REQ002075 | SQB/EV | **Selection vector allocation per comparison — no reuse.** Every comparison kernel (cmpInt64ColsEQ, cmpInt64ColsLT, etc.) allocates `make([]uint16, 0, n)` per call. With BatchSize=1024 and 2-3 Filter operators per query, this is 2-3 allocations of ~2KB per batch. These selection vectors are short-lived and ideal for `sync.Pool` reuse. Fix: add a `sync.Pool` for `[]uint16` with capacity 1024. Comparison kernels acquire from pool instead of allocating. Return to pool when caller is done with the selection vector. Verify: alloc_objects in Filter-heavy benchmarks reduced by 30-50% per comparison; no correctness regression. | medium | small |  | SQB/EV/eval_vec.go |
| REQ002077 | SQB/EV | **Per-batch column allocation in arithmetic/projection chains — no buffer reuse.** Each arithmetic operation in evalArithIntBatch (eval_vec.go:1573) allocates `make([]int64, batch.Size)` for the output column (`make([]float64, ...)` for float, `make([]string, ...)` for string ops). For `a+b*2+c*3`, the expression tree allocates 4 separate output columns (8KB each = 32KB) per batch. The EvalBatchExpr memo cache avoids re-evaluation but doesn't reuse output buffers. Fix: add a sync.Pool for `[]int64`/`[]float64`/`[]string` slices of size 1024. Arithmetic kernels acquire from pool instead of allocating. Extend UT.ColumnData with a Pooled flag for automatic return. Verify: projection-heavy queries show 1.5-2x speedup; alloc_objects reduced by 40-60% per arithmetic chain. | medium | small |  | SQB/EV/eval_vec.go, SQB/UT/batch.go |
| REQ002078 | SQB/EX → ENG/LS | **Filter predicate pushdown to LSM scan level.** SeqScan reads all rows from LSM into batches (1024 rows), then Filter evaluates the predicate per-batch. For highly selective queries (e.g., `WHERE pk = 9999` on 10K rows), the entire table is scanned and most rows are filtered out, wasting LSM merge iteration + page cache I/O. Fix: push simple comparison predicates (col = literal, col > literal, col < literal) down to the LSM iterator level. The LSM iterator can skip non-matching key ranges during merge iteration, reducing rows read from storage. IndexScan already handles point lookups; this extends pushdown to non-indexed columns via key-range filtering during SeqScan. Verify: selective queries show 2-5x speedup; full-table scans show no regression. | high | large |  | SQB/EX/vec_transform.go, ENG/LS/sst_iterator.go, ENG/LS/borrowed_iter.go |
| REQ002079 | SQB/EX | **SQL text normalization for plan cache — increase hit rates.** Plan cache keys on raw SQL text. Queries with different whitespace, case, or constant formats (`WHERE x > 5` vs `WHERE x > 5.0` vs `WHERE  x  >  5`) produce separate cache entries. Normalize SQL before caching: lowercase identifiers, collapse whitespace, detect constant-foldable expressions. Increases plan cache hit rate for workloads with SQL text variance. Verify: plan cache hit rate increases 20-40% on SLT workloads; no correctness regression. | medium | medium |  | SQB/EX/ex.go |
| REQ002080 | SQB/EX | **Operator fusion beyond FusedBatchScan — Aggregate+Filter, Join+Filter, Sort+Limit.** `tryFusedBatchScan` detects `Limit(Project(Filter(SeqScan)))` but misses other common fusion opportunities: (1) Aggregate+Filter: `SELECT count(*) FROM t WHERE x > 5` can push filter into aggregate to reduce input rows, (2) Join+Filter: push predicates below join operators earlier in planning, (3) Sort+Limit: Top-N optimization for `ORDER BY ... LIMIT n` (TopNSort exists but may not be selected optimally), (4) Distinct+Aggregate: optimize `SELECT DISTINCT count(*)` patterns. Add fusion detection in `tryVectorizePlan` for these shapes. Verify: queries using these patterns show 1.5-3x speedup; no correctness regression. | high | large |  | SQB/EX/vec_transform.go |
| REQ002081 | SQB/EX | **Adaptive join strategy selection based on data characteristics.** VectorizedHashJoin with parallel probe is good, but join strategy selection is static. Implement adaptive selection: (1) Small build side (< 1000 rows): hash join (current), (2) Medium + indexed: index nested loop join if available, (3) Large + sorted: sort-merge join, (4) Cross product detection: optimize `1=1` joins (currently creates NLJ per memory). Add cardinality estimation and cost-based selection in planner. Verify: join-heavy queries show 2-4x speedup; no correctness regression. | high | large |  | SQB/EX/planner_select.go, SQB/EX/vec_transform.go |
| REQ002082 | SQB/EX | **Correlated subquery unnesting — transform to joins.** Correlated subqueries execute per-row, causing O(N×M) performance. Transform correlated subqueries into joins: (1) `WHERE x IN (SELECT y FROM t2)` → semi-join, (2) `WHERE EXISTS (SELECT ...)` → semi-join, (3) Scalar subqueries → lateral join or aggregation. Detect correlation in subquery analysis pass and rewrite to join form. Verify: queries with correlated subqueries show 5-10x speedup; no correctness regression. | high | large |  | SQB/EX/planner_select.go, SQF/RE/rewrite.go |
| REQ002084 | SQB/EX | **Query result caching for read-heavy workloads.** For read-only queries with identical SQL and parameters, cache result sets to avoid re-execution. Implement LRU cache keyed by (SQL text, parameter values, schema version). Invalidate on table modifications (INSERT/UPDATE/DELETE). Size limit based on memory budget. Useful for SLT tests and analytical workloads with repeated queries. Verify: read-heavy workloads show 3-5x speedup; cache hit rate > 50% on SLT; no correctness regression. | medium | large |  | SQB/EX/ex.go |
| REQ002085 | SYS/CT | **Add SYS/CT contract types cluster — eliminate reverse dependencies from SQB/DT and TXN to upper layers.** Create `internal/SYS/CT/` as a pure-interfaces-and-value-types package (zero implementation code). Move shared contract types that are currently defined in upper layers and aliased downward: (1) from `SQF/PL`: `Operator` interface, `Row` struct, `ExecContext` struct, `ColInfo` struct, `TxWriter`/`InMemoryTxWriter` interfaces, `Value`/`ValueKind` (currently aliased from AP through PL), `ErrNoRows` sentinel; (2) from `SYS/AP`: `Value` struct, `ValueKind` type, `CollateFunc` type, all Value constructors (`NewIntValue`, etc.), kind constants; (3) from `SQB/DT`: `Store`/`BatchStore`/`BatchDeleteStore`/`StatsCatalog` interfaces, `SessionCounterAccessor`/`DBAttachManager` interfaces. After extraction: `SQF/PL` imports `SYS/CT` instead of defining Operator/Row/ExecContext locally; `SQB/DT` imports `SYS/CT` instead of aliasing from SQF/PL and SYS/AP; `TXN/VL` imports `SYS/CT` instead of `SQB/DT` for Store/Writer types; `SYS/AP` re-exports `SYS/CT` types via type aliases so external consumers (`cmd/razor`, `driver/`, `tests/`) see no API change. `SYS/CT` itself imports only `LOG/EC` (for error types used in interface signatures). Result: all reverse edges eliminated — dependency flow is strictly downward: `SYS/CT → SQF/PL`, `SYS/CT → SQB/DT`, `SYS/CT → TXN/VL`. No layer imports "up" anymore. Verify: `go build ./...` passes; `go test ./internal/... -race -count=1` passes; all SLT results identical; `SQB/DT/types.go` no longer contains type aliases to `SQF/PL` or `SYS/AP`. | high | large |  | SYS/CT/ (new), SQF/PL/types.go, SQB/DT/types.go, SQB/DT/storage.go, SYS/AP/ap.go, TXN/VL/vl.go |
| REQ002086 | SQB/EX | **Split SQB/EX god package (149 files, 40 internal imports) into statement-type sub-packages.** SQB/EX is the most coupled package in the codebase — it imports all 7 other SQB clusters plus 3 SQO, 4 SQF, 3 DBG, 1 ENG, and SYS/AP. Any change cascades through EX. Decompose by SQL statement lifecycle into sub-packages: (1) `SQB/EX/dml/` — INSERT/UPDATE/DELETE execution (imports: WT+DT+EV+UT+SQF/PL+SYS/AP); (2) `SQB/EX/ddl/` — CREATE/DROP/ALTER execution (imports: AD+DT+SQF/PS+SYS/AP); (3) `SQB/EX/pragma/` — PRAGMA execution (imports: DT+SYS/AP); (4) `SQB/EX/query.go` — Query() entry point + shared dispatch, stays at EX root level; (5) `SQB/EX/context.go` — ExecContext and shared execution state stays at root. Each sub-package imports only the clusters it needs, cutting per-package import fan-out from 40 to ~10-15. The EX root package re-exports sub-package constructors so callers (SYS/SY, SYS/SE) see no API change. Verify: `go build ./...` passes; `go test ./internal/SQB/EX/... -race -count=1` passes; no sub-package imports another sub-package; all SLT results identical. | high | large | REQ002085 | SQB/EX/ (restructure), SQB/EX/dml/ (new), SQB/EX/ddl/ (new), SQB/EX/pragma/ (new) |
| REQ002087 | SQB/OP | **Split SQB/OP monolith (97 files, 32K lines) into operator-category sub-packages.** SQB/OP mixes joins, scans, sort, push-down, and vectorized ops in one package. Decompose by operator category: (1) `SQB/OP/core/` — Operator interface, Factory, pipeline base types (operator.go, factory.go, push_pipeline.go, push_operator.go). Must be thin — interfaces + factory only, no concrete operators; (2) `SQB/OP/scan/` — SeqScan, IndexScan, BitmapScan, CoveringIndexScan, IndexOnlyScan; (3) `SQB/OP/join/` — NLJ, HashJoin, MergeJoin, GraceHashJoin, CrossJoin, HashCrossJoin; (4) `SQB/OP/sort/` — Sort, TopNSort, spill logic; (5) `SQB/OP/scalar/` — Project, Filter, Limit, Offset, Distinct, CompoundOp; (6) `SQB/OP/vec/` — Vectorized operators (FusedBatchScan, VectorizedHashJoin, VectorizedOffset, VectorizedNestedLoopJoin, batch adapters); (7) `SQB/OP/pushdown/` — FilterPushdown, PushFilter, PushLimit, PushProject. Each sub-package imports `SQB/OP/core/` for shared interfaces. `SQB/OP/core/` imports only `SQB/DT`, `SQB/UT`, `SQF/PL`, `SYS/AP` (mirroring the current OP dependency set minus the concrete operator implementations). Verify: `go build ./...` passes; `go test ./internal/SQB/OP/... -race -count=1` passes; no sub-package imports another sibling sub-package (all go through core); all SLT results identical. | high | large | REQ002085 | SQB/OP/ (restructure), SQB/OP/core/ (new), SQB/OP/scan/ (new), SQB/OP/join/ (new), SQB/OP/sort/ (new), SQB/OP/scalar/ (new), SQB/OP/vec/ (new), SQB/OP/pushdown/ (new) |
| REQ002088 | tools | **Add dependency DAG linter — enforce architectural layering rules in CI.** The current layering (LOG→FIL→MEM→WAL→ENG→TXN→SQF→SQO→SQB→SYS→DBG) works by convention only. A future PR could accidentally make ENG import SQB and nothing would catch it until code review. Add `tools/deps_check/main.go` that: (1) parses all `import` paths in `internal/`; (2) maps each import to its subsystem (LOG, FIL, MEM, WAL, ENG, TXN, SQF, SQO, SQB, SYS, DBG); (3) checks each import against a whitelist of allowed cross-subsystem dependencies; (4) exits non-zero with a clear message on violation. Key rules to enforce: (a) ENG must never import SQF/SQO/SQB/SYS; (b) WAL must never import SQF/SQO/SQB/TXN; (c) SQO must never import SQB; (d) SQB/OP must never import SQB/EX; (e) SYS/CT (after REQ002085) must only import LOG/EC; (f) SQB/DT must not import SQF/PL or SQB/EX (after REQ002085, it imports SYS/CT instead). Add to CI pipeline and `before-commit-cases.sh`. Verify: `go run tools/deps_check/main.go ./internal/...` exits 0 on current codebase (after REQ002085); deliberately adding a forbidden import causes exit 1 with diagnostic message. | high | small | REQ002085 | tools/deps_check/ (new), before-commit-cases.sh |
| REQ002089 | SQB | **Separate test infrastructure from implementation packages.** Test helpers are co-located with implementation, inflating package sizes and creating coupling. Specifically: (1) `SQB/UT` contains `RowOperatorAdapter`, `BatchProducerAdapter`, test store builders, and result comparison helpers used by tests across SQB/OP, SQB/EX, SQB/AG, and SQB/WT. Move shared test utilities to `SQB/testutil/` (adapter.go, fixture.go, compare.go). Test-only types like `RowOperatorAdapter` should not be in the production `SQB/UT` package. (2) REQ-based integration test files (`req001113_remaining_test.go`, `req001645_*_test.go`, etc.) are large (200-800 lines each) and test cross-cutting behavior through the full engine. Move to `tests/integration/` as standalone test packages that import `SYS/AP` as their public API. This reduces SQB/OP and SQB/EX test file counts and makes integration tests independent of internal package restructurings (REQ002086/REQ002087). Verify: `go build ./...` passes; `go test ./internal/SQB/... -race -count=1` passes; `go test ./tests/integration/... -race -count=1` passes; `SQB/UT` no longer exports test-only adapter types; all SLT results identical. | medium | medium | REQ002086, REQ002087 | SQB/UT/ (trim), SQB/testutil/ (new), tests/integration/ (new) |
| REQ002090 | SQB/IR | **Add execution plan IR between optimizer and executor — decouple planner schema from executor.** SQB/EX directly imports SQF/PL (planner operator tree) and SQO/* (optimizer). Any planner internal representation change forces executor changes and vice versa. Define a stable IR (intermediate representation) that decouples planning from execution: (1) `SQB/IR/plan.go` — Plan node types (ScanPlan, JoinPlan, AggregatePlan, SortPlan, etc.) that represent the execution contract without exposing planner internals; (2) `SQB/IR/convert.go` — `PL.Operator → IR.Plan` conversion, absorbs schema changes on one side; (3) `SQB/IR/schema.go` — Plan-level schema (column bindings, type info) independent of PL's Row/ColInfo representation. After this, `SQB/EX` imports `SQB/IR` instead of `SQF/PL` directly. The planner can evolve its internal tree structure (e.g., changing how it represents subqueries or join order) without breaking the executor. The executor can optimize for execution (e.g., adding execution-specific metadata) without being constrained by planner data structures. The `convert.go` layer is the single point that must be updated when either side changes. Evaluate necessity after REQ002085 (SYS/CT) is in place — CT may provide sufficient decoupling for most cases, making IR lower priority. Verify: `go build ./...` passes; `go test ./internal/... -race -count=1` passes; `SQB/EX` no longer directly imports `SQF/PL` operator tree types; all SLT results identical. | medium | large | REQ002085, REQ002086 | SQB/IR/ (new), SQB/EX/ex.go, SQB/EX/vec_transform.go |
| REQ002091 | SYS/AP | **Thin SYS/AP re-export layer — reduce #1 compilation bottleneck (21 dependents, 44% of codebase).** SYS/AP re-exports everything from `LOG/EC` (Error, Kind, Code, SQLSTATE, Module, Layer, Classification, all Kind/Entity/Layer/Op constants, New, Wrap, IsKind, etc.) and `SYS/BK` (BackupOptions, BackupStats, RestoreStats, Backup(), Restore()). Any change to a LOG/EC error constant or SYS/BK type forces SYS/AP recompilation, which cascades to all 21 dependents. Fix: (1) Stop re-exporting `LOG/EC` through `SYS/AP`. Every package that needs structured errors already imports `LOG/EC` directly — the re-export in AP is convenience at the cost of coupling 44% of the codebase to EC's compile time. Remove `error.go`, `error_wrap.go`, `error_classify.go`, `error_code.go`, `error_kind.go` from AP; update any callers that relied on the re-export to import `LOG/EC` directly. (2) Stop re-exporting `SYS/BK` through `SYS/AP`. Only 2-3 packages (cmd/razor, SYS/SY) use backup functionality — they should import `SYS/BK` directly. After thinning, SYS/AP contains only: Engine/Session/Transaction/Stmt interfaces, Options, Result, Rows, IsolationLevel, and re-exports of SYS/CT types (after REQ002085). AP's dependents drop from 21 to ~8-10 (only packages that need the API interfaces). Compilation impact: changes to LOG/EC no longer cascade through SYS/AP to 44% of the codebase; AP recompilation ripple reduced ~50%. Verify: `go build ./...` passes; `go test ./internal/... -race -count=1` passes; `go vet ./...` zero warnings; AP no longer re-exports LOG/EC or SYS/BK types; all SLT results identical. | high | medium | REQ002085 | SYS/AP/ap.go, SYS/AP/error.go, SYS/AP/error_wrap.go, SYS/AP/error_classify.go, SYS/AP/error_code.go, SYS/AP/error_kind.go, callers of AP error/BK re-exports |
| REQ002093 | SQB/WT | **`UPDATE` is 5.8x slower than modernc/sqlite and allocates 183 KB vs 76 B/op — close DML write gap.** `driver/bench_compare_test.go:BenchmarkRazordata_Update` (100 rows, 200ms benchtime): razor 281305 ns/op / 183130 B/op / 1871 allocs/op vs modernc 48341 ns/op / 76 B/op / 4 allocs/op. Razor is 5.82x slower on time, allocates ~2400x more bytes and 467x more objects per `UPDATE ... WHERE age > 25`. Per-row cost dominates — the SQL is single-row-equivalent. PR-1 (Filter shortcut + arena single-row clone + RowIndex O(1) lookup + nil-schema-alloc + FK/Check early-return + pending-nil unique key skip) delivered: ns/op 283 → 137 µs (-52%), B/op 183 → 100 KB (-45%), allocs 1871 → 815 (-56%). Currently ~2.8x modernc on time. Remaining gap to reach ns/op-within-2x and B/op-within-10x addressed by REQ002104 (needsStableData eliminates Filter.refillBatch 28% Data clone). Final verify: `BenchmarkRazordata_Update` ns/op within 2x of `BenchmarkSQLite_Update`; alloc_bytes within 10x. | high | medium | REQ002104 | driver/stmt.go, driver/rows.go, SQB/WT/ (write batch path) |
| REQ002094 | SQB/WT | **`DELETE` is 5.2x slower than modernc/sqlite and allocates 188 KB vs 88 B/op — same write-path churn as UPDATE.** `BenchmarkRazordata_Delete` (100 rows): razor 212299 ns/op / 188689 B/op / 596 allocs/op vs modernc 40517 ns/op / 88 B/op / 4 allocs/op. Razor is 5.24x slower, ~2144x more bytes per delete, 149x more allocs. The bench resets the table each iteration so the cost is per-delete not per-table-build — same per-row churn pattern as REQ002093. PR-1 (Filter shortcut + arena single-row clone + RowIndex O(1) lookup + nil-schema-alloc + FK/Check early-return + pending-nil unique key skip) delivered improvements shared with UPDATE path. Remaining gap addressed by REQ002104 once Update path is optimised (DELETE shares the per-row clone + index-maintain hot path). Final verify: `BenchmarkRazordata_Delete` ns/op within 2x of modernc; alloc_bytes within 10x. | high | small | REQ002093, REQ002104 | driver/stmt.go, SQB/WT/ (delete tombstone path) |
| REQ002105 | SQB/EX | **Add vectorized-aware bench bypassing `database/sql` — measure true engine performance.** All current benches go through `database/sql` driver → volcano-style row-at-a-time `Next()` chain. The vectorized engine (`VectorizedHashJoin`, `FusedBatchScan`, `VectorizedHashAggregate`) is never measured because (a) the planner chooses `HashCrossJoin` over `HashJoin` for small tables, and (b) even when vectorized operators run, their output is converted to rows via `BatchToRowAdapter`. Add a benchmark in `driver/bench_compare_test.go` that calls `EX.Executor.QueryAll` directly (bypassing `database/sql`), and also adds a pure-vectorized path that drains `BatchProducer` directly without `BatchToRowAdapter`. This isolates the true vectorized engine performance from the driver/volcano tax. Also: add a large-table SelfJoin (10K rows) to force the planner into `VectorizedHashJoin` path. Expected: vectorized path is 3-10x faster than row path for the same query. | medium | medium |  | driver/bench_compare_test.go, SQB/EX/ex.go |
| REQ002106 | SQB/EX | **ON-clause equi-join path uses `HashJoin` (vectorizable) instead of `HashCrossJoin` (row-only).** `planner_select.go:1493-1502` creates `HashCrossJoin` for single-column equi-joins from the ON clause (`ExtractSingleOnEquiKey`). `HashCrossJoin` is row-based and has no vectorized equivalent, so small-table equi-joins (like SelfJoin bench) never reach the vectorized engine. Fix: route ON-clause equi-joins through `HashJoin` (line 1479) which `tryVectorizePlan` converts to `VectorizedHashJoin`. For very small tables (< 64 rows), keep `HashCrossJoin` as an optimization. Also make `VectorizedHashJoin` adaptive to batch size: when the build side is small (< 256 rows), use a smaller batch (64 rows) to reduce batch creation overhead. Estimate: SelfJoin bench drops from 249 µs to ~150 µs (1.0x modernc, currently 1.6x) — the vectorized path eliminates the row-by-row probe overhead. | high | medium | REQ001590 | SQB/EX/planner_select.go, SQB/OP/op_vec_join.go, SQB/UT/batch.go |
| REQ002092 | SQB/DT | **Split DT schema registry from shared type aliases — reduce DDL recompilation ripple.** SQB/DT (3,613 lines, 7 files) is really two concerns: (1) **Shared types** (types.go, ~556 lines) — type aliases (Value, ValueKind, Operator, Row, ExecContext, ColInfo, TxWriter, CollateFunc) and core interfaces (Store, BatchStore, BatchDeleteStore, StatsCatalog, SessionCounterAccessor, DBAttachManager). Rarely changes — only when adding new types. (2) **Schema/catalog registry** (schema.go 828 lines + storage.go 1,235 lines = ~2,063 lines) — StoreSchema, UniqueKey, ForeignKeyConstraint, RegisteredIndex, TriggerInfo, TableHandle, ColumnWriter, index maintenance, schema versioning. Changes frequently on every DDL operation. Currently both concerns live in one package, so any DDL schema change to schema.go/storage.go forces recompilation of all 12 DT dependents (SQB/AD, SQB/AG, SQB/EV, SQB/EX, SQB/OP, SQB/UT, SQB/WT, SYS/DS, SYS/SE, SYS/ST, SYS/SY, SYS/TX) — but most only need the type aliases, not the schema registry. Fix: move schema/catalog registry to new `SQB/SC` (schema catalog) package containing schema.go + storage.go content. Only `SQB/AD` (DDL) and `SQB/WT` (write operations) import `SQB/SC`. `SQB/DT` retains only types.go + packed.go + tx_state.go + index_usage.go + arena.go (~2,550 lines → ~1,000 lines after removing schema/storage). After REQ002085, DT's type aliases become direct SYS/CT imports, shrinking DT further to ~500 lines of pure runtime utilities. Compilation impact: DDL changes recompile 2-3 packages (AD, WT, SC) instead of 12; DT becomes a thin stable contract that rarely changes. Verify: `go build ./...` passes; `go test ./internal/... -race -count=1` passes; `SQB/DT` no longer contains schema.go or storage.go; only AD/WT import SQB/SC; all SLT results identical. | high | medium | REQ002085 | SQB/DT/schema.go, SQB/DT/storage.go, SQB/SC/ (new), SQB/AD/ (update imports), SQB/WT/ (update imports) |
| REQ002140 | SQB/EX | **px_validate shadow mode passes full SLT with 0 mismatches.** Build tag `px_validate` runs both pipeline and legacy paths for every SELECT query, compares results via `CompareRows()`, logs discrepancies, returns legacy. Currently: shadow mode has known mismatches (documented at drain_batch.go:40-41) from EXISTS subqueries, CASE WHEN, and other complex expressions in the vectorized path. Fix: (1) Run `go test ./tests/sqlcmp/slt/... -tags px_validate -run TestSLT_InList -v` and triage every mismatch category; (2) Fix each mismatch root-cause in `tryVectorizePlan`, vectorized operators, or SpecializeFunc fallback logic; (3) After 0 mismatches across the full SLT inlist, enable shadow mode in `before-commit-cases.sh` as a required pre-commit gate on a fast subset (select1..4 + aggregates). Verify: SLT select1..4 pass with 0 shadow mismatches; mismatches==0 after running full InList with `-tags px_validate`; `ShadowStats().Mismatches == 0` recorded. | p1 | large | REQ002124, REQ002125, REQ002126, REQ002127 | SQB/EX/drain_batch_shadow.go, SQB/EX/vec_transform.go, SQB/OP/ (vectorized ops), tests/sqlcmp/slt/runner.go |
| REQ002160 | SQB/PX | **Memory pressure handling for HashJoin and Aggregate.** Detect build-side size exceeding threshold and spill to temporary files using FIL/IO temp storage, matching REQ001999 grace-hash-join pattern. Required for production stability on large joins/aggregates. | high | large | REQ001999 | SQB/PX/stage_join.go, SQB/PX/stage_aggregate.go, FIL/IO/ |
| REQ002170 | SQB/EX | **[REQ002131 step 1/7] PipelineBuilder covers all execution paths — replace tryVectorizePlan as the sole query execution path.** `tryVectorizePlan` is currently called from 6 entry points (Query, QueryAll, QueryStream, Exec, CompilePlan, drain_batch.go) and is the core transformation from row-based to vectorized operators. PipelineBuilder already exists and is enabled by default, but it still falls back to the legacy path when `specHasNoLegacyStages` returns false. Fix: (1) Audit all code paths that call `tryVectorizePlan` or directly use `plan.Root.Next(ctx)` — ensure every SELECT/DML/Compound query shape is handled by PipelineBuilder's `specializePlan` → `decomposePlan`; (2) For each remaining legacy path that calls `tryVectorizePlan`, add a `PipelineBuilder.Build()` equivalent that either succeeds or falls back cleanly; (3) After PipelineBuilder handles all shapes, `tryVectorizePlan` is only called from the fallback path, making it safe to delete in step 3. Verify: `go test ./internal/... -count=1` passes with no regressions; `grep -r 'tryVectorizePlan' internal/` shows no production call sites (only comments/deprecated.go). | high | large |  | SQB/EX/ex.go, SQB/EX/stream.go, SQB/EX/drain_batch.go, SQB/EX/vec_transform.go, SQB/PX/builder.go |

