## TBD
| ID | Subsystem | Requirement | Priority | Effort | Deps | Touches |
| --- | --- | --- | --- | --- | --- | --- |
| REQ001998 | ENG/LS | **Columnar memtable — store recent writes in columnar format for fast vectorized reads.** Add `columnarMemtable` in front of skiplist; flush to SST at threshold. ~2x throughput for read-after-write. | high | large | REQ001995 | ENG/LS/skiplist.go, ENG/LS/memtable.go, ENG/LS/engine.go |
| REQ002008 | SQF/PS | **Arena-allocate parser Expr-nodes + mutate-in-place constant folding.** Split from REQ001976 (which shipped Parser pooling). 16.5M alloc objects / ~20% CPU per profile. Arena per-parse, reset on `Parser.Close`. Verify with `slt_good_109..119` `-alloc_objects`. | medium | large | REQ001976 | SQF/PS/expr.go, SQF/PS/ps.go, SQF/RE/rewrite.go |
| REQ002078 | SQB/PX + ENG/LS | **Filter predicate pushdown to LSM scan level.** SeqScan reads all rows from LSM into batches (1024 rows), then Filter evaluates the predicate per-batch. For highly selective queries (e.g., `WHERE pk = 9999` on 10K rows), the entire table is scanned and most rows are filtered out, wasting LSM merge iteration + page cache I/O. REQ002260 pushes predicates to the Scan node in the QueryPlan; this REQ pushes them one level further — into the LSM iterator itself. Fix: the scan stage passes simple comparison predicates (col = literal, col > literal, col < literal) down to the LSM iterator level; the iterator skips non-matching key ranges during merge iteration, reducing rows read from storage. IndexScan already handles point lookups; this extends pushdown to non-indexed columns via key-range filtering during SeqScan. Verify: selective queries show 2-5x speedup; full-table scans show no regression. | high | large | REQ002260 | SQB/PX/stage_scan.go, ENG/LS/sst_iterator.go, ENG/LS/borrowed_iter.go |
| REQ002081 | SQF/QP + SQB/PX | **Adaptive join strategy selection — a QP pass picks hash vs nested-loop vs sort-merge.** Join algorithm choice is static today (PX stage selection). Fix: add a `JoinStrategyPass` on the QueryPlan DAG that, using each join node's estimated cardinality (REQ002264/REQ002276), selects the physical algorithm: (1) small build side (< 1000 rows): hash join, (2) medium + indexed: index nested-loop join when an index is available, (3) large + sorted input: sort-merge join, (4) cross-product (`1=1`): detect and route accordingly. The pass annotates the join node; PX lowering maps the annotation to `HashJoinStage`/`NestedLoopStage`/`SortMergeStage`. Complements join ordering (REQ002277, which picks the order; this picks the algorithm). Verify: join-heavy queries show 2-4x speedup; `go test ./internal/SQF/QP/... ./internal/SQB/PX/... -race -count=1` passes; SLT join corpus identical. | high | large | REQ002264, REQ002277 | SQF/QP/ (new: pass_join_strategy.go), SQO/CO/cost_estimation.go, SQB/PX/builder.go |
| REQ002084 | SQB/EX | **Query result caching for read-heavy workloads.** For read-only queries with identical SQL and parameters, cache result sets to avoid re-execution. Implement LRU cache keyed by (SQL text, parameter values, schema version). Invalidate on table modifications (INSERT/UPDATE/DELETE). Size limit based on memory budget. Useful for SLT tests and analytical workloads with repeated queries. Verify: read-heavy workloads show 3-5x speedup; cache hit rate > 50% on SLT; no correctness regression. | medium | large |  | SQB/EX/ex.go |
| REQ002089 | SQB | **Separate test infrastructure from implementation packages.** Test helpers are co-located with implementation, inflating package sizes and creating coupling. Specifically: (1) `SQB/UT` contains `RowOperatorAdapter`, `BatchProducerAdapter`, test store builders, and result comparison helpers used by tests across SQB/OP, SQB/EX, SQB/AG, and SQB/WT. Move shared test utilities to `SQB/testutil/` (adapter.go, fixture.go, compare.go). Test-only types like `RowOperatorAdapter` should not be in the production `SQB/UT` package. (2) REQ-based integration test files (`req001113_remaining_test.go`, `req001645_*_test.go`, etc.) are large (200-800 lines each) and test cross-cutting behavior through the full engine. Move to `tests/integration/` as standalone test packages that import `SYS/AP` as their public API. This reduces SQB/OP and SQB/EX test file counts and makes integration tests independent of internal package restructurings (REQ002086/REQ002087). Verify: `go build ./...` passes; `go test ./internal/SQB/... -race -count=1` passes; `go test ./tests/integration/... -race -count=1` passes; `SQB/UT` no longer exports test-only adapter types; all SLT results identical. | medium | medium | REQ002086, REQ002087 | SQB/UT/ (trim), SQB/testutil/ (new), tests/integration/ (new) |
| REQ002093 | SQB/WT | **`UPDATE` is 5.8x slower than modernc/sqlite and allocates 183 KB vs 76 B/op — close DML write gap.** `driver/bench_compare_test.go:BenchmarkRazordata_Update` (100 rows, 200ms benchtime): razor 281305 ns/op / 183130 B/op / 1871 allocs/op vs modernc 48341 ns/op / 76 B/op / 4 allocs/op. Razor is 5.82x slower on time, allocates ~2400x more bytes and 467x more objects per `UPDATE ... WHERE age > 25`. Per-row cost dominates — the SQL is single-row-equivalent. PR-1 (Filter shortcut + arena single-row clone + RowIndex O(1) lookup + nil-schema-alloc + FK/Check early-return + pending-nil unique key skip) delivered: ns/op 283 → 137 µs (-52%), B/op 183 → 100 KB (-45%), allocs 1871 → 815 (-56%). Currently ~2.8x modernc on time. Remaining gap to reach ns/op-within-2x and B/op-within-10x addressed by REQ002104 (needsStableData eliminates Filter.refillBatch 28% Data clone). Final verify: `BenchmarkRazordata_Update` ns/op within 2x of `BenchmarkSQLite_Update`; alloc_bytes within 10x. | high | medium | REQ002104 | driver/stmt.go, driver/rows.go, SQB/WT/ (write batch path) |
| REQ002094 | SQB/WT | **`DELETE` is 5.2x slower than modernc/sqlite and allocates 188 KB vs 88 B/op — same write-path churn as UPDATE.** `BenchmarkRazordata_Delete` (100 rows): razor 212299 ns/op / 188689 B/op / 596 allocs/op vs modernc 40517 ns/op / 88 B/op / 4 allocs/op. Razor is 5.24x slower, ~2144x more bytes per delete, 149x more allocs. The bench resets the table each iteration so the cost is per-delete not per-table-build — same per-row churn pattern as REQ002093. PR-1 (Filter shortcut + arena single-row clone + RowIndex O(1) lookup + nil-schema-alloc + FK/Check early-return + pending-nil unique key skip) delivered improvements shared with UPDATE path. Remaining gap addressed by REQ002104 once Update path is optimised (DELETE shares the per-row clone + index-maintain hot path). Final verify: `BenchmarkRazordata_Delete` ns/op within 2x of modernc; alloc_bytes within 10x. | high | small | REQ002093, REQ002104 | driver/stmt.go, SQB/WT/ (delete tombstone path) |
| REQ002105 | SQB/PX + SQB/EX | **Add engine-level bench bypassing `database/sql` — measure true PX pipeline performance.** All current benches go through `database/sql` driver → `BatchToRowAdapter` → row `Next()` chain, so the vectorized PX pipeline is never measured in isolation. Fix: add a benchmark in `driver/bench_compare_test.go` that calls `EX.Executor.QueryAll` directly (bypassing `database/sql`), and a second that drains a `BatchProducer` from the pipeline executor directly without `BatchToRowAdapter`. This isolates true pipeline performance from the driver/row-conversion tax. Also: add a large-table SelfJoin (10K rows) to force a hash-join stage path. Expected: pipeline path is 3-10x faster than row path for the same query. | medium | medium |  | driver/bench_compare_test.go, SQB/PX/executor.go, SQB/EX/ex.go |
| REQ002160 | SQB/PX | **Memory pressure handling for HashJoin and Aggregate.** Detect build-side size exceeding threshold and spill to temporary files using FIL/IO temp storage (supersedes the legacy SQB/OP grace-hash-join). Required for production stability on large joins/aggregates. | high | large |  | SQB/PX/stage_join.go, SQB/PX/stage_aggregate.go, FIL/IO/ |
| REQ002225 | SQO/PF + SQB/PX | **Single FusedScan creation point — an SQO `ScanFusionPass` emits `OP.FusedScan`; PX lowers it 1:1.** FusedScan creation knowledge is encoded twice. The planner constructs `OP.NewFusedScan` inline at `EX/planner_select.go:2116` (REQ001463), and PX independently re-discovers the shape via `tryDecomposeFusedScan`/`tryDecomposeFusedIndexScan` (builder.go:1243/1413). Fix: add a `ScanFusionPass` to the SQO pass chain that rewrites `Limit(Project(Filter(SeqScan/IndexScan)))` into `OP.FusedScan` via `OC.Context.Factory` (using the same predicates as the inline site); delete the inline construction in planner_select.go; `decomposeOp` then lowers `OP.FusedScan` mechanically to `FusedScanStageSpec`. Verify: `OP.NewFusedScan` has exactly one production call site (the pass); `tryDecomposeFusedScan*` gone; `go test ./internal/SQO/... ./internal/SQB/... -race -count=1` passes; SLT select1-select4 pass. | high | large | REQ002256, REQ002257 | SQO/PF/ (new: scan_fusion.go), SQB/EX/planner_select.go, SQB/PX/builder.go |
| REQ002229 | SQB/PX | **Single output-schema resolution — remove the `extractOutputSchema` legacy walker.** Two schema-resolution mechanisms coexist: `decomposePlan`'s bottom-up per-stage `outputSchema` (builder.go:180) and the legacy `extractOutputSchema` walker (builder.go:1487), still used as a fallback at builder.go:161 (`specializePlan` when decomposition returns empty schema) and at builder.go:1043. Fix: make the decomposition schema authoritative for every shape (Window, Compound, DDL, admin) so the fallback is never reached, then delete `extractOutputSchema`. Verify: `grep -rn 'extractOutputSchema' internal/` returns zero production hits; driver `Columns()` output identical on SLT corpus; `go test ./internal/SQB/... -race -count=1` passes. | medium | medium | REQ002228 | SQB/PX/builder.go |
| REQ002230 | SQB/EX | **Single execution-path gate — remove `purePipelineFastPath`.** `purePipelineFastPath` (ex.go:215/303) splits `QueryAll`/`Query`/`QueryStream`/`Exec`/`CompilePlan`/`drainPlanExecCtx` into a pure-PX fast path and a legacy fallback — two execution methods for one entry point. After REQ002227/REQ002229 the fast path is the only correct path. Fix: remove the atomic, the gate checks, the `Enable/Disable/PurePipelineFastPath` accessors, and the fallback branches; the `BuildPipeline`-based flow is the sole path. Verify: `grep -rn 'purePipelineFastPath' internal/` returns zero hits; `go test ./internal/SQB/EX/... -race -count=1` passes; SLT select1-select4 pass. | high | medium | REQ002228, REQ002229 | SQB/EX/ex.go |
| REQ002235 | SQB/WT + SQB/UT | **Single batch↔row conversion path — WT helper duplicates UT (partially done).** `SQB/WT/writers_batch.go` `batchToRow` was already deleted (REQ002246); the remaining `SQB/WT/writers_dml.go:818` `rowsToBatchWT` (a local rows→batch helper) still duplicates `SQB/UT/batch.go` `ToRows`/`ToRowsShared` (batch→row materialization with selection-vector + NULL + type handling) and the inverse path. Fix: replace `rowsToBatchWT` with `UT.ToRowsShared` (or a new single-row `UT.RowAt`) and the equivalent UT-level batch builder; delete the WT-local helper. Verify: `grep -rn 'func rowsToBatchWT' internal/SQB/WT/` returns zero hits; `go test ./internal/SQB/WT/... -race -count=1` passes; SLT insert/update/delete patterns pass. | medium | small |  | SQB/WT/writers_dml.go, SQB/UT/batch.go |
| REQ002250 | SYS/CT + SQF/PL + SQB/DT + SQB/EV + SQB/OP + SQB/EX + SYS/AP | **Shorten the Value/Operator type alias chain — remove intermediate re-exports (partially done).** Value/Operator type aliases cascade through 7 packages: `SYS/CT` (definition) → `SQF/PL` → `SQB/DT` → `SQB/EV` → `SQB/OP` → `SQB/EX` + `SYS/AP`. Each intermediate package re-aliases `type Value = CT.Value`, `type Operator = PL.Operator`, etc. This creates a transitive dependency chain: a change in `CT` invalidates the `.a` cache of every downstream package on the chain. **Already shipped (REQ002250 commit 05584994):** `SQB/EV` and `SQB/AG` now import `CT.Value`/`PL.Row` directly (no longer through DT), removing 2 transitive hops. **Remaining:** (1) `SQB/DT` still re-exports `type Value = CT.Value` (DT/types.go:19) — import `CT.Value` directly; keep `New*Value`/`ValueFromAny`/`CompareValue` conversion functions in DT; (2) `SQB/OP` still re-exports `type Value = pl.Value` (OP/distinct.go:16) — import directly; (3) `SQB/EX` removes `Kind*`/`New*Value` aliases; (4) `SYS/AP` keeps only `Value`/`ValueKind` as public API boundary, removes `Kind*`/`New*Value`/`NullValue` re-exports. Verify: `go build ./...` passes; `go test ./internal/... -race -count=1` passes; incremental recompile after `CT` change no longer rebuilds the full chain. | medium | small |  | SYS/CT/ct.go, SQF/PL/types.go, SQB/DT/types.go, SQB/EV/ev_types.go, SQB/OP/ (store.go, distinct.go), SQB/EX/ex.go, SYS/AP/ap.go |
| REQ002253 | SQF/QP (new) | **Define QueryPlan DAG types — the unified logical+physical plan representation, with NodeType as an open registry.** Add a new package `internal/SQF/QP` with `PlanNode` (NodeType, Schema, Children indices, Exprs, TableName, Cost, Cardinality) and `QueryPlan` (Nodes, RootIdx, Stmt). This is the single data model that replaces both `pl.Operator` trees (SQO) and `PipelineSpec` DAGs (PX). All future optimizations operate on this DAG. **Design constraint — open extension contract:** `NodeType` is a registered extension point, NOT a closed Go enum. Node types are declared via a registry (e.g. `QP.RegisterNode(desc NodeDesc{Name, Lower, Cost, DefaultPasses})`) so adding a new operator/join algorithm requires one registration row, not edits to every switch on the type. `decomposeOp`'s closed-switch pattern must not be recreated in QP. Verify: `go build ./internal/SQF/QP/...` passes; a test registers a dummy node type and runs it through Lower without touching any existing switch. | high | medium |  | SQF/QP/ (new: types.go, registry.go) |
| REQ002254 | SQF/QP + SQB/PX | **Build QueryPlan from existing `pl.Operator` tree — bridge the old and new models.** Add `BuildQueryPlan(root pl.Operator) *QueryPlan` that walks the planner's operator tree and constructs a 1:1 QueryPlan DAG. This is a pure data transformation — no semantics change. The planner continues producing `pl.Operator` trees for backward compatibility; the QueryPlan is built alongside for the new pass pipeline. Verify: unit tests assert DAG structure (node count, types, edges, schema) matches known operator trees; `go test ./internal/SQF/QP/...` passes. | high | medium | REQ002253 | SQF/QP/ (new: builder.go), SQB/OP/ (operator types expose schema) |
| REQ002255 | SQF/QP + SQB/PX | **Lower QueryPlan to PipelineSpec — mechanical 1:1 mapping.** Add `Lower(qp *QueryPlan) *PipelineSpec` that converts each PlanNode to its corresponding StageSpec. This is a pure switch: `NodeFilter → FilterStageSpec`, `NodeProject → ProjectStageSpec`, etc. No fusion, no pattern matching, no optimization — just mapping. Replaces `decomposeOp`'s ~40-branch type switch with a clean registry. Verify: unit tests assert each NodeType maps to the correct StageSpec; lowered PipelineSpec matches `decomposePlan` output for all SLT queries. | high | medium | REQ002253 | SQF/QP/ (new: lower.go), SQB/PX/spec.go |
| REQ002256 | SQB/EX + SQF/QP | **Wire QueryPlan into the planner pipeline — shadow mode first.** After the planner builds the operator tree and runs SQO passes, also build a QueryPlan, run a no-op pass on it, lower it, and use the result as the PipelineSpec. Both paths (`decomposePlan` on operator tree and QueryPlan→Lower) run in parallel; assert identical PipelineSpec output in tests. Verify: `go test ./internal/... -count=1` passes; PipelineSpec output identical to current `decomposePlan` for all SLT queries. | high | medium | REQ002254, REQ002255 | SQB/EX/planner.go, SQB/PX/builder.go |
| REQ002257 | SQF/QP + SQB/PX | **Migrate FilterProjectFusion — register orphaned pass on QueryPlan DAG; delete inline fusion.** Rewrite the existing `FilterProjectFusionPass` (SQO/PF, currently never registered) to operate on QueryPlan: detect `Filter → Project` adjacent nodes and merge them into a single `FilterProject` node. After this, delete the inline `fuseFilterProject` (planner_select.go:415) and `tryDecomposeFusedScan` (builder.go:1243). Verify: `grep -r 'fuseFilterProject' internal/` returns zero; `grep -r 'tryDecomposeFusedScan' internal/` returns zero; `go test ./internal/... -count=1` passes; SLT select1-select4 pass. | high | medium | REQ002256 | SQF/QP/ (new: pass_filter_project.go), SQB/EX/planner_select.go, SQB/PX/builder.go |
| REQ002258 | SQF/QP + SQB/EX | **Migrate ConstantFoldingPass — rewrite to operate on QueryPlan expressions.** Rewrite the existing ConstantFoldingPass to operate on QueryPlan nodes' `Exprs` slots instead of the `pl.Operator` tree. The expression rewriting logic (`RE.RewriteExpr`) stays the same — only the wiring changes. Verify: `go test ./internal/SQF/QP/...` passes; SLT queries with constant expressions produce identical results. | medium | medium | REQ002256 | SQO/PF/constant_folding.go, SQF/QP/ (new: pass_constant_folding.go) |
| REQ002259 | SQF/QP + SQB/EX | **Migrate ColumnPruningPass — rewrite to traverse QueryPlan bottom-up.** Rewrite ColumnPruningPass to traverse the QueryPlan DAG bottom-up, pruning unused columns from each node's schema. The NodeType determines which columns are needed (Sort needs sort keys, Join needs join keys, etc.). Verify: SLT queries with unused columns produce identical results; `go test ./internal/... -count=1` passes. | medium | medium | REQ002256 | SQO/PF/column_pruning.go, SQF/QP/ (new: pass_column_pruning.go) |
| REQ002260 | SQF/QP + SQB/EX | **Migrate PredicatePushdownPass — rewrite to traverse QueryPlan top-down.** Rewrite PredicatePushdownPass to traverse the QueryPlan DAG top-down, pushing filter predicates from Filter nodes into child Scan nodes. The predicate is stored in the Scan node's `Exprs` slot. Depends on ColumnPruning (REQ002259) to reduce columns before pushdown. Verify: SLT queries with WHERE clauses produce identical results; EXPLAIN shows predicates pushed to scan level. | high | medium | REQ002259 | SQO/PF/predicate_pushdown.go, SQF/QP/ (new: pass_predicate_pushdown.go) |
| REQ002261 | SQF/QP + SQB/EX | **Migrate SubqueryDecorrelationPass — rewrite to detect NodeSubquery in DAG.** Rewrite SubqueryDecorrelationPass to detect `NodeSubquery` in the QueryPlan DAG and rewrite them as joins using `OC.Context.SubPlanner`. The existing decorrelation logic is preserved — only the wiring to the DAG changes. Verify: SLT queries with correlated subqueries produce identical results; `go test ./internal/... -count=1` passes. | high | large | REQ002256 | SQO/PF/subquery_decorrelation.go, SQF/QP/ (new: pass_subquery.go) |
| REQ002262 | SQF/QP + SQB/EX | **Migrate IndexSelectionPass — rewrite to examine predicates on Scan nodes.** Rewrite IndexSelectionPass to examine predicates pushed down to Scan nodes and decide SeqScan vs IndexScan. Modifies the Scan node's Type from `NodeScan` to `NodeIndexScan` and stores the index key. Depends on PredicatePushdown (REQ002260). Verify: SLT queries on indexed tables use the same index paths as before. | high | medium | REQ002260 | SQO/PF/index_selection.go, SQF/QP/ (new: pass_index_selection.go) |
| REQ002263 | SQF/QP + SQB/EX | **Migrate remaining 5 passes — SortElimination, LimitPushdown, RedundantPredicateElimination, OrToInExpansion, ImplicitCastElimination.** Each rewrites to operate on QueryPlan DAG nodes' Exprs/Schema slots. These are independent of each other and can be migrated in any order. Verify: each pass: `go test ./internal/... -count=1` passes; SLT queries produce identical results. | medium | medium | REQ002256 | SQO/PF/ (5 files), SQF/QP/ (5 new pass files) |
| REQ002264 | SQF/QP + SQB/EX | **Migrate cost estimation — CO functions operate on QueryPlan nodes, and cost drives choice.** The `CO.EstimateSelectivity`/`EstimateSelectivityWithStats` functions remain unchanged (they operate on `PS.Expr`). The migration is about wiring: the coordinator calls cost estimation from the QueryPlan DAG lookup instead of from the operator tree walk. **Design constraint — cost must drive selection:** after migration, `Cost`/`Cardinality` are consulted by at least one pass to pick among alternatives (e.g. IndexSelection REQ002262 chooses SeqScan vs IndexScan by comparing estimated costs), not stored as a decorative annotation. Verify: EXPLAIN cost values unchanged for all SLT queries; IndexSelectionPass's decision is cost-based, not shape-based. | low | small | REQ002256 | SQO/CO/cost_estimation.go, SQB/EX/cost.go |
| REQ002265 | SQF/QP | **Delete SQO directory — all passes migrated to QP.** After all passes are migrated to the QueryPlan DAG, delete `internal/SQO/OC/`, `internal/SQO/PF/`, `internal/SQO/CO/`. The planner now calls `qp.Optimizer.Optimize(queryPlan)` instead of `oc.Optimizer.Optimize(plan)`. Verify: `grep -r 'SQO/' internal/` returns zero; `go build ./...` passes; `go test ./internal/... -count=1` passes. | high | medium | REQ002257–REQ002264 | internal/SQO/ (delete), SQB/EX/planner.go (update imports) |
| REQ002266 | SQF/PL + SQB/OP | **Remove `pl.Operator` interface — optional cleanup after SQO is gone.** After all SQO passes are deleted and the planner builds QueryPlan directly (no longer needing `pl.Operator` trees for optimization), delete the `pl.Operator` interface and its 4000-line implementation in `SQB/OP`. The row-based execution path is fully replaced by the PipelineExecutor. Verify: `grep -r 'pl.Operator' internal/` returns zero; `go build ./...` passes; `go test ./internal/... -count=1` passes. | medium | large | REQ002265 | SQF/PL/types.go, SQB/OP/ (48 files, massive deletion) |
| REQ002267 | SQB/WT + SQB/PX + SQB/EX | **Extend QueryPlan to the DML pipeline — makes REQ002266 mandatory, not optional.** `SQB/OP` is referenced from three non-planner sides after migration: `PX/builder.go`, `PX/executor.go`, `DT/storage.go`, `WT/writers_dml.go`, `WT/writers_admin.go`, `WT/subq.go`. If DML (INSERT/UPDATE/DELETE) keeps its row-based operator tree, `SQB/OP` cannot be fully deleted. Fix: build QueryPlan for DML too — INSERT wraps a `NodeProject`/`NodeValues` + `NodeInsert`; UPDATE is `NodeUpdate` over a `NodeFilter`/`NodeSeqScan`; DELETE is `NodeDelete`. `decomposeOp`'s DML cases (builder.go:323-461) lower these nodes to `StageSpec`s; `execDMLPipeline` runs through the same `PipelineExecutor`. Verify: DML SLT cases pass; `grep -r 'pl.Operator' SQB/WT/ internal/SQB/PX/` returns zero; `go test ./internal/SQB/... -race -count=1` passes. | high | large | REQ002256 | SQB/WT/writers_dml.go, SQB/WT/writers_admin.go, SQB/WT/subq.go, SQB/PX/builder.go, SQB/EX/drain_batch.go |
| REQ002268 | SQB/EX | **Delete legacy drain path — `drain_batch.go` + shadow + default variants.** After REQ002266/REQ002267, `pl.Operator` trees no longer exist, so `drainPipeline`/`drainBatch`/`drainPlanRows` (drain_batch.go:37/129/216), `execDMLPipeline` (drain_batch.go:72), `isDMLOp` (drain_batch.go:116), and the shadow-comparison machinery (`CompareRows`/`rowsDataEqual`/`valueEqual`/`absDiff`, drain_batch.go:239-292 plus `_shadow.go` and `_default.go`) are dead. Fix: delete all three files; every query entry point drains via `PipelineExecutor`. Verify: `grep -r 'drainPipeline\|drainBatch\|CompareRows' internal/` returns zero; `go test ./internal/SQB/EX/... -race -count=1` passes; SLT select1-select4 pass. | medium | small | REQ002266, REQ002267 | SQB/EX/drain_batch.go, SQB/EX/drain_batch_shadow.go, SQB/EX/drain_batch_default.go |
| REQ002269 | SQF/PL | **Delete learned plan cache — `learned.go`, `memo.go`, `predicate_cache.go` after QueryPlan takes over plan caching.** `NewPlanner` (planner.go) currently wires `PL`'s learning-based `PlanCache` (memoized operator trees keyed by SQL hash). Once the planner builds QueryPlan directly (REQ002256), this cache holds dead `pl.Operator` trees and is never consulted. Fix: delete `PL/learned.go`, `PL/memo.go`, `PL/predicate_cache.go`; keep `PL/types.go` (Operator interface) and `PL/factory.go` (OperatorFactory) until REQ002266. If a plan cache is still desired, add it as a QueryPlan-keyed cache in `SQF/QP` — out of scope here. Verify: `grep -r 'PL\.\(Learn\|NewPlanCache\|PredicateCache\)' internal/` returns zero; `go test ./internal/SQF/PL/... -race -count=1` passes; `go build ./...` passes. | medium | medium | REQ002256 | SQF/PL/learned.go, SQF/PL/memo.go, SQF/PL/predicate_cache.go, SQB/EX/planner.go |
| REQ002270 | SQB/DT | **Remove `DT.Operator` and `DT.ExecContext` row-level execution interfaces.** `DT/storage.go` re-exports the row-based `Operator` interface and `ExecContext` consumed by the legacy `Next(ctx)` path (isDMLOp/execDMLPipeline in drain_batch.go:72/116). After REQ002268 those are the last users. Fix: delete the `Operator` interface and `ExecContext` type from `DT`; callers import the vectorized `PX` path directly. Verify: `grep -r 'DT\.Operator\|DT\.ExecContext' internal/` returns zero; `go test ./internal/SQB/DT/... -race -count=1` passes; `go build ./...` passes. | low | small | REQ002268 | SQB/DT/storage.go, SQB/EX/drain_batch.go |
| REQ002271 | SQB/EV | **Retire scalar evaluation layer — `eval.go` after vectorized coverage is complete.** `EV/eval.go` (2248 lines) is the row-at-a-time evaluator; `eval_vec.go` already covers vectorized evaluation and falls back to it in only 2 places (eval_vec.go:1128/2338). After REQ002267 the remaining scalar users (OP/intermediate_basic.go, OP/compound.go, WT) disappear. Fix: fold the 2 vectorized fallback sites onto a single-row `EvalValue` over a shared 1-row batch; move any unreachable scalar-only functions into `eval_vec.go` or delete them; delete `eval.go`. Verify: `grep -r 'EV\.Eval(' internal/` returns zero; vectorized SLT corpus identical; `go test ./internal/SQB/EV/... -race -count=1` passes. | medium | large | REQ002267 | SQB/EV/eval.go, SQB/EV/eval_vec.go, SQB/EV/eval_vec_funcs.go |
| REQ002272 | SQB/EX | **Unify the three query entry points — Exec / Query / QueryAll on one path.** ex.go:835 `Exec`, :984 `Query`, :1081 `QueryAll` each run Build→execute with overlapping materialization (QueryAll re-maps rows, Query wraps Exec). After REQ002230 the fast path is the only path, so: `Exec` is the core; `Query` is `Exec` + schema/rows projection; `QueryAll` is `Query` + materialize. Fix: collapse to a single internal `run` that produces `Result`, with thin exported wrappers for each signature. Verify: `go test ./internal/SQB/EX/... -race -count=1` passes; driver `Query`/`QueryRow`/`Exec` behaviors unchanged on SLT corpus. | medium | small | REQ002230 | SQB/EX/ex.go |
| REQ002273 | SQB/EX | **Delete remaining row-level plan structures — `plan_node.go` and planner `FilterProject`/`Plan` leftovers.** After REQ002266 the planner no longer builds `pl.Operator` trees, so `EX/plan_node.go` (row-level `PlanNode`/`Plan` wrappers) and any planner-side `FilterProject`-shape structs that duplicated the SQO pass are dead. Fix: delete `plan_node.go`; verify the SQO/QP pass is the sole Filter+Project fusion owner (REQ002257). Verify: `grep -r 'plan_node\|FilterProjectPlan' internal/SQB/EX/` returns zero; `go test ./internal/SQB/EX/... -race -count=1` passes; SLT select1-select4 pass. | low | medium | REQ002266, REQ002257 | SQB/EX/plan_node.go, SQB/EX/planner_select.go, SQB/EX/planner.go |
| REQ002274 | SQF/QP | **Add physical properties to PlanNode — ordering and distribution tracking.** Industrial optimizers track the ordering (sorted keys, direction) and distribution (single-node vs hash/broadcast) produced by each node, so order-aware rewrites (merge join reusing an existing sort, group-by exploiting index order, eliminating redundant SORT stages) are possible. Today the QueryPlan has Schema but no ordering metadata, so every sort is materialized even when the input is already ordered. Fix: add `Ordering` (list of `(col, asc)` keys) and `Distribution` fields to `PlanNode`, with derivation helpers; expose a `NodeRequiresSort` pass that drops a Sort node when its child already satisfies the required ordering. Verify: a plan with `SeqScan(order=idx) → Sort → Project` loses the Sort node; `go test ./internal/SQF/QP/... -race -count=1` passes; SLT results identical. | high | medium | REQ002253 | SQF/QP/ (new: props.go, pass_sort_elimination.go), SQB/PX/spec.go |
| REQ002275 | SQF/QP + SQB/EX | **EXPLAIN provenance — every pass and cost decision is attributable.** Debugging the optimizer requires knowing which pass rewrote what, and why an index was chosen. Fix: add a `PlanTrace` to QueryPlan that records each pass execution (name, before/after node counts, touched node IDs) and each cost decision (alternatives considered, chosen cost, driving statistic). Wire `EXPLAIN QUERY PLAN` to print the trace alongside the final DAG. This is the extension-point validation tool for the open registry (REQ002253). Verify: `EXPLAIN QUERY PLAN` on SLT queries shows pass history; `go test ./internal/SQB/EX/... -race -count=1` passes. | medium | medium | REQ002253 | SQF/QP/ (new: trace.go), SQB/EX/planner.go, SQB/EX/explain.go |
| REQ002276 | SQF/QP + SQO/CO | **Cost model consumes real statistics — histograms and NDV on table scans.** REQ002264 wires cost into choice, but the estimates come from `EstimateSelectivityWithStats` which today uses coarse defaults when no stats exist. Fix: build a lightweight statistics layer in `SQF/QP` — per-table row count, per-column NDV (distinct values), and optionally 1-2 bucket histograms for the most-selective columns — populated lazily on first scan of a table and cached in the engine. Cost estimation (REQ002264) reads these when present, falling back to defaults otherwise. Verify: queries on skewed data produce better IndexScan/join-order choices than defaults; `go build ./...` passes; SLT results identical (stats only refine choice, never change semantics). | medium | large | REQ002264 | SQF/QP/ (new: stats.go), SQO/CO/cost_estimation.go, ENG/ (stats cache) |
| REQ002277 | SQF/QP | **Cost-based join ordering — compare alternative join trees, pick cheapest.** Today join order is fixed by the parser/planner; the cost field (REQ002264) is not used to reorder joins. Fix: add a `JoinReorderPass` that, for a chain of N inner-equijoins (N ≤ 8), enumerates join permutations under the DP size-capped regime, computes each candidate's estimated cost via the cost model, and rewrites the DAG to the cheapest tree. This is the first pass that genuinely exercises cost-driven selection across multiple alternatives. Verify: 3-way join queries pick the order implied by selectivity (smallest estimated result first); `go test ./internal/SQF/QP/... -race -count=1` passes; SLT join corpus identical (reordering preserves semantics). | medium | large | REQ002264, REQ002276 | SQF/QP/ (new: pass_join_reorder.go), SQO/CO/cost_estimation.go |
| REQ002278 | SQF/QP | **Batch-size and parallel-degree tuning pass — exploit the QueryPlan DAG's metadata.** PX stages hard-code batch sizes; industrial engines adapt batch size to plan shape (small top-of-pipeline batches for narrow projections, large build-side batches for hash joins) and parallel degree to available CPUs. Fix: add a `TuningPass` that, using each node's Cardinality estimate (REQ002264) and width, annotates stage batch sizes; expose a `SET parallel_degree = N` knob that scales parallelism hints. Verify: benchmark suite shows ≤ 5% regression on narrow vs wide queries; `go test ./internal/SQB/PX/... -race -count=1` passes. | low | medium | REQ002264 | SQF/QP/ (new: pass_tuning.go), SQB/PX/spec.go, SQB/PX/stage.go |
| REQ002279 | SQF/QP + SQB/PX | **EXPLAIN ANALYZE — execution-time stats collection per stage.** REQ002275 traces plan-time decisions; the execution phase is unmeasured. Fix: add per-stage runtime counters (rows produced, wall time, peak memory) to the stage interface, collected by `PipelineExecutor` during execution and attached to the `PlanTrace` from REQ002275. New `EXPLAIN ANALYZE` SQL form runs the query and prints the trace augmented with real counters. Verify: `EXPLAIN ANALYZE SELECT ...` on SLT queries shows real rows/time/memory per stage; `go test ./internal/SQB/PX/... ./internal/SQB/EX/... -race -count=1` passes. | high | medium | REQ002275 | SQF/QP/ (new: trace.go), SQB/PX/executor.go, SQB/PX/stage.go, SQB/EX/explain.go |
| REQ002280 | SQF/QP + SQB/EX | **EXPLAIN ANALYZE — estimate-vs-actual comparison, the cost model's verification tool.** REQ002279 collects real counters but they are not yet compared to the planner's estimates. Fix: format the trace as a two-column view per stage — Estimated (from Cost/Cardinality, REQ002264, and statistics, REQ002276) vs Actual (from REQ002279) — surfacing selectivity and cardinality errors directly. This is the acceptance harness for all future cost-model work (REQ002276, REQ002277). Verify: `EXPLAIN ANALYZE` prints both columns; on uniform data the two columns converge (est within 10x of actual for SLT select1-select4); `go test ./internal/SQB/EX/... -race -count=1` passes. | high | small | REQ002279, REQ002276 | SQF/QP/ (new: trace.go), SQB/EX/explain.go, SQB/EX/planner.go |
| REQ002281 | SQF/QP + SQB/EX | **QueryPlan plan cache — LRU keyed by (normalized SQL, schema version, stats version, SET knobs).** REQ002269 deleted the legacy learning-based cache that held `pl.Operator` trees; the QueryPlan DAG is now the natural cacheable unit. Fix: add a plan cache in the planner path: key = normalized SQL text + schema version + stats version + relevant `SET` state; value = built `*QueryPlan` (post-optimization, pre-Lower). On hit, skip `BuildQueryPlan` and all passes; on miss, build, optimize, cache. Size-capped LRU with stats-version invalidation (REQ002285). Verify: executing the same SQL twice runs the pass pipeline once (asserted via trace); `go test ./internal/SQB/EX/... -race -count=1` passes; SLT results identical. | medium | medium | REQ002256, REQ002276 | SQF/QP/ (new: plan_cache.go), SQB/EX/planner.go |
| REQ002282 | SQB/PX | **Pipeline pipelining — run adjacent stages as producer/consumer instead of materializing entire batches.** `PipelineExecutor` currently runs stages with full-batch materialization between them; for wide intermediate results this costs memory and latency. Fix: add a `PipelinedExecutor` that connects an upstream stage's output to a downstream stage's input via a bounded channel, letting `Filter → Project → Aggregate` streams rows without intermediate materialization. Fall back to the current materializing executor for stages that require two passes (sort, hash join build). Verify: `Filter → Project` queries show ≤ 50% peak-memory on large tables; `go test ./internal/SQB/PX/... -race -count=1` passes; SLT select1-select4 results identical. | medium | large | REQ002256 | SQB/PX/ (new: pipeline_executor.go), SQB/PX/executor.go, SQB/PX/spec.go |
| REQ002283 | SQB/PX + SQB/EX | **Global execution memory budget — per-query quota that spills instead of failing.** REQ002160 spills hash join build sides; aggregation and sort do not, and there is no per-query ceiling. Fix: add a memory budget to the executor's context, tracked per stage (allocator wrapper around hash tables, sort buffers, spill buffers); when the budget is exceeded, stages spill to FIL/IO temp storage rather than panic or grow unbounded. Expose `PRAGMA memory_limit = N` (0 = unlimited, default). Verify: large-group-by and large-sort queries under a small `memory_limit` complete via spill without OOM; `go test ./internal/SQB/PX/... -race -count=1` passes. | high | large | REQ002282, REQ002160 | SQB/PX/executor.go, SQB/PX/stage_join.go, SQB/PX/stage_aggregate.go, SQB/PX/stage_sort.go, FIL/IO/ |
| REQ002284 | SQB/WT + ENG/LS | **Write-path version bump — schema and stats version increment on every committed DML.** Statistics (REQ002276) and the plan cache (REQ002281) become stale after writes; today nothing invalidates them. Fix: maintain a per-table `(schemaVersion, statsVersion)` in engine metadata, incremented by the write path (WT, unified in REQ002267) after each committed INSERT/UPDATE/DELETE; persist to `meta.razor` on close and restore on open. Reads that mutate stats assumptions (REQ002276) re-derive lazily when the version moves. Verify: after a DML, `PRAGMA table_stats_version` (or equivalent debug) shows an incremented value; restart preserves the version; `go test ./internal/SQB/WT/... ./internal/ENG/LS/... -race -count=1` passes. | high | small | REQ002267, REQ002276 | SQB/WT/ (write path), ENG/LS/engine.go, ENG/LS/meta.go |
| REQ002285 | SQF/QP + SQB/EX + SQB/WT | **Version-driven invalidation — plan cache and stats cache check (table, version) on every hit.** REQ002281 caches QueryPlans and REQ002276 caches statistics, but neither checks freshness. Fix: both caches store the `(schemaVersion, statsVersion)` (REQ002284) they were built with; on lookup, if the current version differs, the entry is evicted and rebuilt. Wire the write path (REQ002284) to increment the version, making invalidation automatic — no explicit cache-clearing call sites. Verify: execute a query, INSERT, execute again — the second execution rebuilds the plan and stats (asserted via trace/version); cached-hit query after no writes returns the cached plan; `go test ./internal/SQB/... -race -count=1` passes; SLT results identical. | high | medium | REQ002281, REQ002284 | SQF/QP/plan_cache.go, SQF/QP/stats.go, SQB/WT/ (write path) |
| REQ002286 | SQF/QP + SQB/EV + SQB/AG + SYS | **Unified engine registration entry — one `Engine.Register` for nodes, functions, aggregates, and access methods.** Extension points today are fragmented: QP node registry (REQ002253), EV function registry, AG aggregate registry, and storage access methods each have their own registration API. Fix: define a single `Engine.Register(desc Extension)` covering node types, scalar functions, aggregate functions, and access methods; migrate the existing per-subsystem registries to delegate to it; the engine's `Open` path seeds built-ins through it. This makes the extension surface discoverable in one place and testable in one suite. Verify: a test registers a dummy function, node, and aggregate through `Engine.Register` and uses all three in one query; `go test ./internal/SYS/... ./internal/SQB/EV/... ./internal/SQB/AG/... ./internal/SQF/QP/... -race -count=1` passes. | medium | large | REQ002253, REQ002278 | SQF/QP/registry.go, SQB/EV/function_registry.go, SQB/AG/aggregate_registry.go, SYS/ (engine entry) |
| REQ002287 | SYS/CT + SQB/EV + SQB/OP + ENG/LS | **Frozen type-model contract — sealed `ValueKind` + `TokenType` registries; new types are additive extensions, never a cross-cutting refactor.** Today the SQL surface already declares column types (`DECIMAL`, `JSON`, `DATE`, `TIME`, `TIMESTAMP` in `SQF/LX/token.go:82-88`) that the execution type `ValueKind` (only `null/int/float/text/blob/bool`, `SYS/CT/ct.go:25-32`) cannot represent — a future type addition would touch lexer, parser, AST, planner, executor (52 files switch on `ValueKind`), and storage format simultaneously. Fix: (1) freeze `ValueKind` and `TokenType` as stable enums with reserved code ranges — `ValueKind` 0–15 for core scalar types, 16–31 reserved for the declared-but-unimplemented types (`decimal`, `json`, `date`, `time`, `timestamp`), 32+ for extensions; (2) define the `TypeDesc` contract in `SYS/CT` (parse, validate, compare, hash, encode/decode, scalar-eval fallback) that every future type implements to plug into EV, QP nodes, and ENG/LS encoding without touching their switch tables; (3) route all `ValueKind`/`TokenType` switches through a single dispatch layer (registry lookup), so adding a type = adding one `TypeDesc`, not editing 52 switch sites. Verify: `TypeDesc` interface exists and built-in types register through it; a test adds a dummy `TypeDesc` type and runs it through parse→eval→store→read without modifying any existing `switch v.Kind`; `go test ./internal/SYS/CT/... ./internal/SQB/EV/... ./internal/ENG/LS/... -race -count=1` passes; SLT results identical. | high | large |  | SYS/CT/ct.go (freeze enums, add TypeDesc), SQB/EV/ev_types.go (dispatch), ENG/LS/ (encoding via TypeDesc), SQF/LX/token.go (align) |
| REQ002288 | SYS/CT + SQB/OP + SQB/EV + SQB/DT + SQB/UT | **Delete legacy row fields from `CT.Row` — `Planner`, `ColIndex`, `StoreKey`, `RowFromSubsetDecode`, `DataStable`, `ExecCtx`.** `CT.Row` (SYS/CT/ct.go:170-184) carries 6 row-level execution artifacts used by the legacy `Next(ctx)` path: `Planner`, `ColIndex`, `StoreKey`, `RowFromSubsetDecode`, `DataStable`, `ExecCtx`. After the QueryPlan migration (REQ002266) the row-based operator tree is gone; these fields remain referenced in 128 sites, all in `SQB/OP` (operators.go 34, intermediate_basic.go 27), `SQF/PL` (types.go 14), `SQB/EV` (eval.go/eval_vec.go 20), `SQB/DT` (arena.go/storage.go/types.go/schema.go 13), `SQB/UT` (batch.go 3), and `SQB/WT` (writers_dml.go, matview.go 2). Fix: delete the 6 fields from `CT.Row`; migrate the few legitimate uses (row→batch materialization in UT, arena decode in DT) to local variables or explicit params; the row struct keeps only `Cols`/`Types`/`Data`/`Outer`. Verify: `grep -rn '\.ColIndex\|\.StoreKey\|\.RowFromSubsetDecode\|\.DataStable\|\.ExecCtx\|\.Planner' internal/` returns zero hits outside tests; `go build ./...` passes; `go test ./internal/... -race -count=1` passes; SLT select1-select4 pass. | medium | medium | REQ002266 | SYS/CT/ct.go, SQB/OP/ (48 files), SQF/PL/types.go, SQB/EV/eval.go, SQB/EV/eval_vec.go, SQB/DT/arena.go, SQB/DT/storage.go, SQB/UT/batch.go, SQB/WT/writers_dml.go |
| REQ002289 | SQB/EV + SQB/PX | **Single expression-evaluation engine — fold the scalar fallback into the vectorized path via a reusable 1-row batch.** `EV/eval.go` (2248 lines, row-at-a-time scalar evaluator) and `EV/eval_vec.go` (3537 lines, vectorized) coexist; eval_vec falls back to scalar in only 2 places (eval_vec.go:1128/2338), yet eval.go's scalar `Eval`/`EvalValue` API remains the surface for 29 consumer files across PX/EX/AG/WT/OP. Fix: (1) make the vectorized evaluator the sole entry — every call site invokes `EvalBatch` over a shared `sync.Pool` 1-row batch, so the scalar branch is exercised through the vectorized front door; (2) delete `eval.go`'s public entry points and move the 2 scalar fallback kernels into `eval_vec_funcs.go` as private helpers; (3) keep behavior identical — no expression semantics change. This is the execution-side companion to REQ002271 (which targets `eval.go` deletion after WT migration); this REQ completes it for the remaining PX/AG callers. Verify: `grep -rn 'EV\.Eval(\|EV\.EvalValue(' internal/` returns zero production hits; all 29 consumer files call the vectorized front door; `go test ./internal/SQB/EV/... ./internal/SQB/PX/... ./internal/SQB/AG/... -race -count=1` passes; SLT select1-select4 pass. | medium | large | REQ002267, REQ002271 | SQB/EV/eval.go, SQB/EV/eval_vec.go, SQB/EV/eval_vec_funcs.go, SQB/PX/ (stage_*.go), SQB/AG/aggregate.go, SQB/WT/ |
| REQ002290 | SQB/EV | **Delete `eval.go` entirely once all scalar entry points are unreachable.** REQ002289 routes every caller through the vectorized front door; this REQ is the final deletion. Fix: delete `SQB/EV/eval.go`; move any still-reachable scalar-only helper functions (e.g. `EvalInHash`, `EvalFunction` used by the 1-row fallback) into `eval_vec_funcs.go` or `scalar_funcs.go`; ensure `grep -r 'eval.go' internal/` shows no import. Verify: `grep -rln 'SQB/EV/eval' internal/` returns zero; `go build ./...` passes; `go test ./internal/SQB/EV/... -race -count=1` passes; vectorized SLT corpus identical. | medium | small | REQ002289 | SQB/EV/eval.go, SQB/EV/eval_vec_funcs.go, SQB/EV/scalar_funcs.go |
| REQ002291 | driver + SQB/EX | **Unify parameter binding — single `ParamBinder` shared by driver and engine.** `driver/stmt.go` (`Stmt.Exec`/`Stmt.Query`, converting `[]driver.Value` to engine args) and `driver/conn.go` (`Conn.ExecContext` with `[]driver.NamedValue`) each convert/bind parameters, and the engine's PX stages re-carry `params []any` through every stage (stage.go, stage_filter.go, stage_compound.go, stage_window.go, stage_dml.go). The conversion + placement logic is duplicated. Fix: extract a `ParamBinder` (in `driver/` or a shared `SQB/EX` helper) that (1) converts `[]driver.Value`/`[]driver.NamedValue` → `[]any` with SQL type coercion, (2) validates placeholder count against `NumInput`, (3) is called from both `Stmt.Exec`/`Stmt.Query` and `Conn.ExecContext`; engine stages consume the already-bound slice without re-converting. Verify: driver behavior on parameterized statements unchanged (SLT param cases); `go test ./driver/... ./internal/SQB/EX/... -race -count=1` passes. | medium | medium |  | driver/stmt.go, driver/conn.go, SQB/PX/stage.go, SQB/PX/executor.go |
| REQ002292 | SQB/PX + driver | **Unify result materialization — one rows→`[]DT.Row` path used by `QueryAll`, driver `Rows`, and `EXPLAIN ANALYZE` counters.** Row materialization is implemented in overlapping places: `EX.QueryAll` (ex.go:1081) builds `[]DT.Row`, `driver/rows.go` wraps a row iterator, and REQ002279's per-stage counters need a shared row-visitor. Fix: define a single `RowSink` interface in `SQB/PX` (`Write(UT.Batch)`, `Close()`); `QueryAll` uses a `SliceSink`, the driver uses a `DriverSink` that streams to `database/sql` rows, and EXPLAIN ANALYZE wraps the sink to count rows. Delete the bespoke materialization loops in ex.go. Verify: `grep -rn 'func.*QueryAll' internal/SQB/EX/` shows QueryAll delegating to the sink; `go test ./driver/... ./internal/SQB/PX/... ./internal/SQB/EX/... -race -count=1` passes; driver query results identical on SLT. | medium | medium | REQ002279, REQ002272 | SQB/PX/ (new: sink.go), driver/rows.go, SQB/EX/ex.go |
| REQ002293 | SQF/PS + SQB/EX + SQB/PX + SYS | **Centralized SET/PRAGMA configuration registry — every engine knob is one registered entry, not scattered parsing.** SET/PRAGMA statements parse in `SQF/PS` (ps.go:424/452, ddl.go:436/684, transaction.go:80) and dispatch to ad-hoc handlers in `SQB/EX/ex.go`/`planner.go` and DBG packages. Future knobs (memory_limit REQ002283, parallel_degree REQ002278, table_stats_version REQ002284, tracer verbosity) would each add a new scattered parse→dispatch site. Fix: add a `Config` registry (in `SYS/`) with `Register(name, SetFunc, GetFunc, Scope)`; PS parses SET/PRAGMA into a generic `name = value` pair; the executor looks up the registered handler; unknown names return a clear error. Migrate existing knobs (memory_limit, parallel_degree, debug_* tracers) onto the registry. Verify: a test registers a dummy knob, sets it via SQL, reads it back via PRAGMA; existing `PRAGMA debug_*` behavior identical; `go test ./internal/SQF/PS/... ./internal/SQB/EX/... ./internal/DBG/... -race -count=1` passes; SLT pass. | medium | medium | REQ002283, REQ002278 | SYS/ (new: config.go), SQF/PS/ps.go, SQF/PS/ddl.go, SQB/EX/ex.go, DBG/DC/control.go |
| REQ002294 | SQB/EX + driver | **Frozen prepare/compile API — `CompilePlan`/`ExecCompiled` become the single statement path for the driver.** `driver/conn.go:17` `Prepare` → `Prepare(c, query)` and `driver/stmt.go` `Exec`/`Query` currently re-parse+re-plan per call, while `EX.CompilePlan`/`ExecCompiled` (ex.go:1267/1329) offer a compiled-once path that the driver does not use. Fix: (1) define the compile contract — `CompilePlan(sql) (*CompiledPlan, error)` is the sole entry to parsing+planning; `ExecCompiled(ctx, cp, args)` is the sole execution entry; (2) `Stmt.Exec`/`Stmt.Query` call `CompilePlan` once at prepare time and `ExecCompiled` per call (reusing the plan, re-binding params via REQ002291); (3) `Conn.ExecContext`/`Conn.QueryContext` for one-off statements inline compile+exec but through the same two functions. Verify: prepared-statement behavior identical on SLT; `BenchmarkRazordata_Update` (which uses the driver) shows no regression; `go test ./driver/... ./internal/SQB/EX/... -race -count=1` passes. | medium | medium | REQ002291, REQ002230 | driver/conn.go, driver/stmt.go, SQB/EX/ex.go, SQB/EX/planner.go |
| REQ002295 | SQB/EX + SQB/PX | **Unify query entry points — `Exec`/`Query`/`QueryAll`/`QueryStream` delegate to one internal `run` producing a `Result`.** ex.go currently has 4+ near-parallel entry methods (Exec:835, Query:984, QueryAll:1081, QueryStream) each with its own Build→execute→materialize wiring and panic-fallback scaffolding. REQ002272 collapses Exec/Query/QueryAll but leaves QueryStream separate. Fix: after REQ002272, route QueryStream through the same internal `run` with a streaming `RowSink` (REQ002292); the four public methods become thin wrappers selecting the sink (slice vs stream) and return shape (Result vs *Rows). Verify: `QueryStream` returns rows incrementally (first row before full materialization, asserted with a slow source); `go test ./internal/SQB/EX/... -race -count=1` passes; SLT select1-select4 pass. | low | medium | REQ002272, REQ002292 | SQB/EX/ex.go, SQB/EX/stream.go |
| REQ002296 | SQO/PF + SQB/EX + SQF/QP | **Single constant-folding implementation — absorb EX's inline `fold_cse.go` into the QP `ConstantFoldingPass`.** Constant folding is implemented twice: `SQO/PF/constant_folding.go` (`ConstantFoldingPass`, migrated to QP by REQ002258) and `SQB/EX/fold_cse.go` (400 lines: `foldConstants` at :130, plus CSE helpers `eliminateCommonSubexpressions` :364, `isConstantExpr` :49, `exprHash` :282), called inline from `planner_select.go:613` (WHERE) and `:911` (join ON). Both rewrite `PS.Expr` trees but live in different phases — one in the pass pipeline, one baked into predicate planning. Fix: while migrating ConstantFoldingPass to QueryPlan (REQ002258), absorb `fold_cse.go`'s `foldConstants` + CSE logic into the QP pass (or a shared `SQF/RE` helper both call); delete `fold_cse.go`; `planner_select.go:613/911` rely on the pass instead of inline folding. Verify: `grep -rln 'fold_cse\|foldConstants' internal/` returns zero production hits; constant-folding behavior identical (SLT queries with constant WHERE/ON clauses); `go test ./internal/SQF/QP/... ./internal/SQB/EX/... -race -count=1` passes. | medium | medium | REQ002258 | SQO/PF/constant_folding.go, SQB/EX/fold_cse.go, SQB/EX/planner_select.go, SQF/QP/ (pass_constant_folding.go) |
| REQ002297 | SQB/WT + SQB/DT + SQB/EX | **Single constraint-checking entry — one `ConstraintChecker` dispatched by (constraint type, operation).** Constraint enforcement lives primarily in `SQB/WT/constraints.go` (`ValidateCheck` :259, `CheckUnique` :333, `EncodeUniqueKey` :434, `InMemoryLookup` :549) but is invoked from scattered write paths (`writers_dml.go:1109` unique check with self-exclusion, `DT/arena.go` row encode), and DML bypasses some checks depending on path. Fix: define a `ConstraintChecker` in `SQB/WT` exposing one entry `Enforce(schema, op, row, pending, lookup)` where `op ∈ {INSERT, UPDATE, DELETE}` dispatches to the check functions; the write path (REQ002267 DML-in-QueryPlan) calls it exactly once per row; FK/check/unique always run regardless of DML path. Verify: `grep -rn 'CheckUnique\|ValidateCheck' internal/` shows only the checker as caller; FK/unique violations reported identically on SLT insert/update/delete cases; `go test ./internal/SQB/WT/... ./internal/SQB/DT/... -race -count=1` passes. | medium | medium | REQ002267 | SQB/WT/constraints.go, SQB/WT/writers_dml.go, SQB/DT/arena.go, SQB/PX/stage_dml.go |
| REQ002298 | SQB/AG + SQB/EV + SYS | **Aggregate execution joins the unified `Engine.Register` — window + aggregate funcs register through one extension point.** REQ002286 unifies node/function/aggregate registration, but `SQB/AG`'s aggregate dispatch still uses its own `aggregate_registry.go` (219 lines, `registerAggregateFunc` :28 with built-ins `COUNT/SUM/AVG/MIN/MAX/GROUP_CONCAT/STRING_AGG`) and `window.go` (873 lines) carries a parallel window-function dispatch. Fix: route aggregate + window function registration through `Engine.Register` (REQ002286's `Extension` covering aggregates); `aggregate_registry.go` becomes a thin adapter or is deleted; window built-ins register the same way. Verify: a test registers a custom aggregate via `Engine.Register` and uses it in a `GROUP BY` query; built-in aggregates behave identically on SLT; `go test ./internal/SQB/AG/... ./internal/SYS/... -race -count=1` passes. | medium | medium | REQ002286 | SQB/AG/aggregate_registry.go, SQB/AG/aggregate.go, SQB/AG/window.go, SYS/ (engine entry) |
| REQ002299 | SQB/EX + SQB/WT + TXN | **Single transaction-state machine — BEGIN/COMMIT/ROLLBACK and write-hook routing through one `TxController`.** Transaction handling is split: `driver/conn.go:56 Begin` → `driver.Tx`, `SQB/EX` exposes `SetTxWriter`/`TxWriter` hooks (ex.go:252-262) that notify `DT` on every key write, `DT/tx_state.go` holds global knob state (busy timeout, FK switch, WAL autocheckpoint), and TXN/LC + TXN/VL own the low-level protocol. Fix: define a `TxController` in `SQB/EX` (or TXN) that owns the statement-level state machine (BEGIN/COMMIT/ROLLBACK + autocommit), the tx-writer notification, and the connection-level knobs from `tx_state.go`; driver `Begin`/`Commit`/`Rollback` and engine `Begin`-statement handling both delegate to it; `tx_state.go`'s globals move under the controller (per-connection or per-engine scope). Verify: driver tx behavior identical on SLT tx cases; `SetTxWriter` notifications unchanged; `go test ./driver/... ./internal/SQB/EX/... ./internal/SQB/WT/... -race -count=1` passes. | medium | medium | REQ002291 | driver/conn.go, SQB/EX/ex.go, SQB/EX/tx.go (new), SQB/DT/tx_state.go, TXN/ |
