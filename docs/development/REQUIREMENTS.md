## TBD

| ID | Subsystem | Requirement | Priority | Effort | Deps | Touches |
| --- | --- | --- | --- | --- | --- | --- |
| REQ000543 | SQL/EX | Shape-specialized fast paths — extend REQ000311/313 with shape-driven specialization that captures hot path inline as small specialized Go funcs (not full JIT); specialize per plan shape: `Filter{int64_col_lit_eq}`, `Project{cols=N}`, `HashAggregate{group_by_int64}`, etc.; registered in `codegen_ops.go`; benchmarks must show >1.5x over the interpreted path. Deferred: XL effort, iter-29. | medium | XL | REQ000311 (codegen framework), REQ000313 (ADQC) | `SQL/EX/codegen/shapes/` (new); `SQL/EX/adqc_specialize.go` (new); `SQL/EX/shape_bench_test.go` (new) |
| REQ000546 | SQL/EX | Vectorized string columns via unsafe pointer + length prefix — replace `[]string` columns with `[]byte` raw + `[]uint32` lengths (or `[]struct{Ptr uintptr; Len uint32}`); eliminates string-header copy and enables real SIMD string compare (memcmp); backward-compat path via `Batch.StringValue(col,row)` accessor. Deferred: L effort, iter-29. | medium | L | REQ000149 (batch memory), REQ000310 (SIMD) | `SQL/EX/batch.go` `Column` for text/varchar/blob; `SQL/EX/eval_vec.go` `compareStringColsVec`; `SQL/EX/simd_dispatch.go` runtime detection |
| REQ000549 | MEM/BF | Block-level MVCC version tagging — buffer pool slots carry an optional `versionRange [startTS, endTS]` field; `BufferPool.GetWithTS(blockID, readTS)` returns the page whose version range covers `readTS` (or the page whose range is the latest, then we filter via the version chain at read time); enables cross-transaction cache sharing with snapshot consistency. Deferred: XL effort, iter-29. | low | XL | iter-02 (buffer pool), REQ000539 (sharded), REQ000307 (MV-OCC) | `MEM/BF/mvcc_slot.go` (new) + `bf.go` `GetWithTS`; `TXN/SN/snapshot.go` registers `readTS` on Get |
| REQ000556 | SQL/PS+EX | `CREATE VIRTUAL TABLE ... USING module_name` — BNF line 23; no T_VIRTUAL token, no parser path, no executor. SQLite-compatible modules (fts5, rtree) cannot be registered. Add lexer tokens (`T_VIRTUAL`, `T_USING` already exists for JOIN), parser path (`parseCreateVirtualTable` + `VirtualTableStmt` AST), executor module-dispatch loop. Deferred: XL effort, iter-29. | high | XL | iter-08 (catalog), REQ000250 (BTree) | `SQL/LX/lx.go`+`token.go` (T_VIRTUAL); `SQL/PS/ps.go` `parseCreateVirtualTable` + `SQL/PS/ast.go` `VirtualTableStmt`; `SQL/EX/ex.go` `buildWriterOp` route; new `SQL/EX/virtual.go` registry |
| REQ000641 | SQL/EX | **In-memory tables (`:memory:` mode) do not support transaction rollback** — `TestDS_Transaction` (internal/SYS/DS/ds_test.go:99) fails because the in-memory Insert path at `writers.go:95-205` writes directly to the package-level `tables` map without going through any TxWriter hook. When TX.Transaction.Rollback runs, it has no record of writes made via this path, so the rolled-back rows remain in memory. The engine-backed path (`nextFromStore`) does support rollback (via TX.Writer → RecordWrite → rollback restoration), but the in-memory path does not. Fix: have the in-memory Insert path also call `CurrentTxWriter().RecordWrite(tableKey, encodedRow)` before appending to `tables[table]`. Same applies to Update/Delete. This is a SQL semantics bug: ROLLBACK must restore pre-tx state regardless of storage backend | high | L | iter-08 (SQL execute) | `SQL/EX/writers.go` in-memory Insert/Update/Delete paths; see test failure on iter-29 run |
| REQ000642 | ALL | **`interface{}` → `any` migration** — Replace all 421 occurrences of `interface{}` with `any` across the codebase. Go 1.22+ project should use the modern type alias. Bulk replacement via `gofmt -s` or `sed`; verify with `go vet ./...` and `go test ./... -race -count=1` | medium | M | none | all `*.go` files under `internal/` |
| REQ000643 | ALL | **Remove `Get` prefix from getters** — Rename 18 getter methods to follow Google Go Style Guide convention (function names should not use `Get` prefix unless the underlying concept uses "get"). Affected: `VersionChain.GetHead` → `Head`, `MV.GetVersionChain` → `VersionChain`, `MV.GetOrCreateVersionChain` → `EnsureVersionChain`, `Executor.GetSnapshot` → `Snapshot`, `Executor.GetSessionID` → `SessionID`, `Rows.GetCols` → `Cols`, `Rows.GetTypes` → `Types`, `sessionStateAccessor.GetChangesCount` → `ChangesCount`, `GetLastInsertRowID` → `LastInsertRowID`, `GetTotalChangesCount` → `TotalChangesCount`, `engine.GetStats` → `Stats`, `tableRegistry.Get` → `Lookup`, `catalog.GetTable` → `Table`, `GetTableByName` → `TableByName`, `Catalog.GetIndex` → `Index`, `GetByName` → `ByName`, `slotManager.GetSlot*` → `Slot*`, `SegmentManager.GetSegment` → `Segment`, `shardedBufferPool.GetSlot` → `Slot`. Update `SessionCounterAccessor` interface and all call sites | medium | M | none | `TXN/MV/version.go`, `TXN/MV/mv.go`, `SQL/EX/ex.go`, `SYS/AP/ap.go`, `SYS/SE/se.go`, `ENG/LS/engine.go`, `ENG/LS/table.go`, `ENG/LS/catalog.go`, `TXN/VL/slot.go`, `FIL/LF/lf.go`, `MEM/BF/sharded_bp.go` |
| REQ000644 | SQL/PS | **Add doc comments to AST types** — All exported types in `ast.go` need doc comments beginning with the type name. Includes `Expr` and `Stmt` interfaces, all 24 expression types (`NumberLiteral`, `FloatLiteral`, `StringLiteral`, `BoolLiteral`, `NullLiteral`, `Ident`, `QualifiedName`, `AliasedExpr`, `CastExpr`, `Param`, `BinaryExpr`, `UnaryExpr`, `FunctionCall`, `AggregateFunc`, `WindowFunc`, `StarExpr`, `ListExpr`, `BetweenExpr`, `CaseExpr`, `InExpr`, `ExistsExpr`, `SubqueryExpr`, `IntervalLiteral`, `RaiseFunc`), all statement types (`CreateTable`, `DropTable`, `Insert`, `Update`, `Delete`, `Select`, `CompoundStmt`, `BeginTX`, `CommitTX`, `RollbackTX`, `ExplainStmt`, `CreateIndexStmt`, `DropIndexStmt`, `CreateViewStmt`, `AlterTableStmt`, `PragmaStmt`, `AnalyzeStmt`, `VacuumStmt`, `TriggerStmt`, `SavepointStmt`, `ReleaseSavepointStmt`, `RollbackToStmt`, `WithStmt`, `TruncateStmt`, `ReindexStmt`, `DropViewStmt`, `DropTriggerStmt`, `CreateMatViewStmt`, `DropMatViewStmt`, `RefreshMatViewStmt`, `SetTransactionStmt`, `ValuesStmt`, `AttachStmt`, `DetachStmt`), helper types (`ColDef`, `Pair`, `OrderItem`, `JoinClause`, `WhenClause`, `WindowSpec`, `WindowFrame`, `FrameBound`, `ForeignKeyConstraint`, `UniqueKey`, `IndexedColumn`, `OnConflict`, `ConflictAction`, `CompoundOp`, `ExplainMode`), `Parser`, `NewParser()`, `ErrSyntax`, `SyntaxError` | medium | S | none | `SQL/PS/ast.go`, `SQL/PS/ps.go`, `SQL/PS/error.go` |
| REQ000645 | TXN/MV | **Add doc comments to MV types** — `MV` struct, `NewMV()`, and all exported methods (`GetVersionChain`, `GetOrCreateVersionChain`, `Insert`, `FindVisible`, `Commit`); `VersionChain` struct and methods (`GetHead`, `Insert`, `Commit`, `FindVisible`); `VersionNode` methods (`IsUncommitted`, `IsVisible`, `TxnID`, `BeginTS`, `EndTS`, `Key`, `Value`, `Deleted`, `Next`, `Commit`); `Arena` struct and methods (`NewArena`, `Alloc`, `Remaining`, `YoungRemaining`, `OldRemaining`, `Size`, `YoungSize`, `OldSize`); `GetArena`, `PutArena`, `ReclaimOldGenerations` | medium | S | none | `TXN/MV/mv.go`, `TXN/MV/version.go`, `TXN/MV/arena.go` |
| REQ000646 | ENG/LS | **Add doc comments to LS types** — `ReadStats` struct, `Version` struct, `SSTFileMeta` struct, `ManifestCheckpoint` struct, `RangeIter` interface, `Options` struct, `DefaultOptions()`, `Engine` methods (`Insert`, `Get`, `Delete`, `NewIterator`, `Sync`, `Close`, `Stats`, `ManualCompact`, `SetCompactionStyle`, `SetRateLimiter`, `Compaction`, `Flush`, `Catalog`), `PageCache` struct and methods (`NewPageCache`, `Get`, `Put`, `Invalidate`, `Len`), `PageCache` exported constants (`DefaultPageCacheSize`, `PageSize`) | medium | S | none | `ENG/LS/engine.go`, `ENG/LS/api.go`, `ENG/LS/manifest.go`, `ENG/LS/page_cache.go`, `ENG/LS/stats.go` |
| REQ000647 | ALL | **Remove dead code** — `ENG/LS/compaction.go:128-129` (`_ = lastKey` / `_ = lastVal` — assigned in loop but never read, either use for dedup or remove); `ENG/LS/manifest.go:148` (`_ = i` in `for i, level := range v.levels` — change to `for _, level := range v.levels`); audit all `_ =` suppressions across codebase for similar patterns | low | S | none | `ENG/LS/compaction.go`, `ENG/LS/manifest.go` |
| REQ000648 | ENG/LS | **Log discarded errors in compaction cleanup** — `compaction.go` lines 171, 173, 206, 211 call `os.Remove` without handling the error. On failure, stale SST files or symlinks remain silently. Add `log.Warn` on failure for at least the non-loop paths; for the loop path, collect first error and log after loop | low | S | none | `ENG/LS/compaction.go` |
| REQ000649 | ENG/LS | **Table-driven tests for skiplist** — Consolidate `TestInsertSingle`, `TestInsertMultiple`, `TestFindNonExistent`, `TestInsertDuplicateKey` into a single `TestSkipList` with `t.Run` subtests and a test-case struct table. Follow Google style: table-driven tests are preferred when tests differ only in inputs/expected outputs | low | S | none | `ENG/LS/skiplist_test.go` |
| REQ000650 | TXN/MV | **Table-driven tests for MV** — Consolidate `TestMVGetVersionChain`, `TestMVGetOrCreateVersionChain`, `TestMVInsert`, `TestMVFindVisible`, `TestMVMultipleKeys` into a single `TestMV` with `t.Run` subtests. Share setup via a helper function | low | S | none | `TXN/MV/mv_test.go` |
| REQ000659 | WAL | **Extract shared `atomicBool` to `WAL/walutil`** — `WAL/FL/atomic_bool.go`, `WAL/RP/atomic_bool.go`, `WAL/WR/atomic_bool.go` contain identical `atomicBool` struct (FL even acknowledges duplication in its comment). Extract to `WAL/walutil/atomic_bool.go` so all three import from one place. Alternatively, replace with `sync/atomic.Bool` + `CompareAndSwap` wrapper since project requires Go 1.22+. The custom `atomicBool.set()` returns `bool` (CAS success) which `atomic.Bool.CompareAndSwap` also provides. | medium | S | none | `WAL/walutil/atomic_bool.go` (new, or delete all 3 and use `sync/atomic.Bool`); `WAL/FL/fl.go`, `WAL/RP/rp.go`, `WAL/WR/wr.go` — import from walutil or use `atomic.Bool`; delete `FL/atomic_bool.go`, `RP/atomic_bool.go`, `WR/atomic_bool.go` |
| REQ000660 | ENG/LS+TB | **Extract shared catalog to `ENG/catalog`** — `ENG/TB/catalog.go` (459 lines) and `ENG/LS/catalog.go` (973 lines) share identical: `schemaVersionV1/V2/Current`, `catalogMagic` (`'R','C','A','T'`), `catalogHeaderSize` (17), 5 error sentinels (`ErrCatalogCorrupt`, `ErrUpgradeRequired`, `ErrCatalogNotFound`, `ErrCatalogExists`, `ErrCatalogClosed`), `Catalog` struct layout, `NewCatalog()` constructor, `bootstrap()` logic, and core `Drop()`/`Get()`/`GetByName()` methods. Extract shared catalog to `ENG/catalog/catalog.go`. TB imports from it directly. LS imports from it and adds index/stats extensions on top. Eliminates ~400 lines of duplication and prevents silent divergence when one is updated but not the other. Depends on REQ000653 (shared error sentinels). | high | L | REQ000653 | new `ENG/catalog/catalog.go` — shared `Catalog` struct, `NewCatalog`, `bootstrap`, `Drop`, `Get`, `GetByName`, `Put`, `List`, encode/decode; `ENG/catalog/errors.go` — shared sentinel definitions; `ENG/TB/catalog.go` — delete, import from `ENG/catalog`; `ENG/LS/catalog.go` — delete shared portions, import from `ENG/catalog`, keep LS-specific index/stats methods; update all tests |
| REQ000661 | WAL/RP | **Reference `wr.SegSize` instead of private copy** — `rp.go:32` defines `const rpSegSize = int64(64 * 1024 * 1024)` independently of `wr.go:16` `const SegSize = int64(64 * 1024 * 1024)`. If the segment size changes, RP would silently diverge. Replace `rpSegSize` with `wr.SegSize` import. Verify no circular import (RP already imports WR for `wr.Writer` interface). | low | S | none | `WAL/RP/rp.go` line 32 — delete `rpSegSize`, replace usage with `wr.SegSize`; `WAL/RP/rp_test.go` — update any references |
| REQ000662 | MEM/SP+ENG/ID | **Reference `df.DefaultBlockSize` for block size constant** — `MEM/SP/sp.go:8` defines `const BlockSize = 4096` and `ENG/ID/id.go:14` defines `const pageSize = 4096` independently of `FIL/DF/df.go:17` `const DefaultBlockSize = 4096` and `SYS/AP/ap.go:66` `const DefaultPageSize = 4096`. Four independent definitions of the same constant. Replace `SP.BlockSize` with `df.DefaultBlockSize` reference. Replace `ID.pageSize` with `df.DefaultBlockSize` reference. Verify no circular imports (SP and ID already import DF transitively). | low | S | none | `MEM/SP/sp.go` line 8 — replace with `df.DefaultBlockSize` import; `ENG/ID/id.go` line 14 — replace with `df.DefaultBlockSize` import; `MEM/SP/sp_test.go`, `ENG/ID/id_test.go` — update references |
| REQ000663 | ALL | **Standardize `closed` guard on `atomic.Bool`** — 6 packages use plain `bool` for closed guards which is not goroutine-safe: `ENG/TB/catalog.go:234`, `ENG/LS/catalog.go:80`, `ENG/ID/id.go:34`, `SYS/ST/st.go:23`, `SYS/AP/ap.go:194`, `LOG/HK/hook.go:33`. Convert all to `atomic.Bool`. This aligns with the 10+ packages already using `atomic.Bool` and eliminates the goroutine-safety violation. For each: change `closed bool` to `closed atomic.Bool`, change `if e.closed` to `if e.closed.Load()`, change `e.closed = true` to `e.closed.Store(true)`. | medium | M | none | `ENG/TB/catalog.go`, `ENG/LS/catalog.go`, `ENG/ID/id.go`, `SYS/ST/st.go`, `SYS/AP/ap.go`, `LOG/HK/hook.go` — convert `closed bool` to `atomic.Bool`; all `*_test.go` in these packages — verify no regressions |
| REQ000664 | SYS/AP | **Embed subsystem stats types instead of re-declaring** — `AP.LSMTreeStats` (ap.go:284) re-declares `MemtableHits/SSTHits/DiskReads` which are identical to `ENG/LS.ReadStats` fields. `AP.BufferPoolStats` (ap.go:291) re-declares `Hits/Misses/Evictions` which are a subset of `MEM/BF.BufferStats` fields. `AP.WALStats` (ap.go:298) re-declares fields identical to `WAL/RP.Stats`. `AP.TxnStats` (ap.go:321) re-declares `Active/Committed/Aborted` which are a subset of `TXN/VL.TxnStats`. Replace re-declarations with embedded types: `LSMTreeStats struct { ls.ReadStats }`, `BufferPoolStats struct { bf.BufferStats }`, etc. This prevents field drift when subsystem stats evolve. | medium | M | none | `SYS/AP/ap.go` — embed subsystem stats types in `EngineStats` sub-structs; `SYS/SY/sy.go` — update `Stats()` aggregation to use embedded fields; `SYS/AP/ap_test.go` — update field references |
| REQ000665 | SYS/AP+ENG/LS | **Centralize default constants** — `ENG/LS/engine.go:58` hardcodes `MemTableSize: 64 * 1024 * 1024` instead of referencing `AP.DefaultMemTableSize`. `ENG/LS/engine.go:56` hardcodes `MemTableShards: 1`. Audit all subsystem `Options` defaults and replace with `AP.Default*` references. Applies to: `ENG/LS.DefaultOptions()`, `MEM/BF` options, `WAL/WR` options, `LOG/LG` options. Each subsystem's default function should reference `AP.Default*` constants so there is a single source of truth for tunable defaults. | low | S | none | `ENG/LS/engine.go` `DefaultOptions()` — reference `AP.Default*` constants; `MEM/BF/bf.go` — reference `AP.Default*` for buffer pool defaults; `WAL/WR/wr.go` — reference `AP.Default*` for WAL defaults |
| REQ000666 | ENG/LS | **Atomic stats counters in engine** — `engine.go:192` `Read()` takes `statsMu.Lock()` (write lock) to increment `stats.MemtableHits`, serializing all concurrent reads on the hot path. Replace `ReadStats` struct fields with `atomic.Int64` counters: `MemtableHits`, `SSTHits`, `DiskReads`. `Read()` calls `e.stats.MemtableHits.Add(1)` without lock. `Stats()` loads all three atomically. Eliminates lock contention on the primary read path. | high | S | none | `ENG/LS/engine.go` — replace `statsMu sync.RWMutex` + `stats ReadStats` with `stats struct { MemtableHits, SSTHits, DiskReads atomic.Int64 }`; `Read()` line 192 — remove lock, use `Add(1)`; `Stats()` — load atomically; `ENG/LS/engine_test.go` — verify stats still correct under concurrent reads |
| REQ000667 | ENG/LS | **Remove duplicate `requestFlush` loop** — `engine.go:152-164` `flushActiveMemtable` iterates `frozen.shards()` twice: first loop appends shards to `e.memtables` and calls `e.fm.requestFlush(shard)`, second loop calls `e.fm.requestFlush(shard)` again. Each shard's flush is requested twice, causing duplicate flush jobs. Remove the second loop entirely. | high | S | none | `ENG/LS/engine.go` lines 152-164 — delete the second `for _, shard := range frozen.shards()` loop; `ENG/LS/flush_compaction_test.go` — verify flush count is correct |
| REQ000668 | TXN/LC | **Set `runtime_GoschedFn` default to `runtime.Gosched`** — `epoch.go:147` defines `var runtime_GoschedFn = func() {}` (no-op). `WaitForDrain` calls this in a spin loop, burning CPU without yielding to the Go scheduler. Change default to `runtime.Gosched` so the spin loop yields. The variable exists for test override; the production default must yield. | high | S | none | `TXN/LC/epoch.go` line 147 — change `func() {}` to `runtime.Gosched`; `TXN/LC/epoch_test.go` — verify `WaitForDrain` doesn't burn CPU |
| REQ000669 | TXN/SN | **Copy snapshot slice before searching in `ReadView.Get`** — `snapshot.go:46-63` holds `mu.Lock()` during version chain traversal, blocking concurrent `addSnapshot` calls for the entire duration of the search. Fix: copy `rv.snapshot` slice under lock, release lock, then search the copy. Also replace `string(snap.Key) == string(key)` at line 48 with `bytes.Equal(snap.Key, key)` to eliminate 2 string allocations per comparison on the hot read path. | high | S | none | `TXN/SN/snapshot.go` `Get()` — copy slice under lock, release, search copy; line 48 — replace `string()` comparison with `bytes.Equal`; `TXN/SN/snapshot_test.go` — verify concurrent Get/addSnapshot doesn't deadlock |
| REQ000670 | ENG/LS | **Replace `string(key)` comparisons with `bytes.Equal` in index** — `index.go:129` (`Find`) and `index.go:143` (`Delete`) convert `[]byte` keys to `string` for comparison: `string(curr.key) == string(key)`. Each conversion allocates a new string on the heap. Use `bytes.Equal(curr.key, key)` instead, consistent with the B-tree implementation in `id.go`. Eliminates 2 allocations per index lookup. | medium | S | none | `ENG/LS/index.go` lines 129, 143 — replace `string(curr.key) == string(key)` with `bytes.Equal(curr.key, key)`; add `"bytes"` import; `ENG/LS/index_test.go` — verify correctness |
| REQ000671 | ENG/DP | **Pre-allocate buffers in `EncodeBlock`/`DecodeBlock`/`EncodeRow`** — `dp.go:138` `EncodeBlock` uses `var buf []byte` without capacity, growing via `append` in a loop over all KVs. Total size estimable from sum of key/value lengths + varint overhead + restart array. `dp.go:192` `DecodeBlock` uses `var kvs []KV` without capacity; `numRestarts` gives a lower bound. `dp.go:32` `EncodeRow` uses `var buf []byte` without capacity; max size computable from schema. Also `dp.go:164` allocates `make([]byte, 4)` just to write a uint32 — use stack-allocated `[4]byte` instead. | medium | S | none | `ENG/DP/dp.go` line 138 — pre-calculate `totalSize`, `buf := make([]byte, 0, totalSize)`; line 192 — `kvs := make([]KV, 0, numRestarts)`; line 32 — pre-allocate from schema; line 164 — `var numBuf [4]byte`; `ENG/DP/dp_test.go` — verify round-trip still correct |
| REQ000672 | WAL/WR | **Pool LZ4 hash table** — `lz4/lz4.go:90` `compressBlock` allocates `make([]int, hashSize)` (16 KB = 4096 * 4 bytes) on every call and initializes with `for i := range table { table[i] = -1 }`. This is on the hot WAL write path. Use `sync.Pool` for the hash table: `Get` from pool, reset to -1, `Put` back after compression. Alternatively allocate once in the `Compress` function and pass through to `compressBlock`. | medium | S | none | `WAL/WR/lz4/lz4.go` — add `var hashTablePool = sync.Pool{New: func() any { t := make([]int, hashSize); return &t }}`; `compressBlock` — get from pool, defer put; `WAL/WR/lz4/lz4_test.go` — verify compression correctness; benchmark before/after |
| REQ000673 | FIL/DF | **Remove unnecessary buffer zeroing in `returnTempBuf`** — `df.go:323-327` zeros 4096 bytes on every buffer return to pool: `for i := range b { b[i] = 0 }`. The buffer is always fully overwritten before next use via `copy` (in `WriteBlock`) or `Pread` (in `ReadBlock`). The zeroing is unnecessary overhead on every I/O operation. Remove the loop; just `tempBufPool.Put(&b)`. | medium | S | none | `FIL/DF/df.go` lines 323-327 — remove zeroing loop; `FIL/DF/df_test.go` — verify no data leakage between blocks (concurrent read/write test) |
| REQ000674 | FIL/MF | **Eliminate double-encoding in `writeMeta`** — `mf.go:161-175` `writeMeta` calls `p.MarshalBinary()` (encodes all fields into `data`), then re-encodes the same fields into `raw` via `binary.LittleEndian.PutUint32` calls to insert the checksum at the right offset. This is redundant encoding work. Fix: have `MarshalBinary` return a buffer with a placeholder checksum, then `writeMeta` computes CRC over the buffer and overwrites the last 4 bytes. Or compute checksum from `data` directly. | medium | S | none | `FIL/MF/mf.go` — refactor `writeMeta` to use `MarshalBinary` output directly; compute CRC from `data[columnarHeaderSize:]` and overwrite checksum bytes; `FIL/MF/mf_test.go` — verify round-trip |
| REQ000675 | FIL/LF | **Remove TOCTOU `os.Stat` before `unix.Open`** — `lf.go:88-101` calls `os.Stat(path)` to check if file exists and `info.IsDir()`, then calls `unix.Open(path, O_RDWR, 0)`. The file could change between `Stat` and `Open` (TOCTOU race). The `IsDir()` check is redundant — `unix.Open` on a directory with `O_RDWR` returns `EISDIR`. Remove `os.Stat` call entirely; just `unix.Open` and handle the error (ENOENT, EISDIR, etc.). | medium | S | none | `FIL/LF/lf.go` lines 88-101 — remove `os.Stat`, just `unix.Open` and handle errors; `FIL/LF/lf_test.go` — verify error on missing file, error on directory |
| REQ000676 | WAL/RP | **Eliminate double file open in replay** — `rp.go:154-164` opens segment file via `r.sm.GetSegment(segNum)` which returns a `*FileHandle` with an FD, then opens the same file again via `unix.Open(fh.Path, O_RDONLY, 0)`. The first FD from `GetSegment` is never used for reading. Fix: use `fh.FD` directly for `Pread` calls, or if the handle must be read-only, open once with `O_RDONLY` and skip `GetSegment`. Eliminates one redundant `open` syscall per segment during replay. | medium | S | none | `WAL/RP/rp.go` lines 154-164 — remove `unix.Open`, use `fh.FD` for `Pread`; `WAL/RP/rp_test.go` — verify replay still works |
| REQ000677 | ENG/SC | **Remove stateless `Validator` type** — `sc.go:50-54` defines `type Validator struct{}` with zero fields. `NewValidator()` allocates a heap object that carries no state. All methods (`ValidateRow`, `ValidateType`, `ValidateConstraints`, `CompareColumnDef`) use the receiver only for dispatch. Convert to package-level functions: `sc.ValidateRow(row, schema)`. Also remove `ValidateConstraints` (lines 114-120, always returns nil) and pass-through wrappers `EncodeBigInt`/`DecodeBigInt` (lines 151-157, identical to `EncodeInt`/`DecodeInt`). Eliminates allocation per call-site and removes dead code. | medium | S | none | `ENG/SC/sc.go` — convert methods to functions, remove `Validator`/`NewValidator`, remove `ValidateConstraints`, remove `EncodeBigInt`/`DecodeBigInt`; `ENG/SC/sc_test.go` — update calls from `v.ValidateRow(...)` to `sc.ValidateRow(...)`; `ENG/LS/schema.go` — update re-exports if any |
| REQ000678 | ENG/DP | **Unify `Pair` and `KV` types** — `dp.go:17-25` defines both `type Pair struct { Key []byte; Value []byte }` and `type KV struct { Key []byte; Value []byte }`. These are structurally identical. `Pair` is used in `EncodeBlock` parameter, `KV` is returned by `DecodeBlock`. Unify into a single type (e.g., `KV`). Update `EncodeBlock` signature and all callers. | low | S | none | `ENG/DP/dp.go` — remove `Pair`, use `KV` everywhere; `ENG/DP/dp_test.go` — update; callers of `EncodeBlock` — update parameter type |
| REQ000679 | ENG/SC | **Combine redundant `CTInt`/`CTBigInt` branches** — `sc.go:84-109` `ValidateType` has separate `case CTInt` and `case CTBigInt` branches with identical logic (`if len(val) != 8 { return ErrTypeMismatch }`). Same for `CTFloat` and `CTTimestamp`. Combine into `case CTInt, CTBigInt, CTFloat, CTTimestamp: if len(val) != 8 { return ErrTypeMismatch }`. Reduces code duplication and improves readability. | low | S | none | `ENG/SC/sc.go` lines 84-109 — combine cases; `ENG/SC/sc_test.go` — verify all types still validated |
| REQ000680 | ENG/LS | **Pre-allocate `memtables` slice in engine** — `engine.go:109` uses `make([]memtableIface, 0)` which grows via `append` every time a memtable is frozen. Initial capacity of 4 would cover the typical case (1 active + up to 3 frozen). Same for `sst_writer.go:47` `make([]indexEntry, 0)` — typical SST has hundreds of entries; `make([]indexEntry, 0, 256)` eliminates early re-allocations. Also `TXN/SN/snapshot.go:27` `make([]VersionChainSnapshot, 0)` — pre-allocate based on expected read set size. | low | S | none | `ENG/LS/engine.go` line 109 — `make([]memtableIface, 0, 4)`; `ENG/LS/sst_writer.go` line 47 — `make([]indexEntry, 0, 256)`; `TXN/SN/snapshot.go` line 27 — `make([]VersionChainSnapshot, 0, 16)` |
| REQ000681 | ENG/SC | **Zero-copy `EncodeVarchar`/`EncodeText`** — `sc.go:186-199` `EncodeVarchar(v string) []byte` and `EncodeText(v string) []byte` both do `return []byte(v)` which allocates a new `[]byte` on every call. If the caller already has a `[]byte`, they must convert to `string` first, causing double allocation. Add `EncodeVarcharBytes(v []byte) []byte` that returns `v` directly (identity, zero-copy). For the `string` variant, consider `unsafe` zero-copy: `*[]byte)(unsafe.Pointer(&v))` — the codebase already uses `unsafe.Pointer` in `TXN/MV/mv.go:13` for `bytesToString`. | low | S | none | `ENG/SC/sc.go` — add `EncodeVarcharBytes`/`EncodeTextBytes` functions; update callers that have `[]byte` to use the new functions; `ENG/SC/sc_test.go` — add tests for new functions |
