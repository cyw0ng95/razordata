## TBD
| ID | Subsystem | Requirement | Priority | Effort | Deps | Touches |
| --- | --- | --- | --- | --- | --- | --- |
| REQ000987 | EX/store | **No `TableHandle` abstraction — raw Store used everywhere with repeated schema+key+encode pattern.** Every caller (`writers.go`, `operators.go`, `integrity.go`) independently: (1) looks up the schema via `schemaFor`, (2) builds the key prefix, (3) calls `encodeRow`, (4) calls `store.Put/Get/Delete`. This three-step pattern is duplicated across 10+ call sites. **Fix:** (1) Introduce a `TableHandle` struct that bundles `{store Store, schema *storeSchema, prefix []byte}` with methods `InsertRow(Row)`, `GetRow(key)`, `Scan(prefix) Iterator`. (2) Replace the repeated pattern at all call sites. (3) Add `TestTableHandle_InsertGetScan` verifying the abstraction. | medium | medium | none | EX/store.go, EX/writers.go, EX/operators.go |
| REQ000994 | TXN/VL | **No lock manager — OCC-only concurrency causes abort cascades on write-heavy workloads.** `TXN/VL/` has no lock table, no lock acquisition, no deadlock detection, and no wait-for graph. Concurrency control is entirely OCC-based (validate at commit). Write-heavy workloads cause abort cascades: many transactions read the same rows, all try to commit, only one succeeds, the rest abort and retry, causing livelock. Also, `SELECT ... FOR UPDATE` cannot be implemented without row-level locks. **Fix:** (1) Add a lock table (`map[key]LockState` with wait queues). (2) Implement deadlock detection (wait-for graph or timeout-based abort). (3) Add `TestLockManager_ConcurrentWrites` verifying no abort cascade under contention. | high | large | none | TXN/VL/manager.go, TXN/VL/validate.go |
| REQ000995 | TXN/VL | **No savepoint or partial rollback — compound statements cannot recover from partial failure.** `TXN/VL/protocol.go`: the `Tx` interface exposes `Commit` and `Abort` but no `Savepoint(name)` or `RollbackTo(name)`. The write set is a flat `[]KeyRange` with no ability to truncate to a prior point. Trigger bodies that call multiple DMLs, or multi-statement transactions, cannot recover from partial failure — the entire transaction must abort. **Fix:** (1) Implement savepoints by snapshotting the write-set length and reverting the MVCC chain on rollback-to-savepoint. (2) Add `TestSavepoint_PartialRollback` verifying that rolling back to a savepoint undoes only the writes after the savepoint. | high | medium | none | TXN/VL/protocol.go |
| REQ001004 | PS/ast | **No source location tracking on AST nodes — error messages cannot reference precise locations.** | high | medium | none | PS/ast.go, PS/ps.go |
| REQ001008 | LS | **No range tombstone support — `DELETE WHERE col > 100` must delete each row individually.** `LS/api.go:12-16`: deletion uses a magic tombstone value (`0xDEADBEEF`). Range deletions must delete each row individually, which is O(N) for the matching rows. A range-tombstone marker can suppress a key range during SST reads in O(1). **Fix:** (1) Add a range-tombstone record type to SST blocks. (2) Update `sstReader.Find` to check range tombstones before scanning. (3) Add `TestRangeTombstone_SuppressesRange` verifying the optimization. | medium | medium | none | LS/api.go, LS/sst_reader.go |
| REQ001011 | EX/ex | **No prepared statement cache — every execution re-parses and re-plans.** `EX/memo.go:10-12`: the only memoization is `serializeKey(stmt)` which hashes the AST. There is no cache of compiled plan trees. Every `Exec`/`Query` call re-parses the SQL, re-plans, and re-compiles. For OLTP workloads with repeated queries this is a major performance gap. **Fix:** (1) Add a plan cache (LRU, key=normalized SQL + param types). (2) Cache compiled `Operator` trees. (3) Add `TestPreparedCache_HitRate` verifying repeated queries use cached plans. | medium | medium | none | EX/memo.go, EX/ex.go |
| REQ001014 | TXN/LC | **`ReclaimPool` silently drops half its contents under pressure — memory leak.** `TXN/LC/reclaim_pool.go:33-41`: when the pool exceeds `maxPoolSize`, the first half of pointers are nilled and dropped without being freed. These pointers leak — they reference heap objects that are never freed. **Fix:** (1) Drain the dropped pointers into a deferred-free mechanism or call the epoch reclaimer before dropping. (2) Add `TestReclaimPool_NoLeakUnderPressure` verifying all pointers are eventually freed. | medium | small | none | TXN/LC/reclaim_pool.go |
| REQ001020 | EX/perf | **No hash-based LEFT/RIGHT/FULL OUTER JOIN — only NLJ with O(N*M/32) right-side scans.** `join.go:227-391`: ALL LEFT, RIGHT, and FULL OUTER JOINs fall through to NestedLoopJoin, which re-scans the right side for every left row (batch of 32). HashJoin only supports INNER JOIN. For a 10K-left × 1K-right LEFT JOIN, NLJ does 312 right-side scans. **Fix:** (1) Implement hash-based LEFT OUTER: build hash table on right side, probe with left, track unmatched right rows. (2) Reduces right-side work from O(N*M/32) to O(N+M). (3) Add `TestHashLeftOuterJoin_Basic` and `BenchmarkHashLeftOuterJoin` verifying correctness and performance. | high | large | none | EX/join.go, EX/hashjoin.go |
| REQ001045 | EX/parallel | **Store-backed parallel SeqScan — split LSM key range across workers for real data paths.** Current `ParallelSeqScan` works on `[]Row` (in-memory only). For LSM-backed tables, the key prefix range can be split into N sub-ranges, each assigned to a worker with its own `Store.Iterator`. Workers scan their sub-range concurrently, producing rows in parallel. This covers the real data path (persistent tables via `ENG/LS/`). **Fix:** (1) Add `ParallelStoreSeqScan` operator that accepts `Store`, `schema`, key prefix, and worker count. (2) Split the key prefix range into N equal sub-ranges using `store.SplitRange(prefix, N)`. (3) Each worker opens its own iterator over its sub-range. (4) Coordinator merges results (or streams directly if order doesn't matter). (5) Add `TestParallelStoreSeqScan_RangeSplit` and `BenchmarkParallelStoreSeqScan_Persistent` verifying correctness and speedup on 1M-row LSM table. | high | large | none | EX/operators_parallel.go, EX/store.go |
| REQ001047 | EX/parallel | **Parallel hash join build/probe — build right-side hash table in parallel, probe with left partitions.** `hashjoin.go` builds the hash table on the right side sequentially, then probes with left rows one at a time. For equi-joins with large right sides (100K+ rows), the build phase is a bottleneck. Parallel approach: (1) partition right side by `hash(join_key) % N`, (2) build N partial hash tables concurrently, (3) partition left side by same hash, (4) probe each left partition against its matching right partition concurrently. **Fix:** (1) Add `ParallelHashJoin` operator. (2) Use `WorkerPool` for build and probe phases. (3) Add `TestParallelHashJoin_Correctness` and `BenchmarkParallelHashJoin_LargeRight` verifying correctness and speedup. | high | large | none | EX/hashjoin.go (new parallel variant) |
| REQ001048 | EX/parallel | **Wire `SubCompactor` into the compaction loop — parallel sub-compaction exists but is orphaned.** `ENG/LS/subcompact.go` implements `SubCompactor` that splits input SSTs into key-range partitions and runs sub-merges through `errgroup` with NUMA-aware thread pinning. However, `compaction.go:compactionJob.Run()` is the only compaction path and it runs serially. The `SubCompactor` is never called. **Fix:** (1) In `compactionManager.requestCompaction()`, dispatch through `SubCompactor` when input SST count > 4. (2) Keep serial path as fallback for small compactions. (3) Add `TestSubCompactor_Integration` and `BenchmarkCompaction_Parallel` verifying correctness and speedup. | high | medium | none | ENG/LS/compaction.go, ENG/LS/subcompact.go |
| REQ001055 | EX/parallel | **NUMA-aware operator placement — detect NUMA topology and pin workers to local memory.** `ENG/NM/numa.go` detects NUMA nodes but is only used by `SubCompactor`. For query execution, workers that access the same data should be pinned to the same NUMA node to avoid cross-node memory access (2-3x latency penalty). **Fix:** (1) In `WorkerPool`, accept a `NUMATopology` parameter. (2) When partitioning work (parallel scan, parallel agg, parallel join), assign partitions to workers on the same NUMA node as the data. (3) Use `nm.BindThread(node)` to pin worker goroutines. (4) Add `TestNUMA_WorkerPlacement` verifying workers are pinned to local NUMA nodes. | low | medium | none | EX/parallel.go, ENG/NM/numa.go |
| REQ001073 | EX/plan | **No semi-join optimization — `EXISTS (SELECT ...)` materializes full subquery.** `planner.go:2344-2400`: `EXISTS (SELECT 1 FROM t WHERE t.id = outer.id)` materializes the entire subquery result. Semi-join can stop after finding the first match. **Fix:** (1) Detect `EXISTS` subqueries and convert to semi-join. (2) Semi-join stops scanning the right side after finding the first match. (3) Add `TestPlanner_SemiJoin` verifying `EXISTS` uses semi-join instead of materialization. | medium | large | none | EX/planner.go |
| REQ001077 | EX/plan | **No transitive dependency inference — `WHERE a = b AND b = c` does not imply `a = c`.** `planner.go:2344-2400`: transitive equality (`a = b AND b = c`) is not inferred to `a = c`. This prevents using an index on `c` when the query filters on `a`. **Fix:** (1) Build a transitive closure of equality predicates. (2) Add inferred predicates to the WHERE clause. (3) Add `TestPlanner_TransitiveEquality` verifying the inference. | medium | medium | none | EX/planner.go |
| REQ001078 | EX/plan | **No view merging — views are always materialized as subqueries.** `planner.go:2344-2400`: when a view is referenced, the view's SELECT is always materialized as a subquery. For simple views (single table, no aggregation), the view can be merged into the outer query. **Fix:** (1) Detect mergeable views (single table, no aggregation, no DISTINCT, no ORDER BY/LIMIT). (2) Replace the view reference with the view's SELECT, pushing outer predicates into the view. (3) Add `TestPlanner_ViewMerging` verifying the optimization. | medium | large | none | EX/planner.go |
| REQ001079 | EX/plan | **No subquery flattening — nested subqueries are not flattened.** `planner.go:2344-2400`: `SELECT * FROM (SELECT * FROM t WHERE x > 10) WHERE y < 20` creates a nested subquery. The inner and outer WHERE clauses can be merged. **Fix:** (1) Detect flattenable subqueries (single table, no aggregation, no DISTINCT). (2) Merge the subquery's WHERE clause with the outer WHERE clause. (3) Add `TestPlanner_SubqueryFlattening` verifying the optimization. | medium | medium | none | EX/planner.go |
| REQ001090 | EX/hashjoin | **HashJoin buildAndProbe allocates 19.7GB per select4 run (40% of all allocations).** `hashjoin.go:164-301` (`buildAndProbe`): per-profile, `HashJoin.buildAndProbe` is the #1 allocator. Bucket slices grow incrementally via `append`, leftRows + rightRows materialization copies every row, dataBuf pre-allocation is `totalMatches*dataPerRow` which can be huge for cross joins. **Fix (already applied then reverted):** First attempt capped `dataBuf` pre-allocation to 64K rows × dataPerRow. This caused `slice bounds out of range` panic at hashjoin.go:315 because output `Row.Data` sub-slices point INTO the dataBuf backing array — when `append` reallocates, the previously emitted slices become dangling. REVERTED to original `totalMatches*dataPerRow` pre-allocation. The 1GiB/2GiB `MaxResultRows` cap at the SLT driver level is the correct place to bound this allocation. Touches: hashjoin.go:294. | medium | medium | none | EX/hashjoin.go |
| REQ001091 | EX/project | **Project `dataBuf` is per-Operator, not pooled — 3857 allocs per select4 run.** `intermediate.go:368` (`Project.Next`): every query allocates a new `dataBuf = make([]Value, 0, 64*dataPerRow)`. For 3857 select4 queries this is 3857 buffer allocations. **Fix:** (1) Add a `sync.Pool` of `*[]Value` slices for Project dataBuf. (2) Get from pool in `Project.Next`, return in `Project.Close()`. (3) Tune pool chunk size to 64*8=512 values (matches common 8-column output). | medium | small | none | EX/intermediate.go |
| REQ001098 | EX/eval | **Correlated subquery comparison `x.c > t1.c` returns wrong results — row resolution across outer/inner scope broken.** select1/2/3 have ~200+ `result mismatch` failures on correlated subqueries like `SELECT count(*) FROM t1 AS x WHERE x.c>t1.c AND x.d<t1.d`. The subquery references both the outer row (`t1.c`) and the inner row alias (`x.c`). Root cause: `EvalValue` for `QualifiedName` does not correctly resolve `t1.c` against the outer row when the outer row has been aliased or when the inner scan has a different alias. Also `Ident` without qualifier may resolve to the wrong scope. Verified via `evalSubquery` / `evalExists` which calls `newSubqueryPlanner(outer)` — the `outer` row may carry stale alias/colIndex after NLJ column projection. **Fix:** (1) In `evalInSubquery` and `evalExists`, when building the subquery planner, ensure the outer row's `Cols` and `colIndex` correctly reflect the original table name (not the join-internal prefixed form). (2) In `EvalValue(*PS.QualifiedName)`, when the table qualifier matches the outer row's `tableName`, prefer the outer row over inner row lookups. (3) Add `TestCorrelatedSubquery_Comparison` with `x.c > t1.c` pattern. | high | medium | none | EX/eval.go |
| REQ001102 | EX/plan | **No MergeJoin operator — outer joins and sorted inputs fall back to NestedLoopJoin.** `planner.go:2100-2171`: join operator selection only produces `HashJoin`, `HashCrossJoin`, or `NestedLoopJoin`. No `MergeJoin` case exists. PostgreSQL uses merge join for sorted inputs (ORDER BY + JOIN on same column) and outer joins (O(N+M) vs NLJ's O(N×M)). For a 10K-left × 1K-right LEFT JOIN on sorted keys, NLJ does 312 right-side scans; merge join would do one pass each. **Fix:** (1) Add `MergeJoin` operator with two sorted input streams and mark/unmark for outer joins. (2) In `planSelect`, detect when both join sides produce sorted output (from Sort, IndexScan on sorted column, or ORDER BY). (3) Add merge join cost: `left_cost + right_cost + merge_overhead`. (4) Add `TestMergeJoin_Basic`, `TestMergeJoin_LeftOuter`, `TestMergeJoin_RightOuter`, `TestMergeJoin_FullOuter`, `BenchmarkMergeJoin_SortedInputs` verifying correctness and performance. | high | large | REQ001020 | EX/planner.go, EX/hashjoin.go (new MergeJoin) |
| REQ001103 | EX/plan | **No bushy join support — left-deep only limits star join and independent sub-join performance.** `planner.go:1970-2252`: `groupBushyJoins` exists but only groups tables for sequential left-deep execution within groups. The join construction at line 2186+ still produces a left-deep tree. PostgreSQL's DP naturally produces bushy plans when cheaper. For a star join (fact table F joined to dimensions D1, D2, D3 independently), left-deep forces F into the first join producing large intermediate results; bushy joins D1↔D2, D3↔(D1D2), then F↔(D1D2D3) with smaller intermediates. **Fix:** (1) Implement DP-based join ordering (or GEQO for large joins) that produces bushy trees. (2) Add `BushyJoin` operator or modify join construction to support non-left-deep trees. (3) Cost model for bushy plans. (4) Add `TestPlanner_BushyJoin_StarSchema`, `TestPlanner_BushyJoin_IndependentSubjoins` verifying bushy plans are selected. | high | large | REQ001102 | EX/planner.go |
| REQ001104 | EX/plan | **Cost model is unitless — no I/O vs CPU distinction, no parallelism cost, no memory pressure modeling.** `planner.go:470-544`: `estimateCost()` uses simple formulas: SeqScan=1.0, IndexSeek=0.05, NestedLoop=left×right, HashJoin=left+right. PostgreSQL uses `seq_page_cost`, `random_page_cost`, `cpu_tuple_cost`, `cpu_index_tuple_cost`, `cpu_operator_cost`. This matters for index vs seq scan decisions on large tables where random I/O is expensive. **Fix:** (1) Add cost parameters: `seqPageCost`, `randomPageCost`, `cpuTupleCost`, `cpuIndexTupleCost`, `cpuOperatorCost`. (2) Revise cost formulas: SeqScan = `rows × seqPageCost`, IndexScan = `indexPages × randomPageCost + rows × cpuIndexTupleCost`, NestedLoop = `outer × (innerStartup + innerPerTuple × cpuOperatorCost)`, HashJoin = `buildCost + probeCost + hashTableOverhead`. (3) Add memory pressure estimation for hash joins and sorts (estimate when spill to disk occurs). (4) Add `TestCostModel_IndexVsSeqScan`, `TestCostModel_Parallelism` verifying cost model produces correct decisions. | medium | large | REQ001102, REQ001103 | EX/planner.go |
| REQ001105 | PS/select | **No LATERAL join support — subqueries in FROM cannot reference outer tables.** PostgreSQL's LATERAL allows `SELECT * FROM t1, LATERAL (SELECT * FROM t2 WHERE t2.id = t1.id)`. The parser doesn't recognize LATERAL. `planSelect` has no LATERAL handling. Subquery handling in `subq.go` only supports `outerInjector` for correlated subqueries in WHERE, not LATERAL in FROM. **Fix:** (1) Add LATERAL keyword to lexer and parser. (2) In `planSelect`, detect LATERAL subqueries and plan them with outer reference resolution. (3) Add `TestPlanner_Lateral_Basic`, `TestPlanner_Lateral_Function` verifying LATERAL subqueries work. | medium | large | none | PS/select.go, EX/planner.go, EX/subq.go |
| REQ001106 | EX/plan | **No bitmap heap scan — multiple index conditions cannot be combined efficiently.** PostgreSQL creates a bitmap from multiple index scans and scans the heap once. Razor's index selection code only considers one index at a time. For `WHERE a = 1 OR b = 2`, PostgreSQL combines two index scans into a bitmap; Razor falls back to seq scan or NLJ. **Fix:** (1) Add `BitmapHeapScan` operator. (2) Add bitmap data structure for combining multiple index scan results. (3) In planner, detect when multiple index conditions can be combined (OR, AND with different columns). (4) Add `TestBitmapHeapScan_OrConditions`, `TestBitmapHeapScan_AndConditions` verifying correctness. | medium | large | REQ001104 | EX/planner.go, EX/operators.go |
| REQ001107 | EX/plan | **No index-only scan — covering queries still fetch from heap.** PostgreSQL can satisfy queries entirely from the index when all needed columns are in the index. Razor's `IndexScan` always fetches from the heap. For `SELECT a FROM t WHERE a = 1` with an index on `a`, PostgreSQL reads only the index; Razor reads index + heap. **Fix:** (1) Add `IndexOnlyScan` operator. (2) Add visibility map for index-only scan (to avoid heap fetches). (3) In planner, detect covering indexes (all projected columns are in the index). (4) Add `TestIndexOnlyScan_CoveringQuery`, `BenchmarkIndexOnlyScan_VsHeapScan` verifying correctness and performance. | medium | large | REQ001104 | EX/planner.go, EX/operators.go |
| REQ001108 | EX/plan | **No index condition pushdown — indexable conditions not pushed into index scan.** PostgreSQL pushes indexable conditions into the index scan to reduce heap fetches. Razor's `IndexScan` doesn't support condition pushdown; all filtering is done by `Filter` operators above the scan. For `WHERE a = 1 AND b > 10` with an index on `a`, PostgreSQL pushes `a = 1` into the index scan; Razor scans all rows with `a = 1` and filters `b > 10` after heap fetch. **Fix:** (1) Add condition pushdown logic in `IndexScan`. (2) In planner, extract indexable conditions and pass them to `IndexScan`. (3) Add `TestIndexConditionPushdown_Basic`, `BenchmarkIndexConditionPushdown_Performance` verifying correctness and performance. | low | medium | REQ001107 | EX/planner.go, EX/operators.go |
| REQ001109 | EX/plan | **No GEQO for large joins — single-start N3 produces suboptimal plans for >8 tables.** PostgreSQL uses GEQO (genetic algorithm) for joins with >12 tables. Razor switches to single-start N3 for >8 tables. For very large joins, N3 may produce suboptimal plans. **Fix:** (1) Implement GEQO with population-based search. (2) Integrate with cost model. (3) Add planning time management. (4) Add `TestGEQO_LargeJoin` verifying GEQO produces better plans than N3 for 15+ table joins. | low | large | REQ001103, REQ001104 | EX/planner.go |
| REQ001113 | EX/join | **Multi-table cross-join (implicit comma-join) with equality predicates produces incorrect cardinality — 330/3857 select4 records fail.** `select4.test`: 351 of 3857 records fail; ~330 are join cardinality errors. Failures include both under-count (got 1, want 3/4/5) and over-count (got 1152 cells, want 576; got 56 cells, want 28). Different table orderings for the same query produce different wrong results: e.g., L29268 (got 0 cells, want 576) vs L29292/L29304 (got 1152 cells, want 576). Even simple 2-table joins with equality-to-constant predicates fail: `SELECT x1, d9 FROM t1, t9 WHERE 281=a1 AND c9=232` yields 1 row instead of 3; `SELECT x9, e8 FROM t9, t8 WHERE e8=893 AND b9=12` yields 1 row instead of 3. Both under-count and over-count present, ruling out a simple over-filtering bug. Different join orders produce different (wrong) cardinalities, indicating the join execution (HashJoin/NLJ) or planner predicate distribution is order-dependent. Root cause narrow candidates: (1) The `splitPredicatesByTable` function misroutes predicates for implicit comma-joins, causing some predicates to be lost or double-counted. (2) `eliminateCommonSubexpressions` previously dedup'd InExpr (fixed in REQ001111) but may still incorrectly dedup simple equality predicates. (3) The HashJoin/NLJ join logic has an order-dependent bug that produces different cardinalities for different table orderings. (4) `tryHashCrossJoin` selection (REQ001093) may misclassify some cross-joins. **Fix:** (1) Add a test running 20+ representative failing select4 queries with all table order permutations, verifying ALL permutations produce identical correct results. (2) Debug `splitPredicatesByTable` for implicit comma-joins — verify every predicate is assigned to exactly one table. (3) Debug `eliminateCommonSubexpressions` — verify simple equalities (`a=1`, `b=2`) are not dedup'd when they reference different tables. (4) Debug `HashJoin.buildAndProbe` — add assertions that left/right row counts produce expected cardinality for known test cases. (5) Debug `NestedLoopJoin.Next` — add assertions that all join predicates are evaluated for each row pair. (6) Add `TestCrossJoin_Cardinality_AllPermutations` with 5 representative failing queries and all 120 permutations passing. | critical | high | REQ001092, REQ001093, REQ001111 | EX/join.go, EX/hashjoin.go, EX/planner.go, EX/nlj.go |
| REQ001114 | EX/eval | **IN-list predicate on cross-join table column produces NULL projected values.** `select4.test` L29056-L29101: `SELECT e3+491, a7 FROM t3, t7 WHERE a3 in (637,591,710,644) AND e7=280` returns `[NULL, NULL]` instead of `[638, 894]`. Four table ordering variants all produce the same wrong result. The row is present (count=1 matches expected), but both projected column values are NULL. Since `a3 IN (...)` matches the row correctly (the row passes the filter), `e3` and `a7` should be readable from the same row. The NULLs suggest either: (1) the join/scan produces a row with uninitialized Data slots, (2) column index resolution for the projected expressions is wrong after the IN-list predicate is pushed, or (3) the IN-list pre-computed lookup map (REQ001087) incorrectly nullifies column values for matching rows. Single-table queries with IN-list predicates pass — the bug only manifests in multi-table cross-join contexts. **Fix:** (1) Add `TestINList_Join_ProjectedValues` verifying all projected columns are non-NULL for predicate-matching rows in a 2-table cross-join with IN-list. (2) Debug `compileFilterExpr` for IN-list — verify the compiled closure does not modify row Data values. (3) Debug column index resolution in `EvalValue` for projection after IN-list-predicated cross-join. (4) Check if the IN-list lookup map (REQ001087) stores pointer references that accidentally share state across rows. | high | medium | REQ001087, REQ001092 | EX/eval.go, EX/intermediate.go |
| REQ001115 | EX/eval | **`x*` (VARCHAR rowid column) returns wrong value in multi-table joins — likely upstream join/predicate bug, not x* storage.** `select4.test` L31329-L31368: 4 queries with `SELECT x3` from a 4-table join fail. The `x*` column is `VARCHAR(30)` storing literal strings like `'table tn3 row 89'` (note: NOT pipe-delimited — the actual format is `table tnN row M` with spaces). The engine stores and retrieves these via standard VARCHAR path (store.go KindText encode/decode). Root cause is NOT x* synthesis — it is likely upstream: (1) REQ001113 join cardinality errors produce wrong rows, or (2) REQ001092 SLT parser bug splits multi-space strings into phantom cells. **Fix:** (1) Fix REQ001113 (join cardinality) and REQ001092 (parser) first — this REQ likely resolves as a side effect. (2) If still failing after those fixes, debug column index resolution in join output for x* columns. (3) Add `TestRowidColumn_Format` verifying x* returns exact stored VARCHAR value. | low | small | REQ001113, REQ001092 | EX/eval.go, EX/store.go, EX/intermediate.go |
| REQ001117 | SQB/OP | **Extract `SQB/OP` cluster — operators: SeqScan, IndexScan, Filter, Project, Sort, Limit, Insert, Update, Delete, HashJoin, NestedLoopJoin, HashCrossJoin, Compound.** `SQB/EX` contains 195 files including all operator implementations. The `OP` cluster should contain: `operators.go`, `operators_parallel.go`, `operators_vec.go`, `join.go`, `hashjoin.go`, `hashcrossjoin.go`, `compound.go`, `intermediate.go`, `plan_node.go`, `source.go`, `values.go`, `virtual.go`, `batch.go`, `shape_specialize.go`, and their test files. **Fix:** (1) Create `internal/SQB/OP/` directory. (2) Move operator files from `SQB/EX` to `SQB/OP`. (3) Update imports in moved files and remaining `SQB/EX` files. (4) `SQB/OP` imports `SQF/PL` for `Operator`/`Row`/`Value` types. (5) `SQB/EX` imports `SQB/OP` for operator construction. (6) Verify `go build ./...` succeeds and `go test ./... -race -count=1` passes. | high | large | REQ001116 | SQB/EX/operators.go, SQB/EX/join.go, SQB/EX/hashjoin.go, SQB/EX/hashcrossjoin.go, SQB/EX/compound.go, SQB/EX/intermediate.go, SQB/EX/plan_node.go, SQB/EX/source.go, SQB/EX/values.go, SQB/EX/virtual.go, SQB/EX/batch.go, SQB/EX/shape_specialize.go, SQB/EX/operators_parallel.go, SQB/EX/operators_vec.go |
| REQ001118 | SQB/EV | **Extract `SQB/EV` cluster — evaluation: eval.go, eval_vec.go, all scalar functions.** `SQB/EX` contains all scalar function evaluation. The `EV` cluster should contain: `eval.go`, `eval_vec.go`, `eval_extras_test.go`, `eval_fixes_test.go`, `eval_req772_test.go`, `eval_scalar_funcs_test.go`, `eval_test.go`, `eval_vec_test.go`, `corefunc_test.go`, `corefunc_quick_test.go`, `coalesce_test.go`, `specialform_test.go`, `overflow_test.go`, `null_req_test.go`, `neg_literal_test.go`, and their test files. **Fix:** (1) Create `internal/SQB/EV/` directory. (2) Move eval files from `SQB/EX` to `SQB/EV`. (3) Update imports in moved files and remaining `SQB/EX` files. (4) `SQB/EV` imports `SQF/PL` for `Value` type. (5) `SQB/EX` and `SQB/OP` import `SQB/EV` for eval functions. (6) Verify `go build ./...` succeeds and `go test ./... -race -count=1` passes. | high | large | REQ001116 | SQB/EX/eval.go, SQB/EX/eval_vec.go, SQB/EX/eval_extras_test.go, SQB/EX/eval_fixes_test.go, SQB/EX/eval_req772_test.go, SQB/EX/eval_scalar_funcs_test.go, SQB/EX/eval_test.go, SQB/EX/eval_vec_test.go, SQB/EX/corefunc_test.go, SQB/EX/corefunc_quick_test.go, SQB/EX/coalesce_test.go, SQB/EX/specialform_test.go, SQB/EX/overflow_test.go, SQB/EX/null_req_test.go, SQB/EX/neg_literal_test.go |
| REQ001119 | SQB/AG | **Extract `SQB/AG` cluster — aggregation: aggregate.go, aggregate_vec.go, hashagg.go, hashagg_parallel.go, window.go, aggregate_registry.go.** `SQB/EX` contains all aggregation and window function implementations. The `AG` cluster should contain: `aggregate.go`, `aggregate_vec.go`, `aggregate_vec_test.go`, `hashagg.go`, `hashagg_parallel.go`, `window.go`, `window_test.go`, `aggregate_registry.go`, `distinct.go`, `hashagg_planner_test.go`, `aggregate_distinct_iter27_test.go`, and their test files. **Fix:** (1) Create `internal/SQB/AG/` directory. (2) Move aggregation files from `SQB/EX` to `SQB/AG`. (3) Update imports in moved files and remaining `SQB/EX` files. (4) `SQB/AG` imports `SQF/PL` for `Operator`/`Row`/`Value` types and `SQB/EV` for eval functions. (5) `SQB/EX` and `SQB/OP` import `SQB/AG` for aggregation operators. (6) Verify `go build ./...` succeeds and `go test ./... -race -count=1` passes. | high | large | REQ001116, REQ001118 | SQB/EX/aggregate.go, SQB/EX/aggregate_vec.go, SQB/EX/aggregate_vec_test.go, SQB/EX/hashagg.go, SQB/EX/hashagg_parallel.go, SQB/EX/window.go, SQB/EX/window_test.go, SQB/EX/aggregate_registry.go, SQB/EX/distinct.go, SQB/EX/hashagg_planner_test.go, SQB/EX/aggregate_distinct_iter27_test.go |
| REQ001120 | SQB/AD | **Extract `SQB/AD` cluster — ADQC/planning: adqc*.go, planner.go, memo.go, selectivity.go, shape_specialize.go, cache_stats.go.** `SQB/EX` contains ADQC, planner, memo, and selectivity estimation. The `AD` cluster should contain: `adqc.go`, `adqc_cache.go`, `adqc_fallback.go`, `adqc_telemetry.go`, `adqc_test.go`, `planner.go`, `planner_iter22_test.go`, `planner_test.go`, `memo.go`, `selectivity_test.go`, `cache_stats.go`, `cache_stats_test.go`, `shape_specialize.go`, `cost_indexscan_test.go`, `cost_selection_iter27_test.go`, and their test files. **Fix:** (1) Create `internal/SQB/AD/` directory. (2) Move ADQC/planning files from `SQB/EX` to `SQB/AD`. (3) Update imports in moved files and remaining `SQB/EX` files. (4) `SQB/AD` imports `SQF/PL` for `Operator`/`Row` types and `SQB/EV` for eval functions. (5) `SQB/EX` imports `SQB/AD` for planner and ADQC. (6) Verify `go build ./...` succeeds and `go test ./... -race -count=1` passes. | high | large | REQ001116, REQ001118 | SQB/EX/adqc.go, SQB/EX/adqc_cache.go, SQB/EX/adqc_fallback.go, SQB/EX/adqc_telemetry.go, SQB/EX/adqc_test.go, SQB/EX/planner.go, SQB/EX/planner_iter22_test.go, SQB/EX/planner_test.go, SQB/EX/memo.go, SQB/EX/selectivity_test.go, SQB/EX/cache_stats.go, SQB/EX/cache_stats_test.go, SQB/EX/shape_specialize.go, SQB/EX/cost_indexscan_test.go, SQB/EX/cost_selection_iter27_test.go |
| REQ001121 | SQB/WT | **Extract `SQB/WT` cluster — write operators: writers.go, source.go, store.go, alter_table.go, fk.go, subq.go, view.go, constraints.go, matview.go.** `SQB/EX` contains all write operators and DDL executors. The `WT` cluster should contain: `writers.go`, `source.go`, `store.go`, `store_test.go`, `alter_table.go`, `alter_table_test.go`, `fk.go`, `subq.go`, `view.go`, `constraints.go`, `constraints_test.go`, `matview.go`, `matview_test.go`, `index_usage.go`, `index_usage_test.go`, `index_ddl_test.go`, `index_e2e_test.go`, `index_scan_iter22_test.go`, `index_scan_iter27_test.go`, `gencol_test.go`, and their test files. **Fix:** (1) Create `internal/SQB/WT/` directory. (2) Move write operator files from `SQB/EX` to `SQB/WT`. (3) Update imports in moved files and remaining `SQB/EX` files. (4) `SQB/WT` imports `SQF/PL` for `Operator`/`Row`/`Value` types, `SQB/EV` for eval functions, and `SQB/OP` for operator types. (5) `SQB/EX` imports `SQB/WT` for write operators. (6) Verify `go build ./...` succeeds and `go test ./... -race -count=1` passes. | high | large | REQ001116, REQ001117, REQ001118 | SQB/EX/writers.go, SQB/EX/source.go, SQB/EX/store.go, SQB/EX/store_test.go, SQB/EX/alter_table.go, SQB/EX/alter_table_test.go, SQB/EX/fk.go, SQB/EX/subq.go, SQB/EX/view.go, SQB/EX/constraints.go, SQB/EX/constraints_test.go, SQB/EX/matview.go, SQB/EX/matview_test.go, SQB/EX/index_usage.go, SQB/EX/index_usage_test.go, SQB/EX/index_ddl_test.go, SQB/EX/index_e2e_test.go, SQB/EX/index_scan_iter22_test.go, SQB/EX/index_scan_iter27_test.go, SQB/EX/gencol_test.go |
| REQ001122 | SQB/UT | **Extract `SQB/UT` cluster — utilities: coerce.go, pragma.go, integrity.go, decimal.go, datetime.go, json.go, explain.go, analyze.go, sort_parallel.go, pipeline.go, simd_dispatch.go, parallel.go.** `SQB/EX` contains all utility functions and cross-cutting concerns. The `UT` cluster should contain: `coerce.go`, `coerce_test.go`, `pragma.go`, `pragma_fk_test.go`, `integrity.go`, `integrity_test.go`, `decimal.go`, `decimal_test.go`, `datetime.go`, `datetime_test.go`, `json.go`, `json_test.go`, `explain.go`, `explain_test.go`, `analyze.go`, `analyze_test.go`, `sort_parallel.go`, `sort_parallel_test.go`, `pipeline.go`, `pipeline_test.go`, `simd_dispatch.go`, `simd_dispatch_test.go`, `parallel.go`, `parallel_test.go`, `txn_debug.go`, `txn_debug_test.go`, `batch.go`, `batch_test.go`, and their test files. **Pattern:** For files with EX dependencies (e.g., `pragma.go` uses `Row`/`Value`/`ErrNoRows`), split the file — keep EX-dependent parts in EX, move UT parts to UT, create `EX -> UT` dependency chain. Pure UT files (no EX deps) move entirely. **Fix:** (1) Create `internal/SQB/UT/` directory. (2) Move pure UT files from `SQB/EX` to `SQB/UT`. (3) For files with EX deps, split: UT parts go to UT, EX-dependent parts stay in EX. (4) Update imports in moved files and remaining `SQB/EX` files. (5) `SQB/UT` imports `SQF/PL` for `Operator`/`Row`/`Value` types and `SQB/EV` for eval functions. (6) `SQB/EX` and other clusters import `SQB/UT` for utilities. (7) Verify `go build ./...` succeeds and `go test ./... -race -count=1` passes. **PoC done:** `simd_dispatch.go` (pure UT, moved entirely), `coerce.go` (pure UT, moved entirely), `decimal.go` (pure UT, moved entirely, `evalDecimalCast` exported to `EvalDecimalCast`), `json.go` (pure UT, moved entirely, `evalJSONFunc`/`isJSONFunc` exported), `parallel.go` (pure UT, moved entirely, EX files import UT for `WorkerPool`), `batch.go` (pure UT, moved entirely, EX files import UT for `Batch`/`Column`/`GetBatch`/`BatchSize`/etc.), `txn_debug.go` (pure UT, moved entirely, `TxnDebugInfo` moved from `plan_node.go` to UT), `pragma.go` (split: `PragmaListener`/`RegisterPragmaListener`/`UnregisterAllPragmaListeners`/`NotifyPragmaChange` → UT, `PrAGMAResult` stays in EX), `datetime.go` (pure UT, moved entirely, `evalDateTimeFunc`/`isDateTimeFunc`/`DateTimeArithmetic`/`IntervalValue`/`ParseInterval` exported). `simd_dispatch_test.go` split: `TestDetectFeatures_AnyArch` → UT, `TestEvalBatch_8Wide` → EX (EX-dependent). EX files updated: `eval.go`, `constraints.go`, `ex.go`, `planner.go`, `plan_node.go`, `operators_parallel.go`, `intermediate.go`, `sort_parallel.go`, `hashagg_parallel.go`, `operators_vec.go`, `pipeline.go`, `adqc.go`, `adqc_cache.go`, `adqc_fallback.go`, `aggregate_vec.go`, `eval_vec.go`, `join.go`, `operators.go`, `writers.go`, and test files. **Remaining:** `integrity.go`, `explain.go`, `analyze.go`, `sort_parallel.go`, `pipeline.go` all have EX dependencies and require splitting.hSize`/etc.), `txn_debug.go` (pure UT, moved entirely, `TxnDebugInfo` moved from `plan_node.go` to UT). `simd_dispatch_test.go` split: `TestDetectFeatures_AnyArch` → UT, `TestEvalBatch_8Wide` → EX (EX-dependent). EX files updated: `eval.go`, `constraints.go`, `ex.go`, `planner.go`, `plan_node.go`, `operators_parallel.go`, `intermediate.go`, `sort_parallel.go`, `hashagg_parallel.go`, `operators_vec.go`, `pipeline.go`, `adqc.go`, `adqc_cache.go`, `adqc_fallback.go`, `aggregate_vec.go`, `eval_vec.go`, `join.go`, `operators.go`, and test files. **Remaining:** `datetime.go`, `integrity.go`, `explain.go`, `analyze.go`, `sort_parallel.go`, `pipeline.go`, `pragma.go` all have EX dependencies and require splitting. | high | large | REQ001116, REQ001118 | SQB/EX/coerce.go, SQB/EX/coerce_test.go, SQB/EX/pragma.go, SQB/EX/pragma_fk_test.go, SQB/EX/integrity.go, SQB/EX/integrity_test.go, SQB/EX/decimal.go, SQB/EX/decimal_test.go, SQB/EX/datetime.go, SQB/EX/datetime_test.go, SQB/EX/json.go, SQB/EX/json_test.go, SQB/EX/explain.go, SQB/EX/explain_test.go, SQB/EX/analyze.go, SQB/EX/analyze_test.go, SQB/EX/sort_parallel.go, SQB/EX/sort_parallel_test.go, SQB/EX/pipeline.go, SQB/EX/pipeline_test.go, SQB/EX/simd_dispatch.go, SQB/EX/simd_dispatch_test.go, SQB/EX/parallel.go, SQB/EX/parallel_test.go, SQB/EX/txn_debug.go, SQB/EX/txn_debug_test.go, SQB/EX/batch.go, SQB/EX/batch_test.go |
| REQ001123 | SQB/EX | **Finalize `SQB/EX` as core types only — Operator/Row/Value/ExecContext, executor factory, and glue.** After extracting OP, EV, AG, AD, WT, and UT clusters, `SQB/EX` should contain only: `ex.go` (Executor: `Exec()`, `Query()`), `execctx.go` (ExecContext), and test files that belong to EX-level integration (e.g., `executor_test.go`, `e2e_test.go`, `testutil_test.go`, `coverage_test.go`, `catalog_integration_test.go`, `memstore_test.go`). All other files should have been moved to their respective clusters. **Fix:** (1) After all 6 cluster extractions, verify `SQB/EX` contains only the core files listed above. (2) `SQB/EX` imports `SQF/PL` for `Operator`/`Row`/`Value`/`ExecContext` types and all 6 child clusters. (3) Run `go build ./...` and `go test ./... -race -count=1` — all tests must pass. (4) Run `go vet ./...` — zero warnings. (5) Run `gofmt -s -l .` — no drift. (6) Verify file count in `SQB/EX` is ≤50 (down from 195). | high | large | REQ001116, REQ001117, REQ001118, REQ001119, REQ001120, REQ001121, REQ001122 | SQB/EX/ex.go, SQB/EX/execctx.go, all remaining SQB/EX files |
| REQ001124 | SYS/SY | **Goroutine leak on open/close cycles — 200 extra goroutines after 10 cycles.** `closed_test.go:275` (`TestNoGoroutineLeakFromClosedFlag`): 200 goroutines leaked after 10 open/close cycles. `shutdown_e2e_test.go:94` (`TestShutdown_NoGoroutineLeak`): delta=400 after 20 open/close cycles. The SY subsystem spawns goroutines (background workers, WAL flushers, etc.) that are not properly joined on `Close()`. **Fix:** (1) Audit all `go` statements in `SYS/SY/` and ensure each goroutine respects a `done` channel or `context.Cancel`. (2) In `Close()`, signal all background goroutines to stop and wait for them to finish. (3) Add `TestGoroutineLeak_CloseWaitsForWorkers` verifying zero leaked goroutines after close. | high | medium | none | SYS/SY/closed.go, SYS/SY/shutdown.go |
| REQ001125 | EX/eval | **`CHANGES()` and `TOTAL_CHANGES()` return wrong results in dual tests.** `tests/sqlcmp/dual`: `changes_function` and `total_changes_function` cases fail with result mismatch. These functions should return the number of rows modified by the most recent INSERT/UPDATE/DELETE. The dual test framework seeds data and checks aggregate results. Root cause likely: (1) the change counter is not incremented for in-memory table writes, or (2) the counter resets between statements. **Fix:** (1) Debug `CHANGES()` and `TOTAL_CHANGES()` evaluation in `eval.go` — verify they read the correct counter. (2) Verify the counter is incremented for all write paths (in-memory and LSM). (3) Add `TestChangesFunction_InMemoryWrite` verifying correct count after INSERT/UPDATE/DELETE. | medium | small | none | EX/eval.go, EX/writers.go |
| REQ001126 | EX/eval | **Correlated subquery EXISTS/NOT EXISTS returns wrong results in dual tests.** `tests/sqlcmp/dual`: `correlated_subquery_exists` and `correlated_subquery_not_exists` cases fail. These test `EXISTS (SELECT ... WHERE outer.x = inner.y)` patterns. Likely related to REQ001098 (correlated subquery comparison broken). The dual test is a concrete regression that should be fixed alongside or after REQ001098. **Fix:** (1) Fix REQ001098 first — this REQ likely resolves as a side effect. (2) If still failing, debug `evalExists` for correlated references in the dual test's specific query patterns. (3) Add `TestDual_Exists_Correlated` verifying the exact dual test case. | medium | small | REQ001098 | EX/eval.go |
| REQ001127 | EX/eval | **`NULL NOT IN (list)` returns wrong result in dual test.** `tests/sqlcmp/dual`: `null_not_in_list` case fails. SQL `NOT IN` with NULL in the list has three-valued logic: `x NOT IN (1, NULL)` returns NULL (not TRUE) when x is not 1, because `x != NULL` is NULL. The dual test expects a specific result that the engine gets wrong. **Fix:** (1) Debug `evalInValue` / `evalInListBatch` for NULL handling in NOT IN. (2) Verify three-valued logic: `NULL NOT IN (1, NULL)` → NULL, `5 NOT IN (1, NULL)` → NULL, `1 NOT IN (1, NULL)` → FALSE. (3) Add `TestNotIn_NullThreeValuedLogic` verifying correct NULL propagation. | medium | small | none | EX/eval.go |
| REQ001128 | EX/eval | **UNION ALL/UNION DISTINCT with NULLs returns wrong results in dual tests.** `tests/sqlcmp/dual`: `union_all_with_nulls` and `union_distinct_with_nulls` cases fail. UNION ALL should preserve all rows including duplicates and NULLs. UNION DISTINCT should deduplicate, treating two NULLs as equal for dedup purposes (per SQL standard). Root cause likely: (1) NULL comparison in UNION DISTINCT uses `=` instead of `IS NOT DISTINCT FROM`, or (2) NULL rows are dropped from UNION ALL. **Fix:** (1) Debug UNION ALL/UNION DISTINCT implementation in `compound.go`. (2) Verify NULL handling: UNION ALL preserves NULL rows, UNION DISTINCT treats NULL=NULL for dedup. (3) Add `TestUnion_DistinctNullDedup` verifying NULL dedup behavior. | medium | small | none | EX/compound.go |
| REQ001129 | EX/eval | **INSERT ... SELECT returns wrong result in dual test.** `tests/sqlcmp/dual`: `insert_select` case fails. The test inserts rows via `INSERT INTO t2 SELECT * FROM t1` and verifies the result. Root cause likely: (1) column index mismatch between source and target tables, (2) NULL propagation during copy, or (3) the INSERT operator doesn't correctly read from the SELECT subquery. **Fix:** (1) Debug `buildInsertRow` for INSERT ... SELECT path. (2) Verify column mapping between source SELECT output and target table schema. (3) Add `TestInsertSelect_ColumnMapping` verifying correct row insertion. | medium | small | none | EX/writers.go, EX/source.go |
| REQ001131 | ENG/LS | **Read path holds RLock during entire SST search — blocks all reads on slow page faults.** `engine.go:160-185`: `Read()` holds `e.mu.RLock()` while searching active memtable → frozen memtables → SST files. If an SST read causes a page fault or disk I/O, all concurrent reads are blocked behind the same lock. **Fix:** (1) Snapshot the manifest under RLock, then release the lock before doing I/O-heavy SST reads. The manifest is immutable once snapshotted so this is safe. (2) Or use a read-copy-update pattern where the manifest pointer is atomically loaded. (3) Add `TestEngine_Read_ConcurrentWithFlush` verifying reads aren't blocked by flush. | high | medium | none | ENG/LS/engine.go |
| REQ001132 | ENG/LS | **Skip-list `randomLevel()` holds global mutex on every insert — serialization point under write concurrency.** `skiplist.go:60-68`: `randomLevel()` acquires `sl.rmu` for every insert to generate a random level. Under high write concurrency, this becomes a bottleneck since all inserts to the same skiplist serialize on this lock. The `math/rand/v2` package is imported but not used here. **Fix:** (1) Use a lock-free PRNG: `atomic.Uint64` as xorshift state (no lock needed). (2) Or use per-goroutine RNG via `rand/v2` with a seed derived from the goroutine ID. (3) Add `BenchmarkSkiplist_Insert_Concurrent` measuring throughput before/after. | high | small | none | ENG/LS/skiplist.go |
| REQ001133 | ENG/LS | **Page cache uses global RWMutex on every Get — reduces throughput under concurrent reads.** `page_cache.go:71-81`: `Get()` acquires `c.mu.RLock()` for every cache hit. The `index` map is not goroutine-safe, but a global lock means all cache lookups serialize on the read lock, limiting concurrency on multi-core systems. **Fix:** (1) Shard the cache into N buckets with per-shard locks (N = runtime.GOMAXPROCS). (2) Or use `sync.Map` for the index (optimized for read-heavy concurrent access). (3) Add `BenchmarkPageCache_Get_Concurrent` measuring throughput under 8/16/32 goroutines. | medium | medium | none | ENG/LS/page_cache.go |
| REQ001134 | ENG/ID | **`encodeHeader` allocates `[]byte` on every call — hot path in BTree flush.** `id.go:663-670`: `encodeHeader` does `make([]byte, headerSize)` (16 bytes) on every call. This is called from `encodePage` which is called for every dirty page during flush. For a BTree with 10K dirty pages, this is 10K small heap allocations that could be stack-allocated. **Fix:** (1) Use a stack-allocated `[16]byte` array and return a slice of it. (2) Or accept a pre-allocated buffer parameter from the caller. (3) Add `BenchmarkBTree_Flush_Allocs` verifying zero allocs per header encode. | low | small | none | ENG/ID/id.go |
| REQ001135 | TXN/VL | **OCC validation scans all 1024 slots on every commit — O(C × R) with `string()` alloc in hot loop.** `validate.go:53-71`: `Validate()` iterates ALL `MaxConcurrentTXNs` (1024) slots to find conflicting transactions. Two separate loops (read-write + write-write) double the work. The inner comparison `mySlot.readSet[string(kr.Start)]` allocates a new `string` from `[]byte` on every iteration, creating GC pressure under high commit throughput. **Fix:** (1) Merge the two loops into a single pass. (2) Replace `map[string]uint64` readSet with `map[uint64][]byte` using FNV-1a hash key to avoid `string()` allocation. (3) Maintain a sorted index of recently committed slot timestamps to skip inactive slots in O(1). (4) Add `BenchmarkVL_Validate_Concurrent` measuring commit throughput under 16/64 concurrent transactions. | high | medium | none | TXN/VL/validate.go, TXN/VL/slot.go |
| REQ001136 | WAL/WR | **Global mutex on every WAL append — serializes all writes.** `wr.go:194-195`: `Append()` acquires `w.mu.Lock()` for every batch, serializing all WAL writes. Under high write concurrency (OLTP with 100+ concurrent writers), this becomes the write throughput ceiling. The mutex also covers segment rotation and buffer flush, which involve disk I/O. **Fix:** (1) Use a lock-free ring buffer for appends, with a single background goroutine draining to disk. (2) Or use per-segment locks to allow concurrent appends to different segments. (3) Or adopt a producer-consumer pattern: writers push to a channel, a single writer goroutine batches and flushes. (4) Add `BenchmarkWAL_Append_Concurrent` measuring throughput under 8/16/32 goroutines. | high | medium | none | WAL/WR/wr.go |
| REQ001137 | TXN/VL | **`readSet` allocates new map on every transaction Begin — OLTP allocation pressure.** `slot.go:107`: `AllocateSlot()` does `slot.readSet = make(map[string]uint64)` for every new transaction. For short-lived OLTP transactions (10K+ commits/sec), this is 10K map allocations/sec that could be avoided by pooling. **Fix:** (1) Pool maps: use `sync.Pool` of `map[string]uint64`, clear with `clear(m)` (Go 1.21+) on release instead of allocating fresh. (2) Or use a flat `[]ReadEntry` slice (already defined) with linear scan instead of a map — for typical readSets (< 100 entries), linear scan is faster than map overhead. (3) Add `BenchmarkVL_Begin_Allocs` verifying zero heap allocs per Begin. | high | small | none | TXN/VL/slot.go |
| REQ001138 | WAL/WR | **Triple allocation per record encode — WAL write amplification.** `encode.go:52-92`: `encodeRecordCompressed` allocates up to 3 times per record: (1) `body` buffer, (2) pooled `buf` (may realloc if too small), (3) final `result` copy. For a WAL batch with 100 records, this is 300 allocations per Append call. **Fix:** (1) Encode directly into the segment buffer (zero-copy). (2) Or batch-encode: buffer all records into a single pooled buffer, then write the entire batch at once. (3) Remove the final `result` copy — return the pooled buffer slice directly (caller must consume before next encode). (4) Add `BenchmarkWAL_Encode_PerRecord` verifying ≤1 alloc per record. | high | medium | none | WAL/WR/encode.go |
| REQ001139 | FIL/DF | **Double file open on O_DIRECT fallback — fd race and wasted syscall.** `df.go:127-138`: If O_DIRECT is not supported, the code closes the first fd and opens a second one without O_DIRECT. This creates a race window where the fd is closed but not yet reopened, and wastes a syscall. **Fix:** (1) Use `fcntl(fd, F_SETFL, O_DIRECT)` on the existing fd instead of close+reopen. This is atomic and avoids the race. (2) If fcntl fails, fall back gracefully without closing the original fd. (3) Add `TestDF_Open_ODIRECTFallback` verifying the fd is valid after fallback. | medium | small | none | FIL/DF/df.go |
| REQ001146 | SQF/LX | **scanIdent truncates multi-byte UTF-8 identifiers — emits invalid UTF-8 lexemes.** `lx.go:532-562` `scanIdent`: inner loop advances 1 byte per iteration. When the first byte is non-ASCII (`>= 0x80`), `unicode.IsLetter(rune(c))` accepts it as an identifier start (correct), but the loop then advances only 1 byte. The continuation bytes (0x80..0xBF) fail the same check (not letters), so the loop terminates after the first byte. Result: `中文` lexes as `"\xe4"` followed by two T_ERROR tokens for the continuation bytes, instead of one T_IDENT with lexeme `中文`. Discovered by FuzzUTF8Preserved (see iter-1140-1144 fuzz_test.go). **Fix:** (1) When the current byte is non-ASCII, decode the full rune via `utf8.DecodeRuneInString` and advance by the rune width. (2) Apply the same fix to `scanNumber` for non-ASCII digits (rare but consistent). (3) Verify all emitted lexemes are valid UTF-8 via `utf8.ValidString`. (4) Extend FuzzUTF8Preserved to also verify that lexeme byte-length equals (input-endOffset - input-startOffset) for contiguous identifiers. | high | small | REQ001141 | SQF/LX/lx.go, SQF/LX/fuzz_test.go |
| REQ001147 | SQF/LX | **FuzzPeekNextConsistent test logic has a desync — compares reference and test lexers across different cursor positions.** `internal/SQF/LX/fuzz_test.go` `FuzzPeekNextConsistent`: each iteration calls `ref.Next()` (advancing ref by 1) and `test.Next()` (advancing test by 1) but ALSO `ref.Next()` a second time inside the loop to fetch the second-upcoming token for comparison. Net effect: ref advances by 2 tokens per iteration, test by 1, so the invariant check `p2 == refTok2` always fails after the first iteration. The lexer itself is correct (manually traced via cmd/dbg_peek). The test must be rewritten so ref and test advance at the same rate. **Fix:** (1) Drive both lexers with the same sequence of `Peek/Peek2/Next` calls and compare token-by-token without an extra ref.Next() inside the loop. (2) Or use two identical lexers, advance both with the same call sequence, assert token streams are identical. (3) Add a positive control: confirm baseline lexer (no Peek) and Peek-driven lexer produce identical token sequences on the seed corpus. | low | small | REQ001140 | SQF/LX/fuzz_test.go |
| REQ001148 | SQF/LX | **Token struct is 80+ bytes — wastes memory locality and cache lines.** `token.go:224-244`: Token contains `Lexeme string`, `Literal any`, `LitInt int64`, `LitFloat float64`, `LitStr string`, `LitErr error`, `Line int`, `Col int` — totaling ~80 bytes. For `T_INT`/`T_FLOAT` tokens, `Lexeme` is redundant (it holds the same text as the raw input slice, and callers only need `LitInt`/`LitFloat`). For `T_STRING`, `Lexeme` and `LitStr` are identical strings (both hold the processed value). The `Literal any` field is dead code (never set by the lexer after REQ001143). **Fix:** (1) Remove the `Literal any` field entirely — it is unused. (2) For `T_INT` and `T_FLOAT` tokens, do not populate `Lexeme` (set it to empty or to the same slice as the raw input). (3) Consider making `Line`/`Col` uint32 to save 4 bytes if needed. (4) Add `BenchmarkToken_Size` measuring struct size before/after. (5) Add `TestToken_FieldConsistency` verifying typed fields are populated correctly per token type. | medium | small | REQ001143 | SQF/LX/token.go |
| REQ001149 | SQF/LX | **skipWhitespaceAndComments scans byte-by-byte — could bulk-scan whitespace runs.** `lx.go:535-579`: `skipWhitespaceAndComments` calls `peek()` + `advance()` for every byte of whitespace. For inputs with long whitespace runs (e.g., indented SQL, multi-line queries), this is O(N) byte-at-a-time. A bulk scan using `strings.IndexByte` or manual loop over `l.input[l.pos:]` skipping ASCII space/tab runs in a tight inner loop (no per-byte function call overhead) would reduce branch mispredictions. **Fix:** (1) Add an ASCII fast-path inner loop that skips `' '` and `'\t'` runs directly via `l.input` indexing without calling `advance()` per byte. (2) Update `line`/`col` counters in bulk (col += run length, no newlines in space/tab runs). (3) Keep `advance()` path for newlines and comments only. (4) Add `BenchmarkLexer_SkipWhitespace` with long whitespace runs measuring before/after. | low | small | REQ001144 | SQF/LX/lx.go |
| REQ001150 | SQF/LX | **scanString escape path allocates strings.Builder — could use stack buffer for short strings.** `lx.go:607-617`: when `hasEscape` is true, `scanString` allocates a `strings.Builder`, copies bytes one at a time, then calls `.String()`. For typical SQL string literals (short, few escapes), a stack-allocated `[256]byte` buffer would avoid the heap allocation entirely. Only fall back to `strings.Builder` for strings exceeding the buffer. **Fix:** (1) Allocate a fixed `[256]byte` stack buffer before the escape loop. (2) Write into the buffer; if it overflows, switch to `strings.Builder`. (3) This covers 95%+ of SQL string literals (which are short). (4) Add `BenchmarkLexerScanString_Escaped` measuring allocs for short escaped strings. | low | small | REQ001010 | SQF/LX/lx.go |
| REQ001151 | SQF/LX | **scanOperator uses a 25-case switch — a 256-byte lookup table would be branchless.** `lx.go:778-856`: `scanOperator` is a large switch on the first character. The most common operators (`=`, `<`, `>`, `(`, `)`, `,`, `;`) each take one branch. A 256-byte `var operatorTable [256]struct{tok TokenType; next byte}` lookup table would map the first byte directly to its token type (and the second byte needed for 2-char operators like `<=`, `>=`, `<>`, `||`, `<<`, `>>`), eliminating the branch chain entirely. **Fix:** (1) Define `var opTable [256]opEntry` at package level, initialized in `init()`. (2) Replace the switch with a single table lookup. (3) For 2-char operators, peek the second byte from the table entry. (4) Add `BenchmarkLexerScanOperator` measuring before/after. | low | small | none | SQF/LX/lx.go |
| REQ001152 | SQF/LX | **Token.String() allocates on every call — concatenates type name + lexeme.** `token.go:472-477`: `Token.String()` does `tokenTypeNames[t.Type] + ":" + t.Lexeme` which allocates a new string every time. This is called in error messages, debug logging, and test output. For hot paths that log tokens frequently, this creates GC pressure. **Fix:** (1) Use `strings.Builder` with a pooled or stack-allocated builder. (2) Or cache common token strings (e.g., all single-char operators and keywords). (3) Or make `String()` return a `string` backed by a `strings.Builder` that the caller owns. (4) Add `BenchmarkTokenString` measuring allocs per call. | low | small | none | SQF/LX/token.go |
| REQ001153 | SQF/LX | **NewLexer heap-allocates on every call — could use sync.Pool for hot-path lexing.** `lx.go:329-335`: `NewLexer(input)` returns `&Lexer{...}` which heap-allocates. For OLTP workloads parsing many small queries, this creates per-query allocation pressure. A `sync.Pool` of `*Lexer` would amortize allocation across queries. **Fix:** (1) Add `var lexerPool = sync.Pool{New: func() any { return &Lexer{} }}`. (2) Add `func GetLexer(input string) *Lexer` that gets from pool and sets `input`. (3) Add `func PutLexer(l *Lexer)` that clears fields and returns to pool. (4) Keep `NewLexer` for callers that don't need pooling. (5) Add `BenchmarkLexer_Pool` measuring allocs with pool vs without. | low | small | none | SQF/LX/lx.go |
| REQ001154 | SQF/LX | **Keyword trie nodes waste memory — 216 bytes per node (27 pointers).** `lx.go:32-35`: each `keywordNode` has `children [27]*keywordNode` (216 bytes) plus `value TokenType` (8 bytes). The trie has ~150 nodes, consuming ~33KB. Most nodes have 1-3 children; the 27-slot array is mostly nil pointers. A compact representation (e.g., packed child indices into a flat array, or a sorted child list per node) would improve cache locality during lookup and reduce memory footprint. **Fix:** (1) Profile trie memory usage with `unsafe.Sizeof`. (2) Replace `[27]*keywordNode` with a sorted `[]childEntry{byte, *keywordNode}` slice (typically 1-3 entries). (3) Binary search the child list instead of direct indexing. (4) Or use a flat array representation (all nodes in a single `[]keywordNode` slice, children as indices). (5) Add `BenchmarkKeywordTrie_Lookup_Compact` measuring lookup speed with compact vs current representation. | low | small | REQ001145 | SQF/LX/lx.go |
