## TBD

| ID | Subsystem | Requirement | Priority | Effort | Deps | Touches |
| --- | --- | --- | --- | --- | --- | --- |
| REQ001508 | SQB/UT | **Incremental ANALYZE trigger.** **Fix:** (1) Add `RowChangeTracker`. (2) Increment on INSERT/UPDATE/DELETE. (3) Check threshold periodically. (4) Add `PRAGMA auto_analyze_threshold`. (5) Add test. Estimate: ~120 LoC, 1 file new + 2 modified, 1 day. | medium | medium | REQ001507 | SQB/UT/analyze.go, SQB/EX/ex.go, SQB/EX/planner.go |
| REQ001509 | SQB/OP | **Memoize node for parameterized NestedLoopJoin.** **Fix:** (1) Add `Memoize` operator. (2) Cache on outer parameter hash. (3) Detect correlated subqueries in planner. (4) Add test. (5) Opt-in via cost threshold. Estimate: ~200 LoC, 1 file new + 1 modified + 1 test, 1-2 days. | medium-high | medium | REQ001497 | SQB/OP/memoize.go (new), SQO/CO/select_plan.go |
| REQ001522 | SQB/EX | **Stream goroutine path clone via RowArena bump allocator instead of make+copy.** `cloneRowToHeap` does `make([]Value, n) + copy` for each row sent through the goroutine channel, totaling ~6MB in select1. These heap allocations go through GC (contributing to 20% GC CPU). **Fix:** (1) Pre-allocate a `RowArena` per stream goroutine. (2) Before sending the row through the channel, clone it into the arena via `CloneRow`. The arena is reset when the goroutine exits. Estimate: ~30 LoC, 1 file modified, 0.25 day. | low | small | REQ001511 | SQB/EX/stream.go |
| REQ001526 | SQB/OP | **SeqScan RowArena reuse across consecutive queries on the same table.** Each query creates a new SeqScan with a fresh RowArena. For consecutive queries reading the same table (select1/2/3 pattern of 1000+ queries on t1), the arena init + grow cycles repeat unnecessarily. **Fix:** When the SeqScan is closed and the table hasn't been modified, keep the largest slab alive and reuse it on the next query's decodeRowBuffered instead of Init() from scratch. Track via a table-generation counter incremented on INSERT/UPDATE/DELETE. Estimate: ~80 LoC, 1 file modified + 1 test, 0.5 day. | low | small | none | SQB/OP/seq_scan.go |
| REQ001530 | SQB/OP | **Limit pushdown for early SeqScan termination.** When the outer plan has a LIMIT clause, the pipeline still produces all matching rows through every operator before Limit discards extras. **Fix:** (1) Thread remaining limit count through `ExecContext.LimitRemaining`. (2) SeqScan checks `ExecContext.LimitRemaining` in its materialization loop and stops early. (3) Propagate limit from `OP.Limit` to ExecContext during plan setup. (4) Add benchmark verifying limit (LIMIT 10) wall time at 100 rows matches 10-row scan (not 100-row scan). Estimate: ~80 LoC, 3 files modified + 1 test, 0.5 day. | medium | small | none | SQB/OP/seq_scan.go, SQB/OP/limit.go, SQB/EX/ex.go, SQB/DT/exec_context.go |
| REQ001531 | SQB/EX | **`tryLazyAggregateTopLevel` must guard against DISTINCT aggregates.** REQ001528 dispatches to `computeLazyAggregate`, which does not implement DISTINCT deduplication. Without a `!agg.Distinct` guard, `SELECT count(DISTINCT col) FROM t1` silently returns the same result as `count(col)` — a correctness bug. Caught by `TestAggregate_Distinct_Count/Sum/Avg` during development. **Fix:** Add `!agg2.Distinct` to the guard condition in `planSelect` so DISTINCT aggregates fall through to the normal HashAggregate operator tree. | high | small | REQ001528 | SQB/EX/planner_select.go, SQB/EX/eval_test.go |
| REQ001532 | SQB/EX | **Plan cache normalization overhead — skip AST clone for queries without literal parameters.** pprof 显示 planWithCache 占 where 查询 52% 分配。对无文字节点查询（star/count_star/order），AST 克隆是纯浪费。 **Fix:** NormalizeForMemo 中预扫描文字节点，无则直接返回原始 stmt——跳过克隆和分配。**效果:** count_star 101KB→61KB（-40KB, -39%）; star -2 allocs。Implemented in MM/memo.go。 | high | small | none | SQB/EX/plan_executor.go, SQO/MM/memo.go |
| REQ001533 | SQB/OP, SQB/DT | **SeqScan per-row decode allocation optimization.** Each SeqScan row decode allocates from its own RowArena (64KB slab per SeqScan) plus per-column Value structs and string copies. For `SELECT * FROM t1` (100 rows, 6 cols), SeqScan accounts for ~85% of 554 allocations. **Fix:** (1) Share pipeline RowArena instead of creating per-SeqScan arena. (2) Pre-size arena to engineBatchSize on first use. (3) Reuse string buffer in `DecodeRowInto` for short strings. Estimate: ~80 LoC, 2 files modified, 0.5 day. | high | medium | REQ001527 | SQB/OP/seq_scan.go, SQB/DT/arena.go |
| REQ001534 | SQB/OP | **Project/FilterProject batch buffer allocation from RowArena.** Project and FilterProject use a heap-allocated batch buffer (`dataBuf`) that grows via `make([]Value, n)`. Even with pooling, cold-start queries allocate fresh buffers. **Fix:** When pipeline RowArena is available, allocate the batch buffer's growth slabs from the arena instead of `make()`. Benchmark verifying star/where/where_like ~20KB reduction. Estimate: ~50 LoC, 2 files modified, 0.3 day. | medium | small | REQ001527 | SQB/OP/project.go, SQB/OP/filter_project.go |
| REQ001535 | SQB/OP | **HashJoin bucket slice pre-allocation to eliminate append growth.** Each HashJoin creates N buckets with initially nil rightRows/hashes slices. The per-row `append` causes slice growth reallocation for each bucket. **Fix:** Count right-side rows during the first pass, pre-allocate buckets to exact sizes before the second pass. Estimate: ~60 LoC, 1 file modified, 0.3 day. | medium | small | none | SQB/OP/hashjoin.go |
| REQ001536 | SQB/AD | **AdaptiveOp compiledFn registry — wire real specializations instead of "no codegen fn registered".** `AdaptiveOp.tryCompile` (`internal/SQB/AD/adqc.go:139-175`) currently always falls through to "no codegen fn registered" because no operator ever registers a `compiledFn`. The cache (`AdqcCache`), the `AdaptiveOp` wrapper, and the `FallbackOp` are all wired but **no compile-time specialization exists**. `rg "compiledFn\|Register.*OpType\|adqc\.Compile" internal/` returns 0 hits. **Fix:** (1) Add `internal/SQB/AD/registry.go` with `Register(opType string, compile func(op any) (CompiledFn, error))` and `Get(opType) (compile func, ok bool)`. (2) Change `AdaptiveOp.tryCompile` to call `registry.Compile(opType, a.Inner)` and store the returned closure in `compiledFn` before setting state to `AdqcCompiled`. (3) For unknown op types, preserve current fallback ("no codegen fn registered"). (4) Add `TestRegistry_RegisterLookup`, `TestAdaptiveOp_CompiledState_AfterRegister`. (5) Benchmark: `BenchmarkAdaptiveOp_CompiledVsInterpreted` showing that registered specializations actually swap the hot path. Estimate: ~120 LoC, 2 files new + 1 modified + 2 tests, 1 day. | high | small | none | SQB/AD/registry.go (new), SQB/AD/adqc.go, SQB/AD/adqc_test.go |
| REQ001537 | SQB/EX/PE | **Pipeline executor (dataflow driver) — fused operator chains execute as a single function instead of per-row volcano dispatch.** Today's `AdaptiveOp.Next()` falls back to `Inner.Next()` which dispatches through the volcano `Operator.Next(ctx) (Row, error)` interface. For `SeqScan → Filter → Project` on 100 rows, that's 300 interface calls + 300 `Row` allocations + 300 `[]Value` allocations. The modern approach (Velox, DuckDB pipeline, Presto native) fuses adjacent operators into one tight inner loop with batch-level exchange. **Fix:** (1) Add `internal/SQB/EX/PE/` cluster with `Pipeline` interface (`Open(ctx) error`, `GetBatch(ctx) (*UT.Batch, error)`, `Close() error`). (2) Add `PipelineExecutor` that walks a tree of `DT.Operator` and groups adjacent fusible operators into a `Pipeline` based on a fusion policy (`SeqScan+Filter+Project`, `HashJoin+Project`, `HashAggregate+Project`, etc.). (3) Pipeline owns the `AdaptiveOp` wrappers — when one is `AdqcCompiled`, the pipeline routes through the registered `compiledFn` (REQ001536). (4) Output batch comes from `sync.Pool`. (5) Top-of-tree adapter (`BatchToRowAdapter`) only runs at the final projection feeding `database/sql`, not per row. (6) Volcano `Operator.Next()` remains the legacy fallback for plans the pipeline executor refuses to fuse (recursive CTE, subquery, non-vectorizable expr). (7) Tests: `TestPipeline_FusesScanFilterProject`, `TestPipeline_FallsBackToVolcano_ForSubquery`. Estimate: ~300 LoC, 4 files new + 1 modified + 3 tests, 2 days. | high | medium | REQ001536 | SQB/EX/PE/pipeline.go (new), SQB/EX/PE/executor.go (new), SQB/EX/PE/fuse.go (new), SQB/EX/PE/registry.go (new), SQB/EX/ex.go |
| REQ001538 | SQB/AD | **Specialization: SeqScan → Filter → Project fused pipeline.** Closes the 2-5× scan/agg gap and the 100-300× allocation gap reported in `AGENTS.md` benchmarks. Reuses existing `VectorizedSeqScan`/`VectorizedFilter`/`VectorizedProject` for the per-batch loop body — does NOT reimplement. **Fix:** (1) Register compile function `compileScanFilterProject(scan *SeqScan, filter Expr, project []int) CompiledFn` in `SQB/AD/registry.go`. (2) At compile time, resolve column ordinals + types + output projection once into closure-captured state. (3) At call time, the closure pulls a columnar batch from the scan, applies filter as a selection vector, projects into the output batch, returns it. Zero per-row `Row` struct allocation. (4) Eligibility gate: filter must be in the vectorizable subset (`CompareInt64ColLit`/`CompareStringColLit`/`CompareFloat64ColLit`/`CompareInt64Cols` hoisted operators in `SQB/EV/eval_vec.go`). Non-vectorizable filter falls back to row-at-a-time. (5) AdaptiveOp measures first 10 invocations and picks the winner per-plan. (6) Benchmark: `BenchmarkSeqFilterProject_FusedVsVolcano` — expect ≥70% reduction in allocs/op and ≥2× speedup on TPC-H Q1-style scans. Estimate: ~150 LoC, 1 file new + 1 modified + 1 benchmark, 1 day. | high | medium | REQ001536, REQ001537 | SQB/AD/specialize_scan_filter_project.go (new), SQB/AD/registry.go, SQB/AD/adqc.go |
| REQ001539 | SQB/AD | **Specialization: HashAggregate → Project fused pipeline.** Closes the GROUP BY hot-path allocation gap. `HashAggregate` today materializes the full group table before emitting any row. Fused version streams partial groups into the output batch as soon as they complete. **Fix:** (1) Register compile function `compileHashAggProject(agg *HashAggregate, project []int) CompiledFn` capturing group-by keys + agg funcs + output projection. (2) Closure emits groups into output `*UT.Batch` incrementally; uses `sync.Pool` for the output batch. (3) First-row latency: emit groups as soon as the input is drained (no "materialize-all-then-emit"). (4) AdaptiveOp cost gate: skip specialization when group count exceeds memory budget (fall back to existing `HashAggregate`). (5) Tests: `TestHashAgg_Fused_GroupByOrderBy_Limit`, `TestHashAgg_Fused_EmitsIncrementally`. (6) Benchmark: `BenchmarkHashAgg_FusedVsVolcano` — expect ≥80% reduction in per-group allocations and ≥10× lower first-row latency on 10K-row GROUP BY. Estimate: ~200 LoC, 1 file new + 1 modified + 2 tests + 1 benchmark, 1.5 days. | high | medium | REQ001536, REQ001537 | SQB/AD/specialize_hashagg_project.go (new), SQB/AD/registry.go |
| REQ001540 | SQB/AD | **Specialization: HashJoin (build-probe) → Project fused pipeline.** Closes part of the `join_equi` 65× gap on the projection side. Per-output-row `Row` allocation in the NLJ/HashJoin projection step is ~40% of `join_equi` allocations (per pprof). **Fix:** (1) Register compile function `compileHashJoinProject(join *HashJoin, project []int) CompiledFn`. (2) Closure captures hash table + probe keys + output projection. (3) At call time, drains the hash table probe results into a columnar output batch in one pass — no per-row `Row` struct. (4) Eligibility gate: single-column equi-join only (multi-column deferred per `SQB.md` open issues). (5) AdaptiveOp cost gate: skip when build-side row count exceeds memory budget. (6) Tests: `TestHashJoin_Fused_EquiJoin_ProjectCols`, `TestHashJoin_Fused_BatchOutputOrder`. (7) Benchmark: `BenchmarkHashJoin_FusedVsVolcano`. Estimate: ~180 LoC, 1 file new + 1 modified + 2 tests + 1 benchmark, 1.5 days. Note: does NOT close the 65× LSM read-amplification gap on `join_equi` itself — that gap is structural (NLJ probes LSM per outer row). Closes the projection-side allocations only. | high | medium | REQ001536, REQ001537 | SQB/AD/specialize_hashjoin_project.go (new), SQB/AD/registry.go |
| REQ001541 | SQB/AG | **Streaming aggregation — emit groups as soon as the group is drained (progressive emission).** Today `HashAggregate` materializes the full group table before emitting the first row, so first-row latency for `SELECT a, count(*) FROM t GROUP BY a LIMIT 10` on 100K input rows is ~50-100ms even though the first 10 groups are complete after ~1ms. Modern engines (Hyper, DuckDB) emit groups as soon as no more input can change them. **Fix:** (1) Add a `StreamingEmit bool` flag on `HashAggregate` (default off, opt-in via REQ001539 fused specialization first). (2) When enabled, on each input batch: (a) for each group touched by the batch, check if a "watermark" group key has been fully processed upstream; (b) emit completed groups to the output batch immediately. (3) Watermark heuristic for OLTP (≤10K input rows): emit groups in the order they're first touched (good-enough for embedded workloads). (4) Bounded memory: emit oldest 10% of groups whenever the partial group table exceeds the budget; correctness preserved by re-emitting if a later batch updates an already-emitted group (UPDATE path only — fine for OLTP append-only). (5) Tests: `TestStreamingEmit_FirstRowLatency`, `TestStreamingEmit_CorrectnessOnUpdates`. (6) Benchmark: `BenchmarkHashAgg_StreamingEmit_Limit10_100KInput` — expect <2ms first-row latency. Estimate: ~250 LoC, 1 file modified + 2 tests + 1 benchmark, 2 days. | medium | medium | REQ001539 | SQB/AG/hashagg.go |
| REQ001542 | SQB/EX/PE | **Pipeline executor cost gate — skip fusion when input batch < 64 rows.** Vectorization overhead (selection-vector allocation, batch pool acquire, columnar-batch setup) beats row-at-a-time execution below ~64 input rows. Modern engines measure this and skip fusion below the threshold. **Fix:** (1) `PipelineExecutor.ShouldFuse(estimatedRows int64) bool` returns false when `estimatedRows < 64` OR when the plan contains non-vectorizable expressions (regex, JSON path, user-defined functions). (2) When `false`, fall back to volcano `Operator.Next()` chain. (3) AdaptiveOp cost measurement (first 10 invocations) records wall-clock time per invocation and picks the faster path per-plan for subsequent invocations. (4) PRAGMA `disable_pipeline_executor` for debugging fallback behavior. (5) Tests: `TestPipeline_CostGate_BelowThreshold_FallsBackToVolcano`, `TestPipeline_CostGate_NonVectorizableExpr_FallsBackToVolcano`. (6) Benchmark: `BenchmarkPipeline_CostGate_TinyTable` — expect fused path skipped, no overhead. Estimate: ~80 LoC, 1 file modified + 2 tests, 0.5 day. | medium | small | REQ001537, REQ001538, REQ001539, REQ001540 | SQB/EX/PE/executor.go, SQB/EX/PE/pipeline.go |
| REQ001543 | ~~**CANCELLED 2026-07-12** — duplicate of REQ001506. BloomFilter (`SQB/UT/bloom.go`, 59 lines, power-of-2 sized, double-hash, ~1% FPR) and HashJoin integration (`SQB/OP/hashjoin.go:106` field + `:533` `NewBloomFilter(rightTotal)` build) already shipped. Bloom pre-filter gap already closed; only the cost-gate threshold (skip when build < 1000 rows) remains unimplemented — captured inline in the original commit. Row removed from TBD; no work needed.~~ | — | — | — | — | — |
| REQ001544 | ~~**CANCELLED 2026-07-12** — already-shipped. EX path already short-circuits via `tryStatsCountShortcut` (REQ001514, `planner.go:498`) and emits one row via `NewValuesOp` (`planner_select.go:71`). Lazy fallback via `tryLazyAggregateTopLevel` (REQ001528) covers stats-unavailable case. Baseline `count_star/razor` = 49,156 ns/op, 112 allocs (sqlcmp); in-process `BenchmarkExecutorCountStar` = 34,130 ns/op, 315 allocs. The 100KB / 112 allocs in sqlcmp is `tests/sqlcmp/slt/razor_driver.go:268-283` per-row `make([]Value,1) + copy` overhead, not the EX path. EX side is already closed.~~ | — | — | — | — | — |
| REQ001545 | ENG/LS | **Merge-heap hot path: inline branch-free key comparison instead of `bytes.Compare`.** `mergeHeap.less` (`internal/ENG/LS/api.go:469-471`) calls `bytes.Compare` on every heap pop during N-way SST merge. For typical 8-64 byte keys this involves a length-then-byte-loop ABI call. **Fix:** (1) Add `ltKeyInlined(a, b []byte) bool` that short-circuits on the first 8/16 bytes: read the first `min(len(a), len(b), 16)` bytes as `uint64` (LE) and compare as integers before falling back to per-byte. (2) For equal prefixes beyond 8B, compare next 8B in a tight loop. (3) Length tie-break: shorter key < longer key (lexicographic). (4) Wire into `mergeHeap.less` and any other `bytes.Compare(<, <)` site on the read path (`sst_reader.go:decodeBlock`, `borrowed_iter.go`). (5) Tests: existing sst_iterator_test.go covers correctness; add `BenchmarkMerge_LessInlinedVsBytesCompare` on 100/1K/10K row merges — expect 5-15% throughput gain. Estimate: ~50 LoC, 2 files modified + 1 benchmark, 0.3 day. | medium | small | none | ENG/LS/api.go, ENG/LS/sst_reader.go |
| REQ001546 | ENG/LS | **Page-cache `Get`: defer `visited.Store` until after `RLock` release.** `PageCache.Get` (`internal/ENG/LS/page_cache.go:92-104`) holds `s.mu.RLock` across `slot.visited.Store(1)`. On multi-core workloads, concurrent readers hitting the same slot bounce the cache line between cores (false sharing on `visited atomic.Int32`). **Fix:** (1) In `Get`, replace `slot.visited.Store(1)` with local-variable capture of visited state, then `defer` the store after `s.mu.RUnlock`. (2) Same pattern in `Put`. (3) Verify correctness: `visited` is LRU-K hint, not correctness state — eventual consistency is sufficient. (4) Test: `TestPageCache_RaceReadersNoFalseSharing` running 8 goroutines × 1M reads on the same slot, measuring p99 latency vs current implementation. (5) Benchmark: `BenchmarkPageCache_Get_Contended` before/after — expect 20-40% p99 latency reduction under 8-core concurrent reads. Estimate: ~10 LoC, 1 file modified + 1 test + 1 benchmark, 0.1 day. | medium | small | none | ENG/LS/page_cache.go |
| REQ001547 | ENG/LS | **Page-cache `Put`: split eviction from slot reuse — never run `evict()` under the write lock.** `PageCache.Put` (`internal/ENG/LS/page_cache.go:106-141`) holds `s.mu.Lock` across `s.evict()`, which is O(cap) linear scan (`page_cache.go:143-160`). With 64MB / 4KB = 16384 slots per shard, every Put averages half-shard scan inside the write lock — serializes all readers on that shard. **Fix:** (1) Add `s.findEmptySlot() *pageEntry` returning the first invalid slot without mutating state, lock-free. (2) `Put` calls `findEmptySlot` under Lock; if nil, fall back to current eviction. (3) Move full eviction to a separate background goroutine: when `s.size == s.cap`, mark one stale slot as `tombstoned` (atomic state), evict in worker pool off-line. (4) Add `s.tombstoneSlot()` returning a slot marked for lazy eviction. (5) Keep correctness: evict-before-reuse fence ensures `Put` never overwrites a live slot. (6) Tests: `TestPageCache_PutUnderConcurrentReaders`, `TestPageCache_TombstonedSlotEvictedAsync`. (7) Benchmark: `BenchmarkPageCache_Put_P99Latency` — expect 50%+ p99 reduction under mixed Put/Get load. Estimate: ~60 LoC, 1 file modified + 2 tests + 1 benchmark, 0.5 day. | medium | medium | none | ENG/LS/page_cache.go, ENG/LS/cache.go |
| REQ001548 | ENG/LS | **Compaction I/O bandwidth quota via existing `rate_limiter.go` — protect query latency from compaction bursts.** `compactionManager.runJob` (`internal/ENG/LS/compaction.go:569`) runs to completion without yielding disk bandwidth to query reads. `rate_limiter.go` already exposes a token-bucket interface but is currently only used by admission control on writes. **Fix:** (1) Create a `compactionLimiter` (separate token bucket) sized at 70% of detected peak disk throughput during init. (2) `compactionQueue` reads are gated: each block read in `subcompact.go::RunSubCompaction` and `compaction.go::Run.RunPartial` acquires a token before `ReadAt`. (3) On `RequestCompaction` from `MaybeCompact`, dynamically resize the bucket to 30% if `L0 count >= 8` (stress mode). (4) PRAGMA `compaction_io_quota` (default 0.7, range [0, 1]) exposes tuning. (5) `compactionDebtTest` regression: under burst load, query p99 latency should not exceed 2x baseline. (6) Tests: `TestCompaction_QuotaProtectionQueryLatency` runs INSERT burst + concurrent SELECT, measures query p99. (7) Benchmark: `BenchmarkCompaction_QuotaEnabledVsDisabled` on mixed workload. Estimate: ~80 LoC, 3 files modified + 2 tests + 1 benchmark, 1 day. | medium | medium | REQ001506 | ENG/LS/compaction.go, ENG/LS/subcompact.go, ENG/LS/rate_limiter.go |
| REQ001549 | ENG/LS | **L0-burst adaptive subcompaction concurrency — auto-scale `SetSubCompactorConcurrency` from manifest state.** `compactionManager` exposes `SetSubCompactorConcurrency` (`internal/ENG/LS/compaction.go:523`) but `MaybeCompact` (`:608`) does not call it based on `manifest.L0Count()`. After an INSERT burst (e.g., bulk load), L0 grows past the stall threshold (~4 files) but subcompaction concurrency stays at the configured minimum — extending recovery time. **Fix:** (1) In `MaybeCompact`, before scheduling a level-0 compaction job, check `manifest.L0Count()`. (2) Compute target concurrency: `min(maxConcurrent, ceil(L0Count / 2))` — 4 L0 files → 2 subcompactors, 8 files → 4. (3) Call `SetSubCompactorConcurrency(target)` before `requestCompaction(0)`. (4) Reset to configured default when L0 count returns below threshold. (5) Add metric `compaction_l0_subcompactor_scale` (gauge) and log line at level Info. (6) Tests: `TestCompaction_L0BurstScalesConcurrency` (insert 8x flushthreshold rows, verify subcompactor N observed via counter). (7) Benchmark: `BenchmarkCompaction_BulkLoadRecovery` — measure wall time from burst-end to L0 < 2 before/after adaptive scaling. Estimate: ~40 LoC, 2 files modified + 1 test + 1 benchmark, 0.4 day. | medium | small | REQ001506 | ENG/LS/compaction.go, ENG/LS/manifest.go |
| REQ001550 | SQB/EX | **`(*Planner).hasIndexOnTable` nil deref on select4.test and select5.test.** SLT runner panics with `invalid memory address or nil pointer dereference [recovered, repanicked]` at `internal/SQB/EX/join_order.go:69` when running `corpus/test/select4.test` (and `select5.test`). Stack: `(*Planner).hasIndexOnTable` -> `(*plannerAsStatsProvider).HasIndex` -> `SQO/JN.N3` -> `SQO/JN.MultiStart`. The function receives a nil `*TableMeta` (or analogous descriptor), reads field at offset 0x38, and crashes. Discovered 2026-07-12 during SLT baseline run (see `docs/development/reports/SLT-FAILURE-CLUSTER-2026-07-12.md`). **Fix:** (1) Add nil check at `hasIndexOnTable` entry; on nil return `(false, nil)` (have-no-index) so N3 falls back to NLJ. (2) Audit `join_order.go:13` `plannerAsStatsProvider.HasIndex` for the same nil guard. (3) Add `TestN3_NoIndexNilSafe` that calls N3 with an empty tables slice. (4) Add `TestSLT_Select4_NoPanic` mirroring the corpus drive. Estimate: ~25 LoC, 1-2 files modified + 2 tests, 0.2 day. | high | small | none | SQB/EX/join_order.go, SQB/EX/planner_select.go |
| REQ001551 | SQB/EX | **Stream lifetime use-after-free panics in `corpus/test/index/between/1/slt_good_0.test`.** SLT runner trips Go runtime fatal `found pointer to free object` when crossing from `evidence/slt_lang_update.test` into `index/between/1/slt_good_0.test` (very early in alphabetical order). REQ001511 fixed an analogous stream row lifetime issue; the panic here suggests a remaining code path still hands caller-stored row pointers after the underlying iterator / batch has been advanced or closed. Discovered 2026-07-12. **Fix:** (1) Reproduce reliably by running `go test -tags slt_corpus -count=1 -v -run 'TestSLT_PerFile/index/between/1/slt_good_0' ./tests/sqlcmp/slt/`. (2) Run with `-race -d=checkptr` to localize. (3) Audit `SQB/EX/stream.go` for residual zero-copy paths that hand out pointers after the row slot has been recycled. Likely fix: prefer owned-copy semantics in 1-2 hot callbacks; or extend row lifetime via arena (REQ001527) where benign. (4) Add `TestStream_NoUseAfterFree` that runs `evidence/slt_lang_update.test` immediately followed by `index/between/1/slt_good_0.test` and asserts no fatal. (5) Add `TestStream_ConcurrentAdvance_DoesNotInvalidateBorrowedRows`. Estimate: ~80 LoC, 1 file modified + 2 tests, 0.7 day. | high | medium | REQ001511 | SQB/EX/stream.go, tests/sqlcmp/slt/per_file_test.go |
| REQ001552 | SQB/EV | **`InSubquery` correctness — `evidence/in1.test` still produces 12 IN failures despite `EvalInValue` implementing 3VL.** Investigation 2026-07-12: `EvalInValue` (`internal/SQB/EV/eval.go:973`) and `evalInSubquery` (`:1092`) already implement full SQL three-valued-logic (hadNull propagation, NULL target, empty-subquery, etc.). Unit tests (`TestEvalInList_ThreeValuedLogic_REQ001552`, `internal/SQB/EV/eval_in_three_valued_test.go`) lock all four shapes (A/B/C/D) and pass. The 12 SLT failures must therefore be in a downstream path that *calls* `EvalInValue` — likely: (a) `expression rewriter` (CSE / SQO/RW) collapsing `x IN (sub)` into a different form before evaluation, (b) `Eval` (legacy any-typed path at `eval.go:343`) instead of `EvalValue`, or (c) subquery row materialization dropping NULL rows. **Fix:** (1) Add `slog` trace at `EvalInValue` entry to confirm whether in1.test really hits it; flag if it does not. (2) If eval never reached: trace at `Eval` / `EvalValue` (`:369`) for the same query. (3) Likely culprit candidates: `internal/SQB/EV/eval_vec.go` vectorized batch path for IN-list, or `internal/SQB/OP/filter_pushdown.go` rewriting IN-subquery before evaluation. (4) Once located, add regression test mirroring in1.test schema (4 rows + 1 NULL) and verify the 12 failures clear to 0. Estimate: ~120 LoC, 1-2 files modified + 1 integration test, 1 day. | high | small | none | SQB/EV/eval.go, SQB/EV/eval_vec.go, SQB/OP/filter_pushdown.go |
| REQ001553 | SQB/EX | **Multi-column projection column-order flips on DISTINCT/AGG/CROSS JOIN.** Pattern dominates `random/aggregates` (130 files, ~36.5K fail total). Examples from SLT output: `got [81 | 253], want [253 | 81]`; `got [64 | NULL], want [NULL | 64]`; `got [-5395 | 3], want [3 | -5395]`. The values are correct, but column order in the result row is reversed vs the SELECT list. Discovered 2026-07-12. **Fix:** (1) Locate the column-order reversal: candidates are `SQB/OP/project.go` (output projection list order), `SQB/OP/hashagg.go` (group-by key ordering), `SQB/EX/planner_select.go` (column resolution order during plan), or `SQB/AG/aggregate.go` (aggregate output layout). (2) Reproduce with a minimal `TestProjection_ColumnOrderPreserved` query like `SELECT DISTINCT col1+col2, col2-col1 FROM t ORDER BY 1, 2`. (3) Identify the bug; typical root cause: hash-table iteration order leaking into output or Plan builder visiting SELECT items in reverse. (4) Fix and re-run SLT. (5) Add a determinism guard: for tests, optionally `PRAGMA iteration_order = sorted` to remove nondeterminism. Estimate: ~80 LoC, 1-2 files modified + 2 tests, 0.5 day. | high | medium | none | SQB/EX/planner_select.go, SQB/OP/project.go, SQB/OP/hashagg.go |
| REQ001554 | SQB/EX | **`SELECT count(*) FROM t WHERE col=<value>` returns column-count rather than filtered-row-count.** `evidence/in1.test` shows values like `WHERE x=3` returning 18 (≈t1's column count), `WHERE x=1` returning 8, `WHERE y='unknown'` returning 16, when all expected values are 1-3. Strongly correlates with REQ001553 (multi-column projection order issue) — same code path appears to read a count from a different column instead of filtering `t`. Discovered 2026-07-12. **Fix:** (1) Add `TestCountStar_WhereConstFalseish` using `SELECT count(*) FROM t1 WHERE x=9999` expecting `0`. (2) Run SLT `evidence/in1.test` and confirm only the IN/NOT IN cases (now fixed by REQ001552) remain. (3) If still failing, audit `SQB/OP/seq_scan.go` predicate pushdown to ensure `WHERE col=<lit>` is correctly threaded into the SeqScan's filter rather than being placed on a column that doesn't exist. (4) Likely also fixed by REQ001553; merge evidence if so. Estimate: ~30 LoC, 1 test, 0.1 day if not absorbed by REQ001553. | high | small | REQ001552, REQ001553 | SQB/OP/seq_scan.go, SQB/EX/planner_select.go |

## Deferred

| ID | Reason |
| --- | --- |
| REQ001497 | Planner to SQO/CO migration blocked by EX ↔ CO import cycle. Current PlanBuilder callback in CO.Optimizer provides equivalent functionality. Requires a shared types package to break the cycle. |