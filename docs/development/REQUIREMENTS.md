## TBD

| ID | Subsystem | Requirement | Priority | Effort | Deps | Touches |
| REQ000316 | SQL | Incremental materialized views (auto-maintained aggregation views with query routing) | medium | L | iter-08 (operators), iter-12 (catalog) | `SQL/EX/matview.go` (new); `ENG/LS` triggers on view base tables |
| REQ000321 | TXN | Deterministic Simulation Testing framework (FoundationDB-style scheduled threads + simulated clock + simulated disk; millions of random schedules) | high | XL | iter-17 (chaos), iter-13 (recovery) | new `tests/dst/` framework; subsystem-aware simulated drivers |
| REQ000537 | ENG/LS | Sharded memtable (per-prefix skiplist) — split active memtable into N sharded skiplists keyed by `hash(key) % N` (N=16 default); directory routes lookup; flush merges shards into L0 SST; eliminates global CAS contention on head pointer under high write concurrency. New `internal/ENG/LS/sharded_memtable.go` + `shard.go` | high | L | iter-04 (skiplist), iter-27 (escape-hinted arena) | `ENG/LS/sharded.go` (new); `ENG/LS/engine.go` `Write`/`NewIterator`; `Options.MemTableShards` (default 16, 1 = legacy single-shard mode) |
| REQ000538 | ENG/LS | Zero-copy iterator with borrowed buffers — `mergeIterator.Next()` and `sstIter` return `unsafe.Pointer` references into buffer-pool blocks; iterator registers with engine epoch manager on creation, deregisters on Close; only the public API boundary (`Engine.Get`/`Iterator.Key`/`Iterator.Value` for external callers) materializes owned copies. Eliminates ~6 allocs/row in merge iterator. New `internal/ENG/LS/borrowed_iter.go` + `epoch_iter.go` | high | L | iter-27 (epoch manager), iter-19 (sync.Pool buffers) | `ENG/LS/api.go` `mergeIterator` rewrite; new `ENG/LS/epoch_iter.go`; `TXN/LC/qsbr.go` integration for iterator epoch registration |
| REQ000539 | MEM/BF | Sharded buffer pool with per-shard clock hands — replace single global `map[uint64]*bufferSlot`+`sync.RWMutex` with N=32 shards; `hash(blockID) % N` routes lookup; each shard owns its own sub-map, `hand`, and eviction loop; eliminates global mutex contention under concurrent reads. New `internal/MEM/BF/sharded_bp.go` | high | L | iter-02 (buffer pool), REQ000199 (sharded mutex idea, partial), REQ000303 (W-TinyLFU) | `MEM/BF/sharded_bp.go` (new) + `bf.go` refactor; `Options.BufferPoolShards` (default 32) |
| REQ000542 | WAL/FL | Group commit pipeline — txns waiting on `Sync()` join a group; when the first tx arrives or a 50µs deadline fires, the entire group fsyncs together; each waiter is unblocked with its assigned LSN. Builds on existing async fsync (REQ000301) and io_uring (REQ000295); target: 10x fsync throughput under burst commit load. New `internal/WAL/FL/group_commit.go` | high | L | REQ000184 (write barrier), REQ000301 (async fsync) | `WAL/FL/group_commit.go` (new) + `fl.go` refactor; `Options.GroupCommitTimeout` (default 50µs) |
| REQ000543 | SQL/EX | Shape-specialized fast paths — extend REQ000311/313 with shape-driven specialization that captures hot path inline as small specialized Go funcs (not full JIT); specialize per plan shape: `Filter{int64_col_lit_eq}`, `Project{cols=N}`, `HashAggregate{group_by_int64}`, etc.; registered in `codegen_ops.go`; benchmarks must show >1.5x over the interpreted path. Deferred: XL effort, iter-29. | medium | XL | REQ000311 (codegen framework), REQ000313 (ADQC) | `SQL/EX/codegen/shapes/` (new); `SQL/EX/adqc_specialize.go` (new); `SQL/EX/shape_bench_test.go` (new) |
| REQ000546 | SQL/EX | Vectorized string columns via unsafe pointer + length prefix — replace `[]string` columns with `[]byte` raw + `[]uint32` lengths (or `[]struct{Ptr uintptr; Len uint32}`); eliminates string-header copy and enables real SIMD string compare (memcmp); backward-compat path via `Batch.StringValue(col,row)` accessor. Deferred: L effort, iter-29. | medium | L | REQ000149 (batch memory), REQ000310 (SIMD) | `SQL/EX/batch.go` `Column` for text/varchar/blob; `SQL/EX/eval_vec.go` `compareStringColsVec`; `SQL/EX/simd_dispatch.go` runtime detection |
| REQ000547 | TXN/MV | Per-NUMA arena pools — extend the generational arena (REQ000064/305) with per-NUMA-node arena pools; thread first-touch policy (REQ000309) places new arenas on the calling node; reclaim drains per-node; eliminates contention on global `reclaimMu` and `arenaPool` under high transaction throughput on multi-socket hosts | medium | M | REQ000064 (generational arena), REQ000305 (epoch reclaim), REQ000309 (NUMA) | `TXN/MV/arena.go` `numaArenaPool`; `TXN/LC/epoch.go` per-node drain lists; `ENG/NM/numa.go` allocator policy |
| REQ000549 | MEM/BF | Block-level MVCC version tagging — buffer pool slots carry an optional `versionRange [startTS, endTS]` field; `BufferPool.GetWithTS(blockID, readTS)` returns the page whose version range covers `readTS` (or the page whose range is the latest, then we filter via the version chain at read time); enables cross-transaction cache sharing with snapshot consistency. Deferred: XL effort, iter-29. | low | XL | iter-02 (buffer pool), REQ000539 (sharded), REQ000307 (MV-OCC) | `MEM/BF/mvcc_slot.go` (new) + `bf.go` `GetWithTS`; `TXN/SN/snapshot.go` registers `readTS` on Get |
| REQ000556 | SQL/PS+EX | `CREATE VIRTUAL TABLE ... USING module_name` — BNF line 23; no T_VIRTUAL token, no parser path, no executor. SQLite-compatible modules (fts5, rtree) cannot be registered. Add lexer tokens (`T_VIRTUAL`, `T_USING` already exists for JOIN), parser path (`parseCreateVirtualTable` + `VirtualTableStmt` AST), executor module-dispatch loop. Deferred: XL effort, iter-29. | high | XL | iter-08 (catalog), REQ000250 (BTree) | `SQL/LX/lx.go`+`token.go` (T_VIRTUAL); `SQL/PS/ps.go` `parseCreateVirtualTable` + `SQL/PS/ast.go` `VirtualTableStmt`; `SQL/EX/ex.go` `buildWriterOp` route; new `SQL/EX/virtual.go` registry |
| REQ000571 | ENG/LS | SST page cache — replace `os.ReadFile` with block-level cache + sync.Pool. Currently `readFromSST` (`engine.go:193`) and `mergeIterator.init` (`api.go:246`) call `os.ReadFile` for every SST file on every read/iterate. For a 100 MB SST this allocates 100 MB per read. New `internal/ENG/LS/page_cache.go`: fixed-size cache (default 256 MB), 4 KB blocks keyed by `(fileID, blockOffset)`, clock-sweep eviction, `sync.Pool` for page buffers. `mergeIterator` consults cache before file read. Must handle concurrent access (read path is hot). Benchmarks: measure allocs/op and ns/op for sequential and random reads before/after | critical | L | iter-04 (L0 SST), iter-02 (buffer pool) | `ENG/LS/page_cache.go` (new); `ENG/LS/engine.go` `readFromSST` — replace `os.ReadFile` with cache lookup; `ENG/LS/api.go` `mergeIterator.init` — same; `ENG/LS/compaction.go` — compaction reads can bypass cache (bulk reads) |
| REQ000583 | SQL/PS | Add visitor pattern to AST — currently AST traversal relies on manual type-switch in `PL/memo.go:107-223` with no exhaustiveness check. Missing node types (e.g. `IntervalLiteral`, `WindowFunc`, `RaiseFunc`) silently produce different memo hashes. Fix: add `Accept(Visitor)` to `Expr` and `Stmt` interfaces; create `BaseVisitor` with default no-op implementations; implement `HashVisitor` that writes to `hash.Hash` for memo keys. Refactor `memo.go` to use the visitor. Future visitors: `TypeChecker`, `ConstantFolder`, `PlanSerializer`. Test: verify all AST node types are covered (compile-time exhaustiveness via interface satisfaction) | medium | L | iter-07 (parser) | `SQL/PS/ast.go` — add `Accept` methods; new `SQL/PS/visitor.go`; `SQL/PL/memo.go` — refactor to use visitor |
| REQ000586 | SQL/EX | Thread ExecContext through operators (eliminate globals) — `eval.go:383` `currentSubqueryPlanner`, `ex.go:32` `currentSessionID`, `store.go:98-112` `tables`/`storeSchemas`/`viewRegistry` are all package-level globals. This makes concurrent Executor instances unsafe. Fix: add `ExecContext` struct holding session ID, planner, params, store references; thread it through operators via `Operator.SetContext(ctx)` or pass in `Next(ctx)`. Start with `currentSubqueryPlanner` (most dangerous global). Test: run two Executor instances concurrently on different tables; verify no cross-contamination | medium | L | iter-08 (SQL execute) | `SQL/EX/ex.go` — new `ExecContext` struct; `SQL/EX/eval.go` — remove `currentSubqueryPlanner` global; all operators — accept context |

## DONE

| ID | Subsystem | Requirement | Iteration |
|---|---|---|---|
| REQ000401 | SQL/EX | Scalar function `quote(X)` — SQL literal rendering; strings single-quoted with escape, BLOBs as X'hex' | iter-26 |
| REQ000402 | SQL/EX | Scalar function `random()` — pseudo-random int64; exclude MIN_INT64 | iter-26 |
| REQ000403 | SQL/EX | Scalar function `randomblob(N)` — N-byte random BLOB | iter-26 |
| REQ000404 | SQL/EX | Scalar function `replace(X,Y,Z)` — string substitution; Y="" returns X unchanged | iter-26 |
| REQ000405 | SQL/EX | Scalar function `round(X[,Y])` — round to Y decimal places; Y default 0; Y<0 → 0 | iter-26 |
| REQ000406 | SQL/EX | Scalar function `rtrim(X[,Y])` — trim right; default Y=" " | iter-26 |
| REQ000407 | SQL/EX | Scalar function `sign(X)` — -1/0/+1 or NULL for non-numeric | iter-26 |
| REQ000408 | SQL/EX | Scalar function `soundex(X)` — soundex encoding; "?000" for non-ASCII / NULL | iter-26 |
| REQ000409 | SQL/EX | Scalar function `sqlite_source_id()` — fixed string for v1 | iter-26 |
| REQ000410 | SQL/EX | Scalar function `sqlite_version()` — fixed string for v1 | iter-26 |
| REQ000411 | SQL/EX | Scalar function `total_changes()` — cumulative row-change count since connection open | iter-26 |
| REQ000413 | SQL/EX | Scalar function `unhex(X[,Y])` — hex → BLOB; X invalid → NULL; Y ignored | iter-26 |
| REQ000414 | SQL/EX | Scalar function `unicode(X)` — code point of first char; NULL → NULL | iter-26 |
| REQ000415 | SQL/EX | Scalar function `unistr(X)` — backslash-escape decoder | iter-26 |
| REQ000416 | SQL/EX | Scalar function `unlikely(X)` — no-op pass-through | iter-26 |
| REQ000417 | SQL/EX | Scalar function `zeroblob(N)` — N-byte BLOB of 0x00 | iter-26 |
| REQ000418 | SQL/EX | Scalar function `coalesce(X,Y,...)` — variadic NULL-skipping | iter-26 |
| REQ000419 | SQL/EX | Scalar function `ifnull(X,Y)` — 2-arg NULL coalesce | iter-26 |
| REQ000420 | SQL/EX | Scalar function `length(X)` — code-point count | iter-26 |
| REQ000421 | SQL/EX | Scalar function `like(X,Y[,Z])` — 2-arg pattern match | iter-26 |
| REQ000422 | SQL/EX | Scalar function `lower(X)` — ASCII lower-case | iter-26 |
| REQ000423 | SQL/EX | Scalar function `nullif(X,Y)` — NULL-on-equal | iter-26 |
| REQ000424 | SQL/EX | Scalar function `printf(FORMAT,...)` — alias for format | iter-26 |
| REQ000425 | SQL/EX | Scalar function `substr(X,Y[,Z])` — 1-based, negative start | iter-26 |
| REQ000426 | SQL/EX | Scalar function `substring(X,Y[,Z])` — substr alias | iter-26 |
| REQ000427 | SQL/EX | Scalar function `trim(X[,Y])` — both-sides, default space | iter-26 |
| REQ000433 | SQL/EX | Scalar function `upper(X)` — ASCII upper-case | iter-26 |
| REQ000435 | SQL/PS | CREATE TRIGGER parser (BEFORE/AFTER, FOR EACH ROW, BEGIN...END body) | iter-27 |
| REQ000436 | SQL/PS+PL+EX | Recursive CTE (WITH RECURSIVE flag, FROM-subquery parser fix, inner query dispatch) | iter-27 |
| REQ000437 | SQL/EX | Full SQL aggregate DISTINCT support — `SUM/AVG/MIN/MAX/GROUP_CONCAT(DISTINCT col)` now dedup before aggregating (parity with `COUNT(DISTINCT col)`); NULLs are excluded from the distinct set per SQLite semantics; parser routes DISTINCT through the IDENT aggregate path for `GROUP_CONCAT` | iter-27 |
| REQ000438 | SQL/EX | Scalar function eval error routing fix | iter-26 |
| REQ000439 | SQL/PS | EXPLAIN statement support | iter-26 |
| REQ000440 | SQL/EX | VACUUM and ANALYZE not routed — fixed in buildWriterOp | iter-26 |
| REQ000441 | SQL/EX | ALTER TABLE ADD COLUMN not implemented — fixed in buildWriterOp | iter-26 |
| REQ000442 | SQL/EX | SLT gap survey — 30/42 common patterns identified; gaps tracked in individual REQs | iter-26 |
| REQ000443 | WAL/WR | `encodeRecord` allocation reduction (4→2 allocs/record, 217ns→148ns, 168B→160B) | iter-27 |
| REQ000443b | SQL/EX | Fix negative_literal eval pipeline bug (case-sensitive Lookup, UnaryExpr column extraction) | iter-27 |
| REQ000444 | SQL/EX | UPDATE deadlock fix — `numericFloat` in `eval.go` didn't handle Go `int` type from `?` placeholders, causing UPDATE expressions (like `x + 1`) to return nil; `equalValue` and `Eval(*PS.Param)` normalized to return `int64`; store UPDATE path now captures `oldRow` via `cloneRow` before `applyUpdate` and calls `maintainIndexesOnUpdate` after store insert | iter-27 |
| REQ000445 | SQL/EX | NULL three-valued logic: `<`, `<=`, `>`, `>=`, `=`, `!=` comparisons with NULL operand → return NULL (UNKNOWN), not a boolean | iter-26 |
| REQ000447 | SQL/EX | `count(DISTINCT x)`, `avg(DISTINCT x)`, `sum(DISTINCT x)` — DISTINCT aggregate semantics verified; NULLs skipped, dedup works | iter-26 |
| REQ000451 | SQL/PS | `CREATE TEMP VIEW` parser — `parseCreateView` now accepts optional `TEMP`/`TEMPORARY` keyword between `CREATE` and `VIEW`; `CreateViewStmt.Temporary` field set accordingly; added `T_TEMP`/`T_TEMPORARY` tokens | iter-27 |
| REQ000452 | SQL/PS | `INSERT OR REPLACE` / `REPLACE INTO` parser — `parseInsert` handles `INSERT OR ROLLBACK/ABORT/FAIL/IGNORE/REPLACE` via lexeme comparison (not hard keywords); `parseReplace` handles standalone `REPLACE INTO`; added `ConflictAction` type and field to `Insert` AST | iter-27 |
| REQ000453 | SQL/EX | Scalar `IN (literal-list)` — `SELECT 1 IN (2)`, `SELECT 1 NOT IN (2)` return 1 row with correct boolean. Same fix as REQ000503 (Values operator + `evalIn` path) | iter-28 |
| REQ000454 | SQL/PL | JOIN duplicate rows — `PL/memo.go` `writeStmt` missing `Joins`, `GroupBy`, `Having`, `SubqueryFrom` fields in `Select` serialization; caused different join queries (INNER ON vs CROSS) to share the same plan memo key, reusing stale ON-filtered plan; added missing field serialization | iter-27 |
| REQ000455 | SQL/EX | Subquery planner store propagation — `evalExists`/`evalScalarSubquery`/`evalInSubquery` use `newSubqueryPlanner(outer)` which propagates the store from the outer executor; store-backed correlated subqueries (EXISTS, IN, scalar, NOT EXISTS) work. Verified via `TestReq455_SubqueryPlannerStorePath` | iter-28 |
| REQ000456 | SQL/EX+ENG/LS | ALTER TABLE self-deadlock — `execAddColumn`/`execDropColumn`/`execRename` held `storeMu.Lock()` then called `registerStoreSchemaWithFK` which re-acquired `storeMu`. Fix: `currentCatalog` changed from raw field to `atomic.Pointer[ls.Catalog]` (Catalog() lock-free), `registerStoreSchemaWithFKLocked` created for callers already holding storeMu. ALTER TABLE no longer deadlocks. Verified via `TestReq456_AlterTableNoDeadlock` | iter-28 |
| REQ000457 | SQL/EX | Package-level EX state complete cleanup — `UnregisterAll()` clears `triggerReg` and `tableTriggers` maps | iter-28 |
| REQ000458 | SQL/EX | BETWEEN NULL semantics — `evalBetween` returns nil (UNKNOWN) when any operand is NULL, matching SQL standard. Also fixed: SELECT without FROM now applies WHERE filter. Verified via `TestReq458_BetweenNullSemantics` | iter-28 |
| REQ000459 | SQL/EX | NULL three-valued logic in IN/NOT IN — `evalIn` returns nil (UNKNOWN) when target is NULL; tracks NULL list elements and returns nil instead of false when no match found and any list element was NULL; `evalInSubquery` same NULL tracking for subquery results | iter-27 |
| REQ000460 | SQL/EX | UPDATE executor wrong results — basic UPDATE patterns verified: simple SET, expression SET, multi-column SET, WHERE filtering, full-table UPDATE, no-match no-error. Verified via `TestReq460_UpdateCorrectResults` | iter-28 |
| REQ000463 | SQL/EX | `PRAGMA` statements — `PRAGMA journal_mode`, `PRAGMA synchronous`, `PRAGMA cache_size` routed via `buildWriterOp` → `NewPragma` (no-op stub); planner `planPragma` handles `integrity_check`, `cache_size`, `journal_mode`, `synchronous`, `user_version`. `TestCoverage_pragma` and `TestCoverage_pragmaWithValue` pass | iter-28 |
| REQ000464 | SQL/EX | `LIMIT` / `OFFSET` — `Limit` and `Offset` operators in `intermediate.go`; planner supports both `LIMIT n OFFSET m` and `OFFSET m LIMIT n` syntax orders. `TestCoverage_limitOffset`, `TestTBD_RemainingBugfixes/REQ000464*` pass | iter-28 |
| REQ000465 | SQL/EX | `EXISTS` subquery — `evalExists` uses `newSubqueryPlanner(outer)` (not bare `NewPlanner()`); correlated EXISTS with store-backed tables works via `SetSubqueryPlanner`. `TestCoverage_existsSubquery`, `TestTBD_RemainingBugfixes/REQ000465` pass | iter-28 |
| REQ000466 | SQL/EX | `CASE WHEN` complex expressions — `evalCase` supports both simple CASE (`CASE expr WHEN val THEN`) and searched CASE (`CASE WHEN cond THEN`); `expr` and `val` both go through `Eval()` for arbitrary expression support. `TestCoverage_caseWhen`, `TestTBD_RemainingBugfixes/REQ000466` pass | iter-28 |
| REQ000467 | SQL/EX | `GROUP_CONCAT` with DISTINCT and ORDER BY — DISTINCT dedup via `map[interface{}]bool`; custom separator via 2nd arg; empty group returns NULL. `TestCoverage_groupConcatDistinctSep`, `TestCoverage_groupConcatWithNull`, `TestTBD_RemainingBugfixes/REQ000467` pass | iter-28 |
| REQ000468 | SQL/EX | `HAVING` clause — `Filter` operator applied after aggregation in planner; covers both filtering and 0-rows edge cases. `TestCoverage_aggGroupByHaving`, `TestCoverage_havingEmpty`, `TestExecutorHaving`, `TestTBD_RemainingBugfixes/REQ000468` pass | iter-28 |
| REQ000469 | SQL/EX | `DISTINCT` on non-primary-key columns — `Distinct` operator dedup via stored row key; works on arbitrary column sets. `TestCoverage_distinct`, `TestTBD_RemainingBugfixes/REQ000469` pass | iter-28 |
| REQ000470 | SQL/EX | `ORDER BY` with expressions — `Sort` operator evaluates each `OrderItem.Expr` via `Eval()`, supporting arbitrary expressions like `a+b*2+c*3`. PK-order optimization skips Sort when ORDER BY matches PK. `TestCoverage_orderByExpr`, `TestTBD_RemainingBugfixes/REQ000470` pass | iter-28 |
| REQ000471 | SQL/EX | `CAST` to various types — `evalCast` supports INTEGER, BIGINT, FLOAT/REAL, TEXT, DECIMAL/NUMERIC, BOOL, BLOB; type coercion from int64/float64/string/bool. `TestCoverage_castIntToText`, `TestTBD_RemainingBugfixes/REQ000471` pass | iter-28 |
| REQ000472 | SQL/EX | `COALESCE` with many arguments — variadic implementation iterates `e.Args` returning first non-NULL; supports 2+ args. `TestCoverage_coalesce`, `TestSpecialForms/COALESCE`, `TestTBD_RemainingBugfixes/REQ000472` pass | iter-28 |
| REQ000473 | SQL/PS | PRAGMA parser support — `PragmaStmt` AST + parser already existed; PRAGMA is a no-op in executor; TestPhase1_PragmaNoOp passes | iter-28 |
| REQ000475 | SQL/EX | `DELETE` with `ORDER BY` / `LIMIT` — parser accepts trailing ORDER BY/LIMIT/OFFSET after WHERE; `buildWriterOp` wraps scan with Sort/Limit/Offset before Delete operator. Verified via `TestReq475_DeleteOrderByLimit` | iter-28 |
| REQ000477 | SQL/EX | `INSERT` with `RETURNING` clause — INSERT already implemented RETURNING in both in-memory and store paths (accumulates resultRows, returns via Next()). `Exec` RETURNING path counts Next() calls. Verified via `TestReq477_InsertReturning` | iter-28 |
| REQ000479 | SQL/PS | `CREATE INDEX IF NOT EXISTS` — parser accepts `IF NOT EXISTS` clause; executor skips registration if index already exists; `CreateIndexStmt.IfExists` field added | iter-28 |
| REQ000480 | SQL/EX | `DROP INDEX IF EXISTS` — parser accepts `IF EXISTS` clause; executor no-ops if missing; `DropIndexStmt.IfExists` field added; buildWriterOp routing from Phase 0 | iter-28 |
| REQ000482 | SQL/PS | `AUTOINCREMENT` keyword accepted — `T_AUTOINCREMENT` token added to lexer + keyword map; `ColDef.Autoincrement bool` field added; parser accepts after PRIMARY KEY | iter-28 |
| REQ000483 | SQL/EX | `DEFAULT` values on omitted INSERT columns — `fillDefaults` already applied in both Insert paths (in-memory and store). Verified via `TestReq483_DefaultOnInsertOmittedColumns` | iter-28 |
| REQ000484 | SQL/EX | `CHECK` constraint enforcement — `CreateTable.Next` now extracts `ColDef.Check` expressions and stores them in `storeSchema.checks`; `validateCheck` enforces on INSERT/UPDATE. Verified via `TestReq484_CheckConstraintEnforcement` | iter-28 |
| REQ000485 | SQL/EX | `UNIQUE` constraint enforcement on INSERT — single-column UNIQUE enforced; NULLs allowed per SQL standard. Verified via `TestReq485_UniqueConstraintEnforcement` | iter-28 |
| REQ000486 | SQL/EX | `ON CONFLICT` conflict resolution — `INSERT OR IGNORE` conflict handling added in `writers.go` `checkUnique` error path with `ConflictActionIgnore → continue` skip. In-memory storeSchema registration via `registerInMemorySchema` enables constraint enforcement without backing store | iter-28 |
| REQ000489 | SQL/EX | `REINDEX` routed to executor — `buildWriterOp` already routes `*PS.ReindexStmt` → `NewReindex`. Verified via `TestReq489_ReindexRouting` | iter-28 |
| REQ000491 | SQL/EX | `DROP INDEX` routed to executor — `buildWriterOp` already routes `*PS.DropIndexStmt` → `NewDropIndex`. Verified via `TestReq491_DropIndexRouting` | iter-28 |
| REQ000497 | SQL/EX | DROP TABLE IF EXISTS accepted by parser | iter-28 |
| REQ000498 | SQL/EX | `ALTER TABLE RENAME COLUMN old TO new` — parser adds `RENAME COLUMN` path; executor renames column in schemas map; `AlterTableStmt.NewName` field added | iter-28 |
| REQ000499 | SQL/EX | `ALTER TABLE DROP COLUMN` — `execDropColumn` and `execDropColumnInMemory` now update existing row data in `tables` to remove the dropped column; schema updates correctly. Verified via `TestReq499_AlterTableDropColumn` | iter-28 |
| REQ000500 | SQL/EX | EXPLAIN QUERY PLAN parsing + execution | iter-28 |
| REQ000501 | ENG/LS | Hidden-PK UPDATE/Delete no longer allocates new rowid on every mutation — `extractPKForUpdate()` reuses storeKey suffix; unbounded row accumulation and iterator exhaustion fixed. slt_lang_update.test 27/31 pass (was hanging), select4.test 1055/3857 pass within 30s timeout (was hanging on INSERT) | iter-28 |
| REQ000502 | SQL/EX | SELECT scalar subquery no extra rows — `TestTBD_RemainingBugfixes/REQ000502_scalar_subq_no_dup` passes. Subquery in CASE expression previously produced duplicate rows; planner store propagation and subquery re-execution for store-backed SeqScan fix | iter-28 |
| REQ000503 | SQL/EX | Scalar `IN (literal-list)` — `SELECT 1 IN (2)`, `SELECT 1 NOT IN (2)` return 1 row with correct boolean via `Values` operator + `evalIn` path. Verified via `TestCoverage_scalarIn` | iter-28 |
| REQ000504 | SQL/EX | DELETE executor row count — DELETE in-memory and store paths correctly track `d.rows` (via `len(toDelete)` for batch, `d.rows++` for store) and return via `RowsAffected()`. Verified via `TestReq504_DeleteRowCount` | iter-28 |
| REQ000511 | SQL/EX | INSERT ... ON CONFLICT DO UPDATE implemented | iter-28 |
| REQ000512 | SQL/EX | INSERT/UPDATE/DELETE RETURNING returns full resultRows | iter-28 |
| REQ000513 | SQL/EX | FK validation wired into Update.Next | iter-28 |
| REQ000514 | SQL/EX | FK validation wired into Delete.Next | iter-28 |
| REQ000515 | SQL/EX | `fillDefaults` type coercion — `coerceDefault` coerces DEFAULT values to match column type (int/float/bool/text) via token type switch | iter-28 |
| REQ000516 | SQL/EX | validateCheck called from Update.Next | iter-28 |
| REQ000517 | SQL/EX | checkUnique called from Update.Next | iter-28 |
| REQ000518 | SQL/EX | `RETURNING *` expanded — `expandReturningStar` converts StarExpr to all columns; `colNameForReturning` assigns column names; applied in INSERT/UPDATE/DELETE RETURNING evaluation | iter-28 |
| REQ000519 | SQL/PS | Composite PRIMARY KEY accepted — parser uses first column as PK, registers remaining columns as UNIQUE constraint | iter-28 |
| REQ000520 | SQL/PS | CREATE TABLE AS SELECT parser + executor | iter-28 |
| REQ000521 | SQL/EX | `OFFSET m LIMIT n` reversed syntax fixed — parser tracks `OffsetFirst` flag; planner wraps in correct order | iter-28 |
| REQ000522 | SQL/EX | `Distinct` operator after `Limit` pushdown — confirmed correct in planner.go:649-650 (Distinct before Limit in plan tree). When LIMIT < distinct count the Distinct still runs on full set before Limit caps | iter-28 |
| REQ000523 | SQL/EX | `GROUP_CONCAT(x, sep)` configurable separator — 2nd arg parsed as separator expression in `AggregateFunc.Separator`; `evalAggregateOver` evaluates it once; default `,` when nil | iter-28 |
| REQ000524 | SQL/EX | `COUNT(*)` on empty set returns 0 — confirmed via TestBugfix_CountEmptySet; aggregate returns `int64(0)` for 0-row group | iter-28 |
| REQ000525 | SQL/EX | Correlated subquery IndexScan injection | iter-28 |
| REQ000526 | SQL/EX | `EXPLAIN` returns plan tree — `ExplainStmtOp` renders operator tree with `id/parent/notused/detail` columns; `EXPLAIN QUERY PLAN` mode via `formatPlanTree`; `buildWriterOp` routes `*PS.ExplainStmt` to `NewExplain`. Verified via `TestBugfix_Explain_ReturnsPlan` | iter-28 |
| REQ000527 | SQL/EX | `ANALYZE t1` updates row count — `analyze.go` `analyzeTable` scans entire table, counts rows, stores `RowCount` in `ColumnStats` via `cat.PutStats`; `estimateCost` uses updated statistics. Verified via analyze test suite | iter-28 |
| REQ000528 | SQL/EX | `VACUUM` implemented — `ManualCompact` via store when available; no-op without store; TestVacuum_Integration and TestVacuum_WithStore pass | iter-28 |
| REQ000529 | SQL/PS | `INDEXED BY index_name` / `NOT INDEXED` in SELECT — `parseFrom` accepts `INDEXED BY name` and `NOT INDEXED`; `IndexHint` AST with `IndexedBy` field (empty string = NOT INDEXED, nil = no hint). Verified via `TestParseIndexedBy` | iter-28 |
| REQ000531 | SQL/EX | ALTER TABLE self-deadlock fixed in in-memory helpers | iter-28 (audit) |
| REQ000532 | TXN/MV | Arena.promote() initOldMu taken before nil check | iter-28 (audit) |
| REQ000533 | SQL/LX | `<>` (not-equal) operator — lexer now recognizes `<>` as a single `T_NE` token; added `<>` case in `scanOperator` before standalone `<` | iter-28 |
| REQ000534 | SQL/EX | VIEW WHERE clause merge — `planSelect` now merges outer WHERE with view definition's WHERE using AND (`PS.BinaryExpr` with `T_AND`); preserves outer Cols/OrderBy/Limit/Offset/Distinct/GroupBy/Having. Verified via `TestReq534_ViewWhereMerge` | iter-28 |
| REQ000535 | SQL/EX | `REPLACE INTO` — `Insert` operator gets `conflictAction` field; `buildWriterOp` propagates `ConflictActionReplace`; `Next()` removes conflicting rows via PK match before inserting replacement row; `removeConflicting` (with `storeSchema`) and `removeConflictingInMemory` (with `tablePKs` map) cover both store and in-memory paths | iter-28 |
| REQ000536 | SQL/EX | Correlated subquery re-execution for store-backed SeqScan — `evalQualifiedName` fallback searches the outer chain first, then the current row so `t.id` resolves to the parent row's `id` column; `runSubqueryPlan`'s `defer pl.root.Close()` ensures the store-backed SeqScan creates a fresh iterator per outer-row evaluation. Covers EXISTS, IN, scalar correlated subqueries with engine-backed store path | iter-28 |
| REQ000540 | ENG/LS | L0 buffer cache — l0Cache LRU keyed by blockID; API ready for engine wiring | iter-28.2 |
| REQ000541 | WAL/FL | Batched LSN reservation — Reserve(n) atomically claims [start,start+n) | iter-28.2 |
| REQ000544 | SQL/EX | O(1) `Row.Lookup` via lazy-built `colIndex map[string]int` — `buildColIndex()` builds lowercase key map on first `Lookup()` call; eliminates O(N) `strings.EqualFold` outer-chain scan per Ident reference | iter-28 |
| REQ000545 | SQL/EX | Selection vector runs — SelRange{Start,End} inclusive-end compact representation; selToRanges/rangesToSel helpers | iter-28.2 |
| REQ000548 | SYS/ST | Engine-level prepared statement cache — LRU + ref-counted, Get/Put/Release/Clear/Size | iter-28.2 |
| REQ000550 | SQL/PL | Predicate cache — LRU keyed by string, plan as interface{} | iter-28.2 |
| REQ000551 | TXN/MV | Time-traveling snapshot pruning — GCVersionChain/GCAllChains return prune candidates below oldestActiveReadTS | iter-28.2 |
| REQ000552 | ENG/LS | Adaptive memtable size — targetSize atomic.Int64 + SetTargetSize/TargetSize plumbing | iter-28.2 |
| REQ000554 | SQL/EX | Parallel IN-list evaluation — evalInListBatch with int64 sort+binsearch or string hash set | iter-28.2 |
| REQ000555 | TXN/VL | Lock-free transaction slot recycling — Treiber stack (atomic.Pointer CAS) replaces Mutex-guarded freeList | iter-28.2 |
| REQ000557 | SQL/PS+EX | ATTACH/DETACH DATABASE parser + executor stub returning ErrMultiDatabaseNotSupported | iter-28.2 |
| REQ000558 | SQL/PS+EX | `UPDATE ... ORDER BY ... LIMIT ...` — `Update` AST extended with `OrderBy`/`Limit`/`Offset`/`OffsetFirst` fields; `parseUpdate` calls `parseTrailingClauses` after WHERE; `buildWriterOp` wraps scan with Sort/Limit/Offset before Update operator. Verified via full test suite | iter-28 |
| REQ000559 | SQL/PS+TXN | `BEGIN [DEFERRED|IMMEDIATE|EXCLUSIVE] [TRANSACTION]` — `T_DEFERRED`/`T_IMMEDIATE`/`T_EXCLUSIVE` tokens added; `parseBegin` handles optional mode and TRANSACTION keyword; `BeginTX.Mode` field set; `buildWriterOp` routes to `NewNoop()`. Verified via `go test -race` | iter-28 |
| REQ000560 | SQL/PS+EX | `RAISE(IGNORE|ABORT|ROLLBACK|FAIL, 'msg')` — `T_RAISE` token + `RaiseFunc` AST (`Action`/`Message`); `parsePrimary` detects `RAISE(`; `evalRaise` returns `ErrTriggerAbort`/`ErrIgnoreRow` sentinels. Verified via `go test -race` | iter-28 |
| REQ000561 | SQL/PS+EX | Foreign key `MATCH name` + `[NOT] DEFERRABLE [INITIALLY DEFERRED | INITIALLY IMMEDIATE]` — `T_MATCH`, `T_DEFERRABLE`, `T_INITIALLY` tokens; `ForeignKeyConstraint.{Match,Deferrable,Initially}` fields; column-level and table-level parser handling. Verified via 6 parser tests | iter-28 |
| REQ000562 | SQL/PS+EX | CREATE TABLE WITHOUT ROWID parser support — WithoutRowID flag on CreateTableStmt | iter-28.2 |
| REQ000563 | SQL/PS+EX | `INSERT INTO t DEFAULT VALUES` — parser `parseInsertTail` accepts `DEFAULT VALUES` alternative; `Insert.DefaultValues bool` AST field; executor emits one row with column defaults (or NULLs for nullable). Verified via parser + executor tests | iter-28 |
| REQ000564 | SQL/PS+EX | Top-level `VALUES (1,'a'),(2,'b')` — `ValuesStmt` AST with `Rows [][]Expr`; `parseValues` function; `T_VALUES` dispatch in `Parse()`; `ValuesRows` multi-row operator in planner + `buildWriterOp` routing. Verified via `go test -race` | iter-28 |
| REQ000565 | SQL/PS+EX | `COLLATE collation_name` on indexed columns and ordering terms — `T_COLLATE` token + keyword; `IndexedColumn` struct with `Collation` field; `CreateIndexStmt.IndexedColumns` uses `[]IndexedColumn`; `OrderItem.Collation` field; `CREATE INDEX` and `ORDER BY` COLLATE acceptance. Verified via 4 parser tests | iter-28 |
| REQ000566 | SQL/PS | `CREATE INDEX ... ON t (cols) WHERE expr` (partial index) — `CreateIndexStmt.Where Expr` field; `parseCreateIndex` accepts `WHERE expr` after column list. Verified via 2 parser tests | iter-28 |
| REQ000567 | SQL/PS+EX | `LIKE ... ESCAPE expr` — `BinaryExpr.Escape Expr` AST field; `parseBinary` and `parseNotLike` accept `ESCAPE expr` after LIKE pattern; `matchLike` honors escape character, treating `\%` and `\_` as literal. `T_ESCAPE` token + keyword registered. Verified via `TestParseLikeEscape`, `TestCoverage_likeEscape`, `TestEvalLikeEscape` | iter-28 |
| REQ000568 | SQL/EX | `DECIMAL(P,S)` precision and scale semantics — `ColDef.{Precision,Scale}` fields; `storeSchema.{precision,scale}` arrays populated in CREATE TABLE and ALTER TABLE; `validateDecimal()` called in all INSERT/UPDATE write paths; `FormatDecimal` clamps/validates. Verified via PS test suite | iter-28 |
| REQ000569 | SQL/PS | `INDEXED BY index_name` / `NOT INDEXED` on UPDATE and DELETE — `Update.IndexHint` and `Delete.IndexHint` fields; `parseUpdate` and `parseDelete` accept trailing INDEXED BY/NOT INDEXED. Verified via parser tests | iter-28 |
| REQ000570 | SQL/PS+TXN | `COMMIT` / `END [TRANSACTION]` — `parseCommit()` function; `T_COMMIT`/`T_END` dispatch in `Parse()`; `CommitTX.NewNoop` in `buildWriterOp`. Verified via 4 parser tests | iter-28 |
| REQ000640 | SQL/EX | NOT IN subquery returning single nullable value treats NOT IN as equality comparison — `SELECT 1 FROM t1 WHERE 1 NOT IN (2)` returns 6 rows instead of 3 on a 3-row table. `evalIn` for NOT IN incorrectly includes all rows when the predicate is false (should exclude matching rows). The NOT IN path must return true when the value is NOT in the list, not when it IS in the list. Discovered via evidence/in2.test | iter-28.3 |
| REQ000641 | SQL/EX | DELETE FROM view fails — resolve view to base table in buildWriterOp — `DELETE FROM view1 WHERE x>0` returns "table not registered for storage: view1". SQLite allows DELETE on simple views (single-table, no aggregates/joins). Route view DELETE through the underlying table's delete path. Discovered via evidence/slt_lang_createview.test | iter-28.3 |
| REQ000642 | SQL/EX | REPLACE INTO with engine store fails to remove conflicting rows — after `REPLACE INTO t1(x,y) VALUES(1,'b')` on a table with `x=1,'a'`, `SELECT x,y FROM t1 WHERE x=1` returns 1 row with stale value `'a'` instead of `'b'`. The `removeConflicting` step in the REPLACE path either doesn't execute or doesn't persist. Discovered via evidence/slt_lang_replace.test | iter-28.3 |
| REQ000643 | SQL/PS | Trigger body semicolons not handled — CREATE TRIGGER fails with "expected expression, got ;" — `CREATE TRIGGER t1r1 UPDATE ON t1 BEGIN SELECT 1; END;` fails with "expected expression, got ;". The parser must accept semicolons as statement separators inside the trigger body (BEFORE/AFTER ... BEGIN ... END). The trigger body is a compound statement list, not a single expression. Discovered via evidence/slt_lang_droptrigger.test | iter-28.3 |
| REQ000644 | SQL/EX | GROUP BY qualified column (cor0.col2) in SELECT list fails to resolve — `SELECT + cor0.col2 FROM tab2 AS cor0 GROUP BY cor0.col2` returns "eval error". The evaluator cannot resolve `cor0.col2` in the GROUP BY or SELECT expression when the table has an alias. Fix: resolve qualified names through the alias chain before evaluation. Discovered via random/groupby SLT corpus | iter-28.3 |
| REQ000645 | SQL/EX | DISTINCT + constant expression (no FROM) returns multiple rows — `SELECT DISTINCT 9 + + - 10 + + 68` returns 0 rows instead of 1. When the SELECT list has no FROM clause and the expression is a constant, the Distinct operator must not drop the single computed row. The issue is likely in how `Values` operator (for no-FROM queries) interacts with `Distinct`. Discovered via random/expr SLT corpus | iter-28.3 |
| REQ000646 | SQL/EX | Column-to-column comparison returns wrong results — col comparators not supported — `WHERE (e>c OR e<d)` returns 0 rows (should return ~16). `WHERE x>0` returns 2 rows instead of 1 (evidence tests). The root cause is likely in vectorized or scalar comparison between two column references: comparsion operators (`>`, `<`, `=`, `<>`) comparing a column to another column (not a literal) evaluate incorrectly, producing NULL (row excluded) or doubling the row set. Affects all WHERE clauses with column-to-column comparisons, including JOIN predicates and correlated subquery filters. Discovered via select1.test, evidence/slt_lang_createview.test, evidence/slt_lang_dropview.test | iter-28.3 |
| REQ000647 | SQL/EX | NOT BETWEEN AND returns wrong results when upper bound matches — `WHERE d NOT BETWEEN 110 AND 150` returns 290 cells (58 rows × 5 cols) instead of 145 cells (29 rows × 5 cols) on a 30-row table. The NOT BETWEEN filter does not correctly exclude rows within the range; instead it doubles the result. Likely `evalBetween` mis-handles the NOT case or the range bounds. Discovered via select1.test L124 pattern | iter-28.3 |
| REQ000648 | SQL/EX | IS NULL filter on table with many rows returns wrong count — `WHERE a IS NULL` returns 4 rows instead of 8 from a 30-row table. The NULL check on a column reference either misses some rows (false negatives) or the underlying data has incorrect NULL tracking. Affects all `IS NULL` and `IS NOT NULL` filter predicates on column references. Discovered via select2.test L126 | iter-28.3 |
| REQ000649 | SQL/EX | abs() function on column WHERE abs(b-c) > 0 causes nil deref — `WHERE abs(b-c), b, a+b*2+c*3+d*4` (combined with other expressions) produces 48 cells instead of 24 (2× rows). The `abs()` scalar function combined with other column expressions in a WHERE clause produces incorrect row counts. Discovered via select2.test L116 | iter-28.3 |
| REQ000650 | SQL/EX | NULL IN () returns NULL instead of false on in-memory path — `SELECT null IN ()` returns [NULL] instead of [0] (false). Per SQL standard, `NULL IN (empty_list)` is false (0), not NULL, because the empty set contains no elements including NULL. The `evalIn` function should handle the empty-list case by returning `int64(0)` when the left operand is NULL and the list is empty. Discovered via evidence/in1.test L55 | iter-28.3 |
