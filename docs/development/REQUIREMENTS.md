## TBD
| ID | Subsystem | Requirement | Priority | Effort | Deps | Touches |
| --- | --- | --- | --- | --- | --- | --- |
| REQ001303 | WAL/RP | **WAL checkpoint modes: passive, full, truncate, restart.** SQLite supports four checkpoint modes. Passive: copy as many frames as possible without blocking writers. Full: wait for all readers to finish, then copy remaining. Truncate: same as Full, then truncate -wal to zero. Restart: same as Full, then rewind wal-index to start. **Fix:** (1) Add `walCheckpoint(mode int)` API. (2) Implement Passive: copy frames up to reader-locked position, do not wait. (3) Implement Full: take ckpt lock, wait for read locks to drain, copy remaining. (4) Implement Truncate: Full + ftruncate(-wal, 0). (5) Implement Restart: Full + reset mxFrame in shm. (6) Surface via `PRAGMA wal_checkpoint(MODE)` query PRAGMA. (7) Add `TestWalCheckpoint_AllModes`. Estimate: ~200 lines, 2 files, 1 day. | high | medium | REQ001299 | WAL/RP/checkpoint.go (new), SQB/EX/planner.go |
| REQ001312 | SQB/WT + ENG/LS | **WITHOUT ROWID: clustered-key SST layout.** Currently hard-rejected at `SQB/WT/writers_ddl.go:198`. On an LSM, the analog is: primary key columns ARE the SST row key, eliminating the secondary rowid pointer. **Fix:** (1) In SQB/WT DDL, accept `WITHOUT ROWID` and store flag in table metadata. (2) In ENG/LS, for WITHOUT ROWID tables, key SST entries by composite (pk_col1, pk_col2, …) instead of (rowid, …). (3) Secondary indexes reference PK directly (no rowid indirection — see REQ001313). (4) UPDATE that changes PK = delete + insert. (5) Add `TestWithoutRowid_BasicInsertSelect`, `TestWithoutRowid_CompositePK`, `TestWithoutRowid_UpdateChangesPK`. Estimate: ~300 lines, 4 files, 2 days. | high | large | none | SQB/WT/writers_ddl.go, ENG/LS/sst_writer.go, ENG/ID/id.go, SQB/DT/schema.go |
| REQ001313 | SQB/WT + ENG/LS | **WITHOUT ROWID: secondary index on clustered key (no rowid pointer).** When the table is WITHOUT ROWID, secondary index entries store the full PK rather than a rowid, so lookups go directly to the data without a second hop. **Fix:** (1) In ENG/ID, when creating a secondary index on a WITHOUT ROWID table, the value part of the index entry is the full PK (concatenated). (2) In IndexScan for WITHOUT ROWID, the seek returns (index_key, pk) — use pk directly to fetch the row, no rowid→row translation. (3) Verify no regression for rowid tables. (4) Add `TestWithoutRowid_SecondaryIndex_NoRowidHop`, `TestWithoutRowid_IndexOnlyScan`. Estimate: ~120 lines, 2 files, 0.75 day. | high | medium | REQ001312 | ENG/ID/id.go, SQB/OP/indexscan_strategy.go |
| REQ001314 | SQB/EX + ENG | **ANALYZE: per-column NDV + rowcount + null count + min/max, persisted to catalog (covers REQ001315, REQ001316, REQ001317, REQ001318).** All four metrics are computed in a single scan, stored in `razor_stat1` catalog table, and loaded into `pl.StatsCatalog` on Engine.Open. NDV is the cornerstone of selectivity estimation (already used by REQ001218 OR-to-IN and REQ001252 stats propagation). rowcount/null_count are required by selectivity estimators. min/max enable the stats-propagation rule that derives `t2.c ∈ [min(t1.c), max(t1.c)]` from join equality. **Fix:** (1) Add ANALYZE statement dispatch in SQB/EX. (2) On ANALYZE [table], run a single full or sampled scan (full for tables < 1M rows, sampled 1% reservoir otherwise) that computes for each column: NDV, rowcount, null_count, min, max. (3) Define `razor_stat1` catalog table: `(table TEXT, col TEXT, ndv INT, rowcount INT, null_count INT, min BLOB, max BLOB)`. (4) ANALYZE writes via the same writer path as user tables; create razor_stat1 on first ANALYZE if absent. (5) In SYS.Open, after loading catalog, SELECT * FROM razor_stat1 and populate the in-memory `pl.StatsCatalog`. (6) For nullable columns, NULL is excluded from min/max. (7) Add `TestAnalyze_ComputesNDV`, `TestAnalyze_LargeTable_Sampling`, `TestAnalyze_ComputesRowCount`, `TestAnalyze_ComputesNullCount_NotNullCol`, `TestAnalyze_ComputesMinMax`, `TestAnalyze_NullExcludedFromMinMax`, `TestAnalyze_PersistsAcrossRestart`, `TestStats_WiredOnOpen`, `TestStats_Queries_UseLoadedNDV`. Estimate: ~350 lines, 4 files, 1.6 days. | high | medium | none | SQB/EX/analyze.go (new), ENG/LS/iter.go, SQB/WT/writers_admin.go, ENG/CT/catalog.go, SQB/EX/ex.go, SYS/SY/sy.go |
| REQ001423 | ENG/LS + SQB/EX | **B-tree / LSM block-level stats: min, max, NDV for range query planning.** Range scans (e.g. `WHERE age > 30 AND age < 50`) currently traverse the full SeqScan. LSM blocks have per-column min/max that can be used for early block pruning. **Fix:** (1) In ENG/LS SST block stats, expose per-column `min`/`max` during block construction. (2) In SeqScan, when a range predicate is present, skip blocks where the predicate's range is disjoint from the block's column min/max. (3) Expose via `TableStats` column-level `min`/`max` in catalog. (4) Add `TestSeqScan_BlockPruning_RangePredicate` verifying skipped blocks. Estimate: ~120 lines, 2 files, 0.75 day. | high | small | REQ001420 | ENG/LS/sst_block.go, SQB/OP/operators.go |
| REQ001558 | SQB/OP | **Pre-allocate SeqScan projected row metadata + arena Data.** `SeqScan.cloneRow` with projection pushdown (operators.go:536-546) allocates 3 slices (Cols, Types, Data) + 1 map per projected row. For a 100K-row projection query = 400K allocations. **Fix:** (1) Pre-compute `projCols []string`, `projTypes []LX.TokenType` in `NewSeqScan` (schema-derived, constant). (2) Use `RowArena.AllocRow()` for Data slice — bump-pointer allocation from pooled slab. (3) Pre-compute `projColIndex map[string]int` once, store as field. (4) Add `BenchmarkSeqScan_Project_NoAlloc` verifying zero allocations. Estimate: ~100 lines, 1 file, 0.5 day. | high | small | none | SQB/OP/operators.go |
| REQ001567 | SQB/EV | **Pre-allocate function argument buffer in Project/Filter.** `eval.go:1552,1567` — every datetime/json function call allocates `args := make([]any, len(e.Args))`. **Fix:** (1) Add `fnArgBuf []any` field to Project and Filter operators. (2) Initialize in constructor with capacity = max expected function args (typically 5-10). (3) Pass `fnArgBuf` into eval path for function calls. (4) Add `BenchmarkScalarFunc_NoAlloc`. Estimate: ~80 lines, 2 files, 0.5 day. | high | small | none | SQB/EV/eval.go, SQB/OP/operators.go |
| REQ001568 | SQB/EV | **Cache inner table column set in correlated subquery resolution.** `eval.go:129` — `set := make(map[string]struct{}, len(cols))` allocated per correlated subquery evaluation in `extractCorrelatedColumns`. **Fix:** (1) Extend `subqueryColRefCache` to store inner table column set alongside correlated columns. (2) Cache key includes subquery pointer + inner table name. (3) Add `BenchmarkCorrelatedSubquery_NoAlloc`. Estimate: ~60 lines, 1 file, 0.25 day. | high | small | REQ001470 | SQB/EV/eval.go |
| REQ001579 | SQB/EX | **Pre-allocate params slice in ExecContext.** `propagateParams()` in ex.go allocates `asAnySlice(args)` creating `[]any` copy every query execution. **Fix:** (1) Add `paramBuf []any` field to `ExecContext`. (2) Initialize with capacity in `QueryAll`/`Exec` caller. (3) Reuse via `paramBuf = paramBuf[:0]` per execution. (4) Add `BenchmarkExecContext_NoParamAlloc`. Estimate: ~40 lines, 1 file, 0.25 day. | medium | small | none | SQB/EX/ex.go |
| REQ001580 | SQB/EX | **Pre-allocate Value slice from any params in ExecContext.** `valueFromAnySlice` in ex.go allocates `[]Value` for every parameterized query execution. **Fix:** (1) Add `valueParamBuf []Value` field to `ExecContext`. (2) Initialize with capacity = max expected params. (3) Reuse via `valueParamBuf = valueParamBuf[:0]` per execution. (4) Add `BenchmarkExecContext_NoValueParamAlloc`. Estimate: ~40 lines, 1 file, 0.25 day. | medium | small | none | SQB/EX/ex.go |
| REQ001614 | SQB/EX | **Default vectorized execution — remove tryVectorizePlan branching.** Currently `tryVectorizePlan` is an optional optimization applied after planning. The executor has two paths: vectorized (BatchProducer) or scalar (DT.Operator). This creates unnecessary complexity and maintenance burden. **Fix:** (1) In `planSelect`, always produce a `BatchProducer` tree by calling `tryVectorizePlan` as part of planning, not as an afterthought. (2) Replace `isEligible` with a direct `transformOp` call that always succeeds (wrapping in `ScalarBatchProducer` when no native vectorized impl exists). (3) Remove the `if vectorized { ... } else { ... }` branching in `QueryAll`, `Query`, `Exec`, `QueryStream`. (4) Remove the `isVectorized` flag from `CompiledPlan`. (5) Remove `operatorBatchAdapter` — all operators go through `BatchProducer`. Estimate: ~300 lines, 5 files, 3 days. | high | large | REQ001601, REQ001602, REQ001603, REQ001604 | SQB/EX/ex.go, SQB/EX/vec_transform.go, SQB/EX/stream.go, SQB/EX/planner_select.go, SQB/UT/scalar_batch_producer.go |
| REQ001621 | SQB/EX | **Planner: prefer vectorized join operators in cost model.** Current planner (`planner_select.go:1239-1318`) selects HashJoin > HashCrossJoin > MergeJoin > NestedLoopJoin based on equi-join key detection, but cost estimation does not distinguish between vectorized and row-based join performance. For select4's small tables (~100-130 rows each), vectorized joins have lower overhead per match due to batched output. **Fix:** (1) In `estimateCost`, add a `Vectorized` bonus factor (e.g., 0.7x cost) for join operators that support vectorized execution. (2) When multiple join strategies have similar row-based cost, prefer the one with vectorized support. (3) Update `pickCheaperScan` logic to also consider join vectorization eligibility. (4) Add `TestPlanner_VectorizedJoinPreference` verifying vectorized joins selected when costs are comparable. Estimate: ~60 lines, 2 files, 0.5 day. | medium | small | REQ001618, REQ001619, REQ001620 | SQB/EX/planner.go, SQB/EX/cost.go |
| REQ001622 | SQB/OP | **VectorizedHashJoin: parallel build phase.** Current `VectorizedHashJoin.buildHashTable` (`op_vec_join.go:67-157`) reads and hashes all build-side rows sequentially. For select4 queries where build sides can have thousands of rows (after filter pushdown), parallel build distributes hash table construction across CPU cores. **Fix:** (1) Accept a `WorkerPool` in `VectorizedHashJoin` constructor. (2) In `buildHashTable`, partition build batches across workers: each worker drains a subset of batches and builds a local `HashTable`. (3) After all workers complete, merge local hash tables into the global table using `ht.Probe` callback. (4) Use `sync.WaitGroup` for synchronization. (5) Add `TestVecHashJoin_ParallelBuild` and `BenchmarkVecHashJoin_ParallelBuild`. Estimate: ~120 lines, 1 file, 1 day. | medium | small | REQ001618 | SQB/OP/op_vec_join.go, SQB/UT/parallel.go |
| REQ001623 | SLT/select4 | **select4 vectorized join regression test.** Add a regression test that replays select4 join queries and verifies both correctness (via MD5 hash) and vectorized path coverage. **Fix:** (1) Create `select4_vec_test.go` in `tests/sqlcmp/slt/` that replays all join queries from `select4.test` (join0 through join499). (2) For each query, assert that the result hash matches SQLite reference. (3) Assert that the plan uses vectorized join operators (check `BatchProducer` type assertions on transformed tree). (4) Report per-query pass/fail with hash diagnostics on failure. (5) Run as part of `before-commit-cases.sh`. Estimate: ~100 lines, 1 file, 0.5 day. | high | small | REQ001618, REQ001619, REQ001620 | tests/sqlcmp/slt/select4_vec_test.go (new) |
| REQ001624 | SLT/select4 | **select4 join query corpus regression test.** Replay the full select4 corpus setup and run all join queries (join0-join499) against the vectorized executor, asserting MD5 hash matches SQLite reference. **Fix:** (1) In `tests/sqlcmp/slt/`, create `select4_join_vec_test.go` that replays the select4 table setup (t1-t9, ~1000 rows total). (2) Parse `select4.test` for all `query` statements (join0 through join499). (3) For each query, execute via `RazorDriver`, compute result hash with `rowsort`, compare against `sqlite3` reference hash. (4) Report pass/fail per query with hash diagnostics on mismatch. (5) Run as part of `before-commit-cases.sh` pre-commit gate. This validates end-to-end correctness of REQ001618-REQ001620 against the full select4 join corpus. Estimate: ~200 lines, 1 file, 1 day. | high | medium | REQ001618, REQ001619, REQ001620 | tests/sqlcmp/slt/select4_join_vec_test.go (new) |
| REQ001628 | SQB/OP | **Pure batch HashCrossJoin — no row intermediary.** Current `VectorizedHashCrossJoin` (`hashcrossjoin_vec.go`) wraps the row-based `HashCrossJoin` and batches output via `NextBatch` → `hcj.Next()` → `rowToBatch`. The row-based HashCrossJoin reads rows one at a time. **Fix:** (1) HashCrossJoin is used for small-table equi-joins (< 1024 rows) and already materializes all matches upfront. (2) Implement a pure batch version that builds the hash table from the small build batch, then probes with the probe batch in columnar fashion. (3) Emit output batches directly without per-row `Row` construction. (4) Update `transformHashCrossJoin` to use the pure batch constructor. (5) Remove the row-based `HashCrossJoin` wrapper. Estimate: ~150 lines, 1 file, 1 day. | high | medium | REQ001614 | SQB/OP/hashcrossjoin_batch.go (new) |
| REQ001629 | SQB/OP | **VectorizedHashJoin: RIGHT/FULL outer join support.** Current `VectorizedHashJoin` (`op_vec_join.go`) supports INNER and LEFT outer joins via `JoinKind`. RIGHT and FULL outer joins require emitting unmatched probe rows (RIGHT) or both unmatched build and probe rows (FULL). select4 join tests exercise FULL outer join patterns. **Fix:** (1) After probe exhaustion in `NextBatch`, iterate probe batches to find rows that had zero matches (no hash table lookup hit). (2) For RIGHT outer: emit unmatched probe rows with NULL-padded build columns. (3) For FULL outer: emit both unmatched build (LEFT) and unmatched probe (RIGHT) rows. (4) Track probe-side match status during `probePhase` via a `matchedProbe []bool` array indexed by probe row position. (5) Add `TestVecHashJoin_RightOuter`, `TestVecHashJoin_FullOuter`. Estimate: ~180 lines, 2 files, 1.5 days. | high | medium | REQ001619 | SQB/OP/op_vec_join.go, SQB/EX/vec_transform.go |
| REQ001630 | SQB/OP | **VectorizedNestedLoopJoin: outer join support.** Current `VectorizedNestedLoopJoin` (`op_vec_join.go:645-781`) only supports INNER and CROSS joins. The row-based `NestedLoopJoin` supports LEFT/RIGHT/FULL outer joins via `JoinKind` and unmatched-row phases. **Fix:** (1) Add `kind JoinKind` and `matchedProbe []bool`/`matchedBuild []bool` tracking to `VectorizedNestedLoopJoin`. (2) After all probe rows processed, emit unmatched build rows (LEFT/FULL) and unmatched probe rows (RIGHT/FULL). (3) Support NULL-padding for unmatched side columns. (4) Update `transformNestedLoopJoin` to forward join kind. (5) Add `TestVecNLJ_LeftOuter`, `TestVecNLJ_FullOuter`. Estimate: ~150 lines, 1 file, 1 day. | medium | medium | REQ001620 | SQB/OP/op_vec_join.go, SQB/EX/vec_transform.go |
| REQ001631 | SQB/EX | **VectorizedFilter: IN-list bloom filter pushdown.** select4 queries use heavy `IN (list)` predicates (e.g., `a3 in (515,190,306,513,959,788,198)`). Current `VectorizedFilter` evaluates each predicate conjunct per-row via `EvalBatchExpr`. For large IN-lists, this is O(list_size) per row. **Fix:** (1) Detect `col IN (v1, v2, ..., vn)` patterns in `VectorizedFilter` construction. (2) For lists > 8 elements, build a `UT.BloomFilter` from the list values. (3) In the filter kernel, check bloom filter first — skip rows definitively absent without full equality comparison. (4) For rows that pass the bloom filter, do full equality check. (5) Update `transformFilter` to detect and configure IN-list bloom filters. Estimate: ~100 lines, 2 files, 0.75 day. | medium | small | REQ001614 | SQB/OP/operators_vec.go, SQB/EX/vec_transform.go |
| REQ001632 | SQB/EX | **VectorizedProject: CASE WHEN / CAST support.** Current `VectorizedProject` (`operators_vec.go:354-483`) supports simple column references and arithmetic expressions via `EvalBatchExpr`. Complex expressions like `CASE WHEN ... THEN ... END` and `CAST(... AS ...)` are not yet vectorized — they fall back to `ScalarBatchProducer`. select4 queries use arithmetic expressions extensively (`e6+c6`, `a3+149+a5`, `b9*320`). **Fix:** (1) Extend `VectorizedProject` to handle `CASE WHEN` expressions as columnar conditional kernels. (2) Extend `VectorizedProject` to handle `CAST` expressions as typed columnar conversions. (3) For unsupported expression types, fall back to `ScalarBatchProducer` wrapping. (4) Update `transformProject` to detect CASE/CAST patterns. Estimate: ~120 lines, 2 files, 1 day. | medium | small | REQ001614 | SQB/OP/operators_vec.go, SQB/EV/eval_vec.go |
| REQ001633 | SQB/EX | **Subquery vectorization: IN/EXISTS scalar subquery pushdown.** Current subquery handling (`subquery_vec.go`) has `VectorizedInSubquery`, `VectorizedExistsSubquery`, and `VectorizedScalarSubquery` as pre-materialization helpers. However, `transformOp` in `vec_transform.go` has no case for subquery operators — they fall back to `ScalarBatchProducer`. select4 join queries reference subquery patterns in WHERE clauses. **Fix:** (1) Add `case *OP.VectorizedInSubquery` and `case *OP.VectorizedExistsSubquery` to `transformOp`. (2) For `VectorizedInSubquery`, pre-materialize the subquery result into a hash set during `BuildKeySet`, then use the set for O(1) probe-side IN membership checks. (3) For `VectorizedExistsSubquery`, pre-materialize and use a presence set. (4) For scalar subqueries, pre-evaluate at plan time (constant folding) if non-correlated. (5) Update `isEligible` to recognize subquery operators. Estimate: ~150 lines, 2 files, 1 day. | medium | medium | REQ001614 | SQB/OP/subquery_vec.go, SQB/EX/vec_transform.go |
| REQ001634 | SLT/select4 | **select4 full corpus regression test — all operators.** Current select4 tests only cover join queries. The full corpus includes `valuesort` (1000 queries with UNION/EXCEPT/INTERSECT compound ops), `all*` (simple scans), and various expression-heavy queries. **Fix:** (1) Create `select4_full_test.go` in `tests/sqlcmp/slt/` that replays ALL statements from `select4.test` (not just join queries). (2) For each `query` statement, execute via `RazorDriver`, compute result hash with `rowsort`, compare against `sqlite3` reference hash. (3) Categorize results by query type: join, valuesort, all, compound. (4) Report per-category pass rate and overall corpus hash. (5) Run as part of `before-commit-cases.sh` pre-commit gate. This validates end-to-end correctness of ALL vectorized operators against the full select4 corpus (~2832 queries). Estimate: ~150 lines, 1 file, 1 day. | high | small | REQ001614-REQ001633 | tests/sqlcmp/slt/select4_full_test.go (new) |