## TBD
| ID | Subsystem | Requirement | Priority | Effort | Deps | Touches |
| --- | --- | --- | --- | --- | --- | --- |
| REQ001496 | SQO/MM | **Migrate `SQF/PL/memo.go` and `SQF/PL/learned.go` into `SQO/MM/`.** **Fix:** (1) Move `SQF/PL/memo.go` content (xxhash64 LRU, schema-version invalidation, NormalizeForMemo) into `SQO/MM/memo.go`. (2) Replace `SQF/PL/memo.go` with a thin re-export. (3) Move `SQF/PL/learned.go` into `SQO/MM/learned.go`. (4) Delete `SQF/PL/learned.go`. (5) Add `TestMemo_SchemaVersionInvalidation`. Estimate: ~300 LoC migrated, ~30 LoC re-exports, 2 files new + 1 modified, 0.25 day. | high | small | REQ001476, REQ001495 | SQO/MM/memo.go (extend), SQO/MM/learned.go (extend), SQF/PL/memo.go (thin re-export), SQF/PL/learned.go (delete) |
| REQ001497 | SQO/CO | **Migrate `SQB/EX/planner.go` + `planner_select.go` into `SQO/CO/`.** **Fix:** (1) Split the legacy `Planner` struct into `SQO/CO/planner.go`, `SQO/CO/select_plan.go`, `SQO/CO/dml_plan.go`, `SQO/CO/ddl_plan.go`. (2) Update `SQO/CO/optimizer.go` to wrap the migrated `Planner` and expose `Optimizer` interface. (3) Re-export via `SQB/EX/` thin aliases. (4) Use `RegisterOptimizer` injection point at `SQB/EX/executor_factory.go`. (5) Delete legacy files once call graph is clean. Estimate: ~1500 LoC migrated, ~100 LoC re-exports, 6-8 files new + 4 modified, 2 days. | high | large | REQ001477, REQ001478, REQ001496 | SQO/CO/{planner,select_plan,dml_plan,ddl_plan}.go (new), SQO/CO/optimizer.go (extend), SQB/EX/planner.go (delete), SQB/EX/planner_select.go (delete), SQB/EX/executor_factory.go (modify) |
| REQ001498 | SQO/RW | **Migrate `SQF/RE/*` and `SQB/EX/predicate.go` rewrite helpers into `SQO/RW/*`.** **Fix:** (1) Audit `SQF/RE/rewrite.go` content. (2) Port missing functions to `SQO/RW/`. (3) Migrate `SQB/EX/predicate.go` rewrite helpers into `SQO/RW/predicate.go`. (4) Re-export legacy names. (5) Delete `SQF/RE/rewrite.go`. Estimate: ~500 LoC migrated, ~50 LoC re-exports, 2-3 files new + 2 modified, 1 day. | high | medium | REQ001477, REQ001497 | SQO/RW/rewrite.go (extend), SQO/RW/predicate.go (new), SQF/RE/rewrite.go (delete), SQB/EX/predicate.go (slim) |
| REQ001499 | SQB/EX | **Update `SQB/EX/` to depend on `SQO/CO.Optimizer` interface.** **Fix:** (1) Replace direct `NewPlanner()` with `SetOptimizer` injection. (2) Update `Exec`/`Query`/`QueryStream` to dispatch through `optimizer.Plan(stmt)`. (3) Add `Executor.SetOptimizer` setter. (4) Add `TestExecutor_RegisterOptimizer_Injection`. (5) Make `RegisterOptimizer` part of `OpenOptions`. Estimate: ~150 LoC modified, 2-3 files, 0.5 day. | high | small | REQ001497, REQ001498 | SQB/EX/executor_factory.go, SQB/EX/ex.go, SYS/SY/sy.go |
| REQ001500 | SQF/PL | **Delete `SQF/PL/{memo,learned,comparisons}.go` (keep only `types.go`).** **Fix:** (1) Audit callers. (2) Migrate `comparisons.go` into `types.go`. (3) Delete `comparisons.go`. (4) Verify `SQF/PL/` contains only `types.go` + `learned_test.go`. Estimate: ~50 LoC migrated, 0.25 day. | high | small | REQ001496 | SQF/PL/types.go (extend), SQF/PL/comparisons.go (delete), SQF/PL/memo.go (delete), SQF/PL/learned.go (delete) |
| REQ001501 | SQO/SL | **Add `(1 - frac_null)` correction to `joinPredSel`.** **Fix:** (1) In `JoinPredSel`, fetch `null_frac` from `ColumnStats`. (2) Multiply selectivity by `(1 - null_frac_l) × (1 - null_frac_r)`. (3) Fall back if unknown. (4) Add `TestJoinPredSel_NullFractionCorrection`. Estimate: ~30 LoC, 1 file modified + 1 new test file, 0.25 day. | high | small | REQ001494 | SQO/SL/selectivity.go |
| REQ001502 | SQO/CP | **Add `EffectiveCacheSize` field to `CostParams`.** **Fix:** (1) Add `EffectiveCacheSize uint64` field. (2) In `SQO/CM/pg_style.go`, divide `random_page_cost` by cache-hit probability. (3) Default `EffectiveCacheSize = 4 GB`. (4) Add test. Estimate: ~40 LoC, 1 file modified + 1 new test, 0.25 day. | high | small | REQ001473 | SQO/CP/params.go, SQO/CM/pg_style.go |
| REQ001503 | SQO/CP | **Lower default `random_page_cost` from 4.0 to 2.0.** **Fix:** (1) Change `RandomPageCost: 4.0` to `RandomPageCost: 2.0`. (2) Document rationale. (3) Add test. Estimate: ~10 LoC + 1 test, 0.1 day. | medium | small | REQ001473, REQ001502 | SQO/CP/params.go |
| REQ001504 | SQO/JN | **Adaptive `n3HeapMaxSize` by join count.** **Fix:** (1) Replace constant `n3HeapMaxSize=24` with `n3HeapMaxSizeForJoins(K int) int`. (2) Wire into `n3JoinOrdering`. (3) Add test. Estimate: ~30 LoC, 1 file modified + 1 new test, 0.25 day. | medium | small | REQ001495 | SQO/JN/n3.go |
| REQ001505 | SQO/JN | **Star-schema cost inflation (SQLite v3.49.0 port).** **Fix:** (1) Detect star-schema patterns in `GroupBushy`. (2) Multiply per-dimension scan cost by factor ~3.0. (3) Add test. Estimate: ~80 LoC, 1 file modified + 1 new test, 0.5 day. | medium-high | small | REQ001495 | SQO/JN/bushy.go |
| REQ001506 | SQB/OP | **Bloom filter pre-filtering on hash joins.** **Fix:** (1) Add `BuildBloomFilter` to `SQB/UT/`. (2) Build Bloom filter in `HashJoin.Next`. (3) Check in `HashJoin.probeRow`. (4) Default `fpp=0.01`. (5) Add test. (6) Opt-in via setter. Estimate: ~150 LoC, 1 file new + 1 file modified + 1 new test, 0.5-1 day. | medium | medium | none | SQB/OP/hashjoin.go, SQB/UT/bloom.go (new) |
| REQ001507 | SQB/UT | **Per-column statistics target.** **Fix:** (1) Add `StatisticsTarget` to column metadata. (2) Size histogram accordingly. (3) Add ALTER COLUMN SET STATISTICS_TARGET. (4) Default 100. (5) Add test. Estimate: ~100 LoC, 1 file modified + 1 new test, 0.5 day. | medium | small | none | SQB/UT/analyze.go, SQB/DT/schema.go, SQB/EX/planner.go |
| REQ001508 | SQB/UT | **Incremental ANALYZE trigger.** **Fix:** (1) Add `RowChangeTracker`. (2) Increment on INSERT/UPDATE/DELETE. (3) Check threshold periodically. (4) Add `PRAGMA auto_analyze_threshold`. (5) Add test. Estimate: ~120 LoC, 1 file new + 2 modified, 1 day. | medium | medium | REQ001507 | SQB/UT/analyze.go, SQB/EX/ex.go, SQB/EX/planner.go |
| REQ001509 | SQB/OP | **Memoize node for parameterized NestedLoopJoin.** **Fix:** (1) Add `Memoize` operator. (2) Cache on outer parameter hash. (3) Detect correlated subqueries in planner. (4) Add test. (5) Opt-in via cost threshold. Estimate: ~200 LoC, 1 file new + 1 modified + 1 test, 1-2 days. | medium-high | medium | REQ001497 | SQB/OP/memoize.go (new), SQO/CO/select_plan.go |
| REQ001513 | SQB/OP | **pruneRowCols zero-alloc fast path for all-columns-used.** `pruneRowCols` allocates `newCols`, `newTypes`, `newData`, and `newIndex` on every call where `allUsed` is false (~8MB in SLT profile). The `allUsed` early return already covers full-select cases. Most callers with column pruning (e.g. `SELECT a, b FROM wide_table`) still allocate on every row. **Fix:** (1) Hoist the column-prune decision above `nextFromStore` so the RowArena allocates only the pruned column count from the start, eliminating per-row `pruneRowCols` calls entirely. (2) Fall back to the existing per-row path for dynamic pruning (e.g. subqueries). Estimate: ~60 LoC, 1 file modified + 1 new test, 0.5 day. | low | small | none | SQB/OP/seq_scan.go |
