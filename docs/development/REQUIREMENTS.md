## TBD
| ID | Subsystem | Requirement | Priority | Effort | Deps | Touches |
| --- | --- | --- | --- | --- | --- | --- |
| REQ000861 | EX/op | **TestJoinRegress/j4_in_or: 4-table join with `IN`+`OR` returns 1400 rows instead of expected 1160.** `TestJoinRegress/j4_in_or` in `tests/sqlcmp/join_regress_test.go:111` runs `SELECT * FROM t1, t2, t3, t4 WHERE t1.a IN (101, 103) AND (t2.b > 105 OR t4.d < 103)` on 100-row tables. Expected: 1160 rows (per the comment: `10000 × (2/10) × (1 - (6/10)*(7/10)) = 2000 × 0.58 = 1160`). Actual: 1400 rows. The predicate pushdown is not reducing the row count to the expected selectivity — the OR condition is probably not being distributed across the join. With 10000 cartesian rows, t1.a IN (101,103) reduces to 2000, and the OR further restricts to 0.58 × 2000 = 1160. The actual 1400 suggests one of the conditions is being applied but not the OR-distribution, or the OR is being applied before the cross-join reduction. **Fix:** (1) Verify the OR-condition pushdown logic correctly handles `(t2.b > 105 OR t4.d < 103)` — possibly by splitting the OR into two independent scans and unioning. (2) The selective factor of 0.58 comes from `1 - P(NOT OR) = 1 - P(NOT t2.b>105) * P(NOT t4.d<103) = 1 - 0.6 * 0.7 = 0.58` — verify this matches the implementation. **Actual mismatch:** `got=1400 want=1160 (ratio 1.21x)`. | high | medium | iter-31 | EX/join.go, EX/intermediate.go, EX/planner.go |
| REQ000904 | EX/eval | **Recursive CTE executor missing — `WITH RECURSIVE` parses (PS/select.go:470) but the executor has no iteration/termination logic, so `slt_gap_test.go:103` fails on `WITH RECURSIVE cnt(x) AS (SELECT 1 UNION ALL SELECT x+1 FROM cnt WHERE x<5) SELECT x FROM cnt`.** The parser builds a `WithStmt` with a recursive flag and a `CompoundStmt` body; the planner/executor needs to (1) detect recursive references, (2) iterate the non-recursive seed once, then alternate with the recursive arm until it produces an empty result, and (3) dedup and forward rows to the outer query. **Fix:** (1) Add a recursive-CTE operator (`RecursiveCTE`) in `EX/` that tracks rows produced per iteration and stops when a fixed-point is reached. (2) Wire the recursive body so self-references to the CTE name resolve to the accumulated row set from the previous iteration. (3) Cap iterations to prevent infinite loops (e.g. 10000 iters, then error). (4) Add a `TestRecursiveCTE_Execution` test verifying the gap-survey probe and a depth-10 linear recursion. **Test evidence:** `slt_gap_test.go:103` (recursive_cte probe). | medium | medium | iter-31 | EX/eval.go, EX/intermediate.go, PS/select.go |
| REQ000907 | EX/parser | **FETCH FIRST/NEXT n ROWS ONLY is not parsed — tokens `T_FETCH`/`T_FIRST`/`T_NEXT`/`T_ONLY` are defined in `token.go:169-171` but no `parse*` function consumes them, so any query using `FETCH FIRST` fails with `syntax error at "FETCH"`.** FETCH is the SQL-standard alternative to `LIMIT` and is required for full SQL:2008 compatibility. The clause sits where `LIMIT ... OFFSET ...` sits in `parseSelect`. **Fix:** (1) Add `fetchFirst *FetchFirst` field to the `Select` AST node (or reuse `Limit` with a flag). (2) In `PS/select.go` after `LIMIT/OFFSET` parsing, add a branch: `FETCH (FIRST|NEXT) [n] (ROW|ROWS) ONLY` (and `WITH TIES` extension optional). (3) Map the `n` value to the existing limit machinery. (4) Reject `OFFSET` + `FETCH` combination mismatch in the same way SQLite does. (5) Add `TestFetchFirst_ParseAndExecute` covering `FETCH FIRST 5 ROWS ONLY`, `FETCH NEXT 3 ROWS ONLY`, and `FETCH FIRST ROW ONLY`. | low | small | iter-31 | PS/select.go, PS/ps.go, EX/intermediate.go, LX/token.go |
| REQ000908 | EX/eval | **ATTACH DATABASE / DETACH DATABASE are parsed but rejected at runtime with `ErrMultiDatabaseNotSupported` (`writers.go:2189`) — the parser successfully builds `AttachStmt`/`DetachStmt` AST nodes but the executor path immediately errors out, making any cross-database query (or even intra-database `ATTACH` for future schema-attach) impossible.** While the multi-database engine is a large architectural change, the bare AST + catalog machinery for attaching an already-open RazorData database directory should be implementable as a thin alias layer. **Fix:** (1) Add a `catalog` field on `*Executor` mapping `name → *store.Store`. (2) On `ATTACH DATABASE '<path>' AS name`, open a second store and register it in the catalog; on `DETACH`, close and remove. (3) For `CREATE TABLE attached.t(...)` and `SELECT * FROM attached.t`, resolve `name` via the catalog when looking up tables. (4) Reject cross-database transactions / joins between attached and main for v1. (5) Add `TestAttachDetach_SamePath` and `TestAttachDetach_QueryAcross`. | medium | medium | iter-31 | EX/writers.go, EX/ex.go, EX/catalog.go (new), PS/ps.go |
| REQ000909 | EX/ddl | **`CREATE VIRTUAL TABLE ... USING module(args)` parses to `CreateVirtualTableStmt` (`ast.go:666-673`) but has no execution path — the writer dispatcher in `writers.go` has no `*PS.CreateVirtualTable` case, so it falls through to a generic syntax error.** Virtual tables are SQLite's module-extensibility point (FTS5, json1, csv, etc.). Implementing a real module is large, but a minimum viable FTS5-style module or a passthrough CSV module would unblock `PRAGMA` introspection code that depends on virtual tables and would let SQLLogicTest files that touch FTS be partially exercised. **Fix:** (1) Add a `*PS.CreateVirtualTable` case in `buildWriterOp` that registers a stub module — for v1, return a clear `feature not supported: virtual table module <name>` error at `CREATE VIRTUAL TABLE` time (no module dispatch). (2) Ensure that `sqlite_master` introspection of virtual tables returns the expected row format (schema column populated with the module name). (3) Add `TestCreateVirtualTable_StubRegistration` verifying that `sqlite_master` shows the table with the right schema and that data ops on it return the unsupported-module error. | medium | small | iter-31 | EX/writers.go, EX/buildWriterOp, PS/ast.go |
| REQ000910 | EX/ddl | **WITHOUT ROWID tables are accepted by the parser (`PS/ddl.go`) and AST (`ast.go`), but the store layer at `EX/store.go:64` and `EX/constraints.go:43` treats them as rowid tables — `STORAGE WITHOUT ROWID` does not change the actual storage layout, so the resulting table uses rowid-based B-tree even though the AST claims rowid is absent.** This means `INSERT INTO without_rowid_t VALUES (...)` with a non-PK column in place of a rowid does not surface any error to the user, but the rowid is still allocated and queries using `WHERE _rowid_ = ?` succeed when they should fail. **Fix:** (1) Add `isRowid bool` to the `schema` struct, set to false when `WITHOUT ROWID` is present. (2) In `Store.Create`, branch to a clustered-index storage path that uses the PRIMARY KEY as the B-tree key (no implicit rowid column). (3) Block `_rowid_` queries on WITHOUT ROWID tables with `no such column: _rowid_`. (4) Add `TestWithoutRowId_TableCreation`, `TestWithoutRowId_PrimaryKeyClustered`, `TestWithoutRowId_NoRowIdCol`. | medium | large | iter-31 | EX/store.go, EX/constraints.go, EX/writers.go, PS/ddl.go |
| REQ000912 | EX/eval | **NULL three-valued logic broken in index-driven `>` / `<` / `>=` / `<=` comparisons — `SELECT pk FROM tab1 WHERE col3 > 221 ORDER BY 1 DESC` returns 0 rows instead of 8.** Evidence: `tests/sqlcmp/corpus/test/index/orderby/10/slt_good_0.test:354-364` and many parallel tests. tab1 = tab0 with col3 = {846,562,652,51,256,957,656,294,878,105}; `> 221` should match 8 rows. Razor returns 0. The same `>` issue also surfaces in `evidence/slt_lang_createview.test:29-37` (`SELECT x FROM t1 WHERE x>0` returns 2 rows including the NULL x). Root cause: when the IndexScan returns NULL keys, the `> 221` comparison treats `NULL > 221` as FALSE (instead of NULL, which would still exclude from result — wait, NULL should be excluded from WHERE, so it shouldn't be 2 rows). Need to trace whether IndexScan is dropping the non-NULL rows or returning only NULLs. **Fix:** (1) Inspect `IndexScan.Next` to confirm it emits rows whose key is non-NULL and `key > 221`. (2) In `evalCompare` ensure `>` for INTEGER returns TRUE/FALSE only — never NULL. (3) Add `TestIndexScan_RangeWithNull` with a 10-row table where 8 rows have col3 > 221 and 2 have col3 IS NULL; query should return exactly 8. **Test evidence:** orderby slt_good_0=7324 fail; createview 5 fail. | high | medium | iter-31 | EX/operators.go, EX/eval.go, EX/indexscan.go |
| REQ000913 | EX/eval | **Complex WHERE with `OR`, `IN (list)`, `IS NULL` returns 0 rows when used with index — `SELECT pk FROM tab1 WHERE ((col1 < 71.25)) OR (col3 IN (53,42,27,44) OR (col3 IS NULL)) AND (col0 >= 42)` returns 0 cells.** Evidence: `tests/sqlcmp/corpus/test/index/commute/10/slt_good_0.test:115-119` and many parallel tests. The OR-distribution of `IN (list) + IS NULL` over a 3-conjunct AND with `>` comparison is failing when the index is selected. The `(col3 IS NULL)` short-circuits or the index lookup drops rows whose `col3` is NULL. **Fix:** (1) Verify `Filter` correctly handles `(IS NULL)` in disjunction — it should never be eliminated by a `<` or `>` range filter. (2) The index scan must return rows with col3 = NULL and col0 >= 42 regardless of col1 condition. (3) Add `TestIndexScan_OrWithIsNull` reproducing this query shape. **Test evidence:** commute 10/100 = ~6700/7200 fails each. | high | medium | iter-31 | EX/operators.go, EX/intermediate.go, EX/eval.go |
| REQ000915 | EX/eval | **NULLIF and COALESCE with chained unary minus / nested CASE produce wrong results — `SELECT NULLIF(-COUNT(*), +67 * - - (+25) + 89 + -39 * 63) + +54` returns 54 but should return 53.** Evidence: `tests/sqlcmp/corpus/test/random/expr/slt_good_0.test:72-75` and L86-89, L144-147. Multiple parse/eval issues: (1) `NULLIF(-COUNT(*), expr)` with complex chained unary minus inside the second arg computes the second arg wrong (evaluates the `- - 25` as `-(-25)=25` but the surrounding arithmetic differs). (2) `COALESCE(CASE - CASE WHEN ... END WHEN ... END, MIN(ALL -30) * 45 * 77)` returns NULL when the second arg is non-NULL — Razor's `COALESCE` does not fall through to the second arg. (3) `73 - + -75 + +56 * + +COUNT(*) * -89 + +87 DIV - -98 * + +68 * +32` evaluates to 148 instead of -4836 — `DIV` (integer division) precedence is wrong relative to `*`. **Fix:** (1) Audit `evalUnary` for chained `-` and `+` prefix operators — verify a `+ + COUNT(*)` (two unary plus) is parsed as `+ (+ COUNT(*))` not `+ +COUNT(*)`. (2) `COALESCE` must evaluate each arg until non-NULL is found; verify the second-arg type-coercion doesn't error out. (3) `DIV` (integer division operator) should have same precedence as `*` and `/` — check the precedence table in `PS/ps.go:283-305`. (4) Add `TestNullIf_ChainedUnaryMinus`, `TestCoalesce_NestedCase`, `TestDiv_OperatorPrecedence`. **Test evidence:** random/expr = 3035 + 2601 fails. | high | medium | iter-31 | EX/eval.go, EX/aggregate.go, PS/ps.go, PS/expr.go |
| REQ000916 | EX/parser | **`FROM ( tab0 AS cor0 CROSS JOIN tab0 cor1 )` parenthesized table-list in derived table not parsed — `ps: syntax error at "tab0"`.** Evidence: `tests/sqlcmp/corpus/test/random/aggregates/slt_good_0.test:74-78` and L81-85. The query `SELECT + + MIN(ALL - + 32) AS col0, + COUNT(*) * + COUNT(*) FROM ( tab0 AS cor0 CROSS JOIN tab0 cor1 )` has a subquery in the FROM clause containing a parenthesized join expression. The parser does not support `( t1 alias1 JOIN t2 alias2 )` as a derived-table body. **Fix:** (1) In `PS/select.go parseFrom` after parsing the table-or-subquery, allow a parenthesized join expression `( t1 [AS a] [[INNER|LEFT|CROSS] JOIN t2 [AS b] [ON ...]] ... )` and wrap as a derived table. (2) Add `TestParseFrom_ParenthesizedJoin`. (3) Verify the planner handles the synthetic derived-table subquery correctly. **Test evidence:** random/aggregates 883 + 790 fails. | medium | small | iter-31 | PS/select.go, EX/planner.go |
| REQ000918 | EX/eval | **`SUM(DISTINCT -col2)` produces wrong result — `SELECT DISTINCT -SUM(DISTINCT - +col2) + + -98 AS col2 FROM tab1 AS cor0` returns -98 instead of 125.** Evidence: `tests/sqlcmp/corpus/test/random/aggregates/slt_good_0.test:536-538`. The expression `+ + - 98` is `-(98) = -98`. The `-SUM(DISTINCT -col2)` should compute `SUM(DISTINCT -col2)` first then negate. With 10 rows of col2 (text column), this case is broken because col2 is TEXT type. **Fix:** (1) In `evalAggregate` for SUM with TEXT-typed column, either coerce to 0 (SQLite behavior) or return NULL. (2) The current `-98` result suggests SUM is returning 0 (text col → 0 in some path), then `-(0) + -98 = -98`. (3) Add `TestSum_DistinctOnTextColumn` verifying SQLite-compatible behavior. **Test evidence:** random/aggregates. | medium | small | iter-31 | EX/aggregate.go, EX/eval.go |
| REQ000923 | EX/eval | **`SELECT +COUNT(*)` without FROM returns 0 instead of 1 (SQLite quirk).** Evidence: `tests/sqlcmp/corpus/test/random/expr/slt_good_1.test:45-48` and L51-54, L67-70, L73-76, L79-82. SQLite's `SELECT COUNT(*)` (no FROM) returns 1 (one row, count of nothing), but Razor returns 0. The test uses label-1 to enforce the same hash across multiple `SELECT +COUNT(*)` variants. This is a SQLite-specific edge case where COUNT(*) without a FROM is treated as a one-row query with count=0 of an empty relation, returning 1 row. **Fix:** (1) In `evalAggregate` for COUNT(*), if the source relation is empty (no FROM), return 1 instead of 0. (2) Or: handle the no-FROM case in the planner by injecting a synthetic single-row relation. (3) Add `TestCountStar_NoFromReturnsOne`. **Test evidence:** random/expr slt_good_1 = 2601 fails (5 of which are the label-1 COUNT(*) pattern). | low | small | iter-31 | EX/aggregate.go, EX/planner.go |
| REQ000924 | EX/eval | **`WHERE (col1 >= 901.86)` on FLOAT-typed column returns 0 rows (index scan broken for FLOAT comparison).** Evidence: `tests/sqlcmp/corpus/test/index/orderby_nosort/100/slt_good_0.test:379-381` and many parallel queries. tab1 has 100 rows with FLOAT col1; `>= 901.86` should match a few rows. Razor returns 0. Similar to REQ912 (integer comparison) but specifically for FLOAT type — the index lookup may be using wrong key type for FLOAT or rounding incorrectly. **Fix:** (1) Verify `IndexScan` correctly handles FLOAT-typed columns with FLOAT comparison values. (2) Check if `valueFromAny` or `lookupKey` converts the FLOAT correctly. (3) Add `TestIndexScan_FloatRangeQuery` with a 10-row table where col is FLOAT and `WHERE col >= X` should return N rows. **Test evidence:** orderby_nosort/100 = 3576 fails; orderby/100 = 7704 fails. | high | small | iter-31 | EX/operators.go, EX/eval.go |
| REQ000927 | EX/plan | **`SELECT DISTINCT` returns columns in wrong order — `SELECT DISTINCT COUNT(*) DIV +45 AS col1, 95` returns `[95, 0]` instead of `[0, 95]`.** Evidence: `tests/sqlcmp/corpus/test/random/aggregates/slt_good_6.test:277-279` and slt_good_9/11/17. When the SELECT list has both an aliased aggregate column and a constant column, the column order in the result is swapped. The aggregate column appears AFTER the constant. **Fix:** (1) In `Project` operator or `planSelect`, preserve the source column order in the SELECT list. (2) The DISTINCT dedup must not reorder the columns — the output schema must match the SELECT-list order. (3) Add `TestProject_DistinctColumnOrder` reproducing this query. **Test evidence:** aggregates/6/9/11/17. | high | small | iter-31 | EX/operators.go, EX/intermediate.go |
| REQ000929 | EX/eval | **`NOT BETWEEN NULL AND x` returns 0 rows (incorrectly treated as FALSE instead of unknown-allowing).** Evidence: `tests/sqlcmp/corpus/test/random/aggregates/slt_good_7.test:524-526` — `SELECT * FROM tab0 WHERE col1 NOT BETWEEN NULL AND -col1` returns 0 rows, want 9 (all rows). SQLite treats `x NOT BETWEEN NULL AND y` as `NOT (x >= NULL AND x <= y)` = `NOT NULL` = NULL. But since the filter is NULL, the row is excluded — so 0 rows would be expected. The fact that want=9 means SQLite actually returns TRUE for this case (likely because `NULL BETWEEN NULL AND -y` simplifies to TRUE in some implementation). **Fix:** (1) Audit `evalBetween`/`evalNot` for NULL handling — verify whether `x NOT BETWEEN NULL AND y` should return TRUE (as SQLite does) or NULL (three-valued). (2) Add `TestWhere_NotBetweenNull`. **Test evidence:** aggregates/7 L524. | low | small | iter-31 | EX/eval.go, EX/operators.go |
| REQ000931 | EX/eval | **`CAST(NULL AS DECIMAL)` and `CAST(NULL AS SIGNED)` in projection return wrong hash — possibly cast-of-NULL returning non-NULL.** Evidence: `tests/sqlcmp/corpus/test/random/groupby/slt_good_2.test:1470-1472` and slt_good_4 L275-277. `SELECT CAST(NULL AS DECIMAL) FROM tab1, tab0 cor0 GROUP BY cor0.col1` returns wrong hash. Razor may be returning an empty row instead of a NULL row, or the CAST of NULL is producing an empty string/text. **Fix:** (1) In `evalCast` for NULL input, return NULL regardless of target type. (2) Verify the row count for `SELECT CAST(NULL AS X)` is the same as the source row count (1 per input row). (3) Add `TestCast_NullToAnyType`. **Test evidence:** groupby/2/4. | low | small | iter-31 | EX/eval.go, EX/operators.go |
| REQ000939 | EX/eval | **`CAST(NULL AS DECIMAL) + +col1 * -66` returns NULL (correct) but row count or hash differs — possibly NULL spread across joined rows.** Evidence: `tests/sqlcmp/corpus/test/random/aggregates/slt_good_59.test:103-105`. The query has CAST(NULL) in projection and a NOT-IN with computed value in WHERE. Razor returns wrong hash, suggesting the NULL propagation through the join/aggregate may be wrong. **Fix:** (1) Verify CAST(NULL AS DECIMAL) returns NULL row-by-row. (2) Verify the WHERE NOT-IN with computed value `-56 + -7 / col0 * -col2 NOT IN (+-col2)` evaluates correctly. (3) Add `TestCast_NullDecimal_WithNotIn`. **Test evidence:** aggregates/59 L103. | low | small | iter-31 | EX/eval.go, EX/operators.go |
| REQ000940 | EX/eval | **`SELECT DISTINCT col2 - ++col2 + col2 / +col0 FROM tab0` — operator precedence wrong (got 14.420 want 15).** Evidence: `tests/sqlcmp/corpus/test/random/aggregates/slt_good_75.test:119-121`. The expression `col2 - ++col2 + col2 / +col0` should be `((col2 - (+col2)) + (col2 / col0))` = `(0 + (col2/col0))` = col2/col0. For col0=1, col2=15 → 15.0. Razor returns 14.420 (a value that doesn't match this formula). Likely the unary chain `++col2` is being mis-evaluated. **Fix:** (1) In `evalBinary` for `-` with `++col2` on the right, the expression `col2 - ++col2` should be `0` (both sides equal). (2) Verify the chained unary plus evaluates to the column value. (3) Add `TestSubtract_ColumnMinusUnaryPlusSelf`. **Test evidence:** aggregates/75 L119. | low | small | iter-31 | EX/eval.go, EX/operators.go |
| REQ000941 | EX/eval | **`SELECT DISTINCT - MIN(DISTINCT - col0) * - 30 + COUNT(*) AS col1 FROM tab1 WHERE + col0 IS NOT NULL` — DISTINCT projection across NULL filter produces wrong row count or values.** Evidence: `tests/sqlcmp/corpus/test/random/aggregates/slt_good_112.test:59-61`. Razor returns wrong result for this DISTINCT-with-aggregates-and-WHERE combo. **Fix:** (1) Audit the interaction between DISTINCT dedup and the aggregate operators. (2) Verify the WHERE `+col0 IS NOT NULL` filter (REQ921) works when col0 is always non-NULL. (3) Add `TestDistinct_AggregateWithIsNotNull`. **Test evidence:** aggregates/112 L59. | low | small | iter-31 | EX/operators.go, EX/intermediate.go |
| REQ000942 | EX/eval | **`SUM(ALL CAST(NULL AS SIGNED))` returns 0 — SUM of all-NULL should be NULL, not 0.** Evidence: `tests/sqlcmp/corpus/test/random/aggregates/slt_good_73.test:89-91` and slt_good_37 L59. Same root cause as REQ917 (AVG) and REQ918 (SUM DISTINCT): SUM(ALL NULL) over N rows of NULL should be NULL, not 0. **Fix:** (1) In `evalAggregate` for SUM with all-NULL input, return NULL. (2) Verify COUNT(NULL)=0 returns 0 (correct), but SUM(NULL) should be NULL. (3) Add `TestSum_AllNullConstant_ReturnsNull`. **Test evidence:** aggregates/37, 73. | medium | small | iter-31 | EX/aggregate.go, EX/eval.go |
| REQ000943 | EX/eval | **`MAX(ALL + - 64) * CAST(++SUM(ALL - 96) AS SIGNED) WHERE NOT NULL IS NOT NULL` — multiple bugs combined in one query.** Evidence: `tests/sqlcmp/corpus/test/random/aggregates/slt_good_72.test:123-125`. The query has MAX(ALL `+-64`) returning NULL because constant arg broken (REQ928), CAST(SUM(ALL `-96`)) = CAST(SUM-NULL) = NULL (REQ942), and `WHERE NOT NULL IS NOT NULL` returns all rows (REQ921). The 3 bugs combine to produce the wrong result. **Fix:** (1) Fix REQ921, REQ928, REQ942 individually. (2) Add `TestCombinedAggregateAndIsNull`. **Test evidence:** aggregates/72 L123. | low | small | iter-31 | EX/aggregate.go, EX/eval.go, EX/intermediate.go |
| REQ000944 | EX/eval | **`-COUNT(DISTINCT + col0) - ++SUM(DISTINCT -(- 11)) + 32` — chain of DISTINCT aggregates with unary minus produces wrong result.** Evidence: `tests/sqlcmp/corpus/test/random/aggregates/slt_good_111.test:121-123`. COUNT(DISTINCT) and SUM(DISTINCT) chained with unary minus and the addition of a constant. Razor returns wrong value (likely just the constant 32). **Fix:** (1) Fix `SUM(DISTINCT -(-11))` = SUM(DISTINCT 11) = 11 (REQ928). (2) Fix `COUNT(DISTINCT +col0)` (REQ936). (3) Add `TestChain_DistinctAggregates_UnaryChain`. **Test evidence:** aggregates/111 L121. | low | small | iter-31 | EX/aggregate.go, EX/eval.go |
| REQ000949 | EX/join | **Block NLJ batch size (32 rows) limits right-side reuse for cross-joins.** (Partially shipped in iter-32: tinyRightThreshold raised from 64 to 256 for right-side caching. Batch size increase 32→64 deferred — causes select4 regression). Remaining: increase block size from 32 to 64 in `join.go:747`. For cross-joins with no ON/predicate, cache right side across all batches (currently cached per-batch when ≤256 rows). **Benchmark:** `BenchmarkSelect4_Join255` allocs=958/op. | medium | small | iter-32 | EX/join.go, EX/join_test.go |
| REQ000950 | EX/stat | **Column statistics (NDV) are not collected — planner relies on default 100-row estimate and hardcoded selectivity constants.** SQLite's `ANALYZE TABLE` creates `sqlite_stat1` entries with `tbl, idx, stat` storing row count and average-equivalent stats. PostgreSQL relies on `pg_statistic` with NDV, null fraction, MCV lists. Razor has no statistics collection mechanism — `getTableRowCount` returns `len(tables[name])` for in-memory tables or falls back to 100. **Fix:** (1) Add `Catalog.AnalyzeTable(name string)` to the catalog interface — collects: row count, per-column NDV (hash set of values), null count. (2) Store stats in a per-database `sqlite_stat1`-like system table or in-memory map. (3) Wire `ANALYZE TABLE t` SQL statement through the parser (already supported: `PS/ast.go AnalyzeStmt`) to the executor → call `Catalog.AnalyzeTable`. (4) Use NDV in `getTableRowCount` and `estimateJoinPredicateSelectivity`. (5) Add `TestAnalyzeTable_CollectsNDV` verifying that after `ANALYZE TABLE t1`, `getTableRowCount("t1")` returns the correct count and NDV is available for selectivity. | medium | large | iter-32 | EX/stats.go, EX/catalog.go, EX/writers.go, PS/ast.go |
| REQ000959 | SQB | **DEFERRED: Extract `SQB/ST` (Statistics) cluster from `SQB/EX`.** Blocked on import cycle: `SQB/EX` imports `SQB/QC` for CacheStats/AdaptiveOp; `SQB/QC` imports `SQB/EX` for Operator/Row. Until this cycle is broken (by moving Operator/Row to a shared package), ST cannot be created. Tracked for a follow-up iteration. | high | small | iter-36+ | SQB/ST/ |
| REQ000960 | SQB | **DEFERRED: Extract `SQB/QC` (Query Cache) cluster from `SQB/EX`.** Blocked on import cycle: `SQB/QC/adqc.go` implements `Operator` (in SQB/EX), and `SQB/EX` calls `NewAdaptiveOp` (in SQB/QC). The cycle is unresolvable until the `Operator` interface is moved to a shared package (e.g. `SQF/PL`). Tracked for a follow-up iteration. | high | small | iter-36+ | SQB/QC/ |
| REQ000967 | EX/plan | **Multi-start N3 join ordering for 7+ table joins produces wrong results when anchor table is not first.** `select5.test` 7-table join `SELECT x2,x3,x52,x22,x14,x49,x59 FROM t52,t3,t22,t2,t49,t59,t14 WHERE a49=b14 AND a3=9 AND a3=b59 AND b52=a59 AND b22=a2 AND a52=b49 AND a14=b2` returns 0 rows instead of 1. The planner uses single-start N3 for >4 tables (`planner.go:1684`), starting from the first FROM table (t52). The anchor condition `a3=9` is on t3 (second table), so the planner starts from t52 which has no selective filter. The join chain `t52 → t3(a3=9) → t22 → t2 → t49 → t59 → t14` produces 0 rows because the intermediate result sets are wrong. When the same query is rewritten with t3 first (`FROM t3,t59,t52,t49,t14,t2,t22`), it returns 1 row correctly. **Root cause:** The single-start N3 at `planner.go:1684` always starts from `s.From` (first FROM table). For 7+ table joins, the planner should use multi-start N3 (like it does for ≤4 tables at `planner.go:1682`) to try each table as the base and pick the lowest-cost plan. The current threshold of 4 is too low — 7+ table joins with anchor conditions in non-first tables produce wrong results. **Fix:** (1) Raise the multi-start N3 threshold from 4 to at least 7 (or remove the threshold entirely for correctness). (2) Alternatively, detect anchor tables (tables with equality-to-constant predicates like `a3=9`) and always start from the anchor table. (3) Add `TestSelect5_PlannerJoinOrder` verifying that 7-table joins with the anchor table in different positions all return 1 row. **Test evidence:** `TestSelect5_PlannerJoinOrder/anchor-second` in `select5_bench_test.go` fails (0 rows vs 1 expected). | critical | medium | iter-37 | EX/planner.go, EX/select5_bench_test.go |
| REQ000968 | EX/plan | **Planner does not detect anchor tables for join order optimization.** In `select5.test`, each multi-table join query has exactly one "anchor" condition that filters a table to a single row (e.g., `a3=9` filters t3 to 1 row, `a7=9` filters t7 to 1 row, `a8=9` filters t8 to 1 row). The planner should detect these anchor conditions and always start the join order from the anchor table. Currently, the planner starts from the first FROM table regardless of selectivity. For `SELECT ... FROM t52,t3,t22,t2,t49,t59,t14 WHERE a3=9 ...`, the planner starts from t52 (10 rows) instead of t3 (1 row after `a3=9`). This causes: (1) Wrong results for 7+ table joins (REQ000967), (2) Unnecessary intermediate result sets, (3) OOM risk for 64-table joins. **Fix:** (1) In `planSelect`, after `splitPredicatesByTable`, scan `pushedPredicates` for equality-to-constant predicates (e.g., `a3=9`). (2) If an anchor table is found, prepend it to the join order before running N3. (3) For multi-anchor cases (multiple tables with equality-to-constant), pick the table with the most selective predicate. (4) Add `TestPlanner_AnchorTableDetection` verifying that the planner correctly identifies and prioritizes anchor tables. | high | medium | iter-37 | EX/planner.go |
| REQ000969 | EX/join | **Multi-table joins have unbounded memory growth — 64-table join from select5.test risks OOM.** `select5.test` has 64-table joins with 10 rows per table. The NLJ hash cross join materializes both sides into memory (`join.go:403-463`). For a 64-table join, even with correct join ordering, the intermediate result sets can grow exponentially. The current `maxMaterialize` limit of 4096 rows (`join.go:402`) only limits the hash cross join materialization, not the overall join memory. **Memory growth observed:** 4-table join: 1.0MB, 5-table: 1.4MB, 6-table: 1.6MB, 7-table: 2.0MB, 8-table: 2.2MB, 9-table: 2.6MB, 10-table: 2.8MB. Extrapolating to 64 tables: ~18MB (linear) to ~GB (exponential with wrong join order). **Fix:** (1) Add a memory budget to the NLJ operator — track total bytes allocated across all materialized rows and abort with `ErrOOM` when exceeded. (2) Add a `MaxJoinMemory` config option (default: 64MB) to `Executor`. (3) In `tryHashCrossJoin`, check the memory budget before materializing each side. (4) Add `TestNLJ_MemoryBudget` verifying that joins exceeding the budget return `ErrOOM` instead of crashing. | high | large | iter-37+ | EX/join.go, EX/ex.go |
| REQ000970 | EX/eval | **Debug `println` in production — `Values.Next` fires `println("DEBUG Values.Next called, cols:", ...)` on every `SELECT 1+1`.** `values.go:38` contains a leftover debug statement that writes to stderr on every Values operator invocation. This affects every scalar SELECT (no FROM clause), every INSERT ... VALUES, and every VALUES subquery. **Fix:** (1) Delete the `println` line. (2) Add a grep-based CI check for `println(` in non-test files to prevent regression. | high | trivial | none | EX/values.go |
| REQ000971 | EX/const | **CRC32 used for unique constraint key encoding — collision risk causes silent data corruption.** `constraints.go:304-338` uses `crc32.NewIEEE()` (32-bit) to hash column values for UNIQUE constraint checking. CRC32 has ~1-in-2^32 collision probability per distinct value pair. A collision causes either (a) a false "duplicate key" error on INSERT, or (b) a missed duplicate (silent data corruption). The pending set is in-memory, so full binary key encoding is feasible. **Fix:** (1) Replace CRC32 with deterministic binary key encoding (reusing `encodeRow` logic or a similar approach). (2) Alternatively, use `crypto/sha256` truncated to 64 bits — still collision-resistant enough. (3) Add `TestUnique_NoCollision` inserting 100K rows with distinct composite keys and verifying no false positives. | high | small | none | EX/constraints.go |
| REQ000972 | WAL | **`SyncAsync` updates `synced` LSN non-atomically — can regress committed LSN.** `WR/wr.go:288-293` does `if syncedLSN > w.synced.Load() { w.synced.Store(syncedLSN) }` — a non-atomic read-then-write. If two `SyncAsync` goroutines run concurrently, the second may overwrite a higher LSN from the first, causing `SyncedLSN` to regress. Readers checking `synced` may then miss committed data. The synchronous `syncLocked` at line 233-241 correctly uses a CAS loop. **Fix:** (1) Replace with a CAS loop: `for { old := w.synced.Load(); if syncedLSN <= old || w.synced.CompareAndSwap(old, syncedLSN) { break } }`. (2) Add `TestWAL_SyncAsyncConcurrent` verifying that concurrent SyncAsync calls never regress the synced LSN. | high | small | none | WAL/WR/wr.go |
| REQ000973 | CT | **`GetByIDRef` returns mutable pointer under RLock — data race with concurrent Put/Delete.** `CT/catalog.go:251-262` acquires `c.mu.RLock()`, returns a direct `*Entry` pointer into `c.cache`, then releases the lock. Any concurrent `PutRaw`/`Delete` calling `flushLocked` can mutate the entry's fields (e.g., `Indexes` slice) while the caller reads/writes them. This is a data race by design. Similarly, `Cache()` (line 367) and `ByNameMap()` (line 374) return the raw internal maps without any lock held after return. `LS/catalog.go:271-283` (`ColumnStatsByName`) and `LS/catalog.go:288-308` (`TableStats`) iterate these maps without locks. **Fix:** (1) `GetByIDRef` should either return a deep copy or require callers to hold `c.mu.Lock()` explicitly with documented lock ordering. (2) `Cache()`/`ByNameMap()` should return copies or use a snapshot mechanism. (3) `ColumnStatsByName`/`TableStats` should acquire `c.mu.RLock()` before iterating. (4) Add `TestCatalog_ConcurrentAccess` running parallel Put/GetByIDRef/Delete in a race detector test. | high | medium | none | ENG/CT/catalog.go, ENG/LS/catalog.go |
| REQ000974 | EX/store | **`store.go` has inconsistent lock ordering between `storeMu` and `tablesMu` — deadlock risk.** `store.go:270-283` (`nextTableID`) and `store.go:443-453` (`nextTableIDLocked`) have inconsistent lock ordering relative to the catalog lock. `store.go:465` acquires `storeMu` then `store.go:515` acquires `tablesMu` (from `source.go:15`). Other code paths acquire `tablesMu` then `storeMu`. This inconsistent ordering creates a deadlock risk under concurrent DDL + DML. **Fix:** (1) Establish and document a single lock ordering: `tablesMu` → `storeMu` → `catalog.mu` (or vice versa). (2) Audit all call sites that acquire both locks and enforce the ordering. (3) Add `TestStore_LockOrdering` with concurrent CREATE TABLE + INSERT to verify no deadlock. | high | medium | none | EX/store.go, EX/source.go |
| REQ000975 | ENG/ID | **BTree Cursor has no `Close` method and acquires RLock per-operation — concurrent mutation corrupts cursor state.** `ENG/ID/cursor.go` has `Seek`/`Next`/`Key`/`Value`/`Valid` but no `Close`. More critically, `Seek` (line 19-21) acquires `bt.mu.RLock()` for the seek, then releases it. `Next` (line 64-69) re-acquires `bt.mu.RLock()` per step. Between `Seek` and `Next`, a concurrent `Insert`/`Delete` can mutate the tree, invalidating the cursor's `path` and `indices` stacks (which reference page IDs that may be reclaimed/reused). **Fix:** (1) Add `Close() error` to satisfy the `RangeIter` interface. (2) Either hold the lock for the cursor's entire lifetime (cursor = snapshot), or validate path consistency on `Next` (check page IDs haven't been reused). (3) Add `TestBTree_CursorConcurrentMutation` verifying that a cursor scan concurrent with inserts does not return corrupted results. | high | medium | none | ENG/ID/cursor.go |
| REQ000976 | LS | **`shardedIter.Next()` decrements `pos` incorrectly after advancing to next shard — reads from wrong shard.** `LS/sharded_memtable.go:229-246`: when shard `pos` is exhausted, the code does `si.pos++` (advance to next shard), calls `si.its[si.pos].Next()`, then does `si.pos--` (backs up). After the decrement, `pos` points to the *previous* shard (which was exhausted), not the new shard. `Key()`/`Value()` then read from the wrong shard's iterator. **Fix:** (1) After finding a non-empty shard, leave `si.pos` at the new shard index (remove the `si.pos--`). (2) Or restructure as a loop: `for si.pos < len(si.its) { if si.its[si.pos].Next() { return true }; si.pos++ }`. (3) Add `TestShardedIter_AdvanceAcrossShards` verifying correct cross-shard iteration. | high | small | none | LS/sharded_memtable.go |
| REQ000977 | EX/fk | **`encodeFKLookup` is a stub returning nil — FOREIGN KEY store-path validation silently passes.** `fk.go:189-196`: `encodeFKLookup` has `_ = prefix; _ = col; _ = val; return nil`. This means FK validation via the store path (for multi-column FKs or non-in-memory tables) silently succeeds — it never checks anything. Combined with `fk.go:82-88,285-287` where CASCADE/SET NULL/SET DEFAULT are accepted at DDL time but fail at runtime with "not yet implemented". **Fix:** (1) Either implement `encodeFKLookup` with proper key encoding, or return a clear `ErrFeatureNotSupported` at DDL time for FK actions that cannot be enforced. (2) Reject non-RESTRICT/NO-ACTION FK actions at `CREATE TABLE` time with a clear error. (3) Add `TestFK_StorePathValidation` and `TestFK_CascadeRejectedAtDDL`. | high | small | none | EX/fk.go |
| REQ000978 | EX/eval | **`evalFunction` and `evalAggregateOver` use flat switch dispatch — should use function registry pattern.** `eval.go` has 40+ scalar functions dispatched via a `switch name` in `evalFunction`, and `aggregate.go` has 6 aggregate types via `switch name` in `evalAggregateOver`. Both grow linearly with each new function — adding a function requires editing the switch, not just registering. The switch blocks are ~100 lines each. **Fix:** (1) Create `map[string]func(args []Value, row Row) (Value, error)` for scalar functions, populated at init. (2) Create `map[string]func() aggregateState` for aggregates. (3) Replace switch with map lookup. (4) This reduces eval.go by ~100 lines and makes adding functions a one-line registration. (5) Add `TestFunctionRegistry_AllFunctionsRegistered` verifying every documented function name has a registry entry. | high | medium | none | EX/eval.go, EX/aggregate.go |
| REQ000979 | EX/op | **`IndexScan` carries 5 mutually exclusive operational modes as optional fields — Strategy anti-pattern.** `indexscan.go` has grown from an MVP scan to a ~60-line god struct across iterations 22-27. Five modes (in-memory, store-prefix, index-seek, B-tree, range-seek) are encoded as optional fields with mode-switching in `Next()` and 5 constructors. This is a textbook Strategy anti-pattern. **Fix:** (1) Extract a `ScanStrategy` interface with `Next() (Row, error)`, `Close() error`. (2) Create concrete types: `InMemoryScan`, `StorePrefixScan`, `IndexSeekScan`, `BTreeScan`, `RangeSeekScan`. (3) `IndexScan` becomes a thin wrapper that delegates to the selected strategy. (4) Add `TestIndexScan_StrategySelection` verifying each strategy is selected correctly based on table type and predicate. | high | medium | none | EX/indexscan.go |
| REQ000980 | EX/join | **`NestedLoopJoin` struct has 50+ fields — join strategy decided at runtime, not by planner.** `join.go:23-142` is a god object containing fields for 7 execution modes (classic NLJ, hash cross, block NLJ, right-outer materialization, left-outer tracking, column projection, and 4 shared-state pairs). `Next()` (line 295-326) checks conditions to switch between strategies at runtime. The planner has no visibility into which strategy was chosen, making EXPLAIN output incomplete. **Fix:** (1) Extract mode-specific state into sub-structs or, better, make hash cross join and block NLJ truly separate operator types. (2) Move strategy selection to the planner based on statistics. (3) `tryHashCrossJoin` should be `NewHashCrossJoin` called from the planner. (4) Add `TestJoin_StrategyPlannerDriven` verifying the planner selects the right strategy. | high | large | none | EX/join.go, EX/planner.go |
| REQ000981 | EX/plan | **`planSelect()` is 760+ lines — handles 15+ concerns in one function.** `planner.go:1372-2133` handles view resolution, no-FROM expressions, subquery sources, sqlite_master, alias rewriting, index selection, cost-based scan swapping, predicate pushdown, join elimination, N3 ordering, bushy group construction, aggregate selection, HAVING, ORDER BY position resolution, window functions, projection, DISTINCT, LIMIT/OFFSET — all in one function. **Fix:** (1) Split into: `planSelectBase`, `resolveView`, `planSelectJoins`, `pushPredicates`, `planAggregation`, `planOrdering`, `planLimitOffset`. (2) Each sub-function should be under 100 lines. (3) Add `TestPlanner_SelectDecomposition` verifying each sub-function is independently testable. | high | medium | none | EX/planner.go |
| REQ000982 | EX/ddl | **`CreateTable.Next` is 200 lines — violates single-responsibility.** `writers.go:1089-1267` registers the table, resolves PK, builds unique constraints, extracts FK constraints, captures generated columns, builds CHECK constraints, persists to catalog, and writes the SQL reconstruction — all in one function. **Fix:** (1) Split into: `registerTableSchema`, `buildUniqueConstraints`, `buildFKConstraints`, `buildCheckConstraints`, `persistToCatalog`. (2) Each helper should be independently testable. (3) Add `TestCreateTable_SchemaRegistration` verifying each sub-function. | high | medium | none | EX/writers.go |
| REQ000983 | EX/plan | **Three duplicate expression walkers in `planner.go` — must be updated in lockstep.** `walkExprForTables` (line 741-793), `collectTablesFromExpr` (line 1162-1215), and `collectColsFromExpr` (line 1280-1337) each manually type-switch over ~15 expression node types. All three must be updated when a new expression type is added. Additionally, `walkExprForTables` does not handle `SubqueryExpr`/`ExistsExpr`, which can cause incorrect join elimination for correlated subqueries. **Fix:** (1) Create a generic `walkExpr(Expr, func(Expr))` or reuse the existing `AcceptExpr` visitor infrastructure. (2) Replace all three with calls to the shared walker + appropriate callback. (3) This eliminates ~200 lines and prevents future divergence. (4) Add `TestPlanner_ExprWalkerCoverage` verifying all expression types are handled. | high | medium | none | EX/planner.go |
| REQ000984 | EX/writer | **Duplicated RETURNING evaluation block appears 6 times across Insert/Update/Delete.** `writers.go` has the exact same 12-line RETURNING evaluation block in `Insert.Next` (line 209-225), `Insert.nextFromStore` (line 356-372), `Insert.nextFromSelect` (line 662-677), `Update.Next` (line 759-774), `Update.nextFromStore` (line 889-905), and `Delete.Next`/`Delete.nextFromStore` (line 977-994). **Fix:** (1) Extract `evalReturning(exprs []PS.Expr, row Row, params []any) ([]Row, error)` helper. (2) Replace all 6 call sites. (3) Add `TestEvalReturning_Helper` verifying the extracted function. | medium | small | none | EX/writers.go |
| REQ000985 | EX/store | **`encodeRow` allocates per call with no `sync.Pool` — violates hot-path allocation rule.** `store.go:559-601` allocates a new `[]byte` via `append` on every `encodeRow` call. For bulk INSERT (thousands of rows), this creates significant GC pressure. The project rules state "No allocations in hot paths — pre-allocate buffers" and "sync.Pool for reusable page buffers." **Fix:** (1) Use `sync.Pool` for encode/decode scratch buffers. (2) Or accept a pre-allocated buffer parameter. (3) Add `TestStore_EncodeRowNoAlloc` using `testing.AllocsPerRun` to verify zero allocations on the hot path. | medium | small | none | EX/store.go |
| REQ000986 | EX/const | **CHECK constraints re-evaluated from AST per row — not compiled.** `constraints.go:193-211` calls `EvalValue(check, &row, nil)` where `check` is a `PS.Expr` (parser AST node). For every INSERT/UPDATE, the CHECK expression is re-walked from the AST. Complex CHECK expressions (e.g., `CHECK(a > 0 AND b < 100)`) pay the full parsing overhead per row. **Fix:** (1) Pre-compile CHECK expressions into a `CompiledExpr` at schema registration time, similar to how WHERE clauses are compiled for operators. (2) Cache the compiled form in `storeSchema.checks`. (3) Add `TestCheckConstraint_Precompiled` verifying compiled CHECK expressions are faster than AST evaluation. | medium | small | none | EX/constraints.go |
| REQ000987 | EX/store | **No `TableHandle` abstraction — raw Store used everywhere with repeated schema+key+encode pattern.** Every caller (`writers.go`, `operators.go`, `integrity.go`) independently: (1) looks up the schema via `schemaFor`, (2) builds the key prefix, (3) calls `encodeRow`, (4) calls `store.Put/Get/Delete`. This three-step pattern is duplicated across 10+ call sites. **Fix:** (1) Introduce a `TableHandle` struct that bundles `{store Store, schema *storeSchema, prefix []byte}` with methods `InsertRow(Row)`, `GetRow(key)`, `Scan(prefix) Iterator`. (2) Replace the repeated pattern at all call sites. (3) Add `TestTableHandle_InsertGetScan` verifying the abstraction. | medium | medium | none | EX/store.go, EX/writers.go, EX/operators.go |
| REQ000988 | EX/eval | **`BinaryExpr.Op` is `int` instead of `LX.TokenType` — ~40 redundant casts across codebase.** `PS/ast.go:93` declares `Op int` but every construction site casts `int(LX.T_EQ)` and every consumption site casts back `LX.TokenType(op)`. Dozens of redundant casts scattered through `expr.go`, `planner.go`, `eval.go`, `compound.go`, `values.go`. **Fix:** (1) Change `BinaryExpr.Op` to `LX.TokenType`. (2) Remove all cast expressions at construction and consumption sites. (3) Add `go vet` check or lint rule to prevent re-introduction. | low | small | none | PS/ast.go, EX/eval.go, EX/planner.go |
| REQ000989 | EX/planner | **Plan cache eviction is O(n) random deletion — should use LRU or ring buffer.** `planner.go:420-437`: when the memo exceeds `maxPlanCacheSize`, a loop scans the map to find one arbitrary key to delete. Non-LRU, O(n) per eviction. **Fix:** (1) Use a ring buffer of keys for deterministic O(1) eviction. (2) Or implement a proper LRU with `container/list`. (3) Add `TestPlanCache_EvictionPolicy` verifying eviction targets the oldest entry. | low | small | none | EX/planner.go |
| REQ000990 | ENG/ID | **BTree page buffers not pooled — no sync.Pool, no eviction, unbounded memory.** `ENG/ID/id.go:64` (`newPage`) allocates `*page` on the heap. `loadPage` (line 207) allocates `make([]byte, pageSize)` on every cache miss. Pages accumulate in `bt.pages` forever until close — no LRU or clock-sweep eviction. For large indexes this is unbounded memory growth. **Fix:** (1) Add `sync.Pool` for `*page` and `[]byte` buffers. (2) Add LRU eviction to `bt.pages` (e.g., clock sweep or `container/list`). (3) Add `TestBTree_PageEviction` verifying memory is bounded after scanning 10K pages. | low | medium | none | ENG/ID/id.go |
