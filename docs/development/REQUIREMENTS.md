## TBD

| ID | Subsystem | Requirement | Priority | Effort | Deps | Touches |
|---|---|---|---|---|---|---|
| REQ000316 | SQL | Incremental materialized views (auto-maintained aggregation views with query routing) | medium | L | iter-08 (operators), iter-12 (catalog) | `SQL/EX/matview.go` (new); `ENG/LS` triggers on view base tables |
| REQ000321 | TXN | Deterministic Simulation Testing framework (FoundationDB-style scheduled threads + simulated clock + simulated disk; millions of random schedules) | high | XL | iter-17 (chaos), iter-13 (recovery) | new `tests/dst/` framework; subsystem-aware simulated drivers |
| REQ000530 | SQL/EX | Window function `RANGE` frame spec not supported — only `ROWS` frame works. `window.go:38-44` checks `spec.Frame` but `RANGE BETWEEN ...` not implemented | low | M | iter-28 | `SQL/EX/window.go` — implement `RANGE BETWEEN ...` frame spec |
| REQ000537 | ENG/LS | Sharded memtable (per-prefix skiplist) — split active memtable into N sharded skiplists keyed by `hash(key) % N` (N=16 default); directory routes lookup; flush merges shards into L0 SST; eliminates global CAS contention on head pointer under high write concurrency. New `internal/ENG/LS/sharded_memtable.go` + `shard.go` | high | L | iter-04 (skiplist), iter-27 (escape-hinted arena) | `ENG/LS/sharded.go` (new); `ENG/LS/engine.go` `Write`/`NewIterator`; `Options.MemTableShards` (default 16, 1 = legacy single-shard mode) |
| REQ000538 | ENG/LS | Zero-copy iterator with borrowed buffers — `mergeIterator.Next()` and `sstIter` return `unsafe.Pointer` references into buffer-pool blocks; iterator registers with engine epoch manager on creation, deregisters on Close; only the public API boundary (`Engine.Get`/`Iterator.Key`/`Iterator.Value` for external callers) materializes owned copies. Eliminates ~6 allocs/row in merge iterator. New `internal/ENG/LS/borrowed_iter.go` + `epoch_iter.go` | high | L | iter-27 (epoch manager), iter-19 (sync.Pool buffers) | `ENG/LS/api.go` `mergeIterator` rewrite; new `ENG/LS/epoch_iter.go`; `TXN/LC/qsbr.go` integration for iterator epoch registration |
| REQ000539 | MEM/BF | Sharded buffer pool with per-shard clock hands — replace single global `map[uint64]*bufferSlot`+`sync.RWMutex` with N=32 shards; `hash(blockID) % N` routes lookup; each shard owns its own sub-map, `hand`, and eviction loop; eliminates global mutex contention under concurrent reads. New `internal/MEM/BF/sharded_bp.go` | high | L | iter-02 (buffer pool), REQ000199 (sharded mutex idea, partial), REQ000303 (W-TinyLFU) | `MEM/BF/sharded_bp.go` (new) + `bf.go` refactor; `Options.BufferPoolShards` (default 32) |
| REQ000540 | ENG/LS | L0 buffer cache (memtable + L0 SSTs held in pinned buffers) — keep recently-read L0 SST blocks in a small dedicated cache so the L0 merge step in `NewIterator` doesn't re-read from disk on every scan; separate from the buffer pool; size-bounded with LRU; complements the existing memtable path. New `internal/ENG/LS/l0_cache.go` | medium | M | iter-04 (L0 SST), REQ000539 | `ENG/LS/l0_cache.go` (new); `ENG/LS/api.go` `mergeIterator.init` consults L0 cache before file read |
| REQ000541 | WAL | Batched LSN reservation ranges — replace single global `atomic.Int64` LSN counter with per-producer `Reserve(n int) uint64` that atomically claims `[start, start+n)`; producer increments locally; reduces contention on the LSN CAS under N concurrent appenders. New `internal/WAL/FL/lsn_reserve.go` | high | M | iter-03 (LSN), REQ000184 (write buffer) | `WAL/FL/lsn.go` refactor; `WAL/WR/wr.go` `Append` consumes a reserved range |
| REQ000542 | WAL/FL | Group commit pipeline — txns waiting on `Sync()` join a group; when the first tx arrives or a 50µs deadline fires, the entire group fsyncs together; each waiter is unblocked with its assigned LSN. Builds on existing async fsync (REQ000301) and io_uring (REQ000295); target: 10x fsync throughput under burst commit load. New `internal/WAL/FL/group_commit.go` | high | L | REQ000184 (write barrier), REQ000301 (async fsync) | `WAL/FL/group_commit.go` (new) + `fl.go` refactor; `Options.GroupCommitTimeout` (default 50µs) |
| REQ000543 | SQL/EX | Shape-specialized fast paths — extend REQ000311/313 with shape-driven specialization that captures hot path inline as small specialized Go funcs (not full JIT); specialize per plan shape: `Filter{int64_col_lit_eq}`, `Project{cols=N}`, `HashAggregate{group_by_int64}`, etc.; registered in `codegen_ops.go`; benchmarks must show >1.5x over the interpreted path | medium | XL | REQ000311 (codegen framework), REQ000313 (ADQC) | `SQL/EX/codegen/shapes/` (new); `SQL/EX/adqc_specialize.go` (new); `SQL/EX/shape_bench_test.go` (new) |
| REQ000545 | SQL/EX | Selection vector runs — replace `[]uint16` selection vectors with `[]SelRange{Start, End uint16}` for the common filtered case where matched rows form contiguous runs (typical of `BETWEEN`, range predicates, time-range filters); downstream operators iterate runs rather than random indices; complement with RLE-friendly in-place Sel reuse across operators in a pipeline | medium | M | iter-19 (selection vector), REQ000149 (batch memory) | `SQL/EX/batch.go` `SelRange` type + helpers; `SQL/EX/eval_vec.go` `compareXxxRange` variants; `SQL/EX/pipeline.go` Sel propagation |
| REQ000546 | SQL/EX | Vectorized string columns via unsafe pointer + length prefix — replace `[]string` columns with `[]byte` raw + `[]uint32` lengths (or `[]struct{Ptr uintptr; Len uint32}`); eliminates string-header copy and enables real SIMD string compare (memcmp); backward-compat path via `Batch.StringValue(col,row)` accessor | medium | L | REQ000149 (batch memory), REQ000310 (SIMD) | `SQL/EX/batch.go` `Column` for text/varchar/blob; `SQL/EX/eval_vec.go` `compareStringColsVec`; `SQL/EX/simd_dispatch.go` runtime detection |
| REQ000547 | TXN/MV | Per-NUMA arena pools — extend the generational arena (REQ000064/305) with per-NUMA-node arena pools; thread first-touch policy (REQ000309) places new arenas on the calling node; reclaim drains per-node; eliminates contention on global `reclaimMu` and `arenaPool` under high transaction throughput on multi-socket hosts | medium | M | REQ000064 (generational arena), REQ000305 (epoch reclaim), REQ000309 (NUMA) | `TXN/MV/arena.go` `numaArenaPool`; `TXN/LC/epoch.go` per-node drain lists; `ENG/NM/numa.go` allocator policy |
| REQ000548 | SQL/ST | Engine-level prepared statement cache — `Engine.Prepare(sql) → *Stmt` (already exists per session) augmented with an `Engine`-level cache keyed by SQL text; cached entries are reference-counted; `Session.Query(sql)` after `Prepare(sql)` short-circuits parse + plan; distinct from `planMemo` (REQ000162/185) which is keyed by AST hash (still requires parse). New `internal/SYS/ST/engine_cache.go` | medium | M | REQ000098 (session pooling), REQ000162 (plan memo) | `SYS/ST/engine_cache.go` (new); `SYS/AP/ap.go` `Engine.Prepare`; `SYS/SE/se.go` `Session.Query` cache lookup; `Options.StmtCacheSize` (default 256) |
| REQ000549 | MEM/BF | Block-level MVCC version tagging — buffer pool slots carry an optional `versionRange [startTS, endTS]` field; `BufferPool.GetWithTS(blockID, readTS)` returns the page whose version range covers `readTS` (or the page whose range is the latest, then we filter via the version chain at read time); enables cross-transaction cache sharing with snapshot consistency. Slot tagged as `Shared` (safe for many readers) or `Private` (single-txn write). New `internal/MEM/BF/mvcc_slot.go` | low | XL | iter-02 (buffer pool), REQ000539 (sharded), REQ000307 (MV-OCC) | `MEM/BF/mvcc_slot.go` (new) + `bf.go` `GetWithTS`; `TXN/SN/snapshot.go` registers `readTS` on Get |
| REQ000550 | SQL/PL | Predicate cache (per-statement) — within a single `Stmt`, cache the resolved plan for repeated identical parameter tuples (hot loop: `WHERE x = ?` with same `?` value 100x); LRU 32-entry; complements `planMemo` (structural) with value caching (instance); distinct from REQ000548 which caches the entire `Stmt` | low | M | REQ000162 (plan memo), REQ000548 | `SQL/PL/predicate_cache.go` (new); `SQL/PL/planner.go` cache lookup before re-plan |
| REQ000551 | WAL/RP | Time-traveling snapshot pruning — epoch manager periodically asks `TXN/MV` for `oldestActiveReadTS`; the GC pass then reclaims all version nodes with `endTS < oldestActiveReadTS`; run on the same 100ms tick as `ReclaimOldGenerations`; reduces heap pressure from long-lived MVCC chains on read-heavy workloads | medium | M | REQ000064 (generational arena), REQ000164 (epoch background), REQ000302 (QSBR) | `TXN/MV/gc.go` `PruneOldVersions`; `TXN/LC/epoch.go` tick callback; `WAL/RP/checkpoint.go` skip pruned records on replay |
| REQ000552 | ENG/LS | Adaptive memtable size — start at small `MemTableSize` (4 MB); monitor write rate (bytes/sec) and compaction lag; grow toward configured max under sustained load, shrink under burst-and-drain. Reduces write stall risk on flash/SSD storage where compaction can't keep up | low | M | iter-04 (memtable size), REQ000318 (rate limiter) | `ENG/LS/memtable.go` `targetSize atomic.Int64`; `ENG/LS/flush.go` consult `WAL write rate` metric |
| REQ000553 | FIL/DF | Read-ahead / prefetch for sequential SST scans — when `mergeIterator` advances in sorted order and `BlockDevice.ReadBlock` for L_{k>=2} triggers, issue `posix_fadvise(POSIX_FADV_WILLNEED|POSIX_FADV_SEQUENTIAL)` for the next N blocks in the same SST; pairs with the existing io_uring (REQ000295) for Linux and falls back to nothing on other platforms. New `internal/FIL/DF/fadvise.go` | low | S | iter-01 (block device), REQ000295 (io_uring) | `FIL/DF/fadvise.go` (new); `FIL/DF/df.go` `ReadBlock` calls `fadvise` for sequential pattern detected by `mergeIterator` |
| REQ000554 | SQL/EX | Parallel IN-list evaluation — extend the existing batch `EvalBatch` with `evalInListBatch` that materializes the IN list once, then for each column type (int64/string) runs a single vectorized membership test (sort + binary search for ints; hash set for strings); target: 5-10x over the current row-at-a-time `evalIn` | medium | M | iter-19 (SIMD), REQ000149 (batch) | `SQL/EX/eval_vec.go` `evalInListBatch`; `SQL/EX/eval.go` `evalIn` falls back to per-row only when batch context unavailable |
| REQ000555 | TXN/VL | Lock-free transaction slot recycling — currently `Begin` acquires a mutex on the slot free list; replace with a Treiber stack (`atomic.Pointer` CAS-based push/pop); eliminates the global mutex under burst `Begin` load. Backed by a pre-allocated array of `MaxConcurrentTXNs` slots | medium | M | REQ000058 (txn slots), REQ000307 (MV-OCC) | `TXN/VL/slot.go` `slotStack` (atomic.Pointer-based LIFO); `TXN/VL/protocol.go` `Begin` uses `PopSlot()`/`PushSlot()` |
| REQ000556 | SQL/PS+EX | `CREATE VIRTUAL TABLE ... USING module_name` — BNF line 23; no T_VIRTUAL token, no parser path, no executor. SQLite-compatible modules (fts5, rtree) cannot be registered. Add lexer tokens (`T_VIRTUAL`, `T_USING` already exists for JOIN), parser path (`parseCreateVirtualTable` + `VirtualTableStmt` AST), executor module-dispatch loop. SQLite's `Module` interface (xCreate/xConnect/xBestIndex/xFilter/xNext/xEof/xColumn/xRowid/xUpdate/xBegin/xSync/xCommit/xRollback/xRename/xSavepoint/xRelease) must be reproduced; v1 ships only the parser + a registry stub that errors out on use | high | XL | iter-08 (catalog), REQ000250 (BTree) | `SQL/LX/lx.go`+`token.go` (T_VIRTUAL); `SQL/PS/ps.go` `parseCreateVirtualTable` + `SQL/PS/ast.go` `VirtualTableStmt`; `SQL/EX/ex.go` `buildWriterOp` route; new `SQL/EX/virtual.go` registry |
| REQ000557 | SQL/PS+EX | `ATTACH DATABASE expr AS name` / `DETACH DATABASE name` — BNF lines 5, 30; completely absent (no lexer tokens, no AST, no parser, no executor). Multi-database support is a fundamental SQLite feature. v1 ships parser + executor that errors out at runtime with "multi-database not supported in v1" | medium | M | REQ000048 (table registry), REQ000155 (catalog persistence) | `SQL/LX/lx.go`+`token.go` (T_ATTACH, T_DETACH); `SQL/PS/ps.go` `parseAttach`/`parseDetach` + `SQL/PS/ast.go` `AttachStmt`/`DetachStmt`; `SQL/EX/ex.go` `buildWriterOp` route; `SYS/SY/sy.go` catalog registry |
| REQ000562 | SQL/PS+EX | `CREATE TABLE ... WITHOUT ROWID` — BNF line 13; completely absent. WITHOUT ROWID tables use the PRIMARY KEY columns directly as the row storage key (no implicit rowid), giving smaller footprint and faster lookup on tables with a natural composite or single-column primary key. Storage layer must branch on `WithoutRowID` flag | medium | M | REQ000250 (BTree index), REQ000036 (lock-free skiplist), REQ000155 (catalog) | `SQL/PS/ps.go` `parseCreateTable` (line 1466) — accept `WITHOUT ROWID` after close-paren; `SQL/PS/ast.go` `CreateTable.WithoutRowID bool`; `ENG/LS/api.go` Write path branches; `ENG/LS/sst_writer.go` rowid encoding branches; `ENG/TB/catalog.go` persists flag |
| REQ000571 | ENG/LS | SST page cache — replace `os.ReadFile` with block-level cache + sync.Pool. Currently `readFromSST` (`engine.go:193`) and `mergeIterator.init` (`api.go:246`) call `os.ReadFile` for every SST file on every read/iterate. For a 100 MB SST this allocates 100 MB per read. New `internal/ENG/LS/page_cache.go`: fixed-size cache (default 256 MB), 4 KB blocks keyed by `(fileID, blockOffset)`, clock-sweep eviction, `sync.Pool` for page buffers. `mergeIterator` consults cache before file read. Must handle concurrent access (read path is hot). Benchmarks: measure allocs/op and ns/op for sequential and random reads before/after | critical | L | iter-04 (L0 SST), iter-02 (buffer pool) | `ENG/LS/page_cache.go` (new); `ENG/LS/engine.go` `readFromSST` — replace `os.ReadFile` with cache lookup; `ENG/LS/api.go` `mergeIterator.init` — same; `ENG/LS/compaction.go` — compaction reads can bypass cache (bulk reads) |
| REQ000572 | WAL/RP | Fix WAL replay double-counting segment offset — `rp.go:398-404` advances `offset` by `consumed` inside the inner record loop AND again by total `off` after the loop, causing offset doubling for segments larger than 64 KB read buffer. This skips records during replay, potentially losing data on crash recovery. Fix: remove `offset += int64(consumed)` at line 399; keep only the outer `offset += int64(off)` at line 403. Also fix LSN computation at line 393: change `uint64(consumed)` to `uint64(off)` (the record's start position, not its length). Test: create a WAL with >64 KB of records in one segment, trigger replay, verify all records are recovered. Regression: existing WAL replay tests must still pass | critical | S | iter-13 (WAL recovery) | `WAL/RP/rp.go` `forEachRecord` — remove double-count line, fix LSN formula; `WAL/RP/rp_test.go` — new test with large segment |
| REQ000573 | TXN/VL | Fix commit ordering: WAL write BEFORE version chain commit — `protocol.go:200-234` commits version chains (step 3, line 211) BEFORE writing WAL RTCommit record (step 4, line 226). If WAL write fails, version chains are committed but WAL doesn't record the commit — data lost on crash. Fix: reorder to (1) validate, (2) assign commitTS, (3) write WAL RTCommit, (4) sync WAL, (5) commit version chains via `node.Commit(commitTS)`. On step 3-4 failure: set PhaseAborted, version chains remain uncommitted (correct). Test: inject WAL write failure after validate, verify version chain not committed; inject failure after WAL sync, verify same | critical | M | iter-06 (TXN commit), iter-13 (WAL) | `TXN/VL/protocol.go` `Commit` — reorder steps; `TXN/VL/protocol_test.go` — failure injection tests |
| REQ000574 | ENG/LS | Add RWMutex for `engine.memtables`/`activeMem` — `engine.go:62-76` and `engine.go:102-120` have no synchronization on `memtables` slice and `activeMem` pointer. `flushActiveMemtable()` mutates both without lock. Concurrent `Write` and `Read` race. Fix: add `sync.RWMutex` to `engine` struct; `Read`/`NewIterator` take RLock; `flushActiveMemtable` takes full Lock. Keep skiplist lock-free for concurrent writes within a single memtable. Test: run `go test -race ./internal/ENG/LS/...` with concurrent read/write; verify no races reported | critical | M | iter-04 (memtable), iter-09 (concurrency) | `ENG/LS/engine.go` — add `mu sync.RWMutex` field; `Read`/`NewIterator` — `mu.RLock/RUnlock`; `flushActiveMemtable` — `mu.Lock/Unlock` |
| REQ000575 | TXN/MV | Fix uncommitted writes visible to concurrent readers — `version.go:169-175` `FindVisible` returns nodes where `endTS = maxUint64` (uncommitted), making them visible to concurrent readers. This breaks snapshot isolation. Fix: `IsVisible` must also check `committed` flag; `FindVisible` skips nodes where `committed == false` (or use `endTS != maxUint64` as committed proxy). Concurrent readers must only see committed versions. Test: start transaction A, write key X but don't commit; start transaction B, read key X — must NOT see A's uncommitted write. A commits; B still must not see it (snapshot). Only transaction C starting after A commits should see it | high | M | iter-05 (MVCC), iter-06 (TXN protocol) | `TXN/MV/version.go` `IsVisible`/`FindVisible` — filter uncommitted nodes; `TXN/MV/version_test.go` — concurrent read/write visibility test |
| REQ000576 | SQL/EX | Fix codegen batch functions no-ops — `codegen_ops.go` registers 15 batch functions (processFilterBatch, processProjectBatch, etc.) that all return input unchanged (`return batch, nil`). The `AdaptiveOp` (`adqc.go:69-177`) swaps to these after the execution threshold. Since they're no-ops, adaptive compilation silently drops all filter/project logic — a correctness bug. Fix: either (a) implement the batch functions to actually filter/project, or (b) remove the registrations and the adaptive swap-in path until real implementations exist. Option (b) is safer for now. Test: add a test that triggers the adaptive path (execute same query >threshold times) and verifies results match the interpreted path | high | M | REQ000311 (codegen framework), REQ000313 (ADQC) | `SQL/EX/codegen_ops.go` — remove no-op registrations OR implement; `SQL/EX/adqc.go` — add guard if registrations removed; `SQL/EX/adqc_test.go` — adaptive path correctness test |
| REQ000577 | SQL/EX | Fix `VectorizedFilter.WithParams` returning nil — `operators_vec.go:123-126` returns `nil` instead of `f`. Any operator chain calling `WithParams` on a VectorizedFilter gets nil, breaking chaining and causing nil-pointer panics. Fix: `return f` (not `return nil`). Test: construct a VectorizedFilter, call WithParams, verify returned operator is non-nil and functional | high | S | iter-19 (vectorized eval) | `SQL/EX/operators_vec.go` `VectorizedFilter.WithParams` — change `return nil` to `return f`; `SQL/EX/operators_vec_test.go` — new test |
| REQ000578 | SQL/EX | Fix nil params in UPDATE WHERE evaluation — `writers.go:411,506` passes `nil` for params to `Eval(u.where, &row, nil)`, but the Update operator has `u.params`. Bound `?` placeholders in UPDATE WHERE clauses silently fail to resolve. Fix: change `nil` to `u.params` at lines 411, 506. Same issue at `writers.go:673,758` for DELETE. Test: `UPDATE t SET v = v + 1 WHERE id = ?` with param=1 must work | high | S | iter-08 (SQL execute) | `SQL/EX/writers.go` lines 411, 506, 673, 758 — pass `u.params`/`d.params` instead of `nil`; `SQL/EX/writers_test.go` — parameterized UPDATE/DELETE test |
| REQ000579 | SQL/EX | Implement AND/OR short-circuit evaluation — `evalBinary` at `eval.go:243-244` evaluates both operands for `T_AND` before comparing. SQL `AND` should short-circuit: if left is false/NULL, right should not be evaluated (avoids side effects, division by zero, subquery execution). Same for `T_OR` at line 251. Fix: in `evalBinary`, for `T_AND`: evaluate left; if `!truthy(left)` return left (false/NULL); else evaluate right and return. For `T_OR`: evaluate left; if `truthy(left)` return left; else evaluate right and return. Test: `SELECT 1/0 AND 1=1` must return error (or NULL per SQLite), not crash; `SELECT 0 AND 1/0` must return 0 without evaluating right | high | S | iter-08 (SQL eval) | `SQL/EX/eval.go` `evalBinary` — add short-circuit for T_AND/T_OR; `SQL/EX/eval_test.go` — short-circuit tests |
| REQ000580 | SQL/EX | Pre-extract sort keys before sorting — `intermediate.go:172-188` sorts with `sort.SliceStable` where the comparator calls `Eval(k.Expr, &a, s.params)` and `Eval(k.Expr, &b, s.params)` for every comparison. With N rows and K sort keys, this is O(N log N * K) expression evaluations. Fix: before sorting, pre-extract sort keys into `keys := make([][]any, len(rows))` by evaluating each `OrderItem.Expr` once per row. Then sort by comparing pre-extracted keys. Reduces to O(N*K) + O(N log N). Test: benchmark Sort operator with 10K rows and complex ORDER BY expression; verify <2x improvement | high | M | iter-19 (vectorized eval), iter-08 (SQL execute) | `SQL/EX/intermediate.go` `Sort.Next` — pre-extract keys; `SQL/EX/intermediate_test.go` — benchmark |
| REQ000581 | SQL/EX | Replace GLOB regex compilation with byte-matching — `eval.go:1911` `globMatch` calls `regexp.MustCompile` on every invocation. For `WHERE name GLOB '*.txt'` over 10K rows, this compiles 10K identical regexes. Fix: implement `globMatch` as a simple recursive byte-matcher (GLOB uses `*` = any chars, `?` = any char, `[...]` = character class — no regex features needed). Or cache compiled regexes by pattern in a `sync.Map`. Test: verify GLOB patterns work; benchmark shows no per-row regex compilation | high | S | iter-08 (SQL eval) | `SQL/EX/eval.go` `globMatch` — replace regex with byte-matcher; `SQL/EX/eval_test.go` — GLOB tests |
| REQ000582 | SQL/PS | Split `ps.go` into per-statement files — `ps.go` is 3,521 lines containing all parsing logic. Split into: `select.go` (SELECT parsing), `insert.go` (INSERT), `update.go` (UPDATE), `delete.go` (DELETE), `ddl.go` (CREATE/DROP/ALTER), `expr.go` (expression parsing), `window.go` (window functions), `trigger.go` (trigger parsing), `transaction.go` (BEGIN/COMMIT/ROLLBACK/SAVEPOINT), `explain.go` (EXPLAIN), `values.go` (VALUES). Keep `ps.go` as the entry point with `Parse()` dispatch. All methods are already `func (p *Parser) parseXxx()` — splitting is purely file reorganization with no logic changes. Test: all existing parser tests must pass; `go vet ./...` and `gofmt -s -l .` must pass | medium | M | — | `SQL/PS/ps.go` → split into 11 files; no logic changes |
| REQ000583 | SQL/PS | Add visitor pattern to AST — currently AST traversal relies on manual type-switch in `PL/memo.go:107-223` with no exhaustiveness check. Missing node types (e.g. `IntervalLiteral`, `WindowFunc`, `RaiseFunc`) silently produce different memo hashes. Fix: add `Accept(Visitor)` to `Expr` and `Stmt` interfaces; create `BaseVisitor` with default no-op implementations; implement `HashVisitor` that writes to `hash.Hash` for memo keys. Refactor `memo.go` to use the visitor. Future visitors: `TypeChecker`, `ConstantFolder`, `PlanSerializer`. Test: verify all AST node types are covered (compile-time exhaustiveness via interface satisfaction) | medium | L | iter-07 (parser) | `SQL/PS/ast.go` — add `Accept` methods; new `SQL/PS/visitor.go`; `SQL/PL/memo.go` — refactor to use visitor |
| REQ000584 | SQL/PL | Add memo eviction and schema-aware invalidation — `PL/memo.go` stores plans in `map[string]Plan` with no eviction (unbounded growth) and no schema awareness (stale plans after DDL). Fix: (1) add LRU eviction with configurable max entries (default 1024); (2) include schema version in memo key (increment on CREATE/DROP/ALTER TABLE/INDEX); (3) replace SHA-256 with xxhash for faster hashing (no security benefit here). Also add `Memo.Clear()` for DDL events. Test: run 2000 distinct queries, verify memo size stays bounded; run CREATE TABLE then SELECT, verify plan is re-compiled | medium | M | iter-08 (planner), REQ000162 (plan memo) | `SQL/PL/memo.go` — add LRU, schema version in key, xxhash; `SQL/PL/pl.go` — expose Clear(); `SQL/PL/memo_test.go` — eviction + invalidation tests |
| REQ000585 | SQL/EX | Remove duplicate WHERE evaluation in DML — `writers.go:410-418` (Update) and `writers.go:669-679` (Delete) re-evaluate `u.where`/`d.where` on every row, but the Filter child operator already evaluates the same predicate (set up at `ex.go:701,749`). Every qualifying row pays double evaluation. Fix: remove the redundant WHERE check in `Update.Next` (lines 410-418) and `Delete.Next` (lines 669-679), and their `nextFromStore` counterparts (lines 504-512, 756-764). The Filter child handles row selection. Test: UPDATE/DELETE with WHERE must produce identical results before/after; benchmark shows measurable improvement on large tables | medium | S | iter-08 (SQL execute) | `SQL/EX/writers.go` — remove redundant WHERE eval; `SQL/EX/writers_test.go` — verify correctness |
| REQ000586 | SQL/EX | Thread ExecContext through operators (eliminate globals) — `eval.go:383` `currentSubqueryPlanner`, `ex.go:32` `currentSessionID`, `store.go:98-112` `tables`/`storeSchemas`/`viewRegistry` are all package-level globals. This makes concurrent Executor instances unsafe. Fix: add `ExecContext` struct holding session ID, planner, params, store references; thread it through operators via `Operator.SetContext(ctx)` or pass in `Next(ctx)`. Start with `currentSubqueryPlanner` (most dangerous global). Test: run two Executor instances concurrently on different tables; verify no cross-contamination | medium | L | iter-08 (SQL execute) | `SQL/EX/ex.go` — new `ExecContext` struct; `SQL/EX/eval.go` — remove `currentSubqueryPlanner` global; all operators — accept context |
| REQ000587 | ENG/LS | Eliminate per-block dictionary training — `sst_dict.go:91-92` creates a new `dictTrainer` and calls `train()` for every block, scanning the block to build a frequency map. Training is O(blockSize*5) string allocations per block. Fix: train dictionary once per SST (or per memtable flush) using a sample of blocks, then share the trained dictionary across all blocks in that SST. Alternatively, use a pre-trained static dictionary for common column patterns. Test: verify compression ratio is similar; measure CPU time for flush before/after | medium | M | iter-17 (SST compression), REQ000297 (SST dict) | `ENG/LS/sst_dict.go` — train once per SST; `ENG/LS/sst_writer.go` — pass shared dict; `ENG/LS/sst_dict_test.go` — compression ratio regression test |
| REQ000588 | ENG/LS | Package-level `crc32.MakeTable` — `crc32.MakeTable(crc32.Koopman)` is called on every block checksum at `sst_writer.go:152`, `sst_reader.go:230`, `columnar.go:30`, `manifest.go:151`. Each call allocates a new table. Fix: compute once as `var crc32Table = crc32.MakeTable(crc32.Koopman)` at package level in each package (or a shared package). Also fix `manifest.go:129-131` `binary.Write` which uses reflection — replace with `binary.LittleEndian.AppendUint64`. And reuse `[8]byte` buffers instead of `make([]byte, 8)` in loops (`sst_writer.go:230-235`, `manifest.go:139-144`). Test: verify checksums are identical before/after; benchmark manifest encode/decode | medium | S | iter-04 (SST), iter-12 (catalog) | `ENG/LS/sst_writer.go`, `sst_reader.go`, `columnar.go`, `manifest.go` — package-level crc32 table; `ENG/LS/sst_test.go` — checksum regression |
| REQ000589 | ENG/LS | Remove dead code and duplicated functions — (1) `subcompact.go:185-188` `subFileName` duplicates `compaction.go:227-241` `fileName`; remove `subFileName` and call `fileName`. (2) `memtable.go:56-66` `IncRef/DecRef/RefCount` are never called — remove. (3) `flush.go:359-385` `flushManager.Get/Insert` are dead code — remove. (4) `compaction.go:138-139` `_ = lastKey/_ = lastVal` suppress unused warnings for dead variables — remove. (5) `subcompact.go:195-205` `_ = subFileName/_ = subTempPath` — remove after removing `subFileName`. Test: `go vet ./...` and `go build ./...` must pass | low | S | — | `ENG/LS/subcompact.go`, `memtable.go`, `flush.go`, `compaction.go` — remove dead code |
| REQ000590 | ENG/LS | Fix compaction error swallowing — `compaction.go:336-337` has `if err := job.Run(cm.manifest, cm.dir); err != nil {}` — empty error handler swallows all compaction failures silently. No logging, no retry, no notification. Fix: at minimum, log the error with `slog.Error`. Ideally, retry failed compaction up to 3 times with backoff. On persistent failure, surface via engine stats or callback. Test: inject a compaction error (e.g. read-only filesystem), verify it's logged | medium | S | iter-04 (compaction) | `ENG/LS/compaction.go` — add slog.Error + retry; `ENG/LS/compaction_test.go` — error injection test |
| REQ000591 | SQL/EX | Package-level `evalSoundex` map allocation — `eval.go:1971` allocates a `codes` map inside `evalSoundex` on every call. Fix: move to package-level `var soundexCodes = map[byte]byte{...}`. Also: `evalFunction` at `eval.go:600-765` is a 150-line string switch doing `strings.ToUpper` on every call. Store function names in uppercase in the parser (already done for keywords), or replace with a `map[string]funcDef` registry. Test: verify soundex results unchanged; benchmark function dispatch | low | S | iter-26 (scalar functions) | `SQL/EX/eval.go` — package-level soundex map; optionally refactor evalFunction dispatch |
| REQ000592 | ENG/LS | Unify error sentinels — two systems exist: internal `ErrKeyNotFound` (`sst_reader.go:12`) and public `ErrNotFound` (`api.go:13`). Also `index_store.go:180-189` uses string matching (`strings.HasSuffix`) to check `isNotFound` to avoid import cycle. Fix: define the sentinel in a shared package (e.g. `internal/errors/` or `ENG/LS/errors.go`) and use `errors.Is` everywhere. Remove string-matching `isNotFound`. Test: verify error handling paths work with unified sentinel | low | S | iter-04 (ENG), iter-09 (API) | `ENG/LS/sst_reader.go`, `api.go`, `index_store.go` — unified sentinel; `ENG/LS/errors.go` (new) |
| REQ000593 | SQL/PS | Fix bare `ROLLBACK` returning `(nil, nil)` — `ps.go:780-784` silently produces nil AST and nil error for bare `ROLLBACK` (without `TO`). The switch at line 822-824 checks `err != nil` but stmt=nil+err=nil falls through to return `(nil, nil)` at line 834. Fix: treat bare `ROLLBACK` as `ROLLBACK TO IMMEDIATE SAVEPOINT` (SQLite semantics) or return `RollbackTX` node. Add `parseRollback` handling for bare ROLLBACK. Test: parse `ROLLBACK;` — must produce a valid AST node, not nil | medium | S | iter-08 (parser) | `SQL/PS/ps.go` `parseRollback` — handle bare ROLLBACK; `SQL/PS/ps_test.go` — bare ROLLBACK test |
| REQ000594 | WAL/FL | `Sync()` must return batch sync errors — `FL/fl.go:122-128` `Sync()` calls `f.batchCommit.Wait()` but always returns nil, ignoring `f.syncErr`. In contrast `BatchSync()` (line 133-143) correctly returns the error. Fix: after `Wait()`, return `f.syncErr`. Test: mock fsync failure, call Sync(), verify error returned | medium | S | iter-13 (WAL) | `WAL/FL/fl.go` `Sync` — return `f.syncErr`; `WAL/FL/fl_test.go` — sync error test |
| REQ000595 | MEM/BF | `Close` must flush dirty pages — `bf.go:484-489` iterates dirty slots and clears flags but does NOT write data to disk. The hint file is written during Close referencing blocks that were never persisted. Fix: iterate dirty slots, write each to its backing file (via `BlockDevice.WriteBlock`), then clear flags, then write hint file. Test: write data, call Close(), reopen, verify data survives | medium | M | iter-02 (buffer pool) | `MEM/BF/bf.go` `Close` — add WriteBlock for dirty slots; `MEM/BF/bf_test.go` — Close+reopen test |
| REQ000596 | MEM/BF | Fix Pin/Unpin TOCTOU race — `bf.go:350-358` `Pin` acquires RLock, looks up slot, releases RLock, then increments `pinCount`. Between RLock release and pinCount.Add(1), eviction could return the slot's buffer to the pool. Fix: hold RLock through the Pin operation, or use a generation counter on the slot that `Pin` checks atomically. Test: concurrent Get/Pin/Evict with race detector | medium | M | iter-02 (buffer pool) | `MEM/BF/bf.go` `Pin` — hold RLock or use generation counter; `MEM/BF/bf_test.go` — concurrent pin/evict test |
| REQ000597 | ENG/LS | Remove unused `flushManager` duplicate tracking — `flush.go:156-157` `activeMemtable` and `frozenMemtables` fields duplicate `engine.go:23-24` `memtables` and `activeMem`. The flush manager's copies appear unused in the engine lifecycle. Fix: remove `activeMemtable`, `frozenMemtables`, `Get`, `Insert` from `flushManager`. Verify engine still works with only `engine.memtables`/`activeMem`. Test: run full test suite after removal | low | S | iter-04 (flush) | `ENG/LS/flush.go` — remove duplicate fields; `ENG/LS/flush_test.go` — verify flush still works |
| REQ000598 | ENG/LS | Make `MergeIterator` accept explicit dependencies — `api.go:208-209` `MergeIterator` takes `*engine` giving it access to all internals. Tight coupling makes testing and refactoring difficult. Fix: change constructor to accept `[]*memtable`, `*manifest`, `string dir` (and optionally the page cache from REQ000571). The engine constructs these from its own fields. Test: unit test MergeIterator with mock manifest and memtables | low | S | iter-04 (merge iterator) | `ENG/LS/api.go` — refactor MergeIterator constructor; `ENG/LS/api_test.go` — unit test with mocks |
| REQ000599 | ENG/LS | Fix prefix bloom filter writer/reader modulus mismatch — `sst_writer.go:132` uses byte count (`size`) as modulo base, but `sst_reader.go:122` uses bit count (`len(r.prefixBloom)*8`). Writer sets bits at `[0, byteCount)` while reader checks `[0, byteCount*8)`. This causes ~7/8 of prefix bloom queries to produce false negatives — the filter reports "not present" for keys that actually exist, silently dropping prefix range scan results. Fix: make reader use byte count (same as writer) or make writer use bit count (same as regular bloom at `sst_writer.go:121`). Verify `setBloomBitForSize` and `MayContainPrefix` use the same modulus. Test: insert keys with known prefixes, verify `MayContainPrefix` returns true for all inserted prefixes; add unit test with small bloom size to catch off-by-8x | critical | S | iter-04 (SST), iter-22 (secondary index) | `ENG/LS/sst_reader.go` `MayContainPrefix` — fix modulus to match writer; `ENG/LS/sst_writer.go` `setPrefixBloomBit` — verify consistency; `ENG/LS/sst_test.go` — prefix bloom round-trip test |
| REQ000600 | ENG/LS | Fix skiplist higher-level CAS linking — `skiplist.go:125-132` only CAS-links level 0 after insertion. Levels 1 through `lvl-1` compute predecessor/successor pairs but never CAS-link them. `sl.level` can be elevated to 3+ but `head.next[1]` and `head.next[2]` remain nil, so `Find` traverses higher levels finding nothing, then falls to full linear scan at level 0. The data structure degenerates from O(log n) to O(n). Fix: after the level-0 CAS succeeds, loop `for i := 1; i < lvl; i++` and CAS-link each level: `predecessors[i].next[i].CompareAndSwap(successors[i], newNode)`. Handle CAS failure by re-computing predecessors/successors for that level. Test: insert 10K random keys, verify `Find` completes in O(log n) time (benchmark); verify all levels are linked by walking `head.next[1..maxLevel]` | critical | M | iter-04 (skiplist) | `ENG/LS/skiplist.go` `Insert` — add higher-level CAS loop; `ENG/LS/skiplist_test.go` — level-linking verification + benchmark |
| REQ000601 | ENG/LS | Fix compaction not removing overlap files — `compaction.go:193-206` removes only input files from `newLevels[cj.level]` but never removes overlap files from `newLevels[cj.level+1]`. The overlap data is duplicated: old files at level+1 still contain it, and the new merged output also contains it. On each subsequent compaction, the same data is re-merged, causing unbounded disk growth. Fix: after merging, remove overlap files from `newLevels[cj.level+1]` using `removeFiles(newLevels[cj.level+1], cj.overlap)`. Also fix `compaction.go:435-438` to only grab key-range-overlapping files (not ALL files at level+1). Test: create tables that trigger L0→L1 compaction, verify disk usage stays bounded after repeated writes; verify no duplicate keys across levels | critical | M | iter-04 (compaction) | `ENG/LS/compaction.go` — remove overlap files from level+1; filter overlap by key range; `ENG/LS/compaction_test.go` — bounded disk growth test |
| REQ000602 | ENG/LS | Use `Find()` instead of full scan in `readFromSST` — `engine.go:207-212` opens a full SST iterator and scans from the beginning to find a key, even though `sstReader.Find()` (`sst_reader.go:131`) already exists and uses bloom filter + binary search on block index for O(log n) lookup. Fix: replace the iterator loop with `return reader.Find(key)`. Test: verify Get() returns correct values for keys in SSTs; benchmark point lookup before/after | high | S | iter-04 (SST read) | `ENG/LS/engine.go` `readFromSST` — replace iterator loop with `reader.Find(key)` |
| REQ000603 | ENG/LS | Add bounds checks in `openSST` for corrupt files — `sst_reader.go:41-47` slices `data[indexOffset:indexOffset+indexSize]` and `data[bloomOffset:bloomOffset+bloomSize]` without verifying `offset+size <= len(data)`. A corrupt or truncated SST with out-of-range footer values causes an out-of-bounds panic. Fix: add `if indexOffset+uint64(indexSize) > uint64(len(data)) { return nil, ErrCorrupt }` (and same for bloom). Also add validation that `indexOffset < bloomOffset` (index comes before bloom). Test: create truncated SST file, verify `openSST` returns error instead of panicking | high | S | iter-04 (SST reader) | `ENG/LS/sst_reader.go` `openSST` — add bounds checks; `ENG/LS/sst_test.go` — corrupt SST test |
| REQ000604 | ENG/LS | Fix primaryIndex.Insert dedup — `index.go:63-86` walks to the tail of the linked list and appends unconditionally. If the same key is inserted twice, both entries exist. `Find` returns the first (oldest/stale) entry. The counter is also inflated. Fix: during Insert traversal, check if a matching key already exists; if so, atomically update its value instead of appending a new node. Or: mark the old node as deleted and append a new one (CAS on a `deleted` flag). Test: insert same key twice with different values, verify `Find` returns the latest value; verify `Len()` is correct | high | M | iter-04 (primary index) | `ENG/LS/index.go` `Insert` — deduplicate; `ENG/LS/index_test.go` — dedup test |
| REQ000605 | SQL/EX | Fix `band()`/`bor()` NULL three-valued logic — `eval.go:1693-1705` treats NULL as `false` for both AND and OR. SQL requires: `NULL AND true → NULL`, `NULL AND false → false`, `NULL OR true → true`, `NULL OR false → NULL`. Currently `band(nil, true)` returns `false` (should be `NULL`), causing WHERE clauses like `col1 > 5 AND col2 = 'x'` to silently drop rows where `col2` is NULL. Fix: implement SQL truth tables — `band`: if either is false return false; if either is nil return nil; else true. `bor`: if either is true return true; if either is nil return nil; else false. Test: `SELECT NULL AND TRUE`, `SELECT NULL OR FALSE`, `SELECT NULL AND NULL`, `SELECT NULL OR NULL`; verify all return NULL | high | S | iter-08 (SQL eval) | `SQL/EX/eval.go` `band`/`bor` — implement three-valued logic; `SQL/EX/eval_test.go` — NULL AND/OR tests |
| REQ000606 | SQL/EX | Fix `evalSubstr` NULL handling — `eval.go:909-953` calls `fmt.Sprint(rawStr)` at line 917 without nil check. When `rawStr` is NULL, `fmt.Sprint(nil)` returns `"<nil>"` (5-char string). SQL `SUBSTR(NULL, ...)` must return NULL. Fix: add `if rawStr == nil { return nil, nil }` before line 917. Also check `startV` for nil after line 922 — `toInt64(nil)` returns `(0, false)` which hits `ErrEval`, but should return NULL. Test: `SELECT SUBSTR(NULL, 1, 3)` → NULL; `SELECT SUBSTR('hello', NULL, 2)` → NULL | high | S | iter-08 (SQL eval) | `SQL/EX/eval.go` `evalSubstr` — add nil checks; `SQL/EX/eval_test.go` — SUBSTR NULL tests |
| REQ000607 | SQL/EX | Fix `CAST('false' AS BOOLEAN)` — `eval.go:499-500` calls `truthy(v)` for `T_BOOL` cast. `truthy("false")` returns `true` because any non-empty string is truthy. SQLite's `CAST('false' AS BOOLEAN)` returns 0 (false). Fix: for string inputs, parse `"0"`, `"false"`, `"FALSE"`, `""` as false; everything else as true. For `[]byte`, check first byte. Test: `SELECT CAST('false' AS BOOLEAN)` → 0; `SELECT CAST('true' AS BOOLEAN)` → 1; `SELECT CAST('' AS BOOLEAN)` → 0; `SELECT CAST('1' AS BOOLEAN)` → 1 | high | S | iter-08 (SQL eval) | `SQL/EX/eval.go` `evalCast` T_BOOL case — string-aware truthiness; `SQL/EX/eval_test.go` — CAST bool tests |
| REQ000608 | SQL/EX | Fix vectorized comparisons ignoring NULL bitmap — `eval_vec.go:184-213` and all `compareInt64Cols`, `compareFloat64Cols`, `compareStringCols` functions compare values without checking `col.Nulls[i]`. If a value is NULL (zeroed in backing slice), it may incorrectly match literal `0` or fail other comparisons. In SQL, `NULL = 5` must evaluate to NULL (row excluded). Fix: in every vectorized comparison, check `if left.Nulls[i] || right.Nulls[i] { continue }` (skip NULL rows, they don't match any predicate). For column-literal variants, check `col.Nulls[i]` only. Test: create table with NULL values, run `WHERE col = 5` and `WHERE col != 5`, verify NULLs are excluded from both; benchmark to ensure NULL check doesn't regress performance | high | M | iter-19 (vectorized eval) | `SQL/EX/eval_vec.go` — add NULL checks in all compare functions; `SQL/EX/eval_vec_test.go` — NULL in vectorized path test |
| REQ000609 | SQL/EX | Fix `batchToRow` missing column names — `eval_vec.go:487` sets `row.Cols = append(row.Cols, "")` instead of `batch.Cols[c].Name`. When the fallback path (`evalRowFallback`) evaluates column references like `x > 5`, `Eval` looks up `x` by name but all column names are empty, so lookup fails and `x` resolves to the string literal `"x"` instead of the column value. Fix: change line 487 to `row.Cols = append(row.Cols, batch.Cols[c].Name)`. Test: run a query that triggers the vectorized→fallback path with a column reference in the expression; verify correct results | high | S | iter-19 (vectorized eval) | `SQL/EX/eval_vec.go` `batchToRow` — use `batch.Cols[c].Name`; `SQL/EX/eval_vec_test.go` — fallback column reference test |
| REQ000610 | SYS/SE | Fix `sessionStateMap` memory leak — `SE/se.go:61` `sessionStateMap` is a global `map[uint64]*sessionState` that grows monotonically. Every session calls `getOrCreateSessionState` (line 65-79) inserting entries, but no deletion on session close. In a long-running server with connection churn, this leaks memory. Fix: add a `removeSessionState(id)` called from session `Close()`/cleanup. Or use `sync.Map` with periodic eviction. Also fix the data race at lines 173-179 and 326-352: `changesCount`/`lastInsertRowID`/`totalChanges` are plain `int64` read/written from different goroutines — change to `atomic.Int64`. Test: open/close 10K sessions, verify `sessionStateMap` size stays bounded; run `go test -race` on session tests | high | M | iter-09 (SYS), iter-14 (shutdown) | `SYS/SE/se.go` — add cleanup in Close; atomic fields; `SYS/SE/se_test.go` — session lifecycle + race test |
| REQ000611 | SYS/SY | Fix shared Executor race — `SY/sy.go:441-443` returns a single `*executor.Executor` via `e.Executor()`. `TX/tx.go:73-76` calls `SetSnapshot(ts)` and `SetSnapshot(0)` via defer. Two concurrent sessions call `Transaction.Query` → clobber each other's `snapshotTS`. Fix: create a per-transaction executor copy (lightweight struct with snapshot/sessionID fields that don't share mutable state), or thread snapshot through context instead of storing on executor. Test: run two transactions concurrently with different snapshots, verify isolation | high | M | iter-06 (TXN), iter-09 (concurrency) | `SYS/SY/sy.go` — per-txn executor or context-based snapshot; `SYS/TX/tx.go` — remove shared mutable state; `SYS/SY/sy_test.go` — concurrent snapshot test |
| REQ000612 | ENG/LS | Fix catalog `flushLocked` missing fsync before rename — `catalog.go:929-934` calls `os.WriteFile` then `os.Rename` without `f.Sync()`. On crash after Rename returns, the OS may not have flushed the temp file's pages, losing the write. The docstring at line 599 claims "fsync'd to disk before returning." Fix: open the temp file, write, `f.Sync()`, then rename. Test: write catalog, crash (kill -9) before sync, verify old catalog survives; write catalog, crash after sync, verify new catalog survives | medium | S | iter-12 (catalog persistence) | `ENG/LS/catalog.go` `flushLocked` — add fsync; `ENG/LS/catalog_test.go` — durability test |
| REQ000613 | ENG/LS | Fix `GetByID` shallow copy — `catalog.go:811` returns `cp := *entry` which shares `Columns`, `Unique`, `Indexes`, `ColumnStats` slices with the cache entry. A caller mutating `cp.Columns` directly corrupts the in-memory catalog. Compare with `List()` (line 876) which deep-copies. Fix: deep-copy slice fields: `cp.Columns = append([]Column(nil), entry.Columns...)`, same for `Unique`, `Indexes`, `ColumnStats`. Test: call `GetByID`, mutate returned entry's Columns, verify catalog is unchanged | medium | S | iter-12 (catalog) | `ENG/LS/catalog.go` `GetByID` — deep-copy slices; `ENG/LS/catalog_test.go` — mutation isolation test |
| REQ000614 | ENG/LS | Fix `PutStats` no rollback on disk failure — `catalog.go:1073-1083` modifies `entry.ColumnStats` before calling `flushLocked()`. If flush fails, the in-memory cache is mutated but disk isn't, leaving cache inconsistent. Unlike `Put` (line 642) and `Delete` (line 663) which both rollback on failure. Fix: snapshot the old `ColumnStats` before modification; on flush failure, restore from snapshot. Test: inject write failure during PutStats, verify catalog state is unchanged | medium | S | iter-12 (catalog) | `ENG/LS/catalog.go` `PutStats` — rollback on failure; `ENG/LS/catalog_test.go` — failure rollback test |
| REQ000615 | ENG/LS | Fix bloom filter `int(uint32)` 32-bit panic — `sst_writer.go:121` and `sst_reader.go:106` cast `uint32` to `int` then use as modulo. On 32-bit platforms, `int` is 32 bits; `int(h1)` for `h1 > 0x7FFFFFFF` yields negative. `bucket1/8` and `bucket1%8` produce negative indices → panic. Fix: use `int(h1 % uint32(bitCount))` (modulo before cast) or use `uint` for the arithmetic. Test: run tests on GOARCH=386, verify no panics | medium | S | iter-04 (SST) | `ENG/LS/sst_writer.go`, `sst_reader.go` — fix modulo before cast; test on 32-bit |
| REQ000616 | ENG/LS | Fix frozen memtable iteration order — `engine.go:156-163` iterates frozen memtables in forward order (oldest first). If a key exists in multiple frozen memtables, the oldest version is returned. For MVCC correctness, must iterate newest-first (reverse) so the most recent frozen version wins. Fix: reverse the iteration: `for i := len(e.memtables) - 1; i >= 0; i--`. Test: write key K to memtable, flush (creates frozen), write K again with different value to new active, flush again; read K — must return the second value | medium | S | iter-04 (engine read) | `ENG/LS/engine.go` `Read` — reverse frozen memtable iteration; `ENG/LS/engine_test.go` — multi-version read test |
| REQ000617 | TXN/VL | Fix Rollback bypassing WAL — `TX/tx.go:166-198` calls `eng.Insert`/`eng.Delete` directly on the engine, bypassing the WAL writer. Rollback writes are not WAL-logged. If the process crashes during rollback, partial rollback state is unrecoverable. Fix: route rollback writes through the WAL writer (same path as normal writes), or add a special "rollback" WAL record type that the replayer can apply. Test: start transaction, write data, crash during rollback, verify replay restores consistent state | medium | M | iter-06 (TXN), iter-13 (WAL) | `SYS/TX/tx.go` `Rollback` — route through WAL; `SYS/TX/tx_test.go` — crash-recovery test |
| REQ000618 | SYS/SE | Fix `ReleaseSavepoint` no-op — `SE/se.go:272-287` acquires session lock and checks for active transaction but does nothing with the savepoint name. `RELEASE SAVEPOINT` should remove the savepoint from the stack. Without this, the stack grows indefinitely and a subsequent `ROLLBACK TO` of a released savepoint should fail but succeeds. Fix: remove the named savepoint from `t.savepoints` slice. Test: `SAVEPOINT sp1; RELEASE SAVEPOINT sp1; ROLLBACK TO sp1;` → must error | medium | S | iter-08 (SQL execute) | `SYS/SE/se.go` `ReleaseSavepoint` — remove from stack; `SYS/SE/se_test.go` — release+rollback test |
| REQ000619 | SQL/EX | Fix `SIGN(NULL)` returning 0 — `eval.go:1338-1339` returns `int64(0)` for NULL input. SQL `SIGN(NULL)` must return NULL. SQLite's `sign(NULL)` returns NULL. Fix: change to `return nil, nil`. Test: `SELECT SIGN(NULL)` → NULL | medium | S | iter-26 (scalar functions) | `SQL/EX/eval.go` `evalSign` — return nil for nil input |
| REQ000620 | SQL/EX | Fix `INSTR(NULL, ...)` returning 0 — `eval.go:1306-1314` returns `int64(0)` for NULL input in both arguments. SQL `INSTR(NULL, 'x')` and `INSTR('x', NULL)` must both return NULL. Fix: change both to `return nil, nil`. Test: `SELECT INSTR(NULL, 'x')` → NULL; `SELECT INSTR('x', NULL)` → NULL | medium | S | iter-26 (scalar functions) | `SQL/EX/eval.go` `evalInstr` — return nil for nil input |
| REQ000621 | SQL/EX | Fix `OCTET_LENGTH(blob)` — `eval.go:1244-1245` calls `fmt.Sprint(v)` on `[]byte`, producing Go's `[104 101 108 108 111]` representation (length 33 for "Hello"). `OCTET_LENGTH(X'48656C6C6F')` should return 5. Fix: for `[]byte`, return `int64(len(v))` directly. Test: `SELECT OCTET_LENGTH(X'48656C6C6F')` → 5; `SELECT OCTET_LENGTH('')` → 0 | medium | S | iter-26 (scalar functions) | `SQL/EX/eval.go` `evalOctetLength` — handle []byte directly |
| REQ000622 | SQL/EX | Fix `DropIndex` data race — `writers.go:1335-1344` iterates `tableIDs` map after releasing `storeMu`. Concurrent `CREATE TABLE`/`DROP TABLE` can modify the map. Fix: extract table ID set under `storeMu.RLock`, then iterate after unlocking. Test: concurrent DROP INDEX + CREATE TABLE with race detector | medium | S | iter-28 (SQL execute) | `SQL/EX/writers.go` `DropIndex.Next` — snapshot tableIDs under lock |
| REQ000623 | FIL/DF | Fix `bufPool` alignment waste — `df.go:29-37` allocates `DefaultBlockSize+4096` and trims from front until aligned, wasting ~2048 bytes per allocation on average. Fix: use `sliceHeader.Data` alignment via `unsafe.Pointer` math at pool creation time. Or pre-allocate aligned buffers. Test: verify all returned buffers are aligned; measure memory usage before/after | low | S | iter-01 (block device) | `FIL/DF/df.go` — aligned buffer allocation |
| REQ000624 | FIL/DF | Fix `WriteBlock` unnecessary zeroing — `df.go:194-196` zeroes `poolBuf[:DataLen-ChecksumLen]` (4092 bytes) on every write. For small payloads, most zeroing is wasted. Fix: zero only `poolBuf[len(data):DataLen-ChecksumLen]`. Test: verify block contents are correct; benchmark write throughput before/after | low | S | iter-01 (block device) | `FIL/DF/df.go` `WriteBlock` — partial zeroing |
| REQ000625 | LOG/HK | Fix goroutine-per-hook-per-event — `hook.go:76-79` dispatches each event to each hook in a new goroutine. Under high log throughput with multiple hooks, this creates massive goroutine churn. Fix: call hooks sequentially in the dispatch loop, or use a bounded worker pool. Test: log 100K events with 3 hooks, measure goroutine count before/after | low | S | iter-00 (LOG) | `LOG/HK/hook.go` — sequential dispatch or worker pool |
| REQ000626 | LOG/LG | Optimize rotation check frequency — `logger.go:190-214` checks rotation on every log call via two atomic loads (`rotationFn`, `curSize`). Under high throughput this adds overhead. Fix: gate rotation check behind a batch counter (e.g., check every 1024 writes) or only when `curSize` exceeds a soft threshold. Test: benchmark log throughput before/after | low | S | iter-00 (LOG) | `LOG/LG/logger.go` — batched rotation check |
| REQ000627 | SYS/SE | Fix `lock()` deadline check after mutex — `se.go:369-370` calls `s.mu.Lock()` before checking deadline. A goroutine that could fast-path-reject still contends for the mutex. Fix: check deadline before `s.mu.Lock()`. Test: verify timeout behavior under contention | low | S | iter-09 (concurrency) | `SYS/SE/se.go` `lock` — check deadline first |
| REQ000628 | SYS/SY | Fix `Engine.Open` dead code — `sy.go:391-398` `Open` method always returns `ErrNotOpen`. The actual open is done by the package-level `Open` function (line 74). The method is unreachable success path. Fix: either remove the method or make it delegate to the package-level function. Test: verify Engine.Open works if called directly | low | S | iter-09 (API) | `SYS/SY/sy.go` — remove or fix Engine.Open method |
| REQ000629 | SYS/SY | Fix `closeBestEffort` skipping executor — `sy.go:293-297` reverse teardown lists EX but the close loop doesn't call any stop for the executor. Its resources (planner cache, store adapter) are abandoned. Fix: add executor close/cleanup to the teardown sequence. Test: verify no goroutine leaks after Engine.Close | low | S | iter-14 (shutdown) | `SYS/SY/sy.go` `closeBestEffort` — add executor cleanup |
| REQ000630 | BK | Fix backup not acquiring engine lock — `bk.go:50-134` copies files without any engine lock. Docstring says "Writers are blocked during the copy" but there is no mechanism. Backup can race with concurrent writes, producing inconsistent snapshot. Fix: acquire engine read lock (or snapshot mechanism) before copying. Test: concurrent write + backup, verify backup is consistent | high | M | iter-09 (API) | `BK/bk.go` `Backup` — acquire engine lock/snapshot |
| REQ000631 | SQL/EX | Propagate Eval errors in function evaluators — `eval.go:604,610,618,625,633,...` discards errors from `Eval(...)` with `_`. If a sub-expression fails (type mismatch, subquery error), the error is swallowed and the function returns a potentially wrong result. Fix: change all `v, _ := Eval(...)` to `v, err := Eval(...); if err != nil { return nil, err }` in `evalFunction`, `evalConcat`, `evalReplace`, `evalRound` arg[1], etc. Test: create a function call with a failing sub-expression, verify error propagates | medium | M | iter-08 (SQL eval) | `SQL/EX/eval.go` — propagate errors in all function evaluators |
| REQ000632 | ENG/LS | Fix flush orphans SST on manifest failure — `flush.go:118+54-57` renames SST to final path, then if `manifest.Apply()` fails, calls `os.Remove(tmpPath)` — but tmpPath no longer exists (it was renamed). The SST at `finalPath` is orphaned. Fix: on manifest failure, remove `finalPath` instead of `tmpPath`. Or: write manifest before rename. Test: inject manifest write failure, verify no orphaned SST files | low | S | iter-04 (flush) | `ENG/LS/flush.go` — fix cleanup path |
| REQ000633 | ENG/LS | Fix `pkIterator` unsafe against concurrent deletes — `index.go:129-131` captures head pointer without lock. Concurrent `Delete` can unlink nodes while iterating. Nodes are not freed (Go GC), so iterator follows stale pointers and may yield logically deleted entries. Fix: either snapshot the linked list under read lock, or use a generation counter to detect deletions. Test: concurrent insert + delete + iterate with race detector | low | M | iter-04 (primary index) | `ENG/LS/index.go` `Iterator` — safe snapshot; `ENG/LS/index_test.go` — concurrent iteration test |
| REQ000634 | ENG/LS | Fix `ManualCompact` sleep-based synchronization — `compaction.go:411` uses `time.Sleep(10ms)` to wait for compaction. If compaction is slow, 10ms is insufficient. Fix: use a `sync.WaitGroup` or channel signaled when compaction completes. Test: verify ManualCompact blocks until compaction actually finishes | low | S | iter-04 (compaction) | `ENG/LS/compaction.go` `ManualCompact` — proper synchronization |
| REQ000635 | FIL/FS | Fix `Validate` misleading error — `fs.go:34-36` returns `ErrNotAbsolute` ("path must be absolute") when the path IS absolute (it wants relative paths). The error message is backwards. Fix: rename to `ErrMustBeRelative` or change message to "path must be relative". Test: verify error message matches actual constraint | low | S | iter-01 (file I/O) | `FIL/FS/fs.go` — fix error name/message |


## DONE

| ID | Subsystem | Requirement | Iteration |
|---|---|---|---|
