## TBD
| ID | Subsystem | Requirement | Priority | Effort | Deps | Touches |
| --- | --- | --- | --- | --- | --- | --- |
| REQ000987 | EX/store | **No `TableHandle` abstraction — raw Store used everywhere with repeated schema+key+encode pattern.** Every caller (`writers.go`, `operators.go`, `integrity.go`) independently: (1) looks up the schema via `schemaFor`, (2) builds the key prefix, (3) calls `encodeRow`, (4) calls `store.Put/Get/Delete`. This three-step pattern is duplicated across 10+ call sites. **Fix:** (1) Introduce a `TableHandle` struct that bundles `{store Store, schema *storeSchema, prefix []byte}` with methods `InsertRow(Row)`, `GetRow(key)`, `Scan(prefix) Iterator`. (2) Replace the repeated pattern at all call sites. (3) Add `TestTableHandle_InsertGetScan` verifying the abstraction. | medium | medium | none | EX/store.go, EX/writers.go, EX/operators.go |
| REQ001073 | EX/plan | **No semi-join optimization — `EXISTS (SELECT ...)` materializes full subquery.** `planner.go:2344-2400`: `EXISTS (SELECT 1 FROM t WHERE t.id = outer.id)` materializes the entire subquery result. Semi-join can stop after finding the first match. **Fix:** (1) Detect `EXISTS` subqueries and convert to semi-join. (2) Semi-join stops scanning the right side after finding the first match. (3) Add `TestPlanner_SemiJoin` verifying `EXISTS` uses semi-join instead of materialization. | medium | large | none | EX/planner.go |
| REQ001091 | EX/project | **Project `dataBuf` is per-Operator, not pooled — 3857 allocs per select4 run.** `intermediate.go:368` (`Project.Next`): every query allocates a new `dataBuf = make([]Value, 0, 64*dataPerRow)`. For 3857 select4 queries this is 3857 buffer allocations. **Fix:** (1) Add a `sync.Pool` of `*[]Value` slices for Project dataBuf. (2) Get from pool in `Project.Next`, return in `Project.Close()`. (3) Tune pool chunk size to 64*8=512 values (matches common 8-column output). | medium | small | none | EX/intermediate.go |
| REQ001109 | EX/plan | **No GEQO for large joins — single-start N3 produces suboptimal plans for >8 tables.** PostgreSQL uses GEQO (genetic algorithm) for joins with >12 tables. Razor switches to single-start N3 for >8 tables. For very large joins, N3 may produce suboptimal plans. **Fix:** (1) Implement GEQO with population-based search. (2) Integrate with cost model. (3) Add planning time management. (4) Add `TestGEQO_LargeJoin` verifying GEQO produces better plans than N3 for 15+ table joins. | low | large | REQ001103, REQ001104 | EX/planner.go |
| REQ001113 | EX/join | **Multi-table cross-join (implicit comma-join) with equality predicates produces incorrect cardinality — 25/3857 select4 records fail after e00d132 + REQ001155.** select4.test: 351/3857 fail at start of e00d132. After e00d132 fixed groupBushyJoins (REQ001156) and bare-column resolution, drops to 25/3857. After REQ001155 fixed ON-only join elimination, 25/3857 (no further drop; remaining failures are not ON-only). Remaining 25 are all 3-table+ comma-joins with constant-equality predicates on all sides. Symptom: got [NULL | NULL] in row 0 — join output is zero rows or zero-column output. Example: L39860 SELECT x8, e9+131 FROM t8, t9 WHERE e9=383 AND 561=e8 expects 1 row, observed 0. **Progress:** 2-table sub-items verified fixed by req001113_2table_test.go (3 tests pass). **Remaining:** 3-table+ multi-table cross-joins produce zero surviving rows when they should produce ≥1. Suspected in HashJoin.buildAndProbe, NestedLoopJoin.Next, or Project output column offsets after multi-table joins. **Fix:** (1) TestMultiTableJoin_3Table_AllPermutations — 5 representative 3-table select4 cases × 6 table-order permutations, all permutations must match. (2) TestHashJoin_CrossPredicateEnforcement and TestNestedLoopJoin_CrossPredicateEnforcement. (3) TestProject_ColumnOffset_MultiTableJoin verifying projection column offset survives 2-way and 3-way joins. (4) Run all 25 currently-failing select4 queries through fixed executor, expect 0 failures. | high | large | REQ001092, REQ001093, REQ001111, REQ001156 | SQB/OP/join.go, SQB/OP/hashjoin.go, SQB/OP/intermediate.go |
| REQ001117 | SQB/OP | **Partial: remaining files stay in EX — import cycle OP↔AD, EX type deps.** plan_node.go, source.go, shape_specialize.go reference EX-internal types (Insert/Update/Delete from writers.go, PlanNode from plan_node.go) and cannot move to OP without cascading extractions. OP cluster is complete with 17 production files. | high | none | REQ001116 | SQB/EX/plan_node.go, SQB/EX/source.go, SQB/EX/shape_specialize.go |
| REQ001118 | SQB/EV | **Partial: eval_test.go, eval_req772_test.go stay in EX as integration tests.** These test files heavily use EX-internal functions (NewExecutor, UnregisterAll, Row{Cols,Data}, etc.) and cannot move to EV without creating an EX→EV→EX cycle or rewriting tests as external package tests. EV cluster is complete with 13 files (9 test + 4 production). | high | none | REQ001116 | SQB/EX/eval_test.go, SQB/EX/eval_req772_test.go |
| REQ001119 | SQB/AG | **Partial: test files stay in EX — reference EX internals.** aggregate_vec_test.go, hashagg_planner_test.go, aggregate_distinct_iter27_test.go, window_test.go use EX-internal setup (NewExecutor, UnregisterAll, etc.). AG cluster is complete with 7 production files. | high | none | REQ001116, REQ001118 | SQB/EX/aggregate_vec_test.go, SQB/EX/hashagg_planner_test.go, SQB/EX/aggregate_distinct_iter27_test.go, SQB/EX/window_test.go |
| REQ001120 | SQB/AD | **Partial: adqc/cache/index_usage extracted; planner/plan_node/shape_specialize stay in EX.** AD dir exists with adqc.go, adqc_cache.go, adqc_fallback.go, adqc_telemetry.go, adqc_test.go, cache_stats.go, cache_stats_test.go. IndexUsage moved to SQB/DT (broke OP→AD import edge). planner.go (181KB) references EX-internal types (Insert/Update/Delete, etc.) and cannot move to AD without creating AD↔EX cycles. plan_node.go references EX types from writers.go. shape_specialize.go imports OP, and OP previously imported AD — IndexUsage move broke that cycle, but EX type deps remain for shape_specialize.go. | high | medium | REQ001116, REQ001118 | SQB/EX/planner.go, SQB/EX/plan_node.go, SQB/EX/shape_specialize.go, SQB/AD/, SQB/DT/index_usage.go |
| REQ001121 | SQB/WT | **Partial: WT dir exists; files cannot move — EX shared state deps.** All write operator files (writers.go, source.go, alter_table.go, subq.go, view.go, constraints.go, matview.go) use EX-internal shared state (CurrentTxWriter(), IsForeignKeysEnabled(), valueFromAnySlice() unexported helper). Moving to WT would require either (a) moving shared state to DT, or (b) splitting each file. Both are large efforts with cascade risk. index_usage.go is now in SQB/DT (not EX nor WT). | high | large | REQ001116, REQ001117, REQ001118 | SQB/EX/writers.go, SQB/EX/source.go, SQB/EX/alter_table.go, SQB/EX/subq.go, SQB/EX/view.go, SQB/EX/constraints.go, SQB/EX/matview.go |
| REQ001122 | SQB/UT | **Partial: pipeline.go deleted (dead code, 0 callers). sort_parallel.go blocked by UT↔EV↔UT cycle.** UT cluster exists with 23 files. Pure UT files (simd_dispatch, coerce, decimal, json, parallel, batch, txn_debug, analyze, pragma, fk, integrity, datetime) moved in PoC. sort_parallel.go imports EV.BatchValueAt, but EV already imports UT (ParseInterval, EvalDecimalCast, etc.) — moving sort_parallel.go to UT creates UT→EV→UT cycle. EX/pipeline.go was dead code (buildPipeline had 0 callers) and was deleted. EX/explain.go references PlanNode from plan_node.go (stays in EX). | high | small | REQ001116, REQ001118 | SQB/EX/sort_parallel.go, SQB/EX/explain.go |
| REQ001123 | SQB/EX | **Deferred: all 6 cluster extractions (REQ001117-122) must complete before EX finalization.** Currently SQB/EX has 14 production files (+ ~90 test files). Cannot reduce to core-only (ex.go + execctx.go) until planner, plan_node, shape_specialize, writers, source, alter_table, etc. find a new home. Requires breaking EX shared state (CurrentTxWriter, IsForeignKeysEnabled, valueFromAnySlice) into a dependency-free utility package, which is a separate architectural effort. | high | large | REQ001117, REQ001118, REQ001119, REQ001120, REQ001121, REQ001122 | SQB/EX/ex.go, all remaining SQB/EX files |
| REQ001161 | SQB/EX | **REQ001158b: delete remaining var/function re-exports from `SQB/EX/ex.go`.** Commits `094c447` and `ea13758` (REQ001158a) deleted 19 of 63 var re-exports. Batches 1-2 (`8395531`, `c69a3a6`) migrated 15 symbols with 395+ bare-name internal uses. Commit `47379e2` removed 20+ data-helper var re-exports. The last var re-export `NewSeqScanWithStore` was deleted in this pass (no bare-name internal uses remain). **All 44 var/function re-exports are now deleted from ex.go.** Remaining: ~30 `type` alias re-exports (`type SeqScan = OP.SeqScan`, etc.) that are used by bare name throughout `SQB/EX/` (e.g., 36 `*SeqScan` refs in planner.go). These are transparent type aliases — they do not create indirection or allocation overhead. **Fix for type aliases (deferred):** (1) Replace each bare type name with the source-package prefix. (2) Delete the type alias line. (3) Build+test after each batch. Estimated scope: ~100+ bare refs across 30 type aliases. | low | large | REQ001158 | SQB/EX/ex.go |
| REQ001159 | build | **Add dependency check (`depcheck`) — codify the current package import graph as the baseline; fail CI on new violations.** No automated enforcement of the subsystem/cluster layering rule today. The dependency report (`docs/compose/reports/dependency-structure-principles.md`, 2026-06) recommends a `tests/depcheck/` Go program that reads package import paths and checks against an allowlist. The allowlist must be derived from the **current** package layout (no `internal/types/` — see REQ001160). **Fix:** (1) Create `tests/depcheck/main.go` (or a `_test.go` so it runs under `go test`) that walks `go list -deps ./...` and asserts each package only imports from layers at-or-below its own. (2) Define the allowlist per ARCH.md §Dependency Order: LOG→FIL→MEM→WAL→ENG→TXN→SQF→SQB→SYS, plus intra-SQB cluster rules from `SQB.md`. (3) Land as a **warning** (not failure) during iter-36/37. (4) Promote to failure after REQ001123 (SQB finalization) ships. (5) Document the rules in `docs/development/DEPENDENCIES.md`. (6) Add depcheck to the CI script. | high | medium | none | tests/depcheck/, CI, docs/development/DEPENDENCIES.md |
| REQ001160 | docs | **Reject `internal/types/` proposal from `docs/compose/reports/dependency-structure-principles.md` — do not create a neutral `internal/types/` package at "layer 3.5".** The report's central architectural move (extract `Operator`/`Row`/`Value`/`ExecContext` into a new `internal/types/` package between MEM and WAL) was reviewed and rejected on 2026-07-01. Reasons: (1) the "SQF ↔ SQB import cycle" it claims to break does not exist (`SQF/PL` has zero imports from `internal/SQB/**`); (2) `Operator`/`Row` ownership is documented (ARCH.md §Cross-Subsystem Interfaces) as belonging to `SQB/DT` by design; (3) the Subsystem/Function Cluster rule (AGENTS.md) requires every package to be a cluster of a subsystem — there is no "layer 3.5" slot; (4) `ExecContext` is not a kit primitive (it carries per-statement runtime state); (5) `Value` ownership is already a three-package chain `SYS/AP` → `SQF/PL` → `SQB/DT` and adding a fourth alias layer creates ambiguity, not clarity. **Fix:** (1) No code action. (2) Human action: apply the design-doc diffs in `docs/compose/reports/design-doc-update-list.md` to `docs/design/ARCH.md` and `docs/design/subsystems/SQB.md` (AI cannot edit `docs/design/` per Design Protection rule). (3) Track any future schema-cluster or session-counter ownership decision as a separate REQ requiring human design-doc sign-off. | high | small | none | docs/design/ARCH.md, docs/design/subsystems/SQB.md |
| REQ001162 | EX/join | **3-table+ implicit comma-join returns zero rows for SLT select4 cases — HashJoin/NLJ output column shuffling.** Discovered while shipping REQ001113: even after e00d132 + REQ001155 closed, select4.test still has 25/3857 failing records, all 3-table+ comma-joins. Symptom: SLT rowsort output is got [NULL | NULL] in row 0 (e.g., L39860 SELECT x8, e9+131 FROM t8, t9 WHERE e9=383 AND 561=e8 → 0 rows, want 1). Both tables have matching per-table-filtered rows but the join produces no output. Suspected: (a) HashJoin.buildAndProbe drops the cross-table equality predicate when both sides are already per-table-filtered; (b) NestedLoopJoin.Next iterates row pairs but never evaluates the join predicate; (c) Project output column offsets are misaligned after multi-table joins, so x8 and e9+131 map to wrong scan columns. **Fix:** (1) Reproduce minimal 3-table case with hand-seeded data, isolate which operator produces zero rows. (2) Add TestHashJoin_CrossPredicateEnforcement and TestNestedLoopJoin_CrossPredicateEnforcement with both sides single-table-filtered + cross-table predicate. (3) Add TestProject_ColumnOffset_MultiTableJoin verifying the projection column offset survives 2-way and 3-way joins. (4) Run all 25 currently-failing select4 queries through the fixed executor and verify zero failures. **Touches:** SQB/OP/hashjoin.go, SQB/OP/join.go, SQB/OP/intermediate.go. See iter-29.5 req001113-1155-1156-plan.md Gap Analysis Bug 1. | high | large | REQ001113, REQ001155 | SQB/OP/hashjoin.go, SQB/OP/join.go, SQB/OP/intermediate.go |
| REQ001163 | SQF/RE | **Memo key computed on original AST, not rewritten — plan cache misses for constant-foldable queries.** `planner.go:400-410`: the memo key is computed via `SerializeKey(stmt)` on the original parsed AST, but planning uses `RE.Rewrite(stmt)` which constant-folds literals and simplifies boolean expressions. Two semantically equivalent queries that rewrite to identical ASTs (e.g., `SELECT 1+2 FROM t` vs `SELECT 3 FROM t`) get different memo keys and miss the cache. The rewritten AST is what actually drives the planner, so the key should be derived from it. **Fix:** (1) Move `RE.Rewrite(stmt)` before `SerializeKey(stmt)` in `planner.go:400-410`. (2) Compute the memo key on the rewritten AST. (3) Add `TestMemo_CacheHitOnConstantFoldedQueries` verifying that `SELECT 1+2 FROM t` and `SELECT 3 FROM t` share the same memo key. (4) Add a benchmark `BenchmarkPlan_ConstantFoldedCacheHit` measuring plan cache hit rate improvement. | medium | small | none | SQB/EX/planner.go, SQF/RE/rewrite.go |
| REQ001164 | SQF/RE | **Input mutation bug — `rewriteInsert`, `rewriteUpdate`, `rewriteCreateTable` mutate the original AST.** `rewrite.go:69-110`: these three functions do `out := *s` (shallow struct copy) then mutate `out.Values[i]`, `out.Set[i]`, or `out.Cols[i]` in-place. Since slice headers are copied but the backing array is shared, the original statement's slices are mutated. The doc comment on `Rewrite` says "The original statement is not mutated" — this is false for these three functions. Currently harmless because the planner doesn't reuse the original after rewriting, but it's a latent bug: any caller that holds onto the original (e.g., EXPLAIN, logging, retry) will see a mutated AST. **Fix:** (1) In `rewriteInsert`, deep-copy `out.Values` before mutating rows (copy the outer slice and each inner row slice). (2) In `rewriteUpdate`, deep-copy `out.Set` before mutating `Pair.Val`. (3) In `rewriteCreateTable`, deep-copy `out.Cols` before mutating `ColDef.Default`. (4) Add `TestRewrite_DoesNotMutateInput` for each of the three statement types, verifying the original AST is unchanged after `Rewrite`. | medium | small | none | SQF/RE/rewrite.go |
| REQ001165 | SQF/RE | **No benchmarks in RE package — impossible to measure rewrite cost or verify optimizations don't regress.** The RE package has 3,864 lines across 6 files but zero `Benchmark*` functions. This makes it impossible to (a) measure the cost of rewriting on representative query shapes, (b) verify that constant-folding optimizations don't regress, or (c) measure the impact of REQ001163 (memo key on rewritten AST). **Fix:** (1) Add `BenchmarkRewrite_SelectSimple` (SELECT with constant-folding), `BenchmarkRewrite_SelectComplex` (nested expressions, CASE, subqueries), `BenchmarkRewrite_Insert`, `BenchmarkRewrite_Update`, `BenchmarkRewrite_Delete`. (2) Add `BenchmarkRewriteExpr_ConstantFold` and `BenchmarkRewriteExpr_DeepTree`. (3) Add `BenchmarkSplitAnd_LargeConjuncts`. (4) Record baseline numbers. | low | small | none | SQF/RE/rewrite_test.go |
| REQ001167 | SQF/RE | **`SplitAnd`/`SplitOr` called repeatedly on same WHERE expression — redundant tree traversal.** The planner calls `RE.SplitAnd(whereExpr)` at 6+ locations (`planner.go:2079, 2103, 2171, 3120, 5251, 5759, 6175`). Each call recursively walks the AND tree from scratch. For queries with deep conjunct chains, this is O(n²) traversal across the planner's lifetime for a single query. **Partial fix:** Added `BenchmarkSplitAnd_RepeatedCalls` demonstrating overhead. **Remaining:** (1) Add a `conjuncts []Expr` field to the planner's internal state for the current query, computed once after rewriting. (2) Replace all `RE.SplitAnd(whereExpr)` calls with the cached field. | low | small | none | SQF/RE/rewrite.go, SQB/EX/planner.go |
| REQ001168 | SQF/RE | **`flattenSubquery` is overly narrow — misses common single-row subquery patterns.** `rewrite.go:609-631`: only handles `SELECT <literals> FROM <nothing> WHERE <nothing>`. It misses (a) `SELECT <literals> FROM (VALUES ...)` — a known single-row source, (b) `SELECT <literals> FROM t WHERE 1=0` — a known-empty source, (c) `SELECT <literals> FROM t WHERE <constant-equality>` — a known-single-row source if the equality is on a PK. Expanding it would turn more `IN (SELECT ...)` into hash lookups at plan time, avoiding runtime subquery execution. **Fix:** (1) Extend `flattenSubquery` to recognize `FROM (VALUES ...)` subqueries with literal rows. (2) Extend to recognize `WHERE 1=0` (empty result → empty list). (3) Extend to recognize `WHERE pk = <literal>` on a known PK column (single-row result). (4) Add `TestFlattenSubquery_Values`, `TestFlattenSubquery_WhereFalse`, `TestFlattenSubquery_PKEquality`. | medium | medium | none | SQF/RE/rewrite.go |