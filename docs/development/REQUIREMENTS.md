## TBD
| ID | Subsystem | Requirement | Priority | Effort | Deps | Touches |
| --- | --- | --- | --- | --- | --- | --- |
| REQ001271 | DBG/DC | **SQL PRAGMAs for debug tracing orphaned — HandleDebugPragma defined in pragma_debug.go but never wired into the PRAGMA dispatch path. Join tracing only works via socket commands.** The code for debug_join_tracing, debug_join_flush, debug_join_filter, debug_join_summary PRAGMAs exists in UT/pragma_debug.go (HandleDebugPragma) but is never called from the PRAGMA dispatch path in WT.Pragma.Next(). This means all the debug infrastructure (JoinTracer, BufferedTracer, 5 event types, 3 verbosity levels) is inaccessible via SQL — developers can only use socket commands (nc -U <dbdir>/.debug/debug.sock). Missing //go:build !debug stub for HandleDebugPragma also prevents compilation without debug tag. **Fix:** (1) Wire HandleDebugPragma into the PRAGMA dispatch path in WT.Pragma.Next() — route recognized debug PRAGMAs to DC.Control.EnableTrace and the BufferedTracer. (2) Add //go:build !debug stub file pragma_nodebug.go returning ErrRequiresDebugBuild. (3) Add TestDebugPragma_JoinTracing_OnOff and TestDebugPragma_JoinTracing_Flush verifying PRAGMAs work end-to-end. (4) Update docs/development/DEBUG.md SQL PRAGMA examples. | high | small | none | SQB/UT/pragma_debug.go, SQB/UT/pragma_nodebug.go (new), SQB/WT/pragma.go |
| REQ001249 | SQB/EX | **Covering-index scan — skip heap row fetch when all projected columns are in index.** When a query's projection is a subset of the index columns (plus primary key), the index alone provides all data — no heap row fetch is needed. SQLite documents this: "can make many queries run twice as fast." Razordata has `IndexOnlyScan` but it is not auto-emitted by the planner. **Fix:** (1) In planner, detect `IndexScan` with `Project` child where projected column set ⊆ index column set ∪ {PK}. (2) Replace with `IndexOnlyScan` that reads index entries and extracts columns directly, skipping `Store.Get(rowKey)`. (3) Support multi-column indexes with prefix coverage (leftmost N index columns cover projection). (4) Add `TestCoveringIndex_Basic` and `TestCoveringIndex_MultiCol` verifying index-only path. (5) Add `BenchmarkSelect1_CoveringIndex` on queries where index covers projection. Estimate: ~100 lines, 3 files, 0.75 day. | high | small | none | SQB/EX/planner.go, SQB/OP/index_only_scan.go, SQB/OP/operators.go |
| REQ001287 | SQB/OP | **DEFERRED — pruneRowCols allocates 10.3% of total heap — buffer reuse causes aliasing.** Naive buffer reuse (Cols/Types/Data/ColIndex) corrupts Rows held by downstream operators that don't clone data. Requires deeper analysis of operator data-flow lifetime before safe reuse is possible. | high | small | none | SQB/OP/operators.go |
| REQ001292 | SQB/EV | **Correlated scalar subquery re-evaluated per row — cache or pre-bind correlated bounds.** EXPLAIN shows `PROJECT ? [interpreted]` for `(SELECT count(*) FROM t1 AS x WHERE x.c>t1.c AND x.d<t1.d)` — the subquery plan is opaque and executed via `evalScalarSubquery` per row. For 30 rows, this runs 30 full subquery executions against the same table. The `correlatedLRU` cache exists but uses string key serialization (`strings.Join`), adding allocation overhead per cache lookup. **Fix:** (1) Pre-compute correlated column indices at plan time (avoid per-row string key construction). (2) Use `bytes.Buffer` pool for cache key serialization instead of `strings.Join`. (3) Add `BenchmarkEvalScalarSubquery_CorrelatedCache` measuring cache hit vs miss for 1K/10K row workloads. (4) Verify select1/select2/select4 pass. Estimate: ~40 lines, 1 file, 0.25 day. | high | small | none | SQB/EV/eval.go |
| REQ001391 | SQB/EX + tests/sqlcmp | **SLT corpus gap: `wal_mode.test` does not exist.** REQ001297 item (6) ("Verify SLT `wal.test` passes after impl") and REQ001299's SLT concurrency checks reference files that **do not exist** in this repo's SLT corpus. `tests/sqlcmp/corpus` is the `jzombie/sqlite-sqllogictest-corpus` submodule (HEAD `515601a Change output_dir`); `git ls-tree -r HEAD --name-only` on its `test/` tree returns only `select1..select5.test` plus `evidence/`, `index/`, `random/` subdirs. No `wal.test`, `wal3.test`, `wal4.test`, or any `wal_*` file exists. The corpus is **read-only per AGENTS.md** ("Agents may run tests but must never modify corpus files"). **Fix:** (1) Create a locally-managed SLT file that does NOT live in the read-only submodule: add `tests/sqlcmp/wal_mode.test` (or similar non-submodule path) covering basic journal_mode=WAL semantics: `CREATE TABLE`, `INSERT`, `SELECT`, `PRAGMA journal_mode = WAL`, transactional rollback, frame boundary checks. (2) Update `tests/sqlcmp/slt/corpus_test.go` (or equivalent driver entry) to load files from the new local path in addition to the submodule. (3) Add `TestSLT_WalMode_Basic` proving end-to-end WAL-mode semantics on the new file. Estimate: ~150 lines (50 SLT + 100 driver glue), 1 file, 0.5 day. **Source:** audit surfacing in iteration `iter-WAL-SKELETON-REQ1297-1299.md`. | high | small | REQ001297 | tests/sqlcmp/wal_mode.test (new), tests/sqlcmp/slt/corpus_test.go |
| REQ001392 | SQB/EX | **`PRAGMA journal_mode` dispatch is a no-op.** `internal/SQB/EX/planner.go:692` lumps `journal_mode` into a catch-all `case "cache_size", "journal_mode", "synchronous", "user_version"` that returns the current value via `OP.NewPragmaResult` but never actually flips the engine into WAL-as-journal mode. Any SLT or driver that issues `PRAGMA journal_mode = WAL` observes no behavior change. REQ001297 acceptance requires this PRAGMA to enable the SQLite-style WAL pipeline (which the skeleton delivers in `internal/WAL/WR/frame.go` etc.) once the architectural swap lands. **Fix:** (1) In `planPragma`, route `journal_mode` (and `journal_mode = WAL/DELETE/MEMORY/TRUNCATE/PERSIST/OFF`) to a real switch: read returns the current mode; write triggers a state transition. (2) For `WAL`, invoke the new SQLite-WAL open path (REQ001297 item 4) and reject the transition if the engine is in single-process mode without -shm plumbing. (3) For `DELETE`, revert to LSM-recovery WAL (current behavior). (4) Surface mode via `OP.NewPragmaResult`. (5) Add `TestPragma_JournalMode_ReadWriteRoundTrip`, `TestPragma_JournalModeWAL_RequiresShm`. Estimate: ~80 lines, 1 file, 0.4 day. **Source:** audit surfacing in iteration `iter-WAL-SKELETON-REQ1297-1299.md`. | high | small | REQ001297, REQ001391 | SQB/EX/planner.go |
| REQ001393 | tests/sqlcmp/slt | **SLT driver has no PRAGMA-wal-mode wiring.** `tests/sqlcmp/slt/driver.go` and `tests/sqlcmp/slt/razor_driver.go` carry **zero** matches for `journal_mode` / `wal_mode` / `wal` PRAGMA parsing, propagation, or assertion. Even after the corpus gap (REQ001391) is filled and `journal_mode` dispatch (REQ001392) is wired, an SLT like `PRAGMA journal_mode = WAL; SELECT * FROM t1` would either be parsed by the underlying SLT runner but discarded by the driver, or fall through silently. **Fix:** (1) Audit the driver's PRAGMA-handling path (likely a dispatch function in `driver.go` and a statement-execution path in `razor_driver.go`); add explicit cases for `journal_mode` (read returns current, write calls Engine.SetJournalMode and asserts the round-trip value). (2) Add `journal_mode = ?` parameter binding so SLT files can drive multiple modes from a single test. (3) Verify that the new `tests/sqlcmp/wal_mode.test` (REQ001391) executes end-to-end through the driver. (4) Add `TestSLTDriver_JournalMode_Dispatch`. Estimate: ~60 lines, 2 files, 0.3 day. **Source:** audit surfacing in iteration `iter-WAL-SKELETON-REQ1297-1299.md`. | high | small | REQ001391, REQ001392 | tests/sqlcmp/slt/driver.go, tests/sqlcmp/slt/razor_driver.go |
| REQ001303 | WAL/RP | **WAL checkpoint modes: passive, full, truncate, restart.** SQLite supports four checkpoint modes. Passive: copy as many frames as possible without blocking writers. Full: wait for all readers to finish, then copy remaining. Truncate: same as Full, then truncate -wal to zero. Restart: same as Full, then rewind wal-index to start. **Fix:** (1) Add `walCheckpoint(mode int)` API. (2) Implement Passive: copy frames up to reader-locked position, do not wait. (3) Implement Full: take ckpt lock, wait for read locks to drain, copy remaining. (4) Implement Truncate: Full + ftruncate(-wal, 0). (5) Implement Restart: Full + reset mxFrame in shm. (6) Surface via `PRAGMA wal_checkpoint(MODE)` query PRAGMA. (7) Add `TestWalCheckpoint_AllModes`. Estimate: ~200 lines, 2 files, 1 day. | high | medium | REQ001299 | WAL/RP/checkpoint.go (new), SQB/EX/planner.go |
| REQ001304 | SYS/SY + ENG/LS | **PRAGMA auto_compact → LSM auto-trigger.** SQLite's `auto_vacuum = INCREMENTAL` reclaims pages on commit; FULL rebuilds the file. Razordata is LSM — the analog is "auto-trigger a leveled compaction when garbage ratio exceeds a threshold." **Fix:** (1) Add `auto_compact` PRAGMA accepting `none | incremental | full` (default: none). (2) `none` = current behavior. (3) `incremental` = on commit, if `sst_garbage_bytes / sst_total_bytes > 0.3` (configurable), enqueue a partial compaction for one level. (4) `full` = on commit, if ratio > 0.3, enqueue a full leveled compaction. (5) Compaction runs in the background worker pool (LS cluster has it). (6) Add `TestAutoCompact_Triggered`, `TestAutoCompact_Disabled_NoOp`. Estimate: ~120 lines, 3 files, 0.75 day. | high | medium | none | SYS/SY/init.go, ENG/LS/compaction.go, SQB/EX/planner.go |
| REQ001305 | SQB/EX + ENG/LS | **VACUUM statement → forced full compaction.** `VACUUM` is parsed by `SQF/PS/transaction.go` but unimplemented per `slt_gap_test.go:148`. On an LSM, VACUUM means "rebuild all SSTs with garbage discarded" — a full leveled compaction from L0 upward. **Fix:** (1) Wire `VACUUM` statement from SQB/EX dispatch to a new `vacuumFull()` engine method. (2) In LS, force a full compaction pass: all L0 memtable/SST → sorted run; merge all levels bottom-up, dropping tombstones and rewriting without GC gaps. (3) Block new writes during VACUUM (acquire write lock, drain in-flight). (4) `PRAGMA vacuum` is no-op when auto-compact is enabled and recent. (5) Add `TestVacuum_RebuildsSSTs`, `TestVacuum_DropsTombstones`. Estimate: ~150 lines, 3 files, 1 day. | high | medium | REQ001304 | SQB/EX/ex.go, ENG/LS/compaction.go, SQB/WT/writers_admin.go |
| REQ001306 | SQB/EX + ENG/LS | **PRAGMA incremental_vacuum(N) — partial compaction.** Equivalent to SQLite's `PRAGMA incremental_vacuum(N)`. Allows incremental reclamation of N pages per call. **Fix:** (1) Add `incremental_vacuum` PRAGMA with optional integer argument = pages to reclaim (default: all available). (2) On call, drain the freelist (or LSM GC) for up to N pages or until empty. (3) Returns number of pages reclaimed. (4) Requires `auto_vacuum = INCREMENTAL` to be effective. (5) Add `TestIncrementalVacuum_Partial`, `TestIncrementalVacuum_All`. Estimate: ~80 lines, 2 files, 0.5 day. | high | small | REQ001304 | SQB/EX/planner.go, ENG/LS/compaction.go |
| REQ001307 | SQB/WT + TXN/VL | **PRAGMA foreign_keys = ON/OFF session flag.** SQLite defaults foreign_keys = OFF for backward compat; once ON, FKs are enforced for the connection. Per-connection setting; affects only that Engine instance. **Fix:** (1) Add `foreign_keys` PRAGMA to WT.Pragma dispatch (boolean). (2) Store on the per-session state (`Engine.Session().FKEnabled`). (3) In writers_dml.go INSERT/UPDATE/DELETE path, if FKEnabled and constraint target is defined, run the enforcement check (defers to REQ001308–311). (4) PRAGMA foreign_keys is a no-op inside a transaction (SQLite behavior — must be set outside txn). (5) Add `TestForeignKeys_DefaultOff`, `TestForeignKeys_ToggleOn`, `TestForeignKeys_InsideTxn_NoOp`. Estimate: ~60 lines, 2 files, 0.25 day. | high | small | none | SQB/WT/writers_dml.go, SQB/EX/planner.go |
| REQ001308 | SQB/WT | **FK ON DELETE actions: CASCADE, SET NULL, SET DEFAULT, RESTRICT, NO ACTION.** All five actions are parsed (SQF/PS/ddl.go:181 area) but not enforced (WT/fk.go cluster doesn't exist per ARCH.md). **Fix:** (1) For each FK constraint, store the action enum. (2) On DELETE of a parent row, evaluate action: NO ACTION = check children exist (deferred or immediate per REQ001311), RESTRICT = same but error, CASCADE = delete children recursively, SET NULL = set child FK cols to NULL, SET DEFAULT = set to column default. (3) Apply during DML execution; collect into a deferred queue if txn is in immediate mode. (4) Add `TestFK_OnDelete_Cascade`, `TestFK_OnDelete_SetNull`, `TestFK_OnDelete_Restrict`, `TestFK_OnDelete_SetDefault`, `TestFK_OnDelete_NoAction`. Estimate: ~250 lines, 2 files, 1.5 days. | high | medium | REQ001307 | SQB/WT/fk.go (new), SQB/DT/schema.go |
| REQ001309 | SQB/WT | **FK ON UPDATE actions.** Mirror of REQ001308 but for parent UPDATE that changes the PK/FK-referenced columns. Same five actions; SET DEFAULT applies the new default. **Fix:** (1) When a parent row's PK is updated, re-evaluate FK action for each child. (2) CASCADE = update child FK cols to new parent PK. (3) Other actions: same as REQ001308. (4) Note: requires PK-change detection in UPDATE writer. (5) Add `TestFK_OnUpdate_Cascade`, `TestFK_OnUpdate_SetNull`, `TestFK_OnUpdate_Restrict`. Estimate: ~150 lines, 1 file, 0.75 day. | high | medium | REQ001307, REQ001308 | SQB/WT/fk.go |
| REQ001310 | SQB/WT | **FK MATCH FULL / PARTIAL / SIMPLE semantics.** MATCH SIMPLE (default): allow NULL in any FK col. MATCH FULL: all FK cols NULL or all NOT NULL. MATCH PARTIAL: at least one FK col NOT NULL. **Fix:** (1) Store MATCH mode in FK constraint. (2) In FK validation, before applying action, check MATCH: SIMPLE skips check if any FK col is NULL; FULL errors if mixed NULL/NOT-NULL; PARTIAL allows partial-NULL only if at least one NOT-NULL. (3) Test all three modes with multi-column FKs. (4) Add `TestFK_MatchSimple_NullAllowed`, `TestFK_MatchFull_MixedNull_Error`, `TestFK_MatchPartial_PartialNull_Allowed`. Estimate: ~120 lines, 1 file, 0.5 day. | high | small | REQ001308 | SQB/WT/fk.go |
| REQ001311 | SQB/WT + TXN/VL | **FK DEFERRABLE INITIALLY DEFERRED queue.** Parser accepts the syntax (ps_test.go:1490-1573). Default is INITIALLY IMMEDIATE: check at each statement. DEFERRED: collect violations, check at COMMIT. **Fix:** (1) Each FK constraint stores Deferrable and Initially flags. (2) On DML, if Deferrable+Deferred+inside-txn, add violation to a per-txn pending list (TXN/VL). (3) At COMMIT, drain pending list: for each violation, run the action; if action is CASCADE/SET NULL/SET DEFAULT, apply; if NO ACTION/RESTRICT, error → ROLLBACK. (4) IMMEDIATE: check synchronously, error inline. (5) Add `TestFK_Deferred_CollectsUntilCommit`, `TestFK_Deferred_ActionOnCommit`. Estimate: ~200 lines, 2 files, 1 day. | high | medium | REQ001307, REQ001308 | SQB/WT/fk.go, TXN/VL/protocol.go |
| REQ001312 | SQB/WT + ENG/LS | **WITHOUT ROWID: clustered-key SST layout.** Currently hard-rejected at `SQB/WT/writers_ddl.go:198`. On an LSM, the analog is: primary key columns ARE the SST row key, eliminating the secondary rowid pointer. **Fix:** (1) In SQB/WT DDL, accept `WITHOUT ROWID` and store flag in table metadata. (2) In ENG/LS, for WITHOUT ROWID tables, key SST entries by composite (pk_col1, pk_col2, …) instead of (rowid, …). (3) Secondary indexes reference PK directly (no rowid indirection — see REQ001313). (4) UPDATE that changes PK = delete + insert. (5) Add `TestWithoutRowid_BasicInsertSelect`, `TestWithoutRowid_CompositePK`, `TestWithoutRowid_UpdateChangesPK`. Estimate: ~300 lines, 4 files, 2 days. | high | large | none | SQB/WT/writers_ddl.go, ENG/LS/sst_writer.go, ENG/ID/id.go, SQB/DT/schema.go |
| REQ001313 | SQB/WT + ENG/LS | **WITHOUT ROWID: secondary index on clustered key (no rowid pointer).** When the table is WITHOUT ROWID, secondary index entries store the full PK rather than a rowid, so lookups go directly to the data without a second hop. **Fix:** (1) In ENG/ID, when creating a secondary index on a WITHOUT ROWID table, the value part of the index entry is the full PK (concatenated). (2) In IndexScan for WITHOUT ROWID, the seek returns (index_key, pk) — use pk directly to fetch the row, no rowid→row translation. (3) Verify no regression for rowid tables. (4) Add `TestWithoutRowid_SecondaryIndex_NoRowidHop`, `TestWithoutRowid_IndexOnlyScan`. Estimate: ~120 lines, 2 files, 0.75 day. | high | medium | REQ001312 | ENG/ID/id.go, SQB/OP/indexscan_strategy.go |
| REQ001314 | SQB/EX + ENG | **ANALYZE: per-column NDV (number of distinct values).** NDV is the cornerstone of selectivity estimation (already used by REQ001218 OR-to-IN and REQ001252 stats propagation). SQLite computes it via sampling; DuckDB uses HyperLogLog++. **Fix:** (1) Add ANALYZE statement dispatch in SQB/EX. (2) On ANALYZE [table], for each column, run a full or sampled scan to compute NDV. (3) Default to full scan for small tables (< 1M rows), sampled (1% reservoir) for larger. (4) Store result in `razor_stat1` catalog table as `(table, col, ndv, rowcount, null_count, min, max)`. (5) Add `TestAnalyze_ComputesNDV`, `TestAnalyze_LargeTable_Sampling`. Estimate: ~200 lines, 3 files, 1 day. | high | medium | none | SQB/EX/analyze.go (new), ENG/LS/iter.go, SQB/WT/writers_admin.go |
| REQ001315 | SQB/EX + ENG | **ANALYZE: rowcount + null count per column.** Companion to REQ001314; both metrics are required by selectivity estimators. **Fix:** (1) In the same ANALYZE scan, accumulate rowcount (table.cardinality) and per-column null_count. (2) Store alongside NDV in razor_stat1. (3) Reuse the iteration from REQ001314. (4) Add `TestAnalyze_ComputesRowCount`, `TestAnalyze_ComputesNullCount_NotNullCol`. Estimate: ~30 lines added to REQ001314's analyze.go, 0.1 day. | high | small | REQ001314 | SQB/EX/analyze.go |
| REQ001316 | SQB/EX + ENG | **ANALYZE: per-column min/max (for range propagation, REQ001252).** min/max enable the stats-propagation planner rule that derives `t2.c ∈ [min(t1.c), max(t1.c)]` from join equality. **Fix:** (1) In ANALYZE scan, for each column, track min and max via simple compare (text columns use collation). (2) Store in razor_stat1. (3) For nullable columns, NULL is excluded from min/max. (4) Add `TestAnalyze_ComputesMinMax`, `TestAnalyze_NullExcludedFromMinMax`. Estimate: ~40 lines added to REQ001314, 0.15 day. | high | small | REQ001314 | SQB/EX/analyze.go |
| REQ001317 | SQB/EX + ENG/CT | **ANALYZE: persist stats to catalog (razor_stat1).** Stats must survive restarts. **Fix:** (1) Define `razor_stat1` catalog table: `(table TEXT, col TEXT, ndv INT, rowcount INT, null_count INT, min BLOB, max BLOB)`. (2) ANALYZE writes via the same writer path as user tables. (3) On Engine.Open, load razor_stat1 into in-memory pl.StatsCatalog. (4) Add `TestAnalyze_PersistsAcrossRestart`. (5) Add migration: create razor_stat1 on first ANALYZE if absent. Estimate: ~80 lines, 2 files, 0.4 day. | high | small | REQ001314 | SQB/EX/analyze.go, ENG/CT/catalog.go |
| REQ001318 | SQB/EX | **STATS: wire persisted stats into pl.StatsCatalog at open.** pl.StatsCatalog already exists (SQB/EX/planner.go:57) and is used in selectivity estimation (predicate.go:229). The catalog needs to be populated from razor_stat1 on Engine.Open. **Fix:** (1) In SYS.Open, after loading catalog, also SELECT * FROM razor_stat1 and populate the stats catalog. (2) Verify that the existing `ls.ColumnStats{RowCount, DistinctCount, NullCount}` struct accepts the additional min/max fields. (3) Add `TestStats_WiredOnOpen`. (4) Add `TestStats_Queries_UseLoadedNDV`. Estimate: ~60 lines, 1 file, 0.25 day. | high | small | REQ001317 | SQB/EX/ex.go, SYS/SY/sy.go |
| REQ001319 | SQB/WT | **DDL SAVEPOINT: checkpoint catalog version.** The catalog (ENG/CT) has a schema version number that increments on every DDL. SAVEPOINT must snapshot the version. **Fix:** (1) In WT, on SAVEPOINT, push current `catalog.Version` onto a stack (in WT, not TXN/VL, since this is DDL state). (2) Add `catalogCheckpoint()` API. (3) On DDL, increment version; on SAVEPOINT, save current version. (4) Add `TestDDLSavepoint_CheckpointCapturesVersion`. Estimate: ~60 lines, 1 file, 0.25 day. | high | small | none | SQB/WT/alter_table.go, ENG/CT/catalog.go |
| REQ001320 | SQB/WT | **DDL SAVEPOINT: rollback catalog to checkpointed version.** Roll back DDL on `ROLLBACK TO sp`. **Fix:** (1) On `ROLLBACK TO sp`, pop the catalog version stack and restore. (2) Replay catalog from razor_stat1 to rebuild the in-memory schema. (3) Drop any new tables/indexes/triggers added after the savepoint; drop any columns added (compensating ALTER). (4) Add `TestDDLSavepoint_RollbackToDropsAddedColumn`, `TestDDLSavepoint_RollbackToDropsAddedTable`. Estimate: ~150 lines, 2 files, 1 day. | high | medium | REQ001319 | SQB/WT/alter_table.go, ENG/CT/catalog.go |
| REQ001326 | SQB/EX | **CREATE TEMP/TEMPORARY TABLE.** Currently parser rejects per `ddl_test.go:426-429`. Temp tables are connection-scoped, dropped automatically at session end. **Fix:** (1) In WT DDL, accept `TEMP/TEMPORARY` flag; store in a separate `tempCatalog` map (session-scoped, not persisted to disk). (2) TableHandle resolution checks both main catalog and tempCatalog. (3) `sqlite_temp_master` exposes the temp tables (see REQ001328). (4) On session close, drop temp tables. (5) Add `TestTempTable_CreatedInSession`, `TestTempTable_DroppedOnSessionEnd`, `TestTempTable_SameNameAsMain_Shadows`. Estimate: ~150 lines, 3 files, 0.75 day. | high | medium | none | SQB/EX/ex.go, SQB/WT/writers_ddl.go, SQB/DT/schema.go |
| REQ001327 | SQB/EX | **PRAGMA temp_store (DEFAULT/FILE/MEMORY).** Controls where temp tables, temp B-trees, and intermediate sort/aggregate spills are stored. **Fix:** (1) Add `temp_store` PRAGMA accepting `DEFAULT | FILE | MEMORY`. (2) MEMORY = keep all temp data in DRAM (no spill to disk). (3) FILE = spill to `<db>.razor-temp/` directory. (4) DEFAULT = MEMORY up to a threshold, FILE beyond. (5) Add `TestTempStore_Memory_NoTempDir`, `TestTempStore_File_CreatesTempDir`. Estimate: ~100 lines, 2 files, 0.5 day. | high | small | REQ001326 | SQB/EX/planner.go, FIL/FS/fs.go |
| REQ001328 | SQB/OP | **Temp namespace: sqlite_temp_master introspection.** Expose the temp catalog (REQ001326) as a queryable pseudo-table. **Fix:** (1) Add `sqlite_temp_master` to the SqliteMaster operator (SQB/OP/operators.go area). (2) Columns: type, name, tbl_name, rootpage, sql. (3) SELECT against sqlite_temp_master returns temp tables/views/triggers. (4) Add `TestTempMaster_QueryAfterTempCreate`. Estimate: ~60 lines, 1 file, 0.25 day. | high | small | REQ001326 | SQB/OP/operators.go |
| REQ001329 | SYS/DS + FIL/IO | **Multi-process: shared_cache mode.** SQLite's `SQLITE_OPEN_SHAREDCACHE` lets multiple connections in the same process share a single in-memory page cache. Across processes, equivalent to opening the same db file. **Fix:** (1) Add `OpenOptions.SharedCache bool`. (2) When true, acquire a per-DSN RWMutex in SYS/DS; share the engine. (3) Multiple readers, single writer at the engine level. (4) `Engine.Close()` is a no-op for shared instances (last-closer cleans up). (5) Add `TestSharedCache_MultipleSessionsSameEngine`, `TestSharedCache_ConcurrentReaders`. Estimate: ~150 lines, 2 files, 0.75 day. | high | medium | none | SYS/DS/ds.go, SYS/AP/ap.go |
| REQ001330 | SYS/DS + FIL/IO | **Multi-process: POSIX file-locking protocol.** SQLite uses fcntl(F_SETLK) byte-range locks on the db file. Razordata currently has no inter-process locking — only one process can hold the db. **Fix:** (1) At Engine.Open, take a fcntl write lock (or read lock for read-only) on the db file. (2) If lock fails, return ErrLocked. (3) `PRAGMA lock_proxy_file` not needed. (4) `POSIX_FADV_RANDOM` on the fd. (5) Add `TestMultiProcess_SecondOpenErrors`, `TestMultiProcess_FirstCloses_SecondSucceeds`. Estimate: ~150 lines, 2 files, 0.75 day. | high | medium | none | FIL/IO/lock_linux.go (new), SYS/DS/ds.go |
| REQ001331 | SYS/DS + FIL/IO | **Multi-process: SHM mapping for change_counter.** SQLite uses an `*-shm` file for cross-process cache invalidation. A 4-byte change counter at offset 0 of -shm is bumped on every transaction commit; readers reload cache if their snapshot is stale. **Fix:** (1) Open `<db>.razor-shm` with `MAP_SHARED`. (2) On commit, atomic.AddUint32(counter, 1). (3) On read snapshot, check counter; if changed, invalidate caches. (4) Initial size: 32KB reserved (extensible). (5) Add `TestMultiProcess_ChangeCounter_BumpsOnCommit`, `TestMultiProcess_ReaderDetectsStaleCounter`. Estimate: ~200 lines, 2 files, 1 day. | high | medium | REQ001330 | FIL/IO/shm_linux.go (new), TXN/VL/protocol.go |
| REQ001332 | SYS/AP | **Public RegisterCollation API in SYS/AP.** SQLite exposes `sqlite3_create_collation_v2()`. Razordata's parser recognizes COLLATE (SQF/PS/select.go:88) but the engine has no public hook. **Fix:** (1) Add `Engine.RegisterCollation(name string, fn func(a, b []byte) int) error`. (2) Store in a per-engine registry. (3) In ORDER BY/COMPARE evaluation, look up collation by name. (4) On close, clear the registry. (5) Add `TestRegisterCollation_CustomOrder`, `TestRegisterCollation_CollateIndexOrderBy`. Estimate: ~80 lines, 2 files, 0.4 day. | high | small | none | SYS/AP/ap.go, SQB/EV/function_registry.go |
| REQ001333 | SQB/EV | **ICU/locale collation support.** Beyond the built-in `binary`/`nocase`/`rtrim`, support ICU locale-based collations (`en_US`, `de_DE`, `zh_CN`). **Fix:** (1) Add `golang.org/x/text/collate` dependency. (2) Register built-in collations `unicode_en_US`, `unicode_de_DE`, etc. (3) In compareValue, if collation is "unicode_*", use the collate package's StringComparer.Compare. (4) Test: `SELECT 'ä' COLLATE unicode_de_DE` returns different order than binary. (5) Add `TestCollation_ICU_AccentedChars`. Estimate: ~100 lines, 1 file, 0.5 day. | high | small | REQ001332 | SQB/EV/scalar_funcs.go, go.mod |
| REQ001338 | SQB/DT + SQB/WT | **VIRTUAL generated column: on-read evaluation.** Parser accepts VIRTUAL (SQF/PS/ddl.go:100-122), schema stores as nil (SQB/DT/schema.go:39-41), but no read-time evaluation exists. **Fix:** (1) In SQB/DT, mark VIRTUAL columns and store the AST expr. (2) In read path (SQB/OP/operators.go Project/Filter/Scan), before materializing the row, replace VIRTUAL col slots by evaluating the stored expr against the row's other columns. (3) Result is identical to materializing at write; no on-disk cost. (4) Add `TestGenerated_Virtual_SelectMaterializes`, `TestGenerated_Virtual_NotStoredOnDisk`. Estimate: ~150 lines, 3 files, 0.75 day. | high | medium | none | SQB/DT/schema.go, SQB/OP/operators.go, SQB/EV/eval.go |
| REQ001339 | SQB/DT + SQB/EX | **VIRTUAL generated column: usable in indexes.** A VIRTUAL generated column can be referenced in CREATE INDEX and used in WHERE clauses. **Fix:** (1) Allow `CREATE INDEX i ON t (virtual_col)`; persist the index on the VIRTUAL column. (2) On index lookup, recompute the VIRTUAL col from the row, then compare to seek key. (3) Add `TestGenerated_Virtual_IndexLookup`, `TestGenerated_Virtual_IndexUsedInWhere`. Estimate: ~100 lines, 2 files, 0.5 day. | high | small | REQ001338 | SQB/DT/schema.go, ENG/ID/id.go |
| REQ001340 | SQB/DT + SQB/WT | **VIRTUAL generated column: usable in FK references.** A VIRTUAL generated col can be the target of a FK from another table. **Fix:** (1) Allow `FOREIGN KEY (other.x) REFERENCES t(virtual_col)`. (2) On FK enforcement, compute the virtual col on-the-fly. (3) Add `TestGenerated_Virtual_FKTarget`. Estimate: ~60 lines, 1 file, 0.3 day. | high | small | REQ001338, REQ001308 | SQB/WT/fk.go |
| REQ001355 | SQB/OP | **RIGHT [OUTER] JOIN (NLJ).** Return all right rows; left unmatched cols get NULL. **Fix:** (1) Extend NestedLoopJoin to support `rightOuter bool`. (2) Build right-row index, iterate right rows, probe left. (3) Add `TestRightJoin_NLJ_Basic`, `TestRightJoin_NLJ_LeftEmpty`. Estimate: ~80 lines, 1 file, 0.4 day. | high | small | none | SQB/OP/join.go |
| REQ001356 | SQB/OP | **RIGHT [OUTER] JOIN (Hash).** Hash build on right side, probe left, emit all right rows. **Fix:** (1) Extend HashJoin to support right-outer. (2) Build hash on right rows; probe left; for unmatched right rows, emit with left NULLs. (3) Add `TestRightJoin_Hash_Basic`, `TestRightJoin_Hash_LeftEmpty`. Estimate: ~80 lines, 1 file, 0.4 day. | high | small | REQ001355 | SQB/OP/hashjoin.go |
| REQ001357 | SQB/OP | **FULL [OUTER] JOIN (NLJ).** Both sides' unmatched rows emitted. **Fix:** (1) Extend NestedLoopJoin to support `fullOuter bool`. (2) Track matched rows on both sides; emit unmatched at end. (3) Add `TestFullJoin_NLJ_Basic`, `TestFullJoin_NLJ_BothSidesUnmatched`. Estimate: ~100 lines, 1 file, 0.5 day. | high | small | REQ001355 | SQB/OP/join.go |
| REQ001358 | SQB/OP | **FULL [OUTER] JOIN (Hash).** Hash build + probe + unmatched emit both sides. **Fix:** (1) Extend HashJoin to support full-outer. (2) Two-pass: hash-build on right, probe left; then emit unmatched right. (3) Add `TestFullJoin_Hash_Basic`. Estimate: ~100 lines, 1 file, 0.5 day. | high | small | REQ001356, REQ001357 | SQB/OP/hashjoin.go |
| REQ001359 | SQF/PS + SQB/OP | **NATURAL [INNER] JOIN.** Auto-equality on common column names. **Fix:** (1) In SQF/PS, parse NATURAL; collect common column names from both tables; synthesize ON clause. (2) Lower to INNER JOIN with synthesized ON. (3) Add `TestNaturalJoin_Inner_Basic`, `TestNaturalJoin_NoCommonCols_Cross`. Estimate: ~80 lines, 2 files, 0.4 day. | high | small | none | SQF/PS/select.go, SQB/EX/planner.go |
| REQ001360 | SQF/PS + SQB/OP | **NATURAL LEFT/RIGHT/FULL JOIN.** Same as REQ001359 but with outer semantics. **Fix:** (1) Extend parse to accept NATURAL + outer modifier. (2) Lower to the appropriate outer join (REQs 355–358). (3) Add `TestNaturalJoin_Left`, `TestNaturalJoin_Right`, `TestNaturalJoin_Full`. Estimate: ~60 lines, 1 file, 0.3 day. | high | small | REQ001359, REQ001355, REQ001357 | SQF/PS/select.go |
| REQ001366 | SQB/WT | **INSTEAD OF INSERT trigger on view.** Allows updatable views via trigger logic. **Fix:** (1) In trigger dispatch, accept `INSTEAD OF INSERT ON view_name`. (2) When INSERTing into a view, run the INSTEAD OF trigger body instead of the default error. (3) Add `TestInsteadOf_Insert_Basic`, `TestInsteadOf_Insert_MultipleRows`. Estimate: ~150 lines, 2 files, 0.75 day. | high | medium | none | SQB/WT/writers_dml.go, SQB/WT/trigger.go (new) |
| REQ001367 | SQB/WT | **INSTEAD OF UPDATE trigger on view.** Mirror of REQ001366 for UPDATE. **Fix:** (1) Accept `INSTEAD OF UPDATE ON view`. (2) Run trigger body instead of default error. (3) Add `TestInsteadOf_Update_Basic`, `TestInsteadOf_Update_OldNewRefs`. Estimate: ~120 lines, 1 file, 0.6 day. | high | small | REQ001366 | SQB/WT/trigger.go |
| REQ001368 | SQB/WT | **INSTEAD OF DELETE trigger on view.** Mirror of REQ001366 for DELETE. **Fix:** (1) Accept `INSTEAD OF DELETE ON view`. (2) Run trigger body. (3) Add `TestInsteadOf_Delete_Basic`. Estimate: ~100 lines, 1 file, 0.5 day. | high | small | REQ001366 | SQB/WT/trigger.go |
| REQ001369 | SQB/WT | **STRICT table-type mode enforcement.** Parses (ps_test.go:896), runtime ignores. **Fix:** (1) In writers_ddl.go, accept STRICT; store in catalog. (2) In row encoding, use SQLite's 1-byte type tag per column (NULL=0, INT=1, REAL=2, TEXT=3, BLOB=4, ANY=5). (3) On INSERT, validate each value's type matches the declared affinity; error if mismatched. (4) Add `TestStrict_TypeEnforced_InsertMismatch_Error`, `TestStrict_TypeEnforced_NullAllowed`. Estimate: ~150 lines, 2 files, 0.75 day. | high | small | none | SQB/WT/writers_ddl.go, SQB/DT/encode.go |
| REQ001370 | SQB/WT | **TEMP trigger support.** Triggers in the temp namespace. **Fix:** (1) Accept `CREATE TEMP TRIGGER`; store in temp catalog (REQ001326). (2) Trigger firing scans both main and temp catalogs. (3) On session end, temp triggers are dropped. (4) Add `TestTempTrigger_CreatedInSession`, `TestTempTrigger_DroppedOnSessionEnd`. Estimate: ~80 lines, 1 file, 0.4 day. | high | small | REQ001326 | SQB/WT/trigger.go |
| REQ001371 | SQB/EX | **Planner: honor INDEXED BY hint.** `FROM t INDEXED BY idx_name` forces the planner to use the named index. **Fix:** (1) In planner, after parsing the hint, store on the From node. (2) When planning, if hint is set, only consider the named index; error if hint name is invalid. (3) Add `TestIndexHint_ForcesIndex`, `TestIndexHint_InvalidName_Error`. Estimate: ~80 lines, 1 file, 0.4 day. | high | small | none | SQB/EX/planner.go, SQF/PS/select.go |
| REQ001372 | SQB/EX | **Planner: honor NOT INDEXED hint (force SeqScan).** `FROM t NOT INDEXED` forces a sequential scan, ignoring all indexes. **Fix:** (1) Parse NOT INDEXED (SQF/PS/ddl.go:659). (2) In planner, if hint is set, skip index selection entirely; emit SeqScan. (3) Add `TestIndexHint_NotIndexed_ForcesSeqScan`. Estimate: ~40 lines, 1 file, 0.2 day. | high | small | REQ001371 | SQB/EX/planner.go |
| REQ001373 | SQB/WT | **Trigger: RAISE(IGNORE) — swallow row, continue.** RAISE(IGNORE) causes the offending DML to skip the row without error. **Fix:** (1) In evalRaise, accept IGNORE; return a sentinel `ErrRaiseIgnore` that the trigger dispatcher catches and treats as "row skipped." (2) In writers_dml.go, when the sentinel is returned, advance to the next row (do not abort). (3) Add `TestRaiseIgnore_SkipsRow`, `TestRaiseIgnore_DoesNotIncrementRowsAffected`. Estimate: ~80 lines, 2 files, 0.4 day. | high | small | none | SQB/WT/trigger.go, SQB/EV/eval.go |
| REQ001374 | SQB/WT | **Trigger: RAISE(ROLLBACK) — abort transaction.** RAISE(ROLLBACK) aborts the whole transaction. **Fix:** (1) In evalRaise, accept ROLLBACK; return sentinel `ErrRaiseRollback`. (2) In writers_dml.go, propagate the sentinel; the txn dispatcher aborts and rolls back. (3) Add `TestRaiseRollback_AbortsTxn`, `TestRaiseRollback_UndoesPriorWrites`. Estimate: ~60 lines, 2 files, 0.3 day. | high | small | REQ001373 | SQB/WT/trigger.go, SQB/EV/eval.go |
| REQ001375 | SQB/WT | **Trigger: RAISE(FAIL) — abort statement, keep txn.** RAISE(FAIL) aborts the current statement but the transaction continues. **Fix:** (1) In evalRaise, accept FAIL; return sentinel `ErrRaiseFail`. (2) In writers_dml.go, propagate the sentinel; the dispatcher aborts the current statement but does NOT roll back. (3) Add `TestRaiseFail_AbortsStmt`, `TestRaiseFail_KeepsTxn`. Estimate: ~60 lines, 2 files, 0.3 day. | high | small | REQ001373 | SQB/WT/trigger.go, SQB/EV/eval.go |
| REQ001379 | SQB/UT | **PRAGMA quick_check — row count consistency only.** Cheap pre-check before integrity_check. **Fix:** (1) Add `quick_check` PRAGMA. (2) For each user table, compare count(*) to razor_stat1.rowcount. (3) Report "ok" or list discrepancies. (4) Add `TestQuickCheck_Ok`, `TestQuickCheck_RowCountMismatch`. Estimate: ~80 lines, 1 file, 0.4 day. | high | small | REQ001317 | SQB/UT/integrity.go |
| REQ001380 | SQB/UT | **PRAGMA integrity_check — SST checksum walk.** Full integrity check walks every SST block, verifying the per-block CRC32. **Fix:** (1) Iterate all SST files in ENG/LS. (2) For each block, recompute CRC32; compare to stored. (3) Report first 10 failing blocks. (4) Add `TestIntegrityCheck_SSTChecksum`. Estimate: ~150 lines, 2 files, 0.75 day. | high | medium | none | SQB/UT/integrity.go, ENG/LS/sst_reader.go |
| REQ001381 | SQB/UT | **PRAGMA integrity_check — FK consistency.** For each FK, verify all child rows reference a valid parent. **Fix:** (1) Iterate FK constraints. (2) For each, build a set of parent PKs. (3) Verify every child FK col matches an existing parent PK. (4) Add `TestIntegrityCheck_FK_DanglingChild`. Estimate: ~100 lines, 1 file, 0.5 day. | high | small | REQ001308 | SQB/UT/integrity.go |
| REQ001382 | SQB/UT | **PRAGMA integrity_check — index reference.** For each secondary index, verify all entries point to a valid row. **Fix:** (1) Iterate indexes in ENG/ID. (2) For each entry, verify the rowKey resolves to a real row. (3) Add `TestIntegrityCheck_Index_DanglingPointer`. Estimate: ~100 lines, 1 file, 0.5 day. | high | small | none | SQB/UT/integrity.go |
| REQ001383 | SQB/EX + SQB/WT | **RETURNING clause on UPSERT.** `INSERT … ON CONFLICT … DO UPDATE … RETURNING *` returns the post-update row. **Fix:** (1) Extend writers_dml.go UPSERT path to capture returning rows. (2) For DO UPDATE, return the post-update row. (3) For DO NOTHING, return the pre-existing row. (4) Add `TestUpsert_Returning_DoUpdate`, `TestUpsert_Returning_DoNothing`. Estimate: ~80 lines, 1 file, 0.4 day. | high | small | REQ001363 | SQB/WT/writers_dml.go |
| REQ001384 | SQB/WT | **DROP COLUMN: cascade rules doc + tests.** Centralize the cascade rules for DROP COLUMN. **Fix:** (1) Document rules: FK → drop FK; generated col → drop generated col; indexed col → drop index; CHECK → check for reference. (2) Add test matrix `TestDropColumn_CascadeMatrix` covering all 4 cases. (3) Add `TestDropColumn_MultipleCascades`. Estimate: ~120 lines, 2 files, 0.5 day. | high | small | REQ001324, REQ001325 | SQB/WT/alter_table.go |
| REQ001386 | SQB/EX | **Partial index: planner use with multi-clause WHERE.** Use a partial index when the query's WHERE implies the partial predicate. **Fix:** (1) In planner index selection, check if query's WHERE conjuncts imply the partial index's predicate. (2) Use the index if so. (3) Add `TestPartialIndex_PlannerUses_MultiPredicate`, `TestPartialIndex_PlannerSkips_NotImplied`. Estimate: ~100 lines, 1 file, 0.5 day. | high | small | REQ001385 | SQB/EX/planner.go |
| REQ001387 | SQB/UT | **PRAGMA cell_size_check — verify BLOB/text fits in cell.** SQLite 3.46+ checks that no cell overflows the page. Razordata's analog: SST block size check. **Fix:** (1) Add `cell_size_check` PRAGMA. (2) Walk all SST blocks; verify block_size matches declared. (3) Report over/under-sized blocks. (4) Add `TestCellSizeCheck_Ok`, `TestCellSizeCheck_CorruptBlock`. Estimate: ~80 lines, 1 file, 0.4 day. | high | small | none | SQB/UT/integrity.go, ENG/LS/sst_reader.go |
| REQ001390 | SQB/OP | **sqlite_sequence — autoincrement tracking.** Tracks the last used ROWID for AUTOINCREMENT tables. **Fix:** (1) Add sqlite_sequence pseudo-table: `(name TEXT, seq INTEGER)`. (2) On INSERT to an AUTOINCREMENT table, update seq to max(seq, new_rowid). (3) On INSERT when no rowid available, allocate seq+1. (4) Add `TestSqliteSequence_AutoIncrement`. Estimate: ~150 lines, 2 files, 0.75 day. | high | medium | none | SQB/OP/operators.go, SQB/WT/writers_dml.go |
| REQ001412 | SQB/OP | **SeqScan column pruning (REQ001080) — verify effectiveness for select1 query patterns.** REQ001080 implements column-level pruning in SeqScan (only read projected columns from store). Needs verification that pruning actually reaches the storage layer for select1's query patterns (simple SELECT, expression-heavy, subquery). If effective, I/O savings are ~80% for single-column queries vs reading all 5 columns. **Fix:** (1) Add `TestSeqScan_ColumnPruning_Select1Patterns` verifying `SeqScan.Columns()` propagates to storage read path. (2) Add `BenchmarkSeqScan_ColumnPruning_vs_FullScan` measuring I/O reduction. (3) If pruning is not reaching storage, fix the propagation chain. Estimate: ~60 lines, 1 file, 0.25 day. | low | small | REQ001080 | SQB/OP/operators.go |
| REQ001418 | ENG/LS | **`TestMergeIterator_NoHeapBoxing` flakily fails: `mergeIterator allocs/op = 7, want <= 6` (allocation regression).** `internal/ENG/LS/merge_pool_test.go:595` asserts the merge iterator pool path allocates at most 6 bytes per op under `testing.AllocsPerRun`. The test intermittently reports 7 allocs/op. When run alone (`go test -run TestMergeIterator_NoHeapBoxing`) it passes; under `-count=5` it fails 3/5 times. The flake indicates a code path escapes to heap on certain GC pressure / scheduler timing — likely an interface upcast or a deferred closure captures a stack-escaped pointer. **Fix:** (1) Profile with `-benchmem` to find which allocation escapes (heaptrack or `go test -gcflags=-m` for escape analysis). (2) Common culprits: `sync.Pool.Put` taking an interface (causes 1 alloc when value is non-pointer), closure over loop variable, `any` boxing of struct values. (3) Pin the alloc count by eliminating the escape; or relax the assertion to `<= 7` with a comment justifying the relaxed bound. (4) Verify determinism with `go test -race -count=20`. Estimate: ~30 lines (investigation + fix), 1 file, 0.25 day. **Source:** pre-existing failure observed during `go test ./internal/...` audit while implementing REQ001300-1302; verified via `git stash` baseline. | medium | small | none | ENG/LS/merge_pool_test.go, ENG/LS/iterator.go |
| REQ001421 | WAL/WR + SQB/WT | **Write batch buffer for autocommit INSERT/UPDATE.** INSERT is 4.5x, UPDATE is 8.6x slower than modernc.org/sqlite at 100 rows. Current path: each INSERT/UPDATE triggers an independent WAL write with no batch merging. SQLite coalesces autocommit INSERTs into a single WAL frame. **Fix:** (1) Add a per-session `WriteBatch` buffer (in-memory WAL frame accumulator) in WAL/WR. (2) `WAL.Writer` defers fsync until commit or buffer full (e.g. 16 KB). (3) INSERT/UPDATE writers (SQB/WT) append to the buffer instead of flushing per-row. (4) On autocommit, flush once with a single WAL frame. (5) On `BEGIN`, switch to per-row flush (transaction semantics). (6) Verify: 100-row INSERT drops from 4203 µs/op to ≤ 2000 µs/op. Estimate: ~250 lines, 4 files, 1.5 days. | high | medium | REQ001419 | WAL/WR/writer.go (new batch struct), WAL/WR/frame.go, SQB/WT/writers_dml.go |
| REQ001422 | SYS/SE + SQB/EX | **Prepared statement persistence — compile-once, execute-many.** Every query rebuilds the operator tree from scratch. Plan cache exists but does not prevent operator tree construction. **Fix:** (1) Add `Session.Prepare(sql)` returning a reusable `PreparedPlan` that holds the compiled operator tree (closed and re-openable). (2) `PreparedPlan.Exec(args)` and `PreparedPlan.Query(args)` inject parameters and reopen the operator tree without re-planning or re-building. (3) `PreparedPlan.Close()` cleans up the operator tree. (4) Wire `database/sql.Stmt` to cache PreparedPlan across calls. (5) Verify: repeated `SELECT * FROM t` via prepared stmt is ≤ 1.5x the per-query `QueryRow` cost (eliminating plan + tree build). Estimate: ~180 lines, 3 files, 1.5 days. | high | medium | REQ001419 | SYS/SE/stmt.go (new PreparedPlan), SQB/EX/ex.go, driver/stmt.go |
| REQ001423 | ENG/LS + SQB/EX | **B-tree / LSM block-level stats: min, max, NDV for range query planning.** Range scans (e.g. `WHERE age > 30 AND age < 50`) currently traverse the full SeqScan. LSM blocks have per-column min/max that can be used for early block pruning. **Fix:** (1) In ENG/LS SST block stats, expose per-column `min`/`max` during block construction. (2) In SeqScan, when a range predicate is present, skip blocks where the predicate's range is disjoint from the block's column min/max. (3) Expose via `TableStats` column-level `min`/`max` in catalog. (4) Add `TestSeqScan_BlockPruning_RangePredicate` verifying skipped blocks. Estimate: ~120 lines, 2 files, 0.75 day. | high | small | REQ001420 | ENG/LS/sst_block.go, SQB/OP/operators.go |
| REQ001424 | SQB/WT + SQB/DT | **Pre-size UPDATE/DELETE `DT.RowArena` to avoid per-statement slab grow.** `BenchmarkRazordata_Update` shows `(*RowArena).grow` allocates ~626 MB per benchmark run (~26% of total). UPDATE/DELETE writers use the per-statement `ExecContext.RowArena` but never call `Init(estimatedRows, colsPerRow)`. Each statement grows the arena geometrically from 64 KB and discards it on `Reset()` — six grow cycles per statement for a 50-row update. **Fix:** (1) In `WT.(*Update).Next` / `(*Delete).Next`, before the row scan, call `ec.RowArena.(*DT.RowArena).Init(estimatedRows, len(schema.Cols))`. Estimation = `Min(rowCount, filterSelectivity * rowCount)` from stats, fallback 256 rows. (2) For Update with a primary-key WHERE, init with 1 row. (3) Add `TestRowArena_UpdatePresize_EliminatesGrows` verifying slab grew ≤ 1 time over 100 UPDATE iterations. (4) Verify `BenchmarkRazordata_Update` allocations drop ≤ 50%. Estimate: ~40 lines, 2 files, 0.25 day. **Source:** surfacing from REQ001421 perf profile (post single-node NUMA fix + drop driver Begin/Rollback); both NUMA and Begin fixes landed as `85fce6d` + `2a6a171`. | high | small | none | SQB/WT/writers_dml.go, SQB/DT/arena.go |
| REQ001425 | SQB/EX + SYS/SE + driver | **Cache parsed SQL → operator tree mapping for repeated INSERT/UPDATE/DELETE patterns.** `BenchmarkRazordata_Update` profile shows `NewInsert`/`NewUpdate`/`NewDelete` + `OP.NewFilter` + `OP.NewSeqScan` allocate ~0.5 GB across the bench (operator tree construction per exec call). Repeated identical SQL strings (typical OLTP workloads, driver prepared-stmt pattern) re-walk the planner tree for each `Exec`. The parsed-stmt cache (REQ001223) caches the AST but not the operator tree. **Fix:** (1) Add a `sessionExecPlanCache` keyed by `sqlString` (after parameter rebinding) on Session/Executor. (2) On cache hit, validate the cached operator is closed (or reusable); if so, return a reset copy. (3) On cache miss, plan + execute; on success with no active transaction AND no parameters, store the operator tree in the cache. (4) Skip caching when SQL has volatile constructs (`PRAGMA`, `RETURNING`, transactions, EXPLAIN). (5) Add `TestExecPlanCache_RepeatedInsert_ReusesOperator`, `TestExecPlanCache_DriverBench_VerifyHitRate`. Estimate: ~150 lines, 3 files, 1 day. **Source:** surfacing from REQ001421 perf profile. | high | medium | REQ001422 | SQB/EX/ex.go, SYS/SE/se.go, driver/stmt.go |
| REQ001426 | SQB/WT + SQB/DT | **Eliminate per-row `BuildInsertRow` slice copies for bulk INSERT.** `BenchmarkRazordata_Insert` profile after REQ001424+425 lands will show `WT.BuildInsertRow` (0.22 GB / 8%) and `DT.ValueFromAnySlice` (0.24 GB / 9%) as top allocators. BuildInsertRow allocates a new `DT.Row` per row + a `[]Value` per row + an `[]any` boxed-Value conversion per row. **Fix:** (1) Add `WT.NewInsert.InsertRowsBatch(rows []PS.ExprList) (DT.Row, error)` that builds all rows into pre-allocated slices; reuse the `ExecContext.RowArena` for `[]Value` storage (already implemented per REQ001221 but not used in the auto-values path). (2) Add `WT.ValidateRowBatch(rows []DT.Row, schema *StoreSchema) error` that batches Default-fill + Check + Unique-check without per-row interface conversion. (3) The INSERT loop then dispatches the batched path when `len(rows) > 1`. (4) Add `TestInsertBatch_ArenaReuse_NoPerRowAlloc`. Estimate: ~120 lines, 2 files, 0.75 day. **Source:** surfacing from REQ001421 perf profile. | high | medium | REQ001424 | SQB/WT/writers_dml.go, SQB/DT/arena.go |
| REQ001427 | driver | **Route driver-prepared `database/sql.Stmt` through the new `execPlanCache` (REQ001425) — eliminate per-call `session.Exec` lock acquisition.** Per call, `Conn.ExecContext` → `session.Exec(ctx, …)` acquires and releases the session lock even when no transaction is active. Lock cost dominates repeated small INSERTs. With `Stmt.Exec` (driver prepared-stmt pattern), the same SQL runs N times in a row. A per-`Stmt` cached operator tree + parameter binding would skip both parsing and lock acquisition on every call after the first. **Fix:** (1) In `driver/stmt.go` (or equivalent), on first `Stmt.Exec(args)`, lookup the cached prepared operator via REQ001425. (2) On subsequent calls, rebind params and run the cached tree without going through `session.Exec`. (3) Honour per-stmt connection pin. (4) Verify with `BenchmarkRazordata_Insert` running through `db.Prepare("INSERT …").Exec(…)`. (5) Add `TestStmt_Prepared_ReusesOperator`. Estimate: ~80 lines, 2 files, 0.5 day. **Source:** follow-on from REQ001425 + REQ001422 driver-side wiring. | medium | medium | REQ001425, REQ001422 | driver/stmt.go, SYS/SE/se.go |
| REQ001428 | SQB/OP | **MicroOp: simple SELECT bypass operator tree — inline scan+filter+project.** SLT select1 analysis shows ~40% of query time is spent in interface dispatch across the operator tree (SeqScan → FilterProject → AdaptiveOp). For simple single-table SELECT with no JOIN/subquery/GROUP BY/SORT, the full operator tree is over-engineered — a single tight loop reading from source, filtering, and projecting is sufficient. **Fix:** (1) In planner, detect simple SELECT (single FROM, no JOIN/subquery/GROUP BY/ORDER BY/window, no aggregate functions). (2) Emit `MicroOp` instead of SeqScan+FilterProject chain. (3) MicroOp reads from the source (in-memory slice or LSM iterator) in one loop: apply predicate via pre-compiled filter function, project columns, append to output. (4) Result is returned directly without syncStreamPath buffering. (5) Add `BenchmarkSelect1_MicroOp` comparing MicroOp vs standard path. Estimate: ~200 lines, 2 files, 1 day. | high | medium | none | SQB/OP/micro_op.go (new), SQB/EX/planner_select.go |
| REQ001429 | SQB/EV | **Compile WHERE/CASE expressions to flat instruction array.** select1's CASE expression (`CASE WHEN a<b-3 THEN 111...`) is evaluated via AST walk for each of 20 rows. Compiling to a flat opcode array (`PUSH_COL a, PUSH_IMM 3, SUB, PUSH_COL b, CMP_LT, COND_JMP label, ...`) evaluated in a tight `for { switch opcode[i] { ... } }` loop eliminates AST dispatch. **Fix:** (1) Add expression compiler in EV package. (2) Compile returns `[]Opcode` + literal pool. (3) Eval becomes one loop over opcodes. (4) Cache compiled expressions by AST identity (sync.Map keyed by `*PS.BinaryExpr`/`*PS.CaseExpr` pointer). (5) Add `BenchmarkSelect1_CompiledCASE` vs interpreted CASE. Estimate: ~250 lines, 2 files, 1 day. | high | medium | none | SQB/EV/expr_compile.go (new), SQB/OP/intermediate_basic.go |
| REQ001430 | SQB/EX | **Lightweight Query API: bypass database/sql for in-process queries.** SLT select1 bench goes through `database/sql.DB.QueryContext` → `sql.Conn.queryDC` → `rowsFromStatement` → `Stmt.Query` → `ST.Stmt.Query` → `Executor.QueryStream` — 6 abstraction layers for a simple `SELECT * FROM t1`. **Fix:** (1) Add `executor.QueryRaw(sql, args) ([]Row, error)` that skips the entire database/sql driver layer. (2) The SLT runner and benchmarks can call QueryRaw directly when they already hold an Executor reference. (3) Add `BenchmarkSelect1_QueryRaw` showing bypass improvement. Estimate: ~30 lines, 1 file, 0.2 day. | medium | small | none | SQB/EX/ex.go |
| REQ001431 | SQB/DT + SQB/OP | **Columnar small-table layout for select1 workloads.** select1 table has 5 int columns. `SELECT a FROM t1` decodes all 5 columns per row and prunes back to 1. Store columns as contiguous int64 arrays for tables < 1000 rows, enabling zero-decay projection. **Fix:** (1) In DT, when a table's estimated size is < 1000 rows, store data column-major: `cols["a"] = []int64{...}` alongside row-major encoding. (2) SeqScan, when RequestedCols is set, reads from columnar storage directly. (3) INSERT appends to both formats. (4) Add `BenchmarkSelect1_ColumnarProjection`. Estimate: ~300 lines, 3 files, 2 days. | medium | large | REQ001428 | SQB/DT/schema.go, SQB/OP/operators.go, SQB/WT/writers_dml.go |
| REQ001432 | SQO/OC | **SQO/OC skeleton: empty Optimizer establishes the import path before any code moves.** SQB/EX.planner imports SQO/OC and instantiates an `Optimizer` whose `Optimize(plan, ctx)` returns the plan unchanged. This single-step establishes the SQF → SQO → SQB dependency order without touching behavior. Required before any optimization logic can move — without this, SQB/EX cannot import SQO without a circular dep. **Fix:** (1) Create `internal/SQO/OC/optimizer.go` defining `Plan`, `Context`, `Optimizer` types. (2) Implement `(*Optimizer).Optimize(plan, ctx) (*Plan, error)` as a passthrough. (3) Define `CatalogReader` and `StatsReader` interfaces in `internal/SQO/OC/catalog.go` (no implementations yet). (4) In `SQB/EX/planner.go`, create an `*oc.Optimizer` field on `Planner` and store it (do not invoke yet). (5) Add `TestSQO_EmptyOptimizer_PassThrough`. Gate: `grep -r '"SQB/"' SQO/` returns 0; `go build ./... && go test ./...` pass. **Spec:** `docs/compose/specs/sqo-subsystem/design.md` §3 (package layout); `docs/development/iterations/iter-SQO-REQ1432-1456.md` Step 0. Estimate: ~100 lines, 4 files, 0.5 day. | high | small | none | SQO/OC/optimizer.go (new), SQO/OC/catalog.go (new), SQB/EX/planner.go |
| REQ001433 | SQF/PS | **ExprVisitor interface + `Accept()` methods on every Expr node — enables AST traversal without coupling to concrete types.** `SQF/PS/ast.go` currently uses a hand-written switch in `SQB/EX/walkExpr` to visit each Expr subtype. Adding new node types forces changes in every visitor. The ExprVisitor pattern (one method per Expr type) localizes dispatch. **Fix:** (1) Define `ExprVisitor` interface in `internal/SQF/PS/visitor.go` with one method per Expr node type (~20 methods: VisitBinaryExpr, VisitColumnRef, VisitLiteralInt, …). (2) Add `Accept(v ExprVisitor) error` method to every concrete Expr type in `ast.go` (BinaryExpr, UnaryExpr, ColumnRef, Literal*, FuncCall, CaseExpr, SubqueryExpr, InExpr, BetweenExpr, …). Each Accept calls the corresponding Visit method and recurses into children. (3) Add `TestExprVisitor_CoversAllNodeTypes` that creates a counting visitor and verifies every node type dispatches correctly. No callers yet — purely additive. **Spec:** `docs/compose/specs/sqo-subsystem/design.md` §4.1 (interface design); `iter-SQO-REQ1432-1456.md` Step 1.1. Estimate: ~150 lines, 2 files, 0.5 day. | high | small | none | SQF/PS/visitor.go (new), SQF/PS/ast.go |
| REQ001434 | SQF/PL | **OperatorFactory + optional operator interfaces (Parent, ColPrunable, PredicateCarrier, RelationSource, ColumnSchema, IndexInfo).** SQO must construct and manipulate operators via interfaces, never via concrete types from `SQB/OP`. OperatorFactory gives SQO a way to build operator trees without importing concrete constructors; optional interfaces let passes probe for capabilities (e.g., "does this Scan support column pruning?"). **Fix:** (1) Define `OperatorFactory` interface in `internal/SQF/PL/factory.go` with one method per operator type (~15 methods: NewSeqScan, NewIndexScan, NewFilter, NewProject, NewHashJoin, NewNestedLoopJoin, NewAggregate, NewHashAggregate, NewSort, NewLimit, NewDistinct, NewSetOp, NewValues, NewFilterProject, NewAdaptiveOp). All methods return `Operator`, never concrete types. (2) Define `Parent` (Children()/SetChild()), `ColPrunable` (UsedCols/SetUsedCols/RequestedCols/SetRequestedCols), `PredicateCarrier` (Predicate()/SetPredicate()), `RelationSource` (Table()/Schema()), `ColumnSchema` (Columns()/ColumnIndex()), `IndexInfo` (IndexName/IndexColumns/IndexTableID) in `internal/SQF/PL/operator.go`. (3) Compile-check tests: `TestOperatorFactory_InterfaceCompiles`, `TestOptionalInterfaces_InterfaceCompiles`. No implementations yet — purely additive. **Spec:** `docs/compose/specs/sqo-subsystem/design.md` §4.2-§4.3; `iter-SQO-REQ1432-1456.md` Step 1.2. Estimate: ~200 lines, 2 files, 0.5 day. | high | small | REQ001432 | SQF/PL/factory.go (new), SQF/PL/operator.go |
| REQ001435 | SQB/OP + SQB/AG | **Implement OperatorFactory + optional interfaces on every concrete operator type.** Each operator in SQB/OP/AG needs `Children()/SetChild()` so SQO can navigate the tree; the factory implementation so SQO can build trees; and the optional interfaces (PredicateCarrier, ColPrunable, etc.) so passes can probe capabilities. **Fix:** (1) Create `internal/SQB/OP/factory.go` with `opFactory` struct implementing `pl.OperatorFactory`. Each method delegates to the existing `New*` constructor. (2) Add `Children() []pl.Operator` + `SetChild(idx int, child pl.Operator)` to: Filter, FilterProject, Project, Sort, Limit, HashJoin, NestedLoopJoin, Aggregate, HashAggregate, Distinct, SetOp, AdaptiveOp. (3) Add `SetUsedCols/UsedCols/RequestedCols/SetRequestedCols` to SeqScan, IndexScan. (4) Add `Predicate()/SetPredicate()` to Filter, FilterProject. (5) Add `IndexName/IndexColumns/IndexTableID` to IndexScan. (6) Add `Table()/Schema()` to SeqScan, IndexScan. (7) Add `GroupCols()/Aggregates()` to Aggregate, HashAggregate. (8) Add `OrderBy()` to Sort. (9) Add `Limit()/Offset()` to Limit. (10) Add `TestOperatorImplementsOptionalInterfaces` — type-assert each operator type to each interface it should implement. **Spec:** `docs/compose/specs/sqo-subsystem/design.md` §5 (operator integration); `iter-SQO-REQ1432-1456.md` Step 1.3. Estimate: ~300 lines, 8 files, 1 day. | high | medium | REQ001434 | SQB/OP/factory.go (new), SQB/OP/operators.go, SQB/OP/intermediate_basic.go, SQB/OP/hashjoin.go, SQB/OP/join.go, SQB/OP/distinct.go, SQB/OP/compound.go, SQB/AG/aggregate.go, SQB/AG/hashagg.go |
| REQ001436 | SQF/PL | **SQF/PL defines `Operator`/`Row`/`Value`/`ErrNoRows` as type aliases pointing to SQB/DT — Phase 1 of the bidirectional type-alias bridge.** Go type aliases let SQF/PL refer to Operator without owning the definition yet. After this REQ, SQO can import `pl.Operator` and resolve to SQB/DT. The definition still lives in SQB/DT — no behavior change. **Fix:** (1) Create `internal/SQF/PL/types.go` with `package pl; import dt "internal/SQB/DT"; type Operator = dt.Operator; type Row = dt.Row; type Value = dt.Value; var ErrNoRows = dt.ErrNoRows`. (2) Verify SQO/OC/optimizer.go already imports `pl.Operator` from REQ001432 (if not, change it now). (3) `go build ./... && go test ./...` pass. Gate: `grep -r '"SQB/DT"' SQO/` returns 0; SQF/PL imports SQB/DT (intentional, swapped in REQ001438). **Spec:** `docs/compose/specs/sqo-subsystem/design.md` §6.1 (type-alias bridge); `iter-SQO-REQ1432-1456.md` Step 2.1. Estimate: ~20 lines, 2 files, 0.25 day. | high | small | REQ001435 | SQF/PL/types.go (new), SQO/OC/optimizer.go |
| REQ001437 | SQB/OP + SQB/EV + SQB/EX + SQB/AG | **Phase 2 of type-alias bridge: SQB clusters import SQF/PL for interfaces, one file at a time.** Currently `SQB/OP/operators.go`, `SQB/EV/eval.go`, `SQB/EX/ex.go`, `SQB/AG/aggregate.go` import `dt "internal/SQB/DT"` for Operator/Row/Value. They must switch to `pl "internal/SQF/PL"` so that SQF/PL eventually owns the canonical types. **Fix:** Per file: change `import dt "internal/SQB/DT"` → `import pl "internal/SQF/PL"` for the four interface types (Operator/Row/Value/ErrNoRows); leave SQB/DT-specific types (ColumnMeta, RowArena, StoreSchema) on the dt import. Commit one file at a time (~5 lines per file changed). Files in order: SQB/OP/operators.go → SQB/OP/intermediate_basic.go → SQB/OP/hashjoin.go → SQB/OP/join.go → SQB/OP/distinct.go → SQB/OP/compound.go → SQB/EV/eval.go → SQB/EV/scalar_funcs.go → SQB/AG/aggregate.go → SQB/AG/hashagg.go → SQB/EX/ex.go → SQB/EX/planner_select.go → SQB/EX/planner.go. Each commit: `go build ./...` and `go test ./...` must pass. **Spec:** `docs/compose/specs/sqo-subsystem/design.md` §6.2; `iter-SQO-REQ1432-1456.md` Step 2.2. Estimate: ~12 files, ~5 lines each, 0.25 day (1 day total including review). | high | small | REQ001436 | SQB/OP/operators.go, SQB/OP/intermediate_basic.go, SQB/OP/hashjoin.go, SQB/OP/join.go, SQB/OP/distinct.go, SQB/OP/compound.go, SQB/EV/eval.go, SQB/EV/scalar_funcs.go, SQB/AG/aggregate.go, SQB/AG/hashagg.go, SQB/EX/ex.go, SQB/EX/planner_select.go, SQB/EX/planner.go |
| REQ001438 | SQF/PL + SQB/DT | **Phase 3 of type-alias bridge: swap direction — SQF/PL owns Operator/Row/Value; SQB/DT aliases them.** After REQ001437, all importers use SQF/PL. Now the canonical definitions move to SQF/PL and SQB/DT becomes the alias. **Fix:** (1) Copy Operator/Row/Value/ErrNoRows type definitions from `SQB/DT/types.go` to `SQF/PL/types.go` (replace the aliases with the full struct definitions). (2) In `SQB/DT/types.go`, replace definitions with `type Operator = pl.Operator; type Row = pl.Row; type Value = pl.Value; var ErrNoRows = pl.ErrNoRows`. (3) Verify: `grep -r '"SQB/DT"' SQF/` returns 0 (SQF no longer imports SQB); `grep -r '"SQB/DT"' SQO/` returns 0 (SQO imports SQF/PL); all SQB clusters still compile via the alias. (4) `go build ./... && go test ./...` pass. Gate: `grep -r '"SQB/"' SQO/` returns 0 across the entire SQO tree. **Spec:** `docs/compose/specs/sqo-subsystem/design.md` §6.3 (final swap); `iter-SQO-REQ1432-1456.md` Step 2.3. Estimate: ~50 lines, 2 files, 0.25 day. | high | small | REQ001437 | SQF/PL/types.go, SQB/DT/types.go |
| REQ001439 | SQO/CO | **Move predicate-analysis functions from SQB/EX/predicate.go to SQO/CO/predicate.go via delegation wrappers.** SQB/EX/predicate.go holds ~14 helper functions used by `planSelect`: `splitAnd`, `walkExpr`, `walkExprForTables`, `extractTablesFromExpr`, `splitPredicatesByTable`, `predicateCost`, `reorderIndices`, `orderSlice`, `extractColumnLiteral`, `extractColumnLiteralExpr`, `extractSingleEquality`, `extractInListValues`, `extractOrChainEquality`, `extractEqualityAnySide`, `literalValue`, `extractEquiJoinKeys`, `extractSingleOnEquiKey`, `findTableForColumn`, `findTableInSchemas`, `colNameFromExpr`, `isColumnLiteralPair`, `tryApplyPointLookup`, `canPushDown`, `estimatePredicateSelectivity`, `allInSet`, `equiJoinKey`, `literalToBytes`, `resolveSingleTablePredicate`, `resolveTableForColumn`, `splitAlphaNum`, `caseExprCost`, `funcArgCost`. **Pattern per function (4 commits each):** (a) SQO/CO adds `func X(args...) (...) { return ex.X(args...) }` wrapper. (b) SQB/EX caller switches to `co.X(args...)`. (c) SQO/CO inlines the real implementation; SQB/EX original becomes the wrapper. (d) SQB/EX original deleted. After all 14: SQB/EX/predicate.go is empty except for `(p *Planner)` method receivers — those move with their callers in REQ001439-ext. (3) Tests: each function retains its existing unit test (now run from SQO/CO/). (4) Final commit: delete SQB/EX/predicate.go entirely. **Spec:** `docs/compose/specs/sqo-subsystem/design.md` §7.1 (delegation pattern); `iter-SQO-REQ1432-1456.md` Step 3. Estimate: ~14 functions × 4 commits = ~56 commits, each ≤50 lines, 2 days. | high | medium | REQ001438 | SQO/CO/predicate.go (new), SQB/EX/predicate.go (delete) |
| REQ001440 | SQO/CO | **Move cost-model functions from SQB/EX/cost.go to SQO/CO/cost.go via delegation wrappers.** Functions: `estimateCostLegacy`, `estimateCostWithParams`, `estimateSelectivity`, `estimateSelectivityWithStats`, `estimateEqSelectivity`, `estimateRangeSelectivity`, `estimateRowCount`, `getTableRowCount`. Same 4-commit delegation pattern as REQ001439: wrapper → caller switches → inline → original deleted. Cost functions must use `pl.ColumnStats` (interface in `OC`, implementation in `LS`) instead of concrete `ls.ColumnStats` — handle via `StatsReader` interface from REQ001432. Tests: existing `cost_test.go` and `cost_indexscan_test.go` continue to pass from SQO/CO/. Final commit: delete SQB/EX/cost.go. **Spec:** `docs/compose/specs/sqo-subsystem/design.md` §7.2; `iter-SQO-REQ1432-1456.md` Step 3. Estimate: ~8 functions × 4 commits = ~32 commits, each ≤50 lines, 1.5 days. | high | medium | REQ001439 | SQO/CO/cost.go (new), SQO/CO/selectivity.go (new), SQB/EX/cost.go (delete) |
| REQ001441 | SQO/JO | **Move join-ordering functions from SQB/EX/join_order.go to SQO/JO/join_order.go via delegation wrappers.** Functions: `n3JoinOrdering`, `n3JoinOrderingMultiStart`, `filteredRowCount`, `exhaustiveJoinOrder`, `estimateJoinOrderCost`, `n3PredCacheKey`, `findPredicatesForPair`, `findPredicatesForSet`, `hasIndexOnTable`, `joinResultRows`, `isConnectedGraph`, `groupBushyJoins`, `extractTableColumn`. Also move `planner_stats_propagation.go` to `SQO/JO/stats_propagation.go` (uses OperatorFactory from REQ001435). Same 4-commit delegation pattern as REQ001439-440. Critical: `exhaustiveJoinOrder` builds join trees by calling concrete constructors — replace with `pl.OperatorFactory.NewHashJoin(...)` etc. per REQ001435. Tests: existing join-order tests continue to pass from SQO/JO/. Final commit: delete SQB/EX/join_order.go + planner_stats_propagation.go. **Spec:** `docs/compose/specs/sqo-subsystem/design.md` §7.3; `iter-SQO-REQ1432-1456.md` Step 3 + Step 4. Estimate: ~12 functions × 4 commits = ~48 commits, 2 days. | high | medium | REQ001440 | SQO/JO/join_order.go (new), SQO/JO/stats_propagation.go (new), SQB/EX/join_order.go (delete), SQB/EX/planner_stats_propagation.go (delete) |
| REQ001442 | SQO/RS | **Move resolve_slots from SQB/EX/resolve_slots.go to SQO/RS/resolve_slots.go via delegation wrapper, then wire SQO into planSelect.** Functions: `ResolvePlanSlots`, `resolveExprs`, `computeSchema`, `tableSchema`, `childOf`, `secondChild`, `projectColNames`, `resolveExprSlots`. (1) Same 4-commit delegation pattern: wrapper → caller (currently in `planSelect` after plan construction) switches → inline → original deleted. (2) After move, modify `planSelect` to invoke `rs.ResolvePlanSlots(plan, ctx)` instead of inline. (3) Add `TestResolveSlots_RunsFromSQO` verifying the wired path produces identical output. (4) Final commit: delete SQB/EX/resolve_slots.go. **Spec:** `docs/compose/specs/sqo-subsystem/design.md` §7.4; `iter-SQO-REQ1432-1456.md` Step 4. Estimate: ~8 functions × 4 commits = ~32 commits, 1.5 days. | high | medium | REQ001441 | SQO/RS/resolve_slots.go (new), SQB/EX/resolve_slots.go (delete) |
| REQ001443 | SQO/PF | **Pass: Column pruning via `ColPrunable` interface.** Extract the column-pruning logic currently inlined in `planSelect` (lines ~80-130 area) into `SQO/PF/column_pruning.go`. (1) Implement `ColumnPruning(pass *Pass) error` that walks the operator tree using `Transform`, calls `RequestedCols()` on each operator, and propagates downward. (2) For SeqScan, call `SetRequestedCols(...)` to limit columns read from storage. (3) For Project, propagate the projection list downward. (4) For `SELECT *`, skip pruning (all columns needed). (5) Tests: `TestColumnPruning_RemovesUnusedCols`, `TestColumnPruning_StarQuery_Skips`, `TestColumnPruning_PropagatesThroughJoin`. Wire into Optimizer pass chain in REQ001450. **Spec:** `docs/compose/specs/sqo-subsystem/design.md` §8.1; `iter-SQO-REQ1432-1456.md` Step 5.1. Estimate: ~150 lines, 1 file, 0.5 day. | high | small | REQ001435, REQ001442 | SQO/PF/column_pruning.go (new) |
| REQ001444 | SQO/PF | **Pass: FilterProject fusion — merge adjacent Filter+Project into a single FilterProject.** Currently the planner emits separate Filter and Project operators; FilterProject already exists as a fused operator (SQB/OP/intermediate_basic.go) but is not auto-emitted. (1) Implement `FilterProjectFusion(pass *Pass) error` using `Transform` to detect `Filter{child: Project{...}}` or `Project{child: Filter{...}}` patterns. (2) Replace with a single `FilterProject` operator carrying both the predicate and projection. (3) Tests: `TestFilterProjectFusion_FusesChain`, `TestFilterProjectFusion_LeavesNonAdjacent`. (4) Wire into Optimizer pass chain. **Spec:** `docs/compose/specs/sqo-subsystem/design.md` §8.2; `iter-SQO-REQ1432-1456.md` Step 5.2. Estimate: ~120 lines, 1 file, 0.5 day. | high | small | REQ001435 | SQO/PF/filter_project_fusion.go (new) |
| REQ001445 | SQO/PF | **Pass: Predicate pushdown — split WHERE conjuncts and push into scans.** Currently inlined in planSelect. (1) Implement `PredicatePushdown(pass *Pass) error` that uses `co.SplitAnd` (from REQ001439) to break the WHERE into conjuncts, calls `pl.PredicateCarrier.SetPredicate` on each Scan, and unsets the Filter above. (2) For JOIN ON clauses, use `co.splitPredicatesByTable` (from REQ001439) to route conjuncts to the appropriate side. (3) Tests: `TestPredicatePushdown_SplitsAndPushdown`, `TestPredicatePushdown_PushesToBothSides`, `TestPredicatePushdown_JoinConjuncts`. (4) Wire into Optimizer pass chain. **Spec:** `docs/compose/specs/sqo-subsystem/design.md` §8.3; `iter-SQO-REQ1432-1456.md` Step 5.3. Estimate: ~150 lines, 1 file, 0.5 day. | high | small | REQ001439, REQ001442 | SQO/PF/predicate_pushdown.go (new) |
| REQ001446 | SQO/PF | **Pass: Index selection — replace SeqScan with IndexScan when a predicate matches an index column.** Currently inlined in planSelect. (1) Implement `IndexSelection(pass *Pass) error` that checks for `PredicateCarrier` + `RelationSource` on each Scan. (2) For each predicate, look up matching indexes via `CatalogReader.Indexes(table)`. (3) If found and selectivity is better than SeqScan, call `pl.OperatorFactory.NewIndexScan(...)` and replace the SeqScan. (4) Tests: `TestIndexSelection_PicksIndexOnPredicateCol`, `TestIndexSelection_NoIndex_FallsBackToSeqScan`, `TestIndexSelection_CompositeIndex_MatchesLeadingCol`. (5) Wire into Optimizer pass chain. **Spec:** `docs/compose/specs/sqo-subsystem/design.md` §8.4; `iter-SQO-REQ1432-1456.md` Step 5.4. Estimate: ~200 lines, 1 file, 0.75 day. | high | medium | REQ001435, REQ001442, REQ001445 | SQO/PF/index_selection.go (new) |
| REQ001447 | SQO/PF | **Pass: Limit pushdown — detect Sort+Limit → TopNSort pattern.** When the plan ends with `Sort → Limit`, replace with a top-N sort that maintains a bounded heap. (1) Implement `LimitPushdown(pass *Pass) error` using `Transform` to find Sort-with-Limit-child. (2) Mark the Sort as `IsTopN` via the `LimitInfo` interface (add to `pl` in REQ001434 if not already done). (3) Sort.Open honors `IsTopN` and uses a bounded heap instead of full sort. (4) Tests: `TestLimitPushdown_TopNDetection`, `TestLimitPushdown_BoundedHeap_VerifiesOutput`, `TestLimitPushdown_NoLimit_NoChange`. (5) Wire into Optimizer pass chain. **Spec:** `docs/compose/specs/sqo-subsystem/design.md` §8.5; `iter-SQO-REQ1432-1456.md` Step 5.5. Estimate: ~120 lines, 1 file, 0.5 day. | high | small | REQ001435 | SQO/PF/limit_pushdown.go (new), SQB/OP/sort.go |
| REQ001448 | SQO/PF | **Pass: Subquery decorrelation — convert EXISTS → semi-join, IN (subq) → semi-join.** Currently handled in SQB/EV/eval.go via re-evaluation per outer row. (1) Implement `SubqueryDecorrelation(pass *Pass) error` using `ExprVisitor` (from REQ001433) to find `ExistsSubquery` and `InSubquery` nodes. (2) Replace with `SemiJoin` operator built via `pl.OperatorFactory.NewHashJoin(semiJoin=true)`. (3) Tests: `TestSubqueryDecorrelation_ExistsToSemiJoin`, `TestSubqueryDecorrelation_InToSemiJoin`, `TestSubqueryDecorrelation_Correlated_StillWorks`. (4) Wire into Optimizer pass chain. **Spec:** `docs/compose/specs/sqo-subsystem/design.md` §8.6; `iter-SQO-REQ1432-1456.md` Step 5.6. Estimate: ~200 lines, 1 file, 1 day. | high | medium | REQ001433, REQ001435 | SQO/PF/subquery_decorrelation.go (new), SQB/EV/eval.go |
| REQ001449 | SQO/PF + SQF/RE | **Move constant folding + CSE from SQF/RE to SQO/PF/constant_folding.go.** Currently in `SQF/RE/constant_folding.go` (and `fold_cse.go`). Some of this is AST-rewriting (must happen at planning time before binding), some is operator-tree rewriting (should happen at optimization time). (1) Audit SQF/RE constant folding logic. (2) Move only the **operator-tree** rewrites (e.g., `Filter{Project{1+1}} → Project{2}`) to SQO/PF/. (3) Leave AST-level rewrites (e.g., `x+0 → x`) in SQF/RE — they must run before binding. (4) Tests: `TestConstantFolding_FoldsBinaryExpr`, `TestConstantFolding_ASTRewrites_StayInSQF`. (5) Wire into Optimizer pass chain. **Spec:** `docs/compose/specs/sqo-subsystem/design.md` §8.7; `iter-SQO-REQ1432-1456.md` Step 5.7. Estimate: ~150 lines, 2 files, 0.5 day. | high | small | REQ001442 | SQO/PF/constant_folding.go (new), SQF/RE/constant_folding.go (delete portions) |
| REQ001450 | SQB/EX | **planSelect shrinks from ~316 lines to ~50 lines — replace inline optimization with Optimizer pass chain.** After REQ001443-449, all optimization passes are in SQO/PF/. planSelect should now: (1) construct the initial plan tree, (2) build an `oc.Optimizer` with passes `[ColumnPruning, FilterProjectFusion, PredicatePushdown, IndexSelection, LimitPushdown, SubqueryDecorrelation, ConstantFolding]`, (3) call `optimizer.Optimize(plan, ctx)`, (4) hand off to executor. Remove the no-op Optimizer from REQ001432 and instantiate the real one with the pass chain. (1) Tests: all existing planner tests pass unchanged. (2) Add `TestPlanSelect_OptimizerChain_RunsAllPasses` verifying each pass runs in order. (3) Add `TestPlanSelect_Output_IdenticalToBaseline` comparing before/after output for select1-5 SLT queries. **Spec:** `docs/compose/specs/sqo-subsystem/design.md` §9 (planner integration); `iter-SQO-REQ1432-1456.md` Step 5.8. Estimate: ~80 lines net change (replace ~270 with ~50), 1 file, 0.5 day. | high | small | REQ001443, REQ001444, REQ001445, REQ001446, REQ001447, REQ001448, REQ001449 | SQB/EX/planner_select.go |
| REQ001451 | SQB/EX | **Delete SQB/EX files whose contents have fully moved to SQO (Step 6 cleanup).** After REQ001439-450, these SQB/EX files contain only wrappers, dead code, or now-trivial helpers. (1) Delete `internal/SQB/EX/predicate.go` (all functions moved in REQ001439). (2) Delete `internal/SQB/EX/cost.go` (all functions moved in REQ001440). (3) Delete `internal/SQB/EX/join_order.go` (all functions moved in REQ001441). (4) Delete `internal/SQB/EX/resolve_slots.go` (all functions moved in REQ001442). (5) Delete `internal/SQB/EX/planner_stats_propagation.go` (moved in REQ001441). (6) Delete or trim `internal/SQB/EX/planner_minmax.go` (portions moved). (7) Delete `internal/SQB/EX/fold_cse.go` if constant folding moved to SQO/PF/ (REQ001449). (8) Remove unused imports from remaining SQB/EX files. (9) Remove the now-unused Optimizer field from REQ001432 (replaced in REQ001450). (10) Verify: `go build ./... && go test ./... -race -count=1` pass; SLT select1-4 pass. **Spec:** `docs/compose/specs/sqo-subsystem/design.md` §10 (cleanup); `iter-SQO-REQ1432-1456.md` Step 6. Estimate: ~4000 lines deleted, 0.5 day. | high | small | REQ001450 | SQB/EX/predicate.go (delete), SQB/EX/cost.go (delete), SQB/EX/join_order.go (delete), SQB/EX/resolve_slots.go (delete), SQB/EX/planner_stats_propagation.go (delete), SQB/EX/planner_minmax.go (trim), SQB/EX/fold_cse.go (delete) |
| REQ001452 | docs/design + docs/compose | **Update ARCH.md: add SQO subsystem entry + new dependency order SQF → SQO → SQB.** Currently ARCH.md lists SQF → SQB. After this iteration, SQO sits between them. (1) Add SQO subsystem section to `docs/design/ARCH.md` covering clusters OC, PF, CO, JO, RS. (2) Add the dependency arrow `SQF → SQO → SQB` to the subsystem diagram. (3) Update `docs/compose/specs/sqo-subsystem/requirements.md` success criteria with measured results (lines moved, SLT pass rate, benchmark deltas). (4) Mark REQ001432-451 as done in this `REQUIREMENTS.md` (delete the TBD rows). (5) Update `iter-SQO-REQ1432-1456.md` with actual outcome (commit hashes, lines shipped, deviations). **Spec:** `docs/compose/specs/sqo-subsystem/requirements.md` §5 (success criteria); `iter-SQO-REQ1432-1456.md` (lifecycle outcome). Estimate: documentation-only, 0.5 day. | medium | small | REQ001451 | docs/design/ARCH.md, docs/compose/specs/sqo-subsystem/requirements.md, docs/development/REQUIREMENTS.md, docs/development/iterations/iter-SQO-REQ1432-1456.md |
