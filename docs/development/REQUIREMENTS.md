## TBD

| ID | Subsystem | Requirement | Priority | Effort | Deps | Touches |
| REQ000316 | SQL | Incremental materialized views (auto-maintained aggregation views with query routing) | medium | L | iter-08 (operators), iter-12 (catalog) | `SQL/EX/matview.go` (new); `ENG/LS` triggers on view base tables |
| REQ000537 | ENG/LS | Sharded memtable (per-prefix skiplist) — split active memtable into N sharded skiplists keyed by `hash(key) % N` (N=16 default); directory routes lookup; flush merges shards into L0 SST; eliminates global CAS contention on head pointer under high write concurrency. New `internal/ENG/LS/sharded_memtable.go` + `shard.go` | high | L | iter-04 (skiplist), iter-27 (escape-hinted arena) | `ENG/LS/sharded.go` (new); `ENG/LS/engine.go` `Write`/`NewIterator`; `Options.MemTableShards` (default 16, 1 = legacy single-shard mode) |
| REQ000538 | ENG/LS | Zero-copy iterator with borrowed buffers — `mergeIterator.Next()` and `sstIter` return `unsafe.Pointer` references into buffer-pool blocks; iterator registers with engine epoch manager on creation, deregisters on Close; only the public API boundary (`Engine.Get`/`Iterator.Key`/`Iterator.Value` for external callers) materializes owned copies. Eliminates ~6 allocs/row in merge iterator. New `internal/ENG/LS/borrowed_iter.go` + `epoch_iter.go` | high | L | iter-27 (epoch manager), iter-19 (sync.Pool buffers) | `ENG/LS/api.go` `mergeIterator` rewrite; new `ENG/LS/epoch_iter.go`; `TXN/LC/qsbr.go` integration for iterator epoch registration |
| REQ000539 | MEM/BF | Sharded buffer pool with per-shard clock hands — replace single global `map[uint64]*bufferSlot`+`sync.RWMutex` with N=32 shards; `hash(blockID) % N` routes lookup; each shard owns its own sub-map, `hand`, and eviction loop; eliminates global mutex contention under concurrent reads. New `internal/MEM/BF/sharded_bp.go` | high | L | iter-02 (buffer pool), REQ000199 (sharded mutex idea, partial), REQ000303 (W-TinyLFU) | `MEM/BF/sharded_bp.go` (new) + `bf.go` refactor; `Options.BufferPoolShards` (default 32) |
| REQ000542 | WAL/FL | Group commit pipeline — txns waiting on `Sync()` join a group; when the first tx arrives or a 50µs deadline fires, the entire group fsyncs together; each waiter is unblocked with its assigned LSN. Builds on existing async fsync (REQ000301) and io_uring (REQ000295); target: 10x fsync throughput under burst commit load. New `internal/WAL/FL/group_commit.go` | high | L | REQ000184 (write barrier), REQ000301 (async fsync) | `WAL/FL/group_commit.go` (new) + `fl.go` refactor; `Options.GroupCommitTimeout` (default 50µs) |
| REQ000543 | SQL/EX | Shape-specialized fast paths — extend REQ000311/313 with shape-driven specialization that captures hot path inline as small specialized Go funcs (not full JIT); specialize per plan shape: `Filter{int64_col_lit_eq}`, `Project{cols=N}`, `HashAggregate{group_by_int64}`, etc.; registered in `codegen_ops.go`; benchmarks must show >1.5x over the interpreted path. Deferred: XL effort, iter-29. | medium | XL | REQ000311 (codegen framework), REQ000313 (ADQC) | `SQL/EX/codegen/shapes/` (new); `SQL/EX/adqc_specialize.go` (new); `SQL/EX/shape_bench_test.go` (new) |
| REQ000546 | SQL/EX | Vectorized string columns via unsafe pointer + length prefix — replace `[]string` columns with `[]byte` raw + `[]uint32` lengths (or `[]struct{Ptr uintptr; Len uint32}`); eliminates string-header copy and enables real SIMD string compare (memcmp); backward-compat path via `Batch.StringValue(col,row)` accessor. Deferred: L effort, iter-29. | medium | L | REQ000149 (batch memory), REQ000310 (SIMD) | `SQL/EX/batch.go` `Column` for text/varchar/blob; `SQL/EX/eval_vec.go` `compareStringColsVec`; `SQL/EX/simd_dispatch.go` runtime detection |
| REQ000547 | TXN/MV | Per-NUMA arena pools — extend the generational arena (REQ000064/305) with per-NUMA-node arena pools; thread first-touch policy (REQ000309) places new arenas on the calling node; reclaim drains per-node; eliminates contention on global `reclaimMu` and `arenaPool` under high transaction throughput on multi-socket hosts | medium | M | REQ000064 (generational arena), REQ000305 (epoch reclaim), REQ000309 (NUMA) | `TXN/MV/arena.go` `numaArenaPool`; `TXN/LC/epoch.go` per-node drain lists; `ENG/NM/numa.go` allocator policy |
| REQ000549 | MEM/BF | Block-level MVCC version tagging — buffer pool slots carry an optional `versionRange [startTS, endTS]` field; `BufferPool.GetWithTS(blockID, readTS)` returns the page whose version range covers `readTS` (or the page whose range is the latest, then we filter via the version chain at read time); enables cross-transaction cache sharing with snapshot consistency. Deferred: XL effort, iter-29. | low | XL | iter-02 (buffer pool), REQ000539 (sharded), REQ000307 (MV-OCC) | `MEM/BF/mvcc_slot.go` (new) + `bf.go` `GetWithTS`; `TXN/SN/snapshot.go` registers `readTS` on Get |
| REQ000556 | SQL/PS+EX | `CREATE VIRTUAL TABLE ... USING module_name` — BNF line 23; no T_VIRTUAL token, no parser path, no executor. SQLite-compatible modules (fts5, rtree) cannot be registered. Add lexer tokens (`T_VIRTUAL`, `T_USING` already exists for JOIN), parser path (`parseCreateVirtualTable` + `VirtualTableStmt` AST), executor module-dispatch loop. Deferred: XL effort, iter-29. | high | XL | iter-08 (catalog), REQ000250 (BTree) | `SQL/LX/lx.go`+`token.go` (T_VIRTUAL); `SQL/PS/ps.go` `parseCreateVirtualTable` + `SQL/PS/ast.go` `VirtualTableStmt`; `SQL/EX/ex.go` `buildWriterOp` route; new `SQL/EX/virtual.go` registry |
| REQ000571 | ENG/LS | SST page cache — replace `os.ReadFile` with block-level cache + sync.Pool. Currently `readFromSST` (`engine.go:193`) and `mergeIterator.init` (`api.go:246`) call `os.ReadFile` for every SST file on every read/iterate. For a 100 MB SST this allocates 100 MB per read. New `internal/ENG/LS/page_cache.go`: fixed-size cache (default 256 MB), 4 KB blocks keyed by `(fileID, blockOffset)`, clock-sweep eviction, `sync.Pool` for page buffers. `mergeIterator` consults cache before file read. Must handle concurrent access (read path is hot). Benchmarks: measure allocs/op and ns/op for sequential and random reads before/after | critical | L | iter-04 (L0 SST), iter-02 (buffer pool) | `ENG/LS/page_cache.go` (new); `ENG/LS/engine.go` `readFromSST` — replace `os.ReadFile` with cache lookup; `ENG/LS/api.go` `mergeIterator.init` — same; `ENG/LS/compaction.go` — compaction reads can bypass cache (bulk reads) |
| REQ000583 | SQL/PS | Add visitor pattern to AST — currently AST traversal relies on manual type-switch in `PL/memo.go:107-223` with no exhaustiveness check. Missing node types (e.g. `IntervalLiteral`, `WindowFunc`, `RaiseFunc`) silently produce different memo hashes. Fix: add `Accept(Visitor)` to `Expr` and `Stmt` interfaces; create `BaseVisitor` with default no-op implementations; implement `HashVisitor` that writes to `hash.Hash` for memo keys. Refactor `memo.go` to use the visitor. Future visitors: `TypeChecker`, `ConstantFolder`, `PlanSerializer`. Test: verify all AST node types are covered (compile-time exhaustiveness via interface satisfaction) | medium | L | iter-07 (parser) | `SQL/PS/ast.go` — add `Accept` methods; new `SQL/PS/visitor.go`; `SQL/PL/memo.go` — refactor to use visitor |
| REQ000586 | SQL/EX | Thread ExecContext through operators (eliminate globals) — `eval.go:383` `currentSubqueryPlanner`, `ex.go:32` `currentSessionID`, `store.go:98-112` `tables`/`storeSchemas`/`viewRegistry` are all package-level globals. This makes concurrent Executor instances unsafe. Fix: add `ExecContext` struct holding session ID, planner, params, store references; thread it through operators via `Operator.SetContext(ctx)` or pass in `Next(ctx)`. Start with `currentSubqueryPlanner` (most dangerous global). Test: run two Executor instances concurrently on different tables; verify no cross-contamination | medium | L | iter-08 (SQL execute) | `SQL/EX/ex.go` — new `ExecContext` struct; `SQL/EX/eval.go` — remove `currentSubqueryPlanner` global; all operators — accept context |
| REQ000587 | tests/sqlcmp/slt | sqllogictest runner `diffHashed` sorts individual cells instead of rows — for multi-column queries, sorting individual cells produces a different hash than SQLite's reference implementation (which sorts rows as tab-separated strings, then flattens). First observed on select1.test L101: `SELECT a+b*2+c*3+d*4+e*5, (a+b+c+d+e)/5 FROM t1 ORDER BY 1,2` reports "got 22cec7a..., want 808146289..." Fix: sort rows as tab-separated strings before flattening in `diffHashed` | medium | S | iter-28 (SLT test framework) | `tests/sqlcmp/slt/diff.go` `diffHashed` — sort rows, not cells; new `rowString` helper |
| REQ000588 | TXN/MV | **TOCTOU race in `arena.promote()`** — `promote()` at `arena.go:84-110` takes a snapshot of `a.youngOff` at line 85, then another goroutine's `tryAllocYoung()` may CAS-succeed after line 103 but before `youngOff.Store(0)` at line 105. The promote only copies up to the snapshot offset; `youngOff.Store(0)` at line 105 loses all concurrent allocations made after the snapshot. This causes silent data corruption (allocated data written but never promoted, overwritten by future young generation writes). Fix: replace `youngOff.Store(0)` with CAS loop that subtracts the promoted amount, or migrate to a two-phase promote that drains concurrent allocators before resetting | critical | M | iter-27 (MPMC epoch reclaim), iter-23 (generational arena) | `TXN/MV/arena.go` `promote()` method; see code review iter-29 Bug 1 |
| REQ000589 | TXN/LC | **`EpochManager.Reclaim()` is a no-op** — `epoch.go:72-94` the `Reclaim()` method calculates `safeEpoch` at line 75, then iterates the `sync.Range` callback at lines 76-83 which unconditionally returns `true` (never actually reclaiming anything). Lines 85-89 assign `_ = ptr` as a placeholder. The method signature promises memory reclamation but performs zero work. Production systems experience unbounded memory growth under sustained transaction load. Fix: integrate with `TXN/MV/arena.go`'s generation reclamation and actually free the `pendingOlds` lists whose epoch < safeEpoch | critical | M | iter-27 (epoch reclaim), iter-23 (generational arena) | `TXN/LC/epoch.go` `Reclaim()`; `TXN/MV/arena.go` `reclaimMu` + `pendingOlds`; see code review iter-29 Bug 2 |
| REQ000590 | TXN/VL | **WAL write failure causes slot leak in `txn.Commit()`** — `protocol.go:174-181` when `t.wal.Append()` (line 174) or `t.wal.Sync()` (line 178) fails, the code sets `PhaseAborted` and returns the error. However `finalize()` is never called (line 198), so the transaction slot acquired from the slot allocator is never released back to the free stack. Each WAL failure permanently consumes one slot. Fix: call `finalize()` in the error path before returning, or use `defer finalize()` at the top of the commit method | critical | S | iter-17 (wal protocol), iter-05 (WAL writer) | `TXN/VL/protocol.go` `Commit()` error paths; see code review iter-29 Bug 3 |
| REQ000591 | WAL/FL | **`Flusher.Close()` silently discards buffered data** — `fl.go:134-139` sets the `closed` flag without flushing or draining the in-flight write buffer (`wb`). If `Close()` is called while `wb` contains uncommitted data, those writes are silently lost. Callers expect `Close()` to guarantee durability for all previously `Sync()`'d writes. Fix: call `flushWAL()` or `batchCommit.Wait()` before returning from `Close()`; document that pending (non-sync'd) writes may be lost | critical | S | iter-05 (WAL FL), iter-01 (WAL WR) | `WAL/FL/fl.go` `Close()` method; see code review iter-29 Bug 4 |
| REQ000592 | SQL/PS | **Unchecked type assertions in `parsePrimary()`** — `expr.go:12` `val := p.current.Literal.(int64)` and `expr.go:20` `val := p.current.Literal.(string)` perform unconditional type assertions without the comma-ok pattern. If `p.current.Literal` holds a different type (e.g. float64 from an alternative parser path or a corrupt token), the program panics. Fix: use `val, ok := p.current.Literal.(int64); if !ok { return nil, &SyntaxError{...} }` for both assertions | high | S | iter-07 (parser) | `SQL/PS/expr.go` `parsePrimary()` lines 12, 20; see code review iter-29 Bug 5 |
| REQ000593 | SYS/DS | **`init()` registers SQL driver without `sync.Once`** — `ds.go:29-31` calls `sql.Register("razor", ...)` in package `init()`. If the package is imported multiple times (via transitive dependencies or build tags), `sql.Register` panics on duplicate registration. Fix: wrap in `sync.Once` or check `sql.Drivers()` before registering | high | S | iter-14 (SQL driver), iter-20 (SYS) | `SYS/DS/ds.go` `init()` function; see code review iter-29 Bug 6 |
| REQ000594 | SQL/RE | **`Rewrite` silently discards recursive errors** — `rewrite.go:53-57` calls `Rewrite(s.Left)` and `Rewrite(s.Right)` but only assigns the result to `out.Left`/`out.Right` if `err == nil`. If `Rewrite` returns an error, the original (unrewritten) subtree `s.Left` is silently kept, producing a partially-rewritten AST with no error indication. Fix: return the error immediately when recursive `Rewrite` fails, or accumulate errors | high | S | iter-07 (parser/rewrite) | `SQL/RE/rewrite.go` `Rewrite` compound statement handling lines 53-57; see code review iter-29 Bug 7 |
| REQ000595 | ENG/LS | **`readFromSST` and `MayContain` silently skip read errors** — `engine.go:155-163` and `engine.go:192-200` use `continue` to skip SST files when `os.ReadFile` or `openSST` fails. This masks disk I/O errors (permission denied, file corruption, hardware faults) as "key not found" rather than propagating them to the caller. Fix: at minimum, log the error with `slog.Warn` before continuing; consider returning the error to prevent silent data unavailability | high | S | iter-04 (L0 engine), iter-03 (SST) | `ENG/LS/engine.go` `readFromSST` lines 155-163, `MayContain` lines 192-200; see code review iter-29 Bug 8 |
| REQ000596 | ENG/LS | **`catalog.flushLocked` discards `f.Sync()` error** — `catalog.go:820-823` calls `f.Sync()` and assigns the error to `_`. A failed `Sync()` means the catalog metadata write is not durable — the system believes it has persisted data that may be lost on crash. Fix: capture the sync error and return it, triggering the existing rollback path | high | S | iter-12 (catalog), iter-05 (WAL) | `ENG/LS/catalog.go` `flushLocked` lines 820-823; see code review iter-29 Bug 9 |
| REQ000597 | TXN/LC | **Missing full memory barrier in `qsbr.go` between Store and Load** — `qsbr.go:41` `flag.Store(1)` followed by `qsbr.go:42` `globalEpoch.Load()`. On weakly-ordered architectures (ARM/POWER), the Store may be reordered after the Load. A reader thread on another core could see `flag=1` (believing the thread is in a critical section) while the reader has not yet observed the current `globalEpoch`. This breaks QSBR's correctness guarantee. Fix: insert an explicit memory barrier (e.g. `atomic.StoreInt32` + `atomic.LoadUint64` with proper ordering, or use `sync/atomic`'s `StoreLoad` barrier pattern) between the two operations | high | S | iter-27 (epoch reclaim) | `TXN/LC/qsbr.go` lines 41-42; see code review iter-29 Bug 10 |
| REQ000598 | FIL/LF | **TOCTOU race in `SegmentManager.GetSegment()`** — `lf.go:62-113` acquires `fh.mu.Lock()` at line 64 to check `fh.FD != -1`, then unlocks at line 67, then calls `unix.Open` at line 71 outside the lock. Another goroutine's `Close()` can set `fh.FD = -1` in the window between unlock and re-open, causing the new FD to be opened on a closing file. Fix: hold the lock across the entire check-and-open sequence, or use `atomic.Int32` for FD with compare-and-swap | high | M | iter-05 (WAL), iter-21 (file layer) | `FIL/LF/lf.go` `GetSegment()` lines 62-113; see code review iter-29 Bug 11 |
| REQ000599 | FIL/FS | **`FileSystem.Open()` close-reopens cached handles** — `fs.go:176-203` when a file already exists in the `handles` map, the code closes the existing FD (`unix.Close(fh.FD)`) and re-opens it (`unix.Open`) from scratch. This creates a window where another goroutine with the previous FD may still issue reads/writes on the now-closed FD. Fix: reuse the existing FD directly rather than close+reopen; only reopen if the FD is stale (e.g. file was deleted and recreated) | high | S | iter-21 (file layer), iter-01 (WAL) | `FIL/FS/fs.go` `Open()` lines 176-203; see code review iter-29 Bug 12 |
| REQ000600 | FIL/DF | **`BlockDevice` has no concurrent-close protection** — `df.go:40-47` the `fd` field is a plain `int` (not atomic) and is set to -1 by `Close()` at line 260. All other methods (`ReadBlock`, `WriteBlock`, `Sync`, `Size`) access `d.fd` without synchronisation. A concurrent read and close produces a data race; after close, reads may operate on FD -1 (EBADF) or on a re-used FD number belonging to a different file. Fix: add a `sync.RWMutex` or use `atomic.Int64` for the FD, checking validity under lock in every public method | high | M | iter-02 (block device), iter-21 (file layer) | `FIL/DF/df.go` `BlockDevice` struct and all methods; see code review iter-29 Bug 13 |
| REQ000601 | SYS/SY | **Executor error type matched via string comparison** — `sy.go:505` uses `err.Error() == "key not found"` to detect a not-found condition. String matching on error messages is fragile: message changes (even capitalization or punctuation) silently break the check; wrapped errors (via `fmt.Errorf("%w", ...)`) change the message and also break. Fix: define a sentinel error (e.g. `ls.ErrNotFound`) in the engine layer and use `errors.Is(err, ls.ErrNotFound)` | high | S | iter-14 (SQL exec), iter-20 (SYS) | `SYS/SY/sy.go` `executorStoreAdapter.Get` line 505; see code review iter-29 Bug 14 |
| REQ000602 | SQL/PS | **Unchecked `fmt.Sscanf` return values in interval/type-size parsing** — `expr.go:633` `fmt.Sscanf(p.current.Lexeme, "%d", &n)`, `expr.go:659` and `expr.go:675` all ignore the error return. When `Sscanf` fails (non-numeric input, overflow), `n`/`m` retain their zero value, producing silently wrong type sizes or interval qualifiers. Fix: read the error return, produce a `SyntaxError` with the offending lexeme | high | S | iter-07 (parser) | `SQL/PS/expr.go` lines 633, 659, 675; see code review iter-29 Bug 15 |
| REQ000603 | TXN/LC | **`getGoroutineID()` uses `runtime.Stack` on hot path** — `goid.go:8-35` calls `runtime.Stack(buf, false)` which allocates a 64-byte buffer and parses the goroutine stack trace as a string. This is called on every `Acquire()`/`Release()` in the lock-free epoch manager, making it a significant allocation source under high transaction concurrency. Fix: replace with `runtime.StoreStackInLabels` (Go 1.22+) or a cached goroutine-local ID via `runtime.GetGoExitHandler` + `sync.Pool`, reducing per-call allocation to zero | medium | M | iter-27 (epoch reclaim) | `TXN/LC/goid.go` `getGoroutineID()`; `TXN/LC/epoch.go` `Acquire()`/`Release()` call sites; see code review iter-29 Bug 16 |
| REQ000604 | FIL/DF | **Temporary buffer alignment via O(n) byte-scanning** — `df.go:29-38` `bufPool.New` aligns a byte slice to a 4096-byte boundary by iterating `for len(data)%4096 != 0 { data = data[1:] }` in a loop. This is O(n) per allocation where n is up to 4095, adding measurable latency on every buffer pool allocation. Fix: pre-compute the alignment offset via `(uintptr(unsafe.Pointer(&data[0])) + 4095) & ^uintptr(4095)` or use `mmap` with alignment guarantees | medium | S | iter-02 (block device) | `FIL/DF/df.go` `bufPool` `New` function lines 29-38; see code review iter-29 Bug 17 |
| REQ000605 | TXN/MV | **Hot path `[]byte`→`string` conversion in MVCC chain lookup** — `mv.go:16` `GetVersionChain` and `mv.go:24` `GetOrCreateVersionChain` convert `[]byte` keys to `string` (`string(key)`) for use with `sync.Map`. Each conversion allocates a new string header on the heap. On the read path (every `Engine.Get`), this adds a non-trivial allocation tax. Fix: use string interning (e.g. `intern.BytesToString(b) *(*string)(unsafe.Pointer(&b))`) with a map of `[]byte` keys, or switch to a concurrent hash-trie that natively supports `[]byte` keys | medium | M | iter-27 (MVCC), iter-23 (generational arena) | `TXN/MV/mv.go` `GetVersionChain` line 16, `GetOrCreateVersionChain` line 24; see code review iter-29 Bug 18 |
| REQ000606 | MEM/OF | **`sizeClass` array declared three times** — `of.go:22-39` (`sizeClass()`), `of.go:60-72` (`classIndex()`), and `of.go:77-82` (`NewOffHeap()`) each declare an identical `classes` array of 16 size-class boundaries. This is a DRY violation: any change to the size-class scheme must be applied in three places, inviting inconsistency. Fix: extract as a package-level `var sizeClasses = [...]int{...}` and reference it from all three functions | medium | S | iter-02 (buffer pool), iter-02 (overflow) | `MEM/OF/of.go` `sizeClass()` lines 22-39, `classIndex()` lines 60-72, `NewOffHeap()` lines 77-82; see code review iter-29 Bug 19 |
| REQ000607 | WAL/WR | **Non-atomic read-then-write on `w.synced`** — `wr.go:223-225` and `wr.go:275-276` access `w.synced` (an `atomic.Uint64`) in a read-then-write pattern (`if lsn > w.synced.Load() { w.synced.Store(lsn) }`). Two goroutines executing this concurrently can both read the same old value, both determine their LSN is greater, and both `Store`, with one of the Stores effectively lost (one increment is dropped). While this is benign (the lost Store only makes `synced` lower than true, not higher — no incorrect LSN assignment), it violates Go's memory model and triggers `-race` detector warnings. Fix: replace with `atomic.CompareAndSwap` in a loop, or accept the benign race and document it with a `//nolint:race` annotation | medium | S | iter-01 (WAL WR) | `WAL/WR/wr.go` `syncLocked()` lines 223-225, `SyncAsync()` lines 275-276; see code review iter-29 Bug 20 |
| REQ000608 | ENG/LS | **`skiplist.Find` and `Iterator.Value` acquire `mu.Lock()` on `atomic.Value` reads** — `skiplist.go:176-179` `Find` locks `curr.mu` before `curr.value.Load()`, and `skiplist.go:213-219` `Iterator.Value` does the same. Since `atomic.Value.Load()` is itself thread-safe, the mutex provides no additional safety — it only adds lock contention on the hot read path. Fix: remove the `mu.Lock()`/`Unlock()` around all `atomic.Value.Load()` calls in the skiplist; the `mu` should only protect non-atomic fields | medium | S | iter-04 (skiplist) | `ENG/LS/skiplist.go` `Find()` lines 176-179, `Iterator.Value()` lines 213-219; see code review iter-29 Bug 21 |
| REQ000609 | ENG/LS | **`randomLevel` uses global `math/rand` with implicit mutex** — `skiplist.go:58-63` calls `rand.Intn()` which uses the global `math/rand` source protected by a package-level mutex. Under concurrent skiplist insertions, all goroutines contend on this single mutex, serialising random number generation. Fix: use `rand.New(rand.NewPCG(...))` (Go 1.22+) to create a per-skiplist or per-goroutine random source, eliminating global lock contention | medium | S | iter-04 (skiplist) | `ENG/LS/skiplist.go` `randomLevel()` lines 58-63; see code review iter-29 Bug 22 |
| REQ000610 | ENG/LS | **`compaction.fileName` produces non-digit characters for Level > 9** — `compaction.go:224-227` formats level via `string(rune('0' + meta.Level))`. When `Level > 9`, the expression produces runes beyond `'9'` (e.g. `:` for level 10, `;` for level 11), creating invalid SST filenames that will fail to open or create. Fix: use `fmt.Sprintf("%d", meta.Level)` or `strconv.Itoa(int(meta.Level))` | medium | S | iter-04 (compaction) | `ENG/LS/compaction.go` `fileName()` lines 224-227; see code review iter-29 Bug 23 |
| REQ000611 | ENG/LS | **`table.DropTable` ignores index deletion errors** — `table.go:77-90` calls `index.Delete()` but ignores its error return (uses `_`). If index deletion fails, the table metadata is removed from the registry but the index data remains orphaned, leading to stale index entries. Fix: return the index deletion error to the caller, or at minimum log it with `slog.Warn` | medium | S | iter-12 (catalog), iter-08 (table) | `ENG/LS/table.go` `DropTable()` lines 77-90; see code review iter-29 Bug 24 |
| REQ000612 | ENG/LS | **`flush.go` uses `fmt.Fprintf(os.Stderr)` instead of `slog`** — `flush.go:160-162` writes `fmt.Fprintf(os.Stderr, "flush: mkdir sst: %v\n", err)` on directory creation failure. This violates the project-wide `slog`-only logging policy (`AGENTS.md`: "`log/slog` only. No `fmt.Printf` or `log.Printf`"). Fix: replace with `slog.Warn("flush: mkdir sst", "error", err)` | low | S | iter-04 (flush) | `ENG/LS/flush.go` line 160-162; see code review iter-29 Bug 25 |
| REQ000613 | ENG/LS | **`memtable.Insert` returns `error` but always returns nil** — `memtable.go:25` the `Insert` method signature returns `error` but the implementation unconditionally returns `nil`. This forces callers to write error-handling boilerplate for an infallible operation, violating Go's "don't return useless errors" convention. Fix: remove the `error` return value from the signature; update all callers to expect no error | low | S | iter-04 (memtable) | `ENG/LS/memtable.go` `Insert()` method; all callers of `memtable.Insert()`; see code review iter-29 Bug 26 |
| REQ000614 | ENG/LS | **`columnar.go` unused variables `keysLen` and `valsLen`** — `columnar.go:74` `_ = keysLen` and `columnar.go:118` `_ = valsLen` suppress compiler errors for computed-but-unused values. These represent missing integrity checks: the code computes key/value length counts but never validates them against the expected count. Fix: either use the values for validation (`if keysLen != expectedCount { return ... }`) or remove them entirely | low | S | iter-15 (columnar) | `ENG/LS/columnar.go` lines 74, 118; see code review iter-29 Bug 27 |
| REQ000615 | TXN/MV | **Custom `maxUint64` constant duplicates `math.MaxUint64`** — `version.go:8` defines `const maxUint64 = ^uint64(0)` while the standard library provides `math.MaxUint64`. The custom constant is less readable and may trigger linters. Fix: replace with `math.MaxUint64` from the standard library | low | S | iter-23 (MVCC) | `TXN/MV/version.go` line 8; see code review iter-29 Bug 28 |
| REQ000616 | ENG/LS | **`sst_writer.Finish` allocates byte arrays in loop body** — `sst_writer.go:200-205` declares `var offsetBuf [8]byte` and `var sizeBuf [8]byte` inside the index-entry loop. Each iteration allocates the arrays on the stack (benign for escape analysis) but the pattern is suboptimal: moving them outside the loop avoids repeated zero-initialisation. Fix: hoist both declarations before the loop, then reassign values inside the loop using `binary.LittleEndian.PutUint64` | low | S | iter-03 (SST) | `ENG/LS/sst_writer.go` `Finish()` lines 200-205; see code review iter-29 Bug 29 |
