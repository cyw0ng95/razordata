## TBD
| ID | Subsystem | Requirement | Priority | Effort | Deps | Touches |
| --- | --- | --- | --- | --- | --- | --- |
| REQ001843 | tests/sqlcmp/slt | **Pass random/select/slt_good_0 in pre-commit gate.** 2 failures / 9,905 records. Still has failures after REQ001979 fix. When resolved, add `{Label: "random::select::slt_good_0", Path: "random/select/slt_good_0", Timeout: 60}` to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001845 | tests/sqlcmp/slt | **Pass random/select/slt_good_2 in pre-commit gate.** 5 failures / 9,924 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001846 | tests/sqlcmp/slt | **Pass random/select/slt_good_3 in pre-commit gate.** 6 failures / 9,941 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001847 | tests/sqlcmp/slt | **Pass random/select/slt_good_4 in pre-commit gate.** 1 failure / 9,923 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001848 | tests/sqlcmp/slt | **Pass random/select/slt_good_5 in pre-commit gate.** 8 failures / 9,927 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001849 | tests/sqlcmp/slt | **Pass random/select/slt_good_6 in pre-commit gate.** 5 failures / 9,926 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001850 | tests/sqlcmp/slt | **Pass random/select/slt_good_7 in pre-commit gate.** 2 failures / 9,917 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001851 | tests/sqlcmp/slt | **Pass random/select/slt_good_8 in pre-commit gate.** 4 failures / 9,928 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001852 | tests/sqlcmp/slt | **Pass random/select/slt_good_9 in pre-commit gate.** 3 failures / 9,921 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001853 | tests/sqlcmp/slt | **Pass random/select/slt_good_10 in pre-commit gate.** 3 failures / 9,929 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001854 | tests/sqlcmp/slt | **Pass random/select/slt_good_11 in pre-commit gate.** 2 failures / 9,932 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001855 | tests/sqlcmp/slt | **Pass random/select/slt_good_12 in pre-commit gate.** 8 failures / 9,946 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001856 | tests/sqlcmp/slt | **Pass random/select/slt_good_13 in pre-commit gate.** 7 failures / 9,923 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001857 | tests/sqlcmp/slt | **Pass random/select/slt_good_14 in pre-commit gate.** 2 failures / 9,926 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001858 | tests/sqlcmp/slt | **Pass random/select/slt_good_15 in pre-commit gate.** 2 failures / 9,934 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001859 | tests/sqlcmp/slt | **Pass random/select/slt_good_16 in pre-commit gate.** 3 failures / 9,917 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001860 | tests/sqlcmp/slt | **Pass random/select/slt_good_17 in pre-commit gate.** 3 failures / 9,925 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001861 | tests/sqlcmp/slt | **Pass random/select/slt_good_18 in pre-commit gate.** 5 failures / 9,940 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001862 | tests/sqlcmp/slt | **Pass random/select/slt_good_19 in pre-commit gate.** 3 failures / 9,934 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001863 | tests/sqlcmp/slt | **Pass random/select/slt_good_20 in pre-commit gate.** 1 failure / 9,953 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001864 | tests/sqlcmp/slt | **Pass random/select/slt_good_21 in pre-commit gate.** 2 failures / 9,944 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001865 | tests/sqlcmp/slt | **Pass random/select/slt_good_22 in pre-commit gate.** 4 failures / 9,945 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001866 | tests/sqlcmp/slt | **Pass random/select/slt_good_23 in pre-commit gate.** 1 failure / 9,936 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001867 | tests/sqlcmp/slt | **Pass random/select/slt_good_24 in pre-commit gate.** 10 failures / 9,939 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001868 | tests/sqlcmp/slt | **Pass random/select/slt_good_25 in pre-commit gate.** 3 failures / 9,948 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001869 | tests/sqlcmp/slt | **Pass random/select/slt_good_26 in pre-commit gate.** 7 failures / 9,941 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001870 | tests/sqlcmp/slt | **Pass random/select/slt_good_27 in pre-commit gate.** 3 failures / 9,957 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001871 | tests/sqlcmp/slt | **Pass random/select/slt_good_28 in pre-commit gate.** 7 failures / 9,946 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001872 | tests/sqlcmp/slt | **Pass random/select/slt_good_29 in pre-commit gate.** 4 failures / 9,941 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001873 | tests/sqlcmp/slt | **Pass random/select/slt_good_30 in pre-commit gate.** 2 failures / 9,947 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001874 | tests/sqlcmp/slt | **Pass random/select/slt_good_31 in pre-commit gate.** 3 failures / 9,955 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001875 | tests/sqlcmp/slt | **Pass random/select/slt_good_32 in pre-commit gate.** 8 failures / 9,950 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001876 | tests/sqlcmp/slt | **Pass random/select/slt_good_33 in pre-commit gate.** 7 failures / 9,942 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001877 | tests/sqlcmp/slt | **Pass random/select/slt_good_34 in pre-commit gate.** 4 failures / 9,951 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001878 | tests/sqlcmp/slt | **Pass random/select/slt_good_35 in pre-commit gate.** 9 failures / 9,952 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001879 | tests/sqlcmp/slt | **Pass random/select/slt_good_36 in pre-commit gate.** 4 failures / 9,941 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001880 | tests/sqlcmp/slt | **Pass random/select/slt_good_37 in pre-commit gate.** 2 failures / 9,937 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001881 | tests/sqlcmp/slt | **Pass random/select/slt_good_38 in pre-commit gate.** 2 failures / 9,945 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001882 | tests/sqlcmp/slt | **Pass random/select/slt_good_39 in pre-commit gate.** 4 failures / 9,947 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001883 | tests/sqlcmp/slt | **Pass random/select/slt_good_40 in pre-commit gate.** 6 failures / 9,937 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001885 | tests/sqlcmp/slt | **Pass random/select/slt_good_42 in pre-commit gate.** 6 failures / 9,952 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001886 | tests/sqlcmp/slt | **Pass random/select/slt_good_43 in pre-commit gate.** 4 failures / 9,943 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001887 | tests/sqlcmp/slt | **Pass random/select/slt_good_44 in pre-commit gate.** 9 failures / 9,947 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001888 | tests/sqlcmp/slt | **Pass random/select/slt_good_45 in pre-commit gate.** 5 failures / 9,954 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001889 | tests/sqlcmp/slt | **Pass random/select/slt_good_46 in pre-commit gate.** 8 failures / 9,950 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001890 | tests/sqlcmp/slt | **Pass random/select/slt_good_47 in pre-commit gate.** 4 failures / 9,950 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001891 | tests/sqlcmp/slt | **Pass random/select/slt_good_48 in pre-commit gate.** 4 failures / 9,953 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001892 | tests/sqlcmp/slt | **Pass random/select/slt_good_49 in pre-commit gate.** 8 failures / 9,946 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001893 | tests/sqlcmp/slt | **Pass random/select/slt_good_50 in pre-commit gate.** 7 failures / 9,954 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001894 | tests/sqlcmp/slt | **Pass random/select/slt_good_51 in pre-commit gate.** 8 failures / 9,942 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001895 | tests/sqlcmp/slt | **Pass random/select/slt_good_52 in pre-commit gate.** 4 failures / 9,940 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001896 | tests/sqlcmp/slt | **Pass random/select/slt_good_53 in pre-commit gate.** 5 failures / 9,961 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001897 | tests/sqlcmp/slt | **Pass random/select/slt_good_54 in pre-commit gate.** 4 failures / 9,936 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001898 | tests/sqlcmp/slt | **Pass random/select/slt_good_55 in pre-commit gate.** 9 failures / 9,951 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001899 | tests/sqlcmp/slt | **Pass random/select/slt_good_56 in pre-commit gate.** 1 failure / 9,959 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001900 | tests/sqlcmp/slt | **Pass random/select/slt_good_57 in pre-commit gate.** 2 failures / 9,951 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001901 | tests/sqlcmp/slt | **Pass random/select/slt_good_58 in pre-commit gate.** 5 failures / 9,947 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001902 | tests/sqlcmp/slt | **Pass random/select/slt_good_59 in pre-commit gate.** 4 failures / 9,958 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001903 | tests/sqlcmp/slt | **Pass random/select/slt_good_60 in pre-commit gate.** 6 failures / 9,944 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001904 | tests/sqlcmp/slt | **Pass random/select/slt_good_61 in pre-commit gate.** 4 failures / 9,945 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001905 | tests/sqlcmp/slt | **Pass random/select/slt_good_62 in pre-commit gate.** 5 failures / 9,953 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001906 | tests/sqlcmp/slt | **Pass random/select/slt_good_63 in pre-commit gate.** 3 failures / 9,961 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001907 | tests/sqlcmp/slt | **Pass random/select/slt_good_64 in pre-commit gate.** 7 failures / 9,951 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001908 | tests/sqlcmp/slt | **Pass random/select/slt_good_65 in pre-commit gate.** 3 failures / 9,957 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001909 | tests/sqlcmp/slt | **Pass random/select/slt_good_66 in pre-commit gate.** 3 failures / 9,958 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001910 | tests/sqlcmp/slt | **Pass random/select/slt_good_67 in pre-commit gate.** 5 failures / 9,945 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001911 | tests/sqlcmp/slt | **Pass random/select/slt_good_68 in pre-commit gate.** 6 failures / 9,949 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001912 | tests/sqlcmp/slt | **Pass random/select/slt_good_69 in pre-commit gate.** 10 failures / 9,956 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001913 | tests/sqlcmp/slt | **Pass random/select/slt_good_70 in pre-commit gate.** 5 failures / 9,943 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001914 | tests/sqlcmp/slt | **Pass random/select/slt_good_71 in pre-commit gate.** 8 failures / 9,952 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001915 | tests/sqlcmp/slt | **Pass random/select/slt_good_72 in pre-commit gate.** 9 failures / 9,944 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001916 | tests/sqlcmp/slt | **Pass random/select/slt_good_73 in pre-commit gate.** 7 failures / 9,954 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001917 | tests/sqlcmp/slt | **Pass random/select/slt_good_74 in pre-commit gate.** 7 failures / 9,946 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001918 | tests/sqlcmp/slt | **Pass random/select/slt_good_75 in pre-commit gate.** 8 failures / 9,949 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001919 | tests/sqlcmp/slt | **Pass random/select/slt_good_76 in pre-commit gate.** 7 failures / 9,950 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001920 | tests/sqlcmp/slt | **Pass random/select/slt_good_77 in pre-commit gate.** 3 failures / 9,958 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001921 | tests/sqlcmp/slt | **Pass random/select/slt_good_78 in pre-commit gate.** 4 failures / 9,942 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001922 | tests/sqlcmp/slt | **Pass random/select/slt_good_79 in pre-commit gate.** 3 failures / 9,942 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001923 | tests/sqlcmp/slt | **Pass random/select/slt_good_80 in pre-commit gate.** 7 failures / 9,961 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001924 | tests/sqlcmp/slt | **Pass random/select/slt_good_81 in pre-commit gate.** 3 failures / 9,954 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001925 | tests/sqlcmp/slt | **Pass random/select/slt_good_82 in pre-commit gate.** 4 failures / 9,951 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001926 | tests/sqlcmp/slt | **Pass random/select/slt_good_83 in pre-commit gate.** 4 failures / 9,962 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001927 | tests/sqlcmp/slt | **Pass random/select/slt_good_84 in pre-commit gate.** 4 failures / 9,953 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001928 | tests/sqlcmp/slt | **Pass random/select/slt_good_85 in pre-commit gate.** 9 failures / 9,948 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001929 | tests/sqlcmp/slt | **Pass random/select/slt_good_86 in pre-commit gate.** 4 failures / 9,950 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001930 | tests/sqlcmp/slt | **Pass random/select/slt_good_87 in pre-commit gate.** 7 failures / 9,946 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001931 | tests/sqlcmp/slt | **Pass random/select/slt_good_88 in pre-commit gate.** 6 failures / 9,963 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001932 | tests/sqlcmp/slt | **Pass random/select/slt_good_89 in pre-commit gate.** 4 failures / 9,973 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001933 | tests/sqlcmp/slt | **Pass random/select/slt_good_90 in pre-commit gate.** 2 failures / 9,948 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001934 | tests/sqlcmp/slt | **Pass random/select/slt_good_91 in pre-commit gate.** 5 failures / 9,957 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001935 | tests/sqlcmp/slt | **Pass random/select/slt_good_92 in pre-commit gate.** 6 failures / 9,954 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001936 | tests/sqlcmp/slt | **Pass random/select/slt_good_93 in pre-commit gate.** 4 failures / 9,960 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001937 | tests/sqlcmp/slt | **Pass random/select/slt_good_94 in pre-commit gate.** 7 failures / 9,949 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001938 | tests/sqlcmp/slt | **Pass random/select/slt_good_95 in pre-commit gate.** 5 failures / 9,968 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001939 | tests/sqlcmp/slt | **Pass random/select/slt_good_96 in pre-commit gate.** 8 failures / 9,967 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001940 | tests/sqlcmp/slt | **Pass random/select/slt_good_97 in pre-commit gate.** 2 failures / 9,947 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001941 | tests/sqlcmp/slt | **Pass random/select/slt_good_98 in pre-commit gate.** 7 failures / 9,971 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001942 | tests/sqlcmp/slt | **Pass random/select/slt_good_99 in pre-commit gate.** 6 failures / 9,968 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001943 | tests/sqlcmp/slt | **Pass random/select/slt_good_100 in pre-commit gate.** 5 failures / 9,932 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001944 | tests/sqlcmp/slt | **Pass random/select/slt_good_101 in pre-commit gate.** 4 failures / 9,940 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001945 | tests/sqlcmp/slt | **Pass random/select/slt_good_102 in pre-commit gate.** 4 failures / 9,954 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001946 | tests/sqlcmp/slt | **Pass random/select/slt_good_103 in pre-commit gate.** 9 failures / 9,961 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001947 | tests/sqlcmp/slt | **Pass random/select/slt_good_104 in pre-commit gate.** 7 failures / 9,949 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001948 | tests/sqlcmp/slt | **Pass random/select/slt_good_105 in pre-commit gate.** 7 failures / 9,953 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001949 | tests/sqlcmp/slt | **Pass random/select/slt_good_106 in pre-commit gate.** 7 failures / 9,959 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001950 | tests/sqlcmp/slt | **Pass random/select/slt_good_107 in pre-commit gate.** 3 failures / 9,951 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001951 | tests/sqlcmp/slt | **Pass random/select/slt_good_108 in pre-commit gate.** 6 failures / 9,968 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001952 | tests/sqlcmp/slt | **Pass random/select/slt_good_109 in pre-commit gate.** 7 failures / 9,960 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001953 | tests/sqlcmp/slt | **Pass random/select/slt_good_110 in pre-commit gate.** 5 failures / 9,940 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001954 | tests/sqlcmp/slt | **Pass random/select/slt_good_111 in pre-commit gate.** 5 failures / 9,952 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001955 | tests/sqlcmp/slt | **Pass random/select/slt_good_112 in pre-commit gate.** 6 failures / 9,960 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001956 | tests/sqlcmp/slt | **Pass random/select/slt_good_113 in pre-commit gate.** 9 failures / 9,951 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001957 | tests/sqlcmp/slt | **Pass random/select/slt_good_114 in pre-commit gate.** 13 failures / 9,954 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001958 | tests/sqlcmp/slt | **Pass random/select/slt_good_115 in pre-commit gate.** 12 failures / 9,951 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001959 | tests/sqlcmp/slt | **Pass random/select/slt_good_116 in pre-commit gate.** 4 failures / 9,950 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001960 | tests/sqlcmp/slt | **Pass random/select/slt_good_117 in pre-commit gate.** 7 failures / 9,939 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001961 | tests/sqlcmp/slt | **Pass random/select/slt_good_118 in pre-commit gate.** 9 failures / 9,951 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001962 | tests/sqlcmp/slt | **Pass random/select/slt_good_119 in pre-commit gate.** 8 failures / 9,964 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001963 | tests/sqlcmp/slt | **Pass random/select/slt_good_120 in pre-commit gate.** 4 failures / 9,965 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001964 | tests/sqlcmp/slt | **Pass random/select/slt_good_121 in pre-commit gate.** 5 failures / 9,968 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001965 | tests/sqlcmp/slt | **Pass random/select/slt_good_122 in pre-commit gate.** 9 failures / 9,949 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001966 | tests/sqlcmp/slt | **Pass random/select/slt_good_123 in pre-commit gate.** 6 failures / 9,965 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001967 | tests/sqlcmp/slt | **Pass random/groupby/slt_good_8 in pre-commit gate.** 3 failures / 10,012 records. Still has failures after REQ001979 fix. When resolved, add `{Label: "random::groupby::slt_good_8", Path: "random/groupby/slt_good_8", Timeout: 60}` to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001968 | tests/sqlcmp/slt | **Pass random/groupby/slt_good_9 in pre-commit gate.** 6 failures / 10,012 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001969 | tests/sqlcmp/slt | **Pass random/groupby/slt_good_10 in pre-commit gate.** 7 failures / 10,012 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001970 | tests/sqlcmp/slt | **Pass random/groupby/slt_good_11 in pre-commit gate.** 26 failures / 10,012 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001971 | tests/sqlcmp/slt | **Pass random/groupby/slt_good_12 in pre-commit gate.** 812 failures / 10,012 records. Still has failures after REQ001979 fix. When resolved, add to `includedCases`. | high | medium | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001974 | SQB/EX | **Pool statement-cache LRU elements — eliminate 1.29M `list.Element` allocations.** Replace `container/list` with slice-indexed LRU or `sync.Pool` wrappers. Verify with `slt_good_109..119` `-alloc_objects`. | medium | medium | REQ001693 | SQB/EX/ex.go |
| REQ001998 | ENG/LS | **Columnar memtable — store recent writes in columnar format for fast vectorized reads.** Add `columnarMemtable` in front of skiplist; flush to SST at threshold. ~2x throughput for read-after-write. | high | large | REQ001995 | ENG/LS/skiplist.go, ENG/LS/memtable.go, ENG/LS/engine.go |
| REQ001999 | SQB/OP | **Grace hash join — spill-to-disk for large build sides.** Estimate build-side size; partition and spill to temp files when exceeding memory threshold. Verify with 1M×1K join. | high | large | REQ001981 | SQB/OP/op_vec_join.go, SQB/OP/hashjoin.go |
| REQ002004 | tests/sqlcmp/slt | **Pass remaining random/select/slt_good_* in pre-commit gate.** 122 files still have failures (1-10 per file) after REQ001979 fix. 5 files (slt_good_1, 41, 124, 125, 126) now pass and are in `includedCases`. When resolved, add to `includedCases`. | high | large | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ002005 | tests/sqlcmp/slt | **Pass remaining random/groupby/slt_good_* in pre-commit gate.** 5 files still have failures (3-812 per file) after REQ001979 fix. 9 files (slt_good_0-7, 13) now pass and are in `includedCases`. When resolved, add to `includedCases`. | high | medium | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ002008 | SQF/PS | **Arena-allocate parser Expr-nodes + mutate-in-place constant folding.** Split from REQ001976 (which shipped Parser pooling). 16.5M alloc objects / ~20% CPU per profile. Arena per-parse, reset on `Parser.Close`. Verify with `slt_good_109..119` `-alloc_objects`. | medium | large | REQ001976 | SQF/PS/expr.go, SQF/PS/ps.go, SQF/RE/rewrite.go |
| REQ002011 | SQB/DT | **Return old slab before allocating new one in `RowArena.Init`/`growLocked`.** `Init` replaces `a.slab` without calling `putSlab(a.slab)` on the old value. `growLocked` has the same bug. Old slabs are silently dropped for GC (not tracked since REQ001639 removed `a.slabs`). This leaks ~200 MB per SLT file. Fix: call `putSlab(a.slab)` before reassigning in both `Init` and `growLocked`. Add test `TestRowArena_InitReleasesOldSlab` verifying pool returns. | medium | small |  | SQB/DT/arena.go |
| REQ002012 | SQB/EX | **Eliminate per-row `make+copy` in `drainBatchProducer`.** `drain_batch.go:79` does `make([]Value, len(row.Data))` + `copy()` for every result row because the shared batch buffer is reused. For 10K-row results, that's 10K allocations (5% of alloc bytes, 220 MB). Fix: use `RowArena.AllocRow(len(row.Data))` (bump allocation, zero per-row heap cost) instead of `make+copy`. The arena is already available via `execCtx.RowArena`. Verify with `-alloc_objects` showing `drainBatchProducer` allocations near zero. | medium | medium | REQ002009 | SQB/EX/drain_batch.go |
| REQ002013 | tests/sqlcmp/slt | **Pool `[]Value` conversion in `RazorDriver.QueryRaw`.** `razor_driver.go:287` allocates `make([]Value, len(row.Data))` per result row (6% of alloc objects, 181K objects per file). Fix: pre-allocate a flat `[]Value` buffer sized to `len(rows) × maxCols` and carve sub-slices, or use a `sync.Pool` for result row buffers. Verify with `-alloc_objects` profile. | low | small |  | tests/sqlcmp/slt/razor_driver.go |
| REQ002017 | SQB/OP | **Pool `NestedLoopJoin` operator — eliminate 28 MB allocs.** `NewNestedLoopJoin` allocates the operator struct + `leftBuf` slice on every query with a join. No `sync.Pool` exists. For queries joining 3+ tables, this repeats per join. Fix: pool `NestedLoopJoin` structs, reset fields on reuse (`leftBuf = leftBuf[:0]`), and return to pool in `Close`. Verify with `-alloc_space` showing `NewNestedLoopJoin` flat ≈ 0. | high | medium |  | SQB/OP/intermediate_basic.go |
| REQ002018 | SQB/OP | **Eliminate `Project.Close` copy-to-pool allocs — 29K objects.** `Project.Close` (lines 1069-1098) creates `make([]string, 0, cap)`, `make([]any, 0, cap)`, `make([]func, 0, cap)` just to copy slices before pooling them. This defeats pooling — each Close allocates fresh backing arrays. Fix: set `len` to 0 on the existing slice (`p.prefixCols = p.prefixCols[:0]`) and pool the pointer directly, or reset in-place and skip pooling if the caller holds no reference. Verify with `-alloc_objects` showing `Project.Close` flat ≈ 0. | medium | small |  | SQB/OP/intermediate_basic.go |
| REQ002019 | SQB/OP | **Pool `SeqScan.WithAlias` prefix slices — 34K objects, 1.9 MB.** `WithAlias` (line 316) allocates `make([]string, len(cols))` and `make(map[string]int, len(cols)*2)` on every call. For aggregate queries with GROUP BY, each table alias triggers this. Fix: pool `prefixedCols` slices and `prefixedColIndex` maps; reuse via reset (`m = m[:0]` for slices, `clear(m)` for maps). Verify with `-alloc_objects` showing `WithAlias` flat ≈ 0. | medium | small |  | SQB/OP/operators.go |
| REQ002020 | SQB/OP | **Pool `SeqScan.WithUsedCols` metadata — 36K objects, 4 MB.** `WithUsedCols` (lines 390-401) allocates `usedColSet` map, `pruneBufCols/Types/Data` slices, `pruneBufIndex` map, and `usedColIdx` slice per call. For aggregate queries, each scan triggers this. Fix: pool all allocated metadata; clear maps and reset slices to length 0 in `Close` or `Reset`. Verify with `-alloc_objects` showing `WithUsedCols` flat ≈ 0. | medium | medium |  | SQB/OP/operators.go |
| REQ002021 | SQB/AG | **Drain `Aggregate.materialize` directly via `RowArena` — avoid `batch.ToRows()` copy.** `materialize` (line 145) calls `batch.ToRows()` which allocates `[]Row` per batch, then `append(scalarRowBuf, rows...)` copies again. For scalar aggregates draining 10K rows, that's 11K objects / 15 MB. Fix: use `RowArena.AllocRow` to drain batch values directly into arena-backed rows, skipping the intermediate `[]Row` allocation. Verify with `-alloc_objects` showing `materialize` flat ≈ 0. | medium | medium |  | SQB/AG/aggregate.go |
| REQ002022 | SQB/OP | **Reduce `projectDataBufPool` miss rate — eliminate 32.7 MB allocs.** `OP.init.func3` pool `New` allocates `make([]Value, 0, projectDataBufChunkSize)` (512 values) on every pool miss. Same pattern as REQ002014 but for the Project data buffer pool. Fix: pre-warm on engine start, or increase pool population by reducing GC pressure (REQ002014's tiered pool would help both). Verify with `-alloc_space` showing `OP.init.func3` near zero. | medium | small | REQ002014 | SQB/OP/intermediate_basic.go |
| REQ002026 | SQF/PL | **Pool `Row.ColIndex` map — eliminate 155 MB allocs (4.2%).** `buildColIndex` (line 171) allocates `make(map[string]int, len(r.Cols))` per row. For groupby queries, each row conversion triggers this. Fix: pool `ColIndex` maps; clear and reuse via `clear(m)` instead of re-allocating. Verify with `-alloc_space` showing `buildColIndex` flat ≈ 0. | medium | small |  | SQF/PL/types.go |
| REQ002027 | SQB/AG | **Pool `evalGroupKey` output slice — eliminate 3.8 MB flat + cascading eval allocs.** `evalGroupKey` (line 358) allocates `make([]Value, len(cols))` per group key evaluation. For groupby with 10K groups, that's 10K slices. The real cost is the `EvalValue` calls (2.44 GB cumulative) which cascade through `evalRowFallbackColumn` (REQ002025). Fix: pre-allocate group key buffer on the Aggregate struct and reuse across materialize calls. Verify with `-alloc_space` showing `evalGroupKey` flat ≈ 0. | medium | small |  | SQB/AG/aggregate.go |
| REQ002028 | ENG/LS | **Fix flaky `TestAutoCompact_Incremental_Triggered` — SST file not found during compaction.** Test fails intermittently with `open .../L0_61_7a_1.sst: no such file or directory` in compaction goroutine. Race between test's direct SST manipulation and the compaction goroutine — compaction job picks up a file that gets removed before it runs. Fix: serialize compaction job execution with file lifecycle, or add retry/missing-file handling in compaction path. Repro: run `go test ./internal/ENG/LS -run TestAutoCompact_Incremental_Triggered -count=100`. | high | small |  | ENG/LS/compaction_test.go, ENG/LS/compaction.go |
| REQ002029 | SQB/OP | **Reduce `nljBlockDataBufPool` miss rate — eliminate 110 MB allocs (8-11%).** `OP.init.func7` pool `New` allocates `make([]Value, 0, 4096)` on every miss (join.go:27). For select queries with joins, the NLJ block data buffer pool drains faster than GC refills it. Fix: pre-warm pool on engine start with common sizes, or reduce initial capacity to 1024 (typical batch) and grow on demand. Verify with `-alloc_space` showing `OP.init.func7` near zero. | high | small |  | SQB/OP/join.go |
| REQ002030 | SQB/EX | **Vectorize aggregates with unary expression arguments — ~1037 queries/file fall back to row-based.** `resolveAggDef` (vec_transform.go:480) returns `false` for any aggregate argument that isn't a bare `*PS.Ident`. This causes `transformAggregate` to return nil, falling back to row-based `Aggregate.materialize` even when the child is vectorized. In slt_good_0, ~1037 of 4807 aggregate queries use unary exprs (`SUM(- col0)`, `MIN(+ col0)`, `COUNT(+ + col1)`). Fix: unwrap `*PS.UnaryExpr` in `resolveAggDef` — ignore `+`, pass `-` as a negate flag on `AggDef`. Also handle `*PS.CastExpr` for constant-foldable casts (e.g., `CAST(NULL AS SIGNED)`). Verify: `Aggregate.materialize` flat allocs ≈ 0 in aggregates profile; batch path used instead of row drain. | high | medium |  | SQB/EX/vec_transform.go, SQB/AG/aggregate.go |
| REQ002031 | SQB/EV | **Extend `EvalBatch` predicate eval for IsNotNull, IsNull, Between, Like — eliminate filter fallback.** `EvalBatch` (eval_vec.go:92) only handles `BinaryExpr`, `UnaryExpr`, `InExpr`. Any other predicate (e.g., `col IS NOT NULL`, `col BETWEEN a AND b`, `col LIKE '%x%'`) hits `evalRowFallback` which converts batch→row→eval per row. In groupby profiles, `evalRowFallbackColumn` costs 426 MB (7.1%). Fix: add batch-native cases for `IsNullExpr`/`IsNotNullExpr` (null bitmap check), `BetweenExpr` (two-column compare), and `LikeExpr` (regex per batch). Verify: `evalRowFallback` hits ≈ 0 in groupby profile. | high | medium |  | SQB/EV/eval_vec.go |
| REQ002032 | SQB/EX | **Make `/` integer-dividing when both INTEGER operands resolve to integers — fix slt_good_0 fail[0].** Probe `SELECT col1 / col0 FROM tab2 AS cor0 LIMIT 1` returns `4` (7/2=3.5 → real) but SQLite returns `3` (integer division). The `/` operator on INTEGER columns should follow SQLite affinity: if both operands are exact integers (including after unary folding of `+`/`-`), compute integer quotient; fall back to real only when at least one operand is REAL/storage-typed REAL/null. Affected failing line in `tests/sqlcmp/corpus/test/random/select/slt_good_0.test:46640`: `SELECT cor2.col1 / + cor1.col0 FROM tab1, tab2 AS cor0, tab2 AS cor1, tab0 cor2` — hash got `17900951c7f97aac721fa4da52ceae2a`, want `7233f87dfb156c8a83ad6688e1c36b52`. Fix in expression evaluator: when `T_DIV` sees both operands yielding `ValueInteger`, emit `ValueInteger` quotient; emit `ValueReal` otherwise. Verify by re-running `tests/sqlcmp/slt/probe_req1843_test.go#TestREQ1843ProbeCurrent` (slt_good_0 fail count drops to 1) and adding a focused test `internal/SQB/EX/int_division_test.go` asserting `7/2 == 3`, `7/-2 == -3`, `7/2.0 == 3.5`, `7/NULL == NULL`. Touches REQ001843 (slt_good_0 must move to 1 failure). | high | small | REQ001843 | SQB/EX/eval_expr.go (or operator dispatch), tests/sqlcmp/slt/probe_req1843_test.go, internal/SQB/EX/int_division_test.go |
| REQ002033 | SQB/EV | **Align multi-table JOIN SELECT result hash with SQLite — fix slt_good_2/3/4/5 + slt_good_0 fail[1].** Probe across `random/select/slt_good_{2,3,4,5}.test` shows the first failing record in each is a plain SELECT over a 4-table JOIN (e.g. `SELECT cor0.col2 AS col1 FROM tab1, tab0, tab0 AS cor0, tab1 AS cor1`). These contain no `/`, no nested unary chains, and no obvious expression-tree difference — yet the row/column output hash differs from SQLite (`row 0: got [54], want []` observed). slt_good_0 fail[1] (`- - cor0.col0 + + 69` over 4-table JOIN) is likely the same root cause manifesting through a unary expression. Fix: investigate NLJ/HJ row emission order, null-bit handling, and final `resultSetToValue` cell conversion in the join path; align either via per-column NULL handling or via deterministic row ordering that matches SQLite's rowsort phase. Verify by re-running probe: `slt_good_2 fail ≤ 2`, `slt_good_3 fail ≤ 4`, `slt_good_4 fail == 0`, `slt_good_5 fail ≤ 5`, `slt_good_0 fail == 0` (combined with REQ002032). Touches REQ001843, REQ001845, REQ001846, REQ001847, REQ001848. | high | medium | REQ001843, REQ001845, REQ001846, REQ001847, REQ001848 | SQB/OP/join.go, SQB/EV/eval.go, SQB/EV/resultset.go (or wherever cell materialisation lives), tests/sqlcmp/slt/probe_req1843_test.go |
| REQ002034 | TXN/LC | **Replace `runtime.Stack()` in `GoID()` with fast goroutine ID — eliminate 120ms per test.** `goid.go:13` calls `runtime.Stack(buf[:], false)` on every goroutine entry/exit in QSBR. This takes 120ms of 420ms (29%) in TXN/LC tests — the entire test time is `traceback` + `runtime.Stack`. Fix: use `go:linkname` to read `g.goid` directly from the runtime `g` struct (common in Go concurrency libs), or cache goroutine IDs in a `sync.Pool`-backed TLS slot. The `runtime.Stack` approach allocates and parses a stack trace string — fundamentally slow. Verify: TXN/LC test time drops from 420ms to <50ms; `runtime.Stack` no longer appears in CPU profile. | high | small |  | TXN/LC/goid.go, TXN/LC/epoch.go |
| REQ002035 | SQB/EX | **Share engine instance across EX tests — eliminate 921 MB allocs + GC.** Each test in SQB/EX creates a fresh engine with full init: `NewPageCache` (134 MB), `WarmFilterBatchPool` (116 MB), `flate.NewWriter` (204 MB). This causes 83% CPU in GC (`gcDrain`). Fix: use `TestMain` or `sync.Once` to create one engine per package; reset between tests via `driver.Reset()` instead of `Close()`+`Connect()`. Similar to REQ001454's SLT optimization. Verify: SQB/EX test time drops from 618ms to <200ms; `NewPageCache`/`WarmFilterBatchPool` no longer in per-test alloc profiles. | high | medium |  | SQB/EX/*_test.go |
| REQ002036 | SYS/SY | **Share engine instance across SY tests — eliminate 345 MB allocs + GC.** Same pattern as REQ002035: each SYS/SY test creates a fresh engine. 68% CPU in GC. Fix: shared engine with reset between tests. Verify: SYS/SY test time drops from 512ms to <150ms. | high | medium |  | SYS/SY/*_test.go |
