## TBD
| ID | Subsystem | Requirement | Priority | Effort | Deps | Touches |
| --- | --- | --- | --- | --- | --- | --- |
| REQ000987 | EX/store | **No `TableHandle` abstraction — raw Store used everywhere with repeated schema+key+encode pattern.** Every caller (`writers.go`, `operators.go`, `integrity.go`) independently: (1) looks up the schema via `schemaFor`, (2) builds the key prefix, (3) calls `encodeRow`, (4) calls `store.Put/Get/Delete`. This three-step pattern is duplicated across 10+ call sites. **Fix:** (1) Introduce a `TableHandle` struct that bundles `{store Store, schema *storeSchema, prefix []byte}` with methods `InsertRow(Row)`, `GetRow(key)`, `Scan(prefix) Iterator`. (2) Replace the repeated pattern at all call sites. (3) Add `TestTableHandle_InsertGetScan` verifying the abstraction. | medium | medium | none | EX/store.go, EX/writers.go, EX/operators.go |
| REQ001008 | LS | **No range tombstone support — `DELETE WHERE col > 100` must delete each row individually.** `LS/api.go:12-16`: deletion uses a magic tombstone value (`0xDEADBEEF`). Range deletions must delete each row individually, which is O(N) for the matching rows. A range-tombstone marker can suppress a key range during SST reads in O(1). **Fix:** (1) Add a range-tombstone record type to SST blocks. (2) Update `sstReader.Find` to check range tombstones before scanning. (3) Add `TestRangeTombstone_SuppressesRange` verifying the optimization. | medium | medium | none | LS/api.go, LS/sst_reader.go |
| REQ001157 | ENG/LS | **SubCompactor parallel path is wired but disabled — manifest-write race prevents activation.** Discovered while implementing REQ001048: `compactionJob.Run` writes both the merged SST and a manifest rename (`<dir>/manifest.tmp → <dir>/manifest`) in a single function. When multiple sub-jobs execute concurrently under SubCompactor, they all race on the shared manifest. The dispatcher (`compactionManager.runJob`) currently sets `subCompactionThreshold = 1<<30` (effectively disabled) so the serial path is used everywhere. **Fix:** (1) Split `compactionJob.Run` into two phases: per-sub partial SST write (no manifest touch) and a coordinator that merges all partial outputs in serial + applies the new manifest. (2) Add a per-sub `tmpPath` so partial SSTs don't collide on `compaction.tmp`. (3) Update SubCompactor.RunSubCompaction to return per-sub SST paths instead of running compactionJob.Run directly. (4) Lower `subCompactionThreshold` back to 4 once the partial-output path lands. (5) Add `TestSubCompactor_Parallel_NoManifestRace` running ≥5 inputs through SubCompactor and verifying the manifest is consistent after the run. | high | large | REQ001048 | ENG/LS/compaction.go, ENG/LS/subcompact.go |
| REQ001073 | EX/plan | **No semi-join optimization — `EXISTS (SELECT ...)` materializes full subquery.** `planner.go:2344-2400`: `EXISTS (SELECT 1 FROM t WHERE t.id = outer.id)` materializes the entire subquery result. Semi-join can stop after finding the first match. **Fix:** (1) Detect `EXISTS` subqueries and convert to semi-join. (2) Semi-join stops scanning the right side after finding the first match. (3) Add `TestPlanner_SemiJoin` verifying `EXISTS` uses semi-join instead of materialization. | medium | large | none | EX/planner.go |
| REQ001091 | EX/project | **Project `dataBuf` is per-Operator, not pooled — 3857 allocs per select4 run.** `intermediate.go:368` (`Project.Next`): every query allocates a new `dataBuf = make([]Value, 0, 64*dataPerRow)`. For 3857 select4 queries this is 3857 buffer allocations. **Fix:** (1) Add a `sync.Pool` of `*[]Value` slices for Project dataBuf. (2) Get from pool in `Project.Next`, return in `Project.Close()`. (3) Tune pool chunk size to 64*8=512 values (matches common 8-column output). | medium | small | none | EX/intermediate.go |
| REQ001106 | EX/plan | **No bitmap heap scan — multiple index conditions cannot be combined efficiently.** PostgreSQL creates a bitmap from multiple index scans and scans the heap once. Razor's index selection code only considers one index at a time. For `WHERE a = 1 OR b = 2`, PostgreSQL combines two index scans into a bitmap; Razor falls back to seq scan or NLJ. **Fix:** (1) Add `BitmapHeapScan` operator. (2) Add bitmap data structure for combining multiple index scan results. (3) In planner, detect when multiple index conditions can be combined (OR, AND with different columns). (4) Add `TestBitmapHeapScan_OrConditions`, `TestBitmapHeapScan_AndConditions` verifying correctness. | medium | large | REQ001104 | EX/planner.go, EX/operators.go |
| REQ001107 | EX/plan | **No index-only scan — covering queries still fetch from heap.** PostgreSQL can satisfy queries entirely from the index when all needed columns are in the index. Razor's `IndexScan` always fetches from the heap. For `SELECT a FROM t WHERE a = 1` with an index on `a`, PostgreSQL reads only the index; Razor reads index + heap. **Fix:** (1) Add `IndexOnlyScan` operator. (2) Add visibility map for index-only scan (to avoid heap fetches). (3) In planner, detect covering indexes (all projected columns are in the index). (4) Add `TestIndexOnlyScan_CoveringQuery`, `BenchmarkIndexOnlyScan_VsHeapScan` verifying correctness and performance. | medium | large | REQ001104 | EX/planner.go, EX/operators.go |
| REQ001108 | EX/plan | **No index condition pushdown — indexable conditions not pushed into index scan.** PostgreSQL pushes indexable conditions into the index scan to reduce heap fetches. Razor's `IndexScan` doesn't support condition pushdown; all filtering is done by `Filter` operators above the scan. For `WHERE a = 1 AND b > 10` with an index on `a`, PostgreSQL pushes `a = 1` into the index scan; Razor scans all rows with `a = 1` and filters `b > 10` after heap fetch. **Fix:** (1) Add condition pushdown logic in `IndexScan`. (2) In planner, extract indexable conditions and pass them to `IndexScan`. (3) Add `TestIndexConditionPushdown_Basic`, `BenchmarkIndexConditionPushdown_Performance` verifying correctness and performance. | low | medium | REQ001107 | EX/planner.go, EX/operators.go |
| REQ001109 | EX/plan | **No GEQO for large joins — single-start N3 produces suboptimal plans for >8 tables.** PostgreSQL uses GEQO (genetic algorithm) for joins with >12 tables. Razor switches to single-start N3 for >8 tables. For very large joins, N3 may produce suboptimal plans. **Fix:** (1) Implement GEQO with population-based search. (2) Integrate with cost model. (3) Add planning time management. (4) Add `TestGEQO_LargeJoin` verifying GEQO produces better plans than N3 for 15+ table joins. | low | large | REQ001103, REQ001104 | EX/planner.go |
| REQ001113 | EX/join | **Multi-table cross-join (implicit comma-join) with equality predicates produces incorrect cardinality — 330/3857 select4 records fail.** `select4.test`: 351 of 3857 records fail; ~330 are join cardinality errors. Failures include both under-count (got 1, want 3/4/5) and over-count (got 1152 cells, want 576; got 56 cells, want 28). Different table orderings for the same query produce different wrong results: e.g., L29268 (got 0 cells, want 576) vs L29292/L29304 (got 1152 cells, want 576). Even simple 2-table joins with equality-to-constant predicates fail: `SELECT x1, d9 FROM t1, t9 WHERE 281=a1 AND c9=232` yields 1 row instead of 3; `SELECT x9, e8 FROM t9, t8 WHERE e8=893 AND b9=12` yields 1 row instead of 3. Both under-count and over-count present, ruling out a simple over-filtering bug. Different join orders produce different (wrong) cardinalities, indicating the join execution (HashJoin/NLJ) or planner predicate distribution is order-dependent. **Progress:** root cause (4) `groupBushyJoins` mis-split (REQ001156) fixed in `e00d132`; remaining: (1) `splitPredicatesByTable` misroutes predicates for implicit comma-joins, (2) `eliminateCommonSubexpressions` may dedup simple equality predicates, (3) HashJoin/NLJ order-dependent bug. **Fix:** (1) Add a test running 20+ representative failing select4 queries with all table order permutations, verifying ALL permutations produce identical correct results. (2) Debug `splitPredicatesByTable` for implicit comma-joins — verify every predicate is assigned to exactly one table. (3) Debug `eliminateCommonSubexpressions` — verify simple equalities (`a=1`, `b=2`) are not dedup'd when they reference different tables. (4) Debug `HashJoin.buildAndProbe` — add assertions that left/right row counts produce expected cardinality for known test cases. (5) Debug `NestedLoopJoin.Next` — add assertions that all join predicates are evaluated for each row pair. (6) Add `TestCrossJoin_Cardinality_AllPermutations` with 5 representative failing queries and all 120 permutations passing. | critical | high | REQ001092, REQ001093, REQ001111, REQ001156 | EX/join.go, EX/hashjoin.go, EX/planner.go, EX/nlj.go |
| REQ001117 | SQB/OP | **Partial: extract `SQB/OP` cluster — operators.** The `SQB/OP/` cluster exists (17 files) with `operators.go`, `join.go`, `hashjoin.go`, `hashcrossjoin.go`, `compound.go`, `intermediate.go`, `values.go`, `virtual.go`, `store.go`, `distinct.go`, `execctx.go`, `pragma.go`, `indexscan_strategy.go`, `operators_parallel.go`, `operators_vec.go` and tests. **Remaining:** `plan_node.go`, `source.go`, `shape_specialize.go` still in `SQB/EX` need moving to `SQB/OP`. **Fix:** (1) Move remaining files: `plan_node.go`, `source.go`, `shape_specialize.go` from `SQB/EX` to `SQB/OP`. (2) Update imports. (3) Verify `go build ./...` and `go test ./... -race -count=1` pass. | high | medium | REQ001116 | SQB/EX/plan_node.go, SQB/EX/source.go, SQB/EX/shape_specialize.go, SQB/OP/
| REQ001118 | SQB/EV | **Partial: extract `SQB/EV` cluster — evaluation.** The `SQB/EV/` cluster exists (13 files) with `eval.go`, `eval_vec.go`, `function_registry.go`, `ev_types.go`, and test files (`corefunc_test.go`, `corefunc_quick_test.go`, `coalesce_test.go`, `specialform_test.go`, `overflow_test.go`, `eval_extras_test.go`, `eval_vec_test.go`, `operators_test.go`, `simd_dispatch_test.go`). **Remaining:** `eval_test.go`, `eval_req772_test.go` still in `SQB/EX`. Files `eval_fixes_test.go`, `eval_scalar_funcs_test.go`, `null_req_test.go`, `neg_literal_test.go` no longer exist (removed or migrated). **Fix:** (1) Move remaining `eval_test.go`, `eval_req772_test.go` from `SQB/EX` to `SQB/EV`. (2) Update imports. (3) Verify `go build ./...` and `go test ./... -race -count=1` pass. | high | small | REQ001116 | SQB/EX/eval_test.go, SQB/EX/eval_req772_test.go, SQB/EV/
| REQ001119 | SQB/AG | **Partial: extract `SQB/AG` cluster — aggregation.** The `SQB/AG/` cluster exists (7 files) with `aggregate.go`, `aggregate_vec.go`, `hashagg.go`, `hashagg_parallel.go`, `window.go`, `aggregate_registry.go`, `ag_types.go`. **Remaining:** `aggregate_vec_test.go`, `hashagg_planner_test.go`, `aggregate_distinct_iter27_test.go`, `window_test.go` still in `SQB/EX`. **Fix:** (1) Move remaining test files from `SQB/EX` to `SQB/AG`. (2) Update imports. (3) Verify `go build ./...` and `go test ./... -race -count=1` pass. | high | small | REQ001116, REQ001118 | SQB/EX/aggregate_vec_test.go, SQB/EX/hashagg_planner_test.go, SQB/EX/aggregate_distinct_iter27_test.go, SQB/EX/window_test.go, SQB/AG/
| REQ001120 | SQB/AD | **Extract `SQB/AD` cluster — ADQC/planning: adqc*.go, planner.go, memo.go, selectivity.go, shape_specialize.go, cache_stats.go.** `SQB/EX` contains ADQC, planner, memo, and selectivity estimation. The `AD` cluster should contain: `adqc.go`, `adqc_cache.go`, `adqc_fallback.go`, `adqc_telemetry.go`, `adqc_test.go`, `planner.go`, `planner_iter22_test.go`, `planner_test.go`, `memo.go`, `selectivity_test.go`, `cache_stats.go`, `cache_stats_test.go`, `shape_specialize.go`, `cost_indexscan_test.go`, `cost_selection_iter27_test.go`, and their test files. **Fix:** (1) Create `internal/SQB/AD/` directory. (2) Move ADQC/planning files from `SQB/EX` to `SQB/AD`. (3) Update imports in moved files and remaining `SQB/EX` files. (4) `SQB/AD` imports `SQF/PL` for `Operator`/`Row` types and `SQB/EV` for eval functions. (5) `SQB/EX` imports `SQB/AD` for planner and ADQC. (6) Verify `go build ./...` succeeds and `go test ./... -race -count=1` passes. | high | large | REQ001116, REQ001118 | SQB/EX/adqc.go, SQB/EX/adqc_cache.go, SQB/EX/adqc_fallback.go, SQB/EX/adqc_telemetry.go, SQB/EX/adqc_test.go, SQB/EX/planner.go, SQB/EX/planner_iter22_test.go, SQB/EX/planner_test.go, SQB/EX/memo.go, SQB/EX/selectivity_test.go, SQB/EX/cache_stats.go, SQB/EX/cache_stats_test.go, SQB/EX/shape_specialize.go, SQB/EX/cost_indexscan_test.go, SQB/EX/cost_selection_iter27_test.go |
| REQ001121 | SQB/WT | **Extract `SQB/WT` cluster — write operators: writers.go, source.go, store.go, alter_table.go, fk.go, subq.go, view.go, constraints.go, matview.go.** `SQB/EX` contains all write operators and DDL executors. The `WT` cluster should contain: `writers.go`, `source.go`, `store.go`, `store_test.go`, `alter_table.go`, `alter_table_test.go`, `fk.go`, `subq.go`, `view.go`, `constraints.go`, `constraints_test.go`, `matview.go`, `matview_test.go`, `index_usage.go`, `index_usage_test.go`, `index_ddl_test.go`, `index_e2e_test.go`, `index_scan_iter22_test.go`, `index_scan_iter27_test.go`, `gencol_test.go`, and their test files. **Fix:** (1) Create `internal/SQB/WT/` directory. (2) Move write operator files from `SQB/EX` to `SQB/WT`. (3) Update imports in moved files and remaining `SQB/EX` files. (4) `SQB/WT` imports `SQF/PL` for `Operator`/`Row`/`Value` types, `SQB/EV` for eval functions, and `SQB/OP` for operator types. (5) `SQB/EX` imports `SQB/WT` for write operators. (6) Verify `go build ./...` succeeds and `go test ./... -race -count=1` passes. | high | large | REQ001116, REQ001117, REQ001118 | SQB/EX/writers.go, SQB/EX/source.go, SQB/EX/store.go, SQB/EX/store_test.go, SQB/EX/alter_table.go, SQB/EX/alter_table_test.go, SQB/EX/fk.go, SQB/EX/subq.go, SQB/EX/view.go, SQB/EX/constraints.go, SQB/EX/constraints_test.go, SQB/EX/matview.go, SQB/EX/matview_test.go, SQB/EX/index_usage.go, SQB/EX/index_usage_test.go, SQB/EX/index_ddl_test.go, SQB/EX/index_e2e_test.go, SQB/EX/index_scan_iter22_test.go, SQB/EX/index_scan_iter27_test.go, SQB/EX/gencol_test.go |
| REQ001122 | SQB/UT | **Extract `SQB/UT` cluster — utilities: coerce.go, pragma.go, integrity.go, decimal.go, datetime.go, json.go, explain.go, analyze.go, sort_parallel.go, pipeline.go, simd_dispatch.go, parallel.go.** `SQB/EX` contains all utility functions and cross-cutting concerns. The `UT` cluster should contain: `coerce.go`, `coerce_test.go`, `pragma.go`, `pragma_fk_test.go`, `integrity.go`, `integrity_test.go`, `decimal.go`, `decimal_test.go`, `datetime.go`, `datetime_test.go`, `json.go`, `json_test.go`, `explain.go`, `explain_test.go`, `analyze.go`, `analyze_test.go`, `sort_parallel.go`, `sort_parallel_test.go`, `pipeline.go`, `pipeline_test.go`, `simd_dispatch.go`, `simd_dispatch_test.go`, `parallel.go`, `parallel_test.go`, `txn_debug.go`, `txn_debug_test.go`, `batch.go`, `batch_test.go`, and their test files. **Pattern:** For files with EX dependencies (e.g., `pragma.go` uses `Row`/`Value`/`ErrNoRows`), split the file — keep EX-dependent parts in EX, move UT parts to UT, create `EX -> UT` dependency chain. Pure UT files (no EX deps) move entirely. **Fix:** (1) Create `internal/SQB/UT/` directory. (2) Move pure UT files from `SQB/EX` to `SQB/UT`. (3) For files with EX deps, split: UT parts go to UT, EX-dependent parts stay in EX. (4) Update imports in moved files and remaining `SQB/EX` files. (5) `SQB/UT` imports `SQF/PL` for `Operator`/`Row`/`Value` types and `SQB/EV` for eval functions. (6) `SQB/EX` and other clusters import `SQB/UT` for utilities. (7) Verify `go build ./...` succeeds and `go test ./... -race -count=1` passes. **PoC done:** `simd_dispatch.go` (pure UT, moved entirely), `coerce.go` (pure UT, moved entirely), `decimal.go` (pure UT, moved entirely, `evalDecimalCast` exported to `EvalDecimalCast`), `json.go` (pure UT, moved entirely, `evalJSONFunc`/`isJSONFunc` exported), `parallel.go` (pure UT, moved entirely, EX files import UT for `WorkerPool`), `batch.go` (pure UT, moved entirely, EX files import UT for `Batch`/`Column`/`GetBatch`/`BatchSize`/etc.), `txn_debug.go` (pure UT, moved entirely, `TxnDebugInfo` moved from `plan_node.go` to UT), `pragma.go` (split: `PragmaListener`/`RegisterPragmaListener`/`UnregisterAllPragmaListeners`/`NotifyPragmaChange` → UT, `PrAGMAResult` stays in EX), `datetime.go` (pure UT, moved entirely, `evalDateTimeFunc`/`isDateTimeFunc`/`DateTimeArithmetic`/`IntervalValue`/`ParseInterval` exported). `simd_dispatch_test.go` split: `TestDetectFeatures_AnyArch` → UT, `TestEvalBatch_8Wide` → EX (EX-dependent). EX files updated: `eval.go`, `constraints.go`, `ex.go`, `planner.go`, `plan_node.go`, `operators_parallel.go`, `intermediate.go`, `sort_parallel.go`, `hashagg_parallel.go`, `operators_vec.go`, `pipeline.go`, `adqc.go`, `adqc_cache.go`, `adqc_fallback.go`, `aggregate_vec.go`, `eval_vec.go`, `join.go`, `operators.go`, `writers.go`, and test files. **Remaining:** `integrity.go`, `explain.go`, `analyze.go`, `sort_parallel.go`, `pipeline.go` all have EX dependencies and require splitting.hSize`/etc.), `txn_debug.go` (pure UT, moved entirely, `TxnDebugInfo` moved from `plan_node.go` to UT). `simd_dispatch_test.go` split: `TestDetectFeatures_AnyArch` → UT, `TestEvalBatch_8Wide` → EX (EX-dependent). EX files updated: `eval.go`, `constraints.go`, `ex.go`, `planner.go`, `plan_node.go`, `operators_parallel.go`, `intermediate.go`, `sort_parallel.go`, `hashagg_parallel.go`, `operators_vec.go`, `pipeline.go`, `adqc.go`, `adqc_cache.go`, `adqc_fallback.go`, `aggregate_vec.go`, `eval_vec.go`, `join.go`, `operators.go`, and test files. **Remaining:** `datetime.go`, `integrity.go`, `explain.go`, `analyze.go`, `sort_parallel.go`, `pipeline.go`, `pragma.go` all have EX dependencies and require splitting. | high | large | REQ001116, REQ001118 | SQB/EX/coerce.go, SQB/EX/coerce_test.go, SQB/EX/pragma.go, SQB/EX/pragma_fk_test.go, SQB/EX/integrity.go, SQB/EX/integrity_test.go, SQB/EX/decimal.go, SQB/EX/decimal_test.go, SQB/EX/datetime.go, SQB/EX/datetime_test.go, SQB/EX/json.go, SQB/EX/json_test.go, SQB/EX/explain.go, SQB/EX/explain_test.go, SQB/EX/analyze.go, SQB/EX/analyze_test.go, SQB/EX/sort_parallel.go, SQB/EX/sort_parallel_test.go, SQB/EX/pipeline.go, SQB/EX/pipeline_test.go, SQB/EX/simd_dispatch.go, SQB/EX/simd_dispatch_test.go, SQB/EX/parallel.go, SQB/EX/parallel_test.go, SQB/EX/txn_debug.go, SQB/EX/txn_debug_test.go, SQB/EX/batch.go, SQB/EX/batch_test.go |
| REQ001123 | SQB/EX | **Finalize `SQB/EX` as core types only — Operator/Row/Value/ExecContext, executor factory, and glue.** After extracting OP, EV, AG, AD, WT, and UT clusters, `SQB/EX` should contain only: `ex.go` (Executor: `Exec()`, `Query()`), `execctx.go` (ExecContext), and test files that belong to EX-level integration (e.g., `executor_test.go`, `e2e_test.go`, `testutil_test.go`, `coverage_test.go`, `catalog_integration_test.go`, `memstore_test.go`). All other files should have been moved to their respective clusters. **Fix:** (1) After all 6 cluster extractions, verify `SQB/EX` contains only the core files listed above. (2) `SQB/EX` imports `SQF/PL` for `Operator`/`Row`/`Value`/`ExecContext` types and all 6 child clusters. (3) Run `go build ./...` and `go test ./... -race -count=1` — all tests must pass. (4) Run `go vet ./...` — zero warnings. (5) Run `gofmt -s -l .` — no drift. (6) Verify file count in `SQB/EX` is ≤50 (down from 195). | high | large | REQ001116, REQ001117, REQ001118, REQ001119, REQ001120, REQ001121, REQ001122 | SQB/EX/ex.go, SQB/EX/execctx.go, all remaining SQB/EX files |
| REQ001125 | EX/eval | **`CHANGES()` and `TOTAL_CHANGES()` return wrong results in dual tests.** `tests/sqlcmp/dual`: `changes_function` and `total_changes_function` cases fail with result mismatch. These functions should return the number of rows modified by the most recent INSERT/UPDATE/DELETE. The dual test framework seeds data and checks aggregate results. Root cause likely: (1) the change counter is not incremented for in-memory table writes, or (2) the counter resets between statements. **Fix:** (1) Debug `CHANGES()` and `TOTAL_CHANGES()` evaluation in `eval.go` — verify they read the correct counter. (2) Verify the counter is incremented for all write paths (in-memory and LSM). (3) Add `TestChangesFunction_InMemoryWrite` verifying correct count after INSERT/UPDATE/DELETE. | medium | small | none | EX/eval.go, EX/writers.go |
| REQ001126 | EX/eval | **Correlated subquery EXISTS/NOT EXISTS returns wrong results in dual tests.** `tests/sqlcmp/dual`: `correlated_subquery_exists` and `correlated_subquery_not_exists` cases fail. These test `EXISTS (SELECT ... WHERE outer.x = inner.y)` patterns. Likely related to REQ001098 (correlated subquery comparison broken). The dual test is a concrete regression that should be fixed alongside or after REQ001098. **Fix:** (1) Fix REQ001098 first — this REQ likely resolves as a side effect. (2) If still failing, debug `evalExists` for correlated references in the dual test's specific query patterns. (3) Add `TestDual_Exists_Correlated` verifying the exact dual test case. | medium | small | REQ001098 | EX/eval.go |
| REQ001127 | EX/eval | **`NULL NOT IN (list)` returns wrong result in dual test.** `tests/sqlcmp/dual`: `null_not_in_list` case fails. SQL `NOT IN` with NULL in the list has three-valued logic: `x NOT IN (1, NULL)` returns NULL (not TRUE) when x is not 1, because `x != NULL` is NULL. The dual test expects a specific result that the engine gets wrong. **Fix:** (1) Debug `evalInValue` / `evalInListBatch` for NULL handling in NOT IN. (2) Verify three-valued logic: `NULL NOT IN (1, NULL)` → NULL, `5 NOT IN (1, NULL)` → NULL, `1 NOT IN (1, NULL)` → FALSE. (3) Add `TestNotIn_NullThreeValuedLogic` verifying correct NULL propagation. | medium | small | none | EX/eval.go |
| REQ001128 | EX/eval | **UNION ALL/UNION DISTINCT with NULLs returns wrong results in dual tests.** `tests/sqlcmp/dual`: `union_all_with_nulls` and `union_distinct_with_nulls` cases fail. UNION ALL should preserve all rows including duplicates and NULLs. UNION DISTINCT should deduplicate, treating two NULLs as equal for dedup purposes (per SQL standard). Root cause likely: (1) NULL comparison in UNION DISTINCT uses `=` instead of `IS NOT DISTINCT FROM`, or (2) NULL rows are dropped from UNION ALL. **Fix:** (1) Debug UNION ALL/UNION DISTINCT implementation in `compound.go`. (2) Verify NULL handling: UNION ALL preserves NULL rows, UNION DISTINCT treats NULL=NULL for dedup. (3) Add `TestUnion_DistinctNullDedup` verifying NULL dedup behavior. | medium | small | none | EX/compound.go |
| REQ001129 | EX/eval | **INSERT ... SELECT returns wrong result in dual test.** `tests/sqlcmp/dual`: `insert_select` case fails. The test inserts rows via `INSERT INTO t2 SELECT * FROM t1` and verifies the result. Root cause likely: (1) column index mismatch between source and target tables, (2) NULL propagation during copy, or (3) the INSERT operator doesn't correctly read from the SELECT subquery. **Fix:** (1) Debug `buildInsertRow` for INSERT ... SELECT path. (2) Verify column mapping between source SELECT output and target table schema. (3) Add `TestInsertSelect_ColumnMapping` verifying correct row insertion. | medium | small | none | EX/writers.go, EX/source.go |
| REQ001155 | EX/plan | **Join elimination drops joins whose columns are referenced only in the JOIN ON clause.** Discovered while implementing REQ001103: `collectReferencedTables` (planner.go:1419) explicitly excludes JOIN ON clauses from its table-usage scan (line 1444 comment). For `SELECT t1.a FROM t1 JOIN t2 ON t1.id = t2.t1id`, `t2` is reported as unreferenced (its column `t2.t1id` only appears in the join condition), so `s.Joins` is filtered to drop the t2 join — producing wrong results. **Fix:** (1) Either include JOIN ON clauses in `collectReferencedTables`, or (2) special-case the join elimination so a join is never dropped while its ON clause references the joined table. (3) Add a regression test that runs `SELECT t1.a FROM t1 JOIN t2 ON t1.id = t2.t1id` against a seeded t2 and verifies the join is preserved. | high | small | none | EX/planner.go |
| REQ001156 | EX/plan | **groupBushyJoins heuristic splits non-independent groups, breaking select5 queries.** Discovered while implementing REQ001103: `groupBushyJoins` (planner.go:4418) marks a table `independent` of the running group if no pairKey matches the current group, then starts a new group if the previous group already has ≥ 2 entries. For 4-table chains like `FROM t51,t29,t31,t55 WHERE a51=b31 AND a29=b51 AND b55=a31`, the heuristic produces 2 groups instead of 1 — but the predicates are chain-equi-joins, not truly independent. An attempted bushy-mode path in `planSelectJoins` that skipped equi-key extraction regressed `TestSelect5_MultiTableJoin_OOM/join-4-1..join-10-1`. **Fix:** (1) Tighten the independence test to require *pairwise* column-set disjointness across all candidate groups, not just no immediate pairKey. (2) Or: when a candidate table shares any column with the previous group's predicates via transitive closure, fold it into the existing group instead of starting a new one. (3) Add a regression test with select5 join-4-1's exact predicate pattern and verify groupBushyJoins returns 1 group. | medium | medium | REQ001103 | EX/planner.go |
| REQ001161 | SQB/EX | **REQ001158b: delete remaining var/function re-exports from `SQB/EX/ex.go`.** Commits `094c447` and `ea13758` (REQ001158a) deleted 19 of 63 var re-exports. Batches 1-2 (`8395531`, `c69a3a6`) migrated 15 symbols with 395+ bare-name internal uses. Commit `47379e2` removed 20+ data-helper var re-exports. The last var re-export `NewSeqScanWithStore` was deleted in this pass (no bare-name internal uses remain). **All 44 var/function re-exports are now deleted from ex.go.** Remaining: ~30 `type` alias re-exports (`type SeqScan = OP.SeqScan`, etc.) that are used by bare name throughout `SQB/EX/` (e.g., 36 `*SeqScan` refs in planner.go). These are transparent type aliases — they do not create indirection or allocation overhead. **Fix for type aliases (deferred):** (1) Replace each bare type name with the source-package prefix. (2) Delete the type alias line. (3) Build+test after each batch. Estimated scope: ~100+ bare refs across 30 type aliases. | low | large | REQ001158 | SQB/EX/ex.go |
| REQ001159 | build | **Add dependency check (`depcheck`) — codify the current package import graph as the baseline; fail CI on new violations.** No automated enforcement of the subsystem/cluster layering rule today. The dependency report (`docs/compose/reports/dependency-structure-principles.md`, 2026-06) recommends a `tests/depcheck/` Go program that reads package import paths and checks against an allowlist. The allowlist must be derived from the **current** package layout (no `internal/types/` — see REQ001160). **Fix:** (1) Create `tests/depcheck/main.go` (or a `_test.go` so it runs under `go test`) that walks `go list -deps ./...` and asserts each package only imports from layers at-or-below its own. (2) Define the allowlist per ARCH.md §Dependency Order: LOG→FIL→MEM→WAL→ENG→TXN→SQF→SQB→SYS, plus intra-SQB cluster rules from `SQB.md`. (3) Land as a **warning** (not failure) during iter-36/37. (4) Promote to failure after REQ001123 (SQB finalization) ships. (5) Document the rules in `docs/development/DEPENDENCIES.md`. (6) Add depcheck to the CI script. | high | medium | none | tests/depcheck/, CI, docs/development/DEPENDENCIES.md |
| REQ001160 | docs | **Reject `internal/types/` proposal from `docs/compose/reports/dependency-structure-principles.md` — do not create a neutral `internal/types/` package at "layer 3.5".** The report's central architectural move (extract `Operator`/`Row`/`Value`/`ExecContext` into a new `internal/types/` package between MEM and WAL) was reviewed and rejected on 2026-07-01. Reasons: (1) the "SQF ↔ SQB import cycle" it claims to break does not exist (`SQF/PL` has zero imports from `internal/SQB/**`); (2) `Operator`/`Row` ownership is documented (ARCH.md §Cross-Subsystem Interfaces) as belonging to `SQB/DT` by design; (3) the Subsystem/Function Cluster rule (AGENTS.md) requires every package to be a cluster of a subsystem — there is no "layer 3.5" slot; (4) `ExecContext` is not a kit primitive (it carries per-statement runtime state); (5) `Value` ownership is already a three-package chain `SYS/AP` → `SQF/PL` → `SQB/DT` and adding a fourth alias layer creates ambiguity, not clarity. **Fix:** (1) No code action. (2) Human action: apply the design-doc diffs in `docs/compose/reports/design-doc-update-list.md` to `docs/design/ARCH.md` and `docs/design/subsystems/SQB.md` (AI cannot edit `docs/design/` per Design Protection rule). (3) Track any future schema-cluster or session-counter ownership decision as a separate REQ requiring human design-doc sign-off. | high | small | none | docs/design/ARCH.md, docs/design/subsystems/SQB.md |
