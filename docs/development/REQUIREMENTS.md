## TBD

| ID | Subsystem | Requirement | Priority | Effort | Deps | Touches |
| --- | --- | --- | --- | --- | --- | --- |
| REQ000743 | EX/planner | **RIGHT OUTER JOIN may not emit unmatched right rows.** `SELECT * FROM t1 RIGHT JOIN t2 ON t1.id = t2.id` — the `NestedLoopJoin` sets `leftOuter` for LEFT and FULL joins (line 44: `leftOuter: kind == JoinKindLeft \|\| kind == JoinKindFull`) but does NOT set a corresponding `rightOuter` flag for RIGHT and FULL joins. This means unmatched right-side rows may not be emitted with NULL-padded left columns. Fix: add a `rightOuter` flag to `NestedLoopJoin` and emit NULL-padded rows when the right side has no matching left rows. | medium | medium | — | EX/join.go |
| REQ000744 | EX/planner | **FULL OUTER JOIN may miss unmatched rows from one side.** Same root cause as REQ000743 — `NestedLoopJoin` only tracks `leftOuter` for unmatched left rows. For FULL OUTER JOIN, unmatched rows from the right side (no matching left rows) may not be emitted. The `rightOuter` flag is missing entirely. Fix: add `rightOuter` tracking and emit NULL-padded rows from both sides when no match is found. | medium | medium | REQ000743 | EX/join.go |
| REQ000745 | PS/parser | **Window frame shorthand (without BETWEEN) not supported.** `SELECT ROW_NUMBER() OVER (ORDER BY id ROWS 3 PRECEDING)` fails with a syntax error. SQLite supports shorthand window frame specifications like `ROWS n PRECEDING` (equivalent to `ROWS BETWEEN n PRECEDING AND CURRENT ROW`). The parser only handles the full `ROWS BETWEEN ... AND ...` form. Fix: extend the window frame parser to accept the shorthand form without `BETWEEN`. | low | small | — | PS/ps.go |
| REQ000746 | PS/parser | **GROUPS window frame type not supported.** `SELECT SUM(v) OVER (ORDER BY id GROUPS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW)` fails with a syntax error. SQLite 3.28+ supports `GROUPS` as a window frame type (alongside `ROWS` and `RANGE`). The parser only recognizes `ROWS` and `RANGE`. Fix: add `GROUPS` as a recognized window frame type. | low | small | — | PS/ps.go |
| REQ000747 | PS/parser | **FILTER clause on aggregate functions not supported.** `SELECT COUNT(*) FILTER (WHERE v > 10) FROM t` fails with a syntax error. SQLite 3.30+ supports the `FILTER (WHERE ...)` clause on aggregate and window functions to apply a filter before aggregation. The parser does not recognize the `FILTER` keyword after a function call. Fix: extend the aggregate/window function parser to accept an optional `FILTER (WHERE expr)` clause, and apply it during aggregation. | low | medium | — | PS/ps.go |
| REQ000748 | PS/parser | **EXCLUDE clause in window frames not supported.** `SELECT SUM(v) OVER (ORDER BY id ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW EXCLUDE NO OTHERS)` fails with a syntax error. SQLite 3.28+ supports `EXCLUDE` clauses in window frame specifications (`EXCLUDE CURRENT ROW`, `EXCLUDE GROUP`, `EXCLUDE TIES`, `EXCLUDE NO OTHERS`). Fix: extend the window frame parser to accept an optional `EXCLUDE` clause. | low | small | — | PS/ps.go |
| REQ000749 | EX/eval | **JSON_INSERT, JSON_REPLACE, JSON_REMOVE functions not supported.** `SELECT JSON_INSERT('{"a":1}', '$.b', 2)` returns `ex: eval error`. SQLite 3.38+ supports `JSON_INSERT`, `JSON_REPLACE`, and `JSON_REMOVE` for modifying JSON values. The `json.go` file implements `JSON_EXTRACT`, `JSON_TYPE`, `JSON_VALID`, `JSON_ARRAY`, `JSON_OBJECT`, `JSON_SET` but not the INSERT/REPLACE/REMOVE variants. Fix: add `JSON_INSERT`, `JSON_REPLACE`, and `JSON_REMOVE` to the JSON function dispatch in `json.go`. | low | medium | — | EX/json.go |
| REQ000750 | PS/parser | **Three-part qualified names (`database.table.column`) not supported.** `SELECT main.t.id FROM main.t` fails with a syntax error. SQLite supports three-part qualified names for cross-database references. Razordata only supports two-part names (`table.column`). The parser's `QualifiedName` handling does not chain a third component. Fix: extend the parser to recognize `ident.ident.ident` as a three-part qualified name. Since multi-database is not supported at runtime (ATTACH is rejected), this is low priority. | low | small | — | PS/ps.go |
| REQ000776 | EX/eval | **Replace `any` (interface{}) with tagged-union `Value` type to eliminate boxing overhead.** CPU profile: `convT64` (Go's runtime function for boxing int64 into interface{}) accounts for 10.32% of CPU. `mallocgc` from all boxing operations totals 27.78%. Every `int64` value returned from `Eval`, stored in `Row.Data`, passed through `numericArith`, `compare`, `equalValue`, or returned from `SeqScan.nextFromStore` gets boxed into a 16-byte interface header (type pointer + value pointer). For a 100K-row scan with 5 columns, this is 500K boxing operations. The `any` type pervades the entire executor: `Row.Data []any`, `Eval() (any, error)`, `numericArith(a, b any, ...)`, `compare(a, b any)`, `equalValue(a, b any)`, all aggregate accumulators, all function dispatch. **Root cause:** Go's `any` (empty interface) requires a heap allocation for non-pointer types (int64, float64, bool). The runtime's `convT64` allocates an 8-byte box on the heap, then creates a 16-byte interface header pointing to it. This happens on every value transition: decode→Row.Data, Row.Data→Eval, Eval→arithmetic, arithmetic→comparison, comparison→result. **Fix:** Define a `Value` tagged-union struct that stores all SQL types inline without heap allocation. **Implementation:** (1) Define the type in `ex.go`: `type Value struct { Kind uint8; I64 int64; F64 float64; S string; B []byte }` with kind constants `KindNull=0, KindInt=1, KindFloat=2, KindText=3, KindBlob=4, KindBool=5`. (2) Change `Row.Data` from `[]any` to `[]Value`. (3) Change `Eval` signature from `(any, error)` to `(Value, error)`. (4) Update all 22 expression eval functions (`evalUnary`, `evalBinary`, `evalBetween`, `evalIn`, `evalCase`, `evalAggregate`, `evalFunction`, etc.) to use `Value` instead of `any`. (5) Update `numericArith(a, b Value, op rune)` to switch on `a.Kind` directly — no interface dispatch. (6) Update `compare(a, b Value) int` similarly. (7) Update `equalValue(a, b Value) bool`. (8) Update `decodeRow` to produce `Value` instead of boxing to `any`. (9) Update `Eval` return paths: `return Value{Kind: KindInt, I64: v}, nil` instead of `return v, nil`. (10) Update `Row.Lookup` to return `(Value, bool)`. (11) Update all callers in `intermediate.go`, `aggregate.go`, `window.go`, `source.go`, `writers.go`, `join.go`. (12) Update the `database/sql` driver (`driver/rows.go`) to scan `Value` into `database/sql` types. (13) Add conversion helpers: `func (v Value) Int64() (int64, bool)`, `func (v Value) Float64() (float64, bool)`, `func (v Value) String() string`, `func (v Value) IsNull() bool`. (14) Add benchmarks: `BenchmarkEval_TaggedUnion_IntArith`, `BenchmarkEval_TaggedUnion_Compare`, `BenchmarkScan_TaggedUnion_100KRows`. **Expected:** ~20%+ total CPU reduction. Eliminates all `convT64` boxing (10.32% CPU) and reduces `mallocgc` pressure (27.78% CPU) by ~50-70%. The `Value` struct is 32 bytes (8+8+16+24 with string header and slice header) — fits in a single cache line on most architectures. **Risk:** Pervasive refactor touching ~30 files and ~2000+ LOC. Must be done atomically — partial conversion creates type mismatch errors everywhere. Recommend doing this as a dedicated iteration with comprehensive test coverage. **Dependencies:** Phase 1-4 optimizations should be committed first to avoid merge conflicts. | high | large | REQ000752, REQ000753, REQ000754, REQ000755, REQ000756 | EX/eval.go, EX/ex.go, EX/operators.go, EX/intermediate.go, EX/aggregate.go, EX/source.go, EX/writers.go, EX/join.go, EX/window.go, EX/store.go, EX/aggregate.go, EX/subq.go, EX/constraints.go, driver/rows.go, driver/conn.go |
| REQ000784 | EX/explain | **`explainOperator` function is dead code — never called from EXPLAIN execution path.** `explainOperator` in `explain.go:97-165` recursively walks the operator tree and produces indented text output. However, `ExplainStmtOp.Next` at `explain.go:31-37` calls `formatPlanTree` or `formatExplainNormal` (which operate on `PlanNode` trees, not raw `Operator` trees). The `explainOperator` function is never invoked. Similarly, `formatExplainNormal` at `explain.go:59-95` is redundant with `formatPlanTree` at `plan_node.go:189-230` — both produce the same `(id, parent, notused, detail)` schema. **Fix:** Remove dead code and unify rendering into a single path. **Implementation:** (1) Delete `explainOperator` function (`explain.go:97-165`). (2) Delete `formatExplainNormal` function (`explain.go:59-95`). (3) In `ExplainStmtOp.Next`, always use `formatPlanTree` regardless of mode. For `ExplainNormal` mode, include more detail (expressions, costs); for `ExplainQueryPlan` mode, use simplified output (SQLite-style SCAN/SEARCH/JOIN). (4) Add a `formatMode` parameter to `formatPlanTree` to control verbosity. (5) Add test: `TestExplain_UnifiedRendering`. | low | small | — | EX/explain.go |
| REQ000787 | EX/planner | **Cost estimation engine uses hardcoded values — no real statistics-based estimation.** Current `estimateCost` returns fixed values (SeqScan=1.0, IndexScan=0.1, Filter=0.5, Sort=10.0) regardless of table size, index selectivity, or data distribution. This makes the planner's cost-based index selection (REQ000156) unreliable for real workloads. **Fix:** Build a statistics-driven cost model. **Implementation:** (1) Add `TableStats` struct to catalog: `{RowCount int64, AvgRowWidth int, DistinctCols map[string]int64, MinVal/MaxVal for indexed columns}`. (2) Wire `ANALYZE table` to populate `TableStats` by scanning the table and computing statistics. (3) In `estimateRowCount`: for in-memory tables, return `len(tables[table])`; for store-backed tables, return stats from catalog or track INSERT/DELETE counts. (4) In `estimateFilterCost`: compute selectivity based on column statistics (equality = 1/DISTINCT, range = (max-min)/range, etc.). (5) In `estimateJoinCost`: use `leftRows * rightRows * joinSelectivity` where joinSelectivity is based on key distribution. (6) Add `estimateIndexCost` that accounts for index depth, seek vs scan, and data pages. (7) Add test: `TestCostEstimation_TableSize` verifying SeqScan cost scales with row count. (8) Add test: `TestCostEstimation_IndexSelectivity` verifying index is preferred for high-selectivity predicates. | high | large | — | EX/planner.go, EX/ex.go |
| REQ000788 | EX/explain | **EXPLAIN lacks bottleneck identification — no way to identify performance hotspots.** Even with EXPLAIN ANALYZE (REQ000786), users cannot easily identify which operator is the bottleneck. The output shows per-operator stats but no ranking, no percentage breakdown, no comparison to expected costs. **Fix:** Add bottleneck analysis to EXPLAIN ANALYZE output. **Implementation:** (1) After collecting stats, compute total time and each operator's percentage of total. (2) Highlight operators that exceed a threshold (e.g., >30% of total time or >10x estimated cost). (3) Add `--bottleneck` flag to EXPLAIN ANALYZE that outputs only the top-N slowest operators. (4) Add `BottleneckInfo` struct: `{Operator string, PctOfTotal float64, ActualVsEstimatedRatio float64, Recommendation string}`. (5) In `formatPlanTree`: prepend `**BOTTLENECK**` marker to slow operators. (6) Add recommendation logic: if Sort took >50% time with spilled=true, recommend "consider adding index on ORDER BY columns"; if Filter took >30% with no index, recommend "consider adding index on WHERE columns". (7) Add test: `TestExplainAnalyze_BottleneckDetection` with a query designed to have a known bottleneck. | medium | medium | REQ000786 | EX/explain.go, EX/plan_node.go |
| REQ000789 | EX/explain | **EXPLAIN output is plain text only — no tree visualization or structured export.** Current EXPLAIN output is a flat list of `(id, parent, notused, detail)` rows. For complex plans (3+ table joins, subqueries, CTEs), this is hard to read and visualize. No JSON export, no ASCII tree, no Graphviz output. **Fix:** Add multiple output formats. **Implementation:** (1) Add `--format` flag to EXPLAIN: `text` (default), `tree` (ASCII tree), `json` (structured JSON), `dot` (Graphviz DOT format). (2) For `tree` format: recursively walk the PlanNode tree and render with ASCII characters (`├──`, `└──`, `│`). (3) For `json` format: serialize the PlanNode tree as JSON with nested children. (4) For `dot` format: generate Graphviz DOT output for visualization in tools like Graphviz, Mermaid. (5) Add `PlanNode.ToJSON()` and `PlanNode.ToDOT()` methods. (6) Add test: `TestExplain_Format_Tree` verifying ASCII tree renders correctly for a 3-table join. (7) Add test: `TestExplain_Format_JSON` verifying JSON output is valid and parseable. | medium | medium | — | EX/explain.go, EX/plan_node.go |
| REQ000790 | EX/explain | **EXPLAIN lacks index diagnostics — no way to identify missing or unused indexes.** Users cannot tell from EXPLAIN output whether an index would improve a query, or whether existing indexes are being used efficiently. No index usage statistics, no missing index recommendations, no unused index detection. **Fix:** Add index diagnostics to EXPLAIN output. **Implementation:** (1) Add `IndexUsage` tracker that records each time an index is used (via IndexScan) vs skipped (via SeqScan with WHERE on indexed column). (2) In EXPLAIN output, add `IndexHint` annotation: `"Index not used — consider adding index on WHERE column"` or `"Index idx_a used for 80% of queries, 20% SeqScan fallback"`. (3) Add `ANALYZE INDEX` command to report index usage statistics. (4) Add `--index-hints` flag to EXPLAIN that shows missing index recommendations based on WHERE/ORDER BY/GROUP BY columns. (5) Add `MissingIndexSuggestion` struct: `{Table string, Columns []string, Reason string, EstimatedBenefit string}`. (6) Add test: `TestExplain_IndexHints_MissingIndex` where a query with WHERE on non-indexed column shows a hint. | medium | large | — | EX/explain.go, EX/plan_node.go, EX/operators.go |
| REQ000791 | EX/explain | **EXPLAIN lacks subquery optimization analysis — no way to see if subqueries are unnested or executed naively.** Correlated subqueries can be extremely slow if executed row-by-row. Users cannot tell from EXPLAIN output whether a subquery was unnested/flattened, executed as semi-join, or run naively. No execution count for correlated subqueries. **Fix:** Add subquery optimization analysis to EXPLAIN output. **Implementation:** (1) Add `SubqueryInfo` struct: `{Type string, Unnested bool, ExecutionCount int64, Method string}`. (2) In planner, track whether subqueries are unnested, flattened, or executed as semi-join/anti-join. (3) In EXPLAIN output, add `Subquery` annotation: `"Subquery unnested → SemiJoin"` or `"Correlated subquery executed N times (one per outer row)"`. (4) Add warning for correlated subqueries with high execution count: `"WARNING: correlated subquery executed 10000 times — consider rewriting as JOIN"`. (5) Add test: `TestExplain_Subquery_Unnested` verifying unnested subquery shows optimization info. (6) Add test: `TestExplain_Subquery_Correlated` verifying correlated subquery shows execution count warning. | medium | large | — | EX/explain.go, EX/plan_node.go, EX/planner.go |
| REQ000792 | EX/explain | **EXPLAIN lacks transaction/MVCC debugging — no way to see version visibility and lock behavior.** In MVCC systems, users cannot tell from EXPLAIN why some rows are visible and others are not, or whether locks are blocking execution. No snapshot visibility info, no lock wait tracking, no isolation level impact analysis. **Fix:** Add transaction debugging to EXPLAIN ANALYZE. **Implementation:** (1) Add `TxnDebugInfo` struct: `{SnapshotTS uint64, VisibleRows int64, HiddenByMVCC int64, LockWaitTimeNS int64}`. (2) In `AnalyzeOp`, track MVCC visibility: count rows filtered by snapshot vs rows read. (3) In EXPLAIN ANALYZE output, add `MVCC` annotation: `"MVCC: 100 rows visible, 50 hidden by older versions (snapshot @ ts=12345)"`. (4) Add lock wait tracking: if a row is locked by another transaction, record wait time. (5) Add `--txn-debug` flag to EXPLAIN ANALYZE for verbose transaction info. (6) Add test: `TestExplain_TxnDebug_MVCC` with concurrent transactions showing visibility info. | low | large | — | EX/explain.go, EX/plan_node.go, EX/ex.go |
| REQ000793 | EX/explain | **EXPLAIN lacks plan cache analysis — no way to see cache hit rate and plan reuse.** StmtCache exists (REQ000771) but is never wired. Users cannot see whether prepared statements are being cached and reused, or whether every query is re-parsed. No cache hit rate, no plan reuse statistics, no cache eviction info. **Fix:** Add plan cache analysis to EXPLAIN. **Implementation:** (1) Wire StmtCache as described in REQ000771. (2) Add `CacheStats` struct: `{Hits int64, Misses int64, Evictions int64, MaxSize int}`. (3) Add `EXPLAIN CACHE` command to show current cache statistics. (4) In `EXPLAIN ANALYZE` output, add `Cache` annotation: `"Cache hit (plan reused from prepare)"` or `"Cache miss (plan compiled fresh)"`. (5) Add `--cache-stats` flag to show detailed cache statistics. (6) Add test: `TestExplain_Cache_HitRate` verifying cache hit appears in output after prepare+execute. | low | medium | REQ000771 | EX/explain.go, SYS/ST/st.go, ENG/LS/engine_cache.go |
| REQ000796 | EX/planner | **IndexScan WHERE predicates not evaluated correctly on store-backed tables.** Queries like `SELECT pk FROM tab1 WHERE col0 <= 605 ORDER BY 1 DESC` return 0 rows instead of expected results when the table is backed by the engine store (not in-memory). This is the PL-1 bug, affecting ~40,000+ corpus failures. The IndexScan operator reads from the store via `nextFromIndex` (operators.go:509-518), but the WHERE predicate is either not applied or applied incorrectly. Possible causes: (1) the IndexScan doesn't call `Eval(f.predicate, &row, ...)` after reading each row, (2) the store deserialization produces values in a different type than expected (e.g., `string` vs `int64`), causing comparison operators to fail silently, or (3) the planner picks an IndexScan path that bypasses the Filter operator entirely. **Fix:** Ensure IndexScan properly evaluates WHERE predicates against store-backed rows. **Implementation:** (1) Verify that IndexScan wraps its child with a Filter operator when a WHERE predicate exists. (2) Check `decodeRow` (store.go:594) for type consistency: ensure numeric columns are decoded as `int64`/`float64`, not `string`. (3) Add logging to IndexScan.Next() to trace predicate evaluation results. (4) Add test: `TestIndexScan_WherePredicate` with store-backed table verifying `WHERE col <= val` returns correct rows. (5) Add test: `TestIndexScan_ComparisonTypes` verifying int64, float64, and string comparisons work correctly. (6) Add test: `TestIndexScan_NoFilterDrop` verifying Filter operator is not dropped from plan. Expected: corpus-wide PL-1 failures drop from ~40,000 to ~5,000 (remaining complex predicates). | critical | large | — | EX/operators.go, EX/store.go, EX/planner.go |
| REQ000801 | EX/planner | **N3-style join ordering with cost estimation (SQLite NGQP-inspired).** Current join reordering only sorts equi-join tables first. SQLite's NGQP uses the N-Nearest-Neighbors algorithm to find near-optimal join order in O(K×N) time. For K=8 tables with N=12, this evaluates ~96 partial plans instead of 8! = 40,320. The cost model considers: table row count, index availability, predicate selectivity, and join type. **Fix:** Implement a simplified N3 algorithm that considers all K tables (not just equi-join vs non-equi) and uses a cost model based on estimated row counts. **Implementation:** (1) Add `estimateJoinCost(left Plan, right Table, predicates) float64` to the planner. (2) Implement N3: maintain a priority queue of N=12 best partial plans. (3) At each step, extend each plan by joining the cheapest remaining table. (4) Use the cost model to rank candidates. (5) The final plan is the cheapest complete plan. (6) For K ≤ 8 tables (select4 max), N3 runs in microseconds. Expected: 2-5x improvement over current reordering for queries with mixed equi/non-equi joins. | medium | large | — | EX/planner.go |
| REQ000802 | EX/eval | **ADQC predicate compilation: pre-compile Filter predicates to skip Eval dispatch overhead.** CPU profile of j3_mixed shows `Filter.Next` spends 27% of CPU inside `Eval` (eval.go), most of it in the type-switch on `PS.Expr` (BinaryExpr/Ident/QualifiedName/NumberLiteral dispatch). For repeated SELECT-FROM-WHERE patterns, the same predicate is evaluated millions of times but re-dispatched each call. **Fix:** Cache a compiled predicate function once per Filter (using ADQC = Adaptive Query Compilation). On the first eval call, compile `predicate` to a typed Go function `func(*Row) (bool, error)` that closes over `*Row.Lookup` results directly. Cache by predicate AST hash in `GlobalAdqcCache`. **Implementation:** (1) Add `compiledFn func(*Row) (bool, error)` field to `Filter`. (2) On first `Next()`, walk the predicate AST and emit a specialized function for each `BinaryExpr`/`Ident`/`QualifiedName` pattern (e.g., `t1.a > 5 AND t2.b IN (1,2,3)`). (3) Cache by predicate AST hash in `GlobalAdqcCache` for reuse across executor instances. (4) Fall back to the `Eval`-based path for unsupported AST shapes. (5) Add benchmark: `BenchmarkPredicate_ADQC_vs_Eval`. Expected: 2-5x speedup on j3_mixed and j3_filter_only (combined Filter overhead ~50% of CPU). | medium | medium | — | EX/intermediate.go, EX/eval.go, EX/adqc.go |
| REQ000803 | EX/join | **Column projection pushdown: skip materializing columns that aren't referenced by SELECT/FILTER.** Profile of j3_mixed shows `joinRowsLL` allocating 6× slices per output row even though most queries only reference 2-3 columns out of 15. With `SELECT * FROM t1,t2,t3` and 5-column tables, every emitted row carries 15 columns × 1000 rows = 15K cell allocations just for join output. **Fix:** Build a column-projection plan at JOIN time: identify the union of columns referenced by `SELECT` and `WHERE` (via `collectReferencedTables`/`collectReferencedColumns`). Pass that projection down so `joinRowsLL` only copies the projected columns, and downstream Filter/Project never see the unprojected ones. **Implementation:** (1) Add `collectReferencedColumns(s *PS.Select) map[string]bool` next to the existing `collectReferencedTables`. (2) Pass the projection set to NestedLoopJoin via a new `projectedCols []string` field. (3) In `joinRowsLL`, only copy columns in the projection set (skip the rest). (4) When `SELECT *` is used, projection is the full set (no change). (5) Add benchmark: `BenchmarkProjectionPushdown`. Expected: 30-50% memory reduction and 1.3-2× speedup on j3 cross joins with narrow SELECT/WHERE projections. | medium | medium | REQ000799 | EX/join.go, EX/planner.go |
| REQ000810 | EX/eval | **GROUP BY with column reordering produces wrong results.** `SELECT * FROM tab0 GROUP BY col1, col2, col0` returns different hash than `GROUP BY col0, col1, col2` when the column order differs. The group key serialization should be order-dependent (different column order = different grouping), but the test expects both to produce the same hash (8ad40ad40eb1e471572580fddc8d2aaf). This suggests either: (1) the group key serialization is order-independent when it should be order-dependent, or (2) the test expects order-independent behavior (like SQLite's GROUP BY with SELECT * non-aggregated columns). Root cause: when SELECT * is used with GROUP BY, non-aggregated columns not in GROUP BY produce indeterminate values — SQLite picks the value from the row that GROUP BY placed first. Razordata may be picking different rows. Affects random/groupby SLT failures. **Fix:** Ensure deterministic row selection for non-aggregated columns in GROUP BY, matching SQLite behavior (value from the row that defined the group). **Implementation:** (1) In `Aggregate.materialize`, when building group buckets, store the first row encountered for each group. (2) For non-aggregated columns in SELECT *, return values from the stored first row. (3) Verify the first row is deterministic (order of input rows). (4) Add test: `TestGroupBy_DeterministicSelectStar` verifying GROUP BY with SELECT * returns consistent results. | high | medium | — | EX/aggregate.go |

| REQ000811 | EX/scalar | **Razordata driver returns wrong output for blob/text columns vs oracle (modernc/sqlite).** Dual test `zeroblob_content` returns `[[[0 0 0 0 0 0 0 0]]]` from razor (rendered as `[]byte` in slt harness) while oracle returns `[["..."]]` (rendered as `string`). The slt driver returns blob cells as `[]byte`, oracle returns them as `string`. **Fix:** Either normalize `[]byte` ↔ `string` at the slt driver boundary, or coerce blobs to hex strings on the oracle side for fair comparison. **Implementation:** (1) In `tests/sqlcmp/dual/dual.go`, update `normalizeCell` (line 253) to coerce `[]byte` to `string` before comparing with oracle (already does this for single cells — verify it propagates to blob slice cells). (2) Alternatively, in `tests/sqlcmp/slt/types.go` blob rendering, always render as `string(hex)` so both sides agree. (3) Add test: `TestDual_ZeroblobNormalization` verifying normalized blob cells match oracle. | low | small | — | tests/sqlcmp/dual/dual.go, tests/sqlcmp/slt/types.go |
| REQ000812 | EX/scalar | **Razor returns 0 for `CHANGES()` and `TOTAL_CHANGES()`; oracle returns the correct change count.** Dual test `total_changes_function` returns 0 from razor (expected 3 = 1 INSERT + 1 UPDATE + 1 DELETE). The change counter is either not wired in the in-memory path or wired with wrong scope (per-statement vs per-session). **Fix:** Wire `CHANGES()`/`TOTAL_CHANGES()` to track DML operations per session. **Implementation:** (1) Add `lastChanges`, `totalChanges` fields to executor, scoped per-session. (2) Increment in INSERT (1 per row), UPDATE (rows matched), DELETE (rows deleted). (3) Implement `evalChanges`/`evalTotalChanges` in `eval.go` to read those counters. (4) Add tests for CHANGES() after each DML type. (5) Verify against oracle. | medium | medium | — | EX/ex.go, EX/eval.go, EX/writers.go |
| REQ000813 | EX/cte | **CTE queries return empty when razor engine returns fewer rows than oracle.** Dual tests `cte_basic`, `cte_with_aggregate`, `multiple_ctes` all return `razor=[]` while oracle returns `[[10] [20] [30]]`. The CTE execution path is either short-circuiting on first iteration or the Materialized CTE planner bug drops the SELECT result. **Fix:** Trace CTE evaluation, ensure Materialize operator emits all rows from the CTE definition. **Implementation:** (1) Add `slog` debug log in `CTE.Next()` and `Materialize.Next()` to verify row flow. (2) Verify `cte_definitions` and `cteRows` are populated before returning. (3) Add test: `TestCTE_Basic` verifying `WITH cte AS (SELECT 10 UNION ALL SELECT 20 UNION ALL SELECT 30) SELECT * FROM cte` returns 3 rows. (4) Add test: `TestCTE_Aggregate` with `WITH cte AS (...) SELECT COUNT(*) FROM cte`. | high | medium | — | EX/cte.go, EX/plan.go, EX/intermediate.go |
| REQ000814 | EX/setop | **UNION/UNION ALL with NULLs produces wrong row count in dual tests.** `union_all_with_nulls`, `union_distinct_with_nulls` return wrong row counts. The set operator deduplicates NULLs incorrectly (treats them as distinct values when they should be treated as same), or NULLs are filtered out. **Fix:** Verify `SetOpOperator` handles NULLs correctly per SQL standard (NULLs are equal in DISTINCT but preserved in ALL). **Implementation:** (1) In `SetOpOperator.Next()`, when DISTINCT, hash NULL as a special sentinel that compares equal to other NULLs. (2) When ALL, just emit each row without dedup. (3) Add test: `TestSetOp_UnionAll_Nulls` and `TestSetOp_UnionDistinct_Nulls`. | high | medium | — | EX/setop.go, EX/intermediate.go |
| REQ000815 | EX/eval | **NULL != NULL test returns wrong result in dual comparison.** `null_not_equal` returns `razor=[[<nil>]]]` while oracle returns `[[]]` (empty row count). SQL standard says `NULL <> NULL` is UNKNOWN (not true), so the predicate filters out the row. **Fix:** Ensure `evalBinary` T_NE returns nil for NULL comparisons, and the Filter operator correctly drops rows when predicate is nil (UNKNOWN). **Implementation:** (1) In `eval.go`, `case int(LX.T_NE):` returns `!equalValue(left, right)` — verify NULL returns `nil`. (2) Verify Filter treats `nil` predicate result as "drop the row" (per SQL three-valued logic). (3) Add test: `TestFilter_NullNotEqual` verifying `WHERE NULL <> NULL` returns 0 rows. | medium | small | — | EX/eval.go, EX/intermediate.go |
