## TBD
| ID | Subsystem | Requirement | Priority | Effort | Deps | Touches |
| --- | --- | --- | --- | --- | --- | --- |
| REQ001664 | SQB/EV | **Memoize per-batch column references in EvalBatchExpr to eliminate redundant subtree evaluation.** `evalArithBatch` (`eval_vec.go:1256-1258`) recursively calls `EvalBatchExpr(e.Left, ...)` and `EvalBatchExpr(e.Right, ...)`. For an expression like `a+b*2+c*3+d*4+e*5` over 30 rows, each shared column ref re-walks the BinaryExpr subtree and re-runs `ExtractColumnRef`. In select2 this costs 1.98s of CPU on the left subtree alone (4.47s EvalBatchExpr total cum). The CASE case (`evalCaseBatchExpr`) shows similar redundancy: each `WHEN` clause re-evaluates shared subexpressions across batches. **Fix:** (1) Add a per-batch memo map `map[exprID]UT.Column` keyed by `*Expr` pointer that lives for one `EvalBatchExpr` call. (2) Memoize `*PS.Ident` column refs (already cheap, but extend to other cheap-to-evaluate exprs). (3) At minimum, detect identical `*PS.Ident` repeated inside one batch's expression tree (CSE) and evaluate once. (4) For complex CSE detection: add a `compileMemoizedBatchExpr` pass that pre-computes the memo plan. Estimate: ~100 lines, 2 files, 1 day. | medium | medium | none | SQB/EV/eval_vec.go |
| REQ001675 | SQB/WT | **Pool Insert's scratch slices (bigBuf, insertBuf) and colIdx across INSERT statements.** `Insert.nextFromStore` (`writers_dml.go:393-418`) allocates per-INSERT: `colIdx := make([]int, len(i.cols))` (line 394), `bigBuf = make([]DT.Value, nRows*nCols)` (line 412, only when nRows>1), and `insertBuf := make([]DT.Row, 0, insertChunkSize)` (line 418). For a single-row INSERT (the common case in SLT rsel125/126), `bigBuf` is skipped but `colIdx` and `insertBuf` are allocated fresh each call. Even though individual INSERT alloc is small (~3 small slices × 12 SQLs × 348 iterations ≈ 12K allocations per benchmark), the cumulative GC pressure is non-trivial for workloads that run many INSERTs. **Fix:** (1) Add `scratchColIdx []int` and `scratchInsertBuf []DT.Row` fields to `*Insert`. (2) Reuse via re-slice, grow via `slices.Grow` if capacity insufficient. (3) For `bigBuf`, only allocate when the previous capacity is too small; otherwise re-slice and zero. (4) Verify with `BenchmarkSLT_RSel125` (no regression — likely 5-10% drop in INSERT B/op). Estimate: ~30 lines, 1 file, 0.25 day. | low | small | none | SQB/WT/writers_dml.go |
| REQ001681 | SQB/WT | **Eliminate per-row Cols/Data copy in buildInsertRowFromSelectWithMap for INSERT...SELECT.** `buildInsertRowFromSelectWithMap` (`writers_dml.go:696-718`) allocates per-row copies of Cols and Data: `out.Cols = append([]string(nil), schema...)` (line 699), `out.Data = make([]DT.Value, len(schema))` (line 700), or in the no-column-list path: `out.Cols = append([]string(nil), src.Cols...)` (line 715), `out.Data = append([]DT.Value(nil), src.Data...)` (line 716). For idx1000's 5000 INSERT...SELECT rows, this is 11.8K allocs. The schema is stable across all rows; the Data slice must be fresh per row. **Fix:** (1) Share the Cols slice across all rows (it's never mutated per-row). (2) Only allocate a fresh Data slice. (3) For the column-list path, build the `colIdx` map once per Insert.nextFromSelect call instead of per-row. (4) Verify with `BenchmarkSLT_Idx1000_2` — expected allocs/op drop: 734K → ~720K. (5) Check no regression via `TestSLT_InList/idx1000_2/3/4`. Estimate: ~25 lines, 2 files, 0.25 day. | low | small | none | SQB/WT/writers_dml.go |
| REQ001696 | SQB/OP | **Pool single-row Values operator results — 24K alloc objects / 2.2% of slt_good_0 total.** `Values.Next` (`values.go`) allocates a fresh `DT.Row` per call for no-FROM queries (`SELECT 1+1`, `SELECT COUNT(*)`). 10K queries × ~2.4 avg rows = 24K row allocs. pprof: 2.4MB alloc. **Fix:** (1) Pool the single-row result in the Values operator. (2) Return a pointer to a pooled row instead of a copy. (3) Verify with alloc_objects — expected Values alloc drop: 24K → ~2K. Estimate: ~15 lines, 1 file, 0.1 day. | low | small | none | SQB/OP/values.go |
| REQ001704 | SQB/OP | **Optimize Distinct.Next hash table allocation — 293 MB flat / 9.1% of idx1000_2 total.** `Distinct.Next` allocates a large hash map for deduplication. For idx1000's 5000-row tables, the hash set grows to millions of entries. pprof: 293MB flat + 912MB cumulative. **Fix:** (1) Use a Bloom filter pre-check to skip hash allocation for low-cardinality results. (2) Or use streaming dedup with a bounded window. (3) Pre-size the map based on estimated cardinality from table stats. (4) Verify with alloc_space — expected Distinct alloc drop: 293MB → ~50MB. Estimate: ~40 lines, 1 file, 0.5 day. | high | medium | none | SQB/OP/operators.go |
| REQ001705 | SQB/WT | **Optimize INSERT...SELECT pipeline — 265 MB flat / 8.2% of idx1000_2 total.** `Insert.nextFromSelect` allocates per-row buffers for column mapping, row construction, and default filling. Cumulative: 1271MB across the full insert pipeline. **Fix:** (1) Pool the column mapping buffer across rows. (2) Share the schema slice (immutable per INSERT). (3) Batch row construction into pre-allocated chunks. (4) Verify with alloc_space — expected insert pipeline alloc drop: 265MB → ~80MB. Estimate: ~40 lines, 2 files, 0.5 day. | high | medium | none | SQB/WT/writers_dml.go |
| REQ001706 | SYS/AP | **Reduce Value.ToAny boxing overhead — 1.1M alloc objects / 2.5% of idx1000_2 total.** `Value.ToAny` boxes the Value struct into an `interface{}` for map keys and function arguments. 1.1M calls per benchmark. **Fix:** (1) Use type assertions to avoid boxing for common types. (2) Or use a union-type pattern instead of interface{}. (3) Verify with alloc_objects — expected ToAny alloc drop: 1.1M → ~100K. Estimate: ~20 lines, 1-2 files, 0.25 day. | medium | small | none | SYS/AP/ap.go |
| REQ001714 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_1 in pre-commit gate.** 97 failures / 23,231 records. Root causes: column ordering swap (~60) now fixed, WHERE clause evaluation (~25), aggregate/NULL errors (~12). Remaining ~23 failures tracked in REQ001979. When all resolved, add `{Label: "random::aggregates::slt_good_1", Path: "random/aggregates/slt_good_1", Timeout: 60}` to `includedCases` in `slt_included_list.go`. Estimate: depends on root bug fixes. | high | medium | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001715 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_2 in pre-commit gate.** 133 failures / 23,032 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001716 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_3 in pre-commit gate.** 139 failures / 22,623 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001717 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_4 in pre-commit gate.** 129 failures / 22,673 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001718 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_5 in pre-commit gate.** 136 failures / 22,735 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001719 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_6 in pre-commit gate.** 102 failures / 22,732 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001720 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_7 in pre-commit gate.** 121 failures / 22,565 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001721 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_8 in pre-commit gate.** 131 failures / 22,620 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001722 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_9 in pre-commit gate.** 131 failures / 22,406 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001723 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_10 in pre-commit gate.** 114 failures / 23,112 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001724 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_11 in pre-commit gate.** 132 failures / 22,800 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001725 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_12 in pre-commit gate.** 125 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001726 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_13 in pre-commit gate.** 119 failures / 22,500 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001727 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_14 in pre-commit gate.** 109 failures / 22,700 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001728 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_15 in pre-commit gate.** 117 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001729 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_16 in pre-commit gate.** 133 failures / 22,500 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001730 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_17 in pre-commit gate.** 119 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001731 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_18 in pre-commit gate.** 147 failures / 22,400 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001732 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_19 in pre-commit gate.** 125 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001733 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_20 in pre-commit gate.** 115 failures / 22,700 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001734 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_21 in pre-commit gate.** 122 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001735 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_22 in pre-commit gate.** 132 failures / 22,500 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001736 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_23 in pre-commit gate.** 135 failures / 22,500 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001737 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_24 in pre-commit gate.** 129 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001738 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_25 in pre-commit gate.** 138 failures / 22,500 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001739 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_26 in pre-commit gate.** 123 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001740 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_27 in pre-commit gate.** 133 failures / 22,500 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001741 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_28 in pre-commit gate.** 127 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001742 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_29 in pre-commit gate.** 142 failures / 22,400 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001743 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_30 in pre-commit gate.** 141 failures / 22,400 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001744 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_31 in pre-commit gate.** 133 failures / 22,500 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001745 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_32 in pre-commit gate.** 116 failures / 22,700 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001746 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_33 in pre-commit gate.** 122 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001747 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_34 in pre-commit gate.** 110 failures / 22,700 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001748 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_35 in pre-commit gate.** 139 failures / 22,500 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001749 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_36 in pre-commit gate.** 119 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001750 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_37 in pre-commit gate.** 118 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001751 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_38 in pre-commit gate.** 123 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001752 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_39 in pre-commit gate.** 137 failures / 22,500 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001753 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_40 in pre-commit gate.** 116 failures / 22,700 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001754 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_41 in pre-commit gate.** 110 failures / 22,700 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001755 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_42 in pre-commit gate.** 109 failures / 22,700 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001756 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_43 in pre-commit gate.** 108 failures / 22,700 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001757 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_44 in pre-commit gate.** 95 failures / 22,900 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001758 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_45 in pre-commit gate.** 123 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001759 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_46 in pre-commit gate.** 123 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001760 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_47 in pre-commit gate.** 112 failures / 22,700 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001761 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_48 in pre-commit gate.** 114 failures / 22,700 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001762 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_49 in pre-commit gate.** 114 failures / 22,700 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001763 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_50 in pre-commit gate.** 131 failures / 22,500 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001764 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_51 in pre-commit gate.** 110 failures / 22,700 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001765 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_52 in pre-commit gate.** 120 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001766 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_53 in pre-commit gate.** 127 failures / 22,500 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001767 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_54 in pre-commit gate.** 112 failures / 22,700 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001768 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_55 in pre-commit gate.** 117 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001769 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_56 in pre-commit gate.** 120 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001770 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_57 in pre-commit gate.** 135 failures / 22,500 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001771 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_58 in pre-commit gate.** 127 failures / 22,500 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001772 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_59 in pre-commit gate.** 113 failures / 22,700 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001773 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_60 in pre-commit gate.** 123 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001774 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_61 in pre-commit gate.** 123 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001775 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_62 in pre-commit gate.** 122 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001776 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_63 in pre-commit gate.** 113 failures / 22,700 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001777 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_64 in pre-commit gate.** 100 failures / 22,800 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001778 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_65 in pre-commit gate.** 123 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001779 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_66 in pre-commit gate.** 123 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001780 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_67 in pre-commit gate.** 128 failures / 22,500 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001781 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_68 in pre-commit gate.** 104 failures / 22,800 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001782 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_69 in pre-commit gate.** 104 failures / 22,800 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001783 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_70 in pre-commit gate.** 113 failures / 22,700 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001784 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_71 in pre-commit gate.** 114 failures / 22,700 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001785 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_72 in pre-commit gate.** 125 failures / 22,500 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001786 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_73 in pre-commit gate.** 129 failures / 22,500 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001787 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_74 in pre-commit gate.** 124 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001788 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_75 in pre-commit gate.** 123 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001789 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_76 in pre-commit gate.** 125 failures / 22,500 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001790 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_77 in pre-commit gate.** 119 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001791 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_78 in pre-commit gate.** 116 failures / 22,700 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001792 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_79 in pre-commit gate.** 125 failures / 22,500 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001793 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_80 in pre-commit gate.** 110 failures / 22,700 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001794 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_81 in pre-commit gate.** 123 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001795 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_82 in pre-commit gate.** 120 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001796 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_83 in pre-commit gate.** 142 failures / 22,400 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001797 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_84 in pre-commit gate.** 117 failures / 22,700 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001798 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_85 in pre-commit gate.** 118 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001799 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_86 in pre-commit gate.** 130 failures / 22,500 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001800 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_87 in pre-commit gate.** 123 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001801 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_88 in pre-commit gate.** 125 failures / 22,500 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001802 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_89 in pre-commit gate.** 110 failures / 22,700 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001803 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_90 in pre-commit gate.** 122 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001804 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_91 in pre-commit gate.** 120 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001805 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_92 in pre-commit gate.** 111 failures / 22,700 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001806 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_93 in pre-commit gate.** 102 failures / 22,800 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001807 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_94 in pre-commit gate.** 124 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001808 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_95 in pre-commit gate.** 120 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001809 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_96 in pre-commit gate.** 103 failures / 22,800 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001810 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_97 in pre-commit gate.** 130 failures / 22,500 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001811 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_98 in pre-commit gate.** 123 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001812 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_99 in pre-commit gate.** 123 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001813 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_100 in pre-commit gate.** 132 failures / 22,500 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001814 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_101 in pre-commit gate.** 126 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001815 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_102 in pre-commit gate.** 123 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001816 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_103 in pre-commit gate.** 121 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001817 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_104 in pre-commit gate.** 146 failures / 22,400 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001818 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_105 in pre-commit gate.** 133 failures / 22,500 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001819 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_106 in pre-commit gate.** 127 failures / 22,500 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001820 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_107 in pre-commit gate.** 112 failures / 22,700 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001821 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_108 in pre-commit gate.** 105 failures / 22,800 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001822 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_109 in pre-commit gate.** 134 failures / 22,500 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001823 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_110 in pre-commit gate.** 110 failures / 22,700 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001824 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_111 in pre-commit gate.** 141 failures / 22,400 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001825 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_112 in pre-commit gate.** 148 failures / 22,300 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001826 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_113 in pre-commit gate.** 132 failures / 22,500 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001827 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_114 in pre-commit gate.** 133 failures / 22,500 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001828 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_115 in pre-commit gate.** 114 failures / 22,700 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001829 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_116 in pre-commit gate.** 129 failures / 22,500 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001830 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_117 in pre-commit gate.** 132 failures / 22,500 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001831 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_118 in pre-commit gate.** 114 failures / 22,700 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001832 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_119 in pre-commit gate.** 115 failures / 22,700 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001833 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_120 in pre-commit gate.** 124 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001834 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_121 in pre-commit gate.** 146 failures / 22,400 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001835 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_122 in pre-commit gate.** 126 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001836 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_123 in pre-commit gate.** 138 failures / 22,500 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001837 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_124 in pre-commit gate.** 121 failures / 22,600 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001838 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_125 in pre-commit gate.** 138 failures / 22,500 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001839 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_126 in pre-commit gate.** 129 failures / 22,500 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001840 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_127 in pre-commit gate.** 133 failures / 22,500 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001841 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_128 in pre-commit gate.** 128 failures / 22,500 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001842 | tests/sqlcmp/slt | **Pass random/aggregates/slt_good_129 in pre-commit gate.** 10 failures / 8,000 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small |  | tests/sqlcmp/slt/slt_included_list.go |
| REQ001843 | tests/sqlcmp/slt | **Pass random/select/slt_good_0 in pre-commit gate.** 2 failures / 9,905 records. Root causes: WHERE eval + NULL propagation. Fix via REQ001979. When resolved, add `{Label: "random::select::slt_good_0", Path: "random/select/slt_good_0", Timeout: 60}` to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001844 | tests/sqlcmp/slt | **Pass random/select/slt_good_1 in pre-commit gate.** 1 failure / 9,913 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001845 | tests/sqlcmp/slt | **Pass random/select/slt_good_2 in pre-commit gate.** 5 failures / 9,924 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001846 | tests/sqlcmp/slt | **Pass random/select/slt_good_3 in pre-commit gate.** 6 failures / 9,941 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001847 | tests/sqlcmp/slt | **Pass random/select/slt_good_4 in pre-commit gate.** 1 failure / 9,923 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001848 | tests/sqlcmp/slt | **Pass random/select/slt_good_5 in pre-commit gate.** 8 failures / 9,927 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001849 | tests/sqlcmp/slt | **Pass random/select/slt_good_6 in pre-commit gate.** 5 failures / 9,926 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001850 | tests/sqlcmp/slt | **Pass random/select/slt_good_7 in pre-commit gate.** 2 failures / 9,917 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001851 | tests/sqlcmp/slt | **Pass random/select/slt_good_8 in pre-commit gate.** 4 failures / 9,928 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001852 | tests/sqlcmp/slt | **Pass random/select/slt_good_9 in pre-commit gate.** 3 failures / 9,921 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001853 | tests/sqlcmp/slt | **Pass random/select/slt_good_10 in pre-commit gate.** 3 failures / 9,929 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001854 | tests/sqlcmp/slt | **Pass random/select/slt_good_11 in pre-commit gate.** 2 failures / 9,932 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001855 | tests/sqlcmp/slt | **Pass random/select/slt_good_12 in pre-commit gate.** 8 failures / 9,946 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001856 | tests/sqlcmp/slt | **Pass random/select/slt_good_13 in pre-commit gate.** 7 failures / 9,923 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001857 | tests/sqlcmp/slt | **Pass random/select/slt_good_14 in pre-commit gate.** 2 failures / 9,926 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001858 | tests/sqlcmp/slt | **Pass random/select/slt_good_15 in pre-commit gate.** 2 failures / 9,934 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001859 | tests/sqlcmp/slt | **Pass random/select/slt_good_16 in pre-commit gate.** 3 failures / 9,917 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001860 | tests/sqlcmp/slt | **Pass random/select/slt_good_17 in pre-commit gate.** 3 failures / 9,925 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001861 | tests/sqlcmp/slt | **Pass random/select/slt_good_18 in pre-commit gate.** 5 failures / 9,940 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001862 | tests/sqlcmp/slt | **Pass random/select/slt_good_19 in pre-commit gate.** 3 failures / 9,934 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001863 | tests/sqlcmp/slt | **Pass random/select/slt_good_20 in pre-commit gate.** 1 failure / 9,953 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001864 | tests/sqlcmp/slt | **Pass random/select/slt_good_21 in pre-commit gate.** 2 failures / 9,944 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001865 | tests/sqlcmp/slt | **Pass random/select/slt_good_22 in pre-commit gate.** 4 failures / 9,945 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001866 | tests/sqlcmp/slt | **Pass random/select/slt_good_23 in pre-commit gate.** 1 failure / 9,936 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001867 | tests/sqlcmp/slt | **Pass random/select/slt_good_24 in pre-commit gate.** 10 failures / 9,939 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001868 | tests/sqlcmp/slt | **Pass random/select/slt_good_25 in pre-commit gate.** 3 failures / 9,948 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001869 | tests/sqlcmp/slt | **Pass random/select/slt_good_26 in pre-commit gate.** 7 failures / 9,941 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001870 | tests/sqlcmp/slt | **Pass random/select/slt_good_27 in pre-commit gate.** 3 failures / 9,957 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001871 | tests/sqlcmp/slt | **Pass random/select/slt_good_28 in pre-commit gate.** 7 failures / 9,946 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001872 | tests/sqlcmp/slt | **Pass random/select/slt_good_29 in pre-commit gate.** 4 failures / 9,941 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001873 | tests/sqlcmp/slt | **Pass random/select/slt_good_30 in pre-commit gate.** 2 failures / 9,947 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001874 | tests/sqlcmp/slt | **Pass random/select/slt_good_31 in pre-commit gate.** 3 failures / 9,955 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001875 | tests/sqlcmp/slt | **Pass random/select/slt_good_32 in pre-commit gate.** 8 failures / 9,950 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001876 | tests/sqlcmp/slt | **Pass random/select/slt_good_33 in pre-commit gate.** 7 failures / 9,942 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001877 | tests/sqlcmp/slt | **Pass random/select/slt_good_34 in pre-commit gate.** 4 failures / 9,951 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001878 | tests/sqlcmp/slt | **Pass random/select/slt_good_35 in pre-commit gate.** 9 failures / 9,952 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001879 | tests/sqlcmp/slt | **Pass random/select/slt_good_36 in pre-commit gate.** 4 failures / 9,941 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001880 | tests/sqlcmp/slt | **Pass random/select/slt_good_37 in pre-commit gate.** 2 failures / 9,937 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001881 | tests/sqlcmp/slt | **Pass random/select/slt_good_38 in pre-commit gate.** 2 failures / 9,945 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001882 | tests/sqlcmp/slt | **Pass random/select/slt_good_39 in pre-commit gate.** 4 failures / 9,947 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001883 | tests/sqlcmp/slt | **Pass random/select/slt_good_40 in pre-commit gate.** 6 failures / 9,937 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001884 | tests/sqlcmp/slt | **Pass random/select/slt_good_41 in pre-commit gate.** 1 failure / 9,952 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001885 | tests/sqlcmp/slt | **Pass random/select/slt_good_42 in pre-commit gate.** 6 failures / 9,952 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001886 | tests/sqlcmp/slt | **Pass random/select/slt_good_43 in pre-commit gate.** 4 failures / 9,943 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001887 | tests/sqlcmp/slt | **Pass random/select/slt_good_44 in pre-commit gate.** 9 failures / 9,947 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001888 | tests/sqlcmp/slt | **Pass random/select/slt_good_45 in pre-commit gate.** 5 failures / 9,954 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001889 | tests/sqlcmp/slt | **Pass random/select/slt_good_46 in pre-commit gate.** 8 failures / 9,950 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001890 | tests/sqlcmp/slt | **Pass random/select/slt_good_47 in pre-commit gate.** 4 failures / 9,950 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001891 | tests/sqlcmp/slt | **Pass random/select/slt_good_48 in pre-commit gate.** 4 failures / 9,953 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001892 | tests/sqlcmp/slt | **Pass random/select/slt_good_49 in pre-commit gate.** 8 failures / 9,946 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001893 | tests/sqlcmp/slt | **Pass random/select/slt_good_50 in pre-commit gate.** 7 failures / 9,954 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001894 | tests/sqlcmp/slt | **Pass random/select/slt_good_51 in pre-commit gate.** 8 failures / 9,942 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001895 | tests/sqlcmp/slt | **Pass random/select/slt_good_52 in pre-commit gate.** 4 failures / 9,940 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001896 | tests/sqlcmp/slt | **Pass random/select/slt_good_53 in pre-commit gate.** 5 failures / 9,961 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001897 | tests/sqlcmp/slt | **Pass random/select/slt_good_54 in pre-commit gate.** 4 failures / 9,936 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001898 | tests/sqlcmp/slt | **Pass random/select/slt_good_55 in pre-commit gate.** 9 failures / 9,951 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001899 | tests/sqlcmp/slt | **Pass random/select/slt_good_56 in pre-commit gate.** 1 failure / 9,959 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001900 | tests/sqlcmp/slt | **Pass random/select/slt_good_57 in pre-commit gate.** 2 failures / 9,951 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001901 | tests/sqlcmp/slt | **Pass random/select/slt_good_58 in pre-commit gate.** 5 failures / 9,947 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001902 | tests/sqlcmp/slt | **Pass random/select/slt_good_59 in pre-commit gate.** 4 failures / 9,958 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001903 | tests/sqlcmp/slt | **Pass random/select/slt_good_60 in pre-commit gate.** 6 failures / 9,944 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001904 | tests/sqlcmp/slt | **Pass random/select/slt_good_61 in pre-commit gate.** 4 failures / 9,945 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001905 | tests/sqlcmp/slt | **Pass random/select/slt_good_62 in pre-commit gate.** 5 failures / 9,953 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001906 | tests/sqlcmp/slt | **Pass random/select/slt_good_63 in pre-commit gate.** 3 failures / 9,961 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001907 | tests/sqlcmp/slt | **Pass random/select/slt_good_64 in pre-commit gate.** 7 failures / 9,951 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001908 | tests/sqlcmp/slt | **Pass random/select/slt_good_65 in pre-commit gate.** 3 failures / 9,957 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001909 | tests/sqlcmp/slt | **Pass random/select/slt_good_66 in pre-commit gate.** 3 failures / 9,958 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001910 | tests/sqlcmp/slt | **Pass random/select/slt_good_67 in pre-commit gate.** 5 failures / 9,945 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001911 | tests/sqlcmp/slt | **Pass random/select/slt_good_68 in pre-commit gate.** 6 failures / 9,949 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001912 | tests/sqlcmp/slt | **Pass random/select/slt_good_69 in pre-commit gate.** 10 failures / 9,956 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001913 | tests/sqlcmp/slt | **Pass random/select/slt_good_70 in pre-commit gate.** 5 failures / 9,943 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001914 | tests/sqlcmp/slt | **Pass random/select/slt_good_71 in pre-commit gate.** 8 failures / 9,952 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001915 | tests/sqlcmp/slt | **Pass random/select/slt_good_72 in pre-commit gate.** 9 failures / 9,944 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001916 | tests/sqlcmp/slt | **Pass random/select/slt_good_73 in pre-commit gate.** 7 failures / 9,954 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001917 | tests/sqlcmp/slt | **Pass random/select/slt_good_74 in pre-commit gate.** 7 failures / 9,946 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001918 | tests/sqlcmp/slt | **Pass random/select/slt_good_75 in pre-commit gate.** 8 failures / 9,949 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001919 | tests/sqlcmp/slt | **Pass random/select/slt_good_76 in pre-commit gate.** 7 failures / 9,950 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001920 | tests/sqlcmp/slt | **Pass random/select/slt_good_77 in pre-commit gate.** 3 failures / 9,958 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001921 | tests/sqlcmp/slt | **Pass random/select/slt_good_78 in pre-commit gate.** 4 failures / 9,942 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001922 | tests/sqlcmp/slt | **Pass random/select/slt_good_79 in pre-commit gate.** 3 failures / 9,942 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001923 | tests/sqlcmp/slt | **Pass random/select/slt_good_80 in pre-commit gate.** 7 failures / 9,961 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001924 | tests/sqlcmp/slt | **Pass random/select/slt_good_81 in pre-commit gate.** 3 failures / 9,954 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001925 | tests/sqlcmp/slt | **Pass random/select/slt_good_82 in pre-commit gate.** 4 failures / 9,951 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001926 | tests/sqlcmp/slt | **Pass random/select/slt_good_83 in pre-commit gate.** 4 failures / 9,962 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001927 | tests/sqlcmp/slt | **Pass random/select/slt_good_84 in pre-commit gate.** 4 failures / 9,953 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001928 | tests/sqlcmp/slt | **Pass random/select/slt_good_85 in pre-commit gate.** 9 failures / 9,948 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001929 | tests/sqlcmp/slt | **Pass random/select/slt_good_86 in pre-commit gate.** 4 failures / 9,950 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001930 | tests/sqlcmp/slt | **Pass random/select/slt_good_87 in pre-commit gate.** 7 failures / 9,946 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001931 | tests/sqlcmp/slt | **Pass random/select/slt_good_88 in pre-commit gate.** 6 failures / 9,963 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001932 | tests/sqlcmp/slt | **Pass random/select/slt_good_89 in pre-commit gate.** 4 failures / 9,973 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001933 | tests/sqlcmp/slt | **Pass random/select/slt_good_90 in pre-commit gate.** 2 failures / 9,948 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001934 | tests/sqlcmp/slt | **Pass random/select/slt_good_91 in pre-commit gate.** 5 failures / 9,957 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001935 | tests/sqlcmp/slt | **Pass random/select/slt_good_92 in pre-commit gate.** 6 failures / 9,954 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001936 | tests/sqlcmp/slt | **Pass random/select/slt_good_93 in pre-commit gate.** 4 failures / 9,960 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001937 | tests/sqlcmp/slt | **Pass random/select/slt_good_94 in pre-commit gate.** 7 failures / 9,949 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001938 | tests/sqlcmp/slt | **Pass random/select/slt_good_95 in pre-commit gate.** 5 failures / 9,968 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001939 | tests/sqlcmp/slt | **Pass random/select/slt_good_96 in pre-commit gate.** 8 failures / 9,967 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001940 | tests/sqlcmp/slt | **Pass random/select/slt_good_97 in pre-commit gate.** 2 failures / 9,947 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001941 | tests/sqlcmp/slt | **Pass random/select/slt_good_98 in pre-commit gate.** 7 failures / 9,971 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001942 | tests/sqlcmp/slt | **Pass random/select/slt_good_99 in pre-commit gate.** 6 failures / 9,968 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001943 | tests/sqlcmp/slt | **Pass random/select/slt_good_100 in pre-commit gate.** 5 failures / 9,932 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001944 | tests/sqlcmp/slt | **Pass random/select/slt_good_101 in pre-commit gate.** 4 failures / 9,940 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001945 | tests/sqlcmp/slt | **Pass random/select/slt_good_102 in pre-commit gate.** 4 failures / 9,954 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001946 | tests/sqlcmp/slt | **Pass random/select/slt_good_103 in pre-commit gate.** 9 failures / 9,961 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001947 | tests/sqlcmp/slt | **Pass random/select/slt_good_104 in pre-commit gate.** 7 failures / 9,949 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001948 | tests/sqlcmp/slt | **Pass random/select/slt_good_105 in pre-commit gate.** 7 failures / 9,953 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001949 | tests/sqlcmp/slt | **Pass random/select/slt_good_106 in pre-commit gate.** 7 failures / 9,959 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001950 | tests/sqlcmp/slt | **Pass random/select/slt_good_107 in pre-commit gate.** 3 failures / 9,951 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001951 | tests/sqlcmp/slt | **Pass random/select/slt_good_108 in pre-commit gate.** 6 failures / 9,968 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001952 | tests/sqlcmp/slt | **Pass random/select/slt_good_109 in pre-commit gate.** 7 failures / 9,960 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001953 | tests/sqlcmp/slt | **Pass random/select/slt_good_110 in pre-commit gate.** 5 failures / 9,940 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001954 | tests/sqlcmp/slt | **Pass random/select/slt_good_111 in pre-commit gate.** 5 failures / 9,952 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001955 | tests/sqlcmp/slt | **Pass random/select/slt_good_112 in pre-commit gate.** 6 failures / 9,960 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001956 | tests/sqlcmp/slt | **Pass random/select/slt_good_113 in pre-commit gate.** 9 failures / 9,951 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001957 | tests/sqlcmp/slt | **Pass random/select/slt_good_114 in pre-commit gate.** 13 failures / 9,954 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001958 | tests/sqlcmp/slt | **Pass random/select/slt_good_115 in pre-commit gate.** 12 failures / 9,951 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001959 | tests/sqlcmp/slt | **Pass random/select/slt_good_116 in pre-commit gate.** 4 failures / 9,950 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001960 | tests/sqlcmp/slt | **Pass random/select/slt_good_117 in pre-commit gate.** 7 failures / 9,939 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001961 | tests/sqlcmp/slt | **Pass random/select/slt_good_118 in pre-commit gate.** 9 failures / 9,951 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001962 | tests/sqlcmp/slt | **Pass random/select/slt_good_119 in pre-commit gate.** 8 failures / 9,964 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001963 | tests/sqlcmp/slt | **Pass random/select/slt_good_120 in pre-commit gate.** 4 failures / 9,965 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001964 | tests/sqlcmp/slt | **Pass random/select/slt_good_121 in pre-commit gate.** 5 failures / 9,968 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001965 | tests/sqlcmp/slt | **Pass random/select/slt_good_122 in pre-commit gate.** 9 failures / 9,949 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001966 | tests/sqlcmp/slt | **Pass random/select/slt_good_123 in pre-commit gate.** 6 failures / 9,965 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001967 | tests/sqlcmp/slt | **Pass random/groupby/slt_good_8 in pre-commit gate.** 3 failures / 10,012 records. Root causes: WHERE eval + NULL propagation in HAVING. Fix via REQ001979. When resolved, add `{Label: "random::groupby::slt_good_8", Path: "random/groupby/slt_good_8", Timeout: 60}` to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001968 | tests/sqlcmp/slt | **Pass random/groupby/slt_good_9 in pre-commit gate.** 11 failures / 10,012 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001969 | tests/sqlcmp/slt | **Pass random/groupby/slt_good_10 in pre-commit gate.** 10 failures / 10,012 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001970 | tests/sqlcmp/slt | **Pass random/groupby/slt_good_11 in pre-commit gate.** 40 failures / 10,012 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | small | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001971 | tests/sqlcmp/slt | **Pass random/groupby/slt_good_12 in pre-commit gate.** 828 failures / 10,012 records. Fix via REQ001979. When resolved, add to `includedCases`. | high | medium | REQ001979 | tests/sqlcmp/slt/slt_included_list.go |
| REQ001974 | SQB/EX | **Pool statement-cache LRU elements — 1.29M `list.Element` allocations.** `Executor.putCachedStmt` (`ex.go:516`) calls `c.lru.PushFront(ent)`, and the evict path calls `lru.Remove`/`Back`. Despite the `ex.go:188` comment claiming the LRU is "allocation-free", stdlib `container/list` allocates a `*list.Element` per `PushFront`. With every SELECT inserting into the plan cache, this is 1.29M objects (2.05% of total alloc objects) across `slt_good_109..119`. **Fix:** (1) Replace `container/list` with a slice-indexed LRU (entries hold `prev/next int32` indices, free-list of recycled slots) so insert/evict are allocation-free. (2) Or `sync.Pool` the `*list.Element` wrappers. Verify with `slt_good_109..119` `-alloc_objects` — expected list.Element alloc drop: 1.29M → ~0. Estimate: ~60 lines, 1 file, 0.5 day. | medium | medium | REQ001693 | SQB/EX/ex.go |
| REQ001975 | SQB/AD, SQB/DT | **Eliminate hot-path `fmt.Sprintf` in AdaptiveOp.tryCompile and DT.AggregateLookupKey — 0.85M alloc objects.** `AdaptiveOp.tryCompile` (`adqc.go:162`) computes `opType := fmt.Sprintf("%T", a.Inner)` on every adaptive-op compile, but `opType` is only consumed by a `slog.Debug` call — 524K alloc objects for a debug log that is off by default. `DT.AggregateLookupKey` (`types.go:471`) calls `fmt.Sprintf("%T:%p", e.Arg, e.Arg)` per aggregate with complex args — 328K alloc objects. **Fix:** (1) In `tryCompile`, gate the `opType` computation behind `slog.Enabled(slog.LevelDebug)` (or pass `a.Inner` via a `LogValuer`/`any` in the slog call so slog evaluates `%T` lazily only when enabled). (2) In `AggregateLookupKey`, replace `fmt.Sprintf` with a manual concat: cache the `%T` type name per arg type in a `sync.Map`, append the pointer via `strconv.AppendUint` on `uintptr(unsafe.Pointer(e.Arg))`. Verify with `slt_good_109..119` — expected 0.85M → ~0 alloc objects. Estimate: ~30 lines, 2 files, 0.25 day. | medium | small | none | SQB/AD/adqc.go, SQB/DT/types.go |
| REQ001976 | SQF/PS | **Reduce parser Expr-node allocations — 16.5M objects / ~0.74GB / ~20% CPU.** `Parser.parseUnary` (7.0M objects), `parsePrimary` (5.8M), and `parseBinary` (3.6M) are the top three object allocators across `slt_good_109..119` (expr-heavy workload); together with `Lexer.Next` (1.14s CPU) the parser accounts for ~20% of CPU. Each parsed expression node (`*Ident`, `*NumberLiteral`, `*BinaryExpr`, `*UnaryExpr`, etc.) is a separate heap allocation. The rewriter compounds this: `constantFoldUnaryMinus` (0.88M) and `simplifyBinary` (0.61M) allocate fresh nodes per fold. **Fix:** (1) Allocate parser Expr nodes from a per-parse arena (bump allocator) owned by the `Parser`, discarded after the stmt is consumed by planning; mirror the `cloneArena` pattern (REQ001695). (2) Make `constantFoldUnaryMinus`/`simplifyBinary` mutate in place where safe instead of allocating replacements. (3) Pool the `Parser` itself across statements (reset lexer/arena). Verify with `slt_good_109..119` — expected parser alloc drop: 16.5M → ~2M objects; ~10% CPU reduction. Estimate: ~150 lines, 3 files, 1.5 days. | medium | large | REQ001695 | SQF/PS/expr.go, SQF/PS/parser.go, SQF/RE/rewrite.go |
| REQ001979 | SQB/EV | **Fix NOT/BETWEEN/NOT IN evaluation with complex expressions — 13 failures in slt_good_0.** `NOT col > val` and `NOT ... BETWEEN ... AND ...` and `NOT IN` with computed list items produce wrong row counts. The NOT operator, BETWEEN range check, and NOT IN with complex expressions have evaluation bugs. Affected: L42834, L43419, L55335, L72025, L21739, L83864, L45523, L51121, L61043, L83008, L70936, L88721, L51054. **Fix:** (1) Audit NOT operator evaluation in both compiled filter and Eval paths — ensure NOT correctly inverts the predicate result for non-NULL results. (2) Audit BETWEEN evaluation for computed expressions (e.g. `col2 BETWEEN col0 AND col0 * col2`). (3) Audit NOT IN with computed list items — the hash-set cache fix (REQ001711) only applies to literal lists. (4) Verify with `slt_good_0` — expected failures to drop from 13 to 0. Estimate: ~50 lines, 2-3 files, 0.5 day. | high | medium | REQ001711 | SQB/EV/eval.go, SQB/EV/eval_vec.go, SQB/OP/intermediate_basic.go |
| REQ001981 | SQB/OP | **Vectorize parallel seq scan operators — 4 parallel operators serve rows one at a time.** `ParallelStoreSeqScan` (`operators_parallel.go:49`), `ParallelSeqScanRow` (`operators_parallel.go:184`), `ParallelUnionAll` (`operators_parallel.go:310`), and `ParallelIndexRangeScan` (`operators_parallel.go:762`) all implement only `Next() (Row, error)` with no `NextBatch()` counterpart. These operators pre-materialize results in parallel but then drain them one row at a time via a shared row buffer. This defeats the purpose of parallel execution when downstream operators are vectorized. **Fix:** (1) Add `NextBatch()` methods to all four parallel operators that fill batches from the pre-materialized buffer instead of returning one row. (2) Add these operator types to the `transformOp` switch in `vec_transform.go`. (3) The batch fill should drain the internal buffer up to `BatchSize` (1024) rows per call. (4) Verify with `TestParallelSeqScan*` and a benchmark on `SELECT * FROM big_table` with parallel scan enabled — expected: batch throughput should match non-parallel vectorized scan within 20%. Estimate: ~120 lines, 3 files, 1 day. | high | medium | none | SQB/OP/operators_parallel.go, SQB/EX/vec_transform.go |
| REQ001982 | SQB/AG | **Vectorize ParallelHashAggregate — parallel aggregate has no batch path.** `ParallelHashAggregate.Next()` (`hashagg_parallel.go:41`) materializes all rows from the child, groups them, and serves one result row at a time. Unlike the serial `HashAggregate` which has `VectorizedHashAggregate`, the parallel variant has no `NextBatch()` method and is not in `transformOp`. When the parallel hash agg feeds into a vectorized downstream operator, each row crosses the row→batch adapter boundary, adding unnecessary overhead. **Fix:** (1) Add `NextBatch()` to `ParallelHashAggregate` that drains the grouped results into a batch. (2) Add `ParallelHashAggregate` case to `transformOp` returning a batch wrapper. (3) The batch fill should iterate the grouped result map and emit up to `BatchSize` rows per call. (4) Verify with parallel aggregate benchmarks — expected: eliminates row→batch adapter overhead for parallel agg plans. Estimate: ~80 lines, 2 files, 0.5 day. | medium | medium | none | SQB/AG/hashagg_parallel.go, SQB/EX/vec_transform.go |
| REQ001983 | SQB/WT | **Vectorize INSERT operator — row-at-a-time at operator layer despite batch storage.** `Insert.Next()` (`writers_dml.go:127`) processes one row at a time from `iterValues`, calling `InsertRowBatch` per row for single-row inserts. The storage layer supports `InsertRowBatch` for multi-row batches, but the operator layer never batches multiple INSERT source rows together. For `INSERT INTO t VALUES (1),(2),(3),...,(1000)`, this is 1000 separate storage calls instead of chunked batches. **Fix:** (1) Add `NextBatch()` to `Insert` that drains up to `insertChunkSize` rows from `iterValues` and writes them as a single batch via `InsertRowBatch`. (2) Add `Insert` case to `transformOp` returning a batch-producing wrapper. (3) The vectorized path should reuse the existing `insertChunkSize` batching logic from `nextFromStore` but at the batch interface level. (4) Verify with `BenchmarkSLT_RSel125` and `INSERT INTO t VALUES (...),(...)...` with 10K rows — expected: ~5x throughput improvement for bulk INSERT. Estimate: ~100 lines, 2 files, 0.5 day. | high | medium | none | SQB/WT/writers_dml.go, SQB/EX/vec_transform.go |
| REQ001984 | SQB/WT | **Vectorize UPDATE operator — row-at-a-time iteration over matched rows.** `Update.Next()` (`writers_dml.go:851`) iterates child rows one at a time via `u.iter.Next(ctx)`, applying the SET expression and calling `UpdateRowBatch` per row. For `UPDATE t SET col = col + 1 WHERE id > 1000`, this is row-by-row evaluation and storage write. The storage layer supports `UpdateRowBatch` for multi-row updates. **Fix:** (1) Add `NextBatch()` to `Update` that drains up to `updateChunkSize` child rows, evaluates the SET expressions in batch via `EvalBatchExpr`, and writes via `UpdateRowBatch`. (2) Add `Update` case to `transformOp`. (3) Handle the case where the child is row-based (fall back to `RowOperatorAdapter`). (4) Verify with `UPDATE t SET col = col + 1` on a 10K-row table — expected: ~3x throughput improvement. Estimate: ~120 lines, 2 files, 0.5 day. | high | medium | none | SQB/WT/writers_dml.go, SQB/EX/vec_transform.go |
| REQ001985 | SQB/WT | **Vectorize DELETE operator — row-at-a-time iteration over matched rows.** `Delete.Next()` (`writers_dml.go:1290`) iterates child rows one at a time via `d.iter.Next(ctx)`, calling `DeleteRowBatch` per row. For `DELETE FROM t WHERE col > 1000`, this is row-by-row storage deletion. The storage layer supports `DeleteRowBatch` for multi-row deletes. **Fix:** (1) Add `NextBatch()` to `Delete` that drains up to `deleteChunkSize` child rows and deletes them as a batch via `DeleteRowBatch`. (2) Add `Delete` case to `transformOp`. (3) The batch should collect primary keys from child batches and pass them to `DeleteRowBatch` in one call. (4) Verify with `DELETE FROM t WHERE id > 1000` on a 10K-row table — expected: ~3x throughput improvement. Estimate: ~80 lines, 2 files, 0.5 day. | high | medium | none | SQB/WT/writers_dml.go, SQB/EX/vec_transform.go |
| REQ001986 | SQB/EV | **Reduce expression eval batch→row fallback — batchToRow allocates per row.** `eval_vec.go:968-998` defines `batchToRow()` which converts a single batch row to a `Row` for the per-row `Eval()` fallback. This is used when `EvalBatchExpr()` cannot handle an expression (line 1131: "On eval failure, fall through to per-row fallback"). Each call allocates a fresh `Row` with copied columns and data. For workloads with many complex expressions (CASE, subqueries, function calls), this fallback is hit frequently and generates significant per-row allocation overhead. **Fix:** (1) Pool the `Row` returned by `batchToRow()` — use a `sync.Pool` of `Row` structs sized to the expression's output width. (2) For expressions that always fall back, detect this at plan time and skip the batch→row→batch round-trip entirely (keep the expression in row mode). (3) Audit which expression types trigger fallback and add batch support for the most common ones (e.g., IN with subquery). (4) Verify with slt corpus — expected: reduced alloc_objects for workloads with complex expressions. Estimate: ~60 lines, 2 files, 0.5 day. | medium | medium | none | SQB/EV/eval_vec.go, SQB/EX/vec_transform.go |
| REQ001987 | SQB/OP | **Vectorize Sort materialization — sort drains child row-by-row before batching output.** `Sort.Next()` (`intermediate_sort.go:78`) materializes all child rows into a slice via `for { row, err := child.Next(ctx) }` (line 87), sorts the slice, then serves one row at a time. Although `VectorizedSort` exists and is used when `transformOp` succeeds, the row-based `Sort.materialize()` still drains the child one row at a time. When the child is a `BatchProducer`, the materialize loop should consume batches instead. **Fix:** (1) In `Sort.materialize()`, detect if the child implements `BatchProducer` and drain via `NextBatch()` instead of `Next()`. (2) Append batch rows to the materialization slice in bulk (copy batch columns directly). (3) This reduces the row→batch adapter overhead for sort inputs. (4) Verify with `SELECT * FROM t ORDER BY col` on 10K rows — expected: ~2x faster materialization when child is vectorized. Estimate: ~40 lines, 1 file, 0.25 day. | medium | small | none | SQB/OP/intermediate_sort.go |
| REQ001988 | SQB/OP | **Vectorize Distinct materialization — distinct drains child row-by-row before batching output.** `Distinct.Next()` (`distinct.go:52`) materializes all child rows via `for { row, err := child.Next(ctx) }` (line 61), deduplicates via hash set, then serves one row at a time. Although `VectorizedDistinct` exists, the row-based path still drains child one row at a time. When the child is a `BatchProducer`, the materialize loop should consume batches. **Fix:** (1) In `Distinct.materialize()`, detect `BatchProducer` child and drain via `NextBatch()`. (2) Deduplicate within each batch before appending to the materialization set (reduces hash lookups). (3) Verify with `SELECT DISTINCT col FROM t` on 10K rows — expected: ~2x faster materialization when child is vectorized. Estimate: ~40 lines, 1 file, 0.25 day. | medium | small | none | SQB/OP/distinct.go |
| REQ001989 | SQB/AG | **Vectorize Aggregate materialization — aggregate drains child row-by-row before grouping.** `Aggregate.Next()` (`aggregate.go:87`) materializes all child rows via `for { row, err := child.Next(ctx) }` (line 119-130), groups them, then serves one group at a time. Although `VectorizedHashAggregate` exists, the row-based `Aggregate.materialize()` still drains child one row at a time. When the child is a `BatchProducer`, the materialize loop should consume batches. **Fix:** (1) In `Aggregate.materialize()`, detect `BatchProducer` child and drain via `NextBatch()`. (2) Feed batch rows directly to the grouping state without per-row `Row` construction. (3) Verify with `SELECT col, COUNT(*) FROM t GROUP BY col` on 10K rows — expected: ~2x faster materialization when child is vectorized. Estimate: ~50 lines, 1 file, 0.25 day. | medium | small | none | SQB/AG/aggregate.go |
| REQ001990 | SQB/EV | **Vectorize IN with subqueries — batch-mode hash-set instead of per-row Eval.** `IN (SELECT ...)` with a subquery currently evaluates the subquery, builds a hash set, then checks each row via `Eval()`. The subquery materialization is row-at-a-time (`materialize()` drains child via `Next()`), and the hash-set lookup calls `Eval()` per row. For `SELECT * FROM t WHERE col IN (SELECT id FROM s)` with 10K×10K, this is 100M per-row `Eval` calls. **Fix:** (1) Materialize the subquery via `NextBatch()` when the child is a `BatchProducer` (uses REQ001989's batch drain). (2) Build the hash set from batch columns directly without per-row `Row` construction. (3) For the probe side, evaluate `col` in batch via `EvalBatchExpr` and probe the hash set per-batch. (4) Add `evalBatchINSubquery` to `eval_vec.go`. (5) Verify with `SELECT * FROM t WHERE col IN (SELECT id FROM s)` — expected: ~5x throughput improvement for large IN-subquery. Estimate: ~120 lines, 3 files, 1 day. | high | medium | REQ001986, REQ001989 | SQB/EV/eval_vec.go, SQB/EV/eval.go, SQB/AG/aggregate.go |
| REQ001991 | SQB/EV | **Extend CASE WHEN batch evaluation to complex conditions.** `evalCaseBatchExpr` (`eval_vec.go`) handles simple `WHEN literal THEN expr` patterns but falls back to `batchToRow()` for complex WHEN conditions (subqueries, nested CASE, function calls). The fallback is hit for ~15% of CASE expressions in the slt corpus. **Fix:** (1) Extend `evalCaseBatchExpr` to handle `WHEN <binary_expr>` and `WHEN <unary_expr>` patterns without fallback. (2) For WHEN conditions that reference the same column, batch-evaluate the condition once and use the selection vector for the THEN branch. (3) Add batch support for `WHEN <func_call>` where the function is pure (no side effects). (4) Verify with slt corpus — expected: CASE fallback hits drop from ~15% to <2%. Estimate: ~80 lines, 1 file, 0.5 day. | medium | medium | REQ001986 | SQB/EV/eval_vec.go |
| REQ001992 | SQB/EV | **Batch materialization of scalar subqueries — eliminate per-row subquery re-evaluation.** Scalar subqueries in SELECT lists (e.g., `SELECT col, (SELECT COUNT(*) FROM s WHERE s.id = t.id) FROM t`) are currently evaluated per-row via `Eval()`. Each row triggers a new subquery execution. The `ScalarSubqueryOp` (`explain.go:122`) wraps the subquery and returns one row per `Next()` call. **Fix:** (1) Add `VectorizedScalarSubquery` that materializes the subquery result into a batch (one column, one value per parent row). (2) Add case to `transformOp`. (3) For correlated subqueries, batch the correlation columns from the parent and execute the subquery once per unique correlation key. (4) Verify with `SELECT col, (SELECT COUNT(*) FROM s WHERE s.id = t.id) FROM t` on 10K rows — expected: eliminates 10K subquery executions, ~10x speedup. Estimate: ~100 lines, 2 files, 0.5 day. | high | medium | REQ001986 | SQB/EV/eval_vec.go, SQB/EX/ex.go |
| REQ001993 | SQB/AG | **Batch evaluation for GROUP_CONCAT and STRING_AGG — currently row-at-a-time accumulation.** `VectorizedHashAggregate` handles COUNT/SUM/AVG/MIN/MAX in batch mode but falls back to row-at-a-time for `GROUP_CONCAT` and `STRING_AGG` because string concatenation requires per-row buffer append. These aggregates are common in reporting queries. **Fix:** (1) Add `VectorizedGroupConcat` that accumulates string values into a per-group `strings.Builder` fed from batch columns. (2) Handle ORDER BY within GROUP_CONCAT by buffering `(sortKey, value)` pairs per group, sorting at finalization. (3) Add `VectorizedStringAgg` with the same approach. (4) Verify with `SELECT grp, GROUP_CONCAT(val ORDER BY val) FROM t GROUP BY grp` — expected: batch throughput matches numeric aggregates within 30%. Estimate: ~80 lines, 2 files, 0.5 day. | medium | medium | REQ001982 | SQB/AG/aggregate_vec.go, SQB/AG/hashagg.go |
| REQ001994 | SQB/EV | **Add eval fallback hit counter — instrument batch→row fallback for visibility.** The `batchToRow()` fallback in `eval_vec.go:968` is hit silently for unsupported expressions. There is no visibility into how often it fires or which expressions trigger it. This makes it impossible to prioritize which expressions to vectorize next. **Fix:** (1) Add a `fallbackHits atomic.Int64` counter to the execution context. (2) Increment it in `batchToRow()` and in the `EvalBatchExpr` error path. (3) Add a `PRAGMA eval_fallback_stats` that returns the counter value and resets it. (4) Log the counter at `Debug` level at end of each statement. (5) Verify with `PRAGMA eval_fallback_stats` — expected: non-zero for workloads with complex expressions, zero for fully-vectorized plans. Estimate: ~30 lines, 2 files, 0.25 day. | low | small | none | SQB/EV/eval_vec.go, SQB/EX/ex.go |
| REQ001995 | ENG/LS | **SST block batch read — decode entire data block into columnar format in one pass.** `sstIterator.Next()` (`sst_reader.go:825`) reads one key-value pair per `Next()` call, decoding the value each time. `SeqScan.nextColumnarBatch()` (`operators.go:1102`) loops `it.Next()` 1024 times to fill a batch, paying per-row decode overhead (key comparison, value deserialization, column dispatch). For a 4KB data block containing ~50 rows, this is 50 `Next()` calls with repeated block-header parsing. **Fix:** (1) Add `ReadBlockBatch()` to `sstReader` that reads an entire data block and decodes all rows into a `Batch` in one pass. (2) The method should parse the block header once, then iterate the row entries without re-parsing the block envelope. (3) Return the batch with column pointers directly referencing the decoded block data (zero-copy where possible). (4) Wire into `VectorizedSeqScan.NextBatch()` as the primary read path. (5) Verify with `SELECT * FROM t` on 100K rows — expected: ~2x throughput improvement for seq scan, ~30% reduction in CPU time for decode. Estimate: ~150 lines, 3 files, 1 day. | high | large | none | ENG/LS/sst_reader.go, SQB/OP/operators_vec.go, SQB/OP/operators.go |
| REQ001996 | ENG/LS | **Bloom filter pushdown — skip SST blocks that cannot match the predicate.** Each SST file has a bloom filter for its key range. Currently the bloom filter is checked per-SST in the merge iterator (`mergeIterator` in `api.go:496`), but not per-block within an SST. For `SELECT * FROM t WHERE pk = 42`, if the bloom filter says the key is not in the SST, the entire SST is skipped. But within an SST, every block is still read and decoded. **Fix:** (1) Add per-block bloom filter or first/last key check in `sstReader`. (2) Before calling `ReadBlockBatch()` (REQ001995), check if the block's key range can contain the search key. (3) For range scans, skip blocks whose max key is before the scan start. (4) For point lookups, use the SST's block index to jump directly to the matching block. (5) Verify with `SELECT * FROM t WHERE pk = X` on a 1M-row table — expected: ~5x reduction in blocks read for point lookups. Estimate: ~80 lines, 2 files, 0.5 day. | high | medium | REQ001995 | ENG/LS/sst_reader.go, ENG/LS/index.go |
| REQ001997 | ENG/LS | **Prefix-key scan optimization — skip to start block and stop at end block for BETWEEN.** For `SELECT * FROM t WHERE pk BETWEEN 100 AND 200`, the current scan starts from the first block and iterates forward. The SST block index knows the key range of each block. The scan should jump directly to the block containing key 100 and stop when the block's min key exceeds 200. **Fix:** (1) Use the SST block index (`blockIndex` in `sst_reader.go`) to binary-search for the start block. (2) Set the iterator position to the start of that block. (3) In `nextColumnarBatch()`, stop when the batch's first key exceeds the scan end. (4) For prefix scans (`WHERE pk LIKE 'abc%'`), skip blocks whose prefix doesn't match. (5) Verify with `SELECT * FROM t WHERE pk BETWEEN X AND Y` on a 1M-row table — expected: ~10x reduction in blocks scanned for narrow ranges. Estimate: ~60 lines, 2 files, 0.5 day. | high | medium | REQ001995 | ENG/LS/sst_reader.go, SQB/OP/operators.go |
| REQ001998 | ENG/LS | **Columnar memtable — in-memory skiplist stores columnar format for recent writes.** The current memtable (`skiplist.go:210`) stores key-value pairs in row format. When `VectorizedSeqScan` reads hot data, it decodes every row from the memtable via `it.Next()`. For write-heavy workloads, the memtable is the primary read source. Storing recent writes in columnar format eliminates the decode cost. **Fix:** (1) Add a `columnarMemtable` that stores recent writes as column arrays (one array per column). (2) The columnar memtable sits in front of the skiplist — reads check it first. (3) When the columnar memtable exceeds a threshold (e.g., 10K rows), flush it to an SST and reset. (4) Writes go to both the skiplist (for WAL recovery) and the columnar memtable (for fast reads). (5) Verify with write-heavy SLT benchmarks — expected: ~2x throughput for read-after-write workloads. Estimate: ~200 lines, 4 files, 2 days. | high | large | REQ001995 | ENG/LS/skiplist.go, ENG/LS/memtable.go, ENG/LS/engine.go |
| REQ001999 | SQB/OP | **Grace hash join — spill-to-disk for large build sides.** `VectorizedHashJoin` (`op_vec_join.go:137`) fully materializes the build side in memory. For `SELECT * FROM big_t JOIN small_t ON ...` where `big_t` is the build side and exceeds available memory, the join OOMs. **Fix:** (1) Estimate build-side size from table stats (row count × avg row width). (2) If estimated size exceeds a memory threshold (e.g., 25% of GOMAXPROCS × per-proc memory), switch to grace hash join mode. (3) Partition both sides by hash of the join key into N partitions (N = ceil(estimated_size / threshold)). (4) Spill partitions to temp files. (5) For each partition pair, build a hash table from the (smaller) build partition and probe with the probe partition. (6) Verify with a 1M×1K join where the build side exceeds memory — expected: no OOM, ~80% of in-memory throughput. Estimate: ~300 lines, 4 files, 3 days. | high | large | REQ001981 | SQB/OP/op_vec_join.go, SQB/OP/hashjoin.go |
| REQ002000 | SQB/OP | **Columnar build-side hash table — store hash table entries as batch columns, not row slices.** `VectorizedHashJoin` (`op_vec_join.go:137`) stores the build side as `[]Row` in the hash table. Each probe must reconstruct a `Row` from the hash entry. Storing entries as columnar batches (one batch per hash bucket) eliminates Row construction and enables cache-friendly iteration. **Fix:** (1) Replace `map[uint64][]Row` with `map[uint64]*ColumnarGroup` where `ColumnarGroup` holds `[]UT.Column` arrays. (2) During build, append batch columns directly to the group. (3) During probe, iterate column arrays instead of Row slices. (4) This also enables vectorized equality comparison (compare entire column arrays). (5) Verify with `SELECT * FROM t1 JOIN t2 ON t1.id = t2.id` on 100K×100K — expected: ~30% probe throughput improvement, ~20% memory reduction. Estimate: ~120 lines, 2 files, 1 day. | high | medium | REQ001981 | SQB/OP/op_vec_join.go |
| REQ002001 | SQB/OP | **Parallel hash table build — build the hash table from partitioned child output.** `VectorizedHashJoin` builds the hash table serially from the child's `NextBatch()` output. For large build sides, the build phase is the bottleneck. The build child may already be parallel (via REQ001981), but the hash table construction is single-threaded. **Fix:** (1) Partition the build child's output by hash of the join key into P partitions (P = GOMAXPROCS). (2) Build P independent hash tables in parallel goroutines. (3) Each goroutine processes a disjoint subset of batches. (4) During probe, hash the probe key and route to the correct partition's hash table. (5) Verify with `SELECT * FROM t1 JOIN t2 ON t1.id = t2.id` on 1M×1K — expected: build phase throughput scales linearly with GOMAXPROCS up to 8 cores. Estimate: ~100 lines, 2 files, 1 day. | high | medium | REQ002000 | SQB/OP/op_vec_join.go |
| REQ002002 | SQB/EX | **Push-based execution between operators — producer pushes batches to consumer.** The current execution model is volcano-style pull: each operator calls `child.NextBatch()` to pull data. This incurs virtual dispatch overhead per batch handoff and prevents operator fusion. A push-based model where the producer calls `consumer.ProcessBatch(batch)` eliminates the pull overhead and enables fused operators. **Fix:** (1) Add a `PushOperator` interface: `ProcessBatch(ctx, *Batch) error`. (2) Implement `PushFilter`, `PushProject`, `PushLimit` that process batches inline. (3) Add a `PushPipeline` that chains push operators without batch allocation between them. (4) The pull-based `NextBatch()` wraps the push pipeline for backward compatibility. (5) Verify with `SELECT * FROM t WHERE col > 100 LIMIT 1000` — expected: ~2x throughput for fused filter+project+limit. Estimate: ~200 lines, 4 files, 2 days. | high | large | REQ001986 | SQB/EX/vec_transform.go, SQB/OP/operators_vec.go, SQB/OP/intermediate_basic.go, SQB/OP/intermediate_limit.go |
| REQ002003 | SQB/EX | **Operator fusion for Filter+Project+Limit — single function call, no batch handoff.** After push-based execution (REQ002002), fuse the three most common operator combinations into tight loops: (1) Scan→Filter→Project, (2) Scan→Filter→Limit, (3) Scan→Filter→Project→Limit. Each fusion eliminates a batch allocation, a virtual dispatch, and a selection vector copy. **Fix:** (1) Implement `FusedScanFilterProject` that reads batches from the scan, applies the filter predicate, and projects columns in one pass. (2) Implement `FusedScanFilterProjectLimit` that additionally stops after `limit` rows. (3) The fused operators should use `EvalBatchExpr` for the filter and `EvalBatchExpr` for the project, sharing the same batch. (4) Add detection in `tryVectorizePlan` that matches Scan→Filter→Project→Limit plan shapes and substitutes the fused operator. (5) Verify with `SELECT col1, col2 FROM t WHERE col3 > 100 LIMIT 1000` — expected: ~3x throughput vs non-fused pipeline. Estimate: ~150 lines, 3 files, 1 day. | high | medium | REQ002002 | SQB/EX/vec_transform.go, SQB/OP/operators_vec.go, SQB/OP/fused.go |
| REQ002004 | SQB/OP | **Vectorized sort-merge pipeline — sort produces sorted runs, merge join consumes without materializing both sides.** `VectorizedMergeJoin` (`mergejoin.go:124`) fully materializes both children before merging. For large tables, this doubles memory usage. A sort-merge pipeline where the sorted streams are consumed lazily eliminates the full materialization. **Fix:** (1) Change `VectorizedMergeJoin` to accept two sorted batch iterators instead of materialized slices. (2) The sort operator should produce sorted runs (batches sorted within each run). (3) The merge join consumes the two iterators, advancing whichever has the smaller current key. (4) Handle duplicate keys by buffering one side's groups. (5) Verify with `SELECT * FROM t1 JOIN t2 ON t1.id = t2.id ORDER BY t1.id` on 100K×100K — expected: ~50% memory reduction, ~20% throughput improvement. Estimate: ~150 lines, 3 files, 1 day. | high | medium | REQ001987 | SQB/OP/mergejoin.go, SQB/OP/intermediate_sort.go, SQB/OP/op_vec_join.go |
