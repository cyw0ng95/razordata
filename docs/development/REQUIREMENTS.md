## TBD
| ID | Subsystem | Requirement | Priority | Effort | Deps | Touches |
| --- | --- | --- | --- | --- | --- | --- |
| REQ001645 | SQB/OP | **Generalize parallel hash join to support arbitrary probe-side batch producers.** Current `ParallelHashJoin` (`parallel_hashjoin.go`) requires the probe side to be a `BatchProducer` and the build side to be materializable. It spawns N workers via `WorkerPool` to probe the hash table in parallel. This works for simple cases but doesn't support: (1) Nested loop join parallelization, (2) Merge join parallelization, (3) Adaptive worker count based on data size. **Fix:** (1) Extract a `ParallelProbe` utility in `SQB/UT/parallel.go` that takes a hash table, a probe batch producer, and a worker count. (2) Re-implement `ParallelHashJoin` using `ParallelProbe`. (3) Add a `Parallelism` parameter to `VectorizedHashJoin` that can be set by the planner when the build side is large (> 10K rows). (4) Add `BenchmarkParallelHashJoin_LargeBuild` that measures speedup on a 100K-row build side. Estimate: ~150 lines, 3 files, 1 day. | low | medium | REQ001587, REQ001622 | SQB/OP/parallel_hashjoin.go, SQB/UT/parallel.go, SQB/OP/op_vec_join.go |
| REQ001656 | SQB/EX | **Cross join with unaliased table references produces too few rows.** SLT corpus `random/select/slt_good_124` has 168 failures (out of 4790) where cross-join queries return fewer rows than expected. The pattern is consistent: result row count is 3× too small (e.g., got 3 want 9, got 9 want 27). All affected queries have unaliased table references in the FROM clause (e.g., `FROM tab0, tab1 AS cor0`). The engine appears to treat unaliased tables as single-row or not correctly expanding the Cartesian product. **Fix:** (1) Investigate how the executor handles `FROM tab0, tab1 AS cor0` — specifically whether unaliased tables are correctly registered in the cross-join expansion. (2) Fix the row-count discrepancy so that `tab0` (3 rows) × `tab1` (3 rows) = 9 rows, not 3. (3) Verify fix against all 168 failing cases in `slt_good_124`. Estimate: ~40 lines, 1-2 files, 0.5 day. | critical | small | none | SQB/EX/ |
| REQ001663 | SQB/UT | **Reuse BatchToRowAdapter's per-row slice buffer across refills.** `BatchToRowAdapter.Next` (`batch_adapter.go:53`) calls `b.ToRows()` which allocates a fresh `make([]pl.Row, 0, logical)` (`batch.go:639`) plus `make([]Value, len(names))` per row (`batch.go:648`). When the next batch arrives, the previous slice is GC'd. With select2 the adapter is the dominant alloc site: 11.54GB / 63.22% cum. **Fix:** (1) Add `rowBuf []pl.Row` field to `BatchToRowAdapter`. (2) Have `ToRows` (or a new `ToRowsInto(buf)` variant) write into the supplied buffer, growing only when needed. (3) Drop the per-batch `a.rows = b.ToRows()` in favor of `b.ToRowsInto(a.rowBuf)`. (4) Same for `UT.Batch.ToRowsShared` — make it the default. Estimate: ~60 lines, 2 files, 0.5 day. | medium | medium | REQ001440 | SQB/UT/batch_adapter.go, SQB/UT/batch.go |
| REQ001664 | SQB/EV | **Memoize per-batch column references in EvalBatchExpr to eliminate redundant subtree evaluation.** `evalArithBatch` (`eval_vec.go:1256-1258`) recursively calls `EvalBatchExpr(e.Left, ...)` and `EvalBatchExpr(e.Right, ...)`. For an expression like `a+b*2+c*3+d*4+e*5` over 30 rows, each shared column ref re-walks the BinaryExpr subtree and re-runs `ExtractColumnRef`. In select2 this costs 1.98s of CPU on the left subtree alone (4.47s EvalBatchExpr total cum). The CASE case (`evalCaseBatchExpr`) shows similar redundancy: each `WHEN` clause re-evaluates shared subexpressions across batches. **Fix:** (1) Add a per-batch memo map `map[exprID]UT.Column` keyed by `*Expr` pointer that lives for one `EvalBatchExpr` call. (2) Memoize `*PS.Ident` column refs (already cheap, but extend to other cheap-to-evaluate exprs). (3) At minimum, detect identical `*PS.Ident` repeated inside one batch's expression tree (CSE) and evaluate once. (4) For complex CSE detection: add a `compileMemoizedBatchExpr` pass that pre-computes the memo plan. Estimate: ~100 lines, 2 files, 1 day. | medium | medium | none | SQB/EV/eval_vec.go |
| REQ001667 | SQB/OP | **Eliminate duplicate key copy in SeqScan.nextFromStore.** `nextFromStore` (`operators.go:758-761`) copies the iterator key into `keyBuf`, then `append([]byte(nil), s.currentKey...)` allocates a second independent copy for `storeKey`. Two allocations per row from a 30-row table × thousands of iterations = 240ms cumulative in select2. **Note:** A naive approach (skipping the second copy when `needsStableKey` is false) was attempted in commit 01c0fe1b and reverted in 9b42ec18 because the UPDATE path silently depends on a stable StoreKey even when not flagged. A correct fix requires the planner to propagate a `needsStableKey` flag through the operator tree and only skip the copy when the scan feeds a read-only subtree (no Update/Delete downstream). **Fix:** (1) Add a `needsStableKey` field to SeqScan, default true. (2) When the planner builds a pure read-only plan (no Update/Delete), set it to false. (3) When false, skip `append([]byte(nil), s.currentKey...)` and point `storeKey` at `s.currentKey` directly. (4) Verify with `TestSLT_InList/update` (the must-not-regress case). Estimate: ~30 lines, 2 files, 0.25 day. | low | small | REQ001583 | SQB/OP/operators.go |
| REQ001668 | ENG/LS | **Skip heap pop in mergeIterator dedup loop to reduce struct churn.** `mergeIterator.Next` (`api.go:711-722`) pops the top, then for each subsequent equal-key entry pops + copies + pushes back into the heap. The push at line 716-720 constructs `iterHeapItem{key: ..., value: ..., src: ...}` — a struct value, no alloc, but the `bytes.Equal` + pop + push pattern causes cache misses and is 240ms flat in select2 cum when a single source dominates. **Fix:** (1) Replace the `pop`+`push back` dedup pattern with a `peek`+`advance` pattern: look at `mi.h.items[0]` without popping, advance the source, and only push when the new key is greater than the current top. (2) Verify behavior against SLT select5/select6 (multi-source merge cases). Estimate: ~40 lines, 1 file, 0.5 day. | low | small | REQ001261 | ENG/LS/api.go |
| REQ001673 | SQB/EX | **Use `clear()` instead of `make(map, cap)` in cache reset paths to eliminate 72% of SLT rsel125 reset-time allocations.** SLT random/select/slt_good_125 and slt_good_126 are pure DML setup files (3 CREATE + 9 INSERT each), run repeatedly in `TestSLT_InList` via `driver.Reset()` between iterations. pprof alloc_space shows that **72% (57MB / 78.57MB)** of all mem allocations in `BenchmarkSLT_RSel125` come from the three reset paths: (1) `Planner.clearMemoLocked` (`planner.go:224-225`): `p.memo = make(map[string]*plan, maxPlanCacheSize)` + `p.memoOrder = make([]string, maxPlanCacheSize)` — 23.05MB. (2) `Executor.clearStmtCache` (`ex.go:527`): `e.stmtCache.entries = make(map[string]*stmtCacheEntry, 1024)` — 17.45MB. (3) `Executor.clearTextPlanCache` (`ex.go:1310`): `e.textPlanCache.entries = make(map[string]*textPlanEntry, e.textPlanCache.maxSize)` — 13.34MB. None of these need to reallocate — they should `clear()` the existing map and reset the access counter. **Fix:** (1) Replace `m = make(map[K]V, cap)` with `clear(m)` in all three reset paths. (2) Verify with `BenchmarkSLT_RSel125` and confirm `clearMemoLocked`/`clearStmtCache`/`clearTextPlanCache` allocations drop ~95%. (3) Verify no behavioral regression via `TestSLT_InList` (all 23 included cases pass). Estimate: ~10 lines, 2 files, 0.1 day. | high | small | none | SQB/EX/planner.go, SQB/EX/ex.go |
| REQ001674 | ENG/LS | **Use `clear()` instead of `make()` in PageCache.Reset to avoid re-allocating the shard index map.** `PageCache.Reset` (`page_cache.go:205`) iterates over every shard and at line 209 calls `s.index = make(map[pageKey]*pageEntry, min(s.cap, 64))` for each shard — 5.52MB / 7.02% of `BenchmarkSLT_RSel125` total allocations. The shard already has a sized map from construction; reallocating it on every engine Reset throws away the page cache's warm-up state and forces GC to walk fresh empty maps on subsequent populates. **Fix:** (1) Replace `s.index = make(...)` with `clear(s.index)`. (2) Verify with `BenchmarkSLT_RSel125` and that no shard loses prior capacity under load (verify capacity is preserved across Reset cycles via a benchmark). Estimate: ~5 lines, 1 file, 0.05 day. | medium | small | none | ENG/LS/page_cache.go |
| REQ001675 | SQB/WT | **Pool Insert's scratch slices (bigBuf, insertBuf) and colIdx across INSERT statements.** `Insert.nextFromStore` (`writers_dml.go:393-418`) allocates per-INSERT: `colIdx := make([]int, len(i.cols))` (line 394), `bigBuf = make([]DT.Value, nRows*nCols)` (line 412, only when nRows>1), and `insertBuf := make([]DT.Row, 0, insertChunkSize)` (line 418). For a single-row INSERT (the common case in SLT rsel125/126), `bigBuf` is skipped but `colIdx` and `insertBuf` are allocated fresh each call. Even though individual INSERT alloc is small (~3 small slices × 12 SQLs × 348 iterations ≈ 12K allocations per benchmark), the cumulative GC pressure is non-trivial for workloads that run many INSERTs. **Fix:** (1) Add `scratchColIdx []int` and `scratchInsertBuf []DT.Row` fields to `*Insert`. (2) Reuse via re-slice, grow via `slices.Grow` if capacity insufficient. (3) For `bigBuf`, only allocate when the previous capacity is too small; otherwise re-slice and zero. (4) Verify with `BenchmarkSLT_RSel125` (no regression — likely 5-10% drop in INSERT B/op). Estimate: ~30 lines, 1 file, 0.25 day. | low | small | none | SQB/WT/writers_dml.go |
| REQ001676 | SQB/WT | **Pool Update.nextFromStore's oldBuf/newBuf scratch buffers to eliminate 339MB per-benchmark alloc.** `Update.nextFromStore` (`writers_dml.go:1004-1005`) allocates `oldBuf := make([]DT.Row, 0, updateChunkSize)` and `newBuf := make([]DT.Row, 0, updateChunkSize)` (each cap=256) inside the function body on every call. For `BenchmarkSLT_Update` (378 iterations × 13 UPDATEs), this generates 181MB + 158MB = 339MB flat alloc (27% of total). The buffers are flushed and re-allocated per chunk — never reused across calls. **Fix:** (1) Add `scratchOldBuf []DT.Row` and `scratchNewBuf []DT.Row` to `*Update`. (2) In `nextFromStore`, reuse via re-slice; grow via `slices.Grow` when capacity insufficient. (3) Verify with `BenchmarkSLT_Update` — expected B/op drop: 3.46MB → ~2.5MB (-28%). (4) Check no regression via `TestSLT_InList/update`. Estimate: ~25 lines, 1 file, 0.25 day. | high | small | none | SQB/WT/writers_dml.go |
| REQ001677 | SQB/DT | **Reduce RowArena initial slab size to fit within arenaSlabPool threshold for small tables.** `SeqScan.decodeRowBuffered` (`operators.go:857`) initializes the arena with `engineBatchSize * len(s.schema.Cols)` = 256 * 5 = 1280 Values. The `arenaSlabSize` threshold is ~910 Values (64KB / sizeof(Value)). Since 1280 > 910, the slab falls outside the pool capacity and `getSlab` allocates a fresh slab on every Query call — 460MB flat / 36.67% of `BenchmarkSLT_Update` total alloc. For small tables (< 100 rows), the full batch size is never reached. **Fix:** (1) Cap the arena initial size at `min(engineBatchSize, actualTableRowCount) * nCols` when the planner knows the row count. (2) Default to `min(engineBatchSize, 64) * nCols` when the row count is unknown — 64 rows × 5 cols = 320 Values, well within the 910-slab pool threshold. (3) The arena grows geometrically on overflow, so under-sizing is safe (1 extra grow per 1000-row table). (4) Verify with `BenchmarkSLT_Update` and `BenchmarkSelect3_Queries/star` — no regression, B/op reduction expected. Estimate: ~15 lines, 2 files, 0.25 day. | medium | small | none | SQB/DT/arena.go, SQB/OP/operators.go |
| REQ001678 | SQB/EX | **Use lazy allocation in drainPlanRows for small result sets.** `drainPlanRows` (`drain_batch.go:97`) pre-allocates `out := make([]DT.Row, 0, OP.EngineBatchSize())` = `make([]DT.Row, 0, 256)` — 250MB flat / 19.97% of `BenchmarkSLT_Update` total alloc. For SELECT verification queries in the UPDATE corpus (e.g. `SELECT count(*) FROM t1 WHERE x=3`), the result is always 1 row. The 256-row pre-alloc is wasted. **Fix:** (1) Use a smaller initial cap: `min(OP.EngineBatchSize(), 8)` or start with 0 and let `append` grow naturally. (2) For the common case of small result sets (< 8 rows), this saves 97% of the pre-alloc. (3) For large result sets, the `append` growth is amortized. (4) Verify with `BenchmarkSLT_Update` and `BenchmarkSelect3_Queries/star` — B/op expected to drop ~20% on update bench. Estimate: ~3 lines, 1 file, 0.05 day. | medium | small | none | SQB/EX/drain_batch.go |
| REQ001679 | ENG/LS | **Pool flate.Writer across SST blocks to eliminate per-block compression alloc.** `compressBlockDictShared` (`sst_dict.go:85`) calls `flate.NewWriterDict(&compressed, flate.BestSpeed, dict)` for every SST block, which allocates 82.85MB (38.70% of `BenchmarkSLT_Idx1000_2` total alloc) and 69.50MB for the internal compressor init. With 5000 rows across 5 tables and 10+ indexes, idx1000 generates 50+ SST blocks per iteration. The `flate.Writer` supports `Reset(w io.Writer, dict []byte)` which reinitializes internal buffers without re-allocating the compressor structures. **Fix:** (1) Add a `sync.Pool` for `*flate.Writer` in the SST writer scope. (2) On each block compress, Get from pool, call `Reset(&compressed, dict)`, Write/Close, then Put back. (3) Handle the `Reset` error by falling back to `NewWriterDict`. (4) Verify with `BenchmarkSLT_Idx1000_2` — expected B/op drop: ~34.6MB → ~20MB (-42%). (5) Verify no regression via `TestSLT_InList/idx1000_2/3/4`. Estimate: ~30 lines, 1 file, 0.25 day. | high | small | none | ENG/LS/sst_dict.go |
| REQ001680 | SQB/DT | **Eliminate deep copy of index columns in GetRegisteredIndexes to save 11.79% allocs.** `GetRegisteredIndexes` (`schema.go:277-285`) creates a deep copy of every index entry on every call: `out := make([]RegisteredIndex, len(src))` and `Columns: append([]string(nil), idx.Columns...)`. For idx1000 with 15+ indexes and 5 tables × 1000 INSERTs, this generates 71K alloc objects (11.79% of total). The caller (`MaintainIndexesOnInsert`) reads the columns but never mutates them. **Fix:** (1) Return `src` slice directly (already protected by `StoreMu` lock). (2) If caller-safety is required, use a `sync.Pool` for the `[]RegisteredIndex` slice. (3) Verify with `BenchmarkSLT_Idx1000_2` — expected allocs/op drop: 734K → ~650K (-11%). (4) Check no regression via `TestSLT_InList/idx1000_2/3/4`. Estimate: ~10 lines, 1 file, 0.1 day. | high | small | none | SQB/DT/schema.go |
| REQ001681 | SQB/WT | **Eliminate per-row Cols/Data copy in buildInsertRowFromSelectWithMap for INSERT...SELECT.** `buildInsertRowFromSelectWithMap` (`writers_dml.go:696-718`) allocates per-row copies of Cols and Data: `out.Cols = append([]string(nil), schema...)` (line 699), `out.Data = make([]DT.Value, len(schema))` (line 700), or in the no-column-list path: `out.Cols = append([]string(nil), src.Cols...)` (line 715), `out.Data = append([]DT.Value(nil), src.Data...)` (line 716). For idx1000's 5000 INSERT...SELECT rows, this is 11.8K allocs. The schema is stable across all rows; the Data slice must be fresh per row. **Fix:** (1) Share the Cols slice across all rows (it's never mutated per-row). (2) Only allocate a fresh Data slice. (3) For the column-list path, build the `colIdx` map once per Insert.nextFromSelect call instead of per-row. (4) Verify with `BenchmarkSLT_Idx1000_2` — expected allocs/op drop: 734K → ~720K. (5) Check no regression via `TestSLT_InList/idx1000_2/3/4`. Estimate: ~25 lines, 2 files, 0.25 day. | low | small | none | SQB/WT/writers_dml.go |
| REQ001682 | SQB/OP | **Align projectDataBufPool chunk size with NewProject's preallocSize to avoid pool discard.** `NewProject` (`intermediate_basic.go:848-852`) pre-allocates `dataBuf = make([]Value, 0, preallocSize)` where `preallocSize = max(len(cols) * 1024, 512)`. For a 1-column project, preallocSize = 1024. The `projectDataBufPool` (`intermediate_basic.go:266-271`) has `projectDataBufChunkSize = 512` and its `New` function allocates a 512-capacity buffer. Since 1024 > 512, the pool's buffer is always discarded and a new allocation happens — 42.88MB flat / 6.32% of `BenchmarkSLT_CreateView` total alloc. The pool is effectively dead for 1-2 column projects. **Fix:** (1) Increase `projectDataBufChunkSize` from 512 to 1024, or (2) change the `preallocSize` multiplier from 1024 to 512, or (3) have `NewProject` use the pool buffer directly when it has sufficient capacity and only grow when needed. (4) Verify with `BenchmarkSLT_CreateView` and `BenchmarkSelect4_Queries/star` — expected B/op drop: ~1.95MB → ~1.1MB (-44%). (5) Check no regression via `TestSLT_InList/createview`. Estimate: ~5 lines, 1 file, 0.05 day. | low | small | none | SQB/OP/intermediate_basic.go |
| REQ001683 | SQB/EX | **Fix comma-separated FROM (implicit cross join) when same table appears multiple times with different aliases.** `planSelectJoins` (`planner_select.go:1133-1135`) builds `joinClauseIdx` keyed by `jc.Right` (physical table name). When the same table appears multiple times with different aliases (e.g. `FROM tab0, tab1 AS cor0, tab0 AS cor1, tab0 AS cor2`), the second and third `tab0` entries overwrite the first in the map, causing one or more CROSS JOIN clauses to be silently dropped. The cartesian product size is reduced from 3^N to 3^(N-1) rows. This is the root cause of 51/4790 remaining failures in `random/select/slt_good_124.test` (after the join elimination fix), plus rsel1 and rsel123. **Partially fixed:** CROSS JOIN tables are now preserved during join elimination (line 232-235). This fixed 117/168 failures in rsel124. **Remaining:** The `joinClauseIdx` map collision for duplicate table names with different aliases. A correct fix requires: (1) Change `joinClauseIdx` from `map[string]int` to `map[string][]int`. (2) Use `baseOccurrence` (shared across groups) for the base table lookup, and `tableOccurrence` (per-group) for the inner loop. (3) Verify with `TestSLT_PerFile/random/select/slt_good_124.test` — expected fail count drop: 51 → 0. (4) Also verify rsel1 (810 → 0) and rsel123 (588 → 0). (5) Check no regression via `TestSLT_InList` (all 23 included cases). Estimate: ~30 lines, 1 file, 0.5 day. | high | medium | none | SQB/EX/planner_select.go |
| REQ001685 | SQB/OP | **Fix `SELECT *` column expansion when same table appears multiple times with different aliases.** `SELECT ALL * FROM tab1, tab1 cor0, tab2, tab0 AS cor1` returns 243 cells (81 rows × 3 columns) instead of 972 cells (81 rows × 12 columns). The `*` expansion should include all columns from all 4 table instances. When the same physical table appears multiple times (e.g. `tab1` and `tab1 cor0`), the column expansion may be deduplicating columns by physical table name instead of by alias. This affects 5 failures in rsel124: `SELECT ALL *` with duplicate table names returns fewer columns than expected. **Fix:** (1) Trace the `*` expansion in `planSelect` or `collectReferencedColumns` to find where columns from duplicate table instances are dropped. (2) The expansion should include all columns from each table alias, not just the first occurrence of each physical table. (3) Verify with `TestSLT_PerFile/random/select/slt_good_124.test` — expected fail count drop: 51 → 46. (4) Check no regression via `TestSLT_InList` (all 23 included cases). Estimate: ~20 lines, 1 file, 0.25 day. | high | small | none | SQB/EX/planner_select.go |
| REQ001686 | SQB/EV | **Fix incorrect unary minus expression evaluation with DISTINCT.** `SELECT DISTINCT cor0.col2 + - cor0.col0 FROM tab2, tab0 AS cor0` returns `[-52]` instead of `[-34]`. The expression `cor0.col2 + - cor0.col0` involves a unary minus on a column reference. The value computation is off by 18, suggesting the unary minus is being applied to the wrong operand or the column values are not being correctly resolved. This affects 5 value-computation failures in rsel124. **Fix:** (1) Trace the expression evaluation path for `cor0.col2 + - cor0.col0` to identify where the unary minus is incorrectly applied. (2) Check if the issue is in the `evalArithBatch` vectorized path or the `EvalValue` row-based path. (3) Verify with `TestSLT_PerFile/random/select/slt_good_124.test` — expected fail count drop: 51 → 46. (4) Also check `SELECT DISTINCT cor0.col1 + cor0.col2 + 12 FROM tab0, tab2 AS cor0` (got [110] want [67]) and `SELECT DISTINCT - cor0.col0 col2 FROM tab2, tab0 cor0` (got [-79] want [-89]). Estimate: ~30 lines, 1 file, 0.5 day. | medium | small | none | SQB/EV/eval_vec.go, SQB/EV/eval.go |
| REQ001687 | SQB/OP | **Fix hash mismatch in DISTINCT with duplicate table aliases and cross joins.** Multiple queries in rsel124 with `SELECT DISTINCT ... FROM tab1, tab2 AS cor0, tab1 AS cor1` (same table with different aliases) produce hash-mismatched results. The `joinClauseIdx` map collision (REQ001683) causes the wrong join clause to be used for duplicate table names, which affects the operator tree structure. Once REQ001683 is fixed, these hash mismatches should be resolved. If not, the issue is in the `Distinct` operator or the column resolution in the `Project`/`FilterProject` operator. **Fix:** (1) Fix REQ001683 first (joinClauseIdx map collision). (2) If hash mismatches remain, trace the `Distinct` operator's `Next()` or `NextBatch()` to verify row comparison logic. (3) Verify with `TestSLT_PerFile/random/select/slt_good_124.test` — expected fail count drop: 51 → 0. Estimate: ~10 lines, 1 file, 0.1 day (after REQ001683). | low | small | REQ001683 | SQB/OP/operators.go, SQB/EX/planner_select.go |
| REQ001688 | SQB/EV | **Fix aggregate function evaluation errors in expression context — 52 failures in `random/expr/slt_good_0`.** Aggregate functions (COUNT, SUM, MIN, MAX, AVG) return incorrect values when used in arithmetic expressions, CASE WHEN branches, or with DISTINCT/ALL modifiers. Root causes identified: (1) **Sign handling**: unary minus on aggregates produces wrong sign (L12721: `MIN(DISTINCT 94) * -MIN(-93)` got [8743] want [-8835]; L37110: `MIN(ALL -17) * -MIN(-73)` got [-289] want [-1241]; L81357: `MAX(ALL 88) * MAX(-26)` got [7744] want [-2288]). (2) **COUNT off-by-one**: COUNT(*) returns 0 instead of 1 in CASE ELSE (L18147, L4392, L53651, L67297, L73840, L77073, L80696, L81326). (3) **Aggregate arithmetic**: SUM/MAX/MIN wrong in expressions (L15789: `SUM(-81)*SUM(-26)*61` got [-400221] want [-128466]; L48814: `-MAX(48)+MAX(27)` got [0] want [-21]; L50463: `69*MAX(DISTINCT 10)*35+MAX(9)` got [-24160] want [-24159]; L55163: `20*26*81*16+MAX(DISTINCT -74)` got [673839] want [673994]). (4) **CASE with aggregate in ELSE**: ELSE branch returns wrong value when containing aggregate (L28837: `CASE WHEN 83 THEN AVG(-62) ... ELSE 10*COUNT(*)` got [0] want [10]; L61779: `CASE WHEN SUM(DISTINCT -81)=73 THEN NULL ELSE -63*COUNT(*)` got [0] want [-63]). **Also affects `random/expr/slt_good_1`** (5 additional failures): L30906 (`MAX(-51)*-MAX(DISTINCT 29)` got [-2601] want [-1479] — MAX sign/value error), L52275 (`CASE -68 WHEN SUM(60) THEN COUNT(*) ELSE -COUNT(*)` got [0] want [-1] — COUNT returns 0 in ELSE), L56035 (`CASE 56 WHEN -5 THEN NULL ELSE COUNT(*)` got [0] want [1]), L59736 (`CASE COUNT(*) WHEN SUM(-24) THEN ... ELSE NULL` got [44] want [NULL] — evaluates wrong branch), L76292 (`CASE 8 WHEN 46 THEN NULL ELSE COUNT(*)` got [0] want [1]). **Also affects `random/expr/slt_good_2`** (30 failures): L4666 (MAX sign), L6504 (COUNT off-by-one), L7476 (MIN arithmetic), L20932 (SUM arithmetic), L40598 (COUNT off-by-one), L46967 (CASE+COUNT), L51968 (COUNT off-by-one), L55615 (SUM arithmetic), L56935 (MIN/COUNT), L63375 (COUNT off-by-one), L63965 (COUNT sign), L64583 (aggregate arithmetic), L65920 (CASE+aggregate), L72501 (CASE+COUNT), L72947 (COUNT off-by-one), L72988 (COUNT off-by-one), L74073 (COALESCE+MIN), L74182 (MAX arithmetic), L75909 (aggregate arithmetic), L78153 (CASE+COUNT+SUM), L4311 (CASE+AVG), L26297 (nested CASE+aggregate), L39917 (CASE+aggregate), L46575 (CASE branch), L46138 (MAX arithmetic), L58384 (CASE+MIN/COUNT), L59651 (CASE+SUM), L38580 (MAX arithmetic), L32641 (CASE+aggregate), L37403 (MIN arithmetic). **Fixed (2026-07-22):** Two-phase fix: (1) Added `CaseExpr` handling to `ContainsAggregate` and `buildAggregateVirtualRow` — fixed 76/114 failures (67%). (2) Added `AggregateLookupKey` (DT/types.go) to generate unique keys for aggregates with complex args, eliminating key collision in virtual row — fixed remaining 36 failures. Final results: slt_good_0: 69→0, slt_good_1: 6→1, slt_good_2: 39→1 (112/114 fixed, 98%). Remaining 2 failures: L77866 (NULLIF+aggregate, covered by REQ001689) and L31653 (test bug — SLT expected value wrong, SQLite returns 19.8125 not 19). Estimate: ~80 lines, 3-4 files, 1 day. | critical | medium | none | SQB/EV/eval.go, SQB/EV/eval_vec.go, SQB/OP/operators.go |
| REQ001689 | SQB/EV | **Fix NULLIF/COALESCE arithmetic with aggregate evaluation errors — 13 failures in `random/expr/slt_good_0`.** NULLIF and COALESCE produce incorrect results when their arguments involve aggregate functions. Root cause: the aggregate inside NULLIF/COALESCE evaluates to a wrong value, causing the comparison to fail or COALESCE to pick the wrong branch. Failures: L1188 (`NULLIF(CASE...ELSE 39*53*32*58+72*COUNT(*)*31...)` got [3836401] want [3838633]), L12800 (`COALESCE(-MAX(DISTINCT -99), 58*-87, 34) * -MAX(ALL 66)` got [9801] want [-6534]), L15213 (`SUM(COALESCE(71,3,-91)) - 79 + 38*84*SUM(ALL -98)` got [226600] want [-312848]), L26602 (`COALESCE(COUNT(23), NULLIF(-2,23)) / -COUNT(*)` got [0] want [-1]), L29823 (`COALESCE(CASE...ELSE -MIN(NULLIF(-50, NULL))...)` got [-502] want [-607]), L35184 (off by 1), L39673 (`NULLIF(-86, 90+MAX(ALL -11)) + MAX(ALL 86)` got [-75] want [0]), L51311 (CASE with NULLIF inside ELSE, got [0] want [32508]), L57197 (`CASE NULLIF(63,85) WHEN 12+(-89) THEN NULL ELSE -41*COUNT(*)+36*81+18` got [-2934] want [-2975]), L58698 (`NULLIF(-COUNT(*), 45/-45)` got [0] want [-64]), L61944 (nested CASE+NULLIF+COUNT, got [NULL] want [3943]), L81096 (`NULLIF(-COUNT(*), 97*48/COALESCE(...))` got [222] want [3134]), L84851 (CASE+NULLIF+COUNT+MAX interaction). **Also affects `random/expr/slt_good_1`** (1 additional failure): L77866 (`NULLIF(AVG(DISTINCT -1), -COUNT(*))` got [-29] want [NULL] — NULLIF with aggregate comparison fails). **Also affects `random/expr/slt_good_2`** (8 failures): L12106 (`NULLIF(SUM(-10), COUNT(*)*COUNT(*)+43)` aggregate comparison), L27989 (`NULLIF(-CASE...ELSE COUNT(*), ...)` got [0] want [1]), L35622 (`NULLIF(-CASE...ELSE 33, 92)` got [NULL] want [-2871]), L46138 (`NULLIF(-26, MAX(-74)*-39)` MAX sign error), L47367 (`NULLIF(19, SUM(ALL -33))` SUM arithmetic), L21262 (nested CASE+aggregate, got [NULL] want [60630]), L32641 (`CASE NULLIF(-34, COUNT(*)*...)` got [NULL] want [102]), L72988 (`NULLIF(-21, SUM(31))` SUM+COUNT interaction). **Fix:** (1) Verify NULLIF comparison logic when aggregate argument is involved — ensure aggregate is fully evaluated before comparison. (2) Verify COALESCE branch selection when first argument contains aggregate. (3) Check that aggregate evaluation inside NULLIF/COALESCE uses the same path as standalone aggregates. (4) Verify with `TestSLT_PerFile/random/expr/slt_good_0.test` — expected fail count drop: 69 → ~56; also `slt_good_1.test` — expected fail count drop: 6 → ~5; also `slt_good_2.test` — expected fail count drop: 39 → ~31. Estimate: ~40 lines, 2 files, 0.5 day. | critical | small | REQ001688 | SQB/EV/eval.go, SQB/EV/eval_vec.go |
| REQ001690 | SQB/EV | **Fix CASE NULL propagation in arithmetic — 4 failures in `random/expr/slt_good_0`.** When a CASE expression returns NULL (either via explicit ELSE NULL or no matching WHEN with no ELSE), arithmetic involving that NULL should produce NULL (SQL NULL propagation), not 0. Failures: L6196 (`-CASE WHEN COUNT(*)=COUNT(49) THEN ... WHEN 15 THEN NULL END` — CASE returns NULL, but `-NULL` is treated as 0 instead of NULL, got [1] want [-494] — the engine evaluates the NULL branch and then applies unary minus as `-(NULL)` → 0 instead of NULL); L11703 (`CASE WHEN COUNT(*)=48 THEN 9 WHEN COUNT(*)*68=-69 ELSE NULL END * 46` — ELSE NULL should propagate through `*46` to produce NULL, got [-3174] want [NULL]); L30928 (`CASE -0 WHEN COUNT(*) THEN 4 ELSE NULL END` — WHEN doesn't match (assuming COUNT(*)≠0), ELSE NULL should return NULL not 0, got [4] want [NULL]); L84566 (`-CASE WHEN COUNT(24)=... THEN ... WHEN 55 THEN NULL END + 90*17` — CASE returns NULL, `NULL+90*17` should be NULL, got [1607] want [NULL]). **Fix:** (1) Trace CASE evaluation path for NULL result — ensure NULL is returned as nil/NULL Value, not coerced to 0. (2) Verify that arithmetic operators (unary minus, multiply, add) propagate NULL when any operand is NULL. (3) Check both row-based (`EvalValue`) and vectorized (`evalArithBatch`) paths. (4) Verify with `TestSLT_PerFile/random/expr/slt_good_0.test` — expected fail count drop: 69 → ~65. Estimate: ~20 lines, 1-2 files, 0.25 day. | high | small | REQ001688 | SQB/EV/eval.go, SQB/EV/eval_vec.go |
