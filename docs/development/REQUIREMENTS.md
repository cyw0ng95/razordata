## TBD

| ID | Subsystem | Requirement | Priority | Effort | Deps | Touches |
| --- | --- | --- | --- | --- | --- | --- |
| REQ000850 | EX/ddl | **TestBugfix_DropView_Unknown: `DROP VIEW unknown_v` returns `ex: no such view: unknown_v` instead of a successful no-op.** `TestBugfix_DropView_Unknown` in `internal/SQL/EX/iter28_bugfix_test.go:311` expects `DROP VIEW` on a non-existent view to succeed (SQLite semantics: `IF EXISTS` semantics, but even without `IF EXISTS` the operation is a no-op when nothing matches). The current implementation in `writers.go`/`buildWriterOp` returns an error when the view isn't found in the registry. **Fix:** (1) Change the `*PS.DropView` branch in `buildWriterOp` to silently succeed if the view isn't registered. (2) Add the same to `DropView.RowsAffected()` so the result count is 0 rather than an error. **Test evidence:** `iter28_bugfix_test.go:317` — actual error `ex: no such view: unknown_v`. | high | small | iter-28 | EX/writers.go, EX/ex.go |
| REQ000851 | EX/ddl | **TestBugfix_DropTrigger_Unknown: `DROP TRIGGER unknown_t` returns `ex: no such trigger: unknown_t` instead of a successful no-op.** `TestBugfix_DropTrigger_Unknown` in `internal/SQL/EX/iter28_bugfix_test.go:323` expects `DROP TRIGGER` on a non-existent trigger to succeed. The current implementation in `writers.go`/`buildWriterOp` returns an error when the trigger isn't found. **Fix:** (1) Change the `*PS.DropTrigger` branch in `buildWriterOp` to silently succeed if the trigger isn't registered. (2) Add the same to `DropTrigger.RowsAffected()` so the result count is 0 rather than an error. **Test evidence:** `iter28_bugfix_test.go:329` — actual error `ex: no such trigger: unknown_t`. | high | small | iter-28 | EX/writers.go, EX/ex.go |
| REQ000853 | EX/scalar | **TestDual_AllSeededCases/changes_function: `SELECT CHANGES()` after a single INSERT returns 0 instead of 1.** `TestDual_AllSeededCases/changes_function` in `tests/sqlcmp/dual/probe_cases.go:522` runs the probe in a *fresh, separate execution session* (per-case setup with a new `Executor`). Each case spawns a new Executor instance, so the in-Executor `lastChanges` field is 0. But more importantly, the setup uses `mustExec` which goes through a path that doesn't propagate `execCtx` to the Insert operator for that statement. Combined with the new `ExecContext.LastChanges` initialization for Exec path being `0`, the INSERT succeeds but `LastChanges` never reaches the value the SELECT CHANGES() query reads. **Root cause candidates:** (1) The setup phase in the dual test harness does not call `propagateExecContext` for the setup INSERTs. (2) When `CHANGES()` is called in a fresh executor that has never seen a DML, the persisted value is 0. (3) The `dual_test.go` runner resets the executor between cases but doesn't preserve the per-statement `lastChanges` across the reset. **Actual mismatch:** `razor=[[0]]` vs `oracle=[[1]]`. **Fix:** Verify which path the dual test setup uses (likely `mustExec` with a fresh executor) and ensure `CHANGES()` is computed against the *current* in-memory change counter, falling back to the per-executor accumulator. | high | medium | iter-31 | EX/ex.go, EX/execctx.go, tests/sqlcmp/dual |
| REQ000854 | EX/scalar | **TestDual_AllSeededCases/total_changes_function: `SELECT TOTAL_CHANGES()` after INSERT+UPDATE+DELETE returns 0 instead of 3.** `TestDual_AllSeededCases/total_changes_function` in `tests/sqlcmp/dual/probe_cases.go:531` runs three DML statements (1 INSERT + 1 UPDATE + 1 DELETE) in setup, then queries `TOTAL_CHANGES()`. Expected: 3. Actual: 0. The same per-case executor issue as REQ000853 applies, plus a deeper issue: `ExecContext.TotalChanges` is initialized from `e.totalChanges` at the start of each Exec/Query/QueryAll. In the dual test harness, the setup uses `mustExec` which presumably does *not* update the per-statement `ExecContext.TotalChanges` on the *Executor* (the propagated write-back `e.totalChanges = execCtx.TotalChanges` is only present in `QueryAll`, not in `mustExec`). The current implementation has the read-back only in `QueryAll`/`QueryStreamFromAST`, not in the bare `Exec` path used by setup. **Actual mismatch:** `razor=[[0]]` vs `oracle=[[3]]`. **Fix:** (1) Add `e.totalChanges = execCtx.TotalChanges` write-back to every Exec path that creates an ExecContext (including the setup helper used by the dual test). (2) Or: have the dual test setup use a single `Executor` instance across all setup statements, sharing the cumulative counter. | high | medium | iter-31 | EX/ex.go, EX/execctx.go, tests/sqlcmp/dual |
| REQ000855 | EX/op | **TestDual_AllSeededCases/union_all_with_nulls: UNION ALL with NULLs returns wrong row count and order.** `TestDual_AllSeededCases/union_all_with_nulls` in `tests/sqlcmp/dual/probes_v2.go:547` runs `SELECT x FROM a UNION ALL SELECT x FROM b ORDER BY x` against `a = {1, NULL}` and `b = {NULL, 2}`. Expected: 4 rows in order `[NULL, NULL, 1, 2]`. Actual: 3 rows `[[NULL] [NULL] [2]]` — the `1` is missing. **Root cause:** The new REQ000838/842 streaming path for UNION ALL emits left-then-right, but the rows from `a` for `1` are not being emitted (only 1 row from `a` instead of 2). This suggests the streaming path is not handling the `INTEGER PRIMARY KEY` table's rows correctly, possibly because `INTEGER PRIMARY KEY` shadows the rowid and the SeqScan in the in-memory catalog is skipping one row. **Fix:** (1) Trace `SeqScan` output for the `a` table when it has 2 rows including the primary-key 1. (2) Verify the streaming UNION ALL path emits all left-side rows. (3) Check whether `RegisterTable` skips rows with `INTEGER PRIMARY KEY` shadowing. **Actual mismatch:** `razor=[[<nil>] [<nil>] [2]]` vs `oracle=[[1] [1] [2] [2]]` (note: oracle here is a flattened comparator that shows each row once even if duplicated). | high | medium | iter-31 | EX/operators.go, EX/compound.go, source.go |
| REQ000856 | EX/op | **TestDual_AllSeededCases/union_distinct_with_nulls: UNION (dedup) with NULLs returns wrong rows.** `TestDual_AllSeededCases/union_distinct_with_nulls` in `tests/sqlcmp/dual/probes_v2.go:558` runs `SELECT x FROM a UNION SELECT x FROM b ORDER BY x` against `a = {1, NULL}` and `b = {NULL, 2}`. Expected: 3 rows `[NULL, 1, 2]`. Actual: 2 rows `[NULL, 2]` — the `1` is missing. Likely same root cause as REQ000855: the source-side `1` is not being emitted, and dedup of the remaining `NULL, NULL, 2` produces `[NULL, 2]`. **Fix:** Same as REQ000855. **Actual mismatch:** `razor=[[<nil>] [2]]` vs `oracle=[[1] [2]]`. | high | medium | iter-31 | EX/operators.go, EX/compound.go, source.go |
| REQ000857 | EX/plan | **TestDual_AllSeededCases/self_join: `FROM t a, t b WHERE a.v = b.v AND a.id < b.id` returns 0 rows instead of 1.** `TestDual_AllSeededCases/self_join` in `tests/sqlcmp/dual/probes_v2.go:754` runs `SELECT a.id, b.id FROM t a, t b WHERE a.v = b.v AND a.id < b.id ORDER BY a.id, b.id` against `t = {(1,10), (2,20), (3,10)}`. Expected: `[[1, 3]]` (rows with same v=10 and id1<id2). Actual: `[]` (no rows). The same-table alias case fails — the planner likely eliminates the second reference to `t` (via join elimination) or the table is only registered once with the alias `a`, leaving `b` to read empty. **Fix:** (1) In the join planner, when both sides of `FROM t a, t b` reference the same physical table, treat them as separate scan streams over the same rows (do not eliminate one). (2) The REQ000835/836 self-join alias fix in the prior iteration was partial — verify that `FROM tab0, tab0` produces the cross product (3 × 3 = 9 rows), not the deduplicated 3. **Actual mismatch:** `razor=[]` vs `oracle=[[1 3]]`. | high | medium | iter-31 | EX/planner.go, EX/source.go |
| REQ000858 | EX/parser | **TestDual_AllSeededCases/column_alias_in_where: `SELECT v AS value FROM t WHERE value > 15` returns 0 rows instead of 1.** `TestDual_AllSeededCases/column_alias_in_where` in `tests/sqlcmp/dual/probes_v2.go:862` runs `SELECT v AS value FROM t WHERE value > 15` against `t = {(1,10), (2,20)}`. Expected: `[[20]]`. Actual: `[]`. SQLite (and standard SQL) allows column aliases defined in `SELECT` to be referenced in `WHERE` (unlike `GROUP BY`/`ORDER BY` which are post-SELECT). The current planner/executor treats `value` in `WHERE` as an unknown column and either errors silently or fails to match. **Fix:** (1) When planning `WHERE`, register the SELECT-list aliases as available column names (after the FROM resolution but before the WHERE evaluation). (2) Verify that `value` resolves to `t.v` for filtering. **Actual mismatch:** `razor=[]` vs `oracle=[[20]]`. | high | medium | iter-31 | EX/planner.go, EX/intermediate.go |
| REQ000859 | EX/eval | **TestDual_AllSeededCases/nested_scalar_subquery: `SELECT (SELECT MAX(v) FROM (SELECT v FROM t WHERE v < 30))` returns `[NULL, NULL]` instead of `[20]`.** `TestDual_AllSeededCases/nested_scalar_subquery` in `tests/sqlcmp/dual/probes_v2.go:977` runs a 2-deep correlated subquery against `t = {(1,10), (2,20), (3,30)}`. Expected: `[[20]]` (max of {10, 20}). Actual: `[[<nil>] [<nil>]]` (2 NULL rows instead of 1 with the max). The subquery pipeline returns 2 rows instead of 1 because either (a) the outer query produces 3 rows (one per t row) and each executes the scalar subquery independently, or (b) the subquery's MAX evaluation is short-circuited and emits NULLs. Most likely: the scalar subquery is not being treated as scalar (returns single value) but as a regular subquery (returns all rows), and the outer SELECT * is producing one row per t. The MAX inside the subquery may then not be evaluated. **Fix:** (1) Ensure `SELECT (subquery)` in the projection wraps the subquery as scalar — its result row count must be ≤1. (2) Verify the inner `SELECT MAX(v) FROM (SELECT v FROM t WHERE v < 30)` is evaluated as a single group with the max computed. **Actual mismatch:** `razor=[[<nil>] [<nil>]]` vs `oracle=[[20]]`. | high | medium | iter-31 | EX/eval.go, EX/intermediate.go |
| REQ000860 | EX/writer | **TestDual_AllSeededCases/insert_select: `INSERT INTO dst SELECT * FROM src WHERE v > 15` followed by `SELECT v FROM dst` returns 0 rows instead of 1.** `TestDual_AllSeededCases/insert_select` in `tests/sqlcmp/dual/probes_v2.go:1250` runs setup with `INSERT INTO dst SELECT * FROM src WHERE v > 15` against `src = {(1,10), (2,20)}`, then queries `dst`. Expected: `dst` contains `(2, 20)` → query returns `[[20]]`. Actual: `dst` is empty. The `INSERT INTO dst SELECT ...` path is not materializing the SELECT result into `dst`. **Root cause candidates:** (1) `INSERT INTO ... SELECT` is not implemented for the in-memory `RegisterTable` path — only the store-backed path was wired in buildWriterOp. (2) The Insert operator's `selectPlan` child is not being executed. (3) The `propagateExecContext` is not being called for the selectPlan, so the inner SELECT loses its planner context. **Fix:** (1) In `NewInsertWithStore` / `NewInsert` when `s.Select != nil`, ensure the `selectPlan` is built via `e.planner.Plan(s.Select)` and run via `selectPlan.Next(ctx)` per row. (2) Verify `propagateExecContext` is called on the selectPlan so subqueries inside the SELECT resolve. (3) Add explicit `e.RegisterTable` for the destination if needed. **Actual mismatch:** `razor=[]` vs `oracle=[[20]]`. | high | medium | iter-31 | EX/writers.go, EX/ex.go |
| REQ000861 | EX/op | **TestJoinRegress/j4_in_or: 4-table join with `IN`+`OR` returns 1400 rows instead of expected 1160.** `TestJoinRegress/j4_in_or` in `tests/sqlcmp/join_regress_test.go:111` runs `SELECT * FROM t1, t2, t3, t4 WHERE t1.a IN (101, 103) AND (t2.b > 105 OR t4.d < 103)` on 100-row tables. Expected: 1160 rows (per the comment: `10000 × (2/10) × (1 - (6/10)*(7/10)) = 2000 × 0.58 = 1160`). Actual: 1400 rows. The predicate pushdown is not reducing the row count to the expected selectivity — the OR condition is probably not being distributed across the join. With 10000 cartesian rows, t1.a IN (101,103) reduces to 2000, and the OR further restricts to 0.58 × 2000 = 1160. The actual 1400 suggests one of the conditions is being applied but not the OR-distribution, or the OR is being applied before the cross-join reduction. **Fix:** (1) Verify the OR-condition pushdown logic correctly handles `(t2.b > 105 OR t4.d < 103)` — possibly by splitting the OR into two independent scans and unioning. (2) The selective factor of 0.58 comes from `1 - P(NOT OR) = 1 - P(NOT t2.b>105) * P(NOT t4.d<103) = 1 - 0.6 * 0.7 = 0.58` — verify this matches the implementation. **Actual mismatch:** `got=1400 want=1160 (ratio 1.21x)`. | high | medium | iter-31 | EX/join.go, EX/intermediate.go, EX/planner.go |
| REQ000866 | EX/filter | **`Filter.refillBatch` allocates 350ms of cloneRow and 240ms of compiledFilterFn per benchmark iteration — predicate evaluation could short-circuit on OR with early-exit.** pprof on `BenchmarkJoinRegress/j2_where_or` (`SELECT * FROM t1, t2 WHERE t1.a > 105 OR t2.b < 103`): `Filter.refillBatch` consumes **1.46s of 3.31s (44.11%)** — 980ms is child Next (NLJ below) and 480ms is the `compiledFilterFn` + `cloneRow` pair. The OR condition requires evaluating both sides for every row, but the rows that match `t1.a > 105` early (a small fraction) could skip the `t2.b < 103` evaluation. **Fix:** (1) In the compiled-predicate generator, recognise `OR` and emit a two-step check: evaluate left child first, if true return true without evaluating right. (2) For 3+ way OR, short-circuit on the first true. (3) For AND, short-circuit on the first false. **Expected impact:** OR-heavy queries (like j2_where_or) should drop from 251µs / 243kB / 1407 allocs to ~`180µs / 170kB / 1000 allocs` (-30%). | medium | small | iter-32 | EX/intermediate.go, EX/eval.go |
| REQ000868 | EX/filter | **`Filter.refillBatch` grows `batchEmit` and `batchBuf` on every call (capacity-retention only kicks in after the first refill) — first 32 rows allocate twice instead of zero.** pprof on `BenchmarkJoinRegress/j2_where_or`: `Filter.refillBatch` at `internal/SQL/EX/intermediate.go:158` checks `cap(f.batchBuf) < filterBatchSize` and re-allocates if needed, but the very first call always allocates (the slices are nil). For a query that emits 50 rows in 2 batches, that's 4 allocations (buf + emit) on the first call, then zero on the second. **Fix:** (1) Initialise `f.batchBuf` and `f.batchEmit` in `Filter` constructor with `make([]Row, 0, filterBatchSize)`. (2) Same for `f.batchEmit`. (3) Same for the inner block-NLJ buffers in `NestedLoopJoin` constructor. **Expected impact:** Removes the first-call allocation for every Filter in every query — small but cumulative. | low | small | iter-32 | EX/intermediate.go, EX/join.go |
| REQ000870 | EX/join | **NLJ outer-join path uses `joinRowsLL` which does 3 fresh allocations per output row (Cols, Types, Data).** pprof on `BenchmarkJoinRegress/j2_equi`: `NestedLoopJoin.nextBlock` consumes **22% of allocations** but `joinRowsLL` at `internal/SQL/EX/join.go:745` is on the per-row path. When called via the `rightOuter`/`leftOuter` branches (lines 183, 188, 265, 287, 545), it allocates `out.Cols = make([]string, 0, nCols)`, `out.Types = make([]int, 0, nTypes)`, `out.Data = make([]Value, 0, nData)` for every emitted row. **Fix:** (1) Route the outer-join path through the block-mode `nextBlock` which already has `blkDataBuf` for shareable Data. (2) Pre-compute shared `Cols`/`Types` slices on the operator struct (like `j.sharedCols` already does in `tryHashCrossJoin`) and use `joinRowsLLWithCols` instead. **Expected impact:** Outer-join queries (currently not heavily benchmarked but on the roadmap) should see ~50% allocation reduction. | medium | small | iter-32 | EX/join.go |
| REQ000871 | EX/filter | **Compiled-cmp closure `func(a, b any) bool` boxes `Value` struct into interface for every comparison — a `Value`-typed direct call avoids the boxing.** pprof on `BenchmarkJoinRegress/j4_in_or`: `makeCompiledCmp.func1` (the closure at `internal/SQL/EX/intermediate.go:742`) shows **202.52MB of allocations** from the closure context. The closure captures `cmp func(a, b any) bool` and on every call does `cmp(row.Data[idx], litValue)` — passing a `Value` struct (16 bytes) to a `func(any, any)` boxes it into an interface (16 bytes for the interface header + 16 for the boxed copy = 32 bytes per call). The `litValue` is already a `Value` so the receiver can use it directly. **Fix:** (1) Change `makeCompiledCmp` to take `func(a, b Value) bool` (or even `func(a Value, lit Value) bool` since lit is fixed) and update callers. (2) Inline the cmp logic into the compiled function to avoid the closure call entirely. (3) For literal comparisons, use `cmpOpValue` (Value-typed) instead of `compare` (any-typed). **Expected impact:** Eliminate ~6.5% of all allocations (200MB / 3.1GB in j4_in_or). | medium | small | iter-32 | EX/intermediate.go, EX/eval.go |
| REQ000872 | EX/plan | **`Project.dataBuf` lazy growth at line 313 makes the first 256 rows allocate a fresh `[]Value` of size 2 × 256 = 512 — 4KB allocation per Project, per query.** pprof shows `Project.Next` at `internal/SQL/EX/intermediate.go:285` consumes **1.25s of 2.27s (55%)** in j2_equi, with 100ms on the initial `make([]Value, 0, 256*p.dataPerRow)`. For a query that returns 10 rows, 99% of the buffer is wasted but the full 4KB was allocated. **Fix:** (1) Start the buffer with smaller initial capacity (e.g. 64 * p.dataPerRow) and grow by doubling. (2) For `SELECT *` from a small table, no buffer is needed at all — return rows directly. (3) Add a `Reset()` method that allows the caller to reuse the buffer for paginated queries. **Expected impact:** Small wins per query (~4KB / 100ns), but cumulative across all SELECT queries. | low | small | iter-32 | EX/intermediate.go |
| REQ000874 | EX/op | **`HashCrossJoin.materializeLeft` allocates a fresh `leftPrefixedCols` per row for the first time — should be hoisted to the operator struct.** pprof on `BenchmarkJoinRegress/j2_equi`: `HashCrossJoin.materializeLeft` at `internal/SQL/EX/hashcrossjoin.go:132` accounts for **184.93MB of 1407.87MB (13.14%)**. Lines 142-146 check `if !hasAnyPrefix(row.Cols) { leftPrefixedCols = prefixCols(...) }` per row in the materialization loop. The `leftPrefixedCols` is already cached locally (good), but the per-row `hasAnyPrefix` scan over `row.Cols` is O(cols) per row, and the allocation of `Row` literals in lines 140, 313, 356, 640 is unavoidable. **Fix:** (1) Move `leftPrefixedCols` and the `hasAnyPrefix` check to the operator struct so they're computed once on first call. (2) Pre-allocate a single `Row` literal and reuse it as a template, only varying `Cols`/`Data`/`Types` pointers per row. (3) Use struct-of-arrays layout for materialized rows. **Expected impact:** Modest — 13% of j2_equi allocations. | low | small | iter-32 | EX/hashcrossjoin.go |
| REQ000875 | EX/op | **`HashCrossJoin.build` allocates `hashBucket` slices for every partition per join — pre-allocate once and reuse.** pprof on `BenchmarkJoinRegress/j2_equi`: `HashCrossJoin.build` at `internal/SQL/EX/hashcrossjoin.go:75` accounts for **150.85MB of 1407.87MB (10.72%)**. The `build` function creates a fresh `[]hashBucket` (with `j.buckets = make([]hashBucket, j.partitions)`) and per-bucket `rightRows` and `hashes` slices. Across invocations, the partitions are identical — pre-allocate and reuse. **Fix:** (1) Add a `pool *sync.Pool[hashBucketBuf]` to the operator. (2) On `Open`, get a buffer from the pool; on `Close`, return it. (3) Reset the existing slices in-place (re-slice to 0) instead of re-allocating. **Expected impact:** Save ~10% allocations in j2_equi. | low | small | iter-32 | EX/hashcrossjoin.go, EX/join.go |
| REQ000876 | ENG/LS | **mergeIterator allocates a fresh `[]byte` for every key and value on every `Next()` call — buffer ownership is unclear.** pprof on `BenchmarkJoinRegress/j5_cross`: `mergeIterator.Next` at `internal/ENG/LS/api.go:304` accounts for **910ms of 18.36s (4.96%)** total CPU. Lines 321-322 do `append([]byte(nil), src.Key()...)` and `append([]byte(nil), src.Value()...)` for each row pushed onto the heap. The "owned copy" comment at line 361 says it's already owned but we copy again for safety. **Fix:** (1) Trust the source's `Key()`/`Value()` to return owned bytes (audit the sources to confirm). (2) If the sources don't promise ownership, document it and add a separate non-copying `KeyView()`/`ValueView()` API. (3) Reuse the heap item's key/value buffer across `Pop`/`Push` operations. **Expected impact:** j5_cross: ~5% CPU reduction; less GC pressure. | medium | small | iter-32 | ENG/LS/api.go |
| REQ000878 | EX/join | **NLJ `nextBlock` copies cached right rows via `append(j.blkRightRows[:0], j.cachedRightRows...)` on every batch — O(rightCount) memcpy per batch.** pprof on `BenchmarkJoinRegress/j5_where_in`: the `append` copy at `join.go:799` accounts for **~5MB/batch** of `memmove` CPU (7% of total). For 100-row right tables with 3125 batches, that's 3125 × 100 × 144 bytes = 45MB of redundant copies. The cached rows are never modified — the `Outer` pointer is set in the matching loop but doesn't mutate the Row struct. **Fix:** (1) Use `j.blkRightRows = j.cachedRightRows` directly (no copy) since the matching loop only sets `r.Outer` which is a pointer field on the loop variable, not on the cached slice element. (2) Verify that the `Outer` assignment (`r.Outer = &l`) doesn't escape the loop variable into the cached slice. **Expected impact:** j5_where_in: ~5% CPU reduction from eliminating memmove. | medium | small | iter-33 | EX/join.go |
| REQ000879 | EX/join | **NLJ `nullRightRow()` allocates a fresh Row + `make([]Value, N)` on every LEFT JOIN unmatched row.** pprof on `BenchmarkJoinRegress/j4_in_or` (which uses LEFT JOIN semantics via the OR filter): `nullRightRow` at `join.go:522` is called for every left row that has no match. With 100-row tables and 4-table cross, that's up to 10000 calls per batch. Each call does `make([]Value, len(rightSchema))` (5 elements = 80 bytes). **Fix:** (1) Pre-allocate a single `nullRightRow` template on the operator struct (computed once from the right table schema). (2) Return a Row with `Data` pointing to the shared null slice (safe because null values are immutable). (3) Set `Cols` to the shared right prefixed cols. **Expected impact:** j4_in_or: ~10% alloc reduction for LEFT JOIN queries. | medium | small | iter-33 | EX/join.go |
| REQ000880 | EX/filter | **`makeCompiledCmp` closure boxes `Value` struct into `any` interface for every comparison — `func(a, b Value) bool` avoids boxing.** pprof on `BenchmarkJoinRegress/j4_in_or`: `makeCompiledCmp.func1` at `intermediate.go:742` shows **202MB of allocations** from the closure context. The closure captures `cmp func(a, b any) bool` and on every call does `cmp(row.Data[idx], litValue)` — passing a `Value` struct (16 bytes) to a `func(any, any)` boxes it into an interface (16 bytes for the interface header + 16 for the boxed copy = 32 bytes per comparison). The `litValue` is already a `Value` so the receiver can use it directly. **Fix:** (1) Change `makeCompiledCmp` to accept `func(a, b Value) bool` and update all callers. (2) For the hot literal-comparison path, use `cmpOpValue` (Value-typed) instead of `compare` (any-typed). (3) Alternatively, inline the comparison logic into the compiled function to eliminate the closure call entirely. **Expected impact:** j4_in_or: ~6% alloc reduction (200MB / 3.1GB). | medium | small | iter-33 | EX/intermediate.go, EX/eval.go |
| REQ000885 | EX/join | **NLJ `nullRightRow()` allocates `make([]Value, N)` for every unmatched LEFT JOIN row — 10K calls/batch for 100-row tables.** Benchmark: j4_in_or=5.4ms with 5.2K allocs. The `nullRightRow()` at join.go:522 creates a new Row with `make([]Value, len(rightSchema))` (5 elements = 80 bytes) on every call. For a 4-table cross with LEFT JOIN semantics, this is called for every left row that has no match — up to 10000 times per batch. **Fix:** (1) Pre-allocate a single `nullRightRow` template on the NLJ struct (computed once from the right table schema). (2) Return a Row with `Data` pointing to the shared null slice (safe because null values are immutable — the slice contents never change). (3) Set `Cols` to the shared right prefixed cols. **Expected impact:** j4_in_or: ~10% alloc reduction for LEFT JOIN queries. | medium | small | iter-33 | EX/join.go |
| REQ000886 | EX/join | **NLJ `blkSharedColIndex` is a `map[string]int` rebuilt per batch — map allocation is expensive for small column sets.** Benchmark: j5_where_in=22ms with 29K allocs. The `blkSharedColIndex` at join.go:816 is a `make(map[string]int, N)` where N is typically 5-10 columns. Map allocation involves hash table initialization, bucket allocation, and GC tracking. For 3125 batches × 1 map = 3125 map allocs. **Fix:** (1) Replace `map[string]int` with a sorted `[]kv{key string; idx int}` slice + binary search (5-10 elements is faster than a map). (2) Or: pre-allocate the map once on the operator struct and clear it per batch (maps can be cleared with `for k := range m { delete(m, k) }` which is O(n) but avoids allocation). (3) Or: use a flat `[]string` + linear scan (5 elements = 5 string comparisons, cheaper than map hash). **Expected impact:** j5_where_in: ~3% alloc reduction. | low | small | iter-33 | EX/join.go |
| REQ000887 | EX/compound | **select4 compound queries (UNION/EXCEPT/UNION ALL chains) materialize every intermediate result — 8-branch chains create 8 materialized buffers.** select4 has queries like `SELECT ... FROM t1 UNION ALL SELECT ... FROM t2 UNION ALL ... EXCEPT SELECT ... FROM t8` with 8 branches. Each UNION/EXCEPT materializes both children, then deduplicates. For 8 branches: 7 materializations × 100 rows = 700 Row slices allocated. The streaming fast-path (REQ000838) only helps UNION ALL without post-processing; UNION and EXCEPT always materialize. **Fix:** (1) For UNION ALL chains, use a streaming pipeline that drains each branch sequentially without intermediate materialization. (2) For EXCEPT/INTERSECT, build a hash set from the right side once, then stream the left side and probe — avoid materializing the full left result. (3) Flatten nested `((a UNION b) UNION c)` into a single multi-way operator. **Expected impact:** select4 compound queries from ~5ms to <1ms. | medium | medium | iter-34 | EX/compound.go |
| REQ000888 | EX/filter | **`Filter.refillBatch` allocates `compiledFilterFn` closure per query — the closure captures predicate AST and creates a heap-allocated function.** Benchmark: j4_in_or=5.4ms with 5.2K allocs. The `lookupOrCompilePredicate` at intermediate.go:124 creates a new closure via `makeCompiledCmp` for each unique predicate. The closure itself is a heap-allocated `func(*Row)(bool, error)` that captures the predicate AST. For queries with the same predicate pattern (common in select4), this creates redundant closures. **Fix:** (1) The predicate cache (`predicateCache sync.Map`) already reuses closures across executor instances. Verify it's working correctly. (2) If the cache key (fmt.Sprintf of predicate) is too coarse, refine it to capture the predicate's semantic equivalence. (3) For the hot path, consider generating a comparison function at compile time (like SQLite's codegen) instead of using closures. **Expected impact:** Marginal if cache is working; significant if cache misses are frequent. | low | medium | iter-34 | EX/intermediate.go |
| REQ000889 | EX/plan | **select4 ScaleJoin277 shows O(N) scaling but with 180KB constant overhead per query — the overhead dominates for small row counts.** Benchmark: ScaleJoin277 at rows=10: 89ms/180KB, rows=30: 110ms/180KB, rows=100: 149ms/180KB. The 180KB is constant across all sizes — it's the plan construction + operator tree setup + first-call initialization. For 10-row tables, the actual join work is ~1ms but the overhead is ~88ms (99% overhead). **Fix:** (1) Profile the plan construction path to identify the 180KB allocation source. (2) Likely candidates: `initStmtCache` (2.5MB per executor), `NewPageCache` (3.3MB per engine), or planner memoization structures. (3) Consider lazy initialization — defer plan construction until first query, share plans across queries with the same shape. **Expected impact:** ScaleJoin277 at rows=10 from 89ms to <20ms. | medium | medium | iter-34 | EX/planner.go, EX/ex.go, SYS/SY |
| REQ000890 | EX/plan | **Planner does not reorder 5-7 table joins by selectivity — NLJ processes tables left-to-right regardless of predicate selectivity.** j7_all_filters query: `SELECT t1.a, t2.b FROM t1,t2,t3,t4,t5,t6,t7 WHERE t1.a=t2.b AND t3.c>100 AND t4.d IN(100,105) AND t5.e=t6.a AND t7.a<108`. The planner builds the join tree in FROM-list order: (((((t1,t2),t3),t4),t5),t6),t7. t7 has the most selective predicate (`a<108` → 7/100 rows = 7% selectivity) but is processed LAST. The NLJ must cross-join t1-t6 (10^6 intermediate rows) before reaching t7's filter. **Fix:** (1) Implement greedy join ordering: sort tables by estimated predicate selectivity (most selective first). (2) For the j7_all_filters case, reorder to: t7(7%) → t4(2%) → t3(70%) → t1=t2(10%) → t5=t6(10%). This reduces intermediate result from 10^6 to ~10^2. (3) Use dynamic programming for ≤8 tables (2^8 = 256 subsets). **Expected impact:** j7_all_filters from 3.9s to <100ms (40x improvement). | high | large | iter-34 | EX/planner.go |
| REQ000891 | EX/plan | **Planner does not push cross-table WHERE predicates down to individual scan operators — filters are applied at the NLJ level after cross products are computed.** j7_where query: `WHERE t1.a>100 AND t4.d IN(100,105) AND t7.a<108`. Each predicate references a single table but is applied at the top-level Filter after all 7 tables are cross-joined. The NLJ processes 10^7 rows before any filter eliminates rows. **Fix:** (1) In `splitPredicatesByTable`, assign each single-table predicate to its owning table's SeqScan/IndexScan operator. (2) For `t1.a>100`: push to t1's SeqScan as a pre-filter (reduces t1 from 100 to 70 rows). (3) For `t4.d IN(100,105)`: push to t4's SeqScan (reduces t4 from 100 to 2 rows). (4) For `t7.a<108`: push to t7's SeqScan (reduces t7 from 100 to 7 rows). (5) Combined: 70 × 100 × 100 × 2 × 100 × 100 × 7 = 9.8×10^8 → reduced from 10^7 by filter pushdown + reorder. **Expected impact:** j7_where from 1.35s to <200ms. | high | large | iter-34 | EX/planner.go |
| REQ000892 | EX/join | **NLJ `Row.Lookup` is O(N) linear scan per column — called 16K times/second in j7_all_filters.** pprof on j7_all_filters: `Row.Lookup` at `ex.go:337` accounts for **0.42s (3.2%)** of CPU. | medium | small | iter-33 | EX/ex.go, EX/operators.go |
| REQ000893 | EX/join | **NLJ emits one row at a time through `driver.Rows.Next` → `AP.Rows.Next` → `EX.streamIterator.Next` → channel — 4 function calls + 1 channel receive per row.** pprof on j7_all_filters: `driver.Rows.Next` at 2.17s (16.6%) is the dominant CPU consumer. The path is: `driver.Rows.Next` → `AP.Rows.Next` → `stream.Next()` (channel receive) → `EX.QueryStreamFromAST.func4` (goroutine sends Row to channel) → `NLJ.Next`. Each step involves a function call, interface dispatch, and potential goroutine scheduling. For 16K rows, that's 64K function calls + 16K channel operations. **Fix:** (1) Batch row emission: instead of 1-row-at-a-time channel sends, buffer N rows in the channel and drain in batches. (2) Or: use a ring buffer instead of a channel for zero-allocation row passing. (3) Or: for the dual-runner path, bypass the channel entirely and write directly to `AP.Row` slice. **Expected impact:** j7_all_filters: ~10% CPU reduction (eliminate channel overhead). | medium | medium | iter-34 | EX/ex.go, SYS/SE, SYS/ST, driver/rows.go |
| REQ000894 | EX/join | **NLJ `nextBlock` rebuilds `blkSharedCols`/`blkSharedTypes`/`blkSharedColIndex` per batch — column layout is constant for the operator's lifetime.** pprof on j5_where_in: `nextBlock` at 470MB (96%) flat allocs. The dominant allocators are `make([]string, ...)` for `blkSharedCols` at join.go:810 and `make(map[string]int, ...)` for `blkSharedColIndex` at join.go:816 — rebuilt every batch. For 5-table cross with 100 rows and batch=32: 3125 batches × 2 allocations = 6250 allocs. The column layout never changes because left Cols and right Cols are fixed for the operator's lifetime. **Fix:** (1) Hoist `blkSharedCols`, `blkSharedTypes`, `blkSharedColIndex` to the operator struct level — compute once on first batch, reuse across all subsequent batches. (2) Guard with a `sharedColsBuilt bool` flag. (3) Reset on `Close()`. **Expected impact:** j5_where_in: ~30% alloc reduction (6250 → 0 per-batch allocs). | high | small | iter-33 | EX/join.go |
| REQ000895 | EX/join | **NLJ `nullRightRow()` allocates `make([]Value, N)` for every unmatched LEFT JOIN row — 10K calls/batch for 100-row tables.** pprof on j4_in_or: `nullRightRow` at join.go:522 creates a new Row with `make([]Value, len(rightSchema))` (5 elements = 80 bytes) on every call. For a 4-table cross with LEFT JOIN semantics, this is called for every left row that has no match — up to 10000 times per batch. **Fix:** (1) Pre-allocate a single `nullRightRow` template on the NLJ struct (computed once from the right table schema). (2) Return a Row with `Data` pointing to the shared null slice (safe because null values are immutable). (3) Set `Cols` to the shared right prefixed cols. **Expected impact:** j4_in_or: ~10% alloc reduction for LEFT JOIN queries. | medium | small | iter-33 | EX/join.go |
| REQ000896 | EX/join | **NLJ `nextBlock` copies cached right rows via `append(j.blkRightRows[:0], j.cachedRightRows...)` on every batch — O(rightCount) memcpy per batch.** pprof on `BenchmarkJoinRegress/j5_where_in`: the `append` copy at `join.go:799` accounts for **~5MB/batch** of `memmove` CPU (7% of total). For 100-row right tables with 3125 batches, that's 3125 × 100 × 144 bytes = 45MB of redundant copies. The cached rows are never modified — the `Outer` pointer is set in the matching loop but doesn't mutate the Row struct. **Fix:** (1) Use `j.blkRightRows = j.cachedRightRows` directly (no copy) since the matching loop only sets `r.Outer` which is a pointer field on the loop variable, not on the cached slice element. (2) Verify that the `Outer` assignment (`r.Outer = &l`) doesn't escape the loop variable into the cached slice. **Expected impact:** j5_where_in: ~7% CPU reduction from eliminating memmove. | high | small | iter-33 | EX/join.go |
| REQ000898 | EX/eval | **`compileColRef` closure allocates a fresh `func(*Row) Value` per column reference — called for every compiled predicate.** pprof on `BenchmarkJoinRegress/j5_or_chain`: `compileColRef.func1` at `intermediate.go` accounts for **120ms (2.4%)** CPU. Each closure captures a column index and performs `row.Data[idx]` — the closure itself is a heap-allocated `func` value. For 5 OR conditions × 2 columns per condition = 10 closures per query, each allocated on the heap. **Fix:** (1) Pre-compile column references into a flat `[]func(*Row) Value` slice on the Filter operator, computed once on first use. (2) Or: replace closures with a direct `row.Data[idx]` inline in the compiled predicate function. (3) Or: use a `colRef` struct with an `eval(row *Row) Value` method that avoids closure allocation. **Expected impact:** j5_or_chain: ~2% CPU reduction + ~10% alloc reduction. | medium | small | iter-34 | EX/intermediate.go |
| REQ000899 | EX/filter | **`Filter.refillBatch` allocates `batchBuf` and `batchEmit` on every call — pool reuse only helps after first call.** pprof on `BenchmarkJoinRegress/j5_or_chain`: `Filter.refillBatch` at `intermediate.go:185` accounts for **465MB (14.3%)** flat allocs. The first call always allocates (slices are nil from pool or fresh). For queries that emit 50 rows in 2 batches, that's 4 allocations (buf + emit) on the first call, then zero on the second. **Fix:** (1) Initialize `f.batchBuf` and `f.batchEmit` in `NewFilter` constructor with `make([]Row, 0, filterBatchSize)` — pre-allocate to avoid first-call alloc. (2) Already done for `batchBufPool` — verify the pool actually returns non-nil slices. (3) If pool returns nil, fix the pool to pre-allocate. **Expected impact:** j5_or_chain: ~5% alloc reduction. | low | small | iter-33 | EX/intermediate.go |
| REQ000901 | EX/plan | **`NOT IN` predicate is not pushed down to SeqScan — causes full cross product scan.** Benchmark: `j5_notin_equi` = 1.08ms (3.5x slower than `j5_equi_inlist` = 307µs). The query `WHERE t1.a = t2.b AND t3.c NOT IN (101,103,105) AND t5.e > 105` has a NOT IN predicate that is not recognized by the pushdown logic. The planner treats NOT IN as a generic expression. **Fix:** (1) In `canPushDown`, recognize `NOT IN (list)` as a pushable predicate (equivalent to `col != v1 AND col != v2 AND ...`). (2) Or: convert NOT IN to a set-difference at the scan operator level. (3) Add NOT IN to `splitPredicatesByTable` as a single-table predicate. **Expected impact:** j5_notin_equi: from 1.08ms to <300µs (3.5x improvement). | high | small | iter-34 | EX/planner.go |
| REQ000902 | EX/join | **NLJ `nextBlock` allocates `blkSharedColIndex` as `map[string]int` per batch — map allocation is expensive for 5-10 columns.** pprof on `BenchmarkJoinRegress/j5_or_chain`: `nextBlock` at 1086MB (33.4%) flat allocs. The `blkSharedColIndex` at join.go:816 is `make(map[string]int, N)` where N is typically 5-10 columns. Map allocation involves hash table initialization, bucket allocation, and GC tracking. For 3125 batches × 1 map = 3125 map allocs. **Fix:** (1) Hoist `blkSharedColIndex` to the operator struct level — compute once on first batch, reuse across all subsequent batches. (2) Guard with a `sharedColsBuilt bool` flag. (3) Reset on `Close()`. (4) Alternatively, replace with a sorted `[]kv` slice + binary search (5-10 elements is faster than a map). **Expected impact:** j5_or_chain: ~15% alloc reduction. | high | small | iter-33 | EX/join.go |
| REQ000903 | EX/join | **NLJ `nextBlock` allocates `blkSharedCols` and `blkSharedTypes` per batch — column layout is constant for the operator's lifetime.** pprof on `BenchmarkJoinRegress/j5_or_chain`: `nextBlock` at 1086MB (33.4%) flat allocs. The `blkSharedCols` at join.go:810 is `make([]string, 0, N)` and `blkSharedTypes` at join.go:813 is `make([]int, 0, N)` — rebuilt every batch. For 3125 batches × 2 allocations = 6250 allocs. The column layout never changes because left Cols and right Cols are fixed for the operator's lifetime. **Fix:** (1) Hoist `blkSharedCols` and `blkSharedTypes` to the operator struct level — compute once on first batch, reuse across all subsequent batches. (2) Guard with a `sharedColsBuilt bool` flag. (3) Reset on `Close()`. **Expected impact:** j5_or_chain: ~20% alloc reduction. | high | small | iter-33 | EX/join.go |

| REQ000904 | EX/eval | **Recursive CTE executor missing — `WITH RECURSIVE` parses (PS/select.go:470) but the executor has no iteration/termination logic, so `slt_gap_test.go:103` fails on `WITH RECURSIVE cnt(x) AS (SELECT 1 UNION ALL SELECT x+1 FROM cnt WHERE x<5) SELECT x FROM cnt`.** The parser builds a `WithStmt` with a recursive flag and a `CompoundStmt` body; the planner/executor needs to (1) detect recursive references, (2) iterate the non-recursive seed once, then alternate with the recursive arm until it produces an empty result, and (3) dedup and forward rows to the outer query. **Fix:** (1) Add a recursive-CTE operator (`RecursiveCTE`) in `EX/` that tracks rows produced per iteration and stops when a fixed-point is reached. (2) Wire the recursive body so self-references to the CTE name resolve to the accumulated row set from the previous iteration. (3) Cap iterations to prevent infinite loops (e.g. 10000 iters, then error). (4) Add a `TestRecursiveCTE_Execution` test verifying the gap-survey probe and a depth-10 linear recursion. **Test evidence:** `slt_gap_test.go:103` (recursive_cte probe). | medium | medium | iter-31 | EX/eval.go, EX/intermediate.go, PS/select.go |
| REQ000905 | EX/writer | **PRAGMA `foreign_keys` is not wired — the foreign-key switch has no executor path, so the parser-accepted `PRAGMA foreign_keys = ON` is silently ignored and any `FOREIGN KEY` constraints declared in `CREATE TABLE` are effectively no-op at INSERT/UPDATE/DELETE time.** The PRAGMA dispatch in `writers.go:1634` only handles `table_info`, `database_list`, `index_list`, `table_list`, `foreign_key_list`, `wal_checkpoint`. A `PRAGMA foreign_keys` branch must (1) read the current value, (2) store it on the Executor or store level, and (3) make every DML operator consult it before enforcing FK constraints. **Fix:** (1) Add `foreignKeys` state on `*Executor` (or the store-level config). (2) Implement `Pragma` branch for `foreign_keys` with `= ON/OFF` and bare read form (returns current value). (3) In `applyFKConstraints` paths (insert/update/delete), short-circuit when `foreignKeys == false`. (4) Add `TestPragma_ForeignKeys_Toggle` verifying ON→enforce / OFF→skip behavior. | medium | small | iter-31 | EX/writers.go, EX/ex.go, EX/constraints.go |
| REQ000906 | EX/writer | **PRAGMA `foreign_key_check` returns a syntax-error / unhandled-name — the dispatcher in `writers.go:1634` has no branch for `foreign_key_check`, so users cannot introspect FK violations.** This is the only way to detect orphan rows after bulk load or schema migrations. **Fix:** (1) Add `Pragma` branch in `writers.go:1634` for `foreign_key_check` and `foreign_key_check(table_name)`. (2) Walk the table(s), for each row check that every FK column matches a primary key in the referenced table. (3) Return rows with schema `(table, rowid, parent, fkid)` per SQLite convention; empty result means no violations. (4) Add `TestPragma_ForeignKeyCheck_NoViolations` and a setup with deliberate violation to assert the violation row is reported. | medium | small | iter-31 | EX/writers.go, EX/constraints.go |
| REQ000907 | EX/parser | **FETCH FIRST/NEXT n ROWS ONLY is not parsed — tokens `T_FETCH`/`T_FIRST`/`T_NEXT`/`T_ONLY` are defined in `token.go:169-171` but no `parse*` function consumes them, so any query using `FETCH FIRST` fails with `syntax error at "FETCH"`.** FETCH is the SQL-standard alternative to `LIMIT` and is required for full SQL:2008 compatibility. The clause sits where `LIMIT ... OFFSET ...` sits in `parseSelect`. **Fix:** (1) Add `fetchFirst *FetchFirst` field to the `Select` AST node (or reuse `Limit` with a flag). (2) In `PS/select.go` after `LIMIT/OFFSET` parsing, add a branch: `FETCH (FIRST|NEXT) [n] (ROW|ROWS) ONLY` (and `WITH TIES` extension optional). (3) Map the `n` value to the existing limit machinery. (4) Reject `OFFSET` + `FETCH` combination mismatch in the same way SQLite does. (5) Add `TestFetchFirst_ParseAndExecute` covering `FETCH FIRST 5 ROWS ONLY`, `FETCH NEXT 3 ROWS ONLY`, and `FETCH FIRST ROW ONLY`. | low | small | iter-31 | PS/select.go, PS/ps.go, EX/intermediate.go, LX/token.go |
| REQ000908 | EX/eval | **ATTACH DATABASE / DETACH DATABASE are parsed but rejected at runtime with `ErrMultiDatabaseNotSupported` (`writers.go:2189`) — the parser successfully builds `AttachStmt`/`DetachStmt` AST nodes but the executor path immediately errors out, making any cross-database query (or even intra-database `ATTACH` for future schema-attach) impossible.** While the multi-database engine is a large architectural change, the bare AST + catalog machinery for attaching an already-open RazorData database directory should be implementable as a thin alias layer. **Fix:** (1) Add a `catalog` field on `*Executor` mapping `name → *store.Store`. (2) On `ATTACH DATABASE '<path>' AS name`, open a second store and register it in the catalog; on `DETACH`, close and remove. (3) For `CREATE TABLE attached.t(...)` and `SELECT * FROM attached.t`, resolve `name` via the catalog when looking up tables. (4) Reject cross-database transactions / joins between attached and main for v1. (5) Add `TestAttachDetach_SamePath` and `TestAttachDetach_QueryAcross`. | medium | medium | iter-31 | EX/writers.go, EX/ex.go, EX/catalog.go (new), PS/ps.go |
| REQ000909 | EX/ddl | **`CREATE VIRTUAL TABLE ... USING module(args)` parses to `CreateVirtualTableStmt` (`ast.go:666-673`) but has no execution path — the writer dispatcher in `writers.go` has no `*PS.CreateVirtualTable` case, so it falls through to a generic syntax error.** Virtual tables are SQLite's module-extensibility point (FTS5, json1, csv, etc.). Implementing a real module is large, but a minimum viable FTS5-style module or a passthrough CSV module would unblock `PRAGMA` introspection code that depends on virtual tables and would let SQLLogicTest files that touch FTS be partially exercised. **Fix:** (1) Add a `*PS.CreateVirtualTable` case in `buildWriterOp` that registers a stub module — for v1, return a clear `feature not supported: virtual table module <name>` error at `CREATE VIRTUAL TABLE` time (no module dispatch). (2) Ensure that `sqlite_master` introspection of virtual tables returns the expected row format (schema column populated with the module name). (3) Add `TestCreateVirtualTable_StubRegistration` verifying that `sqlite_master` shows the table with the right schema and that data ops on it return the unsupported-module error. | medium | small | iter-31 | EX/writers.go, EX/buildWriterOp, PS/ast.go |
| REQ000910 | EX/ddl | **WITHOUT ROWID tables are accepted by the parser (`PS/ddl.go`) and AST (`ast.go`), but the store layer at `EX/store.go:64` and `EX/constraints.go:43` treats them as rowid tables — `STORAGE WITHOUT ROWID` does not change the actual storage layout, so the resulting table uses rowid-based B-tree even though the AST claims rowid is absent.** This means `INSERT INTO without_rowid_t VALUES (...)` with a non-PK column in place of a rowid does not surface any error to the user, but the rowid is still allocated and queries using `WHERE _rowid_ = ?` succeed when they should fail. **Fix:** (1) Add `isRowid bool` to the `schema` struct, set to false when `WITHOUT ROWID` is present. (2) In `Store.Create`, branch to a clustered-index storage path that uses the PRIMARY KEY as the B-tree key (no implicit rowid column). (3) Block `_rowid_` queries on WITHOUT ROWID tables with `no such column: _rowid_`. (4) Add `TestWithoutRowId_TableCreation`, `TestWithoutRowId_PrimaryKeyClustered`, `TestWithoutRowId_NoRowIdCol`. | medium | large | iter-31 | EX/store.go, EX/constraints.go, EX/writers.go, PS/ddl.go |
| REQ000911 | EX/parser | **`IN table_name` shorthand not supported — `SELECT 4 IN t4n` returns NULL/0 instead of 1.** Evidence: `tests/sqlcmp/corpus/test/evidence/in1.test:618-621` and L623-626, L629-632, L634-637, L640-643. SQLite supports `expr IN tbl` as sugar for `expr IN (SELECT * FROM tbl)`. The parser currently does not recognize the table-name form of the IN right-hand side — only subquery `(SELECT ...)` and value list `(v1, v2, ...)` are supported. t4n contains `{2,3,4,NULL}` so `4 IN t4n` should return `1`, but Razor returns `NULL` (probably trying to evaluate against an empty result set or NULL-skipping). **Fix:** (1) In `PS/expr.go` `parseInRHS`, when no `(` follows, parse a table name (`QualifiedName`) and wrap it as `SELECT * FROM <name>`. (2) In `evalInValue`/`evalInHashValue` accept the synthetic `SubqueryExpr` form. (3) Add `TestIn_TableShorthand` covering `SELECT 4 IN t4n` and `SELECT 5 NOT IN t4n` with NULL-containing sets. **Test evidence:** `evidence/in1.test:618` (32 fails total in this file). | medium | small | iter-31 | PS/expr.go, EX/eval.go |
| REQ000912 | EX/eval | **NULL three-valued logic broken in index-driven `>` / `<` / `>=` / `<=` comparisons — `SELECT pk FROM tab1 WHERE col3 > 221 ORDER BY 1 DESC` returns 0 rows instead of 8.** Evidence: `tests/sqlcmp/corpus/test/index/orderby/10/slt_good_0.test:354-364` and many parallel tests. tab1 = tab0 with col3 = {846,562,652,51,256,957,656,294,878,105}; `> 221` should match 8 rows. Razor returns 0. The same `>` issue also surfaces in `evidence/slt_lang_createview.test:29-37` (`SELECT x FROM t1 WHERE x>0` returns 2 rows including the NULL x). Root cause: when the IndexScan returns NULL keys, the `> 221` comparison treats `NULL > 221` as FALSE (instead of NULL, which would still exclude from result — wait, NULL should be excluded from WHERE, so it shouldn't be 2 rows). Need to trace whether IndexScan is dropping the non-NULL rows or returning only NULLs. **Fix:** (1) Inspect `IndexScan.Next` to confirm it emits rows whose key is non-NULL and `key > 221`. (2) In `evalCompare` ensure `>` for INTEGER returns TRUE/FALSE only — never NULL. (3) Add `TestIndexScan_RangeWithNull` with a 10-row table where 8 rows have col3 > 221 and 2 have col3 IS NULL; query should return exactly 8. **Test evidence:** orderby slt_good_0=7324 fail; createview 5 fail. | high | medium | iter-31 | EX/operators.go, EX/eval.go, EX/indexscan.go |
| REQ000913 | EX/eval | **Complex WHERE with `OR`, `IN (list)`, `IS NULL` returns 0 rows when used with index — `SELECT pk FROM tab1 WHERE ((col1 < 71.25)) OR (col3 IN (53,42,27,44) OR (col3 IS NULL)) AND (col0 >= 42)` returns 0 cells.** Evidence: `tests/sqlcmp/corpus/test/index/commute/10/slt_good_0.test:115-119` and many parallel tests. The OR-distribution of `IN (list) + IS NULL` over a 3-conjunct AND with `>` comparison is failing when the index is selected. The `(col3 IS NULL)` short-circuits or the index lookup drops rows whose `col3` is NULL. **Fix:** (1) Verify `Filter` correctly handles `(IS NULL)` in disjunction — it should never be eliminated by a `<` or `>` range filter. (2) The index scan must return rows with col3 = NULL and col0 >= 42 regardless of col1 condition. (3) Add `TestIndexScan_OrWithIsNull` reproducing this query shape. **Test evidence:** commute 10/100 = ~6700/7200 fails each. | high | medium | iter-31 | EX/operators.go, EX/intermediate.go, EX/eval.go |
| REQ000914 | EX/plan | **`Planner.n3JoinOrdering` panics with `index out of range [0] with length 0` at `planner.go:2630` on multi-table joins — REQ837 fix incomplete.** Evidence: `tests/sqlcmp/corpus/test/random/select/slt_good_0.test` and `random/groupby/slt_good_0.test` both panic the test binary mid-run with the same stack. The iter-34 fix added `if len(heap) == 0 { break }` but another code path after the break (or before the break) still indexes `heap[0]` or `best.order[0]` without a length check. Specifically the panic location is `n3JoinOrdering` line 2630, which is past the `if len(heap) == 0 { break }` block — meaning the heap has non-zero length but its first element has a zero-length order slice. **Fix:** (1) In `n3JoinOrdering` after popping `best := heap[0]`, also check `len(best.order) == 0` and fall back to the raw `joinTables` order. (2) Audit the heap-pop / re-heapify logic for any path that produces a 0-length `order` slice. (3) Add `TestN3JoinOrdering_EmptyOrderFallback` and `TestPlanner_RandomSelectNoPanic` that reproduces the random_select and random_groupby queries without panic. (4) The fix must also not crash on `random_select_slt_good_0.test` `SELECT DISTINCT * FROM tab0, tab0 AS cor0, tab0 AS cor1` (3-way self-join with DISTINCT). **Test evidence:** random_select + random_groupby = 2 panics; iter-34 only fixed `len(heap) == 0` not `len(order) == 0`. | critical | small | iter-35 | EX/planner.go |
| REQ000915 | EX/eval | **NULLIF and COALESCE with chained unary minus / nested CASE produce wrong results — `SELECT NULLIF(-COUNT(*), +67 * - - (+25) + 89 + -39 * 63) + +54` returns 54 but should return 53.** Evidence: `tests/sqlcmp/corpus/test/random/expr/slt_good_0.test:72-75` and L86-89, L144-147. Multiple parse/eval issues: (1) `NULLIF(-COUNT(*), expr)` with complex chained unary minus inside the second arg computes the second arg wrong (evaluates the `- - 25` as `-(-25)=25` but the surrounding arithmetic differs). (2) `COALESCE(CASE - CASE WHEN ... END WHEN ... END, MIN(ALL -30) * 45 * 77)` returns NULL when the second arg is non-NULL — Razor's `COALESCE` does not fall through to the second arg. (3) `73 - + -75 + +56 * + +COUNT(*) * -89 + +87 DIV - -98 * + +68 * +32` evaluates to 148 instead of -4836 — `DIV` (integer division) precedence is wrong relative to `*`. **Fix:** (1) Audit `evalUnary` for chained `-` and `+` prefix operators — verify a `+ + COUNT(*)` (two unary plus) is parsed as `+ (+ COUNT(*))` not `+ +COUNT(*)`. (2) `COALESCE` must evaluate each arg until non-NULL is found; verify the second-arg type-coercion doesn't error out. (3) `DIV` (integer division operator) should have same precedence as `*` and `/` — check the precedence table in `PS/ps.go:283-305`. (4) Add `TestNullIf_ChainedUnaryMinus`, `TestCoalesce_NestedCase`, `TestDiv_OperatorPrecedence`. **Test evidence:** random/expr = 3035 + 2601 fails. | high | medium | iter-31 | EX/eval.go, EX/aggregate.go, PS/ps.go, PS/expr.go |
| REQ000916 | EX/parser | **`FROM ( tab0 AS cor0 CROSS JOIN tab0 cor1 )` parenthesized table-list in derived table not parsed — `ps: syntax error at "tab0"`.** Evidence: `tests/sqlcmp/corpus/test/random/aggregates/slt_good_0.test:74-78` and L81-85. The query `SELECT + + MIN(ALL - + 32) AS col0, + COUNT(*) * + COUNT(*) FROM ( tab0 AS cor0 CROSS JOIN tab0 cor1 )` has a subquery in the FROM clause containing a parenthesized join expression. The parser does not support `( t1 alias1 JOIN t2 alias2 )` as a derived-table body. **Fix:** (1) In `PS/select.go parseFrom` after parsing the table-or-subquery, allow a parenthesized join expression `( t1 [AS a] [[INNER|LEFT|CROSS] JOIN t2 [AS b] [ON ...]] ... )` and wrap as a derived table. (2) Add `TestParseFrom_ParenthesizedJoin`. (3) Verify the planner handles the synthetic derived-table subquery correctly. **Test evidence:** random/aggregates 883 + 790 fails. | medium | small | iter-31 | PS/select.go, EX/planner.go |
| REQ000917 | EX/eval | **`AVG(DISTINCT NULL)` returns 0 instead of NULL — `SELECT MIN(DISTINCT col1) * - + AVG(DISTINCT + ( - + CAST(NULL AS SIGNED) )) AS col2, COUNT(ALL -col0) AS col2 FROM tab2 AS cor0` returns `[0, 3]` but should return `[NULL, 3]`.** Evidence: `tests/sqlcmp/corpus/test/random/aggregates/slt_good_0.test:446-448` and L453-455. `CAST(NULL AS SIGNED)` is NULL, `AVG(DISTINCT NULL)` over a single-NULL set should be NULL, not 0. **Fix:** (1) In `EX/aggregate.go` `evalAggregate` for AVG with `DISTINCT` and all-NULL input, return NULL (not 0). (2) Verify `SUM` and `COUNT` (DISTINCT) of all-NULL also return NULL/0 correctly. (3) Add `TestAvg_DistinctAllNull` with `SELECT AVG(DISTINCT NULL)`. **Test evidence:** random/aggregates 883 fails. | medium | small | iter-31 | EX/aggregate.go, EX/eval.go |
| REQ000918 | EX/eval | **`SUM(DISTINCT -col2)` produces wrong result — `SELECT DISTINCT -SUM(DISTINCT - +col2) + + -98 AS col2 FROM tab1 AS cor0` returns -98 instead of 125.** Evidence: `tests/sqlcmp/corpus/test/random/aggregates/slt_good_0.test:536-538`. The expression `+ + - 98` is `-(98) = -98`. The `-SUM(DISTINCT -col2)` should compute `SUM(DISTINCT -col2)` first then negate. With 10 rows of col2 (text column), this case is broken because col2 is TEXT type. **Fix:** (1) In `evalAggregate` for SUM with TEXT-typed column, either coerce to 0 (SQLite behavior) or return NULL. (2) The current `-98` result suggests SUM is returning 0 (text col → 0 in some path), then `-(0) + -98 = -98`. (3) Add `TestSum_DistinctOnTextColumn` verifying SQLite-compatible behavior. **Test evidence:** random/aggregates. | medium | small | iter-31 | EX/aggregate.go, EX/eval.go |
| REQ000919 | EX/writer | **INSERT OR REPLACE state leak / wrong semantics — `SELECT x, y FROM t1 WHERE x=2` returns `(2, 'replace')` when it should return `(2, 'insert')` (after `INSERT INTO t1 VALUES(2, 'insert')`).** Evidence: `tests/sqlcmp/corpus/test/evidence/slt_lang_replace.test:31-35` and similar. Razor appears to overwrite the previous `INSERT INTO t1 VALUES(2, 'insert')` with a later `REPLACE INTO t1 VALUES(2, 'replace')` even though they are separate statements separated by other statements (L42-45, L49-55, L59-66, L69-74). Symptom: subsequent `SELECT` reads the most-recent-replace, not the most-recent-insert. **Possible root cause:** (1) The `REPLACE` conflict resolution applies to all INSERTs, not just those that conflict. (2) The conflict resolution state is sticky across statements. (3) `INSERT OR REPLACE` is implementing the same as `REPLACE` for ALL inserts, not just PK-conflicting ones. **Fix:** (1) Audit `buildInsert`/`applyInsert` for `REPLACE` and `INSERT OR REPLACE` branches — verify the REPLACE path is only taken on PK conflict, not unconditionally. (2) Add `TestInsertOrReplace_OnlyOnConflict` verifying that `INSERT OR REPLACE` into a non-conflicting row behaves as plain INSERT. **Test evidence:** slt_lang_replace 3 fail. | high | small | iter-31 | EX/writers.go, EX/insert.go |
| REQ000920 | EX/eval | **`SELECT x, y FROM t1 WHERE x=2` (where t1 has no x=2) returns 1 phantom row — Razor WHERE filter on PK = nonexistent value returns a row.** Evidence: `tests/sqlcmp/corpus/test/evidence/slt_lang_replace.test:24-27` — before any `INSERT INTO ... VALUES(2, ...)` is run, the test queries `WHERE x=2` and expects 0 rows, but Razor returns 1 row. This may be a separate bug from REQ919 (the query at L24 is before any insert of x=2, so the INSERT-OR-REPLACE issue cannot explain it). **Possible root cause:** (1) `SeqScan`/`IndexScan` returns the first row regardless of WHERE filter. (2) `Filter` operator passes through rows when the predicate errors / panics / is nil. (3) `Row.Lookup` returns wrong value. **Fix:** (1) Trace a simple `SELECT x FROM t1 WHERE x=999` on a 2-row table; should return 0 rows. (2) If a predicate error is being silently swallowed, the Filter should propagate the error. (3) Add `TestWhere_PkNoMatchReturnsEmpty` with deterministic data. **Test evidence:** slt_lang_replace L24 fail. | critical | small | iter-31 | EX/intermediate.go, EX/operators.go, EX/eval.go |
| REQ000921 | EX/eval | **`WHERE NULL IS NULL` returns 0 rows instead of all rows — the simplest possible `IS NULL` test fails.** Evidence: `tests/sqlcmp/corpus/test/index/random/10/slt_good_0.test:204-207` and many parallel queries. `SELECT col0 + + col0 AS col4 FROM tab1 AS cor0 WHERE NULL IS NULL` should return 10 rows (NULL IS NULL is always TRUE → no rows filtered out), but Razor returns 0 cells (hash mismatch shows 0 cells vs 10 expected). The same `IS NULL` evaluation is also broken for column references (`WHERE col3 IS NOT NULL` returns wrong hash at L269). This is the same root cause as REQ912/913 (NULL three-valued logic) but with the simplest possible reproduction. **Fix:** (1) The `IS NULL` evaluation in `evalBinary`/`evalIs` for the special `NULL` literal must return TRUE, not NULL. (2) For column `col3 IS NULL`, the value `col3` is read row-by-row and IS NULL must return TRUE if the column value is SQL NULL. (3) Audit `evalBinary` for `T_IS` and `T_NOTNULL` operators — verify the literal `NULL` case. (4) Add `TestWhere_NullIsNull_ReturnsAllRows` and `TestWhere_ColIsNull`. **Test evidence:** index/random/10 slt_good_0 = 1465 fails (most are `IS NULL`/`IS NOT NULL` based on first failure context). | critical | small | iter-31 | EX/eval.go, EX/intermediate.go |
| REQ000922 | EX/eval | **Aggregates with chained unary minus or parens in argument return wrong value — `SELECT - MAX(--col0) FROM tab1` returns NULL instead of -91.** Evidence: `tests/sqlcmp/corpus/test/random/aggregates/slt_good_1.test:64-65` and L70-71, L95-98, L101-104, L107-108. tab1 has col0={51,85,91}; MAX=91, so -91 expected. Razor returns NULL. The unary-minus chain `--col0` (which equals `+col0` = the value itself) is mis-evaluated when wrapped in MAX. Same issue for `SELECT - COUNT(ALL --85) FROM tab0` returns 0 instead of -3. **Fix:** (1) The aggregate argument `expr` in `evalAggregate` may be NULL because the inner `- -col0` is being treated as NULL by some path. (2) Audit `evalUnary` for chained `-` `+` then for parenthesized forms `(...)`. (3) Verify the aggregate operator correctly threads through the unary chain before evaluating. (4) Add `TestAggregate_UnaryMinusArg`. **Test evidence:** random/aggregates slt_good_1 = 790 fails. | high | small | iter-31 | EX/aggregate.go, EX/eval.go |
| REQ000923 | EX/eval | **`SELECT +COUNT(*)` without FROM returns 0 instead of 1 (SQLite quirk).** Evidence: `tests/sqlcmp/corpus/test/random/expr/slt_good_1.test:45-48` and L51-54, L67-70, L73-76, L79-82. SQLite's `SELECT COUNT(*)` (no FROM) returns 1 (one row, count of nothing), but Razor returns 0. The test uses label-1 to enforce the same hash across multiple `SELECT +COUNT(*)` variants. This is a SQLite-specific edge case where COUNT(*) without a FROM is treated as a one-row query with count=0 of an empty relation, returning 1 row. **Fix:** (1) In `evalAggregate` for COUNT(*), if the source relation is empty (no FROM), return 1 instead of 0. (2) Or: handle the no-FROM case in the planner by injecting a synthetic single-row relation. (3) Add `TestCountStar_NoFromReturnsOne`. **Test evidence:** random/expr slt_good_1 = 2601 fails (5 of which are the label-1 COUNT(*) pattern). | low | small | iter-31 | EX/aggregate.go, EX/planner.go |
| REQ000924 | EX/eval | **`WHERE (col1 >= 901.86)` on FLOAT-typed column returns 0 rows (index scan broken for FLOAT comparison).** Evidence: `tests/sqlcmp/corpus/test/index/orderby_nosort/100/slt_good_0.test:379-381` and many parallel queries. tab1 has 100 rows with FLOAT col1; `>= 901.86` should match a few rows. Razor returns 0. Similar to REQ912 (integer comparison) but specifically for FLOAT type — the index lookup may be using wrong key type for FLOAT or rounding incorrectly. **Fix:** (1) Verify `IndexScan` correctly handles FLOAT-typed columns with FLOAT comparison values. (2) Check if `valueFromAny` or `lookupKey` converts the FLOAT correctly. (3) Add `TestIndexScan_FloatRangeQuery` with a 10-row table where col is FLOAT and `WHERE col >= X` should return N rows. **Test evidence:** orderby_nosort/100 = 3576 fails; orderby/100 = 7704 fails. | high | small | iter-31 | EX/operators.go, EX/eval.go |
| REQ000925 | EX/writer | **`UNION ALL` over filtered table + its complement returns 0 rows — `SELECT pk, col0 FROM tab0 WHERE col0 = 49 UNION ALL SELECT pk, col0 FROM tab0 WHERE NOT (col0 = 49)` returns 0 cells instead of 20.** Evidence: `tests/sqlcmp/corpus/test/index/view/10/slt_good_1.test:162-165` and L172-175, L240-243. tab0 has 10 distinct col0 values; first arm returns 0 rows (no col0=49), second arm returns 10 rows. UNION ALL should emit 10 rows × 2 cells = 20 cells. Razor returns 0. This is REQ855 (UNION ALL bug) but the root cause may be different — the WHERE filter returns 0 rows. (a) The first arm `WHERE col0 = 49` returns 0 rows (no match — correct), but then UNION ALL should still emit 10 rows from the second arm. So this is `drainAll` not emitting the second arm's rows after the first arm produces 0 rows. **Fix:** (1) In `compound.go nextStreaming`, verify the left-empty case still pulls from right. (2) Add `TestUnionAll_LeftEmptyRightNonEmpty` and `TestUnionAll_LeftNonEmptyRightEmpty`. **Test evidence:** index/view/10 slt_good_1 = 2782 fails. | high | small | iter-31 | EX/compound.go, EX/operators.go |
| REQ000926 | EX/plan | **Cross join returns wrong row count — `SELECT 33*57 col1 FROM tab1, tab1 AS cor0` (3×3) returns 3 rows instead of 9.** Evidence: `tests/sqlcmp/corpus/test/random/aggregates/slt_good_7.test:499-501`, slt_good_6/11/17 similar patterns. The cross product of two 3-row tables should yield 9 rows. Razor returns 3 (the cardinality of one table). The n-ary join ordering is collapsing equivalent-table joins into a single scan stream. Distinct from REQ857 (self-join via alias) — this case uses 3+3 and same physical table; REQ857 was 1+1. **Fix:** (1) In `n3JoinOrdering` or its successor, when multiple FROM-list tables refer to the same physical table, treat each as a separate scan stream and produce the full Cartesian product. (2) Verify the NLJ block-mode does not re-use the cached right-side row set across the left side. (3) Add `TestCrossJoin_3x3Yields9Rows`. **Test evidence:** aggregates/6/7/11/17; expr/9 also has this pattern. | high | medium | iter-31 | EX/planner.go, EX/join.go, EX/operators.go |
| REQ000927 | EX/plan | **`SELECT DISTINCT` returns columns in wrong order — `SELECT DISTINCT COUNT(*) DIV +45 AS col1, 95` returns `[95, 0]` instead of `[0, 95]`.** Evidence: `tests/sqlcmp/corpus/test/random/aggregates/slt_good_6.test:277-279` and slt_good_9/11/17. When the SELECT list has both an aliased aggregate column and a constant column, the column order in the result is swapped. The aggregate column appears AFTER the constant. **Fix:** (1) In `Project` operator or `planSelect`, preserve the source column order in the SELECT list. (2) The DISTINCT dedup must not reorder the columns — the output schema must match the SELECT-list order. (3) Add `TestProject_DistinctColumnOrder` reproducing this query. **Test evidence:** aggregates/6/9/11/17. | high | small | iter-31 | EX/operators.go, EX/intermediate.go |
| REQ000928 | EX/eval | **Aggregate of constant expression returns NULL/0 — `SELECT MAX(36)`, `MIN(-6)`, `SUM(DISTINCT 17)`, `COUNT(30)` all return NULL/0 instead of the constant value.** Evidence: `tests/sqlcmp/corpus/test/random/expr/slt_good_2.test:74-77` (MAX(28) → NULL), slt_good_10 (MIN(36) → NULL), slt_good_14 (SUM(DISTINCT 17) → 0), slt_good_13 (COUNT(+(30)) → 0), slt_good_19 (MAX(DISTINCT 81) → NULL), slt_good_20 (MIN(ALL -6) → NULL). SQLite treats `SELECT MAX(36)` as a one-row query where MAX of `[36]` = 36, MIN of `[-6]` = -6, SUM = 17, COUNT = 1. Razor is producing NULL/0 for these. Distinct from REQ923 (no-FROM COUNT(*)) — this case has no FROM but the aggregate takes a constant argument. **Fix:** (1) In `evalAggregate`, when the source relation is empty/synthetic (no FROM), execute the aggregate over the implicit single row with the constant argument. (2) For MAX(36), MIN(36), SUM(36), COUNT(36) the result is 36 / 36 / 36 / 1. (3) Add `TestAggregate_ConstantArg_MaxMinSumCount`. **Test evidence:** expr/2/10/13/14/19/20 = ~30 fails each. | high | small | iter-31 | EX/aggregate.go, EX/eval.go |
| REQ000929 | EX/eval | **`NOT BETWEEN NULL AND x` returns 0 rows (incorrectly treated as FALSE instead of unknown-allowing).** Evidence: `tests/sqlcmp/corpus/test/random/aggregates/slt_good_7.test:524-526` — `SELECT * FROM tab0 WHERE col1 NOT BETWEEN NULL AND -col1` returns 0 rows, want 9 (all rows). SQLite treats `x NOT BETWEEN NULL AND y` as `NOT (x >= NULL AND x <= y)` = `NOT NULL` = NULL. But since the filter is NULL, the row is excluded — so 0 rows would be expected. The fact that want=9 means SQLite actually returns TRUE for this case (likely because `NULL BETWEEN NULL AND -y` simplifies to TRUE in some implementation). **Fix:** (1) Audit `evalBetween`/`evalNot` for NULL handling — verify whether `x NOT BETWEEN NULL AND y` should return TRUE (as SQLite does) or NULL (three-valued). (2) Add `TestWhere_NotBetweenNull`. **Test evidence:** aggregates/7 L524. | low | small | iter-31 | EX/eval.go, EX/operators.go |
| REQ000930 | EX/eval | **`HAVING NULL <= NULL` and other HAVING-NULL comparisons return `eval error` instead of a proper NULL/FALSE result.** Evidence: `tests/sqlcmp/corpus/test/random/groupby/slt_good_10.test:321-323` — `HAVING NULL <= NULL` errors out. Also slt_good_11 L114: `HAVING NULL <= NULL` errors. SQLite returns NULL/FALSE (filter is unknown, group excluded) but the row evaluation should not error. **Fix:** (1) In `Filter.evalPredicate` for the `HAVING` clause, when the predicate evaluates to NULL, return NULL (not an error). (2) The check `if predicate errored` should distinguish `eval error` (real failure) from `predicate=NULL` (valid result). (3) Add `TestHaving_NullComparison_NoError`. **Test evidence:** groupby/10 L321, L1362, L1855; groupby/11 L114. | medium | small | iter-31 | EX/operators.go, EX/intermediate.go |
| REQ000931 | EX/eval | **`CAST(NULL AS DECIMAL)` and `CAST(NULL AS SIGNED)` in projection return wrong hash — possibly cast-of-NULL returning non-NULL.** Evidence: `tests/sqlcmp/corpus/test/random/groupby/slt_good_2.test:1470-1472` and slt_good_4 L275-277. `SELECT CAST(NULL AS DECIMAL) FROM tab1, tab0 cor0 GROUP BY cor0.col1` returns wrong hash. Razor may be returning an empty row instead of a NULL row, or the CAST of NULL is producing an empty string/text. **Fix:** (1) In `evalCast` for NULL input, return NULL regardless of target type. (2) Verify the row count for `SELECT CAST(NULL AS X)` is the same as the source row count (1 per input row). (3) Add `TestCast_NullToAnyType`. **Test evidence:** groupby/2/4. | low | small | iter-31 | EX/eval.go, EX/operators.go |
| REQ000932 | EX/eval | **`SELECT col0 / col2 col0 FROM tab0 GROUP BY col2, col0` returns 0.329 for some row but expected 0 — DIV vs `/` operator confusion on integer division.** Evidence: `tests/sqlcmp/corpus/test/random/groupby/slt_good_5.test:420-422`. The query uses `/` (true division) but the result 0.329 is the FLOAT division. The expected 0 means SQLite returns integer division (i.e., SQLite treats `/` as integer division when both operands are INTEGER). Actually SQLite's `/` is always float division; if expected is 0, it means the query is `cor0.col0 / cor0.col2` where col0=1 and col2=3 → 1/3 = 0.333 (floating). Want=0 is wrong. Likely a test artifact or Razor is computing wrong. **Fix:** (1) Verify `evalBinary` for `/` with INTEGER/INTEGER operands returns REAL (0.333), not INT (0). (2) Add `TestDiv_IntegerInteger_ReturnsReal`. **Test evidence:** groupby/5 L420. | low | small | iter-31 | EX/eval.go, EX/operators.go |
| REQ000933 | EX/eval | **Aggregate of TEXT column over DISTINCT returns 0 — `SELECT DISTINCT - - SUM(ALL -33) FROM tab1 cor0` got 0, want -99 (col1=33 expected).** Evidence: `tests/sqlcmp/corpus/test/random/aggregates/slt_good_14.test:53-55` and aggregates/2 L47 (CAST(SUM(ALL -37) AS SIGNED) got 0, want -111). tab1 has 3 rows of col1={14,5,47} (INTEGER). `SUM(ALL -33)` — wait, this is a constant `-33`, not a column. So `SUM(ALL -33)` over 3 rows = -33 + -33 + -33 = -99. Razor returns 0. The issue: `SUM(ALL constant)` with no FROM (or with FROM but DISTINCT). **Fix:** (1) `SUM(ALL -33)` over 3 rows should give -99, not 0. The aggregate with `ALL` keyword applies to the input — the input is the value -33. (2) Verify `evalAggregate` with constant input: each input row contributes the constant to the aggregate. (3) Add `TestSum_AllConstant`. **Test evidence:** aggregates/14 L53; aggregates/2 L47. | medium | small | iter-31 | EX/aggregate.go, EX/eval.go |
| REQ000935 | EX/eval | **`IN (list)` with CAST expressions in the list returns wrong — `WHERE -col2 IN (-14 * -+11, col2, CAST(++col1 AS SIGNED) * -CAST(-col0 AS SIGNED), +col1)` produces wrong result.** Evidence: `tests/sqlcmp/corpus/test/random/aggregates/slt_good_67.test:40-42`. The IN list contains computed expressions including CAST of unary expressions. Razor fails to evaluate the list items correctly, returning wrong membership. Distinct from REQ934 (IN with single negation) — this is IN with a list of mixed expressions and CASTs. **Fix:** (1) In `evalInValue`/`evalInHashValue`, when each list item is a CAST or unary expression, evaluate it correctly to an INTEGER/TEXT/REAL value. (2) Verify the comparison `lhs == listItem` works for CAST outputs. (3) Add `TestIn_ListWithCastExpressions`. **Test evidence:** aggregates/67 L40. | medium | small | iter-31 | EX/eval.go, EX/operators.go |
| REQ000936 | EX/eval | **`COUNT(DISTINCT <unary col>)` returns 0 instead of the count — `SELECT +55 * COUNT(DISTINCT +-col2) AS col2 FROM tab1` got 0, want 55 or 165.** Evidence: `tests/sqlcmp/corpus/test/random/aggregates/slt_good_74.test:114-116`. `COUNT(DISTINCT)` on a unary-negated column (or any expression) is broken. Tab1 has 3 distinct col2 values; `DISTINCT +-col2` should also be 3 distinct values. **Fix:** (1) In `evalAggregate` for COUNT(DISTINCT), the grouping key should be the *evaluated* expression value, not the raw unary token. (2) Add `TestCountDistinct_UnaryColumn`. **Test evidence:** aggregates/74 L114, 28 L104. | medium | small | iter-31 | EX/aggregate.go, EX/eval.go |
| REQ000937 | EX/eval | **`SELECT col1 / (++col0) + +col1 AS col1 FROM tab2` — division inside parens with chained unary plus produces wrong result.** Evidence: `tests/sqlcmp/corpus/test/random/aggregates/slt_good_63.test:149-151`. `(++col0)` should be `+(+col0)` = col0, then `col1/col0 + col1`. Razor's expression evaluator likely treats `++col0` (chained unary plus) incorrectly when wrapped in parens — possibly the AST is `+ (+ col0)` parsed correctly but the division returns wrong. **Fix:** (1) Verify the chained-unary `++col0` in parens evaluates to the col0 value. (2) Check the division `col1 / col0` for INTEGER/INTEGER operands (should be REAL). (3) Add `TestParen_ChainedUnaryInDivision`. **Test evidence:** aggregates/63 L149. | low | small | iter-31 | EX/eval.go, EX/operators.go |
| REQ000938 | EX/eval | **`WHERE NOT 2 < col0` returns wrong rows — `NOT 2 < col0` should be `2 >= col0` (rows where col0 <= 2) but Razor returns a different set.** Evidence: `tests/sqlcmp/corpus/test/random/aggregates/slt_good_62.test:39-41`. Razor's NOT-comparison pre-processing may be optimizing `NOT 2 < col0` to `2 >= col0` but the boundary handling is wrong. **Fix:** (1) Verify the `NOT` operator's interaction with `<` and `>=`. (2) For `NOT 2 < col0` parsed as `NOT (2 < col0) = (2 >= col0)`, the boundary should be inclusive of 2. (3) Add `TestNot_LtWithConstant`. **Test evidence:** aggregates/62 L39. | low | small | iter-31 | EX/eval.go, EX/intermediate.go |
| REQ000939 | EX/eval | **`CAST(NULL AS DECIMAL) + +col1 * -66` returns NULL (correct) but row count or hash differs — possibly NULL spread across joined rows.** Evidence: `tests/sqlcmp/corpus/test/random/aggregates/slt_good_59.test:103-105`. The query has CAST(NULL) in projection and a NOT-IN with computed value in WHERE. Razor returns wrong hash, suggesting the NULL propagation through the join/aggregate may be wrong. **Fix:** (1) Verify CAST(NULL AS DECIMAL) returns NULL row-by-row. (2) Verify the WHERE NOT-IN with computed value `-56 + -7 / col0 * -col2 NOT IN (+-col2)` evaluates correctly. (3) Add `TestCast_NullDecimal_WithNotIn`. **Test evidence:** aggregates/59 L103. | low | small | iter-31 | EX/eval.go, EX/operators.go |
| REQ000940 | EX/eval | **`SELECT DISTINCT col2 - ++col2 + col2 / +col0 FROM tab0` — operator precedence wrong (got 14.420 want 15).** Evidence: `tests/sqlcmp/corpus/test/random/aggregates/slt_good_75.test:119-121`. The expression `col2 - ++col2 + col2 / +col0` should be `((col2 - (+col2)) + (col2 / col0))` = `(0 + (col2/col0))` = col2/col0. For col0=1, col2=15 → 15.0. Razor returns 14.420 (a value that doesn't match this formula). Likely the unary chain `++col2` is being mis-evaluated. **Fix:** (1) In `evalBinary` for `-` with `++col2` on the right, the expression `col2 - ++col2` should be `0` (both sides equal). (2) Verify the chained unary plus evaluates to the column value. (3) Add `TestSubtract_ColumnMinusUnaryPlusSelf`. **Test evidence:** aggregates/75 L119. | low | small | iter-31 | EX/eval.go, EX/operators.go |
| REQ000941 | EX/eval | **`SELECT DISTINCT - MIN(DISTINCT - col0) * - 30 + COUNT(*) AS col1 FROM tab1 WHERE + col0 IS NOT NULL` — DISTINCT projection across NULL filter produces wrong row count or values.** Evidence: `tests/sqlcmp/corpus/test/random/aggregates/slt_good_112.test:59-61`. Razor returns wrong result for this DISTINCT-with-aggregates-and-WHERE combo. **Fix:** (1) Audit the interaction between DISTINCT dedup and the aggregate operators. (2) Verify the WHERE `+col0 IS NOT NULL` filter (REQ921) works when col0 is always non-NULL. (3) Add `TestDistinct_AggregateWithIsNotNull`. **Test evidence:** aggregates/112 L59. | low | small | iter-31 | EX/operators.go, EX/intermediate.go |
| REQ000942 | EX/eval | **`SUM(ALL CAST(NULL AS SIGNED))` returns 0 — SUM of all-NULL should be NULL, not 0.** Evidence: `tests/sqlcmp/corpus/test/random/aggregates/slt_good_73.test:89-91` and slt_good_37 L59. Same root cause as REQ917 (AVG) and REQ918 (SUM DISTINCT): SUM(ALL NULL) over N rows of NULL should be NULL, not 0. **Fix:** (1) In `evalAggregate` for SUM with all-NULL input, return NULL. (2) Verify COUNT(NULL)=0 returns 0 (correct), but SUM(NULL) should be NULL. (3) Add `TestSum_AllNullConstant_ReturnsNull`. **Test evidence:** aggregates/37, 73. | medium | small | iter-31 | EX/aggregate.go, EX/eval.go |
| REQ000943 | EX/eval | **`MAX(ALL + - 64) * CAST(++SUM(ALL - 96) AS SIGNED) WHERE NOT NULL IS NOT NULL` — multiple bugs combined in one query.** Evidence: `tests/sqlcmp/corpus/test/random/aggregates/slt_good_72.test:123-125`. The query has MAX(ALL `+-64`) returning NULL because constant arg broken (REQ928), CAST(SUM(ALL `-96`)) = CAST(SUM-NULL) = NULL (REQ942), and `WHERE NOT NULL IS NOT NULL` returns all rows (REQ921). The 3 bugs combine to produce the wrong result. **Fix:** (1) Fix REQ921, REQ928, REQ942 individually. (2) Add `TestCombinedAggregateAndIsNull`. **Test evidence:** aggregates/72 L123. | low | small | iter-31 | EX/aggregate.go, EX/eval.go, EX/intermediate.go |
| REQ000944 | EX/eval | **`-COUNT(DISTINCT + col0) - ++SUM(DISTINCT -(- 11)) + 32` — chain of DISTINCT aggregates with unary minus produces wrong result.** Evidence: `tests/sqlcmp/corpus/test/random/aggregates/slt_good_111.test:121-123`. COUNT(DISTINCT) and SUM(DISTINCT) chained with unary minus and the addition of a constant. Razor returns wrong value (likely just the constant 32). **Fix:** (1) Fix `SUM(DISTINCT -(-11))` = SUM(DISTINCT 11) = 11 (REQ928). (2) Fix `COUNT(DISTINCT +col0)` (REQ936). (3) Add `TestChain_DistinctAggregates_UnaryChain`. **Test evidence:** aggregates/111 L121. | low | small | iter-31 | EX/aggregate.go, EX/eval.go |
