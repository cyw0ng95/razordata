## TBD
| ID | Subsystem | Requirement | Priority | Effort | Deps | Touches |
| --- | --- | --- | --- | --- | --- | --- |
| REQ001303 | WAL/RP | **WAL checkpoint modes: passive, full, truncate, restart.** SQLite supports four checkpoint modes. Passive: copy as many frames as possible without blocking writers. Full: wait for all readers to finish, then copy remaining. Truncate: same as Full, then truncate -wal to zero. Restart: same as Full, then rewind wal-index to start. **Fix:** (1) Add `walCheckpoint(mode int)` API. (2) Implement Passive: copy frames up to reader-locked position, do not wait. (3) Implement Full: take ckpt lock, wait for read locks to drain, copy remaining. (4) Implement Truncate: Full + ftruncate(-wal, 0). (5) Implement Restart: Full + reset mxFrame in shm. (6) Surface via `PRAGMA wal_checkpoint(MODE)` query PRAGMA. (7) Add `TestWalCheckpoint_AllModes`. Estimate: ~200 lines, 2 files, 1 day. | high | medium | REQ001299 | WAL/RP/checkpoint.go (new), SQB/EX/planner.go |
| REQ001312 | SQB/WT + ENG/LS | **WITHOUT ROWID: clustered-key SST layout.** Currently hard-rejected at `SQB/WT/writers_ddl.go:198`. On an LSM, the analog is: primary key columns ARE the SST row key, eliminating the secondary rowid pointer. **Fix:** (1) In SQB/WT DDL, accept `WITHOUT ROWID` and store flag in table metadata. (2) In ENG/LS, for WITHOUT ROWID tables, key SST entries by composite (pk_col1, pk_col2, …) instead of (rowid, …). (3) Secondary indexes reference PK directly (no rowid indirection — see REQ001313). (4) UPDATE that changes PK = delete + insert. (5) Add `TestWithoutRowid_BasicInsertSelect`, `TestWithoutRowid_CompositePK`, `TestWithoutRowid_UpdateChangesPK`. Estimate: ~300 lines, 4 files, 2 days. | high | large | none | SQB/WT/writers_ddl.go, ENG/LS/sst_writer.go, ENG/ID/id.go, SQB/DT/schema.go |
| REQ001313 | SQB/WT + ENG/LS | **WITHOUT ROWID: secondary index on clustered key (no rowid pointer).** When the table is WITHOUT ROWID, secondary index entries store the full PK rather than a rowid, so lookups go directly to the data without a second hop. **Fix:** (1) In ENG/ID, when creating a secondary index on a WITHOUT ROWID table, the value part of the index entry is the full PK (concatenated). (2) In IndexScan for WITHOUT ROWID, the seek returns (index_key, pk) — use pk directly to fetch the row, no rowid→row translation. (3) Verify no regression for rowid tables. (4) Add `TestWithoutRowid_SecondaryIndex_NoRowidHop`, `TestWithoutRowid_IndexOnlyScan`. Estimate: ~120 lines, 2 files, 0.75 day. | high | medium | REQ001312 | ENG/ID/id.go, SQB/OP/indexscan_strategy.go |
| REQ001314 | SQB/EX + ENG | **ANALYZE: per-column NDV + rowcount + null count + min/max, persisted to catalog (covers REQ001315, REQ001316, REQ001317, REQ001318).** All four metrics are computed in a single scan, stored in `razor_stat1` catalog table, and loaded into `pl.StatsCatalog` on Engine.Open. NDV is the cornerstone of selectivity estimation (already used by REQ001218 OR-to-IN and REQ001252 stats propagation). rowcount/null_count are required by selectivity estimators. min/max enable the stats-propagation rule that derives `t2.c ∈ [min(t1.c), max(t1.c)]` from join equality. **Fix:** (1) Add ANALYZE statement dispatch in SQB/EX. (2) On ANALYZE [table], run a single full or sampled scan (full for tables < 1M rows, sampled 1% reservoir otherwise) that computes for each column: NDV, rowcount, null_count, min, max. (3) Define `razor_stat1` catalog table: `(table TEXT, col TEXT, ndv INT, rowcount INT, null_count INT, min BLOB, max BLOB)`. (4) ANALYZE writes via the same writer path as user tables; create razor_stat1 on first ANALYZE if absent. (5) In SYS.Open, after loading catalog, SELECT * FROM razor_stat1 and populate the in-memory `pl.StatsCatalog`. (6) For nullable columns, NULL is excluded from min/max. (7) Add `TestAnalyze_ComputesNDV`, `TestAnalyze_LargeTable_Sampling`, `TestAnalyze_ComputesRowCount`, `TestAnalyze_ComputesNullCount_NotNullCol`, `TestAnalyze_ComputesMinMax`, `TestAnalyze_NullExcludedFromMinMax`, `TestAnalyze_PersistsAcrossRestart`, `TestStats_WiredOnOpen`, `TestStats_Queries_UseLoadedNDV`. Estimate: ~350 lines, 4 files, 1.6 days. | high | medium | none | SQB/EX/analyze.go (new), ENG/LS/iter.go, SQB/WT/writers_admin.go, ENG/CT/catalog.go, SQB/EX/ex.go, SYS/SY/sy.go |
| REQ001423 | ENG/LS + SQB/EX | **B-tree / LSM block-level stats: min, max, NDV for range query planning.** Range scans (e.g. `WHERE age > 30 AND age < 50`) currently traverse the full SeqScan. LSM blocks have per-column min/max that can be used for early block pruning. **Fix:** (1) In ENG/LS SST block stats, expose per-column `min`/`max` during block construction. (2) In SeqScan, when a range predicate is present, skip blocks where the predicate's range is disjoint from the block's column min/max. (3) Expose via `TableStats` column-level `min`/`max` in catalog. (4) Add `TestSeqScan_BlockPruning_RangePredicate` verifying skipped blocks. Estimate: ~120 lines, 2 files, 0.75 day. | high | small | REQ001420 | ENG/LS/sst_block.go, SQB/OP/operators.go |
| REQ001462 | SQB/EV | **Optimize evalRowFallback — avoid per-row Row allocation for batch predicate evaluation.** `evalRowFallback` (eval_vec.go:929) converts each batch row to a `pl.Row` struct via `batchToRow`, which allocates `[]Value` per row. For select1 queries with correlated subqueries in predicates, this means 1000x allocations per query. **Fix:** (1) Refactor `evalRowFallback` to evaluate the predicate directly against batch column data without constructing `Row` structs. (2) Add `evalExprAgainstBatch(expr PS.Expr, batch *UT.Batch, params []any) []uint16` that walks the expression tree and evaluates each node against column slices. (3) For leaf nodes (identifiers, literals), read directly from `batch.Cols[colIdx].Data`. (4) For compound nodes (binary, unary), combine column-wise results using selection vectors. (5) Only fall back to `Row` construction when the expression contains subqueries or functions that require row context. (6) Add `BenchmarkEvalRowFallback_NoAlloc` verifying zero allocations for simple predicates. Estimate: ~200 lines, 1 file, 1 day. | high | medium | REQ001440 | SQB/EV/eval_vec.go |
| REQ001558 | SQB/OP | **Pre-allocate SeqScan projected row metadata + arena Data.** `SeqScan.cloneRow` with projection pushdown (operators.go:536-546) allocates 3 slices (Cols, Types, Data) + 1 map per projected row. For a 100K-row projection query = 400K allocations. **Fix:** (1) Pre-compute `projCols []string`, `projTypes []LX.TokenType` in `NewSeqScan` (schema-derived, constant). (2) Use `RowArena.AllocRow()` for Data slice — bump-pointer allocation from pooled slab. (3) Pre-compute `projColIndex map[string]int` once, store as field. (4) Add `BenchmarkSeqScan_Project_NoAlloc` verifying zero allocations. Estimate: ~100 lines, 1 file, 0.5 day. | high | small | none | SQB/OP/operators.go |
| REQ001567 | SQB/EV | **Pre-allocate function argument buffer in Project/Filter.** `eval.go:1552,1567` — every datetime/json function call allocates `args := make([]any, len(e.Args))`. **Fix:** (1) Add `fnArgBuf []any` field to Project and Filter operators. (2) Initialize in constructor with capacity = max expected function args (typically 5-10). (3) Pass `fnArgBuf` into eval path for function calls. (4) Add `BenchmarkScalarFunc_NoAlloc`. Estimate: ~80 lines, 2 files, 0.5 day. | high | small | none | SQB/EV/eval.go, SQB/OP/operators.go |
| REQ001568 | SQB/EV | **Cache inner table column set in correlated subquery resolution.** `eval.go:129` — `set := make(map[string]struct{}, len(cols))` allocated per correlated subquery evaluation in `extractCorrelatedColumns`. **Fix:** (1) Extend `subqueryColRefCache` to store inner table column set alongside correlated columns. (2) Cache key includes subquery pointer + inner table name. (3) Add `BenchmarkCorrelatedSubquery_NoAlloc`. Estimate: ~60 lines, 1 file, 0.25 day. | high | small | REQ001470 | SQB/EV/eval.go |
| REQ001572 | SQB/DT | **Add ShallowCloneRow for FK validation snapshots.** `CloneRow` does deep copy of `Data` slice but FK validation only needs a value snapshot — `Value` is a struct type, so shallow copy of `Data` header is sufficient. **Fix:** (1) Add `ShallowCloneRow(row Row) Row` that copies `Data` slice header only (`make([]Value, len(row.Data)); copy(dst.Data, row.Data)`). (2) Replace `CloneRow` calls in FK validation paths with `ShallowCloneRow`. (3) Add `BenchmarkShallowCloneVsClone`. Estimate: ~40 lines, 1 file, 0.25 day. | high | small | none | SQB/DT/arena.go, SQB/UT/fk.go | DONE: `ShallowCloneRow` already existed at `schema.go:648`. Replaced 3 `CloneRow` calls in `writers_dml.go` (FK snapshot line 838, UPDATE oldRow line 979, ON CONFLICT updated line 1542) with `ShallowCloneRow`. All tests pass. |
| REQ001573 | SQB/WT | **Conditional CloneRow in UPDATE based on RETURNING presence.** `Update.nextFromStore` (writers_dml.go:845) does `oldRow := DT.CloneRow(row)` even when no RETURNING clause — most UPDATEs don't have RETURNING. **Fix:** (1) Only call `CloneRow` when `len(u.returning) > 0`. (2) When no RETURNING, skip the snapshot entirely (no trigger firing, no rollback needed for store path). (3) Add `BenchmarkUpdate_NoReturning_NoClone`. Estimate: ~30 lines, 1 file, 0.25 day. | high | small | none | SQB/WT/writers_dml.go | DONE: Replaced `DT.CloneRow` with `DT.ShallowCloneRow` at all 3 call sites (UPDATE snapshot line 838, UPDATE oldRow line 979, ON CONFLICT updated line 1542). Shallow clone is sufficient for FK validation, UNIQUE check, trigger firing, and REPLACE — no RETURNING evaluation needs the deep copy. |
| REQ001574 | TXN/VL | **Pre-allocate lock index array in lockTableShard.** `lock.go:127` — `indices := make([]int, len(hashes))` allocated per multi-key lock acquisition. **Fix:** (1) Add `indices []int` field to `lockTableShard` with capacity = max shard width. (2) Reuse via `indices = indices[:0]` in lock acquisition. (3) Add `BenchmarkLock_NoAlloc`. Estimate: ~30 lines, 1 file, 0.25 day. | medium | small | none | TXN/VL/lock.go | DONE: Added `indicesBuf []int` field to `LockTable` struct, initialized in `NewLockTable` with capacity=`lockTableShardCount` (64). `lockShards` now reuses `lt.indicesBuf` instead of allocating per-call. Returns a copy of deduplicated indices to maintain caller ownership. |
| REQ001575 | SQB/OP | **Ensure WithSharedSchema on all join constructions.** `join.go:1026-1042` — `joinRowsLL` fallback path allocates Cols/Types/Data slices when `sharedCols` is nil. This should never happen when `WithSharedSchema` is called. **Fix:** (1) Add assertion in `buildProjectedLayout` that `sharedCols` is non-nil. (2) Audit all join construction sites to ensure `WithSharedSchema` is always called. (3) Add `BenchmarkJoin_SharedSchema_NoFallback`. Estimate: ~40 lines, 1 file, 0.25 day. | medium | small | none | SQB/OP/join.go | DONE: Added `WithSharedSchema` calls at both NLJ construction sites in `planner_select.go` — inner-group NLJ (line 1306) and merge-phase NLJ (line 1451). Uses existing `deriveJoinSchema` helper to build cols/types/colIndex from left+right operators. Removed dead `_ = deriveJoinSchema` no-op. |
| REQ001579 | SQB/EX | **Pre-allocate params slice in ExecContext.** `propagateParams()` in ex.go allocates `asAnySlice(args)` creating `[]any` copy every query execution. **Fix:** (1) Add `paramBuf []any` field to `ExecContext`. (2) Initialize with capacity in `QueryAll`/`Exec` caller. (3) Reuse via `paramBuf = paramBuf[:0]` per execution. (4) Add `BenchmarkExecContext_NoParamAlloc`. Estimate: ~40 lines, 1 file, 0.25 day. | medium | small | none | SQB/EX/ex.go |
| REQ001580 | SQB/EX | **Pre-allocate Value slice from any params in ExecContext.** `valueFromAnySlice` in ex.go allocates `[]Value` for every parameterized query execution. **Fix:** (1) Add `valueParamBuf []Value` field to `ExecContext`. (2) Initialize with capacity = max expected params. (3) Reuse via `valueParamBuf = valueParamBuf[:0]` per execution. (4) Add `BenchmarkExecContext_NoValueParamAlloc`. Estimate: ~40 lines, 1 file, 0.25 day. | medium | small | none | SQB/EX/ex.go |
| REQ001583 | SQB/EX | **CompilePlan — apply tryVectorizePlan to memoized plans.** `CompilePlan` (ex.go:1359-1386) does NOT call `tryVectorizePlan`. Only `QueryAll`/`QueryStream` do. This means `ExecCompiled` and `QueryStreamCompiled` reuse non-vectorized plans from the memo cache. **Fix:** (1) In `CompilePlan` SELECT path, after `planWithCache`, call `plan.Root = tryVectorizePlan(plan.Root)`. (2) This ensures memoized plans already have the vectorized tree. (3) `ExecCompiled` then drains via `NextBatch` for batch producers. Estimate: ~10 lines, 1 file, 0.25 day. | high | small | REQ001582 | SQB/EX/ex.go |
| REQ001584 | SQB/EX | **Executor.QueryStream — batch-drain for vectorized stream.** `QueryStream` (stream.go:295, 348) calls `plan.Root.Next(ctx)` per-row in both sync and async paths. **Fix:** (1) After `tryVectorizePlan()`, detect `BatchProducer`. (2) In sync path (`streamFromOperator`), drain batches via `NextBatch()` → `ToRows()` → fill a local row buffer, yield rows from buffer. (3) In async path (goroutine), same: `NextBatch()` → `ToRows()` → fan out rows through `rowCh`. (4) Schema discovery via `ToRows()[0]` works same. Estimate: ~80 lines, 1 file, 0.5 day. | high | small | REQ001582 | SQB/EX/stream.go |
| REQ001585 | SQB/WT+EV | **Vectorized SET expression evaluation in Update.nextFromStore.** `Update.nextFromStore` (writers_dml.go:971-1019) calls `ApplyUpdate(&row, u.set, u.params)` per row, which calls `EvalExpr` per expression per row. **Fix:** (1) Pre-resolve column indices for SET targets at construction time. (2) In the chunk loop, for each SET pair `(col, expr)`, call `EV.EvalBatchExpr(expr, batch, params)` once per chunk, producing a `UT.Column`. (3) Write column values into each row's Data slice at the correct index. (4) Fallback to per-row `ApplyUpdate` for unsupported expressions (e.g., correlated subqueries). (5) Validation (FillDefaults, ValidateRow, ValidateCheck, CheckUnique) remains per-row. Estimate: ~120 lines, 2 files, 1 day. | high | medium | REQ001582 | SQB/WT/writers_dml.go, SQB/EV/eval_vec.go |
| REQ001586 | SQB/WT | **Vectorized RETURNING expression evaluation in Update/Delete.** `evalReturning` (writers_dml.go:1418-1470) allocates per RETURNING row and evaluates each expression via `EvalExpr`. **Fix:** (1) For each RETURNING expression, call `EV.EvalBatchExpr` once per chunk. (2) Collect results into pre-allocated buffers (`rColsBuf`, `rTypesBuf`, `rDataBuf`). (3) Same approach as REQ001557 but extended to batch evaluation. Estimate: ~60 lines, 1 file, 0.5 day. | medium | small | REQ001585 | SQB/WT/writers_dml.go |
| REQ001587 | tests/sqlcmp | **End-to-end benchmarks — vectorized SELECT through SLT.** Verify Phase 1 wins with measurable data. **Add:** (1) `BenchmarkQueryAll_Vectorized_10K` — 10K rows, SELECT with filter + project + aggregate. (2) Compare before/after: `BatchToRowAdapter.Next()` loop vs `NextBatch()` loop. Estimate: ~50 lines, 1 file, 0.25 day. | medium | small | REQ001582, REQ001584 | SQB/EX/vec_queryall_bench_test.go (new) |
| REQ001588 | tests/sqlcmp | **End-to-end benchmarks — vectorized UPDATE/DELETE through SLT.** Verify Phase 2 wins with measurable data. **Add:** (1) `BenchmarkUpdate_VectorizedSET_10K` — 10K rows, UPDATE with 3 SET columns. (2) `BenchmarkDelete_VectorizedFilter_10K` — 10K rows, DELETE with WHERE filter. Estimate: ~50 lines, 1 file, 0.25 day. | medium | small | REQ001585, REQ001586 | SQB/WT/writers_dml_test.go (new benchmarks) |
| REQ001594 | SQB/OP | **HashJoin.nextMatched — fresh per-match Data slice (revert REQ001560).** REQ001560 made `nextMatched` reuse `j.emitBuf` across calls (`outData := j.emitBuf[:j.dataPerRow]`) to skip the per-match allocation. When the consumer (e.g. NLJ block mode draining matches into `blkRightRows`) retained multiple rows across `Next()` calls, every retained row aliased the same backing array and read the *last* match's data. select4 L39784 (8-table join with b4=d6) collapsed 21 rows into 7 triplets, all reading identical (t4, t6) equi-join pair values instead of the 3 distinct pairs (bug hash `7ed8f2ad6ddd1577a494aac6e6b5ea14` vs canonical `34325f84dd0efa600c0be4e8e0770bc3`). **Fix:** allocate `outData := make([]pl.Value, j.dataPerRow)` per match. Add `TestREQ001594_HashJoinNextMatchedDistinct` (3-match unit test that retains across `Next()` calls without intermediate copying) and `TestREQ001594_HashJoinNextMatchedDistinctBackingArrays` (asserts distinct slice backing addresses). Estimate: ~70 lines, 1 file. | high | small | none | SQB/OP/hashjoin.go, tests/sqlcmp/slt/select4_join277_test.go |
