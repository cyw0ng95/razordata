# Future Requirements

Requirements deferred from the active backlog (REQUIREMENTS.md). These are
tracked here to preserve the specification but are intentionally de-prioritized
— either they are premature optimizations for the current architecture, or they
depend on conditions that haven't materialized yet.

## TBD
| ID | Subsystem | Requirement | Priority | Effort | Deps | Touches |
| --- | --- | --- | --- | --- | --- | --- |
| REQ001203 | FIL/IO | **Zero-copy fixed file + buffer I/O — eliminate per-operation kernel↔user data copies.** After REQ001202 wires io_uring as the default backend, the ring uses generic fd-based `READ`/`WRITE` SQEs which still copy data through the kernel buffer cache on every operation. `IORING_OP_READ_FIXED` + `IORING_OP_WRITE_FIXED` with `IOSQE_FIXED_FILE` let the kernel access registered user-space pages directly (via mmu notifier), cutting per-operation copies by half. **Fix:** (1) Add `PrepReadFixed(index int, buf []byte, offset int64)` and `PrepWriteFixed(index int, buf []byte, offset int64)` using `IORING_OP_READ_FIXED`/`IORING_OP_WRITE_FIXED`. (2) Add `Ring.RegisterBuffers(regions) → id` for registered page sets. (3) Replace `uring_sqe.PrepRead(fd, buf, off)` with `PrepReadFixed(index, buf, off)` in `BlockDev` hot path. (4) Replace `uring_sqe.PrepWrite(fd, buf, off)` with `PrepWriteFixed(index, buf, off)`. (5) Add `BlockDev.SetReadBufRegion(addr, size)` / `SetWriteBufRegion(addr, size)` for dynamic buffer registration. (6) Validate with `TestFixedFileReadWrite` (Linux only) verifying that `fixed` reads produce identical results to non-fixed reads. (7) Note: `RegisterFiles`/`UnregisterFiles` already implemented in REQ001209. | medium | small | REQ001202, REQ001209 | FIL/IO/uring_linux.go, FIL/DF/df.go |
| REQ001204 | FIL/IO | **Pre-registered file descriptor table for io_uring — eliminate per-operation fd lookup overhead.** After REQ001202, every `PrepRead()`/`PrepWrite()` passes a raw `int fd` that the kernel must resolve via the file descriptor table. `IORING_REGISTER_FILES` pre-registers an array of fds so SQEs reference them by index (one word) instead of full `struct file *` resolution. **Fix:** (1) `BlockDev` opens all data files at init and registers them in bulk via `Ring.RegisterFiles()`. (2) `BlockDev` exposes `FixedFD(fileID) int` → index. (3) Update `SubmitBatch(count)` to use fixed fds by index with `IOSQE_FIXED_FILE`. (4) Benchmarks: `BenchmarkFixedFDvsRawFD` — expect fd lookup overhead removed (~5-10% latency improvement on dense workloads). (5) Note: `RegisterFiles`/`UnregisterFiles` methods on `Ring` already implemented in REQ001209. | low | small | REQ001202, REQ001209 | FIL/IO/uring_linux.go, FIL/DF/df.go |
| REQ001205 | FIL/IO | **REGISTER_BUFFERS batch I/O — single registration for all data pages, eliminating per-op buffer accounting.** After REQ001203, `READ_FIXED`/`WRITE_FIXED` require each buffer to be individually tracked by the kernel's `user_buf` accounting (kernel pins pages, tracks `mmu` notifiers). `IORING_REGISTER_BUFFERS` pre-registers a pool of `io_uring_buf` entries (addr, len, bgid) so the kernel can batch-track entire page sets with one registration instead of per-SQE accounting. **Fix:** (1) Add `Ring.RegisterBuffers(count int, buffers []io_uring_buf) → error` with `IORING_REGISTER_BUFFERS` ioctl. (2) Add `Ring.UnregisterBuffers()` on `Ring.Close()`. (3) Add `BlockDev.CreateBufferPool(size int) → *BufferPool` that pre-allocates fixed-size pages and registers them via `RegisterBuffers`. (4) `BlockDev.ReadAt()` picks a page from the buffer pool, passes `bufIndex` to `sqe`, uses the page as destination. (5) Add `TestBatchBufferRegistration` verifying that registered buffers produce identical results to per-op buffer allocation. | low | medium | REQ001203 | FIL/IO/uring_linux.go, FIL/DF/df.go |
| REQ001210 | FIL/IO | **Multi-SQE batch I/O — submit multiple SQEs in a single io_uring_enter syscall.** Current `readAt`/`writeAt` each submit 1 SQE + SubmitWait(1) = 1 syscall per block. For compaction reads that read sequential blocks, batching N SQEs with SubmitWait(N) cuts syscall overhead N-fold. **Fix:** (1) Add `BlockDev.ReadBatch(offsets []int64, bufs [][]byte) ([]int, error)` that preps N SQEs via Sqe() + PrepRead, calls SubmitWait(N), drains N CQEs. (2) Same for `WriteBatch`. (3) Wire into compaction read path in `ENG/LS/subcompact.go`. (4) Add `BenchmarkUringBatchRead` vs `BenchmarkUringRead` — expect N=16 batch to achieve near-single-syscall throughput. | low | small | REQ001202 | FIL/DF/df.go, FIL/IO/uring_linux.go, ENG/LS/subcompact.go |
| REQ001211 | FIL/IO | **IOPOLL mode — kernel-side polling for direct I/O paths.** `IORING_SETUP_IOPOLL` makes the kernel poll the block device directly instead of sleeping on interrupts. Requires `O_DIRECT` files and alignment. Useful for WAL segments where O_DIRECT is already desired (REQ001208). **Fix:** (1) Add `Ring.SetupIOPoll(entries int)` with `IORING_SETUP_IOPOLL` flag. (2) Add `BlockDev.OpenDirect(path string) (*BlockDev, error)` that opens with O_DIRECT + IOPOLL ring. (3) Validate that `SubmitWait` returns immediately for completed I/O on IOPOLL rings. (4) Add `TestBlockDev_ReadWriteDirect` verifying correct I/O on O_DIRECT file. | low | medium | REQ001202, REQ001208 | FIL/IO/uring_linux.go, FIL/DF/df.go |
| REQ001212 | FIL/IO | **SQPOLL mode — kernel thread polls SQ ring, eliminating submit-side io_uring_enter syscalls.** `IORING_SETUP_SQPOLL` creates a kernel thread that polls the SQ ring for new submissions every `sqThreadIdle` ms. After submission, userspace skips the `io_uring_enter` syscall on the submit side (completions still need `IORING_ENTER_GETEVENTS` or `SubmitWait`). Best for high-throughput workloads where CPU cost of syscall matters (~100ns saved per op). **Fix:** (1) Add `Ring.SetupSQPoll(entries int, idleMs uint32)` with `IORING_SETUP_SQPOLL` flag and `params.sqThreadIdle`. (2) Validate that `Sqe()` + store submission works without calling Flush/SubmitWait on the submit side. (3) Add `TestSQPoll_RingOps` verifying basic read/write with SQPOLL. (4) Benchmark: `BenchmarkUringSQPoll` vs `BenchmarkUringRead` — expect submit-side latency reduction. | low | medium | REQ001202 | FIL/IO/uring_linux.go |
| REQ001213 | FIL/IO | **IO_LINK chain — chain dependent SQEs (read → write → fsync) in a single submission.** `IOSQE_IO_LINK` flag makes SQEs execute in order: the next SQE starts only after the previous completes successfully. A failed SQE breaks the chain. This eliminates userspace re-arming between dependent operations. **Fix:** (1) Add `s.SetLink(enable bool)` that sets/clears `IOSQE_IO_LINK` on an SQE. (2) Add `BlockDev.ReadModifyWrite(blockID uint64, modify func([]byte) []byte)` that links Read → Modify → Write → Fsync in 4 SQEs submitted together. (3) Verify that linking reduces total wall-clock time vs 4 separate SubmitWait calls. (4) Add `TestIOChain_ReadModifyWrite` comparing linked vs unlinked throughput. | low | small | REQ001202 | FIL/IO/uring_linux.go, FIL/DF/df.go |
| REQ001109 | EX/plan | **No GEQO for large joins — single-start N3 produces suboptimal plans for >8 tables.** PostgreSQL uses GEQO (genetic algorithm) for joins with >12 tables. Razor switches to single-start N3 for >8 tables. For very large joins, N3 may produce suboptimal plans. **Fix:** (1) Implement GEQO with population-based search. (2) Integrate with cost model. (3) Add planning time management. (4) Add `TestGEQO_LargeJoin` verifying GEQO produces better plans than N3 for 15+ table joins. | low | large | REQ001103, REQ001104 | EX/planner.go |
| REQ001159 | build | **Add dependency check (`depcheck`) — codify the current package import graph as the baseline; fail CI on new violations.** No automated enforcement of the subsystem/cluster layering rule today. The dependency report (`docs/compose/reports/dependency-structure-principles.md`, 2026-06) recommends a `tests/depcheck/` Go program that reads package import paths and checks against an allowlist. The allowlist must be derived from the **current** package layout (no `internal/types/` — see REQ001160). **Fix:** (1) Create `tests/depcheck/main.go` (or a `_test.go` so it runs under `go test`) that walks `go list -deps ./...` and asserts each package only imports from layers at-or-below its own. (2) Define the allowlist per ARCH.md §Dependency Order: LOG→FIL→MEM→WAL→ENG→TXN→SQF→SQB→SYS, plus intra-SQB cluster rules from `SQB.md`. (3) Land as a **warning** (not failure) during SQB/WT and SQB/UT cluster fills. (4) Promote to failure after REQ001123 (SQB finalization) ships. (5) Document the rules in `docs/development/DEPENDENCIES.md`. (6) Add depcheck to the CI script. | high | medium | none | tests/depcheck/, CI, docs/development/DEPENDENCIES.md |
| REQ001227 | ENG/LS + SQB/OP | **Memory-mapped reads for LSM SSTables — zero-copy page access via mmap.** pprof: `memmove` = 12.0% CPU (data copying in reads). Current LSM reads allocate new `[]byte` buffers per iterator position. BoltDB uses mmap for entire database (zero allocation for reads); LMDB uses mmap for both reads and writes; RocksDB supports `mmap` mode via `Options.SetUseDirectReads(false)`. **Fix:** (1) Add `mmapFiles bool` option to `SYS/AP.Options` (default false). (2) When enabled, `ENG/LS` mmaps SSTable files at open time. (3) Iterator returns `[]byte` pointing into mmap region (valid until compaction/remap). (4) Use `unix.Mmap`/`unix.Munmap` for Linux/macOS, `windows.MapViewOfFile` for Windows. (5) Invalidation: on compaction, munmap old files, mmap new ones. Readers see consistent snapshot via `madvise(MADV_RANDOM)`. (6) Add `BenchmarkSeqScan_MmapVsBuffer` comparing read throughput. (7) Add `TestMmapRead_Consistency` verifying data integrity after compaction. Estimate: ~200 lines, 5 files, 2 days. | medium | large | none | ENG/LS/sstable.go, ENG/LS/iterator.go, SYS/AP/ap.go, SYS/SY/sy.go |
| REQ001233 | SQB/DT + SQB/OP | **Bump (arena) allocator for intermediate row allocation — reduce GC pressure from per-row heap allocation.** Each `Next()` call in the Volcano pipeline heap-allocates a `DT.Row` struct (Data slice + Cols/Types). With 30 rows × ~5 operators per query × 50 queries = ~7500 allocations per select1 run. A bump allocator pre-allocates a slab and assigns row slots; the entire slab is freed after the query completes. **Fix:** (1) Add `RowArena` to `DT` — pre-allocates a `[]DT.Row` slab (default 1024 rows). (2) `RowArena.Alloc()` returns a pointer into the slab (bump pointer). (3) `RowArena.Reset()` resets the bump pointer for the next query. (4) Thread a `*RowArena` through operator trees (via `ExecContext` or `propagateExecCtx`). (5) All row-producing operators (SeqScan, Filter, Project, Sort, Join) switch from `make([]DT.Value, n)` / `&DT.Row{Data: ...}` to `arena.Alloc()`. (6) Add `BenchmarkSelect1_Queries/star_arena` vs `BenchmarkSelect1_Queries/star` verifying 15-25% reduction in allocs/op. Estimate: ~150 lines, 5 files, 1 day. | medium | medium | none | SQB/DT/row.go, SQB/OP/scan.go, SQB/OP/intermediate.go, SQB/EX/ex.go |
| REQ001234 | SQB/EX | **Expression common subexpression elimination (CSE) across SELECT, WHERE, and ORDER BY — compute shared subexpressions once.** select1 queries frequently compute the same subexpression in multiple clauses (e.g., `a+b*2+c*3` appears in both SELECT and ORDER BY; `CASE WHEN a<b-3 THEN...` appears in multiple query arms). Currently each occurrence is evaluated independently. **Reverted (507babc):** initial implementation at 83c351b caused 14/1031 select1 regressions. Root cause: CSE rewrites WHERE expressions to `__cse*` references at line 2174, but predicate pushdown at line 2243 splits and pushes these rewritten predicates to SeqScan. The Scan operator can't evaluate `__cse*` because the precompute Project (line 2344) hasn't been inserted yet — the columns don't exist at Scan time. Additionally, the rewrite interacts badly with EXISTS decorrelation (REQ001235). **Fix (re-land):** (1) Defer WHERE rewrite to AFTER predicate pushdown — SELECT/ORDER BY rewrites are safe at line 2174 since they don't affect pushdown. (2) Ensure precompute Project is inserted before the Filter but after Scan+pushdown. (3) Verify EXISTS decorrelation still works with rewritten WHERE. (4) Run full select1 corpus (1031 queries, 0 failures target). (5) Add `BenchmarkCSE_Select1` verifying ~10-20% reduction in expression evaluation time. Estimate: ~120 lines, 2 files, 1 day. | low | small | REQ001202 (SlotIdx) | SQB/EX/planner.go, SQB/EX/expr_hash_cse_test.go |
| REQ001238 | tests/sqlcmp/slt | **Reuse engine across corpus files — eliminate per-file engine init/teardown overhead.** The SLT runner (`TestSLT_PerFile`) creates a fresh engine (LSM + WAL + buffer pool + `sql.Open`) for every `.test` file and destroys it after the file completes. With ~622 corpus files, this adds ~10-20ms per file of pure framework overhead (9-18s total dead time). Since each file is self-contained (its own CREATE TABLE), a single engine can be reused across all files by resetting the database schema between files via DROP of all user tables. **Fix:** (1) Move engine creation outside the `t.Run` subtest loop. (2) Create one `RazorDriver` before iterating files. (3) After each file, query `sqlite_master` to enumerate all user tables, DROP them all. (4) The next file's `CREATE TABLE` reinitializes the schema. (5) Guard against test pollution by checking that the engine reports zero user tables before starting each file. (6) Add `BenchmarkSLT_EngineReuse` comparing per-file vs shared-engine wall time. (7) Verify that `select1.test` through `select5.test` all produce identical pass/fail results under shared engine. Estimate: ~60 lines, 2 files, 0.5 day. | high | small | none | tests/sqlcmp/slt/razor_driver.go, tests/sqlcmp/slt/per_file_test.go |
| REQ001334 | SYS/AP | **Public RegisterFunc: scalar UDFs.** Mirror of `sqlite3_create_function` for scalar functions. **Fix:** (1) Add `Engine.RegisterFunc(name string, fn func(args []DT.Value) (DT.Value, error), opts FuncOpts) error`. (2) Store in SYS/AP function registry. (3) In eval, when function name is in user registry, dispatch to it. (4) Validate arg count at registration. (5) Add `TestRegisterFunc_Scalar_Basic`, `TestRegisterFunc_Scalar_NullPropagates`, `TestRegisterFunc_Scalar_ErrorsPropagate`. Estimate: ~120 lines, 2 files, 0.5 day. | high | medium | none | SYS/AP/ap.go, SQB/EV/function_registry.go |
| REQ001335 | SYS/AP | **Public RegisterFunc: aggregate UDFs.** Mirror of `sqlite3_create_function` for aggregate functions (custom `Step`/`Final` callbacks). **Fix:** (1) Extend RegisterFunc to accept `func Step(state, args) error` and `func Final(state) Value`. (2) Store per-aggregation state in the aggregate operator (SQB/AG/aggregate.go). (3) Distinguish from built-in aggregates by name lookup. (4) Add `TestRegisterFunc_Aggregate_SumCustom`, `TestRegisterFunc_Aggregate_DistinctSupport`. Estimate: ~200 lines, 2 files, 1 day. | high | medium | REQ001334 | SYS/AP/ap.go, SQB/AG/aggregate.go |
| REQ001336 | SYS/AP | **Public RegisterFunc: window UDFs.** UDFs usable as window functions. **Fix:** (1) Add a third mode to RegisterFunc: window callback receives the entire partition's pre-aggregated state. (2) Integrate with the window operator (SQB/AG/window.go). (3) Add `TestRegisterFunc_Window_PartitionAware`. Estimate: ~150 lines, 2 files, 0.75 day. | high | medium | REQ001334, REQ001335 | SYS/AP/ap.go, SQB/AG/window.go |
| REQ001337 | SYS/AP | **UDF flags: deterministic, direct-only, commuted.** SQLite's `sqlite3_create_function` accepts flags. deterministic = constant-folded; direct-only = no subqueries; commuted = use opposite arg order for optimization. **Fix:** (1) Add `FuncOpts` struct with these flags. (2) In planner, if `deterministic`, cache results per input (constant-fold). (3) If `directOnly`, reject use in subqueries/CHECK constraints. (4) Add `TestUDF_Deterministic_ConstantFolds`, `TestUDF_DirectOnly_RejectsInSubquery`. Estimate: ~80 lines, 1 file, 0.4 day. | high | small | REQ001334 | SYS/AP/ap.go, SQB/EX/planner.go |
| REQ001332 | SYS/AP | **Public RegisterCollation API in SYS/AP.** SQLite exposes `sqlite3_create_collation_v2()`. Razordata's parser recognizes COLLATE (SQF/PS/select.go:88) but the engine has no public hook. **Fix:** (1) Add `Engine.RegisterCollation(name string, fn func(a, b []byte) int) error`. (2) Store in a per-engine registry. (3) In ORDER BY/COMPARE evaluation, look up collation by name. (4) On close, clear the registry. (5) Add `TestRegisterCollation_CustomOrder`, `TestRegisterCollation_CollateIndexOrderBy`. Estimate: ~80 lines, 2 files, 0.4 day. | high | small | none | SYS/AP/ap.go, SQB/EV/function_registry.go |
| REQ001329 | SYS/DS + FIL/IO | **Multi-process: shared_cache mode.** SQLite's `SQLITE_OPEN_SHAREDCACHE` lets multiple connections in the same process share a single in-memory page cache. Across processes, equivalent to opening the same db file. **Fix:** (1) Add `OpenOptions.SharedCache bool`. (2) When true, acquire a per-DSN RWMutex in SYS/DS; share the engine. (3) Multiple readers, single writer at the engine level. (4) `Engine.Close()` is a no-op for shared instances (last-closer cleans up). (5) Add `TestSharedCache_MultipleSessionsSameEngine`, `TestSharedCache_ConcurrentReaders`. Estimate: ~150 lines, 2 files, 0.75 day. | high | medium | none | SYS/DS/ds.go, SYS/AP/ap.go |
| REQ001330 | SYS/DS + FIL/IO | **Multi-process: POSIX file-locking protocol.** SQLite uses fcntl(F_SETLK) byte-range locks on the db file. Razordata currently has no inter-process locking — only one process can hold the db. **Fix:** (1) At Engine.Open, take a fcntl write lock (or read lock for read-only) on the db file. (2) If lock fails, return ErrLocked. (3) `PRAGMA lock_proxy_file` not needed. (4) `POSIX_FADV_RANDOM` on the fd. (5) Add `TestMultiProcess_SecondOpenErrors`, `TestMultiProcess_FirstCloses_SecondSucceeds`. Estimate: ~150 lines, 2 files, 0.75 day. | high | medium | none | FIL/IO/lock_linux.go (new), SYS/DS/ds.go |
| REQ001331 | SYS/DS + FIL/IO | **Multi-process: SHM mapping for change_counter.** SQLite uses an `*-shm` file for cross-process cache invalidation. A 4-byte change counter at offset 0 of -shm is bumped on every transaction commit; readers reload cache if their snapshot is stale. **Fix:** (1) Open `<db>.razor-shm` with `MAP_SHARED`. (2) On commit, atomic.AddUint32(counter, 1). (3) On read snapshot, check counter; if changed, invalidate caches. (4) Initial size: 32KB reserved (extensible). (5) Add `TestMultiProcess_ChangeCounter_BumpsOnCommit`, `TestMultiProcess_ReaderDetectsStaleCounter`. Estimate: ~200 lines, 2 files, 1 day. | high | medium | REQ001330 | FIL/IO/shm_linux.go (new), TXN/VL/protocol.go |
| REQ001287 | SQB/OP | **DEFERRED — pruneRowCols allocates 10.3% of total heap — buffer reuse causes aliasing.** Naive buffer reuse (Cols/Types/Data/ColIndex) corrupts Rows held by downstream operators that don't clone data. Requires deeper analysis of operator data-flow lifetime before safe reuse is possible. | high | small | none | SQB/OP/operators.go |
| REQ001303 | WAL/RP | **WAL checkpoint modes: passive, full, truncate, restart.** SQLite supports four checkpoint modes. Passive: copy as many frames as possible without blocking writers. Full: wait for all readers to finish, then copy remaining. Truncate: same as Full, then truncate -wal to zero. Restart: same as Full, then rewind wal-index to start. **Fix:** (1) Add `walCheckpoint(mode int)` API. (2) Implement Passive: copy frames up to reader-locked position, do not wait. (3) Implement Full: take ckpt lock, wait for read locks to drain, copy remaining. (4) Implement Truncate: Full + ftruncate(-wal, 0). (5) Implement Restart: Full + reset mxFrame in shm. (6) Surface via `PRAGMA wal_checkpoint(MODE)` query PRAGMA. (7) Add `TestWalCheckpoint_AllModes`. Estimate: ~200 lines, 2 files, 1 day. | high | medium | REQ001299 | WAL/RP/checkpoint.go (new), SQB/EX/planner.go |
